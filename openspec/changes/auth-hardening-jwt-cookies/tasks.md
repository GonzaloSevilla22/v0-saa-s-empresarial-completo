> **Tres applies independientes.** Los grupos 1-10 son la **Parte A** (un PR), 11-16 la **Parte B** (otro PR), 17-23 la **Parte C** (otro PR). Cada parte se mergea, se verifica en prod y recibe humo del PO antes de empezar la siguiente. El orden A → B → C es una restricción de riesgo, no una preferencia: D16 del design explica por qué el token handler no se mergea antes de que las rutas estén cubiertas. El grupo 24 es **operación del PO**, no código.
>
> **Governance CRÍTICO (auth).** El PO firmó el **enfoque** el 2026-09-16. Cada parte necesita su sign-off de merge y su verificación en prod antes de la siguiente.
>
> **TDD estricto en todo el change**: por cada task de implementación, el test que la describe se escribe y **se ve fallar** antes del código. Cada parte arranca con su red de seguridad (correr la suite existente y registrar el baseline); un fallo preexistente se **reporta**, no se arregla acá.
>
> **Regla de integridad de función**: toda función SQL reescrita parte de su cuerpo **vivo de prod** (`pg_get_functiondef`), nunca del último archivo de migración. *Gotcha registrado*: el `md5` local y el de prod difieren por los CRLF del checkout de Windows — comparar por líneas con `\r` removido, no por hash crudo.
>
> **Nunca commitear a `main`**: todo por PR, incluso los fixes triviales post-merge. **Nunca** `apply_migration` del MCP para migraciones de prod. El agente **no hace login en prod** (regla: el Browser pane sobre `/auth/login` de prod cuelga el cliente); los `Set-Cookie` de una sesión real los mide el PO.

## 1. Parte A — Checkpoint de estado real (antes de escribir una línea)

- [x] 1.1 SAFETY NET: correr `pytest backend/tests/` completo y registrar el baseline exacto (pasados/fallados/skips + coverage). Un fallo preexistente se reporta como tal y **no** se arregla en esta parte
- [x] 1.2 Prod (sólo lectura, MCP): confirmar `MAX(version)` y el conteo de migraciones; confirmar que **`20261051000001`** sigue libre (`ls supabase/migrations | tail`). El propose lo verificó el 2026-09-16 sobre `20261050000001` / 299 migraciones
- [x] 1.3 Prod (sólo lectura): capturar el `pg_get_functiondef` **vivo** de `rpc_accept_invitation(text)` y diffear línea a línea (con `\r` removido) contra el cuerpo del último archivo de migración. El propose midió md5 `02517dd9bcbbabd723a5bbb0f1c1a406`, 4853 chars, `prosecdef=true`, **firma única**, `proacl={postgres,authenticated,service_role}` — si algo cambió, el design se corrige **antes** de escribir SQL
- [x] 1.4 Prod (sólo lectura): reconfirmar `SELECT tablename FROM pg_publication_tables WHERE pubname='supabase_realtime'`. El propose midió **una sola fila: `notifications`**; si `fiscal_documents` ya apareciera, el `ALTER PUBLICATION` queda como no-op guardado y se documenta
- [ ] 1.5 Confirmar con el PO el **valor exacto** de `SUPABASE_URL` en Render —el texto literal, no "está puesta"— **antes** de mergear esta parte. El fail-fast de 3.x y el `issuer` de 2.2 dependen de eso, y **una barra final produce 401 en el 100% del tráfico** (hoy es invisible porque la URL de JWKS la tolera). Si no se puede confirmar, el merge espera a la task 24.3
- [x] 1.6 `grep -rn "ws_manager\|routers.ws\|/ws/" backend/ frontend/ --include=*.py --include=*.ts --include=*.tsx` (sin `.venv`/`node_modules`) → inventario cerrado de lo que toca el retiro del grupo 5
- [x] 1.7 Prod (sólo lectura): reconfirmar que `account_invitations.email` sigue **`NOT NULL`** (medido el 2026-09-16). Si hubiera dejado de serlo, el design de D14 se corrige antes de escribir SQL: hoy el change **no** declara ninguna rama de "invitación sin email" porque esa fila no puede existir
- [ ] 1.8 Con el reporte del PO (24.7): confirmar el `iss` y el `aud` de un **token real** de prod —incluido si `aud` llega como string o como array—. El descubrimiento OIDC ya prueba el emisor (`"issuer":"https://gxdhpxvdjjkmxhdkkwyb.supabase.co/auth/v1"`, leído anónimamente el 2026-09-16); el `aud` **no** lo prueba el repo: la spec vigente sólo lo cita como justificación para desactivar la comprobación, que no es evidencia. **Previo a escribir 2.3**

> **Checkpoint 1 medido el 2026-09-16 (apply Parte A, grupos 1-6).**
> **1.1** baseline `pytest backend/tests -q --cov=backend`: **2293 passed, 4 skipped, 0 failed**, coverage TOTAL **98%**, exit 0 (313 s). **Cero fallos preexistentes.**
> **1.2** prod `MAX(version) = 20261050000001`, **299** migraciones; `20261051000001` libre en prod **y** en `supabase/migrations/`.
> **1.3** `rpc_accept_invitation(text)` vivo: md5 `02517dd9bcbbabd723a5bbb0f1c1a406`, 4853 **caracteres**, `prosecdef=true`, **firma única** (1 overload), `proacl={postgres,authenticated,service_role}` — **idéntico** a lo que midió el propose. Diff línea a línea (con `\r` removido) contra `20261050000001:316-435`: **cero diferencias** (120 líneas / 4875 **bytes** cada uno; los 4853 de `length()` son caracteres — el delta es UTF-8 de los comentarios en español, no una divergencia). Cuerpo vivo guardado para el grupo 8.
> **1.4** `pg_publication_tables WHERE pubname='supabase_realtime'` → **una sola fila: `notifications`**. El `ALTER PUBLICATION` de 8.8 sigue siendo un cambio real, no un no-op.
> **1.6** inventario cerrado del retiro de `/ws`: `backend/routers/ws.py`, `backend/core/ws_manager.py`, `backend/tests/test_ws.py`, el import + `include_router` de `backend/main.py:45`/`:143`, y sólo **documentación** fuera del backend (`CLAUDE.md`, `AGENTS.md`, `knowledge-base/08`, `knowledge-base/09`, specs). **Cero referencias en `frontend/`.** Los hits de `wsaa`/`wsfe` son SOAP de AFIP, ajenos.
> **1.7** `account_invitations.email` sigue **`NOT NULL`** (`is_nullable='NO'`) → D14 no necesita rama de "invitación sin email".
> **1.5 NO cerrada** — el valor exacto de `SUPABASE_URL` en Render sólo lo puede reportar el PO (task 24.3). **Bloquea el merge de la Parte A, no el apply**: `rstrip("/")` + el test de 2.2b protegen el caso de la barra final.
> **1.8 parcialmente cerrada, sin el PO.** `iss` **confirmado hoy** por lectura anónima del descubrimiento OIDC: `"issuer":"https://gxdhpxvdjjkmxhdkkwyb.supabase.co/auth/v1"` (y el JWKS sigue con una sola clave EC P-256, kid `cb5c6fc1-0196-4faa-b48c-c9190956381d` — F4 vigente). `aud` **confirmado en su valor** por evidencia directa de prod que el propose no había usado: `SELECT aud, count(*) FROM auth.users GROUP BY aud` → **`authenticated`, 40/40 usuarios** — y esa columna es exactamente la que GoTrue copia al claim, así que es fuente de primera mano, no la justificación circular de la spec. Lo único que **no** está medido es la **forma** (string vs array), que requiere decodificar un token real. Se neutraliza por construcción: 2.3 fija la forma string y **2.3b la forma array**, y PyJWT acepta ambas con el mismo `audience=`, así que ninguna de las dos puede sorprender en prod. Queda para que el PO la confirme en 24.7.

## 2. Parte A — Verificación del JWT: emisor, audiencia, `exp`, `sub`, tolerancia de reloj (D8)

- [x] 2.1 RED→GREEN: `backend/tests/test_auth_jwks.py::test_jwks_es256_token_is_accepted` — clave EC P-256 generada en el test, token firmado con ella, `PyJWKClient.get_signing_key_from_jwt` parcheado para devolverla. **Cierra el hueco de §10 de la auditoría**: hoy `grep -E "jwks|ES256|RS256|PyJWKClient" backend/tests/` da 0 hits y la única rama que corre en prod no tiene tests
- [x] 2.2 RED→GREEN: `test_auth_jwks.py::test_token_from_other_issuer_is_rejected` — mismo token, `iss` ajeno → 401. GREEN: normalizar **una sola vez** (`_base_url = settings.supabase_url.rstrip("/")`) y pasar `issuer=f"{_base_url}/auth/v1"` en `_decode_supabase_jwt` (`backend/core/auth.py:76-92`), usando `_base_url` **también** para la URL de JWKS (`:54`)
- [x] 2.2b RED→GREEN: `::test_trailing_slash_in_supabase_url_does_not_break_issuer` — con `SUPABASE_URL` terminada en `/`, un token legítimo **se acepta**. Sin esta normalización, una barra suelta en Render es un 401 para el 100% del tráfico
- [x] 2.3 RED→GREEN: `test_auth_jwks.py::test_token_with_wrong_audience_is_rejected` + `::test_token_with_expected_audience_is_accepted` — GREEN: `audience="authenticated"` y `verify_aud` **encendido** en las dos ramas (hoy `options={"verify_aud": False}` en `auth.py:83` y `:91`). **Depende de 1.8**: el valor y la forma reales de `aud` los reporta el PO, no los infiere el repo
- [x] 2.3b RED→GREEN: `::test_audience_as_array_is_accepted` — un token cuyo `aud` llega como **array** que contiene el valor esperado se acepta (PyJWT lo admite; el test fija la forma real medida en 1.8)
- [x] 2.4 RED→GREEN: `backend/tests/test_auth.py::test_token_without_exp_is_rejected` — hoy un token válidamente firmado **sin `exp`** se acepta. GREEN: `options={"require": ["exp", "sub"]}`
- [x] 2.5 RED→GREEN: `backend/tests/test_auth.py::test_token_without_sub_returns_401_not_500` — hoy `payload["sub"]` (`auth.py:130`) da KeyError y sale como **500** por el catch-all (`backend/main.py:132-140`). GREEN: cubierto por el `require` de 2.4, con aserción explícita sobre el status
- [x] 2.6 RED→GREEN: `test_auth.py::test_clock_skew_within_leeway_is_accepted` — token con `iat` levemente futuro. GREEN: `leeway=30` (hoy 0 con `verify_iat` activo)
- [x] 2.7 RED→GREEN: `test_auth_jwks.py::test_jwks_fetch_failure_logs_warning_and_returns_401` con `caplog` — hoy `PyJWKClientError` hereda de `PyJWTError` y sale por el mismo `except` (`auth.py:93-94`), indistinguible de un token forjado y sin log propio. GREEN: `except PyJWKClientError` propio con `logger.warning`, antes del `except PyJWTError`
- [x] 2.8 TRIANGULATE: `test_auth_jwks.py::test_forged_token_does_not_log_jwks_warning` — el control negativo de 2.7 (una firma inválida **no** debe emitir esa advertencia)
- [x] 2.4b/2.5b/2.6b RED→GREEN (hallazgo **M1** de la ronda de cierre): los mismos tres controles, **en la rama que corre en producción** — `test_auth_jwks.py::test_es256_token_without_exp_is_rejected`, `::test_es256_token_without_sub_is_rejected`, `::test_es256_clock_skew_within_leeway_is_accepted` + `::test_es256_clock_skew_beyond_leeway_is_still_rejected`. Los casos de 2.4-2.6 vivían **todos** en la rama HS256 (`supabase_url=""`), que D9 vuelve inalcanzable en producción: medido con una sonda de mutación, se podía quitar `require` y poner `leeway=0` en la rama JWKS y la suite entera quedaba verde. **RED probado por mutación**: debilitando sólo la rama JWKS fallan exactamente esos 3 casos (el cuarto es la triangulación de "acotada"); restaurada, 16/16
- [x] 2.9 Verificar que `GET /auth/claims-status` sigue usando la **misma** `_decode_supabase_jwt` (`auth.py:153`, intención declarada en `:63-66`) y que no nació un segundo decoder

## 3. Parte A — Palanca HS256 explícita y fail-fast de arranque (D9)

- [x] 3.1 RED→GREEN: `backend/tests/test_config_auth_failfast.py::test_startup_fails_without_supabase_url_and_without_flag` — GREEN: `auth_allow_hs256_fallback: bool = False` en `backend/core/config.py` y `@model_validator` que rechaza `supabase_url` ausente o no-`https` cuando la palanca está apagada
- [x] 3.2 RED→GREEN: `::test_startup_error_names_the_missing_variable` — el mensaje debe nombrar la variable, no decir "configuración inválida"
- [x] 3.3 RED→GREEN: `::test_hs256_branch_requires_explicit_flag` — con la palanca apagada y `supabase_url=""`, la rama HS256 **no** se ejecuta. GREEN: condicionar el `else` de `auth.py:85-92` a la palanca (hoy la elección es por `startswith("http")`, `:76`)
- [x] 3.4 GREEN: activar la palanca **a nivel de entorno, antes de cualquier import de `backend.*`** — `os.environ.setdefault("AUTH_ALLOW_HS256_FALLBACK", "true")` en el **tope** de `backend/tests/conftest.py`, por encima del bloque de imports (`import jwt`…), **y** `AUTH_ALLOW_HS256_FALLBACK: "true"` en el `env:` de **los dos** workflows que arrancan el proceso: el job de `.github/workflows/Backend_Tests.yml` (que hoy no define **ninguna** variable, `:60-75`) **y** el paso `Start FastAPI backend` de `.github/workflows/E2E_Tests.yml` (que escribe `SUPABASE_URL=$API_URL` del Supabase local, es decir **`http://`** 127.0.0.1:54321, y sin la palanca no levanta — hallazgo **B1** de la revisión adversarial del apply; la regla del proyecto es migrar **todos** los callers al endurecer un contrato). Un fixture **NO** alcanza: `settings = Settings()` corre en el import del módulo (última línea de `backend/core/config.py`) y `conftest.py:104` parchea `backend.core.auth.settings` dentro de un fixture, mucho después. Con los 17 bloques de patch de `test_auth.py` y el `jwt.encode(..., algorithm="HS256")` de `conftest.py:5`/`:21` **sin reescribirse**
- [x] 3.4b RED→GREEN del punto anterior, literal: `pytest backend/tests --collect-only` **desde un entorno limpio** (sin variables) se completa. Sin esta task, el validator de 3.1 deja 0 tests recolectados y `--cov-fail-under=87` nunca se evalúa — CI y local en rojo
- [x] 3.4c RED→GREEN (hallazgo **B1**, ronda adversarial de cierre): `test_config_auth_failfast.py::test_every_workflow_that_boots_the_backend_declares_its_verification_path` — candado que recorre `.github/workflows/*.yml` y falla si un paso que arranca el backend (`backend.main:app`, `pytest backend/tests`) no declara **en el propio archivo** cómo verifica los tokens: palanca encendida o `SUPABASE_URL` literal `https://`. Un valor que llega por `$GITHUB_ENV` **no** cuenta, que es exactamente cómo el `http://` del stack local entraba sin que nadie lo viera. Con su **matriz de evasión ejecutada** (`::test_the_boot_detector_catches_the_ways_of_evading_it`: 5 formas que disparan, 5 que no)
- [x] 3.5 TRIANGULATE: correr la suite entera de backend y confirmar cero regresiones sobre el baseline de 1.1
- [x] 3.6 Verificar que `app_env` (declarado en `config.py:7` y hoy **no leído en ninguna línea de la app**) queda leído al menos en el validator de CORS de 4.x — o documentar por qué sigue sin lectores
- [x] 3.7 GREEN: documentar el arranque local en `backend/.env.example` (o el README del backend) — `uvicorn` sin `SUPABASE_URL` deja de levantar, y la salida es `AUTH_ALLOW_HS256_FALLBACK=true`. Documentar también que un despliegue deliberado sobre `http://` aborta por el mismo validator, y que esa es la salida

## 4. Parte A — CORS por allow-list, sin credenciales (D10)

- [x] 4.1 RED→GREEN: `backend/tests/test_cors_allowlist.py::test_foreign_origin_gets_no_acao` — preflight con `Origin: https://evil.example` → sin `access-control-allow-origin`. Reproduce **F5** medido en prod
- [x] 4.2 RED→GREEN: `::test_production_origin_allowed_with_and_without_www` y `::test_vercel_preview_origin_allowed` — GREEN: `allow_origin_regex` con `^https://(www\.)?aliadata\.com\.ar$` y, **con la forma corregida por el hallazgo M1** (medida anónimamente el 2026-09-16, el slug del equipo va al final y el alias vivo es `…-completo-eie.vercel.app` con 200, mientras `…-completo.vercel.app` da 404 `DEPLOYMENT_NOT_FOUND` y queda fuera), `^https://v0-saa-s-empresarial-completo(-[a-z0-9-]+)?-eie\.vercel\.app$` — en `backend/core/cors.py`, consumido por `backend/main.py` y por `backend/core/errors.py`
- [x] 4.3 RED→GREEN: `::test_localhost_allowed_only_outside_production` — `http://localhost:3000` permitido cuando `app_env != "production"`
- [x] 4.4 RED→GREEN: `::test_allow_credentials_is_false` — GREEN: `allow_credentials=False`. Justificación en el design (D10): cero lecturas de cookie en `backend/`, ningún caller manda `credentials:'include'`
- [x] 4.5 RED→GREEN: `::test_problem_body_does_not_reflect_foreign_origin` — un 404/422 con formato de problema hacia un origen ajeno tampoco lleva encabezados de CORS. GREEN: `backend/core/errors.py:78-85` (`cors_error_headers`) usa el mismo criterio que el middleware, no `allowed == "*"`
- [x] 4.6 RED→GREEN: `test_config_auth_failfast.py::test_wildcard_origin_forbidden_in_production` — con `app_env == "production"` y `backend_allowed_origin == "*"` el arranque falla
- [x] 4.7 GREEN: defaults seguros verificados — con `BACKEND_ALLOWED_ORIGIN` **sin definir** (el estado real de Render hoy), prod sigue funcionando por el regex, no por el comodín

## 5. Parte A — Retiro del canal WebSocket (D11)

- [x] 5.1 RED→GREEN: `backend/tests/test_no_websocket_surface.py::test_openapi_has_no_ws_route` y `::test_no_ws_router_registered` — el candado que impide que el canal vuelva por descuido
- [x] 5.2 GREEN: eliminar `backend/routers/ws.py`, `backend/core/ws_manager.py` y `backend/tests/test_ws.py`; quitar `app.include_router(ws.router)` de `backend/main.py:143` y el import correspondiente
- [x] 5.3 TRIANGULATE: `::test_no_query_param_token_extraction` — grep programático que falla si algún camino del backend extrae un token de un parámetro de consulta (hoy `ws.py:41` lo hace). Cubre el requirement nuevo "El token viaja únicamente por el encabezado de autorización"
- [x] 5.4 Verificar que el retiro no rompe nada: `grep -rn "ws_manager\|routers.ws" backend/` (sin `.venv`) → 0 hits; suite de backend en verde
- [x] 5.5 Registrar en el PR que el segundo decoder de JWT (`ws.py:12-34`, sin rama HS256, sin `iss`, sin `aud`, nunca ejercitado) desaparece con el módulo — queda **un solo** decoder canónico

## 6. Parte A — Re-chequeo en la base para las acciones de configuración (D12)

- [x] 6.1 RED→GREEN: `backend/tests/test_guards_config_recheck.py::test_configuration_guard_queries_db_even_with_claim` — con el claim presente, el guard debe consultar `rpc_my_active_account_roles()`. GREEN: rama en `require_account_role` (`backend/core/guards.py:49-75`) que reconoce el conjunto permitido por **capacidad nombrada**, no por identidad ni por igualdad de lista: las constantes de `backend/core/rbac.py` pasan a `frozenset` y existe un registro explícito (p. ej. `SENSITIVE_CAPABILITIES = frozenset({CAN_CONFIGURE})`) contra el que el guard decide por pertenencia
- [x] 6.1b RED→GREEN: `::test_equal_but_distinct_list_does_not_trigger_the_recheck` — un caller que pasa `["owner","admin"]` literal (igual al contenido de `CAN_CONFIGURE`, pero otro objeto) **no** activa el re-chequeo, y sólo lo activa la capacidad nombrada. Candado contra las dos implementaciones ingenuas: `allowed is CAN_CONFIGURE` se rompe con cualquier copia, `allowed == CAN_CONFIGURE` engancha cualquier literal coincidente
- [x] 6.2 RED→GREEN: `::test_revoked_role_denied_before_token_expiry` — claim que declara el rol, base que ya no lo tiene → 403. Es el hallazgo de staleness de la fila 2 de §8 de la auditoría
- [x] 6.3 TRIANGULATE: `::test_non_configuration_guard_still_short_circuits_on_claim` — el hot path **no** paga la query. Candado contra la alternativa rechazada de OQ-4
- [x] 6.4 TRIANGULATE: `::test_configuration_guard_denies_when_db_returns_empty` — sin roles vigentes, deniega; nunca concede por ausencia de información
- [x] 6.5 GREEN: refrescar el docstring envejecido de `backend/core/guards.py:81` ("no existe custom access token hook" — el hook **sí** copia `profiles.role` a `app_metadata.role`, `20260827000001:151-153`)
- [x] 6.6 Correr la suite de los 4 services que consumen `require_account_role` (`cost_centers`, `payment_methods`, `product_categories`, `account_charges`) y confirmar cero regresiones

## 7. Parte A — Edge Function `invoice-ocr` (D13)

- [x] 7.0 GREEN (previa a 7.1, sin la cual 7.1 es inejecutable): extraer la decisión de autorización + resolución del objeto a descargar a **`supabase/functions/_shared/invoice-ocr-core.ts`** como función pura (recibe la fila validada y el body, devuelve el `storage_path` a usar). Hoy esa lógica vive **inline dentro de `Deno.serve`** (`supabase/functions/invoice-ocr/index.ts:79`, body `:103-107`, `SELECT` `:110-115`, descarga `:130-135`), sin nada que testear
- [x] 7.1 RED→GREEN: `frontend/__tests__/invoice-ocr-core.test.ts` — `storage_path` del body distinto del de la fila → se resuelve el de la **fila**, no el del body. **El molde real del repo es vitest importando un núcleo puro de `_shared/`** (precedente: `frontend/__tests__/ai-estadisticas.test.ts` → `supabase/functions/_shared/ai-estadisticas-core.ts`): no existe **ningún** archivo de test bajo `supabase/functions/` ni paso `deno` en `.github/workflows/`, así que la formulación anterior ("molde de los tests de `supabase/functions/` existentes") apuntaba a algo que no existe
- [x] 7.2 GREEN: sumar `storage_path` al `SELECT` de `invoice_documents` (`supabase/functions/invoice-ocr/index.ts:112-115`) y usarlo en la descarga con el cliente de service role (`:132-135`)
- [x] 7.3 TRIANGULATE: el body sigue aceptándose por compatibilidad (`:104-107`) y el caller existente (`frontend/lib/services/invoiceOcrService.ts:117-119`) sigue funcionando sin cambios
- [x] 7.4 Verificar con `npx deno check` desde una copia **fuera del monorepo** con `DENO_NO_PACKAGE_JSON=1` (gotcha registrado: dentro, Deno escribe `workspaces` en `package.json`). Registrar los errores preexistentes de `ai-insights`/`ai-resumen` como tales, sin arreglarlos acá

> **Grupo 7 aplicado el 2026-09-16.** Núcleo puro `supabase/functions/_shared/invoice-ocr-core.ts` (`parseInvoiceOcrRequest` + `resolveInvoiceDownload`), test `frontend/__tests__/invoice-ocr-core.test.ts` **10/10** (RED real primero: el import del módulo inexistente falló la transformación de vite).
> **Un desvío declarado, más permisivo**: el body ya no **requiere** `storage_path` (antes `400 'Faltan document_id o storage_path'`, ahora `400 'Falta document_id'`). Exigir un campo que dejó de leerse es un footgun; la relajación no puede romper ningún caller (el vivo lo sigue mandando y sigue funcionando, cubierto por test) y habilita retirarlo del caller sin coordinación. El campo **no sale** de `parseInvoiceOcrRequest`, así que el handler no puede usarlo por accidente — hay un test que lo assertea sobre el resultado del parseo.
> **Fail-closed**: fila sin `storage_path` → 404, nunca repliegue al valor del body (test propio).
> **Hallazgo de 7.4**: `const parsed` del request **colisionaba** con el `let parsed` de la respuesta de la IA en el mismo scope — 13 errores de `deno check` que `vitest` no podía ver (el test no importa el handler). Renombrado a `parsedRequest`. Después: `deno check invoice-ocr/index.ts _shared/invoice-ocr-core.ts` → **EXIT=0**.
> **Corrección del supuesto de 7.4**: `ai-insights` y `ai-resumen` **ya no** tienen errores preexistentes de `deno check` (EXIT=0 las dos) — ese candidato lo cerró `candidatos-ia-edge` (PR #529). No hay nada que registrar como preexistente.

## 8. Parte A — Migración `20261051000001` (D14)

- [x] 8.1 RED→GREEN: `supabase/tests/test_accept_invitation_binding.sql` bloque (1) — invitación aceptada por **otra** identidad → rechazo con **`P0404`** y el **mismo texto** que el rechazo por token inválido/vencido (contrato ya vigente, `20261050000001:349-352`; la RPC **lanza**, no devuelve `{error}`), y cero filas nuevas en `account_members` y en el pivot de roles
- [x] 8.2 RED→GREEN: bloque (2) — la identidad invitada acepta con normalidad; bloque (3) — comparación insensible a mayúsculas
- [x] 8.3 RED→GREEN: bloque (4) — **el bloque que decide si esto funciona en producción**: fijar `request.jwt.claims` a **exactamente** `{"sub": "<uuid>", "role": "authenticated"}` —la forma literal que empuja el backend (`backend/core/database.py:116` y `:174`), **sin `email`**— y assertear que la aceptación legítima **igual se completa**. GREEN: el guard resuelve el email con `COALESCE(auth.jwt()->>'email', (SELECT email FROM auth.users WHERE id = auth.uid()))` dentro del cuerpo `SECURITY DEFINER`. Un guard keyeado sólo en el claim rechazaría el 100% de las aceptaciones por FastAPI, y un gate que setea los claims a mano (con `email`) pasaría en verde sin probar nada — este repo ya tiene una policy muerta por ese error (`20260724000001_c31_wsaa_access_tickets.sql:40-41`)
- [x] 8.4 RED→GREEN: bloque (5) — dos aceptaciones concurrentes del mismo token: una gana, la otra se rechaza por invitación ya usada, sin segunda membresía. Requiere el `SELECT … FOR UPDATE` antes de validar
- [x] 8.5 GREEN: escribir la migración `20261051000001` reescribiendo `rpc_accept_invitation(text)` **desde el cuerpo vivo capturado en 1.3**, con `DROP FUNCTION` + `CREATE` (nunca `CREATE OR REPLACE` con firma cambiada — gotcha `42725`) y **re-emisión de ACLs** tras el `DROP`: `GRANT EXECUTE` a `authenticated` y `service_role`, `REVOKE EXECUTE` explícito de **`PUBLIC`** y de `anon`. (Es **PostgreSQL** el que otorga `EXECUTE` a `PUBLIC` por default en toda función nueva, no Supabase: revocar sólo `anon` deja el permiso vivo por la vía de `PUBLIC`.)
- [x] 8.6 RED→GREEN: bloque (6) de integridad de función — `pg_get_functiondef('public.rpc_accept_invitation(text)'::regprocedure)` contiene el guard de email, la resolución contra `auth.users` y el `FOR UPDATE` (molde de `supabase/tests/test_operacion_party_guard.sql:521-561`)
- [x] 8.7 RED→GREEN: bloque (7) — **cero overloads** de `rpc_accept_invitation` y ACLs exactas: sin `EXECUTE` para **`PUBLIC`** ni para `anon`
- [x] 8.7b RED→GREEN: completar la **mitad positiva** del gate de ACLs del hook de claims — hoy `20261048000001:690-694` sólo assertea que `anon`/`authenticated` **no** tengan `EXECUTE` sobre `custom_access_token_hook`, y nadie assertea que `supabase_auth_admin` **sí** lo conserve. Perderlo vacía `app_metadata` en silencio, y D12 vuelve los claims más load-bearing que antes. Es una línea, va en el mismo archivo de gate
- [x] 8.7c RED→GREEN (hallazgo **m3** de la ronda de cierre): la migración re-emite el `COMMENT ON FUNCTION public.rpc_my_active_account_roles()`, que desde D12 afirmaba algo falso — *"Único consumidor: el fallback a DB de `require_account_role` … **cuando ni el claim del conjunto ni el singular viajan en el JWT**"* (`20261048000001:229`), cuando para toda capacidad sensible esa función es el camino **primario**, con claim o sin claim. Candado: bloque **(9)** del gate de invitaciones, asserteado sobre el **COMMENT vivo** (no sobre el archivo: un `COMMENT` posterior lo reescribiría sin que el archivo se entere). **RED medido**: contra la base con la migración vieja aplicada, el bloque (9) falla con EXIT=3 nombrando la afirmación vieja
- [x] 8.8 RED→GREEN: `supabase/tests/test_realtime_publication.sql` — `fiscal_documents` **y** `notifications` en `supabase_realtime`; y la reaplicación del `ALTER PUBLICATION` guardado no falla (idempotencia probada ×2)
- [x] 8.9 GREEN: cablear los dos gates nuevos a `.github/workflows/KPI_Validation.yml` (steps al final de la lista, junto a los 77 existentes) y sumar `20261051000001` a la cadena de reaplicación idempotente
- [x] 8.10 Verificar la migración contra la base local aplicándola **dos veces** consecutivas; **no** correr `supabase db reset` (la base local puede estar compartida con otro worktree)

> **Grupo 8 aplicado el 2026-09-16.** Migración `20261051000001` + dos gates nuevos (`test_accept_invitation_binding.sql` 7/7, `test_realtime_publication.sql` 4/4), los dos cableados como steps **78 y 79** de `KPI_Validation.yml` y la migración sumada a la cadena de reaplicación idempotente.
> **Regla de integridad de función — re-verificada hoy, no heredada**: `pg_get_functiondef` vivo de prod vuelto a capturar por MCP → md5 `02517dd9bcbbabd723a5bbb0f1c1a406`, 4853 chars, `prosecdef=true`, 1 overload, `proacl={postgres,authenticated,service_role}`: **idéntico** a 1.3. La transcripción usada para diffear se validó a sí misma (md5 de la copia local == md5 de prod, 120 líneas), y el diff contra el `CREATE` de la migración da **exactamente** las 4 piezas de D14 (declaración `v_caller_email`, resolución por `COALESCE`, `FOR UPDATE`, guard de binding) más el cambio de encabezado `CREATE OR REPLACE`→`CREATE` y el `;` final. **Cero cambios no intencionados.**
> **Corroboración independiente de 1.3**: el cuerpo pre-migración de la base local, con `\r` removido, es **idéntico** al vivo de prod. El delta de `length()` local-vs-prod (4967 vs 4853) es exactamente **114 caracteres CR** = el gotcha CRLF registrado, no una divergencia.
> **RED probado mecánicamente, no declarado**: corrido contra el cuerpo pre-migración, `test_accept_invitation_binding.sql` falla en el bloque (1) — F13 reproducida en la base local (el intruso canjea la invitación ajena). Tras la migración: 7/7 PASS.
> **8.4 con un desvío declarado**: un solo proceso `psql` no puede abrir dos transacciones concurrentes, así que el bloque (5) prueba la **secuencia serializada** (doble canje → sin 2ª membresía) y la exclusión mutua real la sostiene el bloque (6), que assertea el `FOR UPDATE` en el cuerpo vivo **y su orden** respecto de la primera escritura. La concurrencia literal quedaría para un arnés de dos sesiones que este repo no tiene.
> **Hallazgo real, ajeno al propose — interacción de orden entre gates (corregido acá)**: `test_membership_rpcs_pivot_rewrite.sql` reaplica archivos de migración vía `\i`, y el último de su cadena (`20261050000001`) **redefine `rpc_accept_invitation`**, revirtiendo el binding de D14 para el resto de la corrida de CI → el gate nuevo (step 78, posterior) fallaba su bloque (1). **Medido corriendo los 79 gates en el orden real del workflow, no previsto por el design.** Cerrado con el eslabón **13b-reconverge-3** (`\i` de `20261051000001` al final de esa cadena), que es la convención ya establecida en ese archivo: tiene dos reconverges previos por exactamente el mismo mecanismo (Parte C y `20261050000001`). Toda migración que redefina esa función debe sumar el suyo.
> **Defecto del gate propio, encontrado al correrlo y corregido**: el centinela "la RPC no rechazó" estaba **dentro** del bloque que lo cachea con `EXCEPTION WHEN OTHERS`, así que se auto-tragaba y reportaba un **guard ausente** como "esperaba P0404, dio P0001" — encuadraba el fallo como un problema de mapeo de ERRCODE y mandaba a leer el lugar equivocado. Resuelto con un flag (`v_not_rejected`) en los bloques (1) y (5); el gate falla igual, pero ahora dice qué pasó.
> **8.10** migración reaplicada **dos veces** consecutivas: EXIT=0 las dos, 1 sola definición, ACLs exactas, `fiscal_documents` una sola vez en la publicación, guard intacto y el gate de binding 7/7 después. `npx supabase db reset` en el worktree: **limpio**, con `20261051000001` aplicada última (`MAX(version)` = `20261051000001`).
> **Estado de prod reconfirmado hoy** (sólo lectura): `pg_publication_tables WHERE pubname='supabase_realtime'` → **una sola fila, `notifications`** — el `ALTER PUBLICATION` sigue siendo un cambio real.

## 9. Parte A — Documentación y configuración reconciliadas (D15)

- [x] 9.1 `openspec/specs/backend-auth/spec.md:11` — la línea normativa "SHALL … `HS256`" se reconcilia al archivar vía el delta de este change; verificar que el delta cubre el texto vigente y no deja la afirmación vieja en pie
- [x] 9.2 `knowledge-base/08_arquitectura_propuesta.md` — `:238`/`:310` documentan HS256 sin mencionar `SUPABASE_URL` (la variable que **elige la rama**); `:132` dice que el service role es "solo Edge Functions" cuando `backend/services/payments.py:117-126` lo usa
- [x] 9.3 `knowledge-base/02_descripcion_general.md` y `knowledge-base/09_decisiones_y_supuestos.md` (DEC-16) — retirar la mención del canal WebSocket propio como superficie viva del backend
- [x] 9.4 `backend/core/config.py:11-12` — mover/documentar `supabase_url` como variable de **auth**, no bajo el header de pagos
- [x] 9.5 `CLAUDE.md` — retirar la nota de divergencia `python-jose` vs `PyJWT` (ambos manifiestos declaran sólo `PyJWT[crypto]`: `backend/requirements.txt:3`, `backend/pyproject.toml:8`) y la mención de `ws.py`/`ws_manager` en la superficie del backend; correr `python scripts/ci/check_docs_sync.py --fix` **en el mismo PR** (el gate `Docs Sync` lo verifica)
- [x] 9.6 `supabase/config.toml:396`/`:407`/`:418`/`:429`/`:449` — los cinco `verify_jwt = true` pasan a `false` con un comentario explicando que el deploy pasa `--no-verify-jwt` a toda la flota (`.github/workflows/deploy.yml:63`) y que cada función autentica en código, fail-closed
- [x] 9.7 Registrar la ficha de la Parte A en `CHANGES.md`

> **Grupo 9 aplicado el 2026-09-16.**
> **9.1** el delta de `backend-auth` es un `MODIFIED Requirements` cuyo header (`### Requirement: Validación de JWT de Supabase`) **coincide exactamente** con el vigente, así que al archivar reemplaza el bloque completo — se va la línea `SHALL … HS256` **y** su "Nota de cobertura", sin dejar la afirmación vieja en pie. `openspec validate --strict --changes` → **1 passed, 0 failed**.
> **9.2** `knowledge-base/08`: el service role deja de ser "solo Edge Functions" (verificado: `backend/services/payments.py:117-126` usa `settings.service_role_key` para el webhook de MP, que llega sin sesión de usuario); el párrafo del scaffolding declara que el WebSocket se retiró y que `HS256` dejó de ser el default; y la lista de variables de entorno ahora **encabeza con `SUPABASE_URL`** explicando que es la que elige la rama y fija el `issuer`.
> **9.3 con la premisa corregida**: `knowledge-base/02` **no mencionaba** el canal WebSocket propio (0 hits de `ws`/`websocket`/`room_id`) — lo que sí tenía era una fila que afirmaba "Realtime (Supabase) | **No usado en MVP actual**", falsa desde antes y **contradicha por el grupo 8 de este mismo change**; corregida al estado real (publicación con `notifications` + `fiscal_documents`, alcance por RLS). En `knowledge-base/09`, DEC-16 pasa de "WebSocket Python **reservado**" a **RETIRADO** con el porqué (token en la query string, sin consumidor), y el "Estado" de DEC-12 deja de listar la migración de realtime a WS como pendiente: quedó **sin objeto**.
> **9.4 ya estaba hecho** por el grupo 3: `supabase_url` vive bajo el header `── Auth — verificación del JWT ──` con la nota de que "vivía documentada bajo el header de *pagos*, que escondía justamente eso (D15)". Se verificó, no se duplicó.
> **9.5** la nota de divergencia `python-jose` vs `PyJWT` era **falsa en sus dos afirmaciones** y se retira: ambos manifiestos declaran sólo `PyJWT[crypto]` (`requirements.txt:3`, `pyproject.toml:8`) y `grep` da **0** imports de `jose` en `backend/` fuera de `.venv` (`conftest.py` usa `import jwt`). En su lugar queda una nota que sí importa: `SUPABASE_URL` es variable de **auth**. La superficie del backend se corrige a la **medida hoy** — 28 routers / 27 services / 33 repositories, `core/` sin `ws_manager` y con `cors`/`rbac` (la línea decía 26/26/30 y seguía listando `ws_manager`). `check_docs_sync.py --fix` corrido: AGENTS.md regenerado y verificado en verde.
> **9.2/9.3 completadas en la ronda de cierre (hallazgos M2 y m4)**: el grupo 9 había editado **una** de las cuatro afirmaciones de `knowledge-base/08` y las otras tres seguían describiendo el WebSocket como código vivo *en el mismo PR que lo borra* — `:252` ("queda como **infra lista para el futuro**"), `:303` ("ya está scaffoldeado pero **NO se usa en producción**: queda reservado") y la enumeración de `:240`, que en la misma oración decía que el canal se retiró y listaba "migración de realtime a WebSocket" como **pendiente**. Reescritas al estado real (canal retirado con su candado, tiempo real = Supabase Realtime sobre `notifications` + `fiscal_documents` con alcance por RLS, migración **sin objeto**). Re-grep de `ws_manager|routers/ws|/ws/{room_id}` sobre `knowledge-base/`, `CLAUDE.md` y `AGENTS.md`: **0 afirmaciones vivas**; lo que queda está en las specs **principales**, que es exactamente lo que los deltas de este change reemplazan al archivar.
> **9.6** los **cinco** `verify_jwt = true` → `false`, con una nota de bloque sobre `[functions.ai-insights]`. Antes de escribir "cada función autentica en código, fail-closed", se **verificó función por función**: las cinco construyen el cliente con el `Authorization` del request, llaman `auth.getUser()` y responden 401 si no resuelve usuario. `send-email` ya estaba en `false` y se deja (la invoca un DB webhook, sin sesión).
> **9.7** ficha de la Parte A en `CHANGES.md`, al final del bloque cronológico y antes de `## Post-roadmap V2.x`, marcada **🔨 apply aplicado, SIN mergear** con los dos bloqueantes de merge (valor de `SUPABASE_URL` en Render, forma de `aud`) explícitos.

## 10. Parte A — Verificación y cierre

- [x] 10.1 `pytest backend/tests/` completo: cero regresiones sobre el baseline de 1.1, coverage ≥ el umbral de CI
- [x] 10.2 Los 2 gates SQL nuevos + `test_function_acl_gate.sql` + `test_errcode_5char_gate.sql` en verde
- [x] 10.3 Declarado: **sin superficie frontend de usuario** en esta parte — `git diff --stat` no debe tocar ningún archivo bajo `frontend/` **con una única excepción admitida: `frontend/__tests__/**`**, donde vive el test del núcleo puro de `invoice-ocr` (7.1), porque ése es el único arnés de Edge Functions que este repo tiene. Cualquier otro archivo bajo `frontend/` es un desvío que se declara o se revierte
- [x] 10.4 Ronda de revisión adversarial pre-merge; aplicar findings y re-verificar
- [ ] 10.5 **Verificación post-merge en prod** (sólo lectura): `MAX(version) = 20261051000001`; **una sola** definición de `rpc_accept_invitation` sin overload y con el guard en el cuerpo vivo; `anon` sin `EXECUTE`; `pg_publication_tables` con `notifications` **y** `fiscal_documents`; `GET /openapi.json` del backend sin ninguna ruta `/ws`; preflight con `Origin: https://evil.example` → **sin** `access-control-allow-origin` (hoy lo refleja, F5); preflight con `Origin: http://localhost:3000` → **sin** `access-control-allow-origin` (es la mitad del control de D10 que depende de que `APP_ENV=production` esté realmente puesta — ver 24.3); preflight con `Origin: https://v0-saa-s-empresarial-completo-eie.vercel.app` (el alias **vivo** del proyecto, 200 medido el 2026-09-16) → **sí** lo recibe; una llamada real autenticada sigue respondiendo 200 (el `verify_aud`/`iss` nuevo no rompió nada)
- [ ] 10.6 Sign-off del PO para la Parte A antes de empezar la Parte B

> **Grupo 10 cerrado el 2026-09-16** (10.1-10.4; 10.5 y 10.6 quedan por construcción para después del merge).
> **10.1** `pytest backend/tests -q --cov=backend` → **2363 passed, 4 skipped, 0 failed**, coverage TOTAL **98%**, EXIT=0. Con el comando **exacto** del gate de CI (`-m "not integration" --cov-config=backend/pyproject.toml --cov-fail-under=87`) → **2361 passed, 1 skipped, 5 deselected**, **93,45%**, EXIT=0. Baseline de 1.1: 2293/4/0 → **cero regresiones y cero fallos preexistentes** (los grupos 1-6 sumaron 70 tests).
> **10.2** los 2 gates nuevos + `test_function_acl_gate.sql` + `test_errcode_5char_gate.sql` verdes, y además **los 79 gates de `KPI_Validation.yml` corridos en el ORDEN REAL del workflow: 79/79 PASS**, sobre `npx supabase db reset` limpio (`MAX(version) = 20261051000001`). Migración reaplicada **×2** tras eso: EXIT=0 las dos, 1 definición, ACLs exactas `{postgres,authenticated,service_role}`, `fiscal_documents` una sola vez en la publicación, cláusulas vivas, y los dos gates nuevos verdes después de las reaplicaciones.
> **Nota de método que vale más que el número**: correr los gates con `docker exec … < archivo` (stdin) hacía fallar 2 de 79 — `test_membership_rpcs_pivot_rewrite.sql` y `test_cuentas_billetera_tipo.sql` —, y **el segundo es el "fallo local conocido" que el proyecto arrastra registrado como artefacto**. No era ni artefacto irreducible ni hallazgo: los dos usan `\i` con rutas **relativas al cwd del proceso psql**, y por stdin el cwd es el del contenedor. Corriendo como corre CI (`-f` con el cwd en la raíz del repo) **los dos pasan**. Un gate que falla en local por método de invocación esconde interacciones reales — y de hecho escondía una (ver abajo).
> **10.3** `git diff --stat main...HEAD -- frontend/` → **un solo archivo**, `frontend/__tests__/invoice-ocr-core.test.ts` (+134), que es exactamente la única excepción que la task admite. `tsc --noEmit`: **9 errores, el baseline exacto registrado del repo (9=9)**, ninguno en el archivo nuevo. `pnpm vitest run __tests__/invoice-ocr-core.test.ts` → **10/10**. `next-env.d.ts` sin ensuciar.
> **10.4 — ronda adversarial, con un hallazgo real de severidad alta encontrado y cerrado**: el guard de D14 degradaba **FAIL-OPEN**. `IF v_caller_email IS NULL OR lower(v_inv.email) <> lower(v_caller_email)` cubría el NULL del aceptante pero **no el de la invitación**: con `email` NULL, `lower(NULL) <> lower(x)` da NULL, la condición del IF queda NULL, la rama del rechazo no se toma y **un desconocido entra**. **Medido, no razonado**: aflojando el NOT NULL en la base local, `rpc_accept_invitation` le devolvió `{"role":"member","roles":["viewer"]}` a un usuario ajeno al email invitado. El caso es **inalcanzable hoy** (`account_invitations.email` es NOT NULL en prod, re-verificado una tercera vez acá) y por eso la versión anterior del archivo había decidido explícitamente no escribir la rama — razonando bien sobre el caso y mal sobre lo que estaba en juego, que era la **dirección de la degradación** ante un cambio de schema futuro. Cerrado con `v_inv.email IS NULL` explícito, la cabecera y el `COMMENT` corregidos, la aserción estructural nueva **(6-fail-closed)** y el bloque de conducta **(8)** que fabrica la fila imposible aflojando el constraint en un DO propio que lo restaura (y que rollea atrás solo si algo falla — verificado en carne propia). **RED→GREEN probado**: contra el cuerpo sin la cláusula el gate falla en (6-fail-closed) con EXIT=3; con la cláusula, 7/7 + PASS (8). El gate quedó además probado **re-ejecutable** (dos corridas consecutivas verdes), y su bloque (8) tuvo su propio bug de cleanup encontrado al correrlo: borraba sólo la cuenta del owner y `accounts_owner_user_id_fkey` impedía borrar al desconocido de `auth.users`, porque `handle_new_user` aprovisiona una cuenta **por cada** usuario.
> **10.5 / 10.6 no se pueden cerrar desde el apply**: la verificación post-merge en prod exige que la Parte A esté mergeada y desplegada, y el sign-off es del PO. **Pendiente de merge además de eso**: la task **1.5** (valor exacto de `SUPABASE_URL` en Render, que sólo reporta el PO) y la **1.8** (forma de `aud` en un token real; el valor ya está probado con 40/40 de `auth.users` y las dos formas están cubiertas por tests).

## 11. Parte B — Checkpoint y red de seguridad

- [x] 11.1 SAFETY NET: `pnpm vitest run` completo en `frontend/` y registrar el baseline exacto. El flaky preexistente de `AdminSegurosPage.test.tsx` bajo carga está documentado — se reporta, no se arregla. **Registrar explícitamente el estado de las tres suites que esta parte va a romper si se las ignora**: `frontend/__tests__/idle-logout.test.ts` (mockea `@/lib/cookies` como `{ COOKIE_KEYS: { TENANT }, deleteCookie }`, `:25-28` — en cuanto `performIdleLogout` importe `clearAuthUxCookies`, ese mock devuelve `undefined` y el archivo muere con un `TypeError` que **no** es el RED buscado), `frontend/__tests__/lib/python-client.test.ts` (`:8-16`, mockea `getSession` y assertea la forma del header) y `frontend/__tests__/idle-server-enforcement.test.ts:176` (importa `PROTECTED_PREFIXES`)
- [x] 11.2 SAFETY NET: `pnpm tsc --noEmit` y registrar el estado (gotcha: `next-env.d.ts` queda sucio tras un `next dev` previo)
- [x] 11.3 Medir el estado de hoy que la parte corrige: `curl` anónimo sobre los 12 árboles sin gate (`/caja`, `/cobranzas`, `/banco`, `/estadisticas`, `/exportaciones`, `/facturacion`, `/finanzas`, `/organizacion`, `/planes`, `/rentabilidad`, `/reportes`, `/sucursales`) → registrar cuáles devuelven 200 hoy, para el antes/después de 16.4
- [ ] 11.4 Confirmar que la Parte A está mergeada y verificada en prod (11.x no arranca antes)

## 12. Parte B — Cobertura de rutas por construcción (D4)

- [x] 12.1 RED: `frontend/__tests__/lib/protected-routes-coverage.test.ts` — lee `frontend/app/(dashboard)/` del filesystem, ignora directorios que empiezan con `_` y los grupos de ruta `(…)`, y falla si algún árbol no queda cubierto. **Debe fallar hoy nombrando los 12** de 11.3. El test recorre **sólo** `app/(dashboard)`: declara en un comentario que `app/dev-harness/**` queda **fuera de su alcance** a propósito (OQ-8) y no aparenta cubrirlo
- [x] 12.2 GREEN: invertir `PROTECTED_PREFIXES` (`frontend/lib/supabase/middleware.ts:57-61`) a una **allow-list de rutas públicas** + protección por defecto de todo `app/(dashboard)`
- [x] 12.3 TRIANGULATE: `::test_auth_routes_are_never_protected` — `/auth/*` queda fuera del conjunto protegido (razón documentada en `middleware.ts:129-130`: gatear `/auth/callback` produce un loop)
- [x] 12.4 TRIANGULATE: `::test_a_new_route_tree_without_coverage_fails` — crear un directorio temporal en el fixture y ver el test fallar. Sin esto, el candado no está probado
- [x] 12.5 Verificar que `frontend/__tests__/lib/protected-prefixes-proveedores.test.ts` (que hoy assertea **un** prefijo) sigue en verde o se absorbe en el test nuevo, sin perder la aserción
- [x] 12.6 Verificar que `frontend/__tests__/idle-server-enforcement.test.ts:176` (que importa `PROTECTED_PREFIXES`) sigue en verde tras el cambio de forma
- [x] 12.7 RED→GREEN: `::test_api_routes_are_never_redirect_gated` — una petición sin sesión a `frontend/app/api/**` (incluido el futuro `/api/auth/token`) **no** recibe 307 hacia el login. GREEN: excluir `/api` del redirect en el middleware; cada handler responde 401 JSON por su cuenta, como ya hacen los tres existentes (`api/ai/copilot/route.ts:77`, `api/billing/cancel/route.ts:22`, `api/billing/preferences/route.ts:28`). Sin esta task, la lectura natural de "allow-list pública + protegido por defecto" **bloquea el propio token handler** de la Parte C, que recibiría HTML donde espera JSON
- [x] 12.8 GREEN: declarar `/dev-harness` en la allow-list pública **con comentario** (OQ-8): vive fuera de `app/(dashboard)`, ya se auto-gatea con `notFound()` en producción (p. ej. `frontend/app/dev-harness/shell/page.tsx:7`) y es lo que ejercitan los cinco specs de `frontend/e2e/harness/`. Verificar que esos cinco specs siguen en verde

## 13. Parte B — `safeNext()` compartido, redirect al login real, cookies en redirects (D5)

- [x] 13.1 RED→GREEN: `frontend/__tests__/lib/safe-next.test.ts` — destino externo (`@evil.example/`, `//evil.example`, `\\evil.example`, `https://…`) resuelve a la ruta principal; destino interno se conserva. GREEN: extraer el helper desde la validación que hoy vive sólo en `middleware.ts:186`
- [x] 13.2 RED→GREEN: `frontend/__tests__/app/auth-callback-next.test.ts` — el manejador de `/auth/callback` (`route.ts:9`, `:43`, hoy concatena sin validar) aplica `safeNext()`. Cierra el open redirect latente
- [x] 13.3 RED→GREEN: `frontend/__tests__/lib/no-login-literal.test.ts` — falla si aparece el literal `"/login"` como destino de redirect en el árbol de páginas. **Debe fallar hoy** nombrando los 4 de F2
- [x] 13.4 GREEN: los cuatro `redirect("/login")` pasan a `redirect("/auth/login?next=…")` — `frontend/app/(dashboard)/planes/page.tsx:30`, `planes/success/page.tsx:27`, `facturacion/page.tsx:78`, `ventas/ordenes/[id]/page.tsx:60`
- [x] 13.5 RED→GREEN: `frontend/__tests__/lib/middleware-redirect-cookies.test.ts` — un redirect **no destructivo** del middleware conserva las cookies de sesión escritas por `setAll` (`middleware.ts:77-82`). GREEN: copiarlas **sólo** en las tres salidas que no cierran la sesión — email no confirmado (`:119-123`), bounce de admin (`:168-179`) y autenticado en ruta de auth (`:183-189`). La salida `!user` (`:106-111`) no tiene nada que copiar
- [x] 13.6 RED→GREEN (**test negativo, obligatorio**): `::destructive_redirects_emit_no_session_cookie` — la purga de sesión muerta (`middleware.ts:96-102`) y el corte por inactividad (`:141-152`) **no** emiten ningún `Set-Cookie` de `sb-*` que no sea un borrado. Las dos ramas existen para **borrar** todo `sb-*`: copiarles encima las cookies recién escritas las repone y desactiva en silencio la recuperación de "Refresh Token Not Found" y el idle de servidor que D6 está reforzando en esta misma parte

## 14. Parte B — Cierre de sesión uniforme y recuperación del 401 (D6, D7)

- [x] 14.1 RED→GREEN: `frontend/__tests__/lib/clear-auth-ux-cookies.test.ts` — el helper compartido borra `auth:last-activity` **y** `tenant:active`. GREEN: crearlo en `frontend/lib/cookies.ts` y consumirlo desde los tres caminos
- [x] 14.2 RED→GREEN: **extender `frontend/__tests__/idle-logout.test.ts`** (el archivo **ya existe**; NO crear `lib/idle-logout.test.ts`, que lo sombrearía) con `::clears_last_activity_cookie` — hoy `performIdleLogout` borra sólo `tenant:active` (`frontend/lib/auth/idle-logout.ts:50`). **Primero** actualizar su mock de `@/lib/cookies` (`:25-28`, hoy `{ COOKIE_KEYS: { TENANT }, deleteCookie }`) para que exponga `clearAuthUxCookies`: sin eso el archivo muere con un `TypeError` que no es el RED buscado
- [x] 14.3 RED→GREEN: `::uses_local_scope` — `signOut({ scope: 'local' })` en `logout()` (`frontend/contexts/auth-context.tsx:295`) y en `performIdleLogout` (`idle-logout.ts:43`); `closeAllSessions()` (`auth-context.tsx:344`) **conserva** `'global'`. Hoy el `signOut()` pelado es global por default de la librería
- [x] 14.4 RED→GREEN: `::close_all_sessions_clears_cookies_too` — hoy `closeAllSessions` no borra ninguna (`auth-context.tsx:341-347`)
- [x] 14.5 RED→GREEN: `frontend/__tests__/lib/middleware-idle-revokes.test.ts` — la rama idle del middleware (`middleware.ts:131-165`) revoca contra el proveedor **antes** de borrar cookies. Hoy no hay un solo `signOut` en ese archivo
- [x] 14.6 RED→GREEN: `frontend/__tests__/lib/relogin-after-idle.test.ts` — re-login inmediato tras un cierre por inactividad **no** rebota. Es el bounce del primer intento descrito en la fila 5 de §8
- [x] 14.7 RED→GREEN: **extender `frontend/__tests__/lib/python-client.test.ts`** (el archivo **ya existe** y mockea `getSession` + assertea la forma del header, `:8-16`; NO crear `python-client-401.test.ts` en paralelo) con `::401_without_session_navigates_to_login` — un 401 con sesión ausente navega a `/auth/login?reason=expired&next=…`; con sesión viva conserva el error actual. GREEN: `frontend/lib/api/python-client.ts:26-30`
- [x] 14.8 RED→GREEN: `::omits_authorization_header_when_token_is_empty` sobre los tres transportes que hoy mandan `Bearer ` vacío — `frontend/lib/api/subscriptions-client.ts:47-57`, `frontend/components/ventas/sale-receipt-button.tsx:132-140` y `frontend/app/(dashboard)/admin/pagos/page.tsx:146-152` — más el mismo tratamiento del 401 de 14.7. GREEN incluye el arreglo de una línea que la auditoría marca en §5: `python-client.ts:54` mergea `extraHeaders` **después** de los headers de auth, así que un caller puede sobrescribir `Authorization`; se invierte el orden, con su aserción
- [x] 14.9 RED→GREEN: `frontend/__tests__/components/app-sidebar-logout.test.tsx::awaits_logout_before_navigating` — el botón del sidebar espera el `logout()` antes de navegar (`frontend/components/app-sidebar.tsx:353-355` hoy lanza `logout()` y en la línea siguiente fuerza `window.location.href`), con `.catch()` en el call site
- [x] 14.10 RED→GREEN: `frontend/__tests__/app/LoginPage-reason.test.tsx::explains_expired_reason` — `frontend/app/auth/login/page.tsx:32-33` hoy sólo contempla `reason === "idle"`, así que el `reason=expired` que introduce 14.7 llegaría **sin ningún mensaje**. GREEN: renderizar el aviso de sesión vencida, con el mismo mecanismo del aviso de inactividad. Es superficie visible al usuario: entra por la regla del PO del 2026-08-02

## 15. Parte B — Atributo `Secure` y comentarios que mienten

- [x] 15.1 RED→GREEN: `frontend/__tests__/lib/cookie-options.test.ts` — el objeto compartido emite `secure: true` cuando el entorno es producción y `false` fuera. GREEN: crear `frontend/lib/supabase/cookie-options.ts`
- [x] 15.2 GREEN: consumirlo desde los cuatro sitios que construyen cliente — `frontend/lib/supabase/client.ts:4`, `server.ts:6`, `middleware.ts:69`, `frontend/app/auth/callback/route.ts:13`. **En esta parte `httpOnly` sigue en `false`**: cambiarlo rompe el Bearer de FastAPI hasta que exista el token handler (D16)
- [x] 15.3 TRIANGULATE: `::test_all_four_call_sites_share_the_options` — grep programático que falla si un sitio declara sus propios atributos
- [x] 15.3b RED→GREEN: `frontend/__tests__/lib/cookie-attributes-emitted.test.ts` — **assertear el `Set-Cookie` realmente emitido**, no el objeto de opciones: manejar `createServerClient` con un almacén de cookies falso y verificar los atributos que llegan a la respuesta (`Path=/`, `SameSite=Lax`, `Secure` en producción). §10 de la auditoría pide exactamente eso; un test que sólo mira la constante pasa igual si un call site se olvida de pasarla
- [x] 15.4 GREEN: corregir los comentarios falsos — `frontend/lib/cookies.ts:5` ("tokens stay in Supabase httpOnly cookies"), `frontend/lib/api/python-client.ts:14` ("reads from local storage"), `frontend/lib/auth/idle-logout.ts:25` y `:42` ("local scope", que recién ahora será cierto)

## 16. Parte B — Verificación y cierre

- [x] 16.1 `pnpm vitest run` completo: cero regresiones sobre el baseline de 11.1
- [x] 16.2 `pnpm tsc --noEmit`: sin errores nuevos sobre 11.2
- [ ] 16.3 Humo en el stack local: login → dashboard → navegar a una de las 12 rutas antes descubiertas → cerrar sesión → verificar que una segunda pestaña abierta se entera; y que cerrar sesión en el navegador A **no** desloguea al navegador B
- [ ] 16.4 **Verificación post-merge en prod**: `curl` anónimo sobre los 12 árboles de 11.3 → **307** a `/auth/login?next=…`; `curl` anónimo sobre `/planes` → 307 sin `/login` en el stream
- [x] 16.5 Ronda de revisión adversarial pre-merge; aplicar findings y re-verificar
- [x] 16.5b Hallazgos de la ronda del 2026-09-16 (2 bloqueantes, 1 mayor, 6 menores) aplicados con TDD: `safeNext()` rechaza caracteres de control + `resolveSafeRedirect()` comprueba el origen resuelto (BLOCKER 1, regresión introducida por 13.x); el formulario de login consume `safeNext()` (BLOCKER 2, tercer consumidor); candado simétrico de árboles públicos (MAJOR 1); assets en la allow-list; guarda de producción del arnés verificada; el 401 distingue sesión ausente de estado indeterminado y reconoce la renovación; la revocación fallida de la rama idle deja de ser silenciosa. **Rechazados** con su razón en `CHANGES.md`: el orden A → B (la 11.4 sigue abierta, no es del agente de la parte) y `.claude/settings.local.json` (configuración del entorno, no del change: no se commitea)
- [ ] 16.6 Registrar la ficha de la Parte B en `CHANGES.md`; sign-off del PO antes de empezar la Parte C

## 17. Parte C — Checkpoint y red de seguridad

- [ ] 17.1 SAFETY NET: `pnpm vitest run` y `pnpm tsc --noEmit`, baseline registrado. La Parte C parte de `main` con la Parte B ya mergeada (nunca en paralelo: las dos tocan `middleware.ts`)
- [ ] 17.2 Inventario cerrado y congelado de los **43 archivos** con `supabase.auth.*` en el navegador (getUser ×30, getSession ×16, signOut ×4, updateUser ×3, onAuthStateChange ×2, signUp/signInWithPassword/signInWithOtp/resetPasswordForEmail/resend/refreshSession/exchangeCodeForSession ×1) y de los **69** que importan `@/lib/supabase/client`, cada uno con su destino
- [ ] 17.2b Inventario cerrado de los **43 archivos de test** bajo `frontend/__tests__` que mockean `@/lib/supabase/client`, casi todos con `vi.mock(... => ({ createClient: () => ({ auth: {...} }) }))` — p. ej. `auth-context.test.tsx:27`, `auth-context-membership.test.tsx:106`, `ForgotPasswordPage.test.tsx:40` (`resetPasswordForEmail`), `idle-logout.test.ts:21` (`signOut`), `lib/python-client.test.ts:8-16` (`getSession`), `export-button.test.tsx:35`, `ExportacionesPage.test.tsx:58`, `use-statistics-ai.test.tsx:23`, `PriceSuggestionModal.test.tsx:23`. **Cada uno con su destino.** Después de la Parte C el cliente real **no tiene `auth`** (cualquier acceso lanza, `index.mjs:389`): esos dobles codifican un contrato que dejó de existir, así que la suite puede quedar verde mientras producción se rompe. El inventario de 17.2 cuenta sólo call sites de producción
- [ ] 17.3 Confirmar en `node_modules` las capacidades que el diseño asume: `createClient(url, key, { accessToken })` en `@supabase/supabase-js` 2.104.1 (`dist/index.mjs:135-138`, `:383-407`, `:527`) y el callback de `accessToken` en `realtime-js` (`dist/main/RealtimeClient.js:119-120`, `:166`, `:333-344`). Si la versión instalada cambió, el design se corrige antes de escribir código
- [ ] 17.4 Verificar que no hay obstáculos para el nonce: cero `<Script>`/`next/script` en el árbol y un único `dangerouslySetInnerHTML`, que es un `<style>` (`frontend/components/ui/chart.tsx:81`, `style-src`, no `script-src`)

## 18. Parte C — Operaciones de auth al servidor y cookies httpOnly (D1, D2)

- [ ] 18.1 RED→GREEN: `frontend/__tests__/lib/cookie-options.test.ts::emits_http_only` — el objeto compartido de 15.1 emite `httpOnly: true` y `sameSite: 'lax'`
- [ ] 18.2 GREEN: documentar en el propio archivo por qué `Lax` y **no** `Strict` (D2): los enlaces por email entran por `/auth/callback` como navegación top-level y deben llevar la cookie PKCE del verificador
- [ ] 18.3 RED→GREEN: `frontend/__tests__/app/auth-actions.test.ts::sign_in_runs_on_the_server` — la acción de inicio de sesión recibe credenciales + token de captcha y es el servidor quien contacta al proveedor. GREEN: Server Action bajo `frontend/app/auth/`
- [ ] 18.4 RED→GREEN, **una por operación, con su test nombrado** (no un solo GREEN: son siete caminos que manejan credenciales, y el propio encabezado de este archivo exige ver fallar el test primero):
  - [ ] 18.4a `auth-actions.test.ts::sign_up_runs_on_the_server` → `signUp`
  - [ ] 18.4b `::magic_link_runs_on_the_server` → `signInWithOtp`
  - [ ] 18.4c `::password_reset_request_runs_on_the_server` → `resetPasswordForEmail`
  - [ ] 18.4d `::verification_resend_runs_on_the_server` → `resend` (conservando el límite de 30 s que hoy vive en el cliente)
  - [ ] 18.4e `::password_change_runs_on_the_server` → `updateUser` (contraseña)
  - [ ] 18.4f `::email_change_runs_on_the_server` → `updateUser` (email)
  - [ ] 18.4g `::sign_out_runs_on_the_server` → `signOut`, conservando el `scope: 'local'` de la Parte B
  > `exchangeCodeForSession` **no** está en esta lista: ya corre en el servidor (`frontend/app/auth/callback/route.ts:34`); lo que cambia es de dónde toma las opciones de cookie (18.7) y que valida su `next` (13.2)
- [ ] 18.5 TRIANGULATE: `::captcha_token_still_reaches_the_provider` — el `captchaToken` sigue viajando; el helper `submitWithFreshCaptcha` sigue siendo el único camino de envío en toda pantalla con captcha (regla del proyecto)
- [ ] 18.6 TRIANGULATE: `::existing_session_survives_the_switch` — una cookie con el formato actual (no-httpOnly) sigue siendo válida y el servidor la reescribe como httpOnly en el primer refresh: **una carga de página nueva después del deploy no pide credenciales**. La aserción se escribe así, **no** como "cero re-login forzado" absoluto: una pestaña que quedó abierta con el bundle anterior pierde `document.cookie` en cuanto el servidor reescribe la cookie, y con D7 termina en `/auth/login`. Eso es esperado y está en la tabla de riesgos — la mitigación es la ventana de deploy y el aviso del PO (24.8), no código
- [ ] 18.7 GREEN: `frontend/app/auth/callback/route.ts` escribe las cookies con las opciones compartidas y conserva el `safeNext()` de 13.2

## 19. Parte C — Token handler y cliente de navegador (D1)

- [ ] 19.1 RED→GREEN: `frontend/__tests__/app/api-auth-token.test.ts::never_returns_refresh_token` — la respuesta contiene `access_token`, `expires_at` y `user`, y **no** contiene el refresh token bajo ningún nombre
- [ ] 19.2 RED→GREEN: `::returns_no_session_when_cookie_absent` — sin cookie válida, no entrega token
- [ ] 19.3 RED→GREEN: `::refresh_rotates_cookies` — cuando el token vencido se renueva, la respuesta emite las cookies rotadas con las opciones compartidas
- [ ] 19.3b RED→GREEN: `::sends_no_store_and_varies_on_cookie` y `::never_sends_access_control_allow_origin` — la respuesta declara `Cache-Control: no-store` + `Vary: Cookie` y **no** emite ACAO bajo ninguna configuración. Es el único punto donde la credencial protegida vuelve a ser legible, es un `GET` y vive detrás del CDN de Vercel
- [ ] 19.3c RED→GREEN: `::rejects_cross_site_requests` — con `Sec-Fetch-Site` presente y distinto de `same-origin`, el handler rechaza sin emitir token
- [ ] 19.3d RED→GREEN: `::idle_session_is_revoked_not_refreshed` — con `auth:last-activity` fuera del umbral, el handler aplica la **misma** `evaluateIdle` que el middleware (`frontend/lib/auth/idle-server.ts:48-57`), revoca, borra cookies y responde "sin sesión" **en vez de renovar**. Sin esto el handler es un segundo camino de refresh server-side **no gateado** que mantiene viva indefinidamente la sesión de un usuario ausente desde una pestaña de fondo — una **regresión** del único control de idle server-side que hoy existe (el síncrono dentro de `getUser()`, `middleware.ts:131-165`)
- [ ] 19.3e RED→GREEN: `::concurrent_requests_refresh_once` — dos pedidos concurrentes con el token vencido producen **una sola** renovación (dedupe por promesa en vuelo), y el handler **no** renueva si la misma petición ya obtuvo credenciales frescas por otro camino del servidor. Depende de la lectura de 24.9 (rotación y ventana de reuso reales)
- [ ] 19.4 GREEN: crear el Route Handler `frontend/app/api/auth/token/route.ts` sobre `createServerClient` + opciones compartidas, con todo lo anterior
- [ ] 19.4b RED→GREEN: `frontend/__tests__/app/api-auth-status.test.ts` — `GET /api/auth/status` (D18) fuerza una renovación contra el proveedor y devuelve `{ email, email_confirmed_at }` y **ningún token**. GREEN: el handler + recableado de `frontend/app/auth/verify-email/page.tsx`, que hoy depende de cuatro operaciones que van a lanzar: `refreshSession()` (`:68`), `getSession()` (`:80`, `:92`) y `onAuthStateChange` (`:113`). Es la pantalla que mira **todo usuario nuevo**
- [ ] 19.5 RED→GREEN: `frontend/__tests__/lib/access-token-store.test.ts` — el token vive **sólo** en memoria del módulo: no se escribe en cookie, ni en `localStorage`, ni en `sessionStorage`; se renueva antes de vencer, en `visibilitychange` y tras un 401
- [ ] 19.5b RED→GREEN: `::resolves_null_and_never_throws_without_session` — sin sesión, `getAccessToken()` **resuelve** a un valor vacío y no lanza, de modo que `fetchWithAuth` caiga a la anon key (`index.mjs:112`) y una página pública siga renderizando. Si lanza, todo visitante anónimo de una página pública que toque `supabase-js` rompe
- [ ] 19.6 GREEN: `frontend/lib/supabase/client.ts` pasa a `createClient(url, anonKey, { accessToken: () => getAccessToken() })`
- [ ] 19.7 RED→GREEN: `frontend/__tests__/lib/no-browser-auth-calls.test.ts` — grep programático que falla si queda algún `supabase.auth.*` en código de navegador (con `accessToken` configurado, cualquiera de ellos **lanza** en runtime: `index.mjs:389`). Recorre el inventario de 17.2
- [ ] 19.7b RED→GREEN: `frontend/__tests__/lib/no-auth-in-client-mocks.test.ts` — candado en el espíritu del requisito ya vigente "Los dobles de test no pueden divergir del contrato real" (`openspec/specs/backend-auth/spec.md`): **falla si un `vi.mock` de `@/lib/supabase/client` expone `auth`**. Recorre el inventario de 17.2b. Sin este candado, los 43 dobles dejan la suite verde mientras producción lanza
- [ ] 19.8 RED→GREEN, **una por familia de consumidor, con su test nombrado** (los 43 archivos de 17.2 + sus dobles de 17.2b). Los `getUser()`/`getSession()` que sólo querían el token pasan por `getAccessToken()`; los que querían la identidad, por el contexto de sesión de la app:
  - [ ] 19.8a `contexts/auth-context.tsx` (las 9 llamadas) → `auth-context.test.tsx` + `auth-context-membership.test.tsx`
  - [ ] 19.8b `lib/supabase/services.ts:48`/`:64` y `lib/services/*` (`aiInsightService`, `fairAdvisorService`, `invoiceOcrService`)
  - [ ] 19.8c hooks de datos — `hooks/data/use-posts.ts:42`/`:59`/`:112`/`:179` (que derivan `session.user.id` y lo escriben como columna), `use-notifications`, `use-statistics-ai`
  - [ ] 19.8d exports e IA — `components/exports/ExportButton.tsx`, `app/(dashboard)/exportaciones/page.tsx`, `ai-summary-card.tsx`, `PriceSuggestionModal.tsx`, `simulador/page.tsx`
  - [ ] 19.8e pantallas de auth ya recableadas en el grupo 18 — verificar que no quedó ninguna llamada residual
- [ ] 19.8f RED→GREEN: `frontend/__tests__/lib/auth-headers.test.ts` — **un solo** `getAuthHeaders()` en `lib/api/` (D21), consumido por los **ocho** transportes que hoy lo arman por su cuenta: `lib/api/python-client.ts:12-23`, `lib/api/subscriptions-client.ts:47-57`, `components/ventas/sale-receipt-button.tsx:132`, `app/(dashboard)/admin/pagos/page.tsx:147`, `components/exports/ExportButton.tsx:76`, `hooks/data/use-statistics-ai.ts:117`, `app/(dashboard)/simulador/page.tsx:118`, `components/products/PriceSuggestionModal.tsx:69`. Con el test de grep que el escenario de la spec ya describe ("Una sola implementación arma los encabezados"). Sin esta task, 14.8 **contradice** un requisito normativo de este mismo change
- [ ] 19.9 TRIANGULATE: `frontend/__tests__/hooks/use-notifications-realtime.test.ts` — Realtime toma el token por el callback, y tras cada renovación se llama **`supabase.realtime.setAuth()` SIN argumentos**. El test assertea las dos cosas: la llamada sin argumentos y que el callback `accessToken` **se vuelve a invocar** en la reconexión. Con argumento el token queda **pinneado** (*"When a token is explicitly provided, it will be preserved across channel operations… The `accessToken` callback will not be invoked until `setAuth()` is called without arguments"*, `RealtimeClient.js:330-339`; la renovación automática está condicionada a `accessTokenValue` nulo, `:479`) y `use-notifications` + `FiscalDocumentBadge` se quedan mudos al primer vencimiento, sin error visible
- [ ] 19.10 TRIANGULATE: Storage y Functions siguen funcionando con el token del callback — `frontend/components/settings/AvatarUpload.tsx`, `frontend/lib/services/invoiceOcrService.ts` (upload/remove + `functions.invoke`), y los `functions.invoke` de `ai-summary-card.tsx`, `aiInsightService.ts`, `fairAdvisorService.ts`, `frontend/lib/supabase/services.ts`
- [ ] 19.11 TRIANGULATE: las llamadas a Edge Functions por `fetch` a mano siguen llevando el Bearer correcto — `frontend/hooks/auth/use-export-usage.ts:85-92`, `frontend/hooks/data/use-statistics-ai.ts:60-67`, `simulador/page.tsx:118`/`:137`, `PriceSuggestionModal.tsx:69`/`:77`

## 20. Parte C — Bus de eventos de sesión (D1)

- [ ] 20.0 RED→GREEN (**previa a montar nada encima**): `frontend/__tests__/idle-transport.test.ts::supports_multiple_subscribers` — hoy `onMessage` admite **un solo** handler (`handler = h` **sobrescribe**, en las dos implementaciones: `frontend/lib/auth/idle-transport.ts:63-65` y `:104-106`), así que un bus que comparta la instancia **desuscribe en silencio** al temporizador de inactividad. GREEN: lista de handlers + función de baja, conservando la interfaz de 4 métodos (`:32-40`)
- [ ] 20.1 RED→GREEN: `frontend/__tests__/lib/session-bus.test.ts` — el cierre de sesión en una pestaña alcanza a la otra. GREEN: montar el bus sobre `frontend/lib/auth/idle-transport.ts` (BroadcastChannel con fallback a localStorage, `:43-49`, `:115-118`) con **tipos de mensaje propios** (`session:signed-out`, `session:token-refreshed`, `session:signed-in`), ampliando la unión hoy cerrada `IdleMessage = activity | logout` (`:19-27`)
- [ ] 20.1b RED→GREEN: `::manual_logout_is_not_reported_as_idle` — el bus **NO** reutiliza `{type:"logout"}`, que ya **significa** "cierre por inactividad" para su único consumidor (`frontend/hooks/use-idle-timer.ts:129`, `:163-165`). Sin esto, un logout manual haría que las otras pestañas muestren `reason=idle`. TRIANGULATE hermano: `::idle_timer_still_receives_activity_messages` con el bus suscrito al mismo canal (`CHANNEL_NAME = "idle-timeout"`, `:45`) y clave (`LS_KEY = "idle:sync"`, `:46`)
- [ ] 20.2 TRIANGULATE: `::no_second_cross_tab_transport` — grep programático que falla si nace un segundo `new BroadcastChannel` fuera del transporte compartido (regla "reutilización antes que repetición")
- [ ] 20.3 GREEN: retirar las dos suscripciones a `onAuthStateChange` —`frontend/contexts/auth-context.tsx:202` y **`frontend/app/auth/verify-email/page.tsx:113`**— y reemplazarlas por suscripciones al bus (la segunda, junto con el recableado de 19.4b)
- [ ] 20.4 TRIANGULATE: `::token_refresh_propagates` — una renovación en una pestaña deja a las otras con el token nuevo sin pedirlo por separado

## 21. Parte C — CSP con nonce (D3)

- [ ] 21.1 RED→GREEN: `frontend/__tests__/lib/csp-nonce.test.ts::production_script_src_has_no_unsafe` — en producción, `script-src` lleva nonce + `'strict-dynamic'` y **no** lleva `'unsafe-inline'` ni `'unsafe-eval'`
- [ ] 21.2 RED→GREEN: `::nonce_differs_per_request` — dos invocaciones producen nonces distintos
- [ ] 21.3 RED→GREEN: `::unsafe_eval_survives_outside_production` — fuera de producción se conserva
- [ ] 21.4 RED→GREEN: `::style_src_keeps_unsafe_inline` — candado explícito: Tailwind/Radix y el `<style>` de `frontend/components/ui/chart.tsx:81` lo necesitan
- [ ] 21.5 RED→GREEN: `::turnstile_host_still_allowed` — `https://challenges.cloudflare.com` sigue en `script-src`, `connect-src` y `frame-src`
- [ ] 21.5b RED→GREEN: `::wasm_unsafe_eval_in_production` — `script-src` incluye `'wasm-unsafe-eval'`. Retirar `'unsafe-eval'` retira también la única habilitación de `WebAssembly.instantiate`; hoy probablemente no haga falta (cero `.wasm` bajo `frontend/public`, cero `WebAssembly`/`new Function(` en `app/`, `components/`, `lib/`, `hooks/`), pero el comentario del propio archivo sobre los decoders Draco/KTX2 "self-hosted en /public" (`middleware.ts:35-38`) está **envejecido** y esto evita descubrir un 3D roto en prod. Corregir ese comentario acá (entra en la lista de D15)
- [ ] 21.6 GREEN: generar el nonce **una sola vez por petición** en `frontend/middleware.ts` y propagarlo construyendo `requestHeaders = new Headers(request.headers)` con **los dos** encabezados: `x-nonce` **y** `Content-Security-Policy` completa, pasándolos por `NextResponse.next({ request: { headers: requestHeaders } })` y emitiendo la **misma** política (mismo nonce) en la respuesta. **Next NO lee `x-nonce`**: obtiene el nonce de sus propios scripts de arranque e hidratación parseando el encabezado de **petición** `content-security-policy` (`frontend/node_modules/next/dist/server/app-render/app-render.js:150` → `get-script-nonce-from-header.js:13-41`). Con `'strict-dynamic'` —que además **anula `'self'` y todos los hosts de `script-src`**— omitirlo es **pantalla en blanco en el 100% de las páginas de producción**
- [ ] 21.6b GREEN + RED: la propagación se preserva **también dentro de `setAll`**, que hoy hace `supabaseResponse = NextResponse.next({ request })` (`frontend/lib/supabase/middleware.ts:77-82`) y **descarta** los encabezados de petición modificados — justo en las peticiones que rotan cookies, es decir las autenticadas. Test: `csp-nonce.test.ts::nonce_survives_a_cookie_rotating_setAll` — después de un `setAll`, el encabezado de petición `content-security-policy` sigue presente y su nonce es **idéntico** al de la respuesta
- [ ] 21.7 GREEN: leer el nonce con `headers()` en **`frontend/app/layout.tsx`** (donde hoy se renderiza el `ThemeProvider`, `:44-60`) y pasarlo hacia abajo a `next-themes` vía la prop `nonce` de `frontend/components/theme-provider.tsx`. Ese componente es `"use client"` y sólo propaga props: no puede leer los encabezados por su cuenta. `next-themes` 0.4.6 acepta `nonce` (`dist/index.d.ts:45`) y lo aplica a su `<script>` en línea; con `disableTransitionOnChange` —que esta app activa— también inyecta un `<style>` con nonce, que cae en `style-src`
- [ ] 21.8 GREEN + TRIANGULATE: `buildContentSecurityPolicy()` **cambia de firma** (pasa a recibir el nonce), así que `frontend/__tests__/lib/csp-frame-src.test.ts` y `csp-worker-src.test.ts` —que hoy la llaman **sin argumentos** (`csp-frame-src.test.ts:26`, `:34`, `:43`, `:48`)— **se actualizan en este mismo commit** y quedan en verde: `frame-src` con YouTube nocookie y `worker-src 'self' blob:` no se tocan
- [ ] 21.9 Registrar en el PR la consecuencia declarada: un nonce por petición **saca de render estático** a las páginas que hoy se prerenderizan (Next no puede prerenderizar un valor que cambia por request). Las del dashboard ya eran dinámicas por cookies; el costo de latencia en las públicas es aceptado, **no** un motivo para ampliar la política

## 22. Parte C — Verificación visual y funcional de las siete pantallas

- [ ] 22.1 Humo funcional en el stack local, ciclo completo: login → dashboard → una llamada a FastAPI que escriba → una notificación en tiempo real → logout. Los cuatro consumidores del token (PostgREST, FastAPI, Realtime, Storage/Functions) ejercitados
- [ ] 22.2 Verificar en el navegador local que la cookie de sesión aparece marcada `HttpOnly` y que `document.cookie` **no** la muestra
- [ ] 22.3 Verificar el ciclo de renovación: dejar la pestaña oculta más allá del vencimiento del token, volver, y confirmar que la app sigue operando sin recargar
- [ ] 22.4 Pasada visual de las **siete** pantallas recableadas (`/auth/login`, `/auth/register`, `/auth/forgot-password`, `/auth/reset-password`, `/auth/verify-email`, `/auth/callback` en su estado de error, y `/configuracion`) en **4 combinaciones**: desktop + móvil × claro + oscuro. Se verifican los estados de carga y de error, que son los que el recableado toca
- [ ] 22.5 **Verificación en un preview de Vercel, no sólo local**: consola abierta, cero violaciones de CSP en las 7 pantallas de auth + una del dashboard con gráficos (Recharts) + una con 3D (R3F, **incluida la instanciación de WebAssembly de los decoders**). El CSP de dev es más laxo y el captcha tiene stub local: este paso es el único que prueba la política real. **Excepción esperada y que NO se "arregla"**: el preview inyecta scripts de `https://vercel.live` (toolbar/comentarios) que no existen en producción — sus violaciones se registran como artefacto del preview y **jamás** se silencian ampliando la política. Verificar además que Turnstile carga por propagación de confianza desde un script con nonce, no por el host listado (`'strict-dynamic'` vuelve inertes los hosts de `script-src`)
- [ ] 22.6 E2E: `frontend/e2e/fixtures/auth.setup.ts` sigue funcionando (hace login por la UI y guarda `storageState`, que **sí** captura cookies httpOnly); `frontend/e2e/auth.spec.ts` sigue en verde. Actualizar el comentario de `auth.spec.ts:41` sobre el scope global, que dejó de aplicar en la Parte B

## 23. Parte C — Verificación y cierre

- [ ] 23.1 `pnpm vitest run` y `pnpm tsc --noEmit`: cero regresiones sobre 17.1
- [ ] 23.2 `pytest backend/tests/` sin regresiones (no debería tocarse, pero el token cambia de origen)
- [ ] 23.3 Ronda de revisión adversarial pre-merge; aplicar findings y re-verificar
- [ ] 23.4 **Verificación post-merge en prod**: `curl -I` de una página cualquiera → `Content-Security-Policy` con nonce y sin `'unsafe-inline'`/`'unsafe-eval'` en `script-src`. Los atributos de `Set-Cookie` de una sesión real los reporta **el PO** tras su propio login (task 24.5) — el agente no hace login en prod
- [ ] 23.5 Registrar la ficha de la Parte C en `CHANGES.md`, con el riesgo residual declarado (un XSS sigue pudiendo actuar en la página durante la vida del access token) y el candidato que queda abierto: el prefijo `__Host-` de OQ-1
- [ ] 23.6 Escribir/actualizar **a mano** los `## Purpose` al archivar — **gotcha registrado: el `## Purpose` no lo cubre el delta**: el de `openspec/specs/python-backend/spec.md` (hoy dice "expone una API HTTP + WebSocket"), el de `openspec/specs/realtime-websocket/spec.md`, y el de la **capability nueva `session-cookies`**, que nace sin ninguno (molde: `openspec/specs/receivables-panel/spec.md:3`)

## 24. Operación (PO) — acciones en dashboards, no código

- [ ] 24.1 Confirmar en el Dashboard de Supabase que el hook `Customize Access Token` está habilitado (el repo **no puede probarlo**: `config.toml:270-272` gobierna sólo el stack local y `deploy.yml` nunca corre `supabase config push`)
- [ ] 24.2 Tras el merge de la Parte C, poner `Access token expiry` en **900 s** (OQ-5). Antes del token handler **no** conviene: cuadruplicaría los refresh escritos sobre cookies legibles por JS
- [ ] 24.3 Confirmar/poner en Render y reportar el **valor exacto** (el texto literal, no "está puesta"): `SUPABASE_URL` —de la que dependen el fail-fast de 3.x y el `issuer` de 2.2, y **una barra final produce 401 en el 100% del tráfico**—, `SUPABASE_JWT_SECRET`, `BACKEND_ALLOWED_ORIGIN` y **`APP_ENV`** (hallazgo B2/m1 de la revisión adversarial del apply: desde D10 `APP_ENV` es **load-bearing** — con `production` el comodín de CORS está prohibido y `http://localhost:3000` deja de estar admitido; si Render no la define, esas dos mitades del control **no aplican** y nadie lo sabría. Un `GET /health` no lo revela: hace falta el valor literal). Lo recomendado: `APP_ENV=production` y `BACKEND_ALLOWED_ORIGIN` **sin definir**. **Previo al merge de la Parte A**
- [ ] 24.7 Decodificar **un token real** de la propia sesión (sólo el payload, sin firma — p. ej. pegándolo en un decodificador local) y reportar `iss` y `aud`, **incluyendo si `aud` llega como string o como array**. Es lo que habilita 2.3/2.3b: el repo no puede probarlo (la spec vigente sólo cita el valor como justificación para **desactivar** la comprobación, que no es evidencia). **Previo al merge de la Parte A**
- [ ] 24.8 Acordar la **ventana de deploy** de la Parte C (nunca en apertura de mostrador) y avisar a las cuentas activas: las pestañas que queden abiertas cruzando ese deploy son expulsadas al login dentro de una vida de token (ver la tabla de riesgos). Las cargas de página nuevas no piden credenciales
- [ ] 24.9 Leer en el Dashboard de Supabase (Authentication → Sessions) el estado real de **`enable_refresh_token_rotation`** y **`refresh_token_reuse_interval`**. **Previo al merge de la Parte C**: es el parámetro del que pasa a depender la arquitectura cuando el navegador deja de refrescar y varios contextos de servidor pueden presentar el mismo refresh token (D19). El repo **no puede verlo** (§9.2: `config.toml:164`/`:167` describen el stack local y `deploy.yml` nunca corre `supabase config push`)
- [ ] 24.10 Leer en el Dashboard (Authentication → Rate Limits) los límites de `token` y `signup`, y contrastarlos contra los logins concurrentes esperados en apertura. **Previo al merge de la Parte C**: con el login en una Server Action, GoTrue ve la IP de egreso de Vercel para el 100% de los logins, así que los 38 tenants comparten un solo balde por IP y Turnstile pierde el `remoteip` real (D20). Si el límite no da margen, se adopta la variante híbrida de OQ-7 — decisión **antes** de escribir el código, no después
- [ ] 24.11 Leer en el Dashboard el valor real de **`secure_password_change`** (OQ-9). Hoy la app cambia la contraseña con cualquier sesión válida, sin pedir la anterior (`frontend/app/auth/reset-password/page.tsx:46`); si el Dashboard ya lo tiene en `true`, el hallazgo de §4 se cierra sin escribir código. No se arregla en este change (Non-Goal declarado)
- [ ] 24.12 Humo del PO tras la Parte A: emitir un comprobante fiscal y confirmar que el **badge de CAE** (`frontend/components/fiscal/FiscalDocumentBadge.tsx:76-96`) se actualiza solo al pasar de `pending_cae` a `authorized`. Es el primer evento que esa suscripción recibe desde que se escribió: `pg_publication_tables` prueba la publicación, no la entrega
- [ ] 24.4 Confirmar que Turnstile está habilitado en el Dashboard del proyecto (§9.6 de la auditoría: el bloque `[auth.captcha]` está comentado en `config.toml` y no hay ninguna llamada de verificación en el repo, así que los `captchaToken` que la app manda sólo tienen efecto si el proyecto lo tiene activo)
- [ ] 24.5 Humo real del PO tras la Parte C: iniciar sesión en prod desde su propio navegador y reportar los atributos del `Set-Cookie` de la sesión (`HttpOnly`, `Secure`, `SameSite=Lax`, `Path=/`), más el recorrido de una venta completa para confirmar que nada del recableado rompió la operación diaria
- [ ] 24.6 Decidir las **9 OQs** del design (o firmar sus recomendaciones): `__Host-` (recomendación: no ahora), retiro de `/ws` (retirar), scope del logout (local), re-chequeo en base (sólo configuración), expiry del token (900 s), binding de email (estricto), login entero en el servidor vs. híbrido (entero, condicionado a 24.10), `/dev-harness` (público con su propio `notFound()`), re-autenticación al cambiar contraseña (fuera de alcance, empezando por 24.11)
