> **Gobernanza: MEDIA con un tramo ALTO.** El change escribe **registros contables sobre datos reales de usuarios** por un camino asíncrono, desde funciones `SECURITY DEFINER`. Los grupos 4, 5 y 6 se implementan en pasos con checkpoint visible; **nada de SQL se escribe antes de cerrar el grupo 1**. Las decisiones no obvias se surfacean al PO en vez de resolverse en silencio.
>
> **Strict TDD activo.** Cada grupo con lógica arranca por su test en **rojo** (RED → GREEN → TRIANGULATE → REFACTOR), con **SAFETY NET** previo sobre los archivos que se modifican. Un gate SQL que no se vio fallar antes de la migración no prueba nada.
>
> **Regla de integridad de función (repo).** Las cinco reescrituras parten del `pg_get_functiondef` **vivo de producción** —los md5 los entrega el orquestador—, nunca del último archivo de migración. `compras-proveedor-cuenta-corriente` encontró un cuerpo vivo divergido del archivo por una reescritura in-place; el design de este change ya lo advierte (D12).
>
> **Todo `CREATE OR REPLACE`, ninguna firma cambia** (D12) → las ACLs se preservan y **no** aplica el gotcha `42725` de `DROP`+`CREATE`. La única función nueva (`_journal_expense_credit_account`) sí necesita su `REVOKE` explícito en el mismo archivo.
>
> **Interacción con el change en vuelo.** `importador-gastos-transaccional` (migración `20261041000001`) importa gastos invocando `rpc_create_expense` **fila por fila**. Con el productor viviendo dentro de `rpc_create_expense` (D1), **los gastos importados se asientan por el mismo camino sin una sola línea de código adicional en el importador**: este change **no toca** `rpc_import_expenses` y esa no-intervención es un resultado verificable, no un olvido (task 5.6).

## 1. Checkpoint de estado vivo — medir antes de escribir una línea de SQL

- [x] 1.1 Obtener del orquestador los `md5(pg_get_functiondef)` **vivos de producción** de las cinco funciones a reescribir y compararlos contra la tabla de inventario del `design.md`: `rpc_create_expense(text,numeric,date,text,uuid,uuid,uuid,uuid,uuid)` (`4a31c7c6911fe247c76d243eff3e445e`), `rpc_update_expense` (`e21c443ade72c915255a5aa8d3236330`), `rpc_delete_expense(uuid)` (`b76e0fb7862f05aa610d049104fea45a`), `_journal_post_from_event(events)` (`da103f22ebcc1a33bf98c3c2c516a66c`) y `rpc_process_outbox_dispatch(int)` (`8462768d81778e912d61a661b13072de`). **Si alguno difiere: PARAR**, releer el cuerpo vivo, reportar la divergencia al orquestador y partir de él. (Comparar por líneas con `\r` stripped — el CRLF del checkout Windows falsea el md5; memoria del repo, 2026-09-06.)
- [x] 1.2 Hashear también `_journal_sale_debit_account(text)` (`6c3abe8028f0f4fac4d2495fbe73df5b`) —el **molde** del helper nuevo, que **no se toca**— y `rpc_import_expenses(...)` (`21d323a502f914127454b1d96b21b167`), cuyo md5 debe seguir **idéntico** al terminar el change (D1; se re-verifica en 5.6 y 13.4).
- [x] 1.3 Confirmar `MAX(version)` de `supabase_migrations.schema_migrations` en prod **y** en la base local, y **fijar el número de la migración en ese momento**. El orquestador reserva **`20261043000001`** (`20261041000001` es de `importador-gastos-transaccional`, `20261042000001` lo reserva `productos-costo-nullable`); el `design.md` D12 dice `20261042000001` y **queda superado por esta task**. Nunca asumir el número del design: este proyecto ya renumeró una migración tres veces (`cuenta-corriente-party-guard`).
- [x] 1.4 Re-verificar en prod los cuatro hechos de esquema de los que depende el diseño: `public.events` **sin `CHECK`** sobre `event_type`; `journal_lines` con `CHECK (amount > 0)` y `side IN ('debit','credit')`; `journal_entries` con `CHECK (status IN ('posted','reversed'))`, `reversal_of` autorreferencial e **índice único parcial sobre `source_event_id`**; `expenses.date` de tipo `timestamp with time zone` (⚠️ D5 — el parámetro de la RPC es `date`, la columna no).
- [x] 1.5 Re-verificar que el relay sigue siendo un `pg_cron` `* * * * *` sobre `rpc_process_outbox_dispatch(100)` con `ORDER BY occurred_at … FOR UPDATE SKIP LOCKED` — **el orden por ocurrencia es la premisa de D7** y si cambió, D7 hay que rediseñarlo.
- [x] 1.6 Re-verificar las ACLs vivas: `_journal_post_from_event` y `rpc_process_outbox_dispatch` con `postgres` + `service_role` y **sin `authenticated`**; las tres RPC de gasto con `authenticated` además. Registrar el estado exacto para poder assertarlo idéntico después (13.3).
- [x] 1.7 **Mediciones de producción que alimentan OQ-3** (sólo lectura, las cinco consultas del `Migration Plan` §2 del design): (a) universo de gastos y cobertura de forma de pago; (b) reparto por `kind`; (c) gastos que ya movieron dinero de verdad; (d) **control**: cero asientos con `source_doc_type='Expense'` y cero eventos `Expense%` hoy; (e) salud del outbox (pendientes y el más viejo) antes de sumarle tres tipos. Los cinco resultados van al registro del change: son el "antes" del que se hablará al archivar.
- [x] 1.8 Confirmar por grep que `test_cobranzas_reverso.sql` sigue **sin cablear** en `.github/workflows/KPI_Validation.yml` (hallazgo (1) del design; verificado en el propose: `grep -n "test_cobranzas_reverso" .github/workflows/KPI_Validation.yml` no devuelve nada, mientras `test_outbox_single_dispatcher.sql` **sí** corre, línea ~1223). Si alguien lo cableó entre medio, el grupo 7 se reduce.
- [x] 1.9 Confirmar que `P0450` (balance) y `P0451` (asiento original no encontrado, retry) siguen registrados en `backend/core/errors.py` y en `20260803000001_journal_entry_schema.sql` — **este change no crea ningún ERRCODE nuevo**. Si aparece la necesidad de uno, es señal de que algo se apartó del molde.
- [x] 1.10 Re-correr el inventario de **caminos de lectura de gasto** (lección de `cobranzas-reverso`, D11): confirmar que `_EXPENSE_PROJECTION` de `backend/repositories/expense_repository.py` sigue siendo la **única** definición y que la usan `get_by_id`, `list_paginated` **y** el re-`SELECT` de las tres mutaciones. Si aparece una segunda consulta que alimente una pantalla real, los derivados del grupo 8 tienen que sumarse ahí también.

## 2. SAFETY NET — baseline antes de tocar nada

- [x] 2.1 `pytest backend/tests/test_expenses.py backend/tests/test_journal_entries_endpoint.py` → registrar los dos conteos. Son los archivos que cubren lo que este change extiende.
- [x] 2.2 Suite completa de backend y de frontend → registrar los dos totales y la cobertura. Todo fallo previo se declara **pre-existente** y **no se arregla acá** (regla del repo).
- [x] 2.3 Correr, **antes** de la migración, la tanda completa de gates SQL en el orden real de `KPI_Validation.yml` contra una base recién reseteada; registrar los fallos pre-existentes conocidos (`test_cuentas_billetera_tipo.sql` por artefacto de invocación local) para no confundirlos con regresiones propias.
- [x] 2.4 Correr `supabase/tests/test_cobranzas_reverso.sql` **entero** contra `supabase db reset` local, **antes** de tocar nada. Es la primera corrida real de ~950 líneas de gate que nunca corrieron (D8, riesgo declarado). Registrar el resultado: **cualquier rojo que aparezca acá es pre-existente y es un hallazgo a reportar** (OQ-6), no algo que este change rompió.
- [x] 2.5 Correr `supabase/tests/test_outbox_single_dispatcher.sql` (ya cableado) y registrar su conteo: el change toca `rpc_process_outbox_dispatch` y ese gate es el que custodia que siga habiendo **un solo despachador**.
- [x] 2.6 Dejar por escrito, antes de tocar código, **qué aserciones de qué archivos van a cambiar y por qué** — en particular las que hoy enumeran **once** tipos canónicos y van a enumerar **catorce**. Sin esto, verlas romper se lee como "baseline distinto" en vez de como la ampliación deliberada que es.

## 3. Gates SQL — RED primero

> Un gate escrito después de la migración no prueba que la migración haga algo. Los tres bloques de este grupo se escriben y **se ven fallar** antes del grupo 4.

- [x] 3.1 RED: crear `supabase/tests/test_asiento_contable_gastos.sql` con el estilo de los ~40 gates vivos (fixtures propias con dos tenants sintéticos, `RAISE EXCEPTION` con mensaje explícito por bloque, cleanup que **se assertea**). Bloque **(A)**: `_journal_expense_credit_account` existe, es `IMMUTABLE`, y devuelve `1110` para `transfer`/`card`/`check`/`wallet` y `1100` para `cash`/`other`/`NULL` — **sin que exista ningún gasto, evento ni asiento** (D4; es el test que justifica que el mapeo sea función y no `CASE` embebido).
- [x] 3.2 RED: bloque **(B)** — alta de gasto en efectivo → existe `ExpenseCreated` en `public.events` con `aggregate_type='Expense'`, `aggregate_id` = el gasto y `kind='cash'` en la carga; se corre el relay y el asiento resultante tiene débito `5300` = total (con el `cost_center_id` del gasto) y crédito `1100` = total, y **balancea**.
- [x] 3.3 RED: bloque **(C)** — los cuatro `kind` bancarios (`transfer`, `card`, `check`, `wallet`) acreditan `1110`, y el gasto **sin forma de pago** acredita `1100` y **no** `2100` (OQ-1 (a)).
- [x] 3.4 RED: bloque **(D)** — **control negativo de OQ-2**: gasto en efectivo **sin** opt-in de caja (sin sesión abierta) → el asiento acredita igual `1100`. Sin este caso, "el opt-in no gobierna la cuenta" sería verdadero por omisión.
- [x] 3.5 RED: bloque **(E)** — **fecha (D5)**: gasto fechado en un día anterior, procesado hoy → `posted_at` cae en el **día del gasto** en zona `America/Argentina/Mendoza`. Incluir el caso de borde nocturno (la franja en que la hora local y la UTC caen en días distintos) — es el bug de corrimiento que `gastos-forma-pago` documentó.
- [x] 3.6 RED: bloque **(F)** — **edición (D6)**: gasto **sin** movimientos de dinero, con asiento posteado → la edición **procede** (no `P0423`), emite `ExpenseAdjusted`, y al procesarse el original queda `reversed`, existe el contra-asiento con `reversal_of` apuntándolo, y existe el asiento nuevo con el importe editado. Los **tres** balancean.
- [x] 3.7 RED: bloque **(G)** — **borrado**: gasto con asiento → `ExpenseDeleted` → original `reversed` + contra-asiento con lados invertidos, `cost_center_id` preservado por línea, y `source_event_id` propio. El contra-asiento se postea **con la fila del gasto ya borrada** (borrado físico) — assertar que la fila no existe en el momento del posteo.
- [x] 3.8 RED: bloque **(H)** — **D7, el caso que evita el evento envenenado**: gasto **histórico** (sin `ExpenseCreated`) → borrarlo y editarlo **no emite ningún evento** y la operación procede normalmente. Y su gemelo: gasto creado y borrado **antes** de que el relay corra → **sí** emite `ExpenseDeleted` (el predicado mira el evento, no el asiento), y procesados en orden de ocurrencia el asiento se postea y su contra-asiento a continuación.
- [x] 3.9 RED: bloque **(I)** — **idempotencia y aislamiento (D13)**: reprocesar el mismo `ExpenseCreated` no postea un segundo asiento; un `ExpenseAdjusted`/`ExpenseDeleted` sin asiento original levanta `P0451`, deja el evento `pending`, **y el resto del lote se procesa**.
- [x] 3.10 RED: bloque **(J)** — **tenencia**: el asiento del gasto lleva el `account_id` del gasto; un tenant no ve asientos del otro; y `_journal_expense_credit_account` **no tiene `EXECUTE`** para `anon` ni para `authenticated` (espejo de `_journal_sale_debit_account`, D12).
- [x] 3.11 Actualizar el bloque **(11)** de `supabase/tests/test_cobranzas_reverso.sql`: `v_expected` pasa de **once** a **catorce** entradas (D8). Verificar que los **dos anclajes de texto sobreviven** —`substring(... from 'v_event_type NOT IN \(([^)]*)\)')` sobre el helper y `'Consumer 3: JournalEntry.*?IN \(([^)]*)\)'` sobre el dispatcher— y que **la matriz de evasión sigue ejecutándose** (el gate se prueba a sí mismo contra textos sintéticos con una divergencia plantada; sin eso, el verde es indistinguible de un detector vacío). Este bloque debe verse **fallar en rojo** contra la base sin migrar, con los tres tipos de gasto faltando en los dos filtros.
- [x] 3.12 **RED confirmado**: los diez bloques nuevos + el (11) actualizado corridos contra la base **sin** la migración → todos fallan con su mensaje propio. Registrar cuál falló con qué mensaje: es la prueba de que el grupo 4 hizo algo.

## 4. Migración — el helper de mapeo y la rama contable del gasto

- [x] 4.1 Crear `supabase/migrations/20261043000001_asiento_contable_gastos.sql` (número de 1.3) con el encabezado documentado del repo: qué hace, por qué, idempotencia, ERRCODEs reutilizados (`P0450`/`P0451`, ninguno nuevo), y la lista de las cinco funciones reescritas con su md5 de partida.
- [x] 4.2 `CREATE OR REPLACE FUNCTION public._journal_expense_credit_account(p_kind text) RETURNS text LANGUAGE sql IMMUTABLE` — espejo literal de `_journal_sale_debit_account` **menos** el caso `credit`/`1300` (D4). `'1110'` para `transfer`/`card`/`check`/`wallet`; `'1100'` para el resto, incluidos `cash`, `other` y `NULL`. Comentario en el cuerpo explicando por qué el gasto no tiene rama `credit` (la rechaza en la puerta con `P0400`) y por qué el sin-imputar **no** va a `2100` (OQ-1).
- [x] 4.3 ACLs del helper nuevo **en el mismo archivo**: `REVOKE ALL ON FUNCTION ... FROM PUBLIC; REVOKE EXECUTE ... FROM anon, authenticated;` — postura idéntica a `_journal_sale_debit_account`, verificada en 1.6. Es lo que hace pasar el gate de ACLs.
- [x] 4.4 `CREATE OR REPLACE FUNCTION public._journal_post_from_event(...)` **partiendo del cuerpo vivo hasheado en 1.1**. Ampliar el filtro `v_event_type NOT IN (...)` de **once a catorce** sumando `ExpenseCreated`, `ExpenseAdjusted`, `ExpenseDeleted` — **sin tocar el anclaje de texto** del que depende el gate (3.11).
- [x] 4.5 Rama `ExpenseCreated`: `source_doc_type='Expense'`, `source_doc_ref` = el id del gasto; débito **una línea** a `5300` por el total con el `cost_center_id` del payload (D3, mismo trato que `5100` en `PurchaseCreated`); crédito **una línea** por el total a `_journal_expense_credit_account(payload->>'kind')`. **Sin línea `5200`**: el gasto no tiene desglose de IVA (Non-Goal declarado).
- [x] 4.6 `posted_at` de la rama de alta con el **idioma literal** de `SaleOperationCreated` (D5): `((v_payload->>'expense_date')::date + TIME '12:00:00') AT TIME ZONE 'America/Argentina/Mendoza'`, con degradado a `now()` si el payload no trae fecha. ⚠️ **El mediodía y la zona no son decorativos**: sin ellos el asiento corre un día en las franjas de borde.
- [x] 4.7 Rama `ExpenseAdjusted`, molde **byte a byte** de `SaleOperationAdjusted`: localizar el asiento vigente por `(source_doc_type='Expense', source_doc_ref, status='posted', account_id)`; contra-asiento con todos los lados invertidos y `reversal_of` apuntando al original, fechado en `now()`; marcar el original `reversed`; postear el asiento nuevo con los valores editados por el **mismo** mapeo de 4.5, fechado por la fecha de negocio. Si no encuentra el asiento vigente → `P0451` (evento `pending`, reintento) y **nunca** postear el nuevo solo: un ajuste sin su reversa duplica el gasto.
- [x] 4.8 Rama `ExpenseDeleted`, molde de `PurchaseDeleted`: contra-asiento **único** con lados invertidos, `cost_center_id` preservado **por línea**, `reversal_of` al original, `source_event_id` propio, fechado en `now()`; original a `reversed`. La localización **NO** lee `public.expenses` (el borrado es físico y la fila ya no existe cuando el relay corre) — assertado por el bloque (G).
- [x] 4.9 Verificar que las tres ramas entran **después** del reclamo de idempotencia `(event_id,'JournalEntry')` en `operation_idempotency`, igual que las once vivas (D13): no llevan idempotencia propia.
- [x] 4.10 **REFACTOR / control de no-regresión**: releer el diff completo del cuerpo del helper y confirmar que las **once ramas previas quedaron intactas** — mismo orden, mismas condiciones, mismos literales de cuenta. Diff mínimo esperado: el filtro ampliado + tres bloques `ELSIF` nuevos. Cualquier cambio fuera de eso es un hallazgo a reportar.
- [x] 4.11 GREEN parcial: correr los bloques (A)-(G) del gate nuevo contra la base con la migración aplicada hasta acá. Los bloques que dependen de los productores (grupo 5) todavía pueden estar en rojo — registrar cuáles y por qué.

## 5. Migración — los tres productores en las RPC de gasto

- [x] 5.1 `CREATE OR REPLACE FUNCTION public.rpc_create_expense(...)` desde el cuerpo vivo de 1.1, **conservando la firma posicional exacta de nueve parámetros** (el importador la invoca posicionalmente — D1). Sumar el `INSERT INTO public.events` **plano, sin `EXCEPTION`**, dentro de la misma transacción, al final del cuerpo y después de que los movimientos de caja/banco ya se resolvieron.
- [x] 5.2 Carga de `ExpenseCreated`: `account_id`, `expense_id`, `amount`, `expense_date` **como `date` pura** —nunca el `timestamptz` crudo de `expenses.date`, que se castearía en la zona de la sesión del relay (UTC) y correría el día (D5, ⚠️ nota de tipos)—, `cost_center_id` cuando esté imputado, y el `kind` derivado del catálogo **crudo, incluido `NULL`**: la carga reporta lo que ocurrió y la presunción vive en la rama del consumidor.
- [x] 5.3 `rpc_update_expense`: mismo `INSERT` plano de `ExpenseAdjusted`, **envuelto en el predicado de D7** — `IF EXISTS (SELECT 1 FROM public.events WHERE event_type='ExpenseCreated' AND aggregate_id = <expense_id> AND account_id = <account_id>) THEN … END IF`. Fecha del payload por `(v_expense.date)::date`. **No sumar el asiento a los guards `P0423`** (D6): los dos `EXISTS` de caja/banco del cuerpo vivo (líneas ~47-60) quedan **exactamente como están**.
- [x] 5.4 `rpc_delete_expense(uuid)`: `ExpenseDeleted` con el mismo predicado de D7, emitido **antes** del `DELETE FROM public.expenses` del final del cuerpo vivo (después la fila ya no existe para leer su `account_id`). Carga mínima: `account_id` + `expense_id`.
- [x] 5.5 TRIANGULAR: bloque (H) del gate en verde en sus **dos** direcciones — el gasto histórico no emite nada, y el gasto creado-y-borrado-en-el-mismo-minuto **sí** emite. Es el par que prueba que el predicado mira el evento y no el asiento.
- [x] 5.6 **Verificar que `rpc_import_expenses` NO se tocó**: su `md5(pg_get_functiondef)` idéntico al de 1.2, y un bloque del gate que importe un lote de 2 filas y confirme que **existen dos `ExpenseCreated`** emitidos por la delegación, sin una línea de código del importador. Es la prueba de D1 y de la afirmación del proposal sobre el change en vuelo.
- [x] 5.7 **REFACTOR / control de no-regresión** sobre las tres RPC: diff mínimo esperado = un `INSERT INTO public.events` (envuelto en `IF EXISTS` en dos de los tres). Confirmar que **ningún guard, ningún ERRCODE, ninguna resolución de tenant/sucursal/forma de pago y ningún camino de caja/banco cambió**. Cualquier otra diferencia es un hallazgo a reportar antes de seguir.

## 6. Migración — el filtro del despachador, e idempotencia del archivo

- [x] 6.1 `CREATE OR REPLACE FUNCTION public.rpc_process_outbox_dispatch(int)` desde el cuerpo vivo de 1.1: el filtro del **Consumer 3** pasa de once a catorce tipos, **enumerados idénticos** a los del helper (4.4) — mismo conjunto, mismo orden no importa, misma escritura. **Sin tocar la marca de comentario `Consumer 3: JournalEntry`**, que es el anclaje del gate (3.11).
- [x] 6.2 Confirmar que los Consumers 1, 2 y 4 quedaron **intactos** y que sigue habiendo **un solo despachador** (`test_outbox_single_dispatcher.sql` en verde — 2.5 es su baseline).
- [x] 6.3 GREEN: bloque (11) de `test_cobranzas_reverso.sql` en verde con los catorce tipos, **y su matriz de evasión ejecutada** (el comparador detecta la divergencia plantada). Un verde sin matriz no distingue "coinciden" de "el detector no encontró nada".
- [x] 6.4 **Idempotencia del archivo**: reaplicarlo **dos veces** consecutivas sobre la base ya migrada y confirmar `EXIT=0` en las dos, sin error y sin duplicar nada. Todo es `CREATE OR REPLACE` → no debería haber sorpresas, pero `productos-categoria-text-retiro` encontró exactamente ahí un fallo real de su propio TDD.
- [x] 6.5 `supabase db reset` local con la migración incluida: aplica **limpio desde cero**, en el orden real, conviviendo con `20261041000001` (importador) y con la que ocupe `20261042000001`. Si el número quedó tomado, renumerar según 1.3 y volver a correr.

## 7. Cableado de gates en CI

- [x] 7.1 `.github/workflows/KPI_Validation.yml`: sumar el paso del gate nuevo, `psql -v ON_ERROR_STOP=1 … -f supabase/tests/test_asiento_contable_gastos.sql`, con el comentario explicativo que el archivo usa para cada gate (qué cubre, por qué existe).
- [x] 7.2 **Cablear `supabase/tests/test_cobranzas_reverso.sql` entero** (D8, hallazgo (1) del propose): el invariante que este change amplía hoy lo custodia un archivo que **nunca corrió**. Se cablea completo y no sólo el bloque (11): los otros doce bloques son cobertura real del reverso de cobranzas que también debería estar corriendo, y separarlos deja la mitad del problema en pie.
- [x] 7.3 Verificar que el **cleanup** de `test_cobranzas_reverso.sql` no deja huérfanos al correr en CI (patrón conocido de `test_admin_kpis.sql`: `session_replication_role = replica` **no cascadea**). Si deja filas, reportarlo como hallazgo con su conteo; no silenciarlo.
- [x] 7.4 Ídem para el gate nuevo: su cleanup se assertea con un conteo explícito, no se asume.
- [x] 7.5 **Resolver OQ-6 con lo que 2.4 haya encontrado**: si apareció un rojo pre-existente en `test_cobranzas_reverso.sql`, arreglarlo **en este PR si es de una línea** (opción (a)); si no, reportarlo, abrir candidato y **cablear igual** (opción (b)). **No** dejar de cablear: sería dejar sin verificación real el invariante que este change amplía, que es justo lo que la spec prohíbe.
- [x] 7.6 Correr la tanda completa de gates SQL en el **orden real del workflow** contra una base reseteada y comparar contra el baseline de 2.3. Cero regresiones.

## 8. Backend — derivados del gasto y filtros del libro diario

- [x] 8.1 **SAFETY NET**: `pytest backend/tests/test_expenses.py backend/tests/test_journal_entries_endpoint.py` → baseline de este grupo (ya registrado en 2.1).
- [x] 8.2 RED: test que assertea `ExpenseOut.has_journal_entry` y `ExpenseOut.journal_pending` en `backend/tests/test_expenses.py`, con los tres estados posibles: asentado, pendiente (evento sin `processed_at`), sin asiento (histórico).
- [x] 8.3 GREEN: sumar los dos derivados a **`_EXPENSE_PROJECTION`** de `backend/repositories/expense_repository.py` con `EXISTS` en SQL, junto a los cuatro que ese archivo ya calcula así (D11). **Una sola definición**: la proyección es compartida por `get_by_id`, `list_paginated` y el re-`SELECT` de las mutaciones (verificado en 1.10) — no agregar una segunda consulta por fila ni derivarlo en el cliente.
- [x] 8.4 Sumar los dos campos a `ExpenseOut` en `backend/schemas/expenses.py`, junto a `is_payment_locked`/`has_cash_movement`/`has_bank_movement`, con el comentario que explica que son derivados de servidor.
- [x] 8.5 TRIANGULAR: test de que el gasto **histórico** (sin evento) sale con los dos derivados en `false`, y de que el gasto con evento **procesado** sale `has_journal_entry=true, journal_pending=false` — los dos estados no se confunden.
- [x] 8.6 RED: test de `GET /journal-entries` con filtros (`backend/tests/test_journal_entries_endpoint.py`): rango de fechas, `source_doc_type`, `source_doc_ref` y `status`.
- [x] 8.7 GREEN: sumar los cuatro filtros como `Query` opcionales en `backend/routers/journal_entries.py`, empujados por el service hasta el `WHERE` de `JournalEntryRepository`. **Extender el endpoint existente** (D10) — no crear uno paralelo. Conservar la envoltura `{items,total,page,pages}` y el `ORDER BY posted_at DESC, created_at DESC`.
- [x] 8.8 ⚠️ **Lección de `cobranzas-reverso` aplicada al repositorio de asientos**: `JournalEntryRepository` tiene **dos** caminos, `list_by_account` y `list_by_account_page`, con `_fetch_entries` compartido. Empujar los filtros al **`_fetch_entries` compartido** (y al `COUNT(*)` de la paginada, que hoy no lo usa) para que no exista una consulta con filtros y otra sin ellos. Si `list_by_account` resulta no tener consumidores, reportarlo como candidato de código muerto — no arreglarlo acá.
- [x] 8.9 TRIANGULAR: test de que la llamada **sin filtros** se comporta **exactamente como antes** (misma envoltura, mismo orden, mismo total) y de que **ninguna combinación de filtros cruza cuentas**.
- [x] 8.10 El endpoint sigue siendo **de sólo lectura** y **sin `service_role`**: confirmar que no aparece ningún camino de escritura ni ningún `get_service_conn` en el diff.

## 9. Frontend — el estado contable en `/gastos`

> Regla PO 2026-08-02: superficie frontend obligatoria. Este change **sí** produce algo que el usuario debe ver.

- [x] 9.1 **SAFETY NET**: correr los archivos de test de gastos del frontend y registrar sus conteos antes de tocar nada.
- [x] 9.2 Tipos en `frontend/lib/types.ts` (capa canónica, **sin `any`**): sumar `has_journal_entry` y `journal_pending` al tipo del gasto, con el comentario que los declara derivados del servidor.
- [x] 9.3 RED: test del indicador de estado contable con sus **tres** valores (asentado / pendiente / sin asiento), afirmando además el **tono**: pendiente y sin-asiento **no** usan el tono de error.
- [x] 9.4 GREEN: renderizar el estado en las **dos** renderizaciones que la página ya tiene (`frontend/app/(dashboard)/gastos/page.tsx`): el bloque de **tarjetas móviles** (`sm:hidden`, ~L347) y el **grid de escritorio** (`hidden sm:grid`, cabecera ~L295 con `grid-cols-[100px_140px_1fr_120px_80px]`). ⚠️ **Corrección al design**: D9 dice "tabla ~L349 / tarjetas ~L389" y el orden real es el inverso (móvil primero, escritorio después) — verificar contra el archivo, no contra el design. Si el grid gana una columna, ajustar `grid-cols-[…]` en **cabecera y filas**, que están declaradas por separado.
- [x] 9.5 Etiquetas con **tokens semánticos** y **cero colores literales** (`text-emerald-400`/`text-yellow-400` y familia están prohibidos por el gate `token-contrast-aa`). Textos: "Asentado" enlaza; "Pendiente" dice que se registra **en unos minutos**, no que falló; "Sin asiento" explica que el gasto es anterior y **no** aparenta un fallo.
- [x] 9.6 Desde el gasto asentado, enlace al libro diario **filtrado por ese gasto** (`source_doc_ref`) — es lo que hace que el filtro de 8.7 exista. Si OQ-4 se resolviera por (b), este enlace **debe** retirarse: un distintivo que promete un destino inexistente es peor que no tenerlo.
- [x] 9.7 Invalidación: las tres mutaciones de gasto ya invalidan las superficies de dinero (`useInvalidateExpenseLedgers`); confirmar que el listado se refresca para que el estado contable no quede congelado, **sin** sumar un polling nuevo (el posteo es asincrónico por diseño y la página no bloquea nada por él).
- [x] 9.8 Test de a11y en `frontend/__tests__/a11y/gastos.a11y.test.tsx`: el estado contable tiene nombre accesible y el enlace al diario es alcanzable por teclado.

## 10. Frontend — la pantalla del libro diario

> Hallazgo (2) del propose: `GET /journal-entries` existe completo desde `journal-entry-outbox` y tiene **cero consumidores** en el frontend — el anti-patrón textual que la regla del PO existe para prevenir.

- [x] 10.1 RED: test del hook de asientos (`frontend/hooks/data/…`) — payload de filtros exacto, envoltura paginada, clave de caché propia. Tipos en `lib/types.ts`, **sin `any`**.
- [x] 10.2 Ruta `/reportes/libro-diario` con lista de asientos: fecha, documento de origen, estado, y **líneas de débito/crédito expandibles**.
- [x] 10.3 Filtros por **rango de fechas**, **tipo de documento** y **estado**, más el modo "asientos de un documento" que consume el enlace de 9.6.
- [x] 10.4 Un asiento **revertido** se distingue de uno **vigente** sin necesidad de abrirlo (es lo que hace legible el par contra-asiento de una edición o un borrado).
- [x] 10.5 Entrada de menú en `frontend/components/app-sidebar.tsx`, **sin gate de plan** — mismo criterio que "Centros de costo" y "Formas de pago", que son lectura de datos que el propio usuario generó. ⚠️ **Corrección al design**: D9 habla del "grupo **Reportes**"; el grupo real donde viven esos dos ítems hoy se rotula **`Inteligencia`** (~L83-87). Usar el grupo real y no crear uno nuevo; si el PO quiere un grupo "Reportes", es una decisión de navegación aparte.
- [x] 10.6 La pantalla tolera el **desfase del relay** sin parecer rota (D1: una importación de 500 gastos se asienta en ~5 minutos, de a lotes de 100 por minuto). Estado vacío y estado "recién posteado" explicados, sin bloquear nada.
- [x] 10.7 Test de a11y de la pantalla nueva (tabla anunciada, filtros etiquetados, expansión de líneas operable por teclado).

## 11. Verificación por capa

- [x] 11.1 Backend: suite completa verde (2159/2159, 4 skipped, cov 98%) y cobertura ≥ el umbral de CI (87%). Comparado contra el baseline de 2.2 (2144/2144) — delta explicado por los tests nuevos de `test_asiento_contable_gastos.py` y las triangulaciones de `test_journal_entries_endpoint.py`.
- [x] 11.2 Frontend: suite completa **284/284 archivos, 2560/2560 tests, EXIT=0** (baseline 2.2: 281/2519 — delta +3 archivos/+41 tests, exactamente los nuevos de este change). Sin aserciones existentes modificadas del lado frontend, sólo agregadas.
- [x] 11.3 `pnpm exec tsc --noEmit` → 9 errores, **idéntico al baseline** (`__tests__/components/LedgerMovementsPanel.test.tsx`, `purchase-operations-list-payment-lock.test.tsx`, `sale-operations-list-payment-lock.test.tsx`, `use-critical-stock.test.ts`, `pos-payment-methods.test.tsx` ×4, `revenue-canon.test.ts`) — ninguno en un archivo tocado por este change. `next-env.d.ts` no se ensució (no se corrió `next dev`).
- [ ] 11.4 **[DESVÍO — instrucción del orquestador]** `supabase db reset` **NO se corrió**: otro agente corría gates en paralelo sobre la misma base local compartida y el orquestador indicó explícitamente no resetear en esta fase (el revisor hará el reset). En su lugar: los 60 gates wireados en `KPI_Validation.yml` se corrieron dos veces contra la base migrada tal cual estaba (antes y después de la migración) — 51/60 PASS + 9 pre-existentes ajenos (7 por contaminación de fixtures sin reset — `test_admin_kpis`, `test_pagos_cableados_restantes`, `test_pos_banco_movimientos`, `test_asiento_venta_formulario`, `test_delete_guard_ledgers`, `test_banco_caja_historial_ajustes`, `test_compras_proveedor_cuenta_corriente`; 2 por el drift de márgenes de `productos-costo-nullable`, migración en vuelo de otro change — `test_estadisticas_ventas`, `test_estadisticas_ventas_e3`), confirmados en el MISMO estado (ni mejoraron ni empeoraron) antes y después de aplicar esta migración. Dos gates SÍ rotos por este change (`test_function_acl_gate.sql`, bloque 3.10h de `test_gastos_forma_pago.sql`) se corrigieron en el mismo PR (ver 7.x/hallazgos). Queda sin marcar porque la task literal pide el reset — el revisor lo ejecutará.
- [x]/[~] 11.5 **Pasada visual — equivalente automatizado, sin captura de navegador real**: el dev-harness (`frontend/app/dev-harness/`) no cubre `/gastos` ni `/reportes/libro-diario` (páginas autenticadas, fuera de su alcance declarado — sólo popover/shell/bell/tablet-filters); levantar el stack completo con login real está fuera de lo que este apply puede hacer (Turnstile real, sin credenciales de prueba — mismo bloqueo que documentó `caja-compras-cobranzas`). Sustituido por: `token-contrast-aa.test.ts` 35/35 sin regresión (mismo conteo que el baseline del repo, cero violaciones nuevas); `ExpenseJournalStatusBadge` reutiliza LITERALMENTE el patrón de `ClientActivityBadge` (cva + `bg-success/15`/`text-success`/`bg-warning/15`/`text-warning`/`border-border`+`text-muted-foreground`), ya auditado visualmente en `tokens-contraste-aa` (#406-408, contraste ≥4,83:1); tests dedicados que assertean la AUSENCIA de literales Tailwind y el tono no-error de pendiente/sin-asiento (`ExpenseJournalStatusBadge.test.tsx`). El desborde horizontal a 375px se verificó por estructura (mismo patrón `overflow-x-auto` + `min-w-[...]` que `/reportes/formas-pago`, ya auditado), no por captura. **Queda como verificación real pendiente del humo del PO (14.1-14.3)**.
- [x] 11.6 **Humo local de punta a punta** — cubierto por el gate `test_asiento_contable_gastos.sql` (bloques B/C/D crean gasto en efectivo/4 kind bancarios/sin forma de pago y verifican los asientos con cuentas, importes, `posted_at` y balance; F edita uno sin dinero posteado y verifica el par contra-asiento/asiento nuevo; G borra otro y verifica su contra-asiento; H1 borra un histórico y confirma cero eventos pendientes) — EXIT=0, 16/16 bloques PASS (A, B, C-bancario, C-sin-fp, D, E, F, F-bis, F2, F3, G, H1, H2, I.1, I.2, J — F-bis/F2/F3 sumados en la revisión adversarial ronda 1).
- [x] 11.7 **Humo del importador**: lote de 2 filas vía `rpc_import_expenses` (usuario/cuenta real de la base local) → 2 `ExpenseCreated` emitidos, 2 asientos posteados tras el relay; `rpc_import_expenses` sin tocar (md5 idéntico, verificado en 1.2/5.6/13.4). Datos de la corrida limpiados al terminar.
- [x] 11.8 **Control de salud del outbox**: tras los humos (11.6 + 11.7), `SELECT event_type, count(*) FROM events WHERE event_type LIKE 'Expense%' AND processed_at IS NULL` → 0 filas; 10/10 eventos `Expense%` de la sesión quedaron `processed_at` no nulo.

## 12. Documentación, specs y cierre del propose

- [x] 12.1 `npx openspec validate --changes --strict` → 3/3 passed (`asiento-contable-gastos`, `importador-gastos-transaccional`, `productos-costo-nullable`, ninguno archivado todavía en este worktree). `npx openspec validate --specs --strict` → **98/98** confirmado durante el apply. El conteo al archivar lo confirma el orquestador en el momento (depende de qué otros changes hayan archivado antes).
- [x] 12.2 Entrada propia en `CHANGES.md` (después de `productos-categoria-text-retiro`, antes del separador `---`) con lo verificado, los hallazgos reales del apply (los dos gates rotos y corregidos, el desvío de `importador-gastos-transaccional` ya mergeado) y los candidatos/pendientes que deja (sin backfill OQ-3, PR/archive/post-merge pendientes del orquestador).
- [x] 12.3 Puntero actualizado en `CLAUDE.md` (ítem 26 + apertura de "Candidatos" corregida: `asiento-contable-gastos` es trabajo en curso, no un candidato) y `python scripts/ci/check_docs_sync.py --fix` corrido — `AGENTS.md` regenerado.
- [x] 12.4 Verificado contra el archivo real (grep en los 10 archivos de `knowledge-base/` + `modelo-dominio-aliadata-v3.md`): **no existe ningún lugar en la KB que documente el plan de cuentas ni la enumeración de `event_type` del outbox** — `journal_entries`/`journal_lines`/`public.events` no aparecen ni una vez en `04_modelo_de_datos.md`, y "5300"/"1100 Caja"/"partida doble" no aparecen en ningún archivo de la KB (los códigos sólo viven como literales en los comentarios de la migración, tal como el propio `design.md` §"Inventario de la superficie viva" ya documentó). Task condicional ("donde figure documentado") sin nada que editar — hallazgo reportado en el resumen final, no una KB nueva inventada fuera de este alcance.
- [x] 12.5 `CHANGES.md` actualizado: el candidato heredado de `gastos-forma-pago` D10 tachado con `~~...~~ → **resuelto por `asiento-contable-gastos`**`, y el candidato heredado de `cobranzas-catalogo-pagos`/`cobranzas-panel` sobre `test_cobranzas_reverso.sql` sin cablear ídem.
- [x] 12.6 Registradas en `CHANGES.md` (bullet dedicado) y en `design.md` (nueva sección "Resueltas por sign-off del PO") las seis OQs con su resolución — las seis por su recomendación, incluida OQ-3 (sin backfill, con la medición 176/208 sin forma de pago sobre la mesa).
- [ ] 12.7 **[Pendiente del orquestador]** Abrir PR desde la rama de apply y esperar los checks. Esta sesión no commitea/pushea/abre PR (regla dura del brief) — el orquestador toma el worktree con todos los archivos modificados y lo hace.

## 13. Verificación post-merge en producción (sólo lectura)

> **[Pendiente — fuera del alcance de este apply]** Este grupo requiere que la migración esté mergeada y desplegada en producción. Ninguna task de este grupo se marca desde el apply: el orquestador (o quien archive el change) las corre después del merge, contra `mcp__supabase__execute_sql` (read-only) sobre el proyecto real.

- [x] 13.1 `MAX(version)` en prod = la migración de 1.3, y el conteo total de migraciones esperado. **Verificado 2026-09-10 ~15:10 UTC**: `MAX(version) = 20261043000001`; 292 migraciones en `supabase_migrations.schema_migrations`.
- [x] 13.2 **Una sola definición viva** de cada una de las cinco funciones reescritas, **sin sobrecargas**, y `_journal_expense_credit_account` presente con su cuerpo esperado. **Verificado 2026-09-10**: 6 definiciones para las 6 funciones (`_journal_post_from_event`, `rpc_process_outbox_dispatch`, `rpc_create_expense`, `rpc_update_expense`, `rpc_delete_expense`, `_journal_expense_credit_account`) = una sola cada una, sin sobrecargas; el helper nuevo tiene `'1110'` para los bancarios y `ELSE '1100'`; el lookup del "asiento vigente" en `_journal_post_from_event` contiene `reversal_of IS NULL` 2 veces (ExpenseAdjusted y ExpenseDeleted — el blocker de la ronda 1 está en prod). El molde `_journal_sale_debit_account(text)` sigue intacto en prod (cuerpo: credit→1300, transfer/card/check/wallet→1110, else 1100; 354 chars, md5 en prod `acc4e7d76bfded788b54f8f3e578a074`) — el md5 `6c3abe80…` que registró el design/tasks 1.2 para ese molde era una medición local (CRLF), no una divergencia.
- [x] 13.3 ACLs vivas **idénticas al registro de 1.6**: las seis funciones sin `EXECUTE` para `anon`; `_journal_post_from_event`, `rpc_process_outbox_dispatch` y `_journal_expense_credit_account` **sin `authenticated`**; las tres RPC de gasto **con** `authenticated`. **Verificado 2026-09-10**: ACLs vivas idénticas al registro de 1.6: `_journal_expense_credit_account`, `_journal_post_from_event` y `rpc_process_outbox_dispatch` = `{postgres=X, service_role=X}` (sin `authenticated`, sin `anon`); `rpc_create_expense`, `rpc_update_expense`, `rpc_delete_expense` y `rpc_import_expenses` = `{postgres=X, authenticated=X, service_role=X}` (sin `anon`).
- [x] 13.4 `rpc_import_expenses` con su md5 de 1.2 **sin cambios** — la prueba de que el change compuso en vez de modificar. **Verificado 2026-09-10**: `md5(pg_get_functiondef('rpc_import_expenses'))` en prod = `21d323a502f914127454b1d96b21b167`, idéntico al de 1.2.
- [x] 13.5 Los **dos filtros** de `event_type` (helper y Consumer 3) con los **catorce** tipos, extraídos del cuerpo vivo de prod y comparados entre sí. **Verificado 2026-09-10**: el filtro de `_journal_post_from_event` enumera exactamente 14 tipos (CreditNoteIssued, ExpenseAdjusted, ExpenseCreated, ExpenseDeleted, PaymentMade, PaymentMadeReversed, PaymentReceived, PaymentReceivedReversed, PurchaseCreated, PurchaseDeleted, SaleConfirmed, SaleOperationAdjusted, SaleOperationCreated, SaleOperationDeleted); el cuerpo de `rpc_process_outbox_dispatch` contiene esos mismos 14 (Consumer 3) más `CashSessionClosed`, que pertenece al Consumer 4 (notificaciones), no al contable — sin diferencias entre los dos filtros contables.
- [x] 13.6 **Auditoría del "antes"**: cero asientos con `source_doc_type='Expense'` y cero eventos `Expense%` **anteriores** al despliegue (el control (d) de 1.7 re-medido). **Verificado 2026-09-10**: 0 asientos con `source_doc_type='Expense'` y 0 eventos `Expense%` antes del deploy (medido a las 14:5x UTC) y también inmediatamente después (15:10 UTC); outbox con 0 pendientes; 208 gastos en total.
- [x] 13.7 Tras el **primer gasto real**: un asiento con `source_doc_type='Expense'`, balanceado, con la cuenta de crédito correspondiente al `kind` y `posted_at` en el día del gasto. **Pendiente: requiere el primer gasto real.** → **Verificado en prod 2026-09-11 15:40 UTC**: los dos primeros gastos reales tras el deploy (ambos `kind='wallet'`, $326.000 con fecha 2026-09-10 y $15.738 con fecha 2026-09-07, cargados el 2026-09-11 14:25/14:28 UTC) produjeron cada uno un asiento `posted` balanceado `5300 débito / 1110 crédito` (cuenta de crédito correcta para `wallet`), con `posted_at` en la fecha de negocio del gasto (no en la del relay — D5) y el `ExpenseCreated` procesado dentro del minuto siguiente.
- [x] 13.8 **Salud del outbox en prod** a las 24 h: ningún evento `Expense%` acumulándose en `pending` (el modo de falla de D7 es silencioso por construcción — hay que ir a mirarlo). **Pendiente: requiere 24 h.** → **Verificado en prod 2026-09-11 15:40 UTC (24 h 35 min después del deploy)**: 0 eventos `Expense%` pendientes, 0 pendientes en total en el outbox, último `processed_at` 14:29 UTC (el último gasto), cron `relay-process-outbox` con última corrida exitosa 15:40 UTC.
- [x] 13.9 Verificar que el `pg_cron` del relay sigue activo y corriendo cada minuto (el change le suma tres tipos de trabajo; si el cron estuviera caído, nada de esto se notaría). **Verificado 2026-09-10**: `cron.job` `relay-process-outbox` con schedule `* * * * *`, `active=true`, última corrida exitosa 2026-09-10 15:06:00 UTC.

## 14. Humo real del PO y cierre

> **[Pendiente del PO]** Requiere al PO en vivo sobre producción desplegada. Se marcan cuando ocurra la demo real. La 14.5 (engram) ya se hizo desde la sesión de apply — ver el cierre del reporte.

- [x] 14.1 **Humo real con el PO en prod** — humo ejecutado en stack local con usuarios de prueba (autorización PO 2026-09-12); prod a criterio del PO. Ver `CHANGES.md` §"Humos locales de aceptación": gasto en efectivo ($1.500, opt-in de caja pre-tildado) y gasto por transferencia ($2.200, cuenta bancaria seleccionada) — ambos "Asentado" en `/gastos` tras procesar el outbox, asiento 5300 débito / 1100 o 1110 crédito verificado en `journal_entries`/`journal_lines` y `cash_movements`/`bank_movements`; el link "Asentado" arma la URL de `/reportes/libro-diario` filtrada por documento.
- [ ] 14.2 Con el PO: **editar** un gasto sin dinero posteado y ver en el diario el par contra-asiento + asiento nuevo; **borrar** un gasto asentado y ver su contra-asiento. Confirmar con él que el gasto **sigue siendo editable** (D6) y que eso es lo que espera. — verificado TÉCNICAMENTE en stack local con usuarios de prueba (autorización PO 2026-09-12): un gasto SIN movimiento real ($800, checkbox de caja destildado) sí muestra lápiz y produce el par contra-asiento (`reversed`) + asiento nuevo (`posted`) en el diario; borrar el gasto de $2.200 (transferencia) generó el contra-asiento + el movimiento bancario inverso (compensación completa, saldo final $0,00). **Pero** un gasto CON dinero real posteado (caja/banco) resultó INMUTABLE (candado + tooltip, guard `P0423` preexistente de `gastos-forma-pago` D6) — lo contrario de lo que el guion de este ítem asumía; comportamiento exactamente el documentado por D6, no una desviación. Queda `[ ]`: la cláusula "confirmar CON ÉL que eso es lo que espera" es un juicio de producto, no sólo técnico, y no hubo sesión con el PO — sólo autorización para probar en local. Ver `CHANGES.md` §"Humos locales de aceptación".
- [x] 14.3 Con el PO: abrir `/reportes/libro-diario`, filtrar por período y por tipo de documento, y confirmar que ahora el resultado del período **incluye los gastos** — que es el problema que el change vino a cerrar. — verificado end-to-end en stack local con usuarios de prueba (autorización PO 2026-09-12); prod a criterio del PO: `/reportes/libro-diario` filtra por documento (vía el link de `/gastos`) y por período/tipo con "Ver todos"; las líneas de cada asiento son expandibles (débito/crédito coinciden con `journal_lines`); el Tablero (`GASTOS HOY`/`GANANCIA NETA HOY`) y una suma manual sobre `journal_lines` (sin filtrar por `status`, porque cada par revertido/contra-asiento se cancela solo) confirman que los gastos vigentes entran al resultado del período. Ver `CHANGES.md` §"Humos locales de aceptación".
- [ ] 14.4 Confirmar con el PO la decisión de **OQ-3** con las mediciones de 1.7 sobre la mesa: si elige (b) —backfillear sólo los gastos que movieron dinero de verdad— es **aditivo y va en un change aparte**, emitiendo eventos con `source='backfill'` por la vía que la spec ya exige, **nunca** con `INSERT` directo a `journal_entries`.
- [x] 14.5 Guardado en engram el resultado del apply con `topic_key: "opsx/asiento-contable-gastos/apply"` (2026-09-10, project `v0-saa-s-empresarial-completo`).

## 15. Corrección de findings (revisor, ronda 1, 2026-09-10)

- [x] 15.1 **[blocker]** RED: ampliar el bloque **(F)** con una **segunda** `rpc_update_expense` sobre el mismo gasto (tras relay) y assertear que el 5300 neto vigente es el **último** importe y que hay exactamente **una** entry `posted`/`reversal_of IS NULL`; bloque nuevo **(F2)** alta→edición→borrado assertea 5300 neto vigente = **0**. Corridos contra la migración **sin el fix** → ambos fallan (contra-asiento equivocado revertido, neto duplicado/triplicado). GREEN: `AND reversal_of IS NULL` + `ORDER BY posted_at DESC, created_at DESC` en los dos lookups (`ExpenseAdjusted` L992, `ExpenseDeleted` L1102) → los dos bloques pasan.
- [x] 15.2 **[major]** RED: bloque nuevo **(F3)** — editar un gasto sin movimientos de dinero **a** una forma de pago bancaria y assertear que el asiento nuevo sigue acreditando `1100` (nunca `1110` sin `bank_movement` real que lo respalde) y que `bank_movements` del gasto sigue en cero tras la edición. Corrido contra la migración **sin el fix** → falla (acreditaba `1110`). GREEN: `v_final_kind` en `rpc_update_expense` se deriva de la existencia de `bank_movements` del gasto, no del `payment_method` elegido → el bloque pasa.
- [x] 15.3 **[minor]** RED: `backend/tests/test_asiento_contable_gastos.py::TestJournalEntryRepositoryTimezone` — assertea que `_FILTERS` compara `(posted_at AT TIME ZONE 'America/Argentina/Mendoza')::date`, no `posted_at` pelado. Corrido contra el archivo **sin el fix** → falla. GREEN: `_FILTERS` reescrito con `AT TIME ZONE` en los dos bordes del rango → pasa (15/15 del archivo, 17/17 de `test_journal_entries_endpoint.py`).
- [x] 15.4 **[nit]** Dos índices agregados a la misma migración (`idx_journal_entries_source_doc`, `idx_events_aggregate`) — costo despreciable hoy, sin política de retención sobre `events`/`journal_entries`.
- [x] 15.5 **[nit]** CLAUDE.md: ítem 26 movido arriba del blockquote "Pendientes externos del PO" que cerraba la lista 1-25; `python scripts/ci/check_docs_sync.py --fix` corrido en el mismo cambio.
- [x] 15.6 Decisiones documentadas en `design.md` §D14; los dos hallazgos de correctitud/dinero (blocker + major) aplicados según la recomendación del revisor, sin reabrir D6 (el gasto sigue editable) ni D4 (el mapeo kind→cuenta sigue siendo el mismo helper).

## 16. Corrección de findings (revisor, ronda 2, 2026-09-10)

- [x] 16.1 **[major]** `openspec/changes/asiento-contable-gastos/specs/journal-entry/spec.md`: las dos requirements (`ExpenseAdjusted`/`ExpenseDeleted`) sumaron `reversal_of IS NULL` a la tupla de búsqueda del "asiento vigente" (el código ya lo tenía desde 15.1; sólo la spec quedaba enseñando la clave defectuosa), un SHALL normativo ("a lo sumo un asiento vigente por gasto en todo momento") y dos scenarios nuevos (segunda edición del mismo gasto; alta→edición→borrado) que fijan en la spec lo que los bloques (F-bis)/(F2) del gate ya verifican en el código. Confirmado con `npx openspec validate --specs --strict` (98/98) y `--changes --strict` (3/3).
- [x] 16.2 **[nit]** Los dos predicados D7 (`rpc_update_expense` L1722, `rpc_delete_expense` L1928) sumaron `AND aggregate_type = 'Expense'` — `EXPLAIN` confirmó que antes el `EXISTS` sólo tocaba `aggregate_id` en el `Index Cond` de `idx_events_aggregate` (recorrido completo, no por prefijo); con el predicado nuevo el `Index Cond` cubre las dos columnas líderes del índice. Verificado con `EXPLAIN` contra la base local (antes/después) y con el gate `test_asiento_contable_gastos.sql` (16/16 bloques PASS tras el cambio).
- [x] 16.3 **[minor, decisión (b) del revisor — no corregido en código]** Residuo documentado: editar la forma de pago de un gasto a un `kind` bancario sin `bank_movements` reales deja el documento diciendo "Transferencia" mientras el asiento acredita `1100 Caja`. Se elige la opción (b) del revisor (documentar, no rechazar en `rpc_update_expense`) porque es consecuencia de una decisión de diseño ya firmada (D14/D6, ronda 1) y el asiento sigue siendo contablemente correcto — no hay `bank_movement`, no se acredita `1110`, ningún descuadre. Documentado en `design.md` §D15 y en `CHANGES.md` como candidato para que el PO lo confirme en el humo real (14.2).
- [x] 16.4 **[minor]** `CHANGES.md`: corregido "13 bloques A-J" → 16 bloques (con la lista completa incluyendo F-bis/F2/F3), sumado el bullet "Revisión adversarial (2 rondas)" documentando el blocker y el major de la ronda 1 y los cuatro hallazgos de la ronda 2, y sumado el candidato del residuo de 16.3. Mismo fix de "13 bloques" en `.github/workflows/KPI_Validation.yml` (comentario del step) y en `tasks.md` 11.6 (este archivo, arriba).
- [x] 16.5 **[nit]** `design.md` §D9 corregido: el grupo del sidebar es **Inteligencia**, no "Reportes" (ese grupo no existe en `app-sidebar.tsx`) — confirmado por grep contra el código (`app-sidebar.tsx:70` `label: "Inteligencia"`).
- [x] 16.6 **[nit]** `supabase/tests/test_gastos_forma_pago.sql` bloque (3.10h): el comentario prometía verificar el mapeo `5300`/`1110` pero el bloque sólo contaba asientos. Se agregaron dos `SELECT ... INTO v_debit/v_credit FROM journal_lines` con sus `IF` de account_code/amount. Gate corrido completo: 48 bloques PASS, EXIT=0.
- [x] 16.7 **[nit, informativo — sin acción de código]** La línea de la cadena de reaplicación de idempotencia en `KPI_Validation.yml` sigue después de `20261040000001`; el orquestador la reordena al rebasar contra `origin/main` (que ya tiene `20261041000001`/`20261042000001` en esa posición) — documentado en `design.md` §D15, no se reordena en esta corrección de findings porque no hay conflicto semántico y el rebase es responsabilidad del orquestador.
- [x] 16.8 Verificación final tras los 6 fixes: migración reaplicada ×2 (EXIT=0 ambas) contra la base local; gates `test_asiento_contable_gastos.sql` (16/16), `test_gastos_forma_pago.sql` (48/48), `test_cobranzas_reverso.sql` (12/12), `test_function_acl_gate.sql` y `test_outbox_single_dispatcher.sql` — todos EXIT=0; backend `pytest backend/` completo **2160 passed, 4 skipped, cov 98%**; frontend `pnpm vitest run` de los archivos tocados **73/73** y, dado que ningún archivo frontend se modificó en esta ronda, la suite completa también se corrió como regresión: **284/284 archivos, 2560/2560 tests** (idéntico al baseline pre-ronda-2); `tsc --noEmit` 9 errores baseline (ninguno en archivos tocados); `openspec validate --specs --strict` 98/98 y `--changes --strict` 3/3.
