# Tasks — ventas-unidades-conversion

> Apply 2026-09-24 (misma sesión que el propose, en la nube). Corrección post-revisión 2026-09-25 (sección 9, sesión local): la revisión adversarial del PR #584 encontró que varias verificaciones de abajo afirmaban más de lo que se había corrido — se corrigen en el lugar y se marca qué quedó superado. Lo que queda abierto está marcado `[ ]` con el motivo.

## 1. Base de datos — migración `20261062000001_ventas_unidades_conversion.sql`

- [x] 1.1 Verificar los cuerpos VIVOS de las **seis** funciones (`_c29_confirm_order_core`, `rpc_create_sale_operation`, `rpc_create_sale_operation_v2`, `rpc_create_purchase_operation`, `rpc_atomic_update_sale_operation`, `rpc_atomic_update_purchase_operation`) contra prod (sólo `SELECT`) y contra el stack local; registrar el md5 CR-stripped de cada uno y la firma vigente de `rpc_set_product_min_stock` en la cabecera de la migración. Verificación: el preflight (`v_expected` = md5 de partida medido en prod, `v_rewritten` = md5 exacto del cuerpo que la migración deja) ABORTA si el vivo no es ninguno de los dos. *Corrección 2026-09-25*: `v_expected` de `rpc_atomic_update_sale_operation` = `7c8c1b765ca669ad736e8fc181d471bb` (cuerpo de #585) y los otros cinco re-medidos en prod sin cambios; control negativo con el cuerpo sin fusionar → aborta (task 9.2). La versión inicial toleraba la reaplicación por "contiene la llamada al helper", reemplazada por el md5 exacto en 8.4.
- [x] 1.2 Helper `_uom_normalize_quantity(uuid, uuid, numeric) RETURNS numeric` (`STABLE`, `SECURITY INVOKER`, `SET search_path`) con los cuatro casos de D1/D3 y los tokens `P0404` / `P0400 unit_type_mismatch` / `P0400 unit_requires_base_unit` / `P0400 quantity_below_precision`; `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated`. Verificación: bloque (A) del gate, 12/12 casos; `has_function_privilege` en `false` para `anon`/`authenticated`.
- [x] 1.3 `rpc_create_sale_operation_v2` y `rpc_create_purchase_operation`: retirado el bloque `v_unit_factor`, `v_qty_norm` desde el helper, firma intacta. Verificación: diff de cada cuerpo contra el vivo limitado a las líneas previstas (generador con sustituciones de coincidencia única); `test_confirm_core_integrity.sql` en verde.
- [x] 1.4 `_c29_confirm_order_core`: `v_qty_norm` desde el helper en lugar de la cantidad cruda, sin tocar guards, caja, cuenta corriente, fiscal ni outbox. Verificación: bloque (B.2) del gate — `quickSale` de 450 g sobre un producto en kg con stock 10 deja `-0.45` (antes: `-450`).
- [x] 1.5 `rpc_atomic_update_sale_operation` y `rpc_atomic_update_purchase_operation`: pata de aplicación con la cantidad normalizada (también el gate global de stock de la edición de venta); pata de reversa con `-quantity_delta` del último movimiento de la fila vieja y fallback al helper (D6). Verificación: bloques (B.3) y (B.5) del gate — 450 g → 300 g registra `+0.45` y `-0.3`; 2000 g → 1500 g registra `-2` y `+1.5`.
- [x] 1.6 `branch_stock.min_stock` a `numeric(15,4)` (`USING`, `DEFAULT 0`), guardado equivalente para `products.min_stock` en local; `DROP VIEW` + `CREATE VIEW v_products_with_stock` (definición viva de prod, `security_invoker`, ACLs) y `DROP FUNCTION` + `CREATE` de `get_dashboard_critical_stock_items` (su `RETURNS TABLE` declaraba `min_stock integer`); `DROP FUNCTION rpc_set_product_min_stock(uuid, int)` + `CREATE OR REPLACE ... (uuid, numeric)` con el mismo cuerpo y los mismos `GRANT`. Verificación: `information_schema` reporta `numeric/4`; una sola firma de cada RPC; bloque (D) del gate 5/5.
- [x] 1.7 Gate de introspección embebido al final de la migración + **reparación guardada de un gap de historial** encontrada al reconstruir el stack: `products.base_unit_id` (FK + índice parcial) no existe en un stack creado desde las migraciones porque `20260509211504` es un stub aplicado vía MCP — la migración la crea si falta (no-op en prod). Verificación: `supabase db reset` limpio desde cero aplica las 307 migraciones (309 tras la corrección de 2026-09-25) y el gate embebido imprime PASS; reaplicar el archivo sobre la base ya migrada también pasa (reaplicación tolerante).
- [x] 1.8 **(D10, hallazgo del apply)** `v_products_with_stock` gana `base_unit_id` como última columna: la unidad base no viajaba por la API de FastAPI (ni la vista ni `ProductOut` la exponían). Verificación: aserción del gate embebido y del bloque (E); `GET /products` la devuelve (test backend).

## 2. Gate SQL y CI

- [x] 2.1 `supabase/tests/test_ventas_unidades_conversion.sql` con fixtures propias (cuenta por `handle_new_user`, siete unidades por cuenta, productos base-kg / base-g / sin-base / base-u, stock sembrado por `rpc_adjust_branch_stock` para que el invariante del ledger sea exacto) y cleanup completo (incluidas las `sales_orders`, cuyo `created_by` no cascadea desde `auth.users`); bloques (A) definición única 12 casos, (B) cinco caminos con 450 g sobre kg + base g + sin base, (C) tipo cruzado sin rastro en cada camino + borrado + invariante `SUM(quantity_delta) = branch_stock.quantity` con unidades mixtas. Verificación: `PASS (A) 12/12`, `PASS (B) 7/7`, `PASS (C) 7/7` sobre el stack local; control negativo documentado en el archivo.
- [x] 2.2 Bloque (D) de `min_stock`: `rpc_set_product_min_stock(producto, 0.5)` propaga `0.5`, una venta que baja a `0.4` inserta `low_branch_stock_alert` y emite `StockBelowMinimum`, `get_dashboard_critical_stock` y `get_dashboard_critical_stock_items` lo cuentan, el mínimo entero no cambia, el negativo se satura en 0, la vista expone `0.5`. Verificación: `PASS (D) 5/5`.
- [x] 2.3 Gate cableado en `.github/workflows/KPI_Validation.yml` (mismo formato `psql -v ON_ERROR_STOP=1 ... -f`). *Corrección 2026-09-25*: el apply afirmaba "83/83 PASS" corriendo los gates sueltos, **no el workflow**; en CI el paso "Verify G1/G4 migrations are idempotent on reapply" quedó en rojo por esta misma migración (run 36042552278) y los pasos siguientes —el gate nuevo y el de ACLs incluidos— no corrieron. La afirmación se retira; el paso se repara en 9.1 y el resultado vale sólo con el run de CI en verde (7.1).

## 3. Backend

- [x] 3.1 `backend/schemas/products.py`: `min_stock` a `Decimal` con `ge=0` en `ProductCreate`, `Decimal | None` en `ProductUpdate`; `backend/repositories/product_repository.py`: `_SET_MIN_STOCK_SQL` con `$2::numeric` y `_propagate_min_stock(..., Decimal)`. Verificación: `test_ventas_unidades_conversion_min_stock.py` (esquemas aceptan 0,5 y rechazan negativo; el repository manda `Decimal("0.5")` sin truncar; el entero sigue entero).
- [x] 3.2 Suites de productos existentes en verde con el tipo nuevo; tokens `unit_type_mismatch` / `unit_requires_base_unit` / `quantity_below_precision` traducidos en el frontend (`lib/operation-errors.ts`) — el backend ya propaga el `detail` RFC 7807 de las RPCs. Verificación: `pytest backend/tests -m "not integration" --cov=backend --cov-fail-under=87` → 2536 passed, cobertura 93,82 %.
- [x] 3.3 **(D10)** `base_unit_id` en `ProductCreate`/`ProductUpdate` (tri-estado por ausencia, `base_unit_provided` en el router) y `ProductOut`; `INSERT` con la columna; `_NULLABLE_ON_UPDATE` la incluye; guard de tenencia `unit_visible_to_account` → `422 base_unit_not_found`. Verificación: `test_ventas_unidades_conversion_base_unit.py` (lectura, fila sin la columna, alta persiste, unidad ajena 422, tri-estado del `PUT`).

## 4. Frontend — capa canónica

- [x] 4.1 `lib/unit-utils.ts`: `toBaseQuantity(displayQty, unit, productBaseUnit)` (división por el factor de la base + redondeo a 4 decimales), `compatibleUnits`, `isBaseUnit`, `isUnitCompatible`; `lib/cart-utils.ts` retira `unitFactor` (nadie lo leía) y documenta `quantityBase` como "en la unidad base del producto". Verificación: `__tests__/unit-utils-conversion.test.ts` (base kg + g → 0.45, base g + kg → 500, tn, docena, sin base sólo factor 1, tipo cruzado excluido, redondeo).
- [x] 4.2 `lib/operation-errors.ts`: mensajes accionables para los tres tokens, con el nombre del producto cuando hay lookup y acción "Editar producto" para `unit_requires_base_unit`. Verificación: `__tests__/operation-errors-units.test.ts` (5 casos).
- [x] 4.3 **(D10)** `hooks/data/use-products.ts` mapea `base_unit_id → baseUnitId` y lo envía en `POST`/`PUT` (`null` = sin unidad base). Verificación: `__tests__/hooks/use-products-base-unit.test.ts` (lectura, alta, edición).

## 5. Frontend — selectores de unidad

- [x] 5.1 POS (`app/(dashboard)/ventas/pos/page.tsx`): `productBaseUnit` + `unitOptions = compatibleUnits(...)`, base preseleccionada y cantidad al mínimo de la unidad al elegir producto, validación local con `toBaseQuantity(..., productBaseUnit)` en las tres rutas (alta, acumulación, edición de cantidad), "Sin unidad (base)" sólo sin unidad base. Verificación: `tsc` sin errores; suites del POS existentes en verde; la regla del selector se fija en `unit-utils-conversion.test.ts` y en el navegador real (6.4) — no se agregó un test de render del `Select` de Radix (su contenido vive en un portal que jsdom no abre).
- [x] 5.2 `components/forms/sale-form.tsx` y `components/forms/purchase-form.tsx`: mismos cambios sobre la misma función (incluida la cantidad inicial al mínimo de la unidad base al elegir producto en el formulario de venta). Verificación: `tsc`; suites de ambos formularios en verde; contrato del selector cubierto como en 5.1.
- [x] 5.3 Escaneo de código de barras: la línea toma la unidad base del producto y `toBaseQuantity(qty, baseUnit, baseUnit)` (identidad), sin `unitFactor`. Verificación: tests existentes del escáner en verde.

## 6. Frontend — la unidad en pantalla

- [x] 6.1 `app/(dashboard)/stock/page.tsx`: columnas de stock, mínimo y "a reponer" con `formatStock(…, símbolo de la unidad base)`; tarjeta móvil `0.550 / 0.500 kg`; sin "uds" fijo. `buildColumns` se exporta para el test. Verificación: `__tests__/stock-units-display.test.tsx` (kg → `0.550 kg` / `0.500 kg` / `0.600 kg`; sin unidad → `12 uds`).
- [x] 6.2 `components/stock/stock-movements-panel.tsx`: delta y antes/después con `formatQuantity` y el símbolo de la unidad base, resuelto por los mapas cacheados de productos y unidades. Verificación: mismo test (`-0.450 kg`, `1 kg → 0.550 kg`, `+3` sin unidad).
- [x] 6.3 `lib/receipt.ts` + `components/ventas/sale-receipt-button.tsx`: `ReceiptOptions.unitSymbolFor`, cantidad con unidad en HTML, texto para WhatsApp y payload del PDF (el backend imprime el string tal cual). Verificación: `__tests__/lib/receipt.test.ts` (450 g / 1.250 kg / 3; sin lookup no inventa unidad); test del botón ajustado con el mock del hook de unidades.
- [x] 6.4 **(ampliada 2026-09-25) — 17 passed reales** (`playwright --project=harness unidades-visual`, Chromium real, corrida de la tercera revisión 2026-09-25: `17 passed (1.3m)`, con el código final). Verificación visual en desktop (1366) y mobile (375), claro y oscuro, sobre `/dev-harness/unidades` (columnas reales de `/stock`, filas reales del historial y `SaleForm` real con catálogo sintético) vía `e2e/harness/unidades-visual.spec.ts`. Verificación: **9/9** en el navegador real (Chromium del contenedor): `/stock` muestra `0.550 kg` / `12 uds` en la columna y `0.550 / 0.500 kg` / `12 / 30 uds` en la tarjeta móvil sin desborde horizontal, el historial `-0.450 kg` / `+2 kg` / `+0.450 kg`, el selector de un producto en kg ofrece SÓLO g/kg/tn y el de un producto sin unidad base SÓLO unidades base (factor 1); capturas revisadas en las cuatro combinaciones. Ajuste de la corrida: la aserción de móvil buscaba la celda de escritorio (oculta por debajo de `md`) — se corrigió la aserción, no el render. *Corrección 2026-09-25*: el proposal prometía también el POS y el formulario de compra, que no tenían pasada; el arnés suma las vistas `pos` y `purchase` (8 casos nuevos: 2 vistas × 1366/375 × claro/oscuro, mismo `verificarSelectorKg`). *Segunda revisión (2026-09-25)*: **17/17 PASS** en Chromium real (`playwright --project=harness unidades-visual`, 1,2 min), con la aserción nueva del importe de la línea (100 g → $180 en venta y POS, $90 de costo en compra) en las 12 corridas de los tres caminos; capturas revisadas.

## 7. Integración, documentación y cierre

- [ ] 7.1 Corrida completa: el apply la dio por hecha con `db reset` (307 migraciones), "83/83 gates", `pytest` 2536 passed y `vitest`, pero los gates se corrieron sueltos y no el workflow (ver 2.3). Se reabre: vale el run de CI de `validate-kpis`, `Backend_Tests`, `Frontend_Tests` y `E2E_Tests` sobre el commit final de la corrección, en verde (a completar con el run de CI).
- [x] 7.2 `CHANGES.md` (entrada del change con el apply, D10, el movimiento dañado `cf4550c0-…` y los candidatos que quedan), `knowledge-base/05_reglas_de_negocio.md` (RN-24 reescrita: unidad base del producto, mismo tipo, sin base sólo factor 1, reversa por delta guardado; RN-23 con el umbral fraccionario), `CLAUDE.md` ítem 26 + `python scripts/ci/check_docs_sync.py --fix` (AGENTS.md regenerado). Verificación: `check_docs_sync.py` OK. *Corrección 2026-09-25*: ítem 26 acortado a hechos verificables, cifras no verificadas retiradas de `CHANGES.md`, RN-24 con la regla D11 y el dato real de variantes; `AGENTS.md` se regenera en el commit de la corrección.
- [ ] 7.3 Verificación post-merge en prod (sólo `SELECT`). Pendiente hasta el merge y el deploy. *Cuarta revisión*: la lista original nombraba sólo lo que creó el primer apply; ahora cubre todo lo que la migración crea o reescribe (resultado esperado en cada comentario):

  ```sql
  -- 1) versión y los diez cuerpos: una definición cada uno, md5 = v_rewritten de la cabecera
  SELECT max(version), count(*) FROM supabase_migrations.schema_migrations;          -- 20261062000001 / 309
  SELECT p.proname, count(*) AS defs, max(md5(replace(p.prosrc, chr(13), ''))) AS md5
  FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
  WHERE n.nspname = 'public' AND p.proname IN (
    '_c29_confirm_order_core', 'rpc_create_sale_operation_v2', 'rpc_create_purchase_operation',
    'rpc_atomic_update_sale_operation', 'rpc_atomic_update_purchase_operation', 'rpc_create_sale_operation',
    'reporting_sales_lines_in_window', 'rpc_dashboard_kpi_summary', 'rpc_dashboard_channel_margin', 'check_low_margin')
  GROUP BY 1 ORDER BY 1;                                                              -- 10 filas, defs = 1
  -- 2) ACLs: todo false
  SELECT f, has_function_privilege('anon', f, 'EXECUTE') AS anon, has_function_privilege('authenticated', f, 'EXECUTE') AS auth
  FROM unnest(ARRAY['public._uom_normalize_quantity(uuid,uuid,numeric)', 'public._uom_quantity_for_reporting(uuid,uuid,numeric)',
                    'public.fn_product_base_unit_guard()', 'public.fn_uom_in_use_guard()']) AS f;
  -- 3) triggers
  SELECT tgname, pg_get_triggerdef(oid) FROM pg_trigger
  WHERE tgname IN ('trg_product_base_unit_guard', 'trg_uom_in_use_guard');           -- INSERT OR DELETE OR UPDATE OF base_unit_id, account_id, parent_id / UPDATE OF factor, type, base_unit_id
  -- 4) D-F′: precio de línea sin escala
  SELECT table_name, column_name, numeric_scale FROM information_schema.columns
  WHERE table_schema = 'public' AND column_name = 'price' AND table_name IN ('sales_order_items', 'quote_items');   -- numeric_scale NULL
  -- 5) COMMENTs: todo true
  SELECT f, obj_description(f::regprocedure, 'pg_proc') IS NOT NULL
  FROM unnest(ARRAY['public.check_low_margin()', 'public.get_dashboard_critical_stock_items(uuid,integer)',
                    'public.rpc_set_product_min_stock(uuid,numeric)', 'public.fn_product_base_unit_guard()',
                    'public.fn_uom_in_use_guard()', 'public._uom_normalize_quantity(uuid,uuid,numeric)',
                    'public._uom_quantity_for_reporting(uuid,uuid,numeric)']) AS f;
  -- 6) min_stock, firma única (riesgo D-E) y la vista
  SELECT data_type, numeric_scale FROM information_schema.columns
  WHERE table_schema = 'public' AND table_name = 'branch_stock' AND column_name = 'min_stock';                   -- numeric / 4
  SELECT pg_get_function_identity_arguments(oid) FROM pg_proc WHERE proname = 'rpc_set_product_min_stock';     -- una fila: p_product_id uuid, p_min_stock numeric
  SELECT count(*) FROM information_schema.columns
  WHERE table_schema = 'public' AND table_name = 'v_products_with_stock' AND column_name = 'base_unit_id';      -- 1
  ```
- [ ] 7.4 Humo real del PO en prod: producto en kg con stock 1, venta de 450 g desde el POS (también con Docena) y desde el formulario, edición a 300 g, borrado, `/stock` desktop y mobile en `0.550 kg`, mínimo `0.5` con alerta, cambio de unidad base de un producto con stock rechazado (D11); decisiones 2-9 del PO (`design.md` §"Sign-off del PO"), incluidos el ajuste manual del movimiento dañado y OQ-1. Pendiente hasta el deploy.

## 8. Auditoría post-apply (2026-09-24, PR #584)

- [x] 8.1 Variantes heredan la unidad base del padre en el helper y en `v_products_with_stock` (163 variantes afectadas en prod). Verificación: gate A.13 (450 g sobre la variante → 0.45), A.14 (tipo cruzado rechazado), B.6 (venta por el formulario), E (introspección de vista y helper).
- [x] 8.2 Sexto cuerpo: la rama legacy del kill-switch `sale_items_rpc_v2=false` de `rpc_create_sale_operation` pasa por el helper (md5 de partida verificado contra prod). Verificación: gate B.7 con el flag apagado para la cuenta; lista de introspección de los dos gates con seis nombres.
- [x] 8.3 Guard de tenencia de la unidad de la línea en el helper (`P0404`). Verificación: gate A.15 (unidad con `account_id NULL` y no del sistema).
- [x] 8.4 Preflight de reaplicación por md5 exacto del cuerpo nuevo (seis). Verificación: `db reset` desde cero + reaplicación del archivo sobre la base migrada.
- [x] 8.5 Cleanup del gate sin residuos (`units_of_measure`, `payment_methods`, `product_categories`, `audit_logs`). Verificación: conteo de filas huérfanas por `account_id` inexistente = 0 tras dos corridas.
- [x] 8.6 `PUT /products` tri-estado por ausencia en el hook y `undefined` desde el formulario para variante / padre `variant_only` / no rastreado. Verificación: `__tests__/hooks/use-products-base-unit.test.ts`.
- [x] 8.7 El catálogo matchea `/productos?q=<uuid>` (acción "Editar producto"). Verificación: `tsc`; `operation-errors-units.test.ts` sigue fijando el href.
- [x] 8.8 Acumulación por escaneo con la base del producto; `compatibleUnits` delega en `isUnitCompatible`; tokens semánticos en el delta del historial; `buildMobileCard` exportada y montada por el arnés; test del guard de tenencia del backend fija `(unit_id, account_id)`. Verificación: `vitest` + `pytest` + pasada visual 9/9.

## 9. Corrección post-revisión (2026-09-25, PR #584)

> La revisión adversarial (2026-09-24, 37 hallazgos confirmados, 8 bloqueantes) declaró el PR no mergeable. El PO ordenó corregirlo en una sesión local. Decisiones de la corrección: D-A renumeración, D-B fusión en 3 vías, D-C guard de unidad base (provisorio), D-D sin cambios de datos en prod, D-E ventana de deploy documentada (`design.md`).

- [x] 9.1 CI: en la cadena de reaplicación de `KPI_Validation.yml`, `20261039000001` y `20261040000001` se reaplican con tolerancia acotada (sólo *cannot change return type of existing function* sobre `get_dashboard_critical_stock_items`, y *cannot drop columns from view* con el archivo anclado a un único `CREATE OR REPLACE VIEW public.v_products_with_stock`; cualquier otro error → `exit 1`), y `20261062000001` se reaplica al final, sin tolerancia: 6/6 por la rama de reaplicación, gate embebido PASS y schema idéntico. Verificación: arnés con dobles de `psql`/`supabase` que ejecuta el paso extraído del YAML — el paso viejo falla como en CI; el nuevo pasa, y 7 escenarios negativos fallan cada uno por su motivo. El run real de CI queda para 7.1.
- [x] 9.2 Reconciliación con #585: migración renumerada a `20261062000001` sobre `main` (`57791247`); `rpc_atomic_update_sale_operation` = fusión en 3 vías (0 conflictos); `v_expected`/`v_rewritten` reconciliados. Verificación: `db reset` limpio ×3 (309 migraciones), reaplicación con 6 NOTICE + gate embebido PASS, gates de #585 y #582 en verde sobre el cuerpo fusionado; con el cuerpo sin fusionar, `test_facturar_venta_manual.sql` falla y la migración aborta en el preflight.
- [x] 9.3 `/stock` en prod: `use-products.ts` convierte `min_stock` (string decimal de FastAPI) con `Number()`; `lib/format-unit.ts` se defiende de un string. Verificación: `stock-min-stock-decimal-string.test.tsx` y `format-unit-string-defense.test.ts` (fallaban con el mismo `TypeError` de prod).
- [x] 9.4 `COMMENT ON FUNCTION` vivos de `get_dashboard_critical_stock_items` y `rpc_set_product_min_stock` re-emitidos. Verificación: bloque (E) del gate y gate embebido (mutante sin comentarios → falla).
- [x] 9.5 Gate: (F) unidades de sistema, (G) unidad de otra cuenta real vía `handle_new_user`, (H) residuo cero asertado en nueve tablas. Verificación: `PASS (F) 4/4`, `PASS (G) 2/2`, `PASS (H) 9/9`; cada bloque con su mutante en rojo.
- [x] 9.6 Backend D11 (D-C, provisorio): `PUT /products` rechaza cambiar o quitar la unidad base con stock o movimientos (`409 base_unit_locked`). Verificación: `test_ventas_unidades_conversion_base_unit_lock.py`.
- [x] 9.7 Frontend: `toBaseQuantity` sin unidad = espejo del helper; unidad base del formulario de producto sólo con control de stock; inputs de stock y mínimo con `step="any"`; toast de stock insuficiente (venta y POS) con el símbolo de la unidad base; comentarios del importador. Verificación: tests de la tabla de abajo.
- [x] 9.8 Documentación: `design.md` (sign-off pendiente, D1/D4/D7/D9/D10 corregidos, D11, riesgos CI y D-E, OQ-1), `proposal.md` (seis cuerpos, Impact = diff), este archivo, `CHANGES.md`, `CLAUDE.md` ítem 26, RN-24.

## 10. Segunda revisión (2026-09-25, sobre `main` `a7d835ea`)

> Una segunda revisión adversarial (red team A y B) encontró que el importe de la línea ignoraba la conversión, que el reporting costeaba la cantidad cruda, y huecos de test (D6, helper) y de guard (unidad base sólo en FastAPI). Rama rebaseada sobre `main` `a7d835ea` (#586 y #587; conflicto de `CHANGES.md` resuelto conservando las tres entradas).

- [x] 10.1 Contrato de precio D-F (D12): `convertUnitPrice` en `lib/unit-utils.ts`; POS, venta y compra re-expresan el precio/costo al cambiar la unidad; aviso "Cat." re-expresado. Verificación: 4 archivos de test nuevos (RED → GREEN), gate I.1/I.2, arnés visual 17/17.
- [x] 10.2 Edición fraccionaria: mínimo, paso y símbolo de las líneas rehidratadas desde su unidad o la base del producto (venta y compra). Verificación: tests de edición fraccionaria.
- [x] 10.3 `product-form.tsx`: `minStock ?? 10` (un mínimo 0 ya no se guarda como 10). Verificación: `product-form-min-stock-zero.test.tsx`.
- [x] 10.4 Reporting en unidad base (D13): `_uom_quantity_for_reporting` + `reporting_sales_lines_in_window`, `rpc_dashboard_kpi_summary` y `rpc_dashboard_channel_margin` desde el cuerpo vivo; preflight de nueve funciones con mensaje por rama; `KPI_Validation.yml` espera 9/9 (10/10 desde 11.3). Verificación: gate (I), `db reset` limpio (309 migraciones), reaplicación directa (0 reescritos / 9 reaplicados + gate embebido PASS), control negativo del preflight (un cuerpo intermedio → aborta), paso de reaplicación del workflow `EXIT_CODE=0`, 85/85 gates de `KPI_Validation.yml`.
- [x] 10.5 Gate (J): D6 distinguible (delta legacy -450 / +2000, fallback sin movimiento).
- [x] 10.6 Helper: factor ≤ 0 de la unidad de la línea → `P0400 unit_factor_invalid`; gate (K) fija 4 decimales y base propia de variante.
- [x] 10.7 `trg_product_base_unit_guard` (D11 en la tabla): P0409/P0404 para cualquier escritor. Verificación: gate (L), PostgREST incluido; backend: variante heredada y P0409 de la base → 409. *Corrección de la tercera revisión*: acá decía "carrera cerrada" y no lo estaba — el trigger cerraba un lado, pero tres de los seis caminos normalizaban antes del `FOR UPDATE` (reproducido con dos conexiones); se cierra en 11.2.
- [x] 10.8 Numeración: `20261062000001` la toma este PR; los artefactos de `punto-venta-seleccion` (#586) pasan a `20261063000001` y `factura-fiscal-imprimible` (#587) a `≥ 20261064000001`.

## 11. Tercera revisión (2026-09-25, rama `claude/nice-noether-rrkgyh` sobre `eac71d2d`)

> El red team 2 dejó la rama en `mergeable=false`: 1 mayor (precisión del precio en POS y formularios) + menores (TOCTOU en tres caminos, `check_low_margin`, bytes CR, specs sin D12/D13/D11, CI sin correr) + nits (re-parent, textos). Todos cerrados con TDD; evidencia en la sesión (`fix-584/fix-round-2/`).

- [x] 11.1 **Bytes CR literales**: los tres `E'<CR>'` de la migración y el gate pasan a `E'\r'` y los dos archivos a LF (`git ls-files --eol` → `i/lf`; `git diff --numstat` los trata como texto). Verificación: `db reset` (309) con los nueve md5 iguales a `v_rewritten` y ningún cuerpo reescrito con `\r` en `prosrc`.
- [x] 11.2 **TOCTOU**: `rpc_create_sale_operation_v2`, `rpc_create_purchase_operation` y la rama legacy de `rpc_create_sale_operation` normalizan después del `SELECT … FROM products … FOR UPDATE` (la línea sin producto sigue validando la unidad, `P0404`); `v_rewritten` re-medido. Verificación: `supabase/tests/test_ventas_unidades_conversion_race.sh` (cableado en `KPI_Validation.yml`) — (a) cambio de unidad abierto vs compra → `P0400`, (b) compra abierta vs cambio → `P0409`, (c) cambio del padre vs compra de la variante → `P0400`; introspección (E) y gate embebido: normalización después del lock en los seis caminos.
- [x] 11.3 **`check_low_margin`** reescrito desde el cuerpo vivo (md5 `a03b3126…`): importe de la línea contra costo × cantidad en unidad base; décimo cuerpo del preflight (COMMENT nuevo, ACL conservada); `KPI_Validation.yml` espera 10/10. Verificación: gate (N) 5/5.
- [x] 11.4 **D-F′** (decisión provisoria, fila 11 del sign-off): `sales_order_items.price` y `quote_items.price` a `numeric` sin escala (sin reescritura: relfilenode igual); `roundUnitPrice` en `lib/cart-utils.ts` (15 dígitos significativos) para `convertUnitPrice` y `unitPriceFromSubtotal`. Verificación: gate (M) 6/6 ($4,575/g × 100 g = $457,50 en POS, formulario, compra y edición; $1,23456/g sin redondear; docenas), 10 tests de frontend RED → GREEN, arnés visual 17/17.
- [x] 11.5 **Trigger con `parent_id`**: re-parentar o desenganchar una variante que hereda con stock → `P0409`; sin cambio efectivo se permite. Verificación: gate L.8-L.12 (PostgREST incluido).
- [x] 11.6 **Specs delta**: `units-of-measure` (+2 requirements: precio por unidad de la línea sin redondear; unidad base efectiva bloqueada, con la carrera) y `reporting-invariants` (nueva, +2: reporting en unidad base; alerta de margen). Verificación: `openspec validate ventas-unidades-conversion --strict` OK.
- [x] 11.7 Textos: comentario del paso del gate en `KPI_Validation.yml` (A-N) y paso nuevo del arnés de carrera; `CHANGES.md` (número de `punto-venta-seleccion`, candidato del subtotal del POS, entrada de la tercera revisión), `design.md` (D11, D12/D-F′, D13, sección "Tercera revisión", riesgos, sign-off 11-12), `CLAUDE.md` ítem 26 + `check_docs_sync.py --fix`, comentario de `backend/services/products.py`.
- [x] 11.8 Verificación local completa (2026-09-25): `db reset` 309; los 93 pasos de `validate-kpis` en orden con el `run` literal del YAML → 92 OK + `test_facturar_venta_manual_race.sh` 20/20 re-corrido solo (en la corrida completa falló sólo la iteración 6, por `statement_timeout` bajo la carga de `vitest` en paralelo); `pytest` 2563 passed / 1 skipped / 7 deselected (97,77 %); `vitest` 386 archivos / 3585 tests; `tsc` 8 errores = `main`; arnés visual 17/17; `openspec validate --all --strict` 109/109.
- [ ] 11.9 Run real de CI (`validate-kpis` completo, con el paso de reaplicación 10/10, el gate y el arnés de carrera) sobre el commit final: lo hace la fase de push (7.1).

## 12. Cuarta revisión (2026-09-25, rama `claude/nice-noether-rrkgyh` sobre `8bd9250b`)

> El red team 3 (dos lentes) dejó la rama en `mergeable=false`: 1 mayor (asignar la unidad base sobre historia en otra unidad) + 8 menores (listados/exports sin unidad y precio unitario cortado, `ai-precio`/`fair-advisor` fuera de D13, total de dinero redondeado línea a línea, DELETE del padre/write skew/importador, PR body, checklist post-merge, KB) + 7 nits. Lo que toca código, cerrado con TDD; evidencia en la sesión (`fix-584/fix-round-3/`).

- [x] 12.1 **Asignar la unidad base sobre historia en otra unidad** (mayor): `trg_product_base_unit_guard` rechaza con `P0409` asignar (propia, al padre de variantes que heredan o por re-parent) si el grupo tiene stock o movimientos y líneas con otra unidad explícita; `PUT /products` lo espeja (`has_lines_in_other_unit`). Verificación: gate L.13-L.20 (historia kg → `g` y `u` → `kg` rechazados, también por PostgREST; kg → `kg` y sin unidad → `kg` permitidos; padre y re-parent); backend (i)-(k).
- [x] 12.2 **DELETE físico del padre, write skew del re-parent e importador**: el trigger dispara en `DELETE` (GUC local hacia la acción `ON DELETE SET NULL`) y toma `FOR SHARE` el padre nuevo. Verificación: gate L.21-L.24 (DELETE por PostgREST → `P0409` y la variante conserva el padre; borrado conjunto permitido; padre sin base permitido; importador → error de fila 7); arnés de carrera (d) y (e) → `P0409`, rojos contra el cuerpo de la tercera revisión.
- [x] 12.3 **Total de dinero redondeado una vez** en los cuatro cuerpos que lo acumulan; `v_rewritten` re-medido para los cuatro. Verificación: gate (P) 5/5 (venta a crédito v2 y legacy, compra a crédito, edición sin cambios → 665,33; control al centavo 1998); introspección (E) y gate embebido.
- [x] 12.4 **Unidad en uso inmutable**: `trg_uom_in_use_guard` (`P0409 unit_in_use`). Verificación: gate (O) 7/7 (base de productos, líneas, tipo, base, PostgREST; renombrar y corregir una unidad sin uso se permiten).
- [x] 12.5 **Listados, CSV, ticket y PDF**: `formatUnitPrice` (`lib/format.ts`), símbolo de la línea y columna "Unidad" en los listados de ventas y compras (unidades por prop desde la página), ticket HTML y `_format_unit_price` en el PDF. Verificación: `format-unit-price.test.ts`, `operations-list-line-units.test.tsx`, `receipt-unit-price.test.ts`, `test_receipts_unit_price_precision.py`.
- [x] 12.6 **Precio con descuento sin ruido binario** (`use-sales.ts`, alta y edición). Verificación: `use-sales-discount-price-precision.test.ts`.
- [x] 12.7 **Gate**: la introspección de D-F′ cuenta las seis columnas y ubica lock y helper con `regexp_instr` (también en el gate embebido).
- [x] 12.8 **Docs**: 7.3 con las consultas; `design.md` (D9, D11, D12, D13, "Cuarta revisión", riesgos con la regla del bloque de reaplicación, Migration Plan §5, sign-off en orden con las filas 13 y 14); specs delta (`units-of-measure`: asignación, DELETE, importador, carrera del re-parent, excepción del fallback de la reversa, unidad en uso, total redondeado una vez, listados/ticket; `reporting-invariants`: consumidores pendientes); KB 04/05/06; `KPI_Validation.yml` (comentarios del reapply 10/10 con la regla de retiro, gate A-P, arnés (a)-(e)); `CHANGES.md` (entrada de la cuarta revisión, candidatos, regla); `CLAUDE.md` ítem 26 + `check_docs_sync.py --fix`; `proposal.md` (governance).
- [x] 12.9 Verificación local completa (2026-09-25): `db reset` 309 con los diez md5 = `v_rewritten`; los pasos de `validate-kpis` en orden con el `run` literal del YAML → **92 OK / 0 FAIL** (reaplicación 10/10 + gate embebido PASS + schema idéntico, gate A-P, arnés (a)-(e), ACLs); `test_facturar_venta_manual_race.sh` aparte con `ITER=3` → nueve casos 3/3; `pytest` 2570 passed / 1 skipped / 7 deselected (97,78 %); `vitest` 390 archivos / 3601 tests (3599 + 2 timeouts bajo carga en archivos no tocados, verdes solos); `tsc` 8 errores = `main`; `openspec validate --all --strict` 109/109; `check_docs_sync.py` OK. El run real de CI sobre el head final es de la fase de push (11.9).
- [ ] 12.10 Sin cerrar en código, anotados como candidatos en `CHANGES.md`: la columna de unidad en `generate-export`, `ai-precio`/`fair-advisor` fuera de D13 y el precio sub-centavo en el carrito. El PR body y el run real de CI sobre el head final son de la fase de push (7.1, 11.9).

### TDD Cycle Evidence

Las filas del apply original (2026-09-24) no tienen RED registrado: la sesión de la nube escribió tests y código en el mismo commit. Se dejan explícitas para que la ausencia sea visible y no se lean como TDD. Las de la corrección (2026-09-25) tienen su salida RED/GREEN guardada en la evidencia de la sesión.

| Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
|------|-----------|-------|------------|-----|-------|-------------|----------|
| 1.2-1.5, 8.1-8.3 (apply) | `supabase/tests/test_ventas_unidades_conversion.sql` (A-E) | SQL gate | — | ⚠️ no registrado (mismo commit) | ✅ A 15/15, B 9/9, C 7/7, D 5/5, E | ✅ matriz kg/g/sin base/variante/legacy | — |
| 3.x, 8.6 (apply) | `backend/tests/test_ventas_unidades_conversion_min_stock.py`, `…_base_unit.py` | Unit | — | ⚠️ no registrado | ✅ | ✅ | — |
| 4.x-6.3, 8.6-8.8 (apply) | `unit-utils-conversion.test.ts`, `operation-errors-units.test.ts`, `use-products-base-unit.test.ts`, `stock-units-display.test.tsx`, `receipt.test.ts` | Unit / componente | — | ⚠️ no registrado | ✅ | ✅ | — |
| 9.1 reaplicación en CI | arnés con dobles de `psql`/`supabase` sobre el paso extraído del YAML | CI script | N/A (el paso ya estaba rojo en el run 36042552278) | ✅ paso viejo: rc=3 en `20261039000001`, *cannot change return type* (el mismo fallo de CI) | ✅ paso nuevo: rc=0, 6/6 reaplicación + gate PASS + schema idéntico | ✅ 7 escenarios negativos, cada uno por su motivo (otro error en 039; mismo mensaje en otra función; otro error en 040; 5/6; 062 falla; sin PASS; drift) | ✅ comentarios por tolerancia; YAML parsea, `bash -n` OK |
| 9.2 fusión con #585 | `test_facturar_venta_manual.sql` + `_race.sh`, `test_venta_editable_sin_cae.sql` + `_race.sh`, `test_ventas_unidades_conversion.sql` | SQL gate (1 y 2 conexiones) | ✅ gates de #585/#582 verdes en `main` | ✅ cuerpo sin fusionar: `sales_order_out_of_sync … orden 1000.00, venta 2500.00` | ✅ cuerpo fusionado: los 3 gates PASS | ✅ 2 arneses de carrera (N1, `NOWAIT`) + B.3/C.3 | ✅ diff hunk a hunk contra base/ours/theirs |
| 9.2 preflight | la migración (preflight + gate embebido) | SQL migración | ✅ 5/6 md5 de prod = `v_expected`, el 6º = `7c8c1b76…` | ✅ control negativo: cuerpo sin fusionar vivo → aborta | ✅ `db reset` ×3 (309) | ✅ reaplicación: 6 NOTICE + PASS | ✅ cabecera con fecha y origen de cada md5 |
| 9.4 comentarios | `test_ventas_unidades_conversion.sql` (E) + gate embebido | SQL gate | ✅ 5 bloques PASS | ✅ mutante sin COMMENT: 2 fallos | ✅ `obj_description` no nulo | ✅ 2 funciones × 2 gates | — |
| 9.5 unidad de sistema | `test_ventas_unidades_conversion.sql` (F) | SQL gate | ✅ | ✅ mutante sin `NOT is_system` en el guard: F.1-F.4 `P0404` | ✅ 4/4 | ✅ 4 casos (base y g del sistema, mixto, venta −0,45) | ✅ F.1 acumula en vez de abortar |
| 9.5 otra cuenta | `test_ventas_unidades_conversion.sql` (G) | SQL gate | ✅ | ✅ mutante sin guard de tenencia: G.1/G.2 fallan | ✅ 2/2 | ✅ helper + venta de punta a punta, sin rastro | — |
| 9.5 residuo cero | `test_ventas_unidades_conversion.sql` (H) | SQL gate | ✅ | ✅ mutante con cleanup incompleto: 2 filas en `units_of_measure`, 14 en `payment_methods` | ✅ 9/9 tablas | ✅ por cuenta y por usuario | — |
| 9.5 lateral: `text[] \|\| 'literal'` | gate + gate embebido | SQL gate | — | ✅ los mutantes abortaban con `malformed array literal` en vez de reportar | ✅ `'…'::text` en los 49 appends | ✅ m1, m2, m3 | ✅ comentario en los dos `DECLARE` |
| 9.6 D11 guard de unidad base | `backend/tests/test_ventas_unidades_conversion_base_unit_lock.py` | Unit (router → service → repo, asyncpg mockeado) | ✅ 69/69 | ✅ 5 fallan: `assert 200 == 409` | ✅ 8/8 | ✅ 11 casos (asignar con stock, cambiar con stock, stock 0 + movimientos, sin nada, misma unidad, quitar con stock, omitida, 404 + 3 de repositorio) | ✅ una sola lectura de la fila — 80/80; 174/174 en los 11 archivos de productos |
| 9.3 crash `/stock` (hook) | `frontend/__tests__/stock-min-stock-decimal-string.test.tsx` | Unit + componente | ✅ 215/215 | ✅ 3 fallan: `qty.toFixed is not a function` | ✅ 4/4 | ✅ `"0.5000"`, `"5.0000"`, `null`; desktop + tarjeta móvil | ✅ comentario del tipo |
| 9.3 defensa del formateador | `frontend/__tests__/lib/format-unit-string-defense.test.ts` | Unit | ✅ | ✅ 2 fallan con el mismo `TypeError` | ✅ 12/12 | ✅ `formatStock` y `formatQuantity`, entero y decimal | — |
| 9.7 `toBaseQuantity` sin unidad | `frontend/__tests__/lib/unit-utils-no-unit-mirror.test.ts` | Unit | ✅ | ✅ 3 fallan: 3000 en vez de 3, 381 en vez de 0.381 | ✅ 17/17 | ✅ g, mL, redondeo a 4, regresión con factor | ✅ `roundToNumeric4` extraído |
| 9.7 unidad base sólo con control de stock | `frontend/__tests__/components/product-form-base-unit-tracked-only.test.tsx` | Componente | ✅ | ✅ `expected 'u-kg' to be undefined` al pasar a Servicio/Digital | ✅ 19/19 | ✅ tracked+kg viaja; untracked no; edición conserva | — |
| 9.7 `step` de los inputs de stock | mismo archivo, 2º `describe` | Componente | ✅ | ✅ submit bloqueado por `stepMismatch` con 0,5 / 0,55 / 0,25 | ✅ `step="any"` | ✅ edición 0,5 + alta 0,55/0,25 (control: mínimo 2 guardaba) | — |
| 9.7 toast de venta | `frontend/__tests__/components/sale-form-insufficient-stock-unit.test.tsx` | Componente | ✅ | ✅ `0.550 g` en vez de `0.550 kg` (línea nueva y acumulada) | ✅ 4/4 | ✅ g sobre kg, kg sobre kg, sin unidad → `3 uds` | — |
| 9.7 toast del POS | `frontend/__tests__/pos-insufficient-stock-unit.test.tsx` | Componente (página real) | ✅ | ✅ `disponible: 0.55` / `disponible: 3` sin unidad | ✅ 13/13 con los tests del POS | ✅ nueva, acumulada, sin unidad | — |
| 6.4 ampliada | `frontend/e2e/harness/unidades-visual.spec.ts` | E2E (arnés) | — | n/a (pasada visual) | ✅ 17/17 en Chromium real (2026-09-25, segunda revisión) | ✅ POS y compra × 1366/375 × claro/oscuro + importe de la línea | ✅ `verificarSelectorKg` y `verificarImporte100g` compartidos |
| 10.1 precio (helper) | `frontend/__tests__/lib/unit-utils-price-per-line-unit.test.ts` | Unit | ✅ suites de unidades verdes | ✅ 7 fallan: `convertUnitPrice is not a function` | ✅ 7/7 | ✅ g/kg, docena, costo, vuelta atrás, identidad, "sin unidad" = base, invariante con `toBaseQuantity` | — |
| 10.1 precio en venta | `frontend/__tests__/components/sale-form-price-per-line-unit.test.tsx` | Componente | ✅ 169/169 (venta, compra, POS, unidades, producto) | ✅ `expected 1800 to be 1.8`, `expected 100 to be 1200` | ✅ 3/3 | ✅ gramo, docena, vuelta a kg | ✅ aviso "Cat." re-expresado |
| 10.1 precio en POS | `frontend/__tests__/pos-price-per-line-unit.test.tsx` | Componente (página real) | ✅ | ✅ sin el fix (vía `git stash`): `expected 1800 to be 1.8`, `expected 100 to be 1200` | ✅ 2/2 | ✅ gramo y docena | — |
| 10.1 costo en compra | `frontend/__tests__/components/purchase-form-price-per-line-unit.test.tsx` | Componente | ✅ | ✅ sin el fix: `expected 900 to be 0.9`, `expected 100 to be 1200` | ✅ 2/2 | ✅ gramo y docena | — |
| 10.2 edición fraccionaria | `frontend/__tests__/components/{sale,purchase}-form-edit-fractional-qty.test.tsx` | Componente | ✅ | ✅ `expected 'undefined' to be '0.001'`, `expected '1' to be '0.4'` (ambos formularios) | ✅ 5/5 | ✅ una línea discreta sigue con mínimo 1 | ✅ `lineUnitOf` compartido por mínimo, paso y símbolo |
| 10.3 mínimo 0 al editar | `frontend/__tests__/components/product-form-min-stock-zero.test.tsx` | Componente | ✅ | ✅ `expected 10 to be +0` | ✅ 2/2 | ✅ un alta nueva sigue arrancando en 10 | — |
| 10.4 reporting en unidad base | `supabase/tests/test_ventas_unidades_conversion.sql` (I) | SQL gate | ✅ A-H PASS | ✅ I.3 quantity 100 / costo 60000; I.4 ranking 100/60000; I.5 margen -33233.3; I.6 costo por venta 60000; I.7 función inexistente | ✅ 7/7 | ✅ formulario, POS con docena, canónica, ranking, canal, KPI (cuenta B aislada), línea histórica inconvertible | ✅ md5 de los tres cuerpos en el preflight (9 funciones) |
| 10.5 D6 distinguible | mismo gate (J) | SQL gate | ✅ | ✅ por mutación (sin código nuevo): `sedit_u5`, `pedit_p1`, `sedit_u6` —sobrevivientes de la revisión— mueren en J.1/J.2/J.3 | ✅ 3/3 con el cuerpo real | ✅ venta, compra, sin movimiento | — |
| 10.6 helper | mismo gate (K) | SQL gate | ✅ | ✅ K.3: factor -1 `obtuvo -5.0000` y la venta sumaba 5 de stock; `helper_h7`/`helper_h2` mueren en K.1/K.2 | ✅ 3/3 | ✅ factor -1 y 0; base propia de variante con docena y con kg | ✅ K.2 envuelto para reportar en vez de abortar |
| 10.7 unidad base en la tabla | mismo gate (L) + `backend/tests/test_ventas_unidades_conversion_base_unit_lock.py` | SQL gate + Unit | ✅ 11/11 backend | ✅ L.1/L.2/L.5/L.6/L.7 "no rechazó" (también PostgREST como `authenticated`) | ✅ 7/7 + 13/13 | ✅ cambiar, quitar, sin stock, asignar, grupo del padre, tenencia, PostgREST; backend: variante heredada y P0409 de la base → 409 (casos de cobertura: pasan desde el primer run, el comportamiento ya existía) | ✅ el service documenta que es camino rápido |
| 11.1 CR literales | la migración + el gate | SQL / repo | ✅ 9 md5 = `v_rewritten` antes del cambio | ✅ `git ls-files --eol` → `i/-text` en los dos; `grep -c $'\r'` → 2 y 1 bytes sueltos | ✅ `i/lf`; `db reset` 309 y los 9 md5 iguales; `prosrc` sin `\r` | ✅ gate y embebido siguen PASS | — |
| 11.2 TOCTOU | `supabase/tests/test_ventas_unidades_conversion_race.sh` + gate (E) | SQL, dos conexiones | ✅ gate A-L PASS | ✅ (a) `PURCHASE_OK`, stock `2.0000` y 1 movimiento sobre base 'u'; (E) "normaliza antes del FOR UPDATE" en v2 / compra / legacy | ✅ (a)(b)(c) PASS en 12 s; (E) PASS | ✅ (b) orden inverso → `P0409`; (c) herencia de variante → `P0400` | ✅ línea sin producto sigue validando unidad |
| 11.3 alerta de margen | gate (N) | SQL gate | ✅ | ✅ N.1 margen −6.666.566,67 %, N.2 costo 120000 / importe 1, N.3 y N.4 disparaban | ✅ 5/5 | ✅ formulario, POS, base con cantidad > 1, sin costo (control) | ✅ COMMENT nuevo |
| 11.4 D-F′ (base) | gate (M) + (E) | SQL gate | ✅ | ✅ M.1 `amount=4.58`, M.2 edición `2061.000000`, M.3 `amount=1.23`; (E) `sales_order_items.price`/`quote_items.price` escala 2 | ✅ 6/6 | ✅ formulario, compra y sus ediciones (control, ya exactos), docenas | ✅ ALTER guardado (reaplicación no-op), relfilenode igual |
| 11.4 D-F′ (frontend) | `unit-utils-price-per-line-unit.test.ts`, `cart-utils.test.ts`, `pos-price-per-line-unit.test.tsx`, `sale-form-price-per-line-unit.test.tsx`, `purchase-form-price-per-line-unit.test.tsx`, `sale-form-edit-fractional-qty.test.tsx` | Unit + componente | ✅ 25/25 previos | ✅ 10 fallan: `1.2346` vs `1.23456`, `555.57`, `0.0012`, `1234.6`, `1999.98`, `9999.9999`, `3333.3333` | ✅ 35/35; suites relacionadas 211/211 | ✅ g, mg, ida y vuelta, ruido binario, subtotal tipeado, POS/venta/compra, edición (pin) | ✅ `roundUnitPrice` en la capa canónica |
| 11.5 re-parent | gate L.8-L.12 | SQL gate | ✅ L.1-L.7 | ✅ L.8/L.9/L.10 "no rechazó" | ✅ 12/12 | ✅ mismo padre efectivo y base propia → permitido | ✅ grupos separados (producto / variantes que heredan) |
| 12.1 asignar sobre historia en otra unidad | gate L.13-L.20 | SQL gate | ✅ A-P restantes PASS | ✅ `01-gate-RED.txt`: L.13/L.14/L.17/L.18/L.20 "no rechazó" (L.15/L.19 fallan en cascada) | ✅ L 24/24 (`03-gate-GREEN1.txt`) | ✅ kg → `kg` y sin unidad → `kg` permitidos; padre, re-parent y PostgREST rechazados | ✅ mensaje con los símbolos de las dos unidades |
| 12.1 backend | `backend/tests/test_ventas_unidades_conversion_base_unit_lock.py` | Unit (router → service → repo, asyncpg mockeado) | ✅ 12/12 | ✅ `07-be-RED.txt`: 4 fallan (`assert 200 == 409`, `has_lines_in_other_unit` inexistente) | ✅ 16/16 | ✅ con líneas en otra unidad pero sin stock → 200; sin líneas → ni consulta el stock | ✅ líneas antes que el stock |
| 12.2 DELETE e importador | gate L.21-L.24 | SQL gate | ✅ | ✅ L.21 "DELETE del padre por PostgREST: aceptado" y la variante sin padre | ✅ 4/4 | ✅ borrado conjunto y padre sin base permitidos; importador → error de fila | ✅ GUC local único (jsonb) |
| 12.2 write skew | `supabase/tests/test_ventas_unidades_conversion_race.sh` (d)(e) | SQL, dos conexiones | ✅ (a)(b)(c) PASS | ✅ con el cuerpo de la tercera revisión: (d) `BASE_CHANGE_OK` (`05-race-RED.txt`), (e) `REPARENT_OK` (`05b-race-RED-only-e.txt`) | ✅ (a)-(e) PASS (`06-race-GREEN.txt`) | ✅ los dos órdenes | — |
| 12.3 total al centavo | gate (P) + (E) | SQL gate | ✅ | ✅ P.1-P.4 cargo/evento 665.34; (E) acumulador `numeric(15,2)` en los cuatro cuerpos | ✅ 5/5 | ✅ v2, legacy, compra, edición; control al centavo | ✅ `v_rewritten` re-medido |
| 12.4 unidad en uso | gate (O) | SQL gate | ✅ | ✅ O.1-O.5 "no rechazó", factor de la Docena en 6 | ✅ 7/7 | ✅ renombrar y unidad sin uso permitidos | — |
| 12.5 precio unitario y unidad en pantalla | `format-unit-price.test.ts`, `operations-list-line-units.test.tsx`, `receipt-unit-price.test.ts`, `test_receipts_unit_price_precision.py` | Unit + componente | ✅ `receipt.test.ts`, `test_receipts.py` | ✅ `12-fe-units-RED.txt` (11 fallan: `formatUnitPrice is not a function`, sin `450 g`, sin columna Unidad, ticket con `$ 4,58`); `11-be-receipt-RED.txt` (import) | ✅ 27/27 + 12/12 | ✅ ventas y compras, sin unidades no inventa, USD, al centavo = `formatMoney` | ✅ `MONEY_FORMAT` compartido |
| 12.6 descuento | `use-sales-discount-price-precision.test.ts` | Hook | ✅ | ✅ `09-fe-RED.txt`: `4.117500000000001`, `1.9800000000000002` | ✅ 4/4 | ✅ alta y edición, sin descuento intacto | — |
