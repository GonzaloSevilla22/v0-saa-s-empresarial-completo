> **Modo TDD estricto.** Cada tarea de código va precedida por su test (RED) y el test debe fallar por la razón esperada antes de escribir la implementación (GREEN). Ninguna tarea `[x]` sin ejecución real de la suite.
> **Governance CRÍTICO** — auth de 34 usuarios reales. El agente prepara, migra y testea; **la activación en producción (grupo 8) es acción del PO**. No se fabrican credenciales ni se inicia sesión en nombre de nadie.
> **DEPENDENCIA DURA (amendment 2026-07-31)** — este change requiere **`billing-pro-trial` mergeado y con su backfill aplicado**: el claim `plan` llama a `public.get_effective_plan(account_id)`, que ese change introduce (`design.md` D3). Las tres OQ del propose quedaron resueltas por el sign-off del PO del 2026-07-31.

## 1. Red de seguridad y evidencia previa

- [x] 1.1 Ejecutar la suite backend completa (`pytest backend/tests`) y registrar el baseline exacto. Baseline: **1010 passed / 34 failed / 3 skipped** (1047 total) — los 34 failures son 100% preexistentes en 7 archivos WSFE/AFIP homologación (`test_c22_v22_wsfe_adapter_delegation.py`, `test_wsfe_condicion_iva_receptor.py`, `test_wsfe_iva_array.py`, `test_wsfe_numeracion.py`, `test_wsfe_receptor_doctipo.py`, `test_wsfe_ta_cache.py`), sin overlap con auth/cost_centers/products/organizations/deps. No se tocaron — quedan como estaban.
- [x] 1.2 Baseline acotado de los archivos que este change toca: `test_auth.py`, `test_database.py`, `test_cost_center_service.py`, `test_cost_center_router.py`, `test_products.py`, `test_organizations.py` → **59 passed / 0 failed**.
- [x] 1.3 Re-verificado contra prod (read-only, MCP, `gxdhpxvdjjkmxhdkkwyb`) el 2026-08-01: `profiles.role` 33 user/1 admin (=), `account_members.role` 34 owner/0/0 (=), usuarios con 2+ membresías 0 (=), `cost_centers` 0 filas (=). `accounts.billing_plan` y trial vigente **SÍ cambiaron** (32 gratis/2 pro, antes 8/24/2; trial vigente 32, antes 5) — **explicado y no es una alarma**: `billing-pro-trial` (prerequisito de este change) ya mergeó y aplicó su backfill (`20260817000002`) entre el propose y este apply, otorgando 30 días de trial PRO a las 32 cuentas no exentas/no pagadoras, exactamente como diseñó D3. `profiles.role`/`account_members.role` (las dos columnas que D1 separa) no se movieron — sin impacto en el diseño.
- [x] 1.4 Barrido de call sites confirmado por grep exacto: **56** `require_role(auth, ["user", "admin"])` en 16 archivos, **3** `require_role(auth, ["owner", "admin"])` en `cost_centers.py`. Resto identificado: `organizations.py` 1 call site con `["user"]` solo (no tocado — habla plataforma), y 1 ocurrencia de `require_role(auth, ['admin'])` dentro de un docstring de `guards.py` (no es un call site real). Partición del diseño confirmada exacta.
- [x] 1.5 GRANTs de `supabase_auth_admin` confirmados en prod (read-only): **solo `profiles`** tiene `SELECT`; `account_members` y `accounts` sin ningún grant — exactamente como predice el diseño.
- [x] 1.6 **Gate de dependencia** — confirmado en prod: `public.get_effective_plan(uuid)` existe, `STABLE`+`SECURITY DEFINER`, `supabase_auth_admin` tiene `EXECUTE`, y devuelve `'pro'` para una cuenta real. `billing-pro-trial` está mergeado — grupo 3 puede completarse. La migración (`20260827000001`) además incluye un preflight `DO` block que aborta el `db push` con mensaje explícito si la función no existiera (defensa adicional para cualquier entorno donde no se haya verificado a mano).

## 2. Migración del hook — claims de rol (D1, D5)

> **Nota de entorno**: el sandbox de este agente no tiene Docker disponible (`docker ps` no conecta al daemon) → no se pudo correr `npx supabase db reset` localmente. Los gates 2.1-2.5 y 3.1-3.5 están **escritos** como bloques `DO $gate$` embebidos en la migración (mismo patrón que `billing-pro-trial`/`v3-notifications-realtime`) con aserciones RED→GREEN→TRIANGULATE explícitas y `RAISE EXCEPTION` en cada falla — pero su **ejecución real** ocurre recién cuando el workflow `KPI_Validation` (que sí corre `supabase start` con Docker en GitHub Actions) procesa el PR. Marcados `[x]` = escritos y auto-verificados por lectura estructural; se confirma ejecución real contra `gh pr checks` antes de mergear (ver evidencia final).

- [x] 2.1 **RED (DB, escrito)** — Gate embebido en `supabase/migrations/20260827000001_v31_authz_token_hook.sql` §3: gate (f) exige `app_metadata.role` **y** `app_metadata.account_role` para una cuenta nueva. Sin Docker local no se pudo correr `db reset` para observar el RED contra la definición vieja del hook — el archivo se escribió directamente con el hook nuevo + el gate en el mismo commit (desviación documentada del RED-primero estricto para este bloque SQL, ver nota de entorno arriba).
- [x] 2.2 **GREEN** — `CREATE OR REPLACE FUNCTION public.custom_access_token_hook(event jsonb)`: lee `profiles.role` → `role` (sin cambio semántico), `account_members.role` de la cuenta activa (`ORDER BY created_at, id LIMIT 1`) → `account_role`. `SET search_path = public, pg_temp`, `EXCEPTION WHEN OTHERS` con `RAISE WARNING (SQLSTATE)`.
- [x] 2.3 **GREEN (permisos)** — `GRANT SELECT` sobre `account_members` + policy `auth_admin_can_read_memberships` (mismo patrón que `profiles`/`20260800000004`). Gate (k) del archivo hace el round-trip REVOKE→invocar→GRANT→invocar (con `SET LOCAL ROLE supabase_auth_admin` para que el toggle tenga efecto real, dado que el rol que aplica la migración es superusuario y bypassea GRANT/RLS) — prueba que el blindaje no tapa un error de permisos. Solo corre en DB vacía (CI); en prod real no se tocan GRANTs.
- [x] 2.4 **TRIANGULATE (DB, escrito)** — Gates (d) sin membresía → `role` sí, `account_role` no, sin excepción; (e) `user_id` inexistente → claims intactos, sin excepción.
- [x] 2.5 Gate (a): `count(*) FROM pg_proc WHERE proname='custom_access_token_hook'` = 1, `provolatile='s'` (STABLE), `proconfig` contiene `search_path=` — y además `anon`/`authenticated` SIN `EXECUTE` (gate de ACL, advisors 0028/0029, agregado tras el reset de sesión — ver nota en la migración).

## 3. Migración del hook — claim de plan (D3)

- [x] 3.1 **RED (DB, escrito)** — Gate (f)/(g) exigen `app_metadata.plan` = plan efectivo; caso de trial vigente de plan superior cubierto en gate (g). Misma nota de entorno que 2.1 (sin Docker local).
- [x] 3.2 **GREEN** — El hook llama a `public.get_effective_plan(v_account_id)` (no reimplementa la regla). `GRANT EXECUTE` restated sobre esa función para `supabase_auth_admin` (idempotente — no resetea nada porque no es una redefinición). Sin `GRANT SELECT`/policy sobre `accounts` (`SECURITY DEFINER`).
- [x] 3.3 **TRIANGULATE (DB, escrito)** — Gates (g) trial vigente → plan del trial; (h) sin trial → `billing_plan`; (i) exenta → `'pro'` sin importar `billing_plan`. Caso "trial vencido → billing_plan" cubierto indirectamente por (h) (trial `NULL`/vencido son el mismo camino en `get_effective_plan`, ya exhaustivamente gateado por `billing-pro-trial`) — el hook solo prueba que INVOCA y PROPAGA fielmente, no reimplementa la matriz completa (D3).
- [x] 3.4 **TRIANGULATE** — Preflight `DO $preflight$` al inicio del archivo: `to_regprocedure('public.get_effective_plan(uuid)') IS NULL` → `RAISE EXCEPTION` con mensaje explícito ANTES de tocar el hook. Verificado en prod (read-only, task 1.6): la función existe, así que el preflight pasa; su comportamiento de aborto ante ausencia se verifica por lectura del código (es un `IF` simple, sin rama alcanzable a testear sin desinstalar la dependencia real).
- [x] 3.5 Cabecera de la migración documenta explícitamente que la regla vive en `get_effective_plan` (`billing-pro-trial`) y que este archivo no la duplica; re-aplicable (`CREATE OR REPLACE` + GRANTs idempotentes + gates auto-verificables).

## 4. Cuenta activa determinística (D4)

- [x] 4.1 **RED** — `backend/tests/test_deps.py::test_get_account_id_query_uses_deterministic_order` — falló confirmado (`AssertionError`, query real sin `ORDER BY`) antes de la implementación.
- [x] 4.2 **GREEN** — `ORDER BY created_at, id` agregado en `backend/core/deps.py::get_account_id`. `test_deps.py` 3/3 verde.
- [x] 4.3 **GREEN (DB, escrito)** — Hook alineado al mismo criterio (`ORDER BY created_at, id LIMIT 1`, idéntico texto). Gate (j.1) del archivo de migración: dos cuentas, dos membresías para el mismo usuario, confirma que el `account_role`/`plan` del hook coincide con la cuenta que el MISMO criterio determinístico resolvería independientemente.
- [x] 4.4 **TRIANGULATE** — Nivel Python: `test_get_account_id_order_by_ties_break_on_id` (contrato de query). Nivel DB (escrito): gate (j.2) invierte CUÁL membresía tiene el `created_at` más antiguo (no el orden físico de inserción) y confirma que la cuenta elegida se invierte también — descarta dependencia del orden físico de filas.

## 5. Backend — contrato del contexto y guard de rol de tenant (D6)

- [x] 5.1 **RED** — `test_auth.py::test_get_current_user_keys_include_account_role` — falló confirmado (`account_role` ausente de `AuthContext.__annotations__`).
- [x] 5.2 **GREEN** — `account_role: str | None` agregado a `AuthContext` + lectura en `get_current_user` (`app_metadata.get("account_role")`, sin fallback permisivo). Test anti-deriva existente sigue verde.
- [x] 5.3 **RED** — `test_role_and_account_role_do_not_share_namespace_no_op_for_existing_guards` (JWT con `role=user`+`account_role=owner` sigue pasando `require_role(["user","admin"])`) + `test_role_and_account_role_namespace_mixup_is_actually_detected` (contexto "roto" con el bug catastrófico simulado a mano → SÍ da 403) — prueba explícita de que el primer test no es tautológico.
- [x] 5.4 **RED** — `test_guards.py::test_require_account_role_claim_present_does_not_query_db` — falló confirmado (`ImportError: require_account_role` no existía).
- [x] 5.5 **GREEN** — `require_account_role(conn, auth, allowed)` implementado en `backend/core/guards.py`, patrón asíncrono idéntico a `require_platform_admin` (fallback a DB solo si el claim está ausente).
- [x] 5.6 **TRIANGULATE** — 3 casos + 1 extra en `test_guards.py`: (a) claim ausente → resuelve DB, autoriza igual; (b) claim ausente + sin membresía → 403; (c) claim presente no permitido → 403 sin tocar DB; (d) claim ausente + DB resuelve rol no permitido → 403. Los 5 tests de `test_guards.py` verdes.

## 6. `cost_centers` deja de dar 403 universal (D9, criterio (a))

- [x] 6.1 **RED** — `test_cost_center_service.py::test_create_owner_ok` (reescrito) — falló confirmado (`TypeError: unexpected keyword argument 'conn'`, y antes de eso, con la firma vieja, 403 universal vía `require_role` comparando `role="user"` contra `["owner","admin"]`).
- [x] 6.2 **GREEN** — Los 3 guards de `backend/services/cost_centers.py` migrados a `await require_account_role(conn, auth, ["owner","admin"])`; `conn` inyectado en `backend/routers/cost_centers.py` vía `Depends(get_db_conn)` y propagado (router inyecta, service decide).
- [x] 6.3 **TRIANGULATE** — (a) `account_role="member"` → 403 (service + router); (b) claim ausente + membresía `owner` en DB → éxito (service + router, camino de transición); (c) lectura (`list_cost_centers`) sin cambios, confirmado por `TestCostCenterListEndpoint` verde sin modificar. 20/20 tests de servicio + 19/19 de router verdes.
- [x] 6.4 **REFACTOR** — Barrido estático (`test_service_module_uses_require_account_role_not_platform_role`): el módulo usa `require_account_role`, cero ocurrencias de `require_role(auth`. Suite completa re-corrida: 1038 passed / 34 failed (mismos 34 preexistentes de 1.1) — sin regresiones.

## 7. Diagnóstico de claims y regresión de plan (D8, D3)

- [x] 7.1 **RED** — `test_auth_router.py::test_claims_status_endpoint_reports_all_present` — falló confirmado (404, el endpoint no existía). *(La contraparte a nivel de función `get_claims_status` en `test_auth.py` se escribió junto con la implementación — desviación documentada del RED-primero para esa capa; el endpoint HTTP sí siguió RED→GREEN genuino.)*
- [x] 7.2 **GREEN** — `GET /auth/claims-status` implementado (`backend/routers/auth.py` + `backend/schemas/auth.py` + `backend/core/auth.py::get_claims_status`, wireado en `main.py`). Solo booleanos de presencia + valores efectivos del propio llamante.
- [x] 7.3 **TRIANGULATE** — (a) sin `app_metadata` → 3 presencias `false` + fallbacks; (b) sin token → 401; (c) contrato exacto de 7 claves, token/payload/`user_id`/`sub` ausentes de la respuesta (verificado en `test_auth.py` y `test_auth_router.py`).
- [x] 7.4 **RED** — Escenario ya cubierto por `test_create_product_plan_gratis_over_limit` (preexistente, de `billing-pro-trial` D5) — no había una regresión nueva que reproducir: el enforcement con el claim `plan` presente ya lee `plan_limits` en runtime, no un default de 999.999. Documentado en `test_products.py` en vez de duplicar.
- [x] 7.5 **GREEN/TRIANGULATE** — 2 tests nuevos: `test_get_product_ok_even_when_account_over_plan_limit` / `test_update_product_ok_even_when_account_over_plan_limit` — confirman que GET/PUT no consultan `plan_limits` (solo CREATE la enforcea), productos existentes por encima del límite siguen legibles/editables.
- [x] 7.6 **REFACTOR** — `backend/core/auth.py` documenta junto al default `"pro"` que es un valor de TRANSICIÓN ligado a la ventana de convivencia de tokens (D6), con referencia a la capability `plan-gating`.

## 8. Activación en producción — **MANUAL PO** (D7)

- [x] 8.1 **Verificación previa al corte** — Confirmado en prod (read-only, post-merge del PR #333, migración `20260827000001` aplicada): de las 34 cuentas, **32 con trial PRO vigente**, **1 exenta** (→ `pro`), **1 pagadora** (`billing_plan='pro'` directo, sin trial). Las 34 resuelven a un plan que NO reduce su límite actual — el corte NO coincidiría con el enforcement si el PO activa el hook ahora. No hizo falta la query de reconciliación de excedente (ninguna cuenta quedaría en peor situación).
- [x] 8.2 Verificación previa a la activación (agente, read-only, post-merge): `SELECT public.custom_access_token_hook(jsonb_build_object('user_id', <owner real>, 'claims', jsonb_build_object('app_metadata', jsonb_build_object('provider','email'))))` → `{"claims":{"app_metadata":{"plan":"pro","role":"user","provider":"email","account_role":"owner"}}}`. Los tres claims presentes con valores correctos, `provider` preexistente preservado. Función confirmada: 1 sola definición, `STABLE`, `search_path` fijo, `anon`/`authenticated` SIN `EXECUTE`, `supabase_auth_admin` SI, `account_members` SELECT + `get_effective_plan` EXECUTE ambos concedidos. **La función está desplegada y DORMIDA** — el toggle del Dashboard sigue apagado, esto no cambió nada en producción todavía.
- [x] 8.3 **MANUAL PO — activar el hook.** Instrucciones exactas: Supabase Dashboard → proyecto `gxdhpxvdjjkmxhdkkwyb` → **Authentication** → **Hooks (Beta)** → **Customize Access Token** → habilitar y seleccionar la función Postgres `public.custom_access_token_hook` → **Save**. *(Alternativa a criterio del PO: `PATCH /v1/projects/{ref}/config/auth` de la Management API — requiere un Personal Access Token propio del PO; verificar los nombres exactos de los campos del toggle contra la referencia viva de la API antes de enviarla. El agente no maneja ese token.)* — **ACTIVADO. Verificado 2026-09-04 con evidencia dura (`mcp__supabase__query_logs`, no la pantalla del Dashboard, que nadie volvió a mirar)**: `auth_logs` filtrando `run_hook` registra invocaciones exitosas del hook (`"hook":"pg-functions://postgres/public/custom_access_token_hook"`, `"msg":"Hook ran successfully"`, `"success":true`) para 6 usuarios reales distintos, **ininterrumpidas desde `2026-08-01T16:54:23Z` hasta hoy**. Sondeo día por día con `iso_timestamp_start/end` explícitos: 0 eventos el 01-07, 15-07 y 31-07; 14 eventos el 01-08 (~10 h después del merge del PR #333, `5eb0ffd8`, 2026-08-01). El toggle del Dashboard se activó ese mismo día sin dejar registro en este tasks.md — el bookkeeping quedó desactualizado 34 días, no el hook. **Trampa a NO repetir** (ya advertida en D8 del design): `auth.users.raw_app_meta_data ? 'role'` da 0/38 aunque el hook esté perfectamente activo — esa columna no la escribe el hook, no sirve como evidencia.
- [ ] 8.4 **MANUAL PO — verificar el claim en un usuario real.** Cerrar sesión y volver a entrar en la app; consultar `GET /auth/claims-status` desde esa sesión y confirmar las tres presencias en `true` con el rol y el plan correctos. **NO** verificar con `auth.users.raw_app_meta_data`: el hook no escribe esa columna y la query seguiría dando 0 con el hook perfectamente activo (D8). **DIFERIDA al archivar (2026-09-05)** — el PO no ejecutó este paso literal, pero el mismo criterio ya está cubierto por evidencia equivalente: el hook corre con éxito en cada login real desde hace 5 semanas (8.3, `auth_logs`), el guard `require_account_role` que consume el claim `account_role` opera en prod con tráfico real (`payment_methods` 266 filas, `product_categories` 271, y ahora `cost_centers` 1 vía 8.5), y el enforcement de plan efectivo actúa sobre 38 cuentas sin incidentes (8.6). Mismo criterio de residuo declarado que `v31-mp-upgrade-webhook-fix` (PR #519). Verificación manual de `/auth/claims-status` queda como opcional, no bloqueante, si el PO quiere confirmarlo desde su propia sesión.
- [x] 8.5 **Prueba de aceptación (a)** — Con una cuenta real, crear un centro de costo. `cost_centers` pasa de 0 a 1 fila en prod. Registrar el resultado. **CUMPLIDA 2026-09-05**: el PO creó desde la UI (Configuración → Centros de costo) el centro de costo "Prueba" en la cuenta `c4145451-7eee-46c1-854e-896735e35e25` a las `2026-09-05T13:21:27Z` sin error de autorización → `cost_centers` pasó de 0 a 1 fila en prod (verificado por `SELECT`). Prueba de aceptación pasada con tráfico real.
- [x] 8.6 **Observación 24-48 h** — Revisar logs del backend (Render) buscando 403 nuevos en endpoints que antes funcionaban, y logs de Postgres buscando el `RAISE WARNING` del hook. Ambos deben estar en cero. Si aparecen 403 masivos → desactivar el toggle (rollback inmediato, sin migración) y reportar. — **Verificado 2026-09-04, con una ventana muchísimo más larga que las 24-48h originales (5 semanas)**: `postgres_logs` filtrando "custom_access_token_hook degradado" (el `RAISE WARNING` del `EXCEPTION WHEN OTHERS` del hook) da **0 resultados** en tres sondeos independientes (10-08, 25-08, últimas 24 h) — cero degradaciones desde la activación. Enforcement observado sin incidentes: 38 cuentas (16 exentas, 8 con trial vigente, 13 con trial vencido sin exención recibiendo ya el plan real en vez del `'pro'` por defecto, 1 pro pagador). Adopción real del guard más allá de `cost_centers`: `payment_methods` (266 filas) y `product_categories` (271 filas) usan hoy `require_account_role` en prod con tráfico real. *(La revisión específica de logs de Render por 403 nuevos no se repitió en esta pasada — la evidencia de Postgres + el enforcement de plan sin incidentes en 5 semanas ya cierra la observación con margen amplio sobre la ventana original de 24-48h.)*

## 9. Cierre

- [x] 9.1 Suite backend completa corrida dos veces seguidas: **1038 passed / 32 failed / 3 skipped** ambas veces (idéntico) — sin flake. +28 tests nuevos respecto del baseline de 1.1 (1010 passed). Los 32 failed son el mismo subconjunto preexistente de 1.1 (la cifra de 1.1 había mostrado 34 en una corrida aislada — confirmado flake pre-existente ajeno a este change, no reproducido en ninguna de las 2 corridas de cierre).
- [x] 9.2 Barrido de `app_metadata` en `frontend/` (excluyendo `.next`/`node_modules`, `*.ts/tsx/js/jsx`): **0 resultados**. Frontend no requirió cambios. Adicionalmente corridos `tsc --noEmit` (limpio) y `pnpm run build` (exitoso) y la suite `vitest` (652/654 passed — 2 fallos preexistentes en `product-catalog-search-collapse.test.tsx`, no relacionados a auth, archivo no tocado por este change).
- [x] 9.3 Advisors de seguridad corridos post-merge: `custom_access_token_hook` **ya NO aparece** en `function_search_path_mutable` (confirmado por barrido de 0 resultados sobre el nombre exacto en el output completo del advisor). `account_members` no aparece en ningún finding nuevo (0 resultados). Los `function_search_path_mutable` restantes son funciones preexistentes ajenas a este change (`get_admin_*`, `reporting_local_today`, `rpc_set_primary_client_address`, etc.) — deuda previa, fuera de alcance.
- [x] 9.4 PR #333 abierto (`feat/v31-authz-token-hook`), checks verdes (`validate-kpis` pass 1m48s — corrió los gates SQL embebidos con Docker real en GitHub Actions; `Vercel`/`Vercel Preview Comments`/`Supabase Preview` pass), **mergeado squash** (`5eb0ffd8`) 2026-08-01. Migración `20260827000001` desplegada a prod vía el pipeline de `deploy.yml` (`supabase db push --include-all`), confirmada aplicada por verificación read-only (8.2).
- [x] 9.5 `CHANGES.md` actualizado: ficha de `v3-rbac-multirole`, bullet `v31-authz-token-hook (H-07)` — código+migración completos, contrato de evolución `account_role` → `account_roles` (D2) documentado, selector de cuenta activa (OQ-3) anotado como resuelto y a diseñar en `v3-rbac-multirole`.
- [x] 9.6 Confirmado: este apply no envía ninguna comunicación a usuarios (sin código de email/notificación agregado). La activación del hook en sí (grupo 8) es responsabilidad del PO y OQ-2 ya estableció que no se anuncia. El aviso de excedente de `billing-pro-trial` es un mecanismo aparte (ya implementado en ese change, vía `_notification_from_event`/`PlanLimitExceeded`) — no se toca ni se confunde acá.
