# EmprendeSmart (EIE) — Instrucciones para Agentes

> SaaS para microemprendedores de Mendoza: gestión financiera (ventas/compras/gastos/stock) + IA accionable.  
> MVP en producción con usuarios reales — Junio 2026.
>
> ⚠️ **Este archivo se mantiene idéntico en `CLAUDE.md` y `AGENTS.md`.** `CLAUDE.md` es la fuente de verdad (Claude Code lo inyecta solo en su contexto); `AGENTS.md` es la copia que leen los demás agentes. Editá `CLAUDE.md` y corré `python scripts/ci/check_docs_sync.py --fix` **en el mismo PR** — el gate `Docs Sync` lo verifica en cada PR. (Se agregó porque `AGENTS.md` estuvo dos meses congelado anunciando `C-21` como próximo change.)

---

## Stack Tecnológico

> Verificado contra los manifiestos el **2026-08-26** (`frontend/package.json`, `backend/requirements.txt`, `backend/pyproject.toml`, `.github/workflows/`, `vercel.json`).
> Si tu change sube o baja una versión, actualizá esta tabla en el mismo PR — se desactualizó una vez y nadie lo notó por meses.

### Frontend — `frontend/package.json`

| Capa | Tecnología | Versión |
|------|------------|---------|
| **Framework** | Next.js (App Router; Turbopack en `dev`) | 16.1.6 |
| **UI** | React + React DOM | 19.2.3 |
| **Lenguaje** | TypeScript | 5.7.3 |
| **Estilos** | Tailwind CSS + PostCSS + `tailwindcss-animate` | 3.4.17 |
| **Componentes** | shadcn/ui sobre Radix UI (28 primitivas) + `class-variance-authority` + `clsx` + `tailwind-merge` | — |
| **Iconos** | `lucide-react` | 0.544 |
| **Estado global** | Zustand | 5.0.13 |
| **Server state / cache** | TanStack React Query (+ devtools) | 5.100.14 |
| **Formularios** | React Hook Form + Zod + `@hookform/resolvers` | 7.54 / 3.24 |
| **Gráficos** | Recharts + D3.js | 2.15 / 7.9 |
| **3D** | `three` + `@react-three/fiber` + `@react-three/drei` (v4 visual, con `capabilityGate`) | 0.185 / 9.6 / 10.7 |
| **Animación** | `framer-motion` | 12.43 |
| **Temas** | `next-themes` (claro/oscuro — ambos obligatorios en toda UI nueva) | 0.4.6 |
| **Cliente Supabase** | `@supabase/supabase-js` + `@supabase/ssr` | 2.98 / 0.8 |
| **Pagos (cliente)** | `mercadopago` | 2.4 |
| **Anti-bot** | Cloudflare Turnstile (`@marsidev/react-turnstile`) — usar siempre `submitWithFreshCaptcha` | 1.5 |
| **Otros** | `date-fns` 4.1, `sonner`, `cmdk`, `vaul`, `embla-carousel-react`, `react-day-picker`, `input-otp`, `react-resizable-panels` | — |
| **Unit tests** | Vitest + Testing Library (react/dom/jest-dom/user-event) + jsdom | 4.1 / 29 |
| **E2E** | Playwright | 1.61 |
| **Package manager** | pnpm (monorepo, `pnpm-workspace.yaml`) | 10.33.4 |

### Backend Python — `backend/` (EN PRODUCCIÓN — Fase 5 ✅, ya no es "planificado")

> **Modelo híbrido**: el frontend consume FastAPI para datos (mutaciones + lecturas) y sigue hablando directo con Supabase para Auth y Storage. Realtime: Supabase (DEC-16) **+** WebSocket propio del backend (`routers/ws.py` + `core/ws_manager.py`). Ver `knowledge-base/08_arquitectura_propuesta.md` §"Evolución Arquitectónica: Backend Python/FastAPI".
>
> **Superficie real**: 26 routers / 26 services / 30 repositories + `core/` (`auth`, `client_activity`, `config`, `database`, `deps`, `errors`, `guards`, `idempotency`, `redis_client`, `ws_manager`).

| Capa | Tecnología | Notas |
|------|------------|-------|
| **Runtime** | Python 3.12 (CI) | `pyproject` declara `requires-python >=3.11` |
| **Framework API** | FastAPI ≥0.111 + Uvicorn[standard] ≥0.29 | Arquitectura 3 capas: routers → services → repositories |
| **Validación** | Pydantic v2 + `pydantic-settings` ≥2.2 | Nada de payloads sin schema |
| **DB driver** | asyncpg ≥0.29 | Pool con JWT-passthrough (RLS org-based activa como red, NO como guard único) |
| **Auth** | `PyJWT[crypto]` ≥2.8 (`PyJWKClient`) | Los tests usan `python-jose` — ver nota ⚠️ abajo |
| **Cache / rate-limit / idempotencia** | Redis ≥5.0 (Upstash free) | Instancia por env var; no declarada en el repo |
| **Reintentos** | `tenacity` ≥8.2 | — |
| **HTTP client** | `httpx` ≥0.27 | — |
| **PDFs** | `fpdf2` ≥2.7 | Comprobantes y recibos |
| **SOAP AFIP/ARCA** | `zeep` ≥4.2,<5 | Facturación electrónica (C-27) |
| **SDK Supabase** | `supabase-py` ≥2.0 | — |
| **Testing** | pytest ≥8 + pytest-asyncio (`asyncio_mode=auto`) + httpx + asyncpg-stubs | Coverage ≥87% en CI; `omit` de `tests/` y `.venv/` |
| **Deploy backend** | Render (free tier) | Cold start ~50s; el workflow `keep-backend-warm` pingea `/health` cada 10 min para que el webhook de MP no dé 502 |

> ⚠️ **Divergencia conocida de manifiestos**: `pyproject.toml` declara `python-jose` y NO `PyJWT`; `requirements.txt` declara `PyJWT[crypto]` y NO `python-jose`. El código de app importa **PyJWT** (`core/auth.py`), los tests importan **jose** (`tests/conftest.py`). Hoy funciona porque CI instala ambos caminos; si tocás dependencias, no "limpies" uno de los dos sin verificar quién lo importa.

### Datos e infraestructura

| Capa | Tecnología | Notas |
|------|------------|-------|
| **BaaS** | Supabase (Auth, DB, Edge Functions, Storage, Realtime) | Proyecto real: `gxdhpxvdjjkmxhdkkwyb` |
| **DB** | PostgreSQL vía Supabase, con RLS org-based | 263 migraciones; última `20261014000001_sucursal_guard_vaciado_auditoria` |
| **Extensiones PG** | `pg_cron` (grace period, relay outbox) · `pg_net` / DB webhooks (email, outbox, relay CAE) | — |
| **Edge Functions** | Deno (Supabase) — 11 funciones | `ai-insights`, `ai-resumen`, `ai-precio`, `ai-rentabilidad`, `ai-comparativo`, `ai-prediccion`, `ai-simulador`, `fair-advisor`, `invoice-ocr`, `generate-export`, `send-email` |
| **IA** | OpenAI API | `gpt-4o-mini` en las 9 funciones de IA; **`gpt-4o`** (visión) en `invoice-ocr` |
| **Email** | Resend (via Edge Function + DB Webhook) | — |
| **Pagos** | MercadoPago | Webhook en el backend Python: `POST /payments/webhook` (governance CRÍTICO) |
| **Deploy frontend** | Vercel | `vercel.json`: build `cd frontend && pnpm run build`, output `frontend/.next` |
| **CI/CD** | GitHub Actions × 6 | `deploy.yml` (build Next + `supabase db push --include-all` + `functions deploy`), `Backend_Tests`, `Frontend_Tests`, `E2E_Tests`, `KPI_Validation`, `keep-backend-warm`. Node 20 / Python 3.12 |

---

## Base de Conocimiento

Leé estos archivos antes de cualquier change. Son la fuente de verdad del sistema.

| # | Archivo | Cuándo leerlo |
|---|---------|---------------|
| 01 | [knowledge-base/01_vision_y_objetivos.md](knowledge-base/01_vision_y_objetivos.md) | Al empezar. Define UMV, KPIs, alcance. |
| 02 | [knowledge-base/02_descripcion_general.md](knowledge-base/02_descripcion_general.md) | Stack completo, módulos, integraciones. |
| 03 | [knowledge-base/03_actores_y_roles.md](knowledge-base/03_actores_y_roles.md) | Roles, planes (4 tiers), RBAC, RLS. |
| 04 | [knowledge-base/04_modelo_de_datos.md](knowledge-base/04_modelo_de_datos.md) | 23+ tablas, tipos, triggers, ERD. |
| 05 | [knowledge-base/05_reglas_de_negocio.md](knowledge-base/05_reglas_de_negocio.md) | 33 reglas por dominio — leer SIEMPRE antes de tocar lógica. |
| 06 | [knowledge-base/06_funcionalidades.md](knowledge-base/06_funcionalidades.md) | 10 épicas + estado por módulo. |
| 07 | [knowledge-base/07_flujos_principales.md](knowledge-base/07_flujos_principales.md) | 9 flujos E2E (venta, insight, OCR, etc.). |
| 08 | [knowledge-base/08_arquitectura_propuesta.md](knowledge-base/08_arquitectura_propuesta.md) | BaaS pattern, Server/Client, seguridad. |
| 09 | [knowledge-base/09_decisiones_y_supuestos.md](knowledge-base/09_decisiones_y_supuestos.md) | 15 decisiones + 7 supuestos (incl. DEC-12..15 backend Python) — leer antes de proponer. |
| 10 | [knowledge-base/10_preguntas_abiertas.md](knowledge-base/10_preguntas_abiertas.md) | Inconsistencias conocidas — revisá antes de implementar. |
| — | [modelo-dominio-aliadata-v2.md](modelo-dominio-aliadata-v2.md) | Modelo de dominio V2 adoptado (2026-06-09). Leer antes de cualquier change V2 (C-19+), junto a `openspec/explore/2026-06-09-modelo-dominio-v2.md`. |
| — | [modelo-dominio-aliadata-v3.md](modelo-dominio-aliadata-v3.md) | **Modelo V3 adoptado (2026-07-02)** — extensión del V2 (NO lo reemplaza): snapshots, FSM+historial, notificaciones post-commit, soft delete, RBAC multi-rol, estándares de plataforma. Leer junto al V2 para todo change V2.5+/V3; los changes derivados viven en `CHANGES.md` §"Roadmap Modelo V3". |

---

## Skills Disponibles

Los compact rules de cada skill los resuelve el orquestador desde `.atl/skill-registry.md` (generado por `skill-registry`; no versionado — no está en el repo).

| Agente / Rol | Skills que carga |
|---|---|
| **Backend / DB** (migraciones, RLS, RPCs, Supabase) | `supabase`, `supabase-postgres-best-practices` |
| **Backend Python** (FastAPI, capas, Pydantic, async) | `fastapi-templates`, `python-design-patterns`, `python-testing-patterns`, `pytest-coverage` |
| **Frontend / React** (componentes, App Router, SSR, data fetching) | `vercel-react-best-practices`, `nextjs-app-router-patterns` |
| **Auth** (Supabase Auth + Next.js sessions, middleware, OAuth) | `nextjs-supabase-auth` |
| **UI / Design** (accesibilidad, Tailwind, shadcn/ui) | `web-design-guidelines` |
| **QA / Testing** (Playwright, browser testing, screenshots) | `webapp-testing` |
| **Orquestación** (OPSX, KB, roadmap, skills) | `kb-creator`, `roadmap-generator`, `find-skill`, `skill-registry`, `agent-instruction` |

### Routing de skills (cuándo activarlas)

| Situación | Skill |
|---|---|
| SQL queries, schema design, indexes, RLS | `supabase-postgres-best-practices` |
| Auth, Edge Functions, Storage, migraciones | `supabase` |
| Componentes React, Next.js pages, data fetching | `vercel-react-best-practices` |
| App Router, Server Components, Server Actions | `nextjs-app-router-patterns` |
| Sesiones Supabase, middleware, OAuth | `nextjs-supabase-auth` |
| Revisión de UI / accesibilidad / UX | `web-design-guidelines` |
| Testing local del frontend con Playwright | `webapp-testing` |
| Endpoints FastAPI, schemas Pydantic v2, `Depends` | `fastapi-templates` |
| Diseñar capas routers/services/repositories, refactor | `python-design-patterns` |
| Tests pytest, fixtures, mocking asyncpg, async | `python-testing-patterns` |
| Coverage, umbrales en CI, reportes | `pytest-coverage` |

---

## Roadmap de Changes

> Fuente: [CHANGES.md](CHANGES.md) — **roadmap numerado C-01→C-30 COMPLETO** (C-30 archivado 2026-06-20; Fases 1-7 ✅). El trabajo activo es post-roadmap: **Fase V2.5 Finanzas** (cost-center ✅, journal-entry-outbox ✅, **BankReconciliation COMPLETA: C1 ✅ + C2 ✅ + C3 ✅** 2026-07-02) + **modelo de dominio V3** adoptado 2026-07-02 (`modelo-dominio-aliadata-v3.md`, extiende al V2). **RN-97 sigue vigente** para las columnas legacy aún no dropeadas (header plano de `sales`/`purchases` hasta C-20 Grupo 10).

### Próximo change recomendado (activo)
1. ~~**`v3-snapshot-pattern`**~~ ✅ **COMPLETADA 2026-07-02** (PR #255): snapshots de nombre/SKU/costo/IVA en líneas de documentos + `FiscalIdentitySnapshot` del receptor. **Desbloquea C-20 Grupo 10** (línea de servicio = `product_id NULL` + `name_snapshot`). Specs sincronizadas a main; change archivado.
2. ~~**`v3-document-status-history`**~~ ✅ **COMPLETADA 2026-07-03** (PRs #258/#259): tabla append-only `document_status_history`, catálogo `document_status_transitions`, helpers `record_status_transition`, `allowed_role` RBAC-ready. Specs (1 nueva + 5 modificadas) sincronizadas; change archivado.
3. ~~**`v3-notifications-realtime`**~~ ✅ **COMPLETADA 2026-07-04** (PR #262) · ~~**`v3-soft-delete-policy`**~~ ✅ 2026-07-06 · ~~**`v3-provisioning-seed`**~~ ✅ 2026-07-06 (PR #279) · ~~**`v3-catalog-masters`**~~ ✅ 2026-07-06 (PR #282) · ~~**`v3-reporting-invariants`**~~ ✅ 2026-07-07 (PRs #284/#285) · ~~**`v3-api-standards`**~~ ✅ **COMPLETADA 2026-07-07** (PR #287): RFC 7807 uniforme, paginación estándar `{items,total,page,pages}`, `Idempotency-Key` por header (incl. cierre de caja), DEC-24 (UoW = RPCs `SECURITY DEFINER`). Specs (`api-standards` nueva + `base-repositories` modificada) sincronizadas; change archivado.
4. **`v3-rbac-multirole`** [CRÍTICO — solo análisis hasta sign-off PO] → después: percepciones (V2.5, depende del snapshot fiscal ya completo).
5. ~~**`metodos-pago-operaciones`**~~ ✅ **COMPLETADA 2026-08-19** (PRs #419/#420, ad-hoc — pedido directo del PO, fuera de la secuencia `v3-*`): catálogo `payment_methods` (espejo de `cost_centers` + `kind` cerrado + `sort_order` + soft delete) sembrado por cuenta, imputación opcional en ventas/compras (alta y edición con preservación por `model_fields_set`), `rpc_payment_method_report`, superficies completas (selectores, manager en Configuración, badges/filtros, `/reportes/formas-pago`). Backfill de las 120 ventas del POS ejecutado (218 filas, idempotente). Specs sincronizadas (`payment-method` nueva); change archivado.
6. ~~**`pos-catalogo-pagos`**~~ ✅ **COMPLETADA 2026-08-19/20** (PRs #421/#422): unificó el POS al catálogo (grilla reemplaza los 2 botones hardcodeados), `credit` exige cliente + muestra saldo, caja condicionada al camino (no a la etiqueta), CHECK de `sales_orders.payment_method` ampliado a los 7 `kind`. Hallazgo no previsto: el bloque `credit` de C-30 estaba borrado en silencio desde una regresión de julio — restaurado desde la definición viva de prod. Specs sincronizadas; change archivado.
7. ~~**`edicion-preserva-contexto`**~~ ✅ **COMPLETADA 2026-08-20** (PR #423/#424): `branch_id`/`canal`/`unit_id`/`supplier_id`/`cost_center_id` dejan de perderse al editar una operación (quedaban `NULL` en silencio); venta con comprobante fiscal emitido pasa a inmutable (`P0423`); `quantity` acepta decimales en la edición. Specs sincronizadas (`operation-edit-context` nueva); change archivado.
8. ~~**`pagos-cableados-restantes`**~~ ✅ **COMPLETADA 2026-08-20** (PR #425/#426 — cierra OQ-B parcial, OQ-C, OQ-D y OQ-E parcial de `pos-catalogo-pagos`): helper compartido `_pay_register_party_charge` (capability nueva `party-account-charge`) despachado por POS y formulario de venta por igual; el formulario de venta a crédito ya postea el cargo en cuenta corriente (241 operaciones históricas que no lo hacían); opt-in de caja en el formulario con las tres condiciones verificadas en servidor; `rpc_create_purchase_operation` ya no hardcodea `'credit'` en el evento (afectaba el 100% de las 38 compras históricas); `wallet` rutea a `1110 Banco` en el consumidor contable; operación con cargo/movimiento posteado pasa a inmutable (`P0423`, mismo patrón que el bloqueo fiscal). Specs sincronizadas (`party-account-charge` nueva; `cash-session`/`customer-account`/`journal-entry`/`operation-edit-context`/`payment-method` modificadas); change archivado. **Los tres candidatos que dejó registrados ya tienen destino**: `pos-banco-movimientos` ✅ 2026-08-20 (PRs #427/#428, cierra OQ-A), `asiento-venta-formulario` ✅ 2026-08-20 (PRs #431-#435), `compras-proveedor-cuenta-corriente` ✅ 2026-08-23 (#452/#455, ítem 9). Después: `delete-guard-ledgers` ✅ 2026-08-22 (#438/#439) y `banco-caja-historial-ajustes` ✅ 2026-08-22 (#440/#441); fixes ad-hoc #442/#443 (`validate_sign_coherence` + `sale_reversal`).
9. ~~**`compras-proveedor-cuenta-corriente`**~~ ✅ **COMPLETADA 2026-08-23** (apply PR #452 `2819772` + fix post-demo PR #455 + archive; demo real del PO en prod, saldo compensado verificado en DB) (`openspec/changes/compras-proveedor-cuenta-corriente/`, governance MEDIUM): ABM de proveedores (`/proveedores` + entrada de sidebar en Catálogo, backend 3 capas extendiendo el `SupplierRepository` huérfano), identidad fiscal espejo de `clients` (RN-96), selector de proveedor con alta inline en el form de compra, y cargo real en `supplier_accounts` vía `_pay_register_party_charge(..., 'supplier', ...)` cuando la forma de pago es `kind='credit'` (deroga OQ-3/B de C-30 — BREAKING de dominio declarado). Cero lógica nueva de cuenta corriente; activa y cubre con tests tres piezas ya escritas y nunca ejercitadas (guard `P0423` de compra, compensación `P0425`, rama `2100 Proveedores`). Migración `20261009000001` (4 commits `ba0f3b4`/`c0c2790`/`5132e2d`/`caef30a`, rama `opsx/compras-proveedor-cuenta-corriente-apply`). Las 6 OQs se resolvieron todas por su opción recomendada (el PO no respondió) — detalle en `CHANGES.md` (entrada propia). Hallazgos reales: (1) el `pg_get_functiondef` vivo de `rpc_create_purchase_operation` había divergido del último archivo de migración por una reescritura in-place del G3 de `20261003000001` (atrapado por el checkpoint 1.4 antes de escribir SQL); (2) `suppliers.company_id` seguía `NOT NULL` legacy, corregido con `DROP NOT NULL` guardado. La KB (04 y modelo V2) **sí tenía razón** en que `suppliers` ya tenía `tax_id` — corregido en task 15.1, el error estaba en el `design.md` de este mismo change, no en la KB. Verificación (fase D, 2026-08-23): backend 1570/1570, frontend 1331/1332 (1 pre-existente ajeno), tsc sin nuevos errores, a11y con 2 gaps reales corregidos (`supplier-form.tsx`/`searchable-select.tsx`). 14.3/14.5/14.6 se cerraron con la demo real del PO en prod (2026-08-23). El ítem 10 (`cuenta-corriente-party-guard`) ya se aplicó y archivó el mismo día.
10. ~~**`cuenta-corriente-party-guard`**~~ ✅ **COMPLETADA 2026-08-23** (propose PRs #448/#450 · apply PR **#457** `21dbe38` · archive el mismo día; archivada en `openspec/changes/archive/2026-08-23-cuenta-corriente-party-guard/`, governance MEDIUM con un tramo de severidad alta). Cerró el hueco de tenencia de las 3 RPCs de cuenta corriente que mueven dinero (`rpc_register_payment_received`/`_made`/`rpc_register_supplier_charge`): resolvían el tenant desde la sesión pero **no validaban que la parte le perteneciera**, y `c30_get_or_create_*` insertaba igual (el FK a `clients`/`suppliers` no está scopeado) → saldo, cobros y asiento contra una entidad invisible en las listas del tenant. **Diseño opción B**: guard en el choke point `c30_get_or_create_*` (cubre todos los caminos presentes y futuros) + guard explícito en las 3 RPCs como defensa en profundidad, `P0404` → 404 RFC 7807. **OQ-2 se resolvió como hotfix #454**, fuera del change: `_pay_register_party_charge` y `_journal_post_from_event` eran `SECURITY DEFINER` con el tenant por parámetro y `GRANT` a `authenticated` = escritura cross-tenant real; el apply se reconcilió sin REVOKE duplicado. El gate de ACLs sumó el **chequeo (4)** (barrido por convención de nombre) conviviendo con el **(3)** angosto de #454; allowlist del (4) con una sola entrada, `_c29_confirm_order_core`. Migración final **`20261011000001`** (doble renumerado: reservó `20261008000001`, #452 tomó `20261009000001` forzando `20261010000001`, y el hotfix #454 tomó ése). Sin superficie frontend (declarado). **Verificación post-merge en prod** ✅ (`MAX(version)`, ACLs de las 7 funciones, cuerpo vivo con el guard, 3→1 helpers expuestos) y **auditoría de daño histórico 0/9** → no hay reparación pendiente, OQ-5 cerrada por ausencia de datos y del PO sólo resta darse por enterado. Backend 1604/0/3.
11. ~~**`tenancy-guard-caja-outbox`**~~ ✅ **COMPLETADA 2026-08-24** (propose PR **#459** `6191631` · h2 hotfix PR **#460** `5ce75cf` · h1 apply PR **#461** `d41838c` · archive el mismo día; archivada en `openspec/changes/archive/2026-08-24-tenancy-guard-caja-outbox/`, governance CRÍTICO con sign-off del PO firmado el 2026-08-24). Cerró juntos **h1 + h2**, los dos hallazgos laterales de severidad **alta** y **reproducidos** que dejó `cuenta-corriente-party-guard`. **h1**: el POS aceptaba un `p_cash_session_id` de otro tenant y le escribía en la caja — cerrado con guard en dos capas: sucursal en `_c29_confirm_order_core` (`P0422`, predicado copiado del formulario) + backstop de tenant en `c28_register_cash_movement` (`P0401`, resolución por FKs, sin cambiar la firma). **h2 + h2 bis** salieron como hotfix separado (PR #460, por decisión del PO en OQ-1): `REVOKE` de `rpc_process_outbox_batch`/`rpc_mark_event_processed` de los tres roles de aplicación + `REVOKE` a nivel tabla sobre `public.events` + el endpoint del outbox pasó a `get_service_conn` + `require_platform_admin` + `rpc_process_outbox_dispatch`, y se retiró `OutboxRelayService` (los dos relays competían por el mismo flag `processed_at`; ganar el relay Python perdía el asiento contable para siempre). Gates nuevos permanentes: chequeo **(5)** del gate de ACLs (lista curada de funciones que leen/actualizan `public.events`) y `test_outbox_single_dispatcher.sql`. Migraciones `20261012000001_revoke_outbox_cross_tenant.sql` (h2) y `20261013000001_tenancy_guard_caja_sesion.sql` (h1 — el número que preveía el propose se lo llevó el hotfix de h2). **Sin superficie frontend** (declarado y verificado). **Auditoría de daño histórico: 0 en los cuatro conteos** re-medidos en prod (movimientos de caja cross-tenant, eventos procesados sin asiento/sin notificación) — OQ-5 cerrada por ausencia de datos. **Verificación post-merge en prod** ✅ (`MAX(version) = 20261013000001`, 262 migraciones; ACLs y cuerpos vivos con los guards; `events` sin `SELECT`/`INSERT` para `anon`). Dos rondas de revisión adversarial corrigieron, antes del merge: un guard de capa 1 que no era autosuficiente (delegaba en un chequeo posterior evadible declarando también la sucursal ajena), la elección de `P0401` sobre `P0001` para no abortar `db reset`, y dos regresiones propias del detector del chequeo (5). Detalle completo en `CHANGES.md`. **Quedan como chequeo de rutina, no bloqueante**: confirmar el pg_cron `relay-process-outbox` y la demo en vivo al PO.
12. ~~**`sucursal-guard-vaciado-auditoria`**~~ ✅ **COMPLETADA 2026-08-25** (propose + apply PR **#465** `043017a` squash · archive el mismo día; archivada en `openspec/changes/archive/2026-08-25-sucursal-guard-vaciado-auditoria/`, governance MEDIO). Ad-hoc, pedido directo del PO tras el incidente del 22→24-08: una sucursal con 518 productos/585 unidades se desactivó sin ninguna verificación, dejando el negocio invendible dos días. **G1**: disparador `BEFORE UPDATE OR DELETE` sobre `public.branches` (`trg_guard_branch_decommission`, punto de paso obligado que cubre los cuatro caminos de baja) rechaza con `P0428` si hay existencias (`branch_stock <> 0`), sesión de caja abierta o transferencias sin completar; el borrado físico queda prohibido incondicionalmente. `rpc_close_branch` se unificó de `P0409` a `P0428` conservando el token de texto para no romper la traducción del cliente. **G2**: autoría (`created_by`/`deactivated_at`/`deactivated_by`, sin backfill, sin FK dura) + ciclo de vida completo en `audit_logs` vía `trg_audit_branch_lifecycle` (sin notificaciones). **G3**: transferencia entre sucursales descubrible desde `/stock` (acción por fila, reutiliza `TransferStockModal`) y camino directo desde el error de venta por falta de stock. Hallazgo no previsto: 15 gates SQL preexistentes limpiaban con `DELETE FROM branches` en cascada — CI encontró una tercera ronda que el apply local no vio (bloque de cleanup "intruso" separado); 41 wraps `session_replication_role=replica` sobre 20 archivos en total. **Verificación post-merge en prod** ✅ (`MAX(version) = 20261014000001`; 2 triggers vivos; 3 columnas de auditoría; auditoría de daño histórico re-medida en **0** sobre 40 sucursales; humo real: `UPDATE is_active=false` sobre Showroom con 531 productos bloqueado con `P0428`). Specs sincronizadas (`branch-decommission-guard` nueva; `branches` + `branch-stock` modificadas). Detalle completo en `CHANGES.md`.
13. ~~**`v31-tenancy-pool-rls`**~~ ✅ **ROLLOUT COMPLETO 2026-08-26 · ARCHIVADA 2026-08-27** (PR **#469** `0664619`; archivada en `openspec/changes/archive/2026-08-27-v31-tenancy-pool-rls/`, governance CRÍTICO con sign-off del PO en cada uno de los dos pasos). Cierra **H-05**: el pool corría como `postgres` con `BYPASSRLS`, así que la RLS era inerte para el backend. Opción A: transacción explícita por request + claims del JWT con alcance transaccional (Paso 1) + adopción del rol `authenticated` por transacción vía `SET LOCAL ROLE` (Paso 2), ambas palancas **ON en prod** — la RLS se evalúa de verdad y `is_account_writer`/`current_account_ids` son efectivos para el backend. **Deja dos residuos vivos**: el **Paso 3** (rol de login dedicado sin atributo de bypass, OQ-2 opción (b)) y el retiro de las dos palancas. Y un **riesgo aceptado, D6**: *"el cambio de rol por transacción falla abierto si un camino lo omite"* — ver el ítem 14.
14. ~~**`v31-tenancy-role-assertion`**~~ ✅ **COMPLETADA Y ARCHIVADA 2026-08-28** (propose PR **#471** `d1a9c60` · archive PR **#472** `a1a2dc8` — el 5º requirement de `asyncpg-pool` ya está en `main`; puntero corregido 29-08, había quedado anunciando el archive como pendiente después de mergeado). **Retro-spec: no escribió una línea de código** — documenta como requirement la comprobación *fail-closed* de la adopción del rol ya mergeada en `d7b5214` (PR **#470**) y verificada en prod. **Sin superficie frontend** (declarado). **Lo que hay que no perder**: D6 quedó **cerrado parcialmente, NO cerrado** — su modo *inefectividad* (la adopción se emite y no tiene efecto) lo cierra #470; su modo *omisión* (un camino que nunca emite la adopción) **sigue abierto por construcción**, porque la comprobación vive dentro de la dependencia de conexión de request. La garantía estructural del modo omisión es el **Paso 3** del ítem 13, que por eso no pierde su justificación. Evidencia de prod (2026-08-28): cero rechazos del guard → sin daño histórico que auditar; el control positivo prueba que la búsqueda encuentra tráfico cuando lo hay, **no** que hubiera tráfico en la ventana. Detalle completo y los tres candidatos que deja en `CHANGES.md`.
15. ~~**`planes-suscribirse-plan-vigente`**~~ ✅ **COMPLETADA 2026-08-29** (propose + apply + PR **#473** `1ab55a3` mergeado squash + archive el mismo día; archivada en `openspec/changes/archive/2026-08-29-planes-suscribirse-plan-vigente/`, governance MEDIO). El discriminante de CTA de contratación en `/planes` pasa de "¿es el plan efectivo?" a "¿la cuenta tiene una suscripción viva?" — la cuenta con acceso a un tier por cortesía/trial/plan por vencer pero sin suscripción viva ya puede contratar la recurrencia de ese mismo tier (era el caso real que dejó a Daniel sin forma de auto-renovarse desde la UI, ver `mp-real-subscriptions` en `CHANGES.md`). Cuenta exenta (`billing_exempt`) sin ningún CTA en ningún tier, con comparativo completo + aviso explícito de acceso sin cargo; downgrade sigue derivando a `/facturacion` sin cambios. **Hallazgo lateral no bloqueante**: el espejo TypeScript de `getEffectivePlan` no lee `plan_expires_at` — medido en prod 29-08, **0 cuentas afectadas hoy**; no se arregla en este change (100% frontend), queda como candidato barato. Las 5 OQs se resolvieron por sign-off del PO ("aplicalo con todas las recomendaciones"). **Sin superficie backend/DB** (declarado y verificado por `git diff --stat`). Specs sincronizadas (`billing-ui`: 2 MODIFIED + 1 ADDED). Detalle completo en `CHANGES.md`.

16. ~~**`gastos-forma-pago`**~~ ✅ **COMPLETADA 2026-08-30** (propose PR **#475** `348b573` · apply PR **#476** `4914f3b` mergeado 2026-08-30 · archive el mismo día; archivada en `openspec/changes/archive/2026-08-30-gastos-forma-pago/`, **governance MEDIA con un tramo de severidad ALTA**: escribe dinero en caja y en banco). Cierra la deuda OQ-4 de `pos-banco-movimientos`: el gasto era el único documento operativo que no decía con qué se pagó. `expenses` gana `payment_method_id` y `branch_id` (RN-93); el `kind` derivado del catálogo despacha `cash` → movimiento de caja `expense` bajo opt-in **pre-marcado** con tres condiciones verificadas en servidor (D1), `transfer`/`card`/`check`/`wallet` → `bank_movement` incondicional vía el helper compartido de operaciones, `credit` → rechazado con `P0400` (D3, un gasto no tiene contraparte con cuenta corriente). Alta/edición/borrado migraron a tres RPCs `SECURITY DEFINER` nuevas (antes `INSERT` plano + `DELETE` crudo, sin RPC alguna); el borrado compensa los dos libros en la misma transacción (`expense_reversal` nuevo en caja + espejo en banco), con `P0426` si no hay sesión de caja abierta (D8); el gasto con dinero posteado es inmutable (`P0423`, D11). **Hallazgo que cambió el diseño**: el propose midió **0 de 37** `payment_methods` con `bank_account_id` configurado — con el destino sin resolver el helper compartido hace `RETURN NULL` sin error, así que el pedido literal del PO habría fallado en silencio para el 100% de los tenants; cerrado con selector de cuenta bancaria **obligatorio** + `P0412` en el caller de gasto (D5). De paso cierra dos bugs preexistentes: el alta descartaba `branch_id` (0/175 gastos con sucursal) y la edición borraba `cost_center_id` en silencio. Los 175 gastos históricos quedan **sin imputar y sin backfill** (D7, firmado por el PO). Fuera de alcance declarado: asiento contable (diferido a V2.6), eventos al outbox, soft delete, forma de pago en el importador CSV (D13). Superficie completa sobre `/gastos` (ruta y menú ya existentes), listado migrado a `GET /expenses` paginado (D18) para que `is_payment_locked` llegue como derivado del servidor; verificación visual con 16 capturas en las 4 combinaciones, contraste medido ≥4,83:1. Migración `20261015000001`. Specs sincronizadas: `expense-operation` (**capability nueva**, 12 requirements), `cash-movement`/`bank-movement`/`payment-method`/`operation-delete-compensation`/`operation-edit-context` (modificadas/ADDED). Detalle completo, las cinco lecciones verificadas y los candidatos que deja abiertos en `CHANGES.md`.

17. ~~**`qa-integral-modulos`**~~ ✅ **COMPLETADA 2026-09-02** (propose PR **#478** `832880a` mergeado 2026-08-31 · apply PR **#479** `6b992fe` mergeado 2026-09-01 · archive PR de este mismo cierre; archivada en `openspec/changes/archive/2026-09-02-qa-integral-modulos/`, governance LOW/MEDIUM por grupo — MEDIUM lo que toca `ui/popover.tsx`, `ui/sidebar.tsx`, el sistema de toast, el guard de proveedor, billing y las 4 reescrituras de RPC). Cierra por **causa raíz** los 27 hallazgos confirmados del QA integral del 2026-08-30 (informe copiado al change archivado: `qa/INFORME.md` — fuente de verdad) + 3 defectos de backend de la misma corrida. **Dos componentes compartidos explican 17 de los 27 síntomas**: el popover portalizado fuera del shard de scroll del modal (bug del PO: ningún desplegable scrollea dentro de un modal) y el `<main>` del shell sin `min-w-0` (12 pantallas desbordan en móvil escondiendo botones primarios). 16 grupos G1-G16, migración `20261016000001` (**4** RPCs bajo regla de integridad de función — `rpc_delete_expense` se sumó en el apply), 3 OQs no bloqueantes resueltas por su recomendación. **Absorbe el candidato del desborde de `/gastos`** anotado en el ítem 16. Specs sincronizadas al archivar (2026-09-02): `responsive-shell` (**capability nueva**, 6 requirements) + 6 modificadas (`comparative-reports`, `reporting-invariants`, `cash-session`, `data-export`, `supplier-directory`, `expense-operation`); `openspec validate --specs --strict` → 90/90. El apply deja **un residuo con nombre propio**: `tablet-filtros-cta` (a 1024px con el riel expandido el CTA de `/ventas`, `/gastos`, `/compras` y `/clientes` queda fuera del viewport inicial porque la barra de *filtros* no wrappea; el documento ya no se estira y el CTA es alcanzable con scroll propio — la pasada de tablet 768–1024 era Non-Goal declarado, así que la cláusula del delta se acotó a móvil en vez de prometer lo que no cumple). Detalle en `CHANGES.md`.

18. ~~**`seguros-perfil-asesor`**~~ ✅ **COMPLETADA 2026-09-01** (propose + apply PR **#480** `446b1aa` mergeado · archive el mismo día; archivada en `openspec/changes/archive/2026-09-01-seguros-perfil-asesor/`, governance MEDIO). Publica el acuerdo comercial real con el primer partner (Julián Dupás, PAS Mat. N.º 98506) en `/seguros`, que estaba en producción desde 2026-03 con **0 filas** en `community.seguros`. Extiende `community.seguros` con un discriminador `entry_type` (`'offer'`/`'advisor'`, decisión D1/D2 — sin tabla nueva, sigue la regla de reutilización antes que repetición), perfil estructurado en `/seguros/[slug]`, índice `/seguros` adaptativo al conteo real de asesores visibles, tracking de clicks desglosado por vía de contacto (función nueva, el contador legacy no se toca) y superficie completa de alta/edición en `/admin/seguros`. Seed sembrado con `is_visible = false`: publicar fue acción explícita del PO, quien activó el toggle y confirmó visualmente el perfil (landing + perfil) el mismo día, con el primer click real de WhatsApp trackeado. Las 6 OQs del design se firmaron todas. **Hallazgo real**: `ON CONFLICT (slug) DO UPDATE` fallaba con `42P10` porque el índice único de `slug` es parcial — corregido a `ON CONFLICT (slug) WHERE slug IS NOT NULL DO UPDATE`, atrapado antes de prod. **Segundo hallazgo, arreglado aparte**: tipar `AdminStats.timeSeries` (retirando un `useState<any>`) destapó que el chart "Evolución temporal" de `/admin/seguros` venía roto desde que se escribió — corregido en **PR #482** (`9992e71`, sin change OPSX). El mismo `useState<any>` + `processTimeSeries` mal tipado está confirmado idéntico y sin arreglar en `/admin/feria-ia` y `/admin/copilot-ia` (candidato con chip, no urgente). PR satélite **#481** (`fc83172`): sección "Nuestros aliados" en la landing pública, sin change OPSX (superficie distinta, fuera de alcance por OQ-5). Specs sincronizadas (`insurance-advisor-profile` nueva, 11 requirements). Detalle completo en `CHANGES.md`.

19. ~~**`caja-compras-cobranzas`**~~ ✅ **COMPLETADA 2026-09-02** (propose + apply PR **#485** `4c42fb7` mergeado squash 2026-09-02 · archive el mismo día; archivada en `openspec/changes/archive/2026-09-02-caja-compras-cobranzas/`, **governance MEDIA con un tramo de severidad ALTA**: escribe dinero real en caja desde RPCs `SECURITY DEFINER`). Pedido textual del PO: *"No se registra la compra con Efectivo en el historial de caja y tampoco cuando cobrás una cuenta corriente; tiene que funcionar; también cuando se elimine una compra en efectivo se compense."* Cierra tres huecos a la vez —compra en efectivo → caja, cobro/pago de cuenta corriente en efectivo → caja, borrado de una compra con movimiento de caja → compensación— verificados contra el `pg_get_functiondef` **vivo de prod**: ninguna de las cuatro RPCs (`rpc_create_purchase_operation`, `rpc_delete_purchase_operation`, `rpc_register_payment_received`, `rpc_register_payment_made`) tocaba caja. Las tres de alta suman `p_cash_session_id` vía `DROP FUNCTION` + `CREATE` (nunca `CREATE OR REPLACE` con `DEFAULT`, para no dejar un overload vivo — gotcha `42725` ya registrado); revive el tipo `purchase_payment` (reservado y muerto desde C-28, re-etiquetado "Compra en efectivo") y suma `payment_received`/`payment_made`/`purchase_payment_reversal` al vocabulario de caja. La compra exige las **mismas tres condiciones** que venta/gasto (`kind='cash'` + sesión abierta en su sucursal + fecha de hoy); el cobro y el pago de cuenta corriente sólo exigen **dos** (sin fecha ni sucursal propias — un cobro se registra en el instante en que ocurre). **Hallazgo lateral load-bearing (D3)**: 0 de 507 compras tenían `branch_id` — `BranchSelect` era decorativo y los **dos** callers de `PurchaseRepository` pasaban `NULL` literal a la RPC; mismo bug que `gastos-forma-pago` encontró en gastos (0/175), acá load-bearing porque la condición de sucursal del opt-in de caja depende de él. Los 11 documentos históricos (4 compras + 6 cobros + 1 pago) quedan sin backfill por tres razones independientes: imposible derivar qué cobros fueron en efectivo, falsearía el arqueo de las 3 sesiones abiertas hoy (RN-95), y precedente firmado con los 175 gastos de `gastos-forma-pago`. Cero eventos al outbox y cero helpers SQL nuevos (todo delega en `c28_register_cash_movement` ya existente). **Gate roto y corregido**: `test_cuenta_corriente_party_guard.sql` resolvía la firma vieja de 6 args de las RPCs de pago por `pg_get_functiondef(...::regprocedure)`; corregido a la firma nueva de 7. **Humo real del PO (2026-09-02): PASÓ completo** — compra, cobro, pago y borrado con compensación verificados con dinero real en `/caja`; dos observaciones del PO resueltas **sin cambio de código** (caja en negativo con saldo inicial $0 = aritmética honesta, ningún camino bloquea por saldo; un checkbox que el PO creyó nuevo era el del formulario de venta, sin relación con este PR). Migración `20261018000001`. Specs sincronizadas: `purchase-operation` (**capability nueva**, 3 requirements), `cash-movement`/`cash-session`/`customer-account`/`supplier-account`/`payment-method`/`operation-delete-compensation`/`operation-edit-context` (modificadas). Deja dos candidatos con chip: `cobranzas-reverso` y `cobranzas-catalogo-pagos`. Detalle completo en `CHANGES.md`.

20. ~~**`cobranzas-reverso`**~~ ✅ **COMPLETADA 2026-09-02** (propose + apply PR **#487** `d1b34b4` mergeado squash · archive el mismo día; archivada en `openspec/changes/archive/2026-09-02-cobranzas-reverso/`, **governance MEDIA con un tramo de severidad ALTA**: escribe dinero real en cuatro libros desde RPCs `SECURITY DEFINER` nuevas). Cierra **OQ-4 de `caja-compras-cobranzas`** (ítem 19): hasta este change no existía ningún camino para deshacer un cobro de cuenta corriente ni un pago a proveedor mal cargado. Dos RPCs nuevas (`rpc_reverse_payment_received`/`rpc_reverse_payment_made`), molde exacto de `rpc_delete_expense`: compensan cuenta corriente con un contra-movimiento de **tipo propio** (nunca nota de crédito — `credit_note` revierte cargos, no cobros), caja (disparo por existencia del movimiento, nunca por su signo — mismo principio que `caja-compras-cobranzas`) y banco, y disparan la reversión del asiento contable vía el outbox; el documento del pago se **borra** al final (sign-off del PO, OQ-1), corrigiendo gratis el KPI `collected_revenue`. **Sin ventana temporal** (OQ-4): el único límite es el mismo `P0426` + sesión de caja abierta que ya rige para el resto de las compensaciones — decisión estructural (D6), el reverso siempre suma deuda y nunca puede dejar un saldo inválido. **El contra-asiento contable entra en alcance de este change** (a diferencia de `gastos-forma-pago`/`caja-compras-cobranzas`, que lo difirieron): las ramas `PaymentReceived`/`PaymentMade` de `_journal_post_from_event` ya estaban vivas con el 100% de los pagos históricos posteados. **Hallazgo real del propio TDD**: los derivados `is_reversible`/`is_reversal_blocked`/`has_cash_movement`/`has_bank_movement` se sumaron primero sólo al endpoint paginado (`list_movements_page`); las pantallas reales de cuenta corriente usan `get_account()` → `list_movements()`, una query distinta — sin el mismo fix ahí, "Anular" nunca habría aparecido en la UI pese a tests unitarios en verde. Corregido antes del PR. **Gate preexistente roto, encontrado y chipeado** (no corregido en este change): `test_sales_order_payment_method_drop.sql` tiene un falso positivo desde que `caja-compras-cobranzas` agregó `payments_received.payment_method`/`payments_made.payment_method`, columnas reales que su regex de exclusión no contempla. **Specs de `transactional-outbox` saneadas de paso**: el invariante D13 (11 `event_type` canónicos entre el filtro de `_journal_post_from_event` y el del Consumer 3 del dispatcher) pasa de sostenerse sólo por comentario a tener **gate real** con matriz de evasión ejecutada, y la spec declara ahora el conjunto canónico explícito. **Humo real del PO (2026-09-02): PASÓ completo** — "funciona todo", cobro en efectivo anulado con compensación verificada en los cuatro libros. Migración `20261019000001`. Specs sincronizadas: `payment-reversal` (**capability nueva**, 9 requirements), `customer-account`/`supplier-account`/`cash-movement`/`journal-entry`/`operation-delete-compensation`/`transactional-outbox` (modificadas). Deja un candidato vivo: `cobranzas-catalogo-pagos`. Detalle completo en `CHANGES.md`.

21. ~~**`cobranzas-catalogo-pagos`**~~ ✅ **COMPLETADA 2026-09-02** (propose + apply PR **#489** `53fc6a3` mergeado squash 2026-09-02 · archive el mismo día; archivada en `openspec/changes/archive/2026-09-02-cobranzas-catalogo-pagos/`, governance MEDIA). **Cierra la saga de cobranzas completa**: con este change el catálogo `payment_methods` queda unificado en las **cinco** superficies de dinero (POS, venta, compra, gasto, cobros/pagos). Cierra el **Non-Goal 3 de `caja-compras-cobranzas`** (ítem 19) — espejo de `pos-catalogo-pagos` para el mostrador: el cobro de cuenta corriente y el pago a proveedor eran los **dos últimos** documentos operativos con un vocabulario de forma de pago propio (`p_payment_method text` contra `{cash,transfer,card,check}`), ajeno al catálogo de 7 kinds que venta/compra/gasto/POS ya consumen. `rpc_register_payment_received`/`rpc_register_payment_made` cambian el 5º arg de `text` a `uuid` (`DROP FUNCTION` + `CREATE`, mismo molde `42725` de siempre); el `kind` se deriva del catálogo bajo `account_id` (`P0404` fuera de tenant); `credit` se rechaza (`P0400`); `wallet`/`other` se suman a la taxonomía aceptada (6/7 kinds); el ruteo bancario adopta `_pay_register_operation_bank_movement` conservando el guard estricto de cuenta bancaria (D4, mismo hallazgo que `gastos-forma-pago` D5: 0/266 formas de pago con destino configurado). `payments_received`/`payments_made.payment_method` (text, 0/7 poblado) se reemplaza por `payment_method_id` FK, sin backfill. Frontend: `PaymentMethodSelect` gana el contexto `"collection"` compartido por los dos modales; **D11, el hallazgo que más importaba**: el opt-in de caja funcionaba hoy por coincidencia de nomenclatura entre el `<Select>` local y el `kind` del catálogo — con el selector emitiendo un uuid habría desaparecido sin error, cerrado con un test de regresión dedicado. **Checkpoint 1.6 encontró 4 callers no anticipados por el propose** (`supabase/tests/test_pos_banco_movimientos.sql`, `backend/tests/test_c2_bank_payment_routing.py` + dos test files de hooks del frontend), migrados en el mismo PR. Migración `20261020000001`. Verificación: backend 1745/1745 (cov. 97%), frontend 1817/1817 (2 fallos de `AdminSegurosPage.test.tsx` bajo carga, flaky-preexistente confirmado al aislar), `tsc` sin errores nuevos, migración limpia contra `supabase db reset` local y 38/39 gates de `KPI_Validation.yml` en el orden real del workflow (el único que falla, `test_cuentas_billetera_tipo.sql`, no tocado por este change, por un artefacto de invocación local). **Verificación post-merge en prod** ✅ (sesión de archive, 2026-09-02): `20261020000001` viva, 2 RPCs con `p_payment_method_id uuid` sin overload, columna text ausente de ambas tablas, ACLs exactas. **Humo real del PO: PASÓ** ("funciona todo") — catálogo completo en los modales, cobro por billetera al banco, checkbox de caja intacto. **Hallazgo lateral confirmado, no de este change** — candidato con chip (`task_41cb73e9`): el paso de reaplicación de idempotencia de `KPI_Validation.yml` ya está roto en `main` desde antes de este PR (falla incluso en PRs de archive puro) — ninguna de las tres migraciones más recientes se sumó nunca a esa cadena, este PR sigue el mismo precedente. Deja un candidato: **OQ-5**, configurar destinos bancarios por defecto en las 266 formas de pago (fuera de alcance declarado). Specs sincronizadas: `payment-method`/`customer-account`/`supplier-account`/`cash-session`/`payment-reversal` (modificadas). Detalle completo en `CHANGES.md`.

22. ~~**`cobranzas-panel`**~~ ✅ **COMPLETADA 2026-09-03** (propose PR **#494** · apply PR **#497** `23375a6` mergeado y desplegado 2026-09-02, migración `20261021000001` viva en prod · archive el mismo día; archivada en `openspec/changes/archive/2026-09-03-cobranzas-panel/`, governance MEDIA-baja). **Etapa A del módulo de cobranzas**: tablero `/cobranzas` de deudores (RPC nueva `rpc_receivables_report`, migración `20261021000001`, molde de `rpc_payment_method_report` con guard `P0401`), `GET /reports/receivables` + `/summary` (D2: el resumen agrega sobre el MISMO RPC — un solo predicado de deudor), KPI "Por cobrar" en la grilla diaria del Tablero (D6, `KpiCard` gana prop `href`), columna Saldo + acceso a la cuenta en `/clientes` (D9: `LEFT JOIN` 1:0..1 en `_classified_activity_cte`, hot path intacto — SAFETY NET 53→59 backend / 25→30 frontend sin regresiones). Cero escritura de dinero; Cobrar reutiliza `RegisterPaymentForm` en Dialog y la invalidación de `receivables.*` vive en los hooks de mutación (D8, 9 mutaciones). D4 normativo: "Último cargo"/"Último cobro", nunca mora; reversa no rejuvenece; días ART. OQs firmadas: OQ-1/OQ-2 → Etapa B (`cobranzas-vencimientos`, ítem 23); OQ-3 KPI sin BranchFilter; OQ-4 adjustment aparece con "—". Verificación: backend 1766/1766 (cov 91,43%), frontend 1855/1857 (2 = AdminSegurosPage flaky-preexistente), 40 gates SQL (39+nuevo PASS, único FAIL el artefacto local conocido), visual real en 4 combinaciones con stack local completo. **Verificación post-merge en prod** ✅ (sesión de archive, 2026-09-03): `MAX(version)` = `20261021000001`, ACLs vivas de `rpc_receivables_report` (anon sin `EXECUTE`), panel vs. baseline en 11 deudores / $557.000, 0 deudores con cliente dado de baja. **Humo real del PO (2026-09-03): PASÓ** ("funciona todo") — cobrar desde la fila del panel y desde la cuenta corriente del cliente, panel y KPI del Tablero refrescándose solos sin recargar. Hallazgo lateral: `test_cobranzas_reverso.sql` nunca se cableó a CI (candidato, mismo de siempre); el gate nuevo sí quedó en `KPI_Validation.yml`. Specs sincronizadas: `receivables-panel` (**capability nueva**, 11 requirements), `client-activity` (modificada, +2 requirements). Detalle completo en `CHANGES.md`.

23. ~~**`cobranzas-vencimientos`**~~ ✅ **COMPLETADA 2026-09-03** (propose PR **#496** mergeado · apply PR **#498** mergeado, migración `20261022000001` viva en prod · archive el mismo día; archivada en `openspec/changes/archive/2026-09-03-cobranzas-vencimientos/`, **governance MEDIA con tramo de severidad ALTA**: reescribe las funciones que escriben dinero en cuentas corrientes). **Etapa B del módulo de cobranzas** (sign-off del PO 2026-09-02, las 6 OQs firmadas), cierra la saga completa: plazo de pago en cascada (`accounts.default_payment_terms_days` → `clients`/`suppliers.payment_terms_days`, `NULL` = sin plazo, nunca 0 — D2 opt-in), vencimiento congelado en la fila de cargo del ledger (`due_date`, patrón snapshot D1), imputación FIFO **derivada pura** por línea de flotación (D4, invariante `SUM(abierto) = balance` con gate; el reverso reabre solo, jamás es cargo), aging por 5 tramos (sin-vencimiento propio, D5), `rpc_receivables_report` extendido + `rpc_payables_report` espejo, barrido diario `pg_cron` con dedup por día argentino (D8, 9º/10º tipo del Consumer 4 — el conjunto canónico de 11 del Consumer 3 no cambia), plantillas de email del digest, pestaña **Por pagar** en `/cobranzas` (OQ-6), recordatorio de WhatsApp reutilizando `buildWhatsAppUrl` (D12), vencimiento editable en el formulario de venta (POS usa la cascada sin UI, D11), campo de plazo en client/supplier-form (tri-estado D14) y 9ª pestaña Cobranzas en `/configuracion` (D10). **Verificación post-merge en prod** ✅ (sesión de archive, 2026-09-03): `MAX(version)` = `20261022000001` (271 migraciones), una sola definición viva de cada una de las 9 funciones reescritas sin overload, ACLs vivas (`_pay_register_party_charge` sin `authenticated`), cron `cobranzas-overdue-digest-sweep` activo, 0 cargos con `due_date` no nulo y 0 movimientos históricos reescritos. **Demo real del PO (2026-09-03): PASÓ** ("funciona todo") — plazo configurado, venta a crédito con vencimiento, cobro parcial con imputación FIFO, anulación con reapertura, recordatorio por WhatsApp; cubre también la pasada visual de 4 combinaciones (10.4), sin tanda de capturas dedicada porque el sistema de tokens ya garantiza AA (gate `token-contrast-aa`). Specs sincronizadas: `receivables-aging` (**capability nueva**, 9 requirements), `receivables-panel` (modificada: +3 requirements, ~1 modificado, **-1 removido** con `Reason`/`Migration` — la prohibición de mostrar mora, que dejó de aplicar al existir vencimientos reales), `customer-account`/`supplier-account` (cada una +1 requirement, ~2 modificados), `party-account-charge` (~2 modificados: vencimiento opcional resuelto por cascada), `in-app-notifications` (+2 requirements), `transactional-email-delivery` (+1 requirement). Con este ítem **la saga de cobranzas queda completa** (Etapa A ítem 22 + Etapa B ítem 23). Detalle completo en `CHANGES.md`.

24. ~~**`productos-categorias-sku`**~~ ✅ **COMPLETADA Y ARCHIVADA 2026-09-04** (propose PR **#502** `8feaf30` · apply PR **#504** `3d36a3e` · archive el mismo PR que `estadisticas-ventas` (ítem 25); archivada en `openspec/changes/archive/2026-09-04-productos-categorias-sku/`, governance MEDIA). Catálogo `product_categories` por cuenta (espejo de `payment_methods` sin `kind`), `products.category_id` FK como fuente de verdad con `products.category` TEXT conservado como **espejo por trigger** (D1 — ningún lector legacy cambia), seed de las 7 legacy + backfill idempotente (5.084 productos, medición re-confirmada), SKU visible con unicidad **por cuenta** case-insensitive (D4, `rpc_bulk_upsert_products` desde el cuerpo vivo `dc56bfc2…`), tri-estado por `model_fields_set` (D12), `ProductCategorySelect` con alta inline en el lugar (D9), gestor como **10ª pestaña de `/configuracion`** (**D8 — OQ-2 resuelta por el PO EN CONTRA de la recomendación del design, no reabrir**), importador que crea categorías con tope 50 (OQ-1) y template desde el catálogo (D10), y **recategorización en lote** desde `/productos` (D14: un solo `UPDATE` filtrado por `account_id`, padre→variantes, tope 500 troceado). Migración `20261023000001` + 5 gates SQL cableados. **Desvío aditivo**: `v_products_with_stock` gana `category_id` al final (sin eso `ProductOut` nunca la vería). Local: backend 1874/1874 (cov 97%), frontend 1962/1962. **Verificación post-merge en prod** ✅ (2026-09-04, vía Supabase MCP): 36/38 cuentas con 7 categorías y 2 con más (uso real de "crear categoría"), 0 productos sin `category_id` sobre 5.094, 0 espejos desincronizados, índice de SKU sólo alcanzado por `account_id`, ACLs sin `EXECUTE` para `anon`. **Re-medición del reparto por categoría** contra el baseline 2.951/5.084 en "Otros": bajó a 2.942 y aparecieron 4 categorías nuevas creadas por el usuario (Biker, Remera, Short, Gaseosa) — la herramienta se está usando. Specs sincronizadas (`product-category` nueva, `product-sku` nueva, `soft-delete-policy` modificada). Detalle y candidatos en `CHANGES.md`.

25. ~~**`estadisticas-ventas`**~~ ✅ **COMPLETADA Y ARCHIVADA 2026-09-04 (E1+E2+E3)** (propose PR **#503** `0da5239` · apply E1 PR **#505** `bb14bd3`, migración `20261024000001` · apply E2 PR **#506** `ebb8d37`, migración `20261025000001` · apply E3 PR **#507** `71fbcd4`, migración `20261026000001` · fix post-humo PR **#508** `5ede1bc` · archive el mismo PR que `productos-categorias-sku`; archivada en `openspec/changes/archive/2026-09-04-estadisticas-ventas/`, governance LOW-MEDIUM). Pedido del PO: *"módulo de estadísticas de ventas y ranking de productos más vendidos"*. **E1 cierra el pedido literal**: `/estadisticas` (Inteligencia, sin gate de plan) con evolución por día/semana ISO/mes + comparación contra el período anterior y ranking por unidades/importe/margen con variantes agrupadas. Un helper canónico (`reporting_sales_lines_in_window`) es la única definición de "línea de venta del período" y lo consumen `rpc_sales_evolution`, `rpc_product_ranking` **y** `rpc_product_profitability` (reescrita desde el cuerpo vivo, firma intacta, **`last_sale_date` sin corrimiento** — 218/218 corridos en prod, OQ-3); el clamp de historial por plan vive en `reporting_plan_window` (dentro de la RPC, contra `get_effective_plan`, nunca en Python — `auth["plan"]` cae a `pro` sin claim); la evolución resta NC por el mismo helper del Tablero y el gate lo asserta contra `rpc_dashboard_kpi_summary`. **Regla que formaliza**: `sales.date` es fecha de negocio → `::date` pelado, nunca `AT TIME ZONE` (corre un día atrás); `created_at` es instante → sí. Las 5 OQs firmadas el 2026-09-03. **E2 (2026-09-04) suma las dimensiones sobre el mismo helper**: `rpc_sales_breakdown` (una sola RPC para canal / sucursal / día de la semana / **horario de carga** / categoría, con tramo explícito "Sin canal" / "Sin sucursal" / "Sin categoría" que nunca se omite, 7 días y 24 horas siempre completos, la hora desde `created_at AT TIME ZONE` Mendoza — el único `AT TIME ZONE` admitido — y la categoría por `products.category_id` → `product_categories.name`) y `rpc_sales_top_clients` (OQ-2: las ventas sin cliente no compiten y su importe viaja en una fila `unassigned` propia); `GET /reports/statistics/breakdown` + `/clients`; en `/estadisticas` las tarjetas "Por dónde se vende", "Cuándo se vende" (salvedad *"no es el horario de venta"* antes del gráfico, franjas armadas en el cliente sin re-consulta), "Ventas por categoría" y "Top clientes", con el `BranchFilter` compartido viajando a las cuatro consultas. Hallazgo de la pasada visual corregido en el mismo PR: los rótulos de los gráficos verticales se solapaban a 375 px (rótulos cortos en el eje + `preserveStartEnd`, tabla y tooltip conservan el completo). Detalle completo y hallazgos (E1: fixture invertida de `test_qa_integral_fixes.sql`, `SET search_path` que impide inlinear el helper, margen `NULL` inalcanzable por `products.cost NOT NULL`; E2: `category` como 5ª dimensión del breakdown, seeds sintéticos con atributos de cabecera por operación, GoTrue local ES256) en `CHANGES.md`. **E3 (2026-09-04) cierra el change**: export del ranking a CSV (`product_ranking_csv`, 6º `ExportType` con la lista de tipos en UNA sola fuente — `_shared/export-ranking.ts` — y las filas del **mismo** read-model con los **mismos** parámetros que la pantalla; **hallazgo del run real: `export_logs` tenía un CHECK con los 5 tipos legacy que dejaba el historial sin el ranking, ampliado en `20261026000001`**), detalle por producto en `/estadisticas/productos/[id]` (`rpc_product_sales_evolution`, filas `total`/`bucket`/`member`, misma regla de grupo que el ranking y gate de identidad con su fila, `P0404` → 404 RFC 7807, enlace al catálogo por `/productos?q=`) y `ai-estadisticas` (núcleo puro inyectable en `_shared/ai-estadisticas-core.ts`, cuota antes de leer nada, contexto desde las RPCs canónicas sin re-agregar, contador incrementado **sólo** si el insight se persistió; panel en `/estadisticas` con `useAiUsage`). Verificación E3: backend 1943/1943 (cov. 91,97 %), frontend 2101/2101, gate E3 14 bloques, run real de las dos Edge Functions en el stack local (CSV fila a fila idéntico a la RPC; IA hasta OpenAI con clave dummy) y pasada visual en 4 combinaciones. **Fix post-humo (PR #508)**: el humo real del PO encontró el CSV del ranking ilegible en Excel es-AR (separador `,` y punto decimal) — corregido a `;` + BOM + coma decimal sólo en las columnas numéricas del ranking, sin tocar los otros 5 exports. **Verificación post-merge en prod** ✅ (2026-09-04): `MAX(version) = 20261026000001` sobre 275 migraciones, ACLs de las 5 RPCs del módulo sin `EXECUTE` para `anon`, `CHECK export_logs_type_values` con `product_ranking_csv`, Edge Functions activas. **Demo/humo real del PO (2026-09-04)**: recorrió los 11 puntos del módulo conjunto con `productos-categorias-sku` y confirmó "funciona todo bien" (incl. IA con clave real de OpenAI y el fix del CSV). Specs sincronizadas (`sales-statistics` nueva, `product-ranking` nueva — con el requirement "ranking por categoría" reconciliado a "5ª dimensión de `rpc_sales_breakdown`" antes de archivar —, `data-export`/`plan-gating`/`product-profitability`/`reporting-invariants` modificadas). Detalle completo en `CHANGES.md`.

> **Pendientes externos del PO (no bloquean código)**: trámite ARCA homologación (C-27 5.2 / v22 9.1) y config de verificación de email en Supabase. **`v3-rbac-multirole` es CRÍTICO** — análisis solamente hasta sign-off explícito del PO (consume matriz de transiciones de `v3-document-status-history`).

### Candidatos para el próximo `/opsx:propose`

**Quedan dos changes activos, ambos gateados a tareas `[MANUAL PO]` y no a código pendiente**: `v31-authz-token-hook` (44/48 tasks — falta activar el hook de auth en el Dashboard de Supabase, tasks 8.3-8.6) y `v31-mp-upgrade-webhook-fix` (35/38 tasks — falta decidir OQ1 la ventana de convivencia y retirar el webhook legacy, tasks 6.1-6.3). Todo lo demás propuesto está archivado: `qa-integral-modulos` (ítem 17), `seguros-perfil-asesor`, `caja-compras-cobranzas` (ítem 19), `cobranzas-reverso` (ítem 20), `cobranzas-catalogo-pagos` (ítem 21), **`cobranzas-panel`** (Etapa A, ítem 22) y **`cobranzas-vencimientos`** (Etapa B, ítem 23 — con este par **la saga de cobranzas queda completa**), `mp-real-subscriptions` (suscripciones recurrentes de MercadoPago, ✅ archivada 2026-09-02, ver `CHANGES.md`), y **`productos-categorias-sku`** (ítem 24) + **`estadisticas-ventas`** (ítem 25, E1+E2+E3 — ✅ archivadas juntas 2026-09-04) ya cerrados. El gating de fondo sigue siendo `v3-rbac-multirole` (ítem 4, CRÍTICO, bloqueado a sign-off del PO; su dependencia `v31-tenancy-pool-rls` ya está **satisfecha**). Lo que quedó documentado y sin resolver:

Heredado de `caja-compras-cobranzas` (ítem 19), `cobranzas-reverso` (ítem 20) y `cobranzas-catalogo-pagos` (ítem 21, ver `CHANGES.md` para el detalle completo):

- **OQ-5 de `cobranzas-catalogo-pagos`** — configurar destinos bancarios por defecto en las 266 formas de pago (siguen 0/266); D4 lo dejó fuera de alcance explícitamente.
- **Fix del falso positivo pre-existente en `test_sales_order_payment_method_drop.sql`** (hallazgo de `cobranzas-reverso`) — su regex de exclusión no contempla `payments_received.payment_method`/`payments_made.payment_method`, columnas reales que `caja-compras-cobranzas` agregó después de que ese gate se escribiera.
- **El paso "Verify G1/G4 migrations are idempotent on reapply" de `KPI_Validation.yml` está roto en `main`** (hallazgo de `cobranzas-catalogo-pagos`, confirmado corriendo en rojo incluso en PRs de archive puro) — ninguna de las últimas tres migraciones (`seguros-perfil-asesor`, `caja-compras-cobranzas`, `cobranzas-reverso`) se sumó nunca a esa cadena manual de reaplicación; candidato para revisar si el paso sigue ganándose el lugar en el workflow o si hay que repararlo/retirarlo.
- **Headers `### MODIFIED Requirement:` malformados y enumeración desactualizada en `transactional-outbox/spec.md`** (hallazgo lateral 15.1 de `cobranzas-reverso`) — el prefijo del delta quedó horneado en el nombre de dos requirements durante un archive anterior; `cobranzas-reverso` neutralizó el síntoma (declaró el conjunto canónico de 11 tipos en un requirement nuevo con gate real) pero no corrigió los headers viejos.

Heredado de `qa-integral-modulos` (ítem 17, ver `CHANGES.md` para el detalle completo):

- **`tablet-filtros-cta`** — a 1024px con el riel expandido, el CTA primario de `/ventas`, `/gastos`, `/compras` y `/clientes` queda fuera del viewport inicial porque la barra de **filtros** no wrappea (la task 2.4 del change sólo wrappeó las de **acciones**). Alcanzable con el scroll propio del contenedor (con barra visible) y el documento ya no se estira, así que no es una regresión — pero la cláusula "CTA visible" del delta `responsive-shell` se acotó a móvil en vez de prometer lo que la pasada de tablet (Non-Goal declarado) no llegó a medir.
- **Limpieza de `hooks/use-toast`** — sigue con consumidores reales (`app/(dashboard)/exportaciones/page.tsx`, `components/ui/toaster.tsx` nunca montado, el re-export de `hooks/index.ts`), así que G7 no lo retiró (fuera de su alcance declarado). Candidato: migrar `exportaciones/page.tsx` a `sonner` y retirar `use-toast` + `toaster.tsx` en un change chico — sus toasts de "regenerar" son hoy igual de invisibles que los de `ExportButton` antes del fix (mismo defecto H7).
- **Selector de pago a proveedor con 4 métodos fijos** — anotado al pasar en el informe original (§4, sin reportar por no probado), declarado fuera de alcance de este change; candidato aparte si el PO lo prioriza.

Heredado de `seguros-perfil-asesor` (ítem 18, ver `CHANGES.md` para el detalle completo):

- **Chart "Evolución temporal" roto en `/admin/feria-ia` y `/admin/copilot-ia`** — mismo bug gemelo que tenía `/admin/seguros` (ya arreglado en PR #482): `useState<any>` esconde que `processTimeSeries` entrega `{name, value}` cuando `TimeSeriesLinesChart` espera `{period, activations, umv_achieved?}`. Confirmado idéntico por grep en `frontend/lib/services/fairAiToolsService.ts` y `frontend/lib/services/copilotPromptsService.ts`. No bloqueante (paneles de solo lectura de KPIs de admin), queda con chip (`task_17032e09`).
- **`license_authority` del broker (`argbroker.com.ar`)** — OQ-3 quedó parcialmente sin resolver: la matrícula personal de Julián (98506) sí se sembró, pero si el broker exige identificación propia además de la del PAS individual sigue sin decidirse. Completable desde `/admin/seguros` sin migración cuando el PO lo confirme.

Heredado de `cuenta-corriente-party-guard` (ver `openspec/changes/archive/2026-08-23-cuenta-corriente-party-guard/design.md` §"Hallazgos laterales de la revisión de seguridad" y `CHANGES.md` §"Candidatos dados de alta por `cuenta-corriente-party-guard`"):

- **`operacion-party-guard`** (OQ-4 de `cuenta-corriente-party-guard`) — la venta/compra **al contado** con `client_id`/`supplier_id` ajeno sigue sin guard (no crea saldo ni asiento contra un tercero, pero deja una fila mala en `sales`/`purchases`). Guard natural en `rpc_create_sale_operation(_v2)`, `_c29_confirm_order_core` y `rpc_create_purchase_operation`.
- **Endurecimiento de `c30_register_customer_account_movement` / `c30_register_supplier_account_movement`** (h3) — `SECURITY INVOKER` con `EXECUTE` para `anon` en prod; hoy lo frena sólo la ausencia de policies de escritura en esas tablas (defensa de segundo orden, no un incidente).
- **`get_account_ids_for_user(uuid)`** (h4) — devuelve la membresía de cualquier `user_id` sin comparar contra `auth.uid()` (fuga menor: hace falta conocer el `user_id` ajeno).

Heredado de `tenancy-guard-caja-outbox` (ítem 11, ver `openspec/changes/archive/2026-08-24-tenancy-guard-caja-outbox/design.md` §"Open Questions", OQ-4):

- **Revocar `c28_register_cash_movement` de `authenticated`** — es `SECURITY INVOKER` con `GRANT` explícito desde antes de este change; con la capa 2 de `tenancy-guard-caja-outbox` puesta ya no permite escribir en caja ajena, así que dejó de ser una primitiva cross-tenant, pero revocarla del todo exige auditar si algún camino la llama directo desde PostgREST — "revocar algo del hot path de caja sin esa auditoría es cómo se rompe el POS un sábado". Queda anotada como candidata, no urgente.

Heredado de `sucursal-guard-vaciado-auditoria` (ítem 12, ver `CHANGES.md` para el detalle completo):

- **OQ-5** — `/stock` sigue mostrando el agregado del catálogo, no el desglose por sucursal; es la mitad de por qué el incidente del 22-08 fue invisible. G3 sólo agrega el desglose bajo demanda (al transferir), no en la columna principal del listado.
- **OQ-6** — `c26_default_branch` (resolución de la sucursal por defecto) debería avisar cuando el criterio cambia de sucursal; un aviso más caritativo habría amortiguado el incidente aunque el guard no existiera.
- **El "mostrador"/POS sin wirear** (`/ventas/pos`) — usa su propio `friendlyError`, no conectado a `operation-errors.ts`; el camino directo a transferir stock de G3 no llega ahí.
- **OQ-7 (lateral, misma familia que h3 arriba)** — `anon` tiene permisos de escritura y borrado a nivel tabla sobre `branches`; hoy sólo lo frena que las RLS exigen membresía de cuenta.

Heredado de `v31-tenancy-role-assertion` (ítem 14, ver `CHANGES.md` §"Candidatos dados de alta por `v31-tenancy-role-assertion`"). Los tres son de **andamiaje de verificación**, no de comportamiento —el guard de #470 hace lo que dice—, son baratos y podrían ir juntos en un change chico:

- **Test del rastro operativo** — ningún test fija que el rechazo se registre con nivel `CRITICAL`, ni que incluya el rol efectivo observado y el `sub` del request, que es justo lo que el requirement nuevo declara normativo. Hoy se puede degradar el `logger.critical` de `backend/core/database.py` (L143) a `warning`, o vaciarle el payload, y los 20 casos de `backend/tests/test_database.py` siguen verdes. No es cosmético: esa línea **es la señal sobre la que se apoya toda la verificación de producción** del change. Se cierra con un caso `caplog`.
- **Aserción faltante en `test_get_service_conn_step2_never_adopts_role_even_with_both_flags_on`** (`backend/tests/test_database.py:541`) — no assertea `conn_mock.fetchval.assert_not_awaited()`, así que "el contexto de servicio queda fuera del alcance de la comprobación" hoy es verdadero por construcción y no por aserción. El test hermano (L773) sí la tiene: el patrón ya existe en el mismo archivo. **Una línea lo cierra.**
- **Spec `python-backend` envejecida** — el escenario *"Solo el router de payments usa `get_service_conn`"* y la cláusula *"un endpoint que atiende a un usuario autenticado NOT SHALL usar el contexto de servicio"* (`openspec/specs/python-backend/spec.md` L86-89 y L126) afirman algo que prod ya no cumple: son **tres** routers (`fiscal.py:330`, `outbox.py:48`, `payments.py`) y `POST /outbox/process-pending` viola la cláusula deliberadamente (gateado con `require_platform_admin`). No son bugs de aislamiento —las dos son exenciones de diseño— pero **la spec afirma algo falso y ningún gate lo detecta**.

Heredado de `gastos-forma-pago` (ítem 16, ver `openspec/changes/archive/2026-08-30-gastos-forma-pago/design.md`/`tasks.md`):

- **Importador CSV de gastos sin forma de pago** (D13) — alcance declarado y deliberado, no deuda: el importador emite una llamada por fila sin transacción que abarque el lote, así que imputar forma de pago ahí arriesgaría dejar parte de los gastos con movimiento y parte sin él ante un fallo a mitad de proceso.
- **Asiento contable de gastos** (D10) — diferido a V2.6 junto con el resto del plan de cuentas; `_journal_post_from_event` no tiene rama de gasto y `public.events` no tiene ningún `event_type` de gasto. Este change deja lista la forma de pago, que es el dato que le falta al asiento futuro para elegir la contrapartida (`1100 Caja`/`1110 Banco`).
- **`kind='credit'` en gastos** (D3) — rechazado por diseño, no una limitación técnica a levantar: `expenses` no referencia cliente ni proveedor y no hay cuenta corriente que cargar.
- ~~**Desborde horizontal de `/gastos` por debajo de ~1372px**~~ → **absorbido por `qa-integral-modulos`** (ítem 17, grupo G2): la causa raíz es el `<main>` de `SidebarInset` sin `min-w-0` — el QA del 30-08 midió el mismo desborde en 12 pantallas, `/gastos` incluida en tablet 768–1024.

Heredado de `cobranzas-panel` (ítem 22) y `cobranzas-vencimientos` (ítem 23, ver los `design.md` de ambas en `openspec/changes/archive/2026-09-03-cobranzas-panel/` y `openspec/changes/archive/2026-09-03-cobranzas-vencimientos/`):

- **Refactor de `CustomerAccountBalance` a tokens semánticos** (D14 de `cobranzas-panel`) — sigue con `text-yellow-400`/`text-emerald-400` literales en su propia pantalla; declarado fuera de alcance en ambas etapas por no ser la superficie que tocaban.
- **CTE de aging duplicada entre `list_movements()` y `list_movements_page()`** (D7 de `cobranzas-vencimientos`, deuda declarada) — el bloque de cuatro `EXISTS` que deriva `due_date`/`is_overdue`/`days_overdue`/`open_amount` está copiado literal en las dos consultas (y sus espejos de proveedor) porque `cobranzas-reverso` ya mostró que una pantalla real puede usar una consulta distinta a la del endpoint paginado. El change lo **agranda** en vez de arreglarlo; extraerlo a una CTE compartida es un refactor del hot path del historial, ajeno al alcance firmado.
- **Sin forma de corregir un vencimiento mal cargado** (OQ-1 de `cobranzas-vencimientos`, sign-off del PO: dejarlo así) — la operación con cargo posteado es inmutable (`P0423`), así que hoy la única salida es borrar la venta y rehacerla. Una acción "cambiar vencimiento" sobre un cargo abierto es una escritura sobre el ledger que merece su propia RPC, guard y entrada de auditoría — candidato propio, no cubierto por este sign-off.

Heredado de `productos-categorias-sku` (ítem 24, ver `CHANGES.md`):

- **Retiro de `products.category` TEXT** (OQ-3, sign-off PO: no en este change) — change propio que debe migrar `v_products_with_stock` y todos sus lectores (IA, exportaciones, `rpc_product_profitability`) de una sola vez.
- **`idx_products_barcode_unique` sigue alcanzado por `user_id`** (task 4.5) — mismo residuo de tenencia que tenía el SKU; inerte hoy (0 cuentas multiusuario con productos), fuera de superficie de este change.
- **Migración del importador a FastAPI** (D7) — sigue por `rpc_bulk_upsert_products` con supabase-js; migrarlo entero es un change propio.
- **Categoría por defecto configurable** — la fila del CSV sin categoría va hoy a una heurística documentada en la RPC ("Otros" si vive y activa, si no la última activa por `sort_order`).

Heredado de `estadisticas-ventas` E1+E2+E3 (ítem 25, ver `CHANGES.md` y `openspec/changes/archive/2026-09-04-estadisticas-ventas/design.md` §"Notas de implementación de E1", §"… de E2" y §"… de E3"). **Ya NO son candidatos** (resueltos al archivar el 2026-09-04): la reconciliación de la spec `product-ranking` ("ranking por categoría" → 5ª dimensión de `rpc_sales_breakdown`, ahora en `sales-statistics`), el humo real de `ai-estadisticas` con clave de prod (PASÓ) y el CSV ilegible en Excel (fix PR #508). Lo que sigue abierto:

- **Alinear `ai-rentabilidad` con `ai-estadisticas`** — la hermana incrementa el contador de uso aunque el insight venga vacío; `ai-estadisticas` sólo lo incrementa si el insight se generó **y** se persistió. Desvío declarado en E3, candidato barato.
- **Hoja de ranking en el reporte completo XLSX** — `full_report_xlsx` no la incluye (sería la 7ª hoja, con los parámetros de período / orden / agrupación que ese export hoy no tiene cómo elegir).
- **Filtro de canal sin superficie** — `p_canal` existe en las RPCs y en la API de evolución / ranking / breakdown (fail-closed, sin restar NC), pero ninguna pantalla lo expone; candidato barato si el PO lo pide.
- **Migrar las dos copias JS de "top productos"** (`supabase/functions/ai-insights/index.ts`, `frontend/lib/ai/buildBusinessSnapshot.ts`) a `rpc_product_ranking` — Non-Goal declarado del change; hoy son la 2ª y 3ª definición de la misma agregación.
- **Migrar los 3 reportes existentes** (`/reportes/formas-pago`, `/reportes/centros-costo`, `/reportes/sucursal`) a `components/charts/` — D13 los dejó fuera para no ensanchar el diff.
- **`products.cost NOT NULL DEFAULT 0`** hace que "sin costo" y "costo cero" sean indistinguibles para la cascada canónica RN-D2: el margen `NULL` que la spec `product-ranking` prevé es hoy inalcanzable, y un producto cargado sin costo aparenta margen del 100 %. `cost_coverage_pct` (líneas con snapshot) es la marca honesta; distinguir el 0 del NULL en el catálogo es un change de modelo, no de reporting.

Ninguno de los candidatos heredados es urgente por sí solo; quedan para que el PO decida si ameritan un change propio (posiblemente combinados, como se hizo con h1+h2 en `tenancy-guard-caja-outbox`).

### Camino crítico (Fases 6-7)
```
C-19 tenancy-cleanup ─┬→ C-20 sale-items ────────→ C-29 quote-salesorder → C-30 ctas-corrientes
                      ├→ C-21 inventory-unif ────→ C-26 branch-as-root ─┬→ C-27 fiscal-profile (AFIP, CRÍTICO)
                      ├→ C-24 insights-unif                             └→ C-28 cash-session
                      └→ C-25 outbox-activation
C-22 fiscal-identity-clients · C-23 community-schema-split — paralelos e independientes
```

### Fases
| Fase | Changes | Estado | Descripción |
|---|---|---|---|
| 1 — Billing | C-01, C-02, C-03, C-09 | ✅ | Schema + gating engine + grace period + community bug fixes |
| 2 — IA | C-04, C-11, C-12, C-13 | ✅ | Contadores IA split + rentabilidad + reportes comparativos + sugerencia precios |
| 3 — Multi-tenant | C-05, C-06, C-07, C-08 | ✅ | Arquitectura multi-usuario + roles + sucursales + stock multisucursal |
| 4 — Upgrade UX | C-10, C-14 | ✅ | UI de upgrade de plan + módulo de exportaciones |
| 5 — Backend Python | C-15, C-16, C-17, C-18 | ✅ | Capa de datos + migración API + pagos + desacople DataContext (realtime queda en Supabase, DEC-16) |
| 6 — V2.0 Retirada de deuda | C-19 → C-25 | ✅ 7/7 | Tenancy única, sale_items (Group 10 DROP diferido — lo desbloquea `v3-snapshot-pattern`), ledger único de stock en branch_stock, FiscalIdentity en clientes, schema community, insights unificados, outbox activo (C-25 ✅ 2026-06-18; revivido en prod 2026-07-01, #248) |
| 7 — V2.1 Operación | C-26 → C-30 | ✅ 5/5 | Branch como root, FiscalProfile + AFIP CAE async (E2E homologación pendiente PO), CashSession con arqueo, Quote/SalesOrder + quickSale POS (C-29 ✅ 2026-06-17), cuentas corrientes cliente/proveedor (C-30 ✅ 2026-06-20) |
| V2.5 — Finanzas | post-roadmap | 🔨 en marcha | cost-center ✅ + journal-entry-outbox ✅ (partida doble async vía outbox) + **BankReconciliation COMPLETA (C1+C2+C3 ✅, 2026-07-02)** · falta: percepciones. AFIP delegación (v22 ✅) + facturar venta manual ✅ |
| Modelo V3 (retrofit) | `v3-*` | 🔨 en marcha | Snapshots ✅ (2026-07-02), FSM+DocumentStatusHistory ✅ (2026-07-03), notificaciones realtime ⭐, soft delete, RBAC multi-rol (CRÍTICO), provisioning seed, RN-D reporting, estándares API, maestros menores — ver `CHANGES.md` §"Roadmap Modelo V3" (producto-imagenes descartado por PO 2026-07-04) |
| V3 — Inteligencia | ⏳ | ⏳ | AIAgent, KnowledgeBase, automatizaciones, predicción + BOM ligera (`v3-product-composition`) |

---

## Reglas Duras (específicas del proyecto)

> Reglas globales ya definidas en `~/.claude/CLAUDE.md` (orquestador OPSX, governance CRITICAL/HIGH/MEDIUM/LOW, TDD, engram, model assignments): el proyecto las hereda. Acá viven solo las reglas **específicas de este proyecto**.

### Changes / Producto — superficie frontend obligatoria (regla PO 2026-08-02)
- **Todo change que produzca algo que un usuario o el admin deba ver u operar DEBE planificar su superficie frontend en el propose**: qué pantalla/sección lo expone, cómo se llega (ruta + entrada de menú/sidebar) y sus tasks correspondientes en tasks.md. Backend sin puerta de entrada = change incompleto. (Origen: `CostCenterManager` construido y jamás montado en ninguna página; cola de suscripciones ambiguas invisible hasta que el PO preguntó dónde estaba.)
- **La estética se planifica, no se improvisa**: la parte visual usa el design system del proyecto (tokens semánticos, componentes base vía cva) y se verifica en **desktop Y mobile** (responsive) y en **tema claro y oscuro** antes del merge.
- Excepción: changes puramente internos (infra, CI, migraciones sin efecto visible para nadie) — declarar explícitamente "sin superficie frontend" en el proposal, para que la omisión sea una decisión y no un olvido.

### Reutilización antes que repetición (regla PO 2026-08-02)
- **ANTES de escribir una función, componente, hook, query o RPC nueva: buscar si ya existe una que lo haga (o casi)** — grep por dominio/nombre en `lib/`, `hooks/`, `components/`, `backend/services/`, `backend/repositories/` y las RPCs de `supabase/migrations/`. Si existe: reutilizarla o extenderla; NUNCA duplicarla. La lógica repetida diverge y produce bugs silenciosos (casos reales: 3 Edge Functions calculando su propio "plan efectivo" contra una tabla muerta en vez de `get_effective_plan`; criticidad de stock rehecha en 5 lugares hasta canonizarla en `lib/product-stock.ts`).
- **Lo nuevo reusable nace en la capa canónica** (`lib/`, `hooks/`, componentes base, service/repository correspondiente), no embebido en una pantalla o endpoint puntual.
- Equilibrio: esto NO pide abstraer prematuramente (sigue vigente la Regla de Tres para crear abstracciones nuevas) — pide no REESCRIBIR lo que ya existe.

### TypeScript / React
- **NUNCA usar `any`** → usar tipos explícitos o `unknown`. Si un tipo es complejo, definirlo en `lib/types.ts`.
- **PascalCase en componentes React** → `ProductCard.tsx`, `SalesTable.tsx`. Archivos de componentes también en PascalCase.

### Supabase / Auth / Seguridad
- **SIEMPRE `supabase.auth.getUser()` en server-side** → NUNCA confiar en `getSession()` solo para decisiones de auth. `getSession()` no verifica el JWT.
- **NUNCA exponer `SUPABASE_SERVICE_ROLE_KEY` al cliente** → Solo en Edge Functions (servidor). La service_role bypasea toda RLS.
- **NUNCA usar el MCP `apply_migration` para aplicar migrations de producción** → Registra un timestamp diferente al del archivo local y desincroniza el historial. Siempre usar `npx supabase db push` via CLI. Si se usó el MCP accidentalmente, reparar con `npx supabase migration repair --status reverted <timestamp_mcp>` y luego `npx supabase db push`.
- **Dos proyectos Supabase en este proyecto**: `gxdhpxvdjjkmxhdkkwyb` = proyecto real con usuarios (CLI + MCP). `pudaxiwqhwsxuaofsqda` = proyecto del preview de Vercel (vacío, schema más avanzado). Las migrations se aplican siempre al primero vía CLI.

### TypeScript / Imports
- **NUNCA usar `as import("@/ruta").Tipo` en type assertions** → Sintaxis de inline dynamic import inusual que puede tener edge cases con SWC/Turbopack. Importar el tipo explícitamente en la cabecera del archivo y usar `as Tipo` directamente.

### Git / Commits
- **Conventional commits** → `feat(scope): mensaje`, `fix(scope): mensaje`, `chore(scope): mensaje`, `docs(scope): mensaje`. Scope = módulo afectado (ventas, auth, stock, ai, billing, etc.).
- **Co-autoría en commits del agente**:
  ```
  Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
  ```

### Backend Python / FastAPI (Fase 5 — planificado)
- **NUNCA usar `service_role` en el backend** → usar **JWT-passthrough**: inyectar los claims del JWT del usuario en la conexión `asyncpg` para que la RLS org-based siga activa como red de seguridad. Excepción única: jobs administrativos aislados.
- **NUNCA poner lógica de negocio en los routers** → arquitectura de 3 capas obligatoria: `routers` (validación + DI) → `services` (lógica + guards `require_role`/`require_plan`) → `repositories` (acceso a datos / RPCs).
- **SIEMPRE validar con Pydantic v2** en el endpoint antes de tocar la DB. Nada de payloads sin schema.
- **Webhook de pagos = governance CRÍTICO** → migrarlo corriendo en paralelo al webhook actual y comparando resultados antes de cortar. Requiere aprobación humana explícita antes de tocar (dinero real).
- **TDD con `pytest` + `pytest-asyncio`** → tests por cada router/service; coverage mínimo verificado en CI (`pytest-coverage`).
- **NUNCA migrar IA/OCR a Python sin presupuesto** (DEC-15) → los servicios de IA y OCR se quedan en Supabase Edge Functions por ahora; los workers Python (ARQ) están pospuestos.

---

## Skill Routing (cuándo invocar cada skill vía `/skill-name`)

Cuando el pedido del usuario coincide con una skill disponible, invocarla via el Skill tool.

| Pedido del usuario | Skill |
|---|---|
| Ideas de producto / brainstorming | `/office-hours` |
| Estrategia / alcance | `/plan-ceo-review` |
| Arquitectura | `/plan-eng-review` |
| Review de diseño / sistema de diseño | `/design-consultation` o `/plan-design-review` |
| Review completo (pipeline) | `/autoplan` |
| Bugs / errores | `/investigate` |
| QA / testear comportamiento del sitio | `/qa` o `/qa-only` |
| Code review / diff check | `/review` |
| Pulido visual | `/design-review` |
| Ship / deploy / PR | `/ship` o `/land-and-deploy` |
| Guardar progreso de contexto | `/context-save` |
| Restaurar contexto | `/context-restore` |
| Escribir spec / issue de backlog | `/spec` |

---

## Flujo de Trabajo

```
1. Leer CHANGES.md → identificar el change por código C-NN
2. Leer los archivos de KB relevantes para ese change
3. /opsx:propose <nombre-del-change>   → crea proposal + design + tasks
4. /opsx:apply <nombre-del-change>     → implementa las tasks
5. /opsx:archive <nombre-del-change>   → sincroniza specs + cierra el change
6. Marcar [x] en CHANGES.md
```

> Para explorar antes de proponer: `/opsx:explore <tema>`  
> Para skill routing adicional: consultá el CLAUDE.md raíz del proyecto.
