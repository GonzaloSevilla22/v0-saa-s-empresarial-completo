# v31-tenancy-role-assertion — Tasks

> **Retro-spec: este change NO escribe código.** El comportamiento que la spec pasa a exigir ya está en `main` desde el 2026-08-27 (`d7b5214`, PR #470 "fix(tenancy): verifica que SET LOCAL ROLE tomo antes de yieldar la conexion"), con sus cuatro tests, y ya corrió en producción. Las tareas de código y de test se listan **tildadas y con su evidencia**, no porque haya que hacerlas: porque hay que constatar que existen antes de escribir un requirement sobre ellas. Lo pendiente de verdad es documental: `design.md`, PR, merge y archive.
> **Modo TDD: no aplica.** No hay ciclo RED→GREEN porque no hay implementación. El equivalente de este change es el grupo 1: nada se especifica antes de haber leído el código vivo y ejecutado sus pruebas.
> **Governance CRÍTICO por dominio** (aislamiento entre 34 cuentas con dinero real), **documental por alcance**: no toca producción, no toca esquema, no toca ACLs. La revisión que corresponde es de redacción normativa, no de implementación.
> **Regla de oro del alcance**: si aparece una divergencia entre lo que la spec quiere exigir y lo que `backend/core/database.py` hace, se corrige la **redacción de la spec** hacia el comportamiento real. Corregir el código sería otro change.
> Todo commit vía PR desde rama propia — incluidos los fixes triviales post-merge. Nunca a `main` directo.

## 1. Evidencia previa — constatar el comportamiento antes de especificarlo

- [x] 1.1 Confirmar que el código está en `main` y no en una rama viva. — `git log`: `d7b5214` es el HEAD de `main`, mergeado 2026-08-27 11:26:08 -0300, cuatro minutos y cuarenta y cinco segundos después de `0664619` (#469, el archive de `v31-tenancy-pool-rls`).
- [x] 1.2 Leer el bloque vivo en `backend/core/database.py` (dentro de `get_db_conn`, comentario "D6 bis") y fijar el contrato **real** que la spec va a describir. — Lee `SELECT current_user` sobre la misma conexión, compara con `!= "authenticated"` (igualdad estricta, no descarte), emite `logger.critical` con el rol observado y el `sub` del request, y lanza `HTTPException(503, "Database session isolation could not be verified")` **antes** del `yield`. El bloque está anidado bajo la condición de la palanca del Paso 2: con la adopción apagada no se emite nada.
- [x] 1.3 Confirmar que los cuatro tests del PR existen en `backend/tests/test_database.py` y cubren los cuatro caminos. — `test_get_db_conn_step2_verifies_role_took_effect_before_yield` (línea 642), `..._fails_closed_when_role_did_not_take` (678), `..._fails_closed_when_role_check_returns_none` (714), `test_get_db_conn_role_check_absent_when_step2_off` (751).
- [x] 1.4 Ejecutar el archivo de tests completo para que la evidencia sea de esta sesión y no una cita del PR. — `pytest backend/tests/test_database.py -q`, 2026-08-28: **20 passed en 1.53 s**, cero fallos, cero skips. El número no cuadra con un conteo ingenuo de funciones a propósito: son **20 casos sobre 19 funciones de test**, porque `test_get_db_conn_role_check_absent_when_step2_off` está parametrizado sobre dos combinaciones (legacy y Paso-1-solo).
- [x] 1.5 Releer los cuatro requirements vigentes de `openspec/specs/asyncpg-pool/spec.md` (líneas 9, 59, 84 y 99) y confirmar que ninguno ya dice lo que este change va a decir. — El de línea 59 fija la **adopción** del rol y su alcance transaccional; no menciona comprobación, ni criterio de comparación, ni modo de falla. No hay duplicación: el delta va `ADDED`.
- [x] 1.6 Fijar la evidencia de producción que sostiene el proposal, **con sus límites declarados**. — Medido 2026-08-28 sobre la ventana completa desde el merge: **cero rechazos de la comprobación** (`CRITICAL` = 0 y `503` = 0), con control positivo (`GET /products` = 18) que prueba que la búsqueda server-side encuentra tráfico cuando lo hay. Tres precisiones que el proposal tiene que reflejar y no inflar: (a) `CRITICAL`, `BYPASSRLS` y `503` **no son tres señales independientes** — salen de la misma y única línea del código, el `logger.critical` (cuyo texto menciona BYPASSRLS) acompañado del `HTTPException(503)`; contarlas como tres confirmaciones es contar una vez y decir tres. (b) **No hay evidencia positiva por request** de que la comprobación se haya ejecutado: el camino de éxito loguea en `DEBUG` (`backend/core/database.py:156`) y el nivel efectivo de producción está por encima de `DEBUG` —el backend no configura logging en ninguna parte, así que ese logger hereda el nivel del root—, así que no emite nada; "corrió en ≥127 requests autenticados" es una **inferencia** desde el estado de la palanca y del código en `main`, no una medición, y el control positivo sólo prueba que la búsqueda encuentra cosas. (c) **No se midió latencia**: no afirmar que "cuesta lo que dijimos que iba a costar". Lo que sí queda establecido: el guard nunca rechazó a nadie → sin daño histórico que auditar ni reparación pendiente.

## 2. Artefactos del change

- [x] 2.1 `proposal.md` — secciones en el orden del schema (`## Why` · `## What Changes` + `### Fuera del alcance` · `## Capabilities` · `## Impact`), con "Sin superficie frontend" declarado como **excepción argumentada** de la regla PO 2026-08-02, no como etiqueta.
- [x] 2.2 `specs/asyncpg-pool/spec.md` — un solo bloque `## ADDED Requirements`, un requirement, siete escenarios. `SHALL` en la primera línea del cuerpo; idiom `NOT SHALL`; registro abstracto verificado por grep (cero ocurrencias de `SET LOCAL`, `current_user`, `authenticated`, `503`, `HTTPException`, `logger`, `asyncpg`, `postgres`, `BYPASSRLS`, `get_db_conn` en el delta).
- [x] 2.3 Verificar el mapeo test → escenario, para que ningún escenario sea invención y ningún test quede sin contraparte normativa — **incluidos los escenarios que hoy NO tienen test**. Mapeo real, medido en el archivo:
  - **Escenarios 1, 2, 3** — un test cada uno, de los cuatro de 1.3 (`..._verifies_role_took_effect_before_yield`, `..._fails_closed_when_role_did_not_take`, `..._fails_closed_when_role_check_returns_none`).
  - **Escenario 6** — `test_get_db_conn_role_check_absent_when_step2_off`, **parametrizado sobre dos combinaciones**: legacy (ambas palancas apagadas) y Paso-1-solo (transacción encendida, adopción apagada). El segundo es el que importa: es el que atrapa una comprobación sacada fuera de la palanca interna, que es el modo de regresión más probable de este guard.
  - **Escenario 4** (mensaje opaco) — respaldado **parcialmente**: la aserción `assert "postgres" not in str(exc_info.value.detail).lower()` existe sólo en el test de rol-no-tomó; el test de respuesta `None` no assertea nada sobre el detalle.
  - **Escenario 7** (contexto de conexión de servicio) — respaldado por un test previo del cluster, `test_get_service_conn_step2_never_adopts_role_even_with_both_flags_on` (`backend/tests/test_database.py:541`), que cubre el GIVEN exacto (ambas palancas encendidas) y assertea `transaction.assert_not_called()` + `execute.assert_not_called()`. **Límite**: no assertea `fetchval.assert_not_awaited()`, así que la mitad "no se le comprueba rol efectivo" es verdadera por construcción, no por aserción.
  - **Escenario 5** (rastro operativo) — **SIN TEST**. Verificado en esta sesión: `grep -niE "caplog|logger|critical|logging" backend/tests/test_database.py` devuelve una sola línea, y es un **comentario** (636). Ningún test usa `caplog`, parchea el logger, ni assertea el nivel ni el contenido del registro. Consecuencia concreta: se puede degradar `logger.critical` a `warning`, o sacarle el rol observado y el `sub`, y los 20 casos siguen verdes. El escenario queda respaldado sólo por lectura del código vivo (`backend/core/database.py`, bloque "D6 bis"). Se registra como candidato en 5.5; escribir el test acá violaría el alcance declarado (ver 2.6 y la regla de oro de la cabecera).
- [x] 2.4 Condicionar el requirement a la palanca de adopción **en su primera línea**, la misma donde va el `SHALL`. El motivo **no** es evitar una contradicción con el escenario vigente *"Desplegar el código no cambia el comportamiento"* — ese argumento es falso contra el archivo: el requirement vigente de adopción tampoco se condiciona en su cuerpo ("Dentro de la transacción de cada request autenticado, el sistema SHALL adoptar…") y convive sin contradicción con ese escenario, porque la palanca está factorizada en el requirement de *Activación gradual*. El motivo verdadero es propio: la **ausencia** del intercambio adicional con el Paso 2 apagado es contrato de **este** requirement, lo fija su escenario 6 y lo prueba el test parametrizado de 2.3. Se conserva la condición (alternativa evaluada y descartada: sacarla del cuerpo para igualar el registro de los otros cuatro requirements — la cláusula posterior "SHALL existir únicamente donde existe la adopción que verifica" ya cubre el caso apagado, pero dejar la condicionalidad visible en la primera línea de un requirement de seguridad se prefiere a la consistencia estilística).
- [x] 2.5 `design.md`. Se escribe porque el schema `spec-driven` lo declara como artefacto del change y como **dependencia declarada de `tasks`** (`openspec instructions tasks --json` → `dependencies: specs (done), design (not done)`), no porque hoy esté bloqueando algo: medido en esta sesión, `openspec status` reporta `Progress: 3/4 artifacts complete` con `[x] proposal`, `[ ] design`, `[x] specs`, `[x] tasks`, y `applyRequires: ["tasks"]` — o sea que nada impide avanzar. Contenido mínimo: `## Context` (el trade-off D6 heredado, sus **dos** modos, y cuál caduca), `## Goals / Non-Goals`, `## Decisions` (ADDED vs MODIFIED, scoping a la palanca con el argumento corregido de 2.4, igualdad estricta con su alternativa descartada, delta de un solo archivo), `## Risks / Trade-offs` (especificar un contrato más débil que el código → mitigación; y especificar propiedades que el código no entrega → el caso de la constatabilidad de la ausencia, ya corregido en el delta), `## Migration Plan` (ninguno: el comportamiento ya está en prod), `## Open Questions` (incluir el test faltante del escenario 5). — Escrito 2026-08-28 con las nueve secciones previstas. Correcciones de la revisión aplicadas en el mismo pase, todas de redacción y ninguna de contrato: (a) la co-locación física de adopción y verificación se atribuye ahora a los **dos** requirements que la fijan —*Pool asyncpg con JWT-passthrough* (`openspec/specs/asyncpg-pool/spec.md:12`, la transacción explícita por request) más la cláusula de adopción de `:65`—, en `design.md` y en el paréntesis del delta, que antes mandaban al lector al requirement equivocado; (b) D1 deja de decir "no hay solapamiento" a secas y explicita **dónde sí se roza** con el escenario vigente de `:67-70` y por qué ese roce no obliga a un `MODIFIED` (afirma un estado del sistema, no un criterio de decisión); (c) D4 y OQ 3 separan qué viola cada camino: `outbox.py:48` viola el escenario de payments **y** la cláusula del usuario autenticado, `fiscal.py:330` sólo el primero por ser endpoint de máquina con shared secret (`fiscal.py:334`); (d) "anteayer" → fecha absoluta 2026-08-27 (`0664619`, #469), acá y en `proposal.md`; (e) "escritas hace 49 migraciones" → "las 49 migraciones escritas para ese rol", que es lo que dice la fuente (`archive/2026-08-27-v31-tenancy-pool-rls/design.md:131`); (f) preámbulo de Open Questions: "los tres primeros… y el tercero" → "los dos primeros son cambios de código y el tercero es de otra capability".
- [x] 2.6 Pegar la evidencia de prod de 1.6 —**con sus tres límites declarados**— donde quede recuperable después del archive: como bloque dentro de `design.md` o como archivo propio del change. **No va en la spec**: una spec fija contrato, no cita mediciones con fecha. Y tampoco puede exigir que la **ausencia** de fallos sea constatable sobre tráfico real, porque el código no entrega esa propiedad (el camino de éxito loguea en `DEBUG` y el nivel efectivo de prod está por encima de `DEBUG`): lo único observable es la ausencia de `CRITICAL`, indistinguible de "el guard se retiró", "la palanca está apagada" y "no pasó tráfico autenticado". Esa cláusula se recortó del delta por eso — exigirla sería cometer en la spec la misma falacia que motiva el change (ausencia de señal ≠ ausencia de problema). Pedir señal positiva en un nivel que prod emita sería cambio de código, fuera de alcance. — Pegada como sección propia `## Evidencia de producción (2026-08-28)` de `design.md` (no como archivo suelto: así viaja con el change al archive y queda al lado del razonamiento que sostiene). Incluye la medición (cero rechazos, control positivo `GET /products` = 18, suite de esta sesión 20 passed) y los **tres límites textuales**. Un cuarto ajuste salido de la revisión: la afirmación "producción corre en `INFO`" no tenía fuente en el árbol —`backend/` no configura logging en ningún lado (cero `basicConfig`, `setLevel` o `LOG_LEVEL` fuera de `.venv`, sin `render.yaml` versionado) y el logger de `backend/core/database.py:13` hereda el nivel del root, que uvicorn no toca— así que se reformuló a lo que el argumento necesita y sí es verificable: el camino feliz loguea en `DEBUG` (`backend/core/database.py:156`) y el nivel efectivo de prod está **por encima de `DEBUG`**, con lo cual no emite nada. Corregido también en `proposal.md` y en 1.6 de este archivo, para que los tres artefactos digan lo mismo.

## 3. Validación

- [x] 3.1 `openspec validate v31-tenancy-role-assertion --strict`. — 2026-08-28: `Change 'v31-tenancy-role-assertion' is valid`.
- [x] 3.2 Re-correr `--strict` después de `design.md` y confirmar `4/4 artifacts complete` en `openspec status`. — 2026-08-28, después de escribir `design.md` y de aplicar las correcciones de la revisión: `openspec validate v31-tenancy-role-assertion --strict --json` → `"valid": true`, `"issues": []`, `passed: 1 / failed: 0`; `openspec status --change "v31-tenancy-role-assertion"` → **`Progress: 4/4 artifacts complete`** con `[x] proposal`, `[x] design`, `[x] specs`, `[x] tasks` y `"isComplete": true`. El delta sigue en registro abstracto después de la edición del paréntesis: `grep -cE "SET LOCAL|current_user|authenticated|503|HTTPException|logger|asyncpg|postgres|BYPASSRLS|get_db_conn|get_service_conn"` sobre el delta → **0**.
- [x] 3.3 Verificar que el nombre del requirement nuevo no colisiona con ninguno de los cuatro vigentes de `asyncpg-pool` (el header es la clave de sincronización al archivar, y una colisión funde bloques en silencio). — Comparados los headers uno a uno, no por lectura al voleo (`comm -12` sobre los dos conjuntos ordenados de líneas `^### Requirement:`): **intersección vacía**. Vigentes: *Pool asyncpg con JWT-passthrough* (`:9`), *El rol efectivo del camino de request no bypasea la seguridad a nivel de fila* (`:59`), *Activación gradual y reversible del ciclo de vida de conexión* (`:84`), *Configuración de base de datos vía entorno* (`:99`). Nuevo: *La adopción del rol restringido se verifica antes de exponer la conexión*. Ninguna coincidencia exacta ni por prefijo; el más cercano es el de `:59` y difiere desde la primera palabra. Al archivar, la capability suma un quinto bloque en vez de fundirse con el cuarto.
- [ ] 3.4 Confirmar que el change no toca `CLAUDE.md`: si lo tocara, correr `python scripts/ci/check_docs_sync.py --fix` en el **mismo** PR (gate `Docs Sync`).

## 4. PR y merge

- [ ] 4.1 Rama propia `opsx/v31-tenancy-role-assertion` desde `main` actualizado, y verificar antes de commitear que no haya un PR previo ya mergeado para este change.
- [ ] 4.2 Abrir el PR. En el cuerpo: que es un retro-spec de `d7b5214` / #470, que **no cambia una línea de código**, y el mapeo test → escenario de 2.3 **con sus huecos explícitos** (escenario 5 sin test; escenario 4 con una sola aserción; escenario 7 sin aserción de la mitad "no se le comprueba"; escenario 6 cubierto por dos casos parametrizados, y por qué el de Paso-1-solo es el que importa), para que la revisión pueda comprobar el contrato contra las pruebas sin abrir el código y sepa dónde el contrato NO está fijado por la suite.
- [ ] 4.3 Esperar los gates y mergear si están verdes, sin preguntar. Los gates de backend deberían pasar sin novedad: el diff no toca Python. Si `Backend_Tests` fallara, es una regresión ajena a este change — investigarla, no atribuírsela.
- [ ] 4.4 **No hay verificación post-merge en producción.** El merge de este change publica archivos Markdown: no dispara migración, no cambia comportamiento y no hay humo que probar. El estado de prod ya quedó constatado en 1.6, antes de escribir la spec.

## 5. Cierre

- [ ] 5.1 `/opsx:archive v31-tenancy-role-assertion` y confirmar que `openspec/specs/asyncpg-pool/spec.md` pasa de **4 a 5** requirements, con los cuatro vigentes **byte por byte idénticos** (el `ADDED` no debería tocarlos; si el archive los reescribe, revisar antes de commitear). **Además, editar a mano el bloque `## Purpose` de esa misma spec en el mismo commit del archive**: hoy resume la capability como pool + transacción + claims + adopción de rol + expone la dependencia, y es lo primero que lee cualquiera que abra la capability — después del archive va a convivir con un quinto requirement que puede **rechazar** un request con indisponibilidad, y el formato de delta no cubre el Purpose. Agregar una cláusula del estilo "…adopta un rol de base de datos sin bypass de RLS, **comprueba que la adopción tomó antes de entregar la conexión**, y expone la dependencia…". Queda como decisión registrada acá, no como edición silenciosa ni como resumen desactualizado.
- [ ] 5.2 Verificar con `git show --stat` que el commit del archive incluye los borrados del directorio del change y no sólo los archivos renombrados — el archive de haiku ya dejó una vez deletions sin commitear.
- [ ] 5.3 Actualizar `CHANGES.md`: entrada propia del change y nota en la ficha de `v31-tenancy-pool-rls` de que su riesgo aceptado D6 (*"El cambio de rol por transacción falla abierto si un camino lo omite"*) quedó **cerrado parcialmente**: cerrado y especificado en su **modo *inefectividad*** (la adopción se emite y no toma), **abierto todavía en su modo *omisión*** (un camino que nunca emite la adopción — literalmente lo que D6 enuncia), cuya garantía estructural sigue apuntando al **Paso 3** (rol de login sin atributo de bypass, OQ-2 opción (b)). Redactarlo así y no como "D6 quedó cerrado": escribir "cerrado" a secas le sacaría la justificación al Paso 3, que 5.5 registra como residuo vivo — las dos notas tienen que decir lo mismo. Actualizar el puntero de "próximo change" del `CLAUDE.md` si este change lo movió.
- [ ] 5.4 `mem_save` con el cierre: el gate `TENANCY_TX_SCOPE_ENABLED` queda liquidado también del lado de la spec, y la lección que motiva el requirement — **emitir un statement no es lo mismo que que haya tenido efecto**; un guard sin requirement es un guard que la próxima revisión de performance retira "porque es redundante".
- [ ] 5.5 Registrar en `CHANGES.md` los candidatos que este change deja abiertos. Los residuos del cluster de tenancy (**Paso 3** — rol de login sin `BYPASSRLS`, que es lo que cierra el modo *omisión* de D6 — y el retiro de las dos palancas) ya están anotados desde `v31-tenancy-pool-rls` y **siguen vivos**; este requirement no los toca. Tres candidatos nuevos, detectados al redactar y verificados contra el árbol:
  - **Test del rastro operativo (escenario 5)** — hoy ningún test fija que el rechazo se registre con nivel `CRITICAL` ni que incluya el rol observado y el `sub`. Un test con `caplog` sobre `backend/tests/test_database.py` cerraría el hueco. Es la señal sobre la que se apoya toda la verificación de producción de este change, y hoy se puede degradar a `warning` con la suite verde.
  - **Aserción faltante en el test del contexto de servicio** — `test_get_service_conn_step2_never_adopts_role_even_with_both_flags_on` (`backend/tests/test_database.py:541`) no assertea `conn_mock.fetchval.assert_not_awaited()`, así que la mitad "no se le comprueba rol efectivo" del escenario 7 queda verdadera por construcción y no por aserción. Una línea lo cierra.
  - **`python-backend` envejecida por `tenancy-guard-caja-outbox`** — el escenario *"Solo el router de payments usa get_service_conn"* y la cláusula *"Un endpoint que atiende a un usuario autenticado NOT SHALL usar el contexto de servicio"* (`openspec/specs/python-backend/spec.md`) describen un invariante que producción ya viola: `backend/routers/outbox.py:48` (request autenticado con `require_platform_admin`, deliberado) y `backend/routers/fiscal.py:330` también usan `get_service_conn`. No es un bug de aislamiento —ambos son exenciones de diseño— pero la spec afirma algo falso y ningún gate lo detecta. Corresponde un change propio de corrección de esa capability.
  Los tres son cambios de código o de otra capability: no se hacen acá (regla de oro del alcance).
