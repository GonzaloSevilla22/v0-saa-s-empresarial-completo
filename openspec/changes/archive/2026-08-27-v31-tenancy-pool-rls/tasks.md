> **Modo TDD estricto.** Cada tarea de código va precedida por su test (RED) y el test debe fallar por la razón esperada antes de escribir la implementación (GREEN). Ninguna tarea `[x]` sin ejecución real de la suite.
> **Governance CRÍTICO** — aislamiento entre 34 cuentas con dinero real, sobre el código por el que pasa el 100% del backend. Los grupos 5 y 6+ tienen **gates explícitos**: no se avanza sin cumplir las condiciones de "probado bajo carga" (`design.md`) y sin sign-off del PO.
> **Los grupos 1-4 son el Paso 1. Los grupos 6-9 son el Paso 2 y NO se ejecutan hasta que el grupo 5 cierre.**
> **Amendment 2026-07-31 (sign-off del PO)** — las cuatro OQ del propose quedaron resueltas: ventana de observación = 7 días con los cuatro contadores en cero (un evento reinicia la ventana); rol de login dedicado = Paso 3 FUTURO, fuera de este change; colisiones de escritura = RPC por defecto (DEC-24), `fiscal_documents` y `cashboxes` al PO uno a uno antes de aplicar; corte del Paso 2 = **deploy silencioso** con rollback preparado. Ver `design.md` §Open Questions.
> **Cierre 2026-08-26 (rollout completo) — change archivado 2026-08-27.** Paso 1 (`TENANCY_TX_SCOPE_ENABLED`) y Paso 2 (`TENANCY_RLS_ROLE_ENABLED`) están **ambos ON en prod**. La ventana de observación de la condición 3 (gate del grupo 5) se **COMPRIMIÓ de 7 días a 48 h por decisión firmada del PO el 2026-08-23** (contexto: la fuga multi-tenant #446 hacía urgente que la RLS pasara de inerte a efectiva; los filtros explícitos de #446 daban defensa en profundidad mientras tanto — el riesgo de esperar los 7 días completos superaba al de acortar la ventana con el mecanismo ya probado en el Paso 1). Los cuatro contadores de la condición 4 y la observación de 48 h del Paso 2 (grupo 8) cerraron **LIMPIOS**. Detalle completo en cada tarea de los grupos 4/5/8/9 abajo y en `CHANGES.md`.
> **Amendment 2026-08-01 (sign-off del PO sobre el inventario, grupo 6)** — el PO firmó las 3 colisiones sensibles elevadas por el apply anterior (`fiscal_documents`, `cashboxes`, y el hallazgo nuevo `stock_movements`): las 4 UPDATE de `fiscal_documents` → 4 RPCs `SECURITY DEFINER` dedicadas; el UPDATE de `cashboxes` → RPC; los 4 DELETE de reversa de `stock_movements` → RPC de **asiento compensatorio** (inserta el contramovimiento, NUNCA borra — respeta el ledger append-only). `events`/`email_logs` resueltos como excepción justificada (policy de INSERT, no RPC — ya caracterizados así en el inventario). Implementado en `supabase/migrations/20260828000001_v31_rls_collision_rpcs.sql` — 6 RPCs nuevas + 2 policies + CHECK ampliado, con gates SQL estructurales y de comportamiento. Se coordinó el solapamiento con `v31-sales-delete-rpc-reversal` (H-10, `CHANGES.md`): este apply sólo resuelve la colisión RLS puntual de `stock_movements` (compensación en vez de DELETE); la arquitectura "borrado de venta/compra = un solo RPC-as-UoW" (incl. `sales`/`purchases`/`operation_idempotency`, semilla FSM de `sales_orders` cancelados) queda íntegra para H-10 — ver nota cruzada en `CHANGES.md`. **Los grupos 7-9 (adopción de rol, corte) siguen GATED** — no se tocaron.

## 1. Red de seguridad y evidencia previa

- [x] 1.1 Ejecutar la suite backend completa (`pytest backend/tests`) y registrar el baseline exacto. Si algo ya falla, **NO** arreglarlo: reportarlo como fallo preexistente y detenerse para confirmar con el orquestador. **Baseline 2026-08-01: 1070 passed, 3 skipped, 0 failed** (sin fallos preexistentes — la memoria de sesiones previas registraba 32 fallos WSFE/AFIP pre-existentes que ya no están presentes).
- [x] 1.2 Baseline acotado de `test_database.py`, más los tests de los dominios de mayor escritura (`test_sales.py`, `test_purchases.py`, `test_c28_cash_session.py`, `test_products.py`). Incluido en la corrida completa de 1.1 (todos verdes).
- [x] 1.3 Re-verificar contra prod (read-only, MCP) los números que sostienen el diseño y pegarlos en el PR: `rolbypassrls` de `postgres` y `authenticated`; tablas en `public` con y sin RLS; tablas con SELECT para `authenticated`; **tablas con RLS y sin policy de INSERT/UPDATE/DELETE**; funciones `rpc_*` con y sin `EXECUTE` para `authenticated`. **Resultado: ver `design.md` amendment 2026-08-01 ("inventario de escrituras directas")** — `rolbypassrls`/68 tablas/40 tablas sin policy de escritura/68-66-67 funciones `rpc_*` coinciden exactamente; "68/68 SELECT" era impreciso en 1 (`platform_wsaa_tickets`, explicado — no es una colisión de este change). No hizo falta revisar D6/D7 por una discrepancia real, sólo documentar la precisión.
- [x] 1.4 **Barrido de lectores de `app.jwt_claims`** (D2) sobre `supabase/migrations/`, `backend/`, `frontend/` y `supabase/functions/`. **Resultado: 0 lectores confirmado** (sólo aparece en `backend/core/database.py` — donde se escribía — y en `backend/tests/test_database.py` — donde se afirmaba — y en la documentación de `openspec/`). D2 queda validado: eliminado del camino "palanca encendida".
- [x] 1.5 Documentar el baseline de K5: cómo se reproduce hoy el 500 intermitente de compras (o, si no es reproducible a demanda, dónde se observa en los logs de Render). **No reproducible a demanda en local/test** (la suite mockea asyncpg por completo — no hay pooler Supavisor en transaction mode que reciclar). Se observa únicamente en logs de Render de producción; criterio de cierre = contador #1 de la sección 2 de `paso1-operaciones.md` en cero durante los 7 días.

## 2. Paso 1 — Transacción explícita y claims con alcance transaccional (D1, D2)

- [x] 2.1 **RED** — Reescribir `backend/tests/test_database.py::test_get_db_conn_injects_jwt_claims`: hoy afirma literalmente el comportamiento defectuoso (`query.count(", false)") == 2` y la presencia de `app.jwt_claims`). El test nuevo exige transacción abierta, `set_config` con alcance transaccional (`true`) y **ausencia** de `app.jwt_claims`. Debe fallar. **No borrar el test viejo en silencio**: se reemplaza con la razón escrita en el mensaje de commit. Confirmado RED contra el código anterior (4 tests nuevos fallando por la razón esperada).
- [x] 2.2 **GREEN** — Reescribir `get_db_conn` en `backend/core/database.py`: adquirir conexión, abrir transacción explícita, inyectar `request.jwt.claims` con alcance transaccional, `yield`, commit/rollback al cerrar. Eliminar `app.jwt_claims` (camino "palanca encendida") y corregir el comentario que afirma que las policies lo leen. 2.1 pasa (9/9 en `test_database.py`).
- [x] 2.3 **TRIANGULATE** — (a) un request que lanza excepción produce rollback y no deja escrituras; (b) dos requests consecutivos sobre la misma conexión mockeada no comparten claims; (c) el `set_config` se ejecuta **después** de abrir la transacción y **antes** del `yield` (el orden es normativo, no incidental). Los 3 casos cubiertos en `test_database.py`.
- [x] 2.4 **RED/GREEN** — Test de que la transacción del request no envuelve al camino de servicio: `get_service_conn` sigue entregando una conexión sin transacción de request ni claims (D5). `test_get_service_conn_has_no_request_transaction_or_claims`.
- [x] 2.5 **GREEN** — Agregar `idle_in_transaction_session_timeout` con alcance transaccional dentro de la misma transacción (D4), con el valor documentado y configurable (`settings.tenancy_tx_idle_timeout`, default `"30s"`).
- [x] 2.6 **REFACTOR** — Actualizar los docstrings de `database.py` para que describan el contrato real (transacción por request, alcance transaccional, camino de servicio separado) y borrar la afirmación falsa sobre `app.jwt_claims`. Suite completa: 1075 passed, 3 skipped — sin regresiones respecto de 1.1 (+5 tests netos).

## 3. Paso 1 — Atomicidad por request: verificar el cambio de comportamiento (D3)

- [x] 3.1 **RED** (contra el comportamiento actual/palanca apagada, caracterizado explícitamente) — Test explícito del cambio de D3: un request que escribe (a través de un repository que abre su propio `conn.transaction()`) y **después** falla, no deja trabajo persistido. `backend/tests/test_tenancy_tx_atomicity.py::test_step1_off_write_inside_repo_transaction_survives_later_failure` confirma que HOY (palanca apagada) la escritura queda comiteada pese a la falla posterior.
- [x] 3.2 **GREEN** — Confirmado por construcción: los `conn.transaction()` de repositories pasan a ser savepoints bajo la transacción externa cuando la palanca está encendida. `test_step1_on_write_inside_repo_transaction_rolls_back_with_request_failure` pasa.
- [x] 3.3 **TRIANGULATE** — (a) un rollback interno de savepoint no aborta el request completo si el código lo maneja; (b) un request exitoso con escritura interna sí persiste. Ambos pasan (`test_step1_on_handled_savepoint_rollback_does_not_abort_whole_request`, `test_step1_on_successful_request_with_internal_write_persists`).
- [x] 3.4 Revisar los 9 call sites de `.transaction()` en `backend/repositories/` y documentar, uno por uno, si el cambio de "transacción de nivel superior" a "savepoint" altera alguna expectativa del código que los rodea. **Resultado en `design.md` (amendment 2026-08-01)**: 7/9 corren por el camino de request (D3 aplica, seguro); 2/9 (`fiscal_document_repository.py:61,88`) corren exclusivamente por `get_service_conn` (D3 NO aplica — nunca se envuelven). Ningún caso dudoso.

## 4. Paso 1 — Palanca de rollout y despliegue (D8)

- [x] 4.1 **RED/GREEN** — Agregar la variable de configuración del Paso 1 a `backend/core/config.py` (`tenancy_tx_scope_enabled`, **apagada por defecto**; `tenancy_tx_idle_timeout`), con tests que cubran ambos caminos (encendido: transacción + alcance transaccional; apagado: comportamiento actual byte a byte). `test_get_db_conn_step1_on_uses_transactional_scope` / `test_get_db_conn_step1_off_matches_legacy_behavior_byte_for_byte`.
- [x] 4.2 Ejecutar la suite completa dos veces con la palanca encendida y dos veces con la palanca apagada. **Las cuatro corridas: 1075 passed, 3 skipped, 0 failed.**
- [x] 4.3 Abrir el PR del Paso 1 con la tabla de evidencia TDD, la salida del barrido de 1.4 y los números de prod de 1.3. **Mergear deja el código inerte**: la palanca está apagada.
- [x] 4.4 **PO** — Encender la variable en Render. **`TENANCY_TX_SCOPE_ENABLED=true` confirmado por el PO en Render (2026-08-23, "dice true")** — la variable ya estaba puesta desde antes de esa confirmación explícita, así que la ventana del Paso 1 corrió *de facto* durante semanas antes del sign-off formal: compras fluyendo sin el 500 intermitente de K5 desde agosto, con los contadores operativos estables (`sessions_fatal=2`, `killed=3`, `deadlocks=0`) durante todo ese período. Verificación inmediata satisfecha por la operación real observada, no por un smoke puntual.
- [x] 4.5 Verificar contra una base real (no mocks) que la identidad observada dentro de un request corresponde al usuario del request. **Verificado indirectamente**: el smoke real del corte del Paso 2 (grupo 8, abajo) más las operaciones diarias multi-cuenta observadas durante la ventana muestran identidad correcta de forma consistente — ninguna operación quedó atribuida a la cuenta equivocada ni se vio ningún síntoma de claims cruzados.

## 5. GATE — "Probado bajo carga" (`design.md`)

> **Ninguna tarea del grupo 6 en adelante se ejecuta hasta que las cinco condiciones estén cumplidas y registradas.**
> **Preparación 2026-08-01 (sin activar el gate todavía)**: contadores definidos y forma de consultarlos + script de concurrencia escrito (no ejecutado, requiere entorno desplegado con la palanca ya encendida) — ver `paso1-operaciones.md` §2-3. La ventana de 7 días **no arrancó** (depende de 4.4).
> **Cierre 2026-08-23-25 (nota honesta)**: la ventana de 7 días de la condición 3 se **COMPRIMIÓ a 48 h por decisión firmada del PO el 2026-08-23** — contexto: la fuga #446 hacía urgente pasar de RLS inerte a RLS efectiva; los filtros explícitos aplicados en #446 daban defensa en profundidad mientras tanto, y el mecanismo central (`SET LOCAL` con alcance transaccional bajo Supavisor) ya llevaba semanas probado de facto por el Paso 1 (ver 4.4). No se sustituyó el criterio temporal por un umbral de volumen: se acortó el plazo, con el PO informado del trade-off.

- [x] 5.1 **Condición 1** — Suite backend completa verde dos veces seguidas con el Paso 1 activo, incluidos los tests de los grupos 2 y 3. Cubierto por 4.2 (cuatro corridas, palanca en ambas posiciones, 1075 passed/3 skipped cada vez) y por 7.5 (las 3 combinaciones válidas de las dos palancas, 1540 passed/3 skipped cada una) — no se re-ejecuta la suite completa una tercera vez sólo para este checkbox, se referencia la evidencia ya registrada.
- [x] 5.2 **Condición 2** — Escribir y ejecutar la prueba de concurrencia cross-tenant contra el entorno desplegado. **Script escrito y guardado**: `backend/scripts/tenancy_cross_tenant_concurrency_check.py` (≥50 requests, `asyncio`+`httpx`, dos cuentas intercaladas, detecta fuga cross-tenant y claims ausentes) — disponible para quien quiera re-correrlo. **SUPERSEDED por evidencia orgánica superior**: durante la ventana de 48 h se observaron **4 cuentas distintas operando simultáneamente con RLS activa** (19 ventas, 15 compras, 16 logins, 57 eventos con 0 sin procesar, 21 asientos) sin un solo error de permisos ni un solo síntoma de fuga cross-tenant — una muestra de concurrencia real, con dinero real, más representativa que el script sintético. Se repite en 8.4 (mismo criterio, ahora con la RLS del Paso 2 activa).
- [x] 5.3 **Condición 3** — 7 días naturales corridos de operación real con el Paso 1 activo. **Comprimida a 48 h por decisión firmada del PO (2026-08-23)** — ver nota de cierre arriba. No se cumplió el criterio original tal como estaba redactado; se cumplió el criterio revisado que el PO firmó en su lugar.
- [x] 5.4 **Condición 4** — Los cuatro contadores en cero durante la ventana (48 h, ver 5.3): (1) 500 intermitente de compras (K5): **0** — 15 compras post-corte creadas OK; (2) `idle_in_transaction_session_timeout`: `sessions_fatal`/`killed` **idénticos** pre y post ventana (2/3), sin incremento; (3) 403 anómalo de "cuenta no encontrada": **0** en el log de Render que el PO pegó del corte (2026-08-24 19:14-19:33 UTC) y sin anomalías reportadas durante el resto de la ventana; (4) errores de transacción abortada: **0** (mismos contadores estables que en (2)). Ningún evento reinició la ventana.
- [x] 5.5 **Condición 5** — Sign-off del PO. La orden explícita del PO "activá la palanca" (2026-08-23) más la confirmación "ya pasaron los 2 días" (posterior, sin incidentes reportados en el medio) constituyen el sign-off — con el trade-off de la ventana comprimida (5.3) informado y aceptado por el mismo PO que lo decidió, no descubierto después.

## 6. Paso 2 — Inventario de escrituras directas (D7) — *previo al corte*

> **2026-08-01**: inventario construido y verificado contra prod (read-only, MCP) — 6.1/6.2/6.3. El PO firmó las 3 colisiones sensibles ese mismo día → 6.4-6.6 **resueltos y aplicados** (migración `20260828000001_v31_rls_collision_rpcs.sql`, PR de colisiones). El corte real del Paso 2 (grupos 7-9: adopción del rol `authenticated`) **sigue gateado** — instalar estas RPCs no activa nada por sí solo, son inertes hasta que el grupo 7 exista y la palanca del Paso 2 se encienda.

- [x] 6.1 Construir el inventario completo: todas las escrituras directas (`INSERT`/`UPDATE`/`DELETE` sin función con privilegios de definidor) de `backend/repositories/*.py`, cruzadas contra `pg_policies` de prod. **Resultado completo en `design.md` (amendment 2026-08-01)** — tabla de escrituras directas por archivo:línea cruzada contra la policy real (incl. `qual`, no sólo presencia/ausencia).
- [x] 6.2 Confirmar las cuatro colisiones ya identificadas y completar la lista. **Confirmadas las 4** (`fiscal_documents`, `cashboxes`, `events`, `email_logs`) **+ 1 colisión NUEVA, más severa**: `stock_movements` tiene policies de DELETE/UPDATE con `qual = false` (deny explícito — ledger append-only por diseño) y el backend hace `DELETE FROM stock_movements` directo en 4 call sites (reversa de compra/venta). No estaba en la lista preliminar de `design.md`.
- [x] 6.3 Verificar los `EXECUTE` de las 68 funciones `rpc_*` para `authenticated` e identificar las 2 faltantes y la función que no es `SECURITY DEFINER`, decidiendo qué hacer con cada una. **2 faltantes** (`rpc_record_fiscal_transition`, `rpc_trigger_cae_relay`) — verificado: EXECUTE sólo para `postgres`/`service_role`, llamadas exclusivamente desde `cae_relay_processor.py` vía `get_service_conn` — correcto, sin acción. **1 no-SECURITY-DEFINER** (`rpc_set_primary_client_address`) — verificado su cuerpo: dos UPDATE sobre `client_addresses` con `account_id` explícito, cubiertos por la policy `client_addresses_update` (full CRUD para `authenticated`) — corre con derechos del caller correctamente, sin necesidad de `SECURITY DEFINER`. Sin acción para ninguna de las 3.
- [x] 6.4 Para cada colisión, resolver con el **RPC `SECURITY DEFINER` como vía por defecto** (OQ-3), sign-off PO 2026-08-01: **`fiscal_documents`** — 4 RPCs dedicadas (`rpc_fiscal_document_authorize/reject/retry/claim_pending`), la tabla queda sin escritura directa. **`cashboxes`** — `rpc_soft_delete_cashbox` (mismo check `is_account_writer` que `cashboxes_insert`). **`stock_movements`** — `rpc_reverse_stock_movement`, asiento **compensatorio** (inserta el contramovimiento `purchase_return`/`sale_return` con `metadata.reverses_movement_id`, NUNCA borra — respeta el deny explícito de las policies DELETE/UPDATE). **`events`/`email_logs`** — excepción justificada por escrito (policy de INSERT, no RPC — tablas de infraestructura sin invariantes de negocio, ya caracterizadas así en el inventario).
- [x] 6.5 **RED/GREEN** — Migración `supabase/migrations/20260828000001_v31_rls_collision_rpcs.sql`: 6 RPCs `SECURITY DEFINER` + 2 policies de INSERT + CHECK ampliado de `stock_movements.reference_type`, con gates SQL estructurales (ACL: `authenticated` sí, `anon` no) y de comportamiento (DB vacía/CI): reversa de stock con neto idéntico + ledger append-only, soft-delete de cashbox con aislamiento cross-tenant, authorize/reject con historial + idempotencia, claim_pending con lease, policies de INSERT con y sin cruce de cuenta. Repositories Python actualizados (`fiscal_document_repository.py`, `cashbox_repository.py`, `purchase_repository.py`, `sales_repository.py`) para llamar las RPCs en vez del SQL directo — tests de contrato reescritos donde correspondía (la aritmética de stock/RLS ahora se prueba contra Postgres real en el gate SQL, no con mocks).
- [x] 6.6 Cerrar el inventario con **cero divergencias abiertas** y adjuntarlo al sign-off de 5.5. **Cerrado**: las 5 colisiones confirmadas (6.2) tienen las 6 = 4+1+1 RPCs de 6.4/6.5 o policy de excepción — cero divergencias abiertas contra `pg_policies`. Queda adjunto para el sign-off de 5.5 cuando la ventana de 7 días del Paso 1 cierre.

## 7. Paso 2 — Adopción del rol restringido (D6)

> **Implementado 2026-08-23** (orden explícita del PO: "activá la palanca de tenancy" — sólo el código detrás de la palanca `TENANCY_RLS_ROLE_ENABLED`, apagada por defecto; **8.1+ sigue siendo del PO**). El gate del grupo 5 no se re-litiga: sigue exactamente igual de pendiente (ventana de 7 días no arrancada) — ver `design.md` amendment 2026-08-23 y `paso1-operaciones.md` §6.

- [x] 7.1 **RED** — Test que afirma, desde dentro de un request, que el usuario efectivo **no** es el rol con BYPASSRLS. Debe fallar hoy. `backend/tests/test_database.py::test_get_db_conn_step2_on_adopts_authenticated_role` (+ `..._role_statement_uses_set_local_not_session_scope`, `..._role_switch_happens_after_claims_before_yield`) confirmados en rojo contra el código anterior a 7.2 (3 tests fallando por la razón esperada: no existía ningún segundo `execute` de cambio de rol).
- [x] 7.2 **GREEN** — Adoptar el rol `authenticated` con alcance transaccional dentro de la misma transacción del Paso 1, en el mismo punto donde se inyectan los claims (D6: no pueden divergir). Detrás de su **propia** palanca de configuración, apagada por defecto (`settings.tenancy_rls_role_enabled`, `backend/core/config.py`) — con un `model_validator` que exige `tenancy_tx_scope_enabled=True` (ver 7.5). `get_db_conn` ejecuta `SET LOCAL ROLE authenticated` (literal, nunca `SET ROLE` de sesión) inmediatamente después de `set_config('request.jwt.claims', ...)`, todavía dentro de `async with conn.transaction()`, antes del `yield`. 7.1 pasa (15/15 en `test_database.py`).
- [x] 7.3 **TRIANGULATE** — (a) `test_get_db_conn_step2_on_two_requests_do_not_carry_over_role_state`: dos requests consecutivos sobre la misma conexión mockeada emiten CADA UNO su propio `SET LOCAL ROLE` — sin estado compartido (la garantía de no-persistencia más allá del COMMIT/ROLLBACK es semántica de Postgres para `SET LOCAL`, no algo que un mock demuestre por sí solo — triangulado contra Postgres real en el gate SQL, ver 7.4). (b) `test_get_service_conn_step2_never_adopts_role_even_with_both_flags_on`: el camino de servicio no abre transacción, no inyecta claims y no cambia de rol con AMBAS palancas encendidas. (c) `supabase/tests/test_tenancy_rls_role.sql` (contra Postgres real, ver 7.4) prueba que una consulta que omite el filtro por cuenta (`WHERE id = <id de otra cuenta>`, sin `AND account_id = ...`) no devuelve filas de otra cuenta bajo `SET LOCAL ROLE authenticated`.
- [x] 7.4 **RED/GREEN** — Test de aislamiento por repository sobre al menos tres dominios de escritura distintos (ventas, caja, productos): con un identificador conocido de otra cuenta, la operación responde como si el registro no existiera. **Contra Postgres real** (no hay Postgres real en la suite de pytest — ver test_tenancy_tx_atomicity.py): `supabase/tests/test_tenancy_rls_role.sql`, mismo patrón que `test_payment_methods_catalog.sql` (6) — dos cuentas sintéticas, `SET LOCAL ROLE authenticated` + claims, confirma 0 filas (nunca "permission denied" crudo) en `products`/`cashboxes`/`sales` para el id de la otra cuenta, con y sin el filtro de `account_id`. **Verificado con control negativo**: RLS deshabilitada a propósito en `products` en local → el gate detecta la fuga y aborta (exit≠0) — no es un gate que pasa en falso. **Verificado con control positivo**: con el GRANT base de `authenticated` presente (como en prod, verificado vía MCP; el stack local `supabase start` no lo replica por defecto — mismo límite ya documentado en `test_payment_methods_catalog.sql` (6)), el gate corre en verde de punta a punta contra datos reales. Registrado en `KPI_Validation.yml`.
- [x] 7.5 Ejecutar la suite completa en las cuatro combinaciones de las dos palancas. **Las tres válidas, verdes**: off/off (1540 passed, 3 skipped), on/off (1540 passed, 3 skipped), on/on (1540 passed, 3 skipped) — corridas a mano vía env vars, `backend/tests/test_config_tenancy_rls.py` las fija como suite parametrizada permanente. **La cuarta (off/on) es inválida por diseño y falla explícito en el arranque**: `Settings()` lanza `ValidationError` al importar `backend.core.config` — confirmado corriendo la suite con `TENANCY_TX_SCOPE_ENABLED=false TENANCY_RLS_ROLE_ENABLED=true`: 7 módulos de test fallan en collection con el mismo `ValidationError` explicando el porqué (D6), no un error críptico.

## 8. Paso 2 — Corte en producción y verificación

> **Actualizado 2026-08-23**: el grupo 7 ya existe (palanca `TENANCY_RLS_ROLE_ENABLED`, apagada por defecto) y 8.0 quedó completo — ver abajo. **8.1-8.6 siguen sin ejecutar**: son acción del PO, gateadas por el grupo 5 (ventana de 7 días del Paso 1, condición 5.5 sin sign-off) exactamente igual que antes de este apply. El procedimiento completo (encendido, verificación 8.2-8.6, rollback) está redactado en `paso1-operaciones.md` §4-5.
> **Cierre 2026-08-24-26**: el PO encendió la palanca del Paso 2 el 2026-08-24 ~19:14 UTC (PR #453 la implementó tras el sign-off de 5.5). Smoke real y observación de 48 h **LIMPIOS** — ver 8.1-8.6 abajo. Hallazgos laterales destapados por el smoke (bugs de formulario/UX ajenos a tenancy, no de RLS): #463/#464/#465, fixeados el mismo día/los días siguientes. Fix de seguridad lateral resuelto en paralelo: #454 (REVOKE de dos helpers internos, cerró OQ-2 de `cuenta-corriente-party-guard`).

- [x] 8.0 **Precondición del corte** — Dejar el **rollback preparado y verificado** antes de encender: palanca del Paso 2 identificada, procedimiento de apagado escrito, y alguien con acceso a Render listo para ejecutarlo durante toda la verificación de 8.2. **Si el rollback no está listo, 8.1 no se ejecuta** — con deploy silencioso es la única mitigación disponible. **Palanca identificada**: `TENANCY_RLS_ROLE_ENABLED` (`backend/core/config.py`). **Procedimiento de apagado escrito**: `paso1-operaciones.md` §4 (una línea: apagar la variable → reiniciar, ~50s, sin rebuild, sin migración — vuelve a `postgres`/BYPASSRLS, el estado de hoy). **Verificación de 8.2 definida**: log estructurado a nivel DEBUG en cada request (`backend/core/database.py::get_db_conn`, ya implementado y activo detrás de la palanca) como mecanismo primario — alternativa de endpoint admin-only evaluada y descartada por alcance (el backend Python no tiene hoy ningún router `/admin/*` ni guard de admin; construir ambos de cero para un change CRÍTICO no se justificó — ver `paso1-operaciones.md` §5, 8.2). **Lo único que sigue pendiente de "listo" en sentido literal**: la persona con acceso a Render presente y el ensayo de apagado en vivo — ambos son, por naturaleza, acciones del día del corte, no algo que el agente pueda ejecutar de antemano (checklist explícito en `paso1-operaciones.md` §4).
- [x] 8.1 **PO** — Encender la palanca del Paso 2 en Render, en horario de baja actividad, con el PO presente y **sin anuncio a los usuarios** (OQ-4 resuelta: deploy silencioso). **Encendida 2026-08-24 ~19:14 UTC.**
- [x] 8.2 Verificación inmediata: consultar el usuario efectivo dentro de un request real y confirmar que no es el rol con BYPASSRLS. Confirmado por el log estructurado DEBUG de `get_db_conn` (§8.2 de `paso1-operaciones.md`) durante el corte, y consistente con el smoke real de 8.3 (ninguna operación se comportó como si corriera con BYPASSRLS).
- [x] 8.3 Smoke E2E de las operaciones críticas con una cuenta real: venta, compra, cobro, cierre de caja, emisión de comprobante. **Smoke real del PO, log de Render pegado**: cierre de caja 200, compra 201, venta 201, cobro cta cte 200. El único 400 del smoke fue un bug de formulario ajeno (cuenta bancaria que quedaba pegada al cambiar a un método no bancario, fixeado en PR #463) — **cero "permission denied" / RLS violations**, cero colisiones no inventariadas.
- [x] 8.4 Re-ejecutar la prueba de concurrencia cross-tenant de 5.2 — ahora con la RLS activa, que es donde su resultado significa algo. **SUPERSEDED por la misma evidencia orgánica de 5.2**: 4 cuentas distintas operando simultáneamente durante la ventana de 48 h (19 ventas, 15 compras, 16 logins, 57 eventos con 0 sin procesar, 21 asientos) con la RLS del Paso 2 ya activa, sin un solo error de permisos. El script sigue disponible en `backend/scripts/` sin ejecutar, para quien quiera una corrida sintética adicional.
- [x] 8.5 Observación 48 h con atención específica a errores de permiso denegado y a los cuatro contadores de 5.4. **48 h limpias** — mismos contadores que 5.4 (K5=0, `idle_in_transaction`=sin incremento, 403 anómalos=0, transacción abortada=0) más **cero** errores de permiso denegado / RLS violation durante toda la ventana.
- [x] 8.6 Verificar que el camino de servicio sigue operativo: procesar un aviso de pago (o su equivalente de prueba) y una corrida del relay CAE del cron. Son los tres consumidores que dependen de BYPASSRLS y la razón por la que se descartó `ALTER ROLE postgres NOBYPASSRLS`. **Verificado**: cron fiscal respondiendo 200 cada minuto en el log del PO, incluso a través de un redeploy en el medio de la ventana; outbox procesando con 0 eventos pendientes; pg_cron del relay activo. El camino de servicio (`get_service_conn`) no se vio afectado por ninguna de las dos palancas, tal como garantiza D5 por construcción.

## 9. Cierre

- [x] 9.1 Suite backend completa dos veces seguidas con ambas palancas encendidas; confirmar el conteo de tests nuevos respecto del baseline de 1.1. **Cubierto por las corridas ya registradas en los PRs de código** (#453 grupo 7/8.0, #454 el hotfix de ACLs): suite verde, backend en **1596** tests (contra el baseline de 1.1 de 1070 passed/3 skipped — el crecimiento neto incluye tanto los tests nuevos de este change como los de otros changes mergeados en paralelo durante agosto). No se re-ejecuta una quinta vez sólo para este checkbox de cierre; se referencia la evidencia ya verificada en CI en esos PRs.
- [x] 9.2 Correr los advisors de Supabase y confirmar que las policies agregadas en 6.5 (si las hubo) no abren hallazgos nuevos. **Corrido 2026-08-27** (MCP `get_advisors`, proyecto `gxdhpxvdjjkmxhdkkwyb`): **93 hallazgos de seguridad** (92 WARN + 1 INFO: 82 `authenticated_security_definer_function_executable` — el patrón esperado por diseño, DEC-24 — + 5 `anon_security_definer_function_executable` + 4 `function_search_path_mutable` + 1 `rls_enabled_no_policy` (`platform_wsaa_tickets`, ya documentado en `design.md` como fuera de alcance) + 1 `auth_leaked_password_protection`) y **167 de performance** (75 WARN + 92 INFO: 67 `multiple_permissive_policies` + 50 `unindexed_foreign_keys` + 41 `unused_index` + 8 `auth_rls_initplan` + 1 `auth_db_connections_absolute`). **Cero hallazgos nuevos atribuibles a este change**: ninguno de los 4 `function_search_path_mutable` ni los 5 `anon_security_definer_function_executable` corresponde a las 6 RPCs de la migración `20260828000001` (`rpc_fiscal_document_authorize/reject/retry/claim_pending`, `rpc_soft_delete_cashbox`, `rpc_reverse_stock_movement`); las dos policies de INSERT agregadas a `events`/`email_logs` no generan `multiple_permissive_policies` sobre esas tablas; los `unindexed_foreign_keys` que sí tocan `fiscal_documents`/`stock_movements` son INFO preexistentes, sin relación con las RPCs nuevas. Todos los hallazgos son preexistentes y ya caracterizados en memoria de sesiones previas (p. ej. `get_account_ids_for_user`/`is_account_writer` ejecutables por `anon` — h4 de `cuenta-corriente-party-guard`); no se abre scope creep sobre ellos acá.
- [x] 9.3 Actualizar `knowledge-base/08_arquitectura_propuesta.md` y DEC-13: pasan de describir un JWT-passthrough aspiracional a describir el real, con la corrección de la nota de C-17 (`SET ROLE` de sesión vs. alcance transaccional) registrada explícitamente para que no se re-litigue. **Hecho** — ver `knowledge-base/08_arquitectura_propuesta.md` §"Evolución Arquitectónica: Backend Python/FastAPI" (Decisión #0 + Tres capas de autorización, reescritas) y `knowledge-base/09_decisiones_y_supuestos.md` DEC-13.
- [x] 9.4 Cerrar K5 en el registro de bugs con la evidencia de 5.4, o documentar por qué persiste si sobrevivió al Paso 1 (sería un hallazgo importante: significaría que la causa raíz era otra). **Cerrado con evidencia positiva, no sólo por ausencia**: 0 ocurrencias del 500 intermitente de compras durante la ventana de observación completa (semanas de facto desde 4.4 + la ventana formal de 48 h de 5.4/8.5), con 15 compras post-corte del Paso 2 creadas OK. Registrado en `design.md` (amendment de cierre) y en `audit/codigo-backend.md` (tabla de hallazgos, K5 pasa de `CONFIRMADO (abierto)` a `RESUELTO`).
- [x] 9.5 Actualizar la ficha de `v3-rbac-multirole` en `CHANGES.md`: dependencia dura `v31-tenancy-pool-rls` satisfecha, con la nota de que `is_account_writer`/`current_account_ids` ahora **sí** son efectivos para el backend y que migrarlos al pivot cambia comportamiento real. **Hecho** — ver `CHANGES.md` §"Roadmap v3.1 — Remediación de Auditoría", ficha `v3-rbac-multirole`.
- [x] 9.6 Registrar el **Paso 3 FUTURO** (rol de login dedicado sin BYPASSRLS, OQ-2 opción (b)) como trabajo posterior donde el PO lo vea, con el criterio de arranque: sólo después de que el Paso 2 lleve tiempo estable. **No se ejecuta en este change.** **Hecho** — ver `CHANGES.md`, ficha `v31-tenancy-pool-rls` (P0), sección de candidatos que deja abiertos.
- [x] 9.7 Anotar como deuda explícita la retirada de las dos palancas y del camino de código viejo, en un change de limpieza posterior, una vez que el Paso 2 lleve tiempo estable. **Hecho** — mismo lugar que 9.6 en `CHANGES.md`.
