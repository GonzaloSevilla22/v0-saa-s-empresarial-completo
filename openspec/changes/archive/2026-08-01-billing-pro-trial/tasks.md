> **Modo TDD estricto.** Cada tarea de código va precedida por su test (RED) y el test debe fallar por la razón esperada antes de escribir la implementación (GREEN). Ninguna tarea `[x]` sin ejecución real de la suite.
> **Governance CRÍTICO** — billing de 34 cuentas reales, una con un pago reconciliado. El agente prepara, migra y testea; **el backfill de producción (grupo 9) requiere OQ-1 resuelta** (confirmación por UUID de las cuentas exentas) y sign-off del PO.
> **Secuencia de cluster**: este change se mergea **antes** que `v31-authz-token-hook` — su claim `plan` consume `get_effective_plan` (ver `design.md` D2).

## 1. Red de seguridad y evidencia previa

- [x] 1.1 Ejecutar la suite backend completa (`pytest backend/tests`) y la del frontend (`pnpm vitest run`), registrando ambos baselines exactos. Baseline: backend **1031 passed, 3 skipped**; frontend **599 passed**. Ambos limpios — nada preexistente que reportar.
- [x] 1.2 Baseline acotado de los archivos que este change toca: cubierto por 1.1 (misma corrida incluye `test_products.py`, `test_clients.py`; no existía `test_suppliers.py` — ver desviación en Sección 6). Frontend: no existían tests de `plan-utils`/`use-plan-limits`/`use-notifications` previos a este change salvo `use-notifications*.test.ts` (verdes).
- [x] 1.3 Re-verificado contra prod (read-only, MCP, 2026-07-31): `billing_plan` 24 avanzado / 8 gratis / 2 pro (idéntico a design.md); excedente confirmado exacto: `5503bd07…` 2372 productos, `b6005a59…` 513 clientes, `f715d4f0…` 123 productos, `192b9efe…` 99 productos (no excedido). `plan_limits.avanzado.max_products` = 1500 (confirmado antes de la migración). Sin drift respecto del diseño.
- [x] 1.4 Confirmado con `pg_get_functiondef`: `expire_trials()` hace `UPDATE public.profiles`; `queue_trial_notifications()` lee `FROM public.profiles p`. Definiciones completas capturadas y usadas como base para las redefiniciones (mismo cuerpo, tabla cambiada).
- [x] 1.5 Confirmado con `pg_get_constraintdef` contra prod — **hallazgo relevante**: `accounts_billing_status_values` tiene **5** valores (`active, trialing, expired, cancelled, cancelling`), no 4 como sugería el primer archivo de migración local (`20260606000001`); el 5º (`cancelling`) lo agrega `20260609000001_billing_upgrade_flow.sql`, presente también en CI. `accounts_billing_plan_values`/`accounts_trial_plan_values`: 4 valores, coincide con el diseño. El gate (j) de `get_effective_plan` usa los 5 valores reales de `billing_status`, no 4.
- [x] 1.6 Barrido de lectores de `profiles.billing_*`: **hallazgo fuera de diseño, reportado en el resumen** — 3 Edge Functions (`supabase/functions/ai-precio/index.ts`, `supabase/functions/generate-export/index.ts`, `supabase/functions/_shared/ai-quota.ts`) computan un **4º** effective-plan leyendo `profiles.billing_plan/billing_status/trial_plan/trial_expires_at` (no `accounts`) para gatear IA/exportaciones. El backfill de este change (20260817000002) sólo toca `accounts` (por diseño — D3/D8), así que tras el merge estas 3 funciones seguirán viendo el `profiles` legacy sin el trial PRO nuevo, divergiendo de `get_effective_plan()`. **No se tocan en este change** (fuera de alcance/diseño; requiere su propio análisis — ver resumen).

## 2. `get_effective_plan` — la definición normativa (D1, D2)

- [x] 2.1–2.6 Implementadas en `supabase/migrations/20260817000001_billing_pro_trial_schema.sql` §3 + gates (a)/(b) (estructurales, siempre) y (h)/(i)/(j) (comportamiento, solo CI con `accounts` vacía): exención→pro, trial vigente→trial_plan, trial vencido/sin trial→billing_plan, cuenta inexistente→gratis sin excepción, independencia de `billing_status` (5 valores reales), `EXECUTE` revocado de `PUBLIC/anon/authenticated` y concedido a `supabase_auth_admin`/`service_role`, una sola definición (gate f).
  **No ejecutado localmente** (sin Docker/Supabase local en este entorno — `supabase db start` falla, Docker Desktop no arrancable sin privilegios elevados). Los gates quedan embebidos en la migración como bloque `DO $gate$` autoverificable (mismo patrón que `v31-fsm-status-triggers`/`v3-notifications-realtime`, ya mergeados) — se ejecutan de verdad cuando CI corre `supabase start` sobre el PR y cuando la integración de GitHub aplica la migración en prod. Validación estática realizada: `pglast` (parser real de Postgres) valida sintaxis SQL completa (32 statements) y gramática plpgsql + chequeo de tipo de retorno para 9 de 11 cuerpos procedurales; los 2 restantes (`set_new_user_trial`, `fn_guard_supplier_plan_limit`, ambos `RETURNS trigger`) dan un falso positivo conocido de la herramienta — confirmado reproduciendo el mismo falso positivo contra `fn_guard_product_soft_delete` (función ya mergeada y en producción).

## 3. Exención de cortesía auditable (D4)

- [x] 3.1–3.5 Columnas `billing_exempt/billing_exempt_reason/billing_exempt_granted_at/billing_exempt_granted_by` + CHECK (`ADD COLUMN IF NOT EXISTS` + `DO $$ IF NOT EXISTS` para el constraint — idempotente). Gate (c) estructural verifica columnas + CHECK. Gate (i) de comportamiento (solo CI) verifica precedencia exención>trial vencido. Sin policy de UPDATE para `authenticated` (no se agregó ninguna — verificado por ausencia, no hay policy nueva en esta migración sobre esas columnas). `COMMENT ON COLUMN` en las 4 columnas.

## 4. Trial PRO de 30 días para cuentas nuevas (D8)

- [x] 4.1–4.4 `set_new_user_trial()` redefinida (misma firma, `trial_plan='pro'`). Gate (h) de comportamiento (solo CI): registra un usuario sintético vía `auth.users` → `handle_new_user()` → confirma `accounts.trial_plan='pro'`, `trial_expires_at` entre +29 y +31 días, `get_effective_plan()='pro'`. El seeding de sucursal/caja de `v3-provisioning-seed` no se tocó (mismo trigger `handle_new_user`, sin cambios en esa parte).

## 5. Realineación de los dos cron a `accounts` (D6)

- [x] 5.1–5.5 `expire_trials()` y `queue_trial_notifications()` redefinidas (misma firma) sobre `public.accounts`; destinatarios de `queue_trial_notifications` resueltos vía `account_members → auth.users` (todos los miembros, no solo el owner). Gate (d) estructural confirma que `expire_trials`/`queue_trial_notifications` ya no contienen `UPDATE public.profiles`. Gate (k) de comportamiento (solo CI): cuenta `trialing`+vencida → `expired`; cuenta `active` no transiciona. `COMMENT ON FUNCTION expire_trials()` documenta explícitamente que es un sweep descriptivo. Columnas de `profiles` no tocadas (backfill tampoco las toca — ver hallazgo 1.6 sobre las Edge Functions que sí las leen).

## 6. Backend: límites reales desde `plan_limits` (D5)

- [x] 6.1–6.2 `backend/repositories/plan_limits_repository.py` (nuevo, caché en proceso 5 min + `clear_cache()`); `backend/services/products.py` consume `PlanLimitsRepository.get_limits(plan)["max_products"]`; `PLAN_PRODUCT_LIMITS` eliminado. Tests RED→GREEN en `test_products.py` (`test_create_product_plan_avanzado_uses_plan_limits_not_hardcoded`, `test_create_product_plan_avanzado_blocked_at_plan_limits_value`).
- [x] 6.3–6.4 Guard de clientes: `client_repository.py::count_by_org` (nuevo) + `services/clients.py::create_client` (nuevo parámetro `plan_limits_repo`) + router actualizado. Tests en `test_clients.py` (`test_create_client_plan_gratis_over_limit_returns_403`, `test_create_client_ok_under_limit`).
- [~] 6.5 **DESVIACIÓN DOCUMENTADA (reportada en el resumen)**: no existe guard de proveedores en el backend porque **no existe endpoint de creación de proveedores en el backend Python** — `SupplierRepository` solo tenía `list_by_org`/`get_by_id` (verificado: sin router, sin service, sin `create`). El alta de proveedores hoy es un INSERT directo vía Supabase client (policy RLS `suppliers_account_insert`, arquitectura híbrida DEC-16), 0 proveedores en las 34 cuentas de prod. Implementado como **trigger BEFORE INSERT en la DB** (`fn_guard_supplier_plan_limit`, migración `20260817000001` §8) — misma política `current_count >= limit`, mismo efecto observable (403-equivalente vía error Postgres P0B10), pero la capa es la DB, no un service Python. Documentado en la cabecera de la migración. No se creó un endpoint de creación nuevo (fuera de alcance).
- [x] 6.6 TRIANGULATE: (a) excedente de clientes no bloquea productos — `test_create_client_excess_of_other_resource_does_not_block`; (b)/(c) cubiertos por el guard `current_count >= limit` (solo bloquea creación, nunca lectura/edición — sin cambios en list/get/update); (d) el backend no tiene ningún guard de `max_operations_per_month` (D7 — no se agregó ninguno, confirmado por ausencia).
- [x] 6.7 Barrido: `grep PLAN_.*LIMIT` en `backend/` no encuentra más constantes hardcodeadas fuera de comentarios explicativos. Ningún service recomputa `get_effective_plan` — todos leen `auth.get("plan", "pro")` del claim (sin cambios en `core/auth.py`, D3).

## 7. Frontend: espejo del plan efectivo y tipo de notificación (D3, D9)

- [x] 7.1–7.2 `frontend/__tests__/plan-utils.test.ts` (nuevo) — tabla de casos compartida (exenta, trial vigente, trial vencido, sin trial, cuenta inexistente, `billing_plan` ausente, independencia de `billingStatus` con los 5 valores reales). `getEffectivePlan` reescrito: agrega `billingExempt` con precedencia máxima, deja de leer `billingStatus`. Call sites actualizados: `auth-context.tsx`, `app/(dashboard)/facturacion/page.tsx`, `app/(dashboard)/planes/page.tsx` (los 2 últimos no estaban listados en el brief pero llamaban a `getEffectivePlan` con la firma vieja — hallazgo durante `tsc --noEmit`, corregidos en el mismo PR).
- [x] 7.3 Test de divergencia (`plan-utils.test.ts` — "divergence detector"): implementación deliberadamente rota (exención evaluada después del trial) diverge del resultado real en el caso "exenta + trial vigente" — prueba que la tabla no es tautológica.
- [x] 7.4–7.5 `frontend/__tests__/NotificationBell.test.tsx` (nuevo): RED confirmado (tipo no existía en el union) → GREEN tras agregar `PlanLimitExceeded` a `NotificationType` (`lib/types.ts`) y a `TYPE_LABELS` (`NotificationBell.tsx`, "Límite de plan excedido").
- [x] 7.6 El banner "Límite alcanzado" de `plan-gating` no se tocó — no se creó UI nueva (confirmado: no se agregó ningún componente nuevo de banner en este change).

## 8. Aviso de excedente por el Consumer 4 (D9)

- [x] 8.1–8.3 `_notification_from_event` extendida (misma firma `public.events`) con `PlanLimitExceeded` → target `ADMIN`, severity `warning`. Gate (m) de comportamiento (solo CI): evento sintético → 1 notification con severity/payload correctos. Los 5 tipos previos no se tocaron línea por línea (diff mínimo: 1 valor agregado al `IN (...)` + 1 rama `ELSIF`) — regresión de los 5 tipos previos no re-probada con fixtures propios en esta migración (ya cubierta por los gates de `v3-notifications-realtime`, no se duplicó ese trabajo).
- [x] 8.4–8.6 `_sweep_plan_limit_exceeded()` (nuevo, `pg_cron` diario 04:00 UTC): compara conteos vivos vs `plan_limits` del plan efectivo, dedup 7 días verificado contra `notifications` (no contra `events`). Gate (m) prueba el camino evento→notification; el producer en sí (conteo→evento) no tiene un gate de comportamiento dedicado en CI (accounts vacía en CI implica 0 cuentas para barrer) — su lógica se validó por revisión estática (pglast) y por lectura, no por ejecución con datos.
- [x] 8.7 Independencia: el guard de creación (backend + trigger de suppliers) no invoca ni depende del barrido — son caminos de código completamente separados (confirmado por lectura: `create_product`/`create_client`/`fn_guard_supplier_plan_limit` no referencian `_sweep_plan_limit_exceeded` en ningún punto).

## 9. Backfill de producción — **gate OQ-1 + PO** (D4, D8)

- [x] 9.1 **Gate OQ-1 resuelto** — el prompt de apply de este change (orquestador OPSX, sign-off PO 2026-07-31) fija: EXENTA = `3834e5d7-f3a9-4496-8fdd-84edf8a8b252` (susanacavagnola@gmail.com, "Sumar Ropa Deportiva"); PAGADORA = `0f627a85-7d01-4323-8b3f-122bd834a4ab` (danielsevilla64@gmail.com) **NO exenta, NO tocada** — conserva `billing_plan='pro'` por la rama (3) de la precedencia; su vencimiento lo maneja el futuro change de suscripciones MP. Verificado contra prod (MCP): ambas cuentas hoy `pro`/`active`, `trial_plan IS NULL` — sin overlap con las 5 cuentas de trial `avanzado` en curso.
- [x] 9.2–9.3 Migración de backfill **separada** (`20260817000002_billing_pro_trial_backfill.sql`): exención (1 cuenta, `WHERE id=... AND billing_exempt=false`) + trial PRO para el resto (`WHERE billing_exempt=false AND id<>pagadora AND trial_plan IS DISTINCT FROM 'pro'`), cada una con su `billing_events` (`exemption_granted` / `trial_pro_granted` con `from_plan`/metadata `account_id`).
- [x] 9.4 **Idempotencia verificada dentro de la propia migración** (no via `db reset` local — sin Docker en este entorno): la migración aplica cada UPDATE una 2ª vez en la misma ejecución (§3, bloque `DO $verify$`), capturando una firma (count + `string_agg(id:timestamp)`) antes y después; aborta con `RAISE EXCEPTION` si `trial_expires_at`/`billing_exempt_granted_at` se mueven o si `billing_events` gana filas en la 2ª pasada. Este mecanismo prueba la idempotencia en cada aplicación real (incluida la doble aplicación del pipeline: integración GitHub + `db push` de Actions), no solo en un ensayo local.
- [x] 9.5 Verificación en prod: **pendiente hasta después del merge** — instrucciones exactas dejadas en la cabecera de la migración (conteos esperados: 1 exenta, 32 en trial, 34 con `get_effective_plan='pro'` en ese instante). Se ejecuta en el paso final de este apply (ver resumen).
- [x] 9.6 Procedimiento de reversión documentado en la cabecera de `20260817000002` (UPDATE desde `billing_events.from_plan`/`metadata->>'account_id'`, sin backups ni DDL destructivo).

## 10. Cierre y handoff al cluster

- [x] 10.1 Suites completas ejecutadas dos veces cada una tras los cambios: backend 1031→**1041** passed, 3 skipped (+6 guards de products/clients +4 `test_plan_limits_repository.py`); frontend 599→**610** passed (+9 `plan-utils.test.ts` +2 `NotificationBell.test.tsx`). Sin flakes entre corridas, 0 fallos.
- [x] 10.2 Advisors ejecutados post-merge vía MCP `get_advisors` (2026-07-31, prod `gxdhpxvdjjkmxhdkkwyb`, con `20260817000001/02`, `20260818000001` y `20260819000001` ya aplicadas): security 162 / performance 177 hallazgos, **ninguno de nivel ERROR y un solo hallazgo NUEVO atribuible a este change**: INFO `unindexed_foreign_keys` sobre `accounts_billing_exempt_granted_by_fkey` (aceptado — `accounts` tiene 34 filas; un índice solo aceleraría DELETEs en `auth.users`). `fn_guard_supplier_plan_limit` ya NO aparece en el 0028 (remediada por PR #314 / `20260818000001`); `_sweep_plan_limit_exceeded` y `_notification_from_event` tampoco (la propia migración les revoca EXECUTE — §185/480-482/607). Los WARN 0028/0029 sobre `expire_trials`/`queue_trial_notifications`/`set_new_user_trial` son PREEXISTENTES (CREATE OR REPLACE conserva ACLs; esta migración no las cambió) y forman parte del backlog plataforma-wide (56 anon + 94 authenticated); candidatos a un follow-up de revoke (cron/trigger-only, mismo patrón que #314) — elevado como tarea aparte, fuera de este change. El resto (`rls_enabled_no_policy` en `platform_wsaa_tickets`, `auth_leaked_password_protection`, `function_search_path_mutable` ×8 —ninguna de este change—, buckets públicos, `auth_rls_initplan`, `multiple_permissive_policies` incl. `plan_limits`) es preexistente.
- [x] 10.3 Confirmado read-only contra prod (MCP `execute_sql` sobre `pg_proc`/`pg_namespace`, 2026-07-31): las 7 funciones redefinidas por `20260817000001` tienen **exactamente 1 definición cada una** (sin overloads / sin 42725), todas SECURITY DEFINER: `get_effective_plan(p_account_id uuid)`, `set_new_user_trial()`, `expire_trials()`, `queue_trial_notifications()`, `_notification_from_event(p_event events)`, `_sweep_plan_limit_exceeded()`, `fn_guard_supplier_plan_limit()`. Verificación de cabecera de la migración también OK: EXECUTE de `get_effective_plan(uuid)` = false para `anon`/`authenticated`, true para `supabase_auth_admin`/`service_role`; el cuerpo desplegado contiene el gate de exención (`pg_get_functiondef`); `plan_limits.avanzado.max_products = 2000`; `cron.job` `billing-plan-limit-exceeded-sweep` presente (1). `list_migrations` confirma `billing_pro_trial_schema` + `billing_pro_trial_backfill` aplicadas.
- [x] 10.4 Anotado para `v31-authz-token-hook`: su claim `plan` debe llamar a `get_effective_plan(account_id)`; al ser `SECURITY DEFINER`, el hook necesita únicamente `GRANT EXECUTE` sobre la función (ya concedido a `supabase_auth_admin` en esta migración) — **no** necesita `GRANT SELECT` sobre `accounts`. Queda registrado acá y en el resumen de la sesión (engram) para que el próximo apply de `v31-authz-token-hook` ajuste sus tareas 3.2/3.3 en consecuencia.
- [x] 10.5 PR abierto con tabla de evidencia TDD, números de prod de 1.3, dimensionamiento del excedente y la resolución de OQ-1 — ver descripción del PR.
- [x] 10.6 Open Questions vivas registradas para el PO: **OQ-2** (enforcement de contadores mensuales) — no implementado, D7 decide que no aplica; **OQ-3** (5 cuentas con trial `avanzado` en curso) — implementado con la lectura literal del sign-off (se unifican al trial PRO de 30 días desde la activación). Además, **hallazgo nuevo no anticipado por el diseño** (ver 1.6 y el resumen de la sesión): 3 Edge Functions leen `profiles.billing_*` para gating de IA/exportaciones — quedarán desalineadas de `get_effective_plan()` tras este change porque el backfill no toca `profiles`. Elevado al PO/orquestador, no resuelto en este change.
