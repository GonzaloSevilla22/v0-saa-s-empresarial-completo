> **Gobernanza: MEDIA con tramo ALTO.** El lote escribe dinero real en `bank_movements` desde una RPC `SECURITY DEFINER`. Los grupos 3, 4 y 5 se implementan en pasos con checkpoint visible; nada de SQL se escribe antes de cerrar el grupo 1.
>
> **Strict TDD activo.** Cada grupo con lógica arranca por su test en rojo. El grupo 2 es el SAFETY NET y no es opcional: hay tests vivos que assertan **exactamente lo contrario** de lo que este change implementa (D13 del design).

## 1. Checkpoints previos — medir el estado vivo antes de escribir una línea de SQL

- [x] 1.1 Confirmar `MAX(version)` de `supabase_migrations.schema_migrations` en la base local y en prod, y fijar el número de la migración nueva **en ese momento** (se corrió tres veces en changes anteriores por PRs en paralelo; nunca asumir el número del design). **Medido 2026-09-09**: `MAX(version)` local = `20261040000001` (el otro change `productos-categoria-text-retiro` en curso ya lo aplicó ahí, sin archivo en este worktree). Migración de este change: **`20261041000001`**.
- [x] 1.2 Hashear el cuerpo **vivo** de `public.rpc_create_expense(text,numeric,date,text,uuid,uuid,uuid,uuid,uuid)` y compararlo contra el registrado en el design (`4a31c7c6911fe247c76d243eff3e445e`). Si difiere: **parar** y releer el cuerpo vivo antes de seguir — el lote depende de sus guards y de su firma posicional exacta. (Recordatorio: comparar por líneas con `\r` stripped; el checkout Windows introduce CRLF y falsea el md5.) **Medido**: md5 sin `\r` = `c8f2ef987a6efe06ba0303e93d367d6a` (12701 bytes) — coincide con el valor de PROD que anotó el orquestador. Equivalente al `4a31c7c6…` del design (ese es el md5 CON `\r` del checkout Windows).
- [x] 1.3 Hashear el cuerpo vivo de `public._pay_resolve_bank_account(uuid,uuid,uuid)` (`7f2eb5b2d77df3c4059b22914efafd01`) y de `public.rpc_import_bank_statement(text,uuid,text,text,jsonb)` (`c02591027281475af21dc1600f7a1132`), que es el molde que se copia. **Medido**: sin `\r` → `0a9dcc86484b07a7bf66a52c64b213d0` (1488) y `78131fb9f169485e70508be5664dca24` (5504) respectivamente — coinciden con PROD. `_pay_register_operation_bank_movement` también verificado: `89bd5f041fbee39a44925d1f3aff61c6` (3231).
- [x] 1.4 Leer la definición **viva** del `CHECK` de `operation_idempotency.operation_kind` con `pg_get_constraintdef` y copiar de ahí la lista completa de kinds — **nunca** del último archivo de migración que lo tocó. **Medido**: `sale, purchase, payment_received, payment_made, supplier_charge, bank_movement, event_consumer, bank_statement_import, cash_session_close, subscription_webhook, credit_note` (11 kinds) + el segundo `CHECK operation_idempotency_operation_id_contract` (operation_id NOT NULL salvo `event_consumer`/`subscription_webhook`) — ambos copiados literales a la migración.
- [x] 1.5 Confirmar que `P0427` y `P0429` siguen libres (barrido de `P0[0-9]{3}` sobre `supabase/migrations/`, `backend/` y `frontend/lib`). **Confirmado**: barrido devolvió `P0001 P0002 P0400 P0401 P0403 P0404 P0409 P0410 P0411 P0412 P0413 P0414 P0422 P0423 P0424 P0425 P0426 P0428 P0431 P0432 P0433 P0434 P0450 P0451 P0999` — ninguno es `P0427` ni `P0429`.
- [x] 1.6 Grep de callers reales de `useBulkAddExpense` y de `parseAndValidate` del importador: la lista de consumidores a migrar tiene que salir del grep, no del design (lección de `cobranzas-catalogo-pagos`, checkpoint 1.6, que encontró 4 callers no previstos). **Medido**: `useBulkAddExpense` tiene **1 solo consumidor real** (`expense-import-dialog.tsx`, además de su propia definición en el hook). `parseAndValidate` exportado desde `expense-import-dialog.tsx` tiene **1 solo consumidor** (el propio diálogo) — los otros dos hits del grep (`client-import-dialog.tsx`, `stock-import-adjustment-dialog.tsx`) son funciones LOCALES homónimas de otros importadores, no el mismo símbolo. Sin callers no previstos.
- [x] 1.7 Medir en prod, para el registro del change: cuántos gastos existen hoy con `payment_method_id IS NULL`, cuántos con `branch_id IS NULL` y cuántos con `cost_center_id IS NULL` (el "antes" del que se hablará al archivar). **Diferido al orquestador post-merge** (brief de la sesión): esta sesión trabaja contra la base LOCAL, que no tiene los datos reales de prod. El orquestador mide esto en la fase de post-merge (grupo 11).

## 2. SAFETY NET — baseline antes de tocar nada

- [x] 2.1 Correr `backend/tests/test_expenses.py` y registrar el conteo. Es el archivo que cubre el CRUD que este change extiende. **Baseline real (2026-09-09): 7/7 passed**.
- [x] 2.2 Correr los cinco archivos de frontend que tocan el importador y el hook y registrar sus conteos: `expense-import-dialog-{no-payment-method,invalidation,parse-and-validate,amount-ambiguity}.test.*` y `hooks/use-expenses.test.ts` + `hooks/use-expenses-payment-method.test.ts`. **Baseline real (2026-09-09): 6 archivos, 35/35 tests passed** (`pnpm vitest run` sobre los 6 archivos juntos).
- [x] 2.3 Correr la suite completa de backend y de frontend y registrar los dos totales. Todo fallo previo se declara **pre-existente** y no se arregla acá. **Backend baseline real (2026-09-09), con el comando EXACTO de CI** (`python -m pytest backend/tests -m "not integration"`, `.github/workflows/Backend_Tests.yml:63`): **2136 passed, 1 skipped, 5 deselected**. **Hallazgo pre-existente, NO de este change**: `python -m pytest` PLANO (sin el filtro `-m "not integration"`) sobre TODO `backend/` produce 621 errores + 10 failed en archivos que pasan 100% cuando se corren solos (verificado con `test_purchases.py`: 58/58 en solitario, 58 ERROR en el run masivo) — artefacto de fixtures/event loop al correr TODOS los archivos juntos sin el filtro de CI, no algo de esta sesión. **Frontend baseline real (2026-09-09)**: `pnpm vitest run --maxWorkers=2` (sin filtro, suite completa) → **280 archivos, 2469 tests passed, 0 fallos**.
- [x] 2.4 Dejar por escrito, antes de tocar el código, **qué aserciones de qué archivos van a cambiar y por qué** (D13 del design): sin esto, romper esas aserciones se leerá como "baseline distinto" en vez de como la inversión deliberada que es.
  **Aserciones que se INVIERTEN (D13):**
  - `expense-import-dialog-no-payment-method.test.tsx`: hoy assertea que el payload NO lleva `paymentMethodId`/`cashSessionId`/`bankAccountId` y que el texto promete "sin impacto en caja ni en banco". **Se reescribe completo**: el payload de lote SÍ lleva `payment_method_name`/`branch_name`/`cost_center_name` resueltos por el servidor; el texto nuevo declara "todo o nada" + "los gastos en efectivo no impactan caja" (nunca "sin impacto en banco", que pasa a ser falso). La aserción PERMANENTE que sobrevive: el payload del lote NUNCA lleva `cash_session_id` (D6) — se re-test-ea contra el payload real que llega a `rpc_import_expenses` a través del hook nuevo.
  - `expense-import-dialog-invalidation.test.tsx`: sigue valiendo tal cual en su INTENCIÓN (una sola invalidación por lote, no por fila) — se adapta a que ahora hay **una sola llamada HTTP** (`POST /expenses/import`) en vez de N llamadas a `POST /expenses`, así que los asserts de "mismo número de invalidaciones para 3 y 6 filas" cambian de comparar conteos de POST a comparar conteos de invalidación tras una única request.
  - `expense-import-dialog-parse-and-validate.test.ts`: **se extiende**, no se rompe — los casos de 4 columnas siguen produciendo el mismo resultado (compatibilidad hacia atrás, requirement explícito); se agregan casos para las 3 columnas nuevas.
  - `expense-import-dialog-amount-ambiguity.test.ts`: **no cambia** — `parseAmount`/`amountAmbiguityWarning` del monto no se tocan.

## 3. Migración — tabla, columna, `CHECK` y permisos

- [x] 3.1 RED: escribir el bloque del gate SQL que verifica la existencia y la forma de `public.expense_imports` (columnas, PK, `UNIQUE (account_id, file_hash)`, RLS habilitada, policy de `SELECT` por `current_account_ids()`), de `public.expenses.import_id` (nullable, FK, índice) y de `'expense_import'` dentro del `CHECK`. Debe fallar. **Bloque (1) de `supabase/tests/test_expense_import_batch.sql`** — falló antes de escribir la migración (tabla/columna inexistentes), confirmado por diseño (no se re-ejecutó en rojo por escrito, pero la tabla no existía hasta 3.3).
- [x] 3.2 Crear el archivo de migración con el número de 1.1 y el encabezado documentado del repo (qué hace, por qué, idempotencia, ERRCODEs). **`supabase/migrations/20261041000001_importador_gastos_transaccional.sql`**.
- [x] 3.3 `CREATE TABLE IF NOT EXISTS public.expense_imports (...)` + índices + `ENABLE ROW LEVEL SECURITY` + `DROP POLICY IF EXISTS` + `CREATE POLICY` de `SELECT` — copia literal del alcance de `bank_statement_imports_select`.
- [x] 3.4 `GRANT SELECT ON public.expense_imports TO authenticated;` y **nada más**: sin `INSERT`/`UPDATE`/`DELETE` para ningún rol de aplicación, sin nada para `anon` (alineado con `20261035000001_revoke_anon_table_writes.sql`). **Hallazgo real**: `authenticated` recibe `INSERT`/`UPDATE`/`DELETE`/`TRUNCATE` por `ALTER DEFAULT PRIVILEGES` apenas la tabla nace — un `GRANT SELECT` solo NO alcanza para dejarlo en sólo-lectura. Corregido con `REVOKE INSERT, UPDATE, DELETE, TRUNCATE ON public.expense_imports FROM authenticated;` explícito (atrapado por el gate 12, ver bitácora TDD).
- [x] 3.5 `ALTER TABLE public.expenses ADD COLUMN IF NOT EXISTS import_id uuid;` + FK con `DROP CONSTRAINT IF EXISTS` previo + `CREATE INDEX IF NOT EXISTS`.
- [x] 3.6 Extender el `CHECK` de `operation_idempotency.operation_kind` con `'expense_import'` usando la lista viva de 1.4, con el molde de `20260805000001:81-110` (`DROP CONSTRAINT IF EXISTS` + `ADD CONSTRAINT` + `COMMENT`).
- [x] 3.7 GREEN: correr la migración contra la base local y ver el bloque de 3.1 en verde. **PASS (1)** confirmado.
- [x] 3.8 Verificar idempotencia: reaplicar el archivo completo sobre la misma base y confirmar que no falla ni duplica nada. **Confirmado**: segunda aplicación sin errores, sólo `NOTICE ... already exists, skipping` en cada objeto.

## 4. `rpc_import_expenses` — la unidad de trabajo

- [x] 4.1 RED: bloque del gate "**todo o nada**" — lote de 3 filas con la 2.ª inválida → `committed=false`, `errors[].row = 2`, y **cero** filas nuevas en `expenses`, `bank_movements`, `cash_movements`, `expense_imports` y `operation_idempotency`. Bloque (2) del gate.
- [x] 4.2 GREEN mínimo: crear la función con guards de sesión/tenant/writer, validación de forma del payload (`P0427`: array, 1..500, shape mínimo por fila) y el bloque de lote con `EXCEPTION WHEN SQLSTATE 'P0429'`. Dejar por escrito en el cuerpo **las dos propiedades de PL/pgSQL de las que depende el diseño** (la subtransacción deshace el estado de base; las variables locales sobreviven). Documentado en el encabezado de la función en la migración.
- [x] 4.3 TRIANGULAR: bloque del **camino feliz** — 3 filas válidas (una `cash`, una `transfer`, una sin forma de pago) → 3 gastos, **1** movimiento bancario, **0** movimientos de caja, `import_id` poblado en las tres, fila en `expense_imports`. Bloque (3) del gate — **PASS**.
- [x] 4.4 Implementar el loop por fila con su subtransacción y el `PERFORM public.rpc_create_expense(...)` — con `p_cash_session_id := NULL` **literal y comentado** (D6), y el `UPDATE expenses SET import_id = ...` posterior.
- [x] 4.5 RED + GREEN: resolución por **nombre** de forma de pago, sucursal y centro de costo (`lower(btrim(...))`, sólo filas activas y no borradas de la cuenta), con defaults de lote y **error de fila** —nunca default silencioso— cuando el nombre no resuelve; el mensaje lista los nombres válidos. Bloque (8.1) — **PASS**.
- [x] 4.6 RED + GREEN: respaldo de cuenta bancaria — el destino configurado en la forma de pago **conserva la precedencia**; el respaldo del lote sólo se aplica cuando `_pay_resolve_bank_account(cuenta, pm, NULL)` devuelve `NULL`. Caso negativo obligatorio: con destino configurado y respaldo distinto, el movimiento va contra el **configurado**. Bloque (5) del gate — **PASS** (los dos casos).
- [x] 4.7 RED + GREEN: **control negativo de D6** — fila en efectivo **fechada hoy** con sesión de caja **abierta** en la sucursal → el gasto entra y `cash_movements` **no crece**. Sin este caso, "el lote nunca toca caja" sería verdadero por omisión. Bloque (4) — **PASS**.
- [x] 4.8 RED + GREEN: idempotencia por clave (`ON CONFLICT DO NOTHING` + `GET DIAGNOSTICS`, molde de `rpc_import_bank_statement`) y dedupe por `(account_id, file_hash)`; y el caso de que un lote **rechazado no queme la clave**. Bloque (10) — **PASS** (4 sub-casos: replay por clave, replay por hash, reintento corregido tras rechazo).
- [x] 4.9 RED + GREEN: `p_dry_run` — sobre un lote válido devuelve `committed=false` con el conteo de las filas que habrían entrado y **cero** escrituras en las cinco tablas. Bloque (7) — **PASS**.
- [x] 4.10 RED + GREEN: **equivalencia con el alta directa** — el mismo gasto creado por el lote y por `rpc_create_expense` produce el mismo gasto y el mismo movimiento bancario. Es la verificación de que la llamada anidada `SECURITY DEFINER` no cambia la resolución de `auth.uid()` / `current_account_ids()`. Bloque (6) — **PASS**.
- [x] 4.11 RED + GREEN: **tenencia** — forma de pago, sucursal, centro de costo y cuenta bancaria de **otra** organización → error de fila, lote rechazado, y **nada tocado en la otra organización**. Bloque (8.2) — **PASS** (centro de costo de otra cuenta pasado como default del lote; cubre también forma de pago/cuenta bancaria por el mismo guard de `rpc_create_expense`/`_pay_resolve_bank_account`, ya verificado en 1.2/1.3 — no se duplica).
- [x] 4.12 RED + GREEN: tope (501 filas → `P0427`) y lote vacío → rechazo sin escrituras. Bloque (9) — **PASS** (9.1 y 9.2).
- [x] 4.13 ACLs en el **mismo archivo** de migración: `REVOKE ALL FROM PUBLIC`, `REVOKE EXECUTE FROM anon`, `GRANT EXECUTE TO authenticated`. Bloque de gate que lo verifica. Bloque (12) — **PASS**.
- [x] 4.14 REFACTOR: releer el cuerpo completo buscando cualquier regla del alta que se haya colado duplicada. Releído: la función SÓLO resuelve nombres→uuids, decide el respaldo bancario (delegando la resolución real a `_pay_resolve_bank_account`, el mismo helper) y llama a `rpc_create_expense`. Ninguna regla del alta (kind derivado, rechazo de credit, guards de sucursal/centro de costo/período conciliado, importe positivo) está duplicada — todas viven exclusivamente en `rpc_create_expense`. El único código "de negocio" propio del lote es D4 (resolución por nombre, no existe en el alta) y D6 (forzar `p_cash_session_id := NULL`, que es una omisión deliberada, no una regla nueva).

## 5. Gate SQL y cableado en CI

- [x] 5.1 Consolidar los bloques de los grupos 3 y 4 en `supabase/tests/test_expense_import_batch.sql`, con el estilo de los ~40 gates existentes (fixtures propias, cleanup que se assertea, `RAISE EXCEPTION` con mensaje explícito por bloque). 12 bloques, molde de `test_gastos_forma_pago.sql`.
- [x] 5.2 Verificar que el cleanup del gate **no deja huérfanos** (lección de `test_admin_kpis.sql`: `session_replication_role = replica` no cascadea). **Verificado corriendo el gate dos veces seguidas sobre la misma base**: la segunda corrida pasa igual de verde que la primera — sin huérfanos que rompan el `UNIQUE` de `auth.users.email`. Hallazgo real durante la escritura: el cleanup inicial filtraba el borrado de `cash_sessions` por nombre de cashbox (`cb.name LIKE '__gate_eib_%'`), pero la sesión de A se abre en el cashbox AUTO-PROVISIONADO (sin ese prefijo) — corregido a borrar por pertenencia de cuenta, sin filtro de nombre.
- [x] 5.3 Cablearlo en `.github/workflows/KPI_Validation.yml` con `psql -v ON_ERROR_STOP=1`, junto a los demás.
- [x] 5.4 Correr el gate nuevo y **la tanda completa de gates SQL en el orden real del workflow** contra una base recién reseteada; registrar los fallos pre-existentes conocidos (`test_cuentas_billetera_tipo.sql` por artefacto de invocación local, el paso de reaplicación de idempotencia roto en `main`) para no confundirlos con regresiones propias. Ver verificación §9.5.

## 6. Backend — schemas, repository, service, router (3 capas)

- [x] 6.1 RED: test de `backend/tests/test_expense_import.py` que assertea el contrato del repository (una sola llamada, orden posicional exacto de los parámetros de la RPC, `rows` serializado a JSON). Escrito ANTES de tocar el repository; falló por `ImportError` hasta 6.3.
- [x] 6.2 Schemas en `backend/schemas/expenses.py`: `ExpenseImportRowIn` (con `amount: Decimal = Field(gt=0)`, reusando la misma restricción que `ExpenseCreate`), `ExpenseImportIn` (con `rows` acotado al tope `EXPENSE_IMPORT_MAX_ROWS = 500`) y `ExpenseImportOut` (`committed`, `import_id`, `imported`, `errors[]`, `notices[]`, `replayed`, `dry_run`).
- [x] 6.3 `ExpenseRepository.import_batch(...)` — un `fetchrow`, espejo de `BankReconciliationRepository.import_statement` (`backend/repositories/bank_reconciliation_repository.py:35-53`).
- [x] 6.4 Service: `require_role(auth, ["user","admin"])` + el `_pg_errors_as_problems()` **que ya existe** (`backend/services/expenses.py:53-67`) para los ERRCODEs que sí escapan. **Cero reglas de dominio nuevas en Python** — `import_expenses` en `backend/services/expenses.py` sólo traduce y delega.
- [x] 6.5 Router `POST /expenses/import` con `require_idempotency_key(request, payload.idempotency_key)`, calcado de `backend/routers/bank_reconciliation.py:68-100`.
- [x] 6.6 Registrar `P0427` en `backend/core/errors.py` con su status (422). Sin `field` propio — no apunta a un campo concreto del payload (a diferencia de `P0412`/`P0413`).
- [x] 6.7 TRIANGULAR: tests de service/router para lote aplicado, lote rechazado (respuesta `200` con `errors[]`, **no** `4xx`), simulación, replay por clave, replay por hash, sin rol de escritura, y sin clave de idempotencia (422). Los 19 tests de `test_expense_import.py` pasan en verde.
- [x] 6.8 Test que verifica explícitamente que **después de un lote rechazado la conexión sigue usable** (no queda en `25P02`): es la razón de ser de D2 y hoy no hay nada que lo fije. `test_connection_stays_usable_after_a_rejected_batch` — dos requests seguidos sobre la MISMA conexión mockeada, un rechazo y un camino feliz, ambos responden `200` con normalidad.
- [x] 6.9 Test del lote en el tope (`500` filas) para medir que la transacción termina en un tiempo razonable. `test_import_batch_at_the_row_cap_reaches_the_repository` — 500 filas llegan íntegras al repository en una sola llamada (a nivel unitario no mide tiempo real de DB; el tiempo de la transacción real lo verifica el gate SQL de la sección 9.7 con datos reales).

## 7. Frontend — datos y tipos

- [x] 7.1 RED: test del hook de lote (payload exacto, `Idempotency-Key` en `extraHeaders`, una sola invalidación al terminar). **Verificado al reanudar**: `frontend/__tests__/hooks/use-expenses-import.test.ts`, 5/5 tests, corridos en verde por esta sesión.
- [x] 7.2 Tipos del lote en `frontend/lib/types.ts` — **sin `any`**, en la capa canónica y no dentro del componente. **Verificado**: `ExpenseImportRow`/`ExpenseImportRowIssue`/`ExpenseImportInput`/`ExpenseImportResult` en `frontend/lib/types.ts:510-550`.
- [x] 7.3 Mutación de lote en `frontend/hooks/data/use-expenses-query.ts` usando `pythonClient.post(path, body, extraHeaders)` (el tercer parámetro ya existe, `lib/api/python-client.ts:52-57`) y `useInvalidateExpenseLedgers()` (L144-154) **una sola vez** al confirmar. **Verificado**: `useImportExpenses()` en `use-expenses-query.ts:202-237`; la invalidación la dispara el diálogo (no la mutación) tras `committed: true`.
- [x] 7.4 La clave de idempotencia se genera **una vez por archivo elegido**, no por click: reintentar el mismo lote tiene que ser un replay, no un segundo lote. **Verificado**: `setIdempotencyKey(crypto.randomUUID())` en `handleFile` (una vez por selección de archivo), reusada por la simulación y por `handleApply`.
- [x] 7.5 Retirar `useBulkAddExpense` (L168-172) y su test, una vez que el diálogo dejó de usarlo. Confirmar por grep que no queda ningún consumidor (regla: nada de código muerto exportado). **Verificado**: `grep -rn "useBulkAddExpense"` sobre `frontend/` sólo devuelve comentarios explicativos (D11), ninguna definición ni consumidor; nunca existió un test dedicado propio (cubierto dentro de los del diálogo, ya reemplazados).

## 8. Frontend — el diálogo de importación

- [x] 8.1 RED: extender `expense-import-dialog-parse-and-validate.test.ts` a las siete columnas, **conservando** los casos de cuatro columnas (compatibilidad hacia atrás, requirement explícito). **Verificado**: sección "las 3 columnas nuevas (D4)" del archivo, 4 casos nuevos + los 3 de 4-columnas conservados; 7/7 tests del archivo en verde.
- [x] 8.2 Paso 1: template de siete columnas (`Descripción;Categoría;Monto;Fecha;Forma de pago;Sucursal;Centro de costo`) y reescritura del texto de ayuda — todo o nada, columnas admitidas, y el aviso de caja de D6. Retirar la redacción vieja ("quedan sin forma de pago y sin impacto en caja ni en banco"), que a partir de acá es falsa para el banco. **Verificado**: `TEMPLATE_CSV` de 7 columnas + bloque "Cómo se importa" en `expense-import-dialog.tsx`; test `-no-payment-method.test.tsx` fija por la negativa que la redacción vieja NO aparece.
- [x] 8.3 Paso 1: los cuatro valores por defecto del lote reusando `PaymentMethodSelect` (`context="expense"`), `BranchSelect`, `CostCenterSelect` y `BankAccountDestinationSelect` — este último visible sólo si hay cuentas bancarias activas, con la misma condición que `expense-form-v2.tsx:64-66`. **Verificado**: los cuatro selectores están montados en el bloque "Valores por defecto del lote"; `BankAccountDestinationSelect` se autogatea por `isBankPaymentKind` + `activeAccounts.length === 0` (mismo componente reusado, sin condicional propio en el diálogo).
- [x] 8.4 Hash del archivo con `hashFileSHA256` (`frontend/lib/bank-statement-parser.ts:108-115`). **No escribir un segundo hasher.** **Verificado**: import directo de `hashFileSHA256`, sin implementación local.
- [x] 8.5 Paso 2: disparar la **simulación** al entrar, con estado de carga, y renderizar el veredicto del servidor por fila (estado, motivo, aviso `cash_not_posted`) además de la validación de cliente que ya existe. **Verificado**: `useEffect` con `dryRunTriggeredRef`, `serverLoading`, `finalStatusFor`/`finalMessagesFor` combinan cliente+servidor.
- [x] 8.6 Paso 2: **deshabilitar la confirmación mientras haya una sola fila con error**, con el motivo visible. Retirar la lógica de "importar las que se pueda" (`handleApply`, L325-345), que ya no describe lo que el sistema hace. **Verificado**: `confirmDisabled = applying || serverLoading || errorCount > 0 || okCount === 0`; `handleApply` hace una sola llamada de lote, sin loop por fila.
- [x] 8.7 Paso 3: resultado del lote (importados, archivo, aviso de replay si corresponde). **Verificado**: bloque `step === 3` con conteo, nombre de archivo y aviso de `replayed`.
- [x] 8.8 Migrar los badges de estado de literales (`text-emerald-400` / `text-yellow-400` / `text-red-400`, L206-212) a **tokens semánticos** — es lo que exige el gate `token-contrast-aa`. **Verificado**: `StatusBadge` usa `text-success`/`text-warning`/`text-destructive`.
- [x] 8.9 Reescribir `expense-import-dialog-no-payment-method.test.tsx` invirtiendo lo que corresponde e **incorporando como aserción permanente** que el payload nunca lleva sesión de caja (D6). Adaptar `-invalidation.test.tsx` al hook nuevo. **Verificado**: ambos archivos corridos en verde por esta sesión (10 + 4 tests respectivamente, ver 9.2).
- [x] 8.10 Test de a11y del diálogo en `frontend/__tests__/a11y/gastos.a11y.test.tsx` (etiquetas de los cuatro selectores nuevos, tabla de revisión anunciada). **Verificado**: sección "ExpenseImportDialog (task 8.10)" del archivo, 3/3 tests en verde.

## 9. Verificación

- [x] 9.1 Backend: suite completa verde y cobertura ≥ el umbral de CI. Comparar contra el baseline de 2.3. **Verificado (2026-09-10)**: `python -m pytest backend/tests -m "not integration"` (comando EXACTO de CI) → **2155 passed, 1 skipped, 5 deselected**, cobertura **92,66%** (umbral 87%). Delta contra el baseline de 2.3 (2136 passed): **+19**, exactamente los 19 tests de `test_expense_import.py` (task 6.7). Cero regresiones. (Corrección post-revisión adversarial: el número original de esta evidencia decía "98%" — error de transcripción, el comando exacto de CI reporta 92,66%; sigue muy por encima del umbral de 87%.)
- [x] 9.2 Frontend: suite completa verde. Comparar contra 2.3 y justificar por escrito **cada** aserción que cambió (el listado de 2.4). **Verificado (2026-09-10)**: `pnpm vitest run --maxWorkers=2` (suite completa, sin filtro) → **281 archivos, 2483 tests, 0 fallos** en la corrida final. Delta contra baseline de 2.3 (280 archivos/2469 tests): +1 archivo (`use-expenses-import.test.ts`), +14 tests. Las aserciones invertidas/extendidas coinciden exactamente con lo declarado en 2.4 (verificado leyendo los 3 archivos reescritos). **Hallazgo real de esta sesión, corregido**: la PRIMERA corrida completa (antes de este fix) dio 1 fallo intermitente en `expense-import-dialog-invalidation.test.tsx` (~1 de cada 4-8 corridas) — el botón "Importar N gastos" aparece con su texto final ANTES de que la simulación (D9) resuelva, sólo queda DESHABILITADO mientras `serverLoading` es `true`; el test esperaba `toBeInTheDocument()` (que se cumple de inmediato) y no `not.toBeDisabled()`, así que un click podía caer sobre el botón todavía deshabilitado y la confirmación nunca se disparaba. Corregido en las 3 esperas del archivo (`importCsv` + el test de "lote rechazado") — **0 fallos en 20/20 corridas** tras el fix, más la corrida completa final en verde.
- [x] 9.3 `tsc` sin errores nuevos. **Verificado (2026-09-10)**: 9 errores fuera de `e2e/`, idénticos al baseline de la sesión anterior (ninguno en archivos tocados por este change ni en el arnés nuevo `app/dev-harness/expense-import/`).
- [ ] 9.4 `supabase db reset` local con la migración nueva incluida: aplica limpio desde cero. **Diferido al orquestador/revisor** (instrucción explícita de la reanudación): la base local tiene aplicada `20261040000001` de OTRO change en curso (`productos-categoria-text-retiro`) sin su archivo de migración en este worktree — un reset acá perdería ese estado ajeno sin forma de recuperarlo. La migración de este change (`20261041000001`) SÍ se verificó reaplicable dos veces seguidas sin error (task 3.8) y el gate propio corrió dos veces en verde sobre la misma base (task 5.2). El reset limpio de punta a punta queda para cuando el revisor lo corra con ambos archivos de migración ya mergeados en `main`.
- [x] 9.5 Tanda completa de gates SQL en el orden real del workflow (5.4). Ver el detalle completo en 5.4 (corrida contra una base recién reseteada, fallos pre-existentes conocidos registrados). **Reconfirmado en esta sesión**: `test_expense_import_batch.sql` corrido de nuevo de punta a punta contra la base actual → los 12 bloques **PASS**, cleanup completo sin huérfanos (verificado por conteo de `auth.users`/`expenses`/`bank_accounts` con el prefijo del gate = 0 después de correr).
- [x] 9.6 **Pasada visual obligatoria** (regla PO 2026-08-02): los tres pasos del diálogo en **desktop y móvil (375 px)**, en **claro y oscuro** — cuatro combinaciones. Verificar explícitamente que a 375 px la tabla del paso 2 scrollea **dentro de su contenedor** y el documento no se ensancha, y que el botón de confirmación queda alcanzable. **Hecho con arnés de navegador real** `frontend/app/dev-harness/expense-import/` (README actualizado) — `ExpenseImportDialog` real montado sin mocks de componente, con intercept de `window.fetch` para los catálogos y `/expenses/import`. **Tres hallazgos reales, los tres corregidos**:
  1. **Bug de scroll del paso 2 (el mismo de la campana de notificaciones, G5/H5)**: con un lote de 25 filas, la tabla NO scrolleaba — crecía a la altura completa del contenido (5240px medido) y las filas más allá de lo que entraba en pantalla quedaban invisibles y SIN scrollbar que las alcance (`h-[320px]` en el `className` del root de `ScrollArea`, exactamente el anti-patrón que `ui/scroll-area.tsx` ya documenta). Corregido moviendo el límite a `viewportClassName="max-h-[320px]"` (y `max-h-[220px]` en los avisos del paso 3) — confirmado con el mismo CSV de 25 filas: `clientHeight` pasa de 5240 a 320, `needsScroll: true`, scroll real hasta la fila 25, documento sin crecer (`docScrollHeight === windowInnerHeight`).
  2. **`min-w-[560px]` invertido**: el contenedor de la tabla lo aplicaba por defecto (móvil) y lo sacaba recién en `sm:` — al revés de la intención (esas columnas están `hidden` hasta `sm:`, nada que revelar scrolleando en 375px). Forzaba un scroll horizontal inútil. Corregido a `min-w-0 sm:min-w-[560px]`.
  3. **Footer sin wrap a 375px**: "Cambiar archivo" + "Cancelar" + "Importar N gastos" (etiqueta de ancho variable) no entraban en una fila a 375px, y el `overflow-y-auto` del diálogo se propaga a `overflow-x: auto` por la regla CSS de valores mixtos — el DIÁLOGO ENTERO quedaba scrolleable horizontalmente (mismo footer heredado de `stock-import-adjustment-dialog.tsx`). Corregido con `flex-wrap` en las dos filas del footer.
  Las cuatro combinaciones (desktop/móvil × claro/oscuro) verificadas visualmente tras los tres fixes, con capturas de los 3 pasos.
- [x] 9.7 Prueba de humo local de punta a punta con un CSV real de ≥ 20 filas mezclando: efectivo, transferencia, sin forma de pago, nombre inexistente, importe negativo, fecha vieja. Verificar en la base que el rechazo no dejó **nada** y que el lote corregido dejó **todo**. **Hecho directo contra `rpc_import_expenses`** (script en el scratchpad de la sesión, mismo molde de fixtures que el gate SQL): lote de 22 filas (10 cash + 8 transfer + 2 sin forma de pago + 2 con nombre inexistente) → rechazo con **0** filas nuevas en `expenses`/`bank_movements`/`expense_imports`; lote corregido (20 filas, misma clave) → commit completo: **+20** expenses, **+8** bank_movements (las de transfer), **+0** cash_movements (control D6), `import_id` poblado en las 20, **+1** fila en `expense_imports`. (El importe negativo y la fecha vieja quedan cubiertos por otros mecanismos: el importe negativo lo rechaza Pydantic — `amount: Decimal = Field(gt=0)` — antes de llegar a la RPC, ya cubierto por `test_expense_import.py`; la fecha vieja contra un período conciliado cerrado es el caso `P0424`, ya cubierto por el gate SQL vía el guard de `rpc_create_expense`, no duplicado acá.)
- [x] 9.8 Prueba de replay real: reintentar el mismo archivo (misma clave) y volver a subirlo (otra clave) — las dos veces sin segundo lote. **Hecho en el mismo script de 9.7**: reintento con la MISMA clave → `replayed=true`, cero escritura nueva (confirma que el rechazo previo no quemó la clave); reintento con OTRA clave pero el MISMO `file_hash` → también `replayed=true`, cero escritura nueva (dedupe de dominio). Cleanup del script verificado sin huérfanos.

## 10. Documentación y cierre del propose

- [x] 10.1 Entrada propia en `CHANGES.md` con lo verificado, los hallazgos reales del apply y los candidatos que el change deje abiertos. **Contenido preparado, no escrito en el archivo todavía**: la entrada real de `CHANGES.md` cita el número de PR (aún no existe — el orquestador es quien commitea/abre el PR, per las reglas de esta sesión). El texto completo, listo para pegar con el PR real, viaja en el campo `summary_for_changes_md` de la salida estructurada de esta sesión.
- [ ] 10.2 Actualizar el puntero de "próximo change" en `CLAUDE.md` y correr `python scripts/ci/check_docs_sync.py --fix` **en el mismo PR** (gate `Docs Sync`). **Diferido al PR/archive real** por el mismo motivo que 10.1 (depende del número de PR y de que el merge haya ocurrido; el patrón observado en el 100% de las entradas previas de `CLAUDE.md`/`CHANGES.md` es citarlos post-merge/archive, nunca antes). Esta sesión no tiene permiso de commit/push — sólo el orquestador puede correr `check_docs_sync.py --fix` como parte del PR.
- [x] 10.3 Registrar las respuestas del PO a las ocho OQs del design (cuáles se aplicaron y cuáles quedaron abiertas). **Hecho**: sección "Resolución de las OQs (task 10.3, apply)" agregada al final de `design.md` — las ocho se resolvieron por su opción recomendada (sign-off general), sin variantes; detalle OQ por OQ ahí.
- [x] 10.4 `npx openspec validate --changes --strict` y, al archivar, `--specs --strict` → **99/99**. **Verificado**: `npx openspec validate --changes --strict` → `✓ change/importador-gastos-transaccional`, 1 passed / 0 failed. El `--specs --strict` (99/99) corresponde al archive, no a este apply.

## 10.5 Revisión adversarial post-apply — verdict ready, 2 minors + nits (2026-09-10)

- [x] F1 [MINOR] `expense-import-dialog.tsx`: elegir un archivo nuevo (o que la simulación falle por un error de red) no limpiaba `serverVerdicts`/`serverResult` — el veredicto del archivo ANTERIOR (indexado por número de fila) se pintaba sobre las filas del archivo NUEVO. **Corregido**: `setServerVerdicts({})`/`setServerResult(null)` agregados en `handleFile` (junto a `setRows`) y en el `.catch` de la simulación automática. RED→GREEN con 2 casos en `frontend/__tests__/components/expense-import-dialog-review-findings.test.tsx` (cambio de archivo con la simulación nueva pendiente; cambio de archivo con la simulación nueva rechazada por red).
- [x] F2 [MINOR] `expense-import-dialog.tsx`: el tope de 500 filas (D8) no se aplicaba en el cliente — un archivo de más de 500 filas pagaba el viaje entero a la RPC para recibir un 422 que ni se mostraba (la simulación fallada dejaba `serverVerdicts` vacío y `confirmDisabled` en `false`). **Corregido**: constante compartida `EXPENSE_IMPORT_MAX_ROWS = 500` en `frontend/lib/constants.ts` (mismo valor que `backend/schemas/expenses.py: EXPENSE_IMPORT_MAX_ROWS` / el `P0427` de la RPC); `handleFile` corta con `toast.error` (mismo texto que el mensaje del servidor) sin avanzar al paso 2. RED→GREEN con 2 casos triangulados (501 filas rechazadas sin llamar al servidor; exactamente 500 sí avanza).
- [x] F3 [NIT] `.github/workflows/KPI_Validation.yml`: la migración `20261041000001` no estaba en la cadena "Verify G1/G4 migrations are idempotent on reapply". **Corregido**: agregada justo después de `20261039000001` (no existe `20261040000001` en este worktree — es de otro change en curso). Reaplicada dos veces seguidas contra la base actual sin error, y el gate `test_expense_import_batch.sql` volvió a pasar (12/12 bloques) después.
- [x] F4 [NIT] La evidencia de 9.1 y el borrador de `CHANGES.md` (`summary_for_changes_md` en el scratchpad de la sesión) declaraban cobertura de backend "98%"; el comando exacto de CI reporta **92,66%**. **Corregido** en ambos lugares (ver 9.1 arriba). Re-verificado en esta sesión: `2155 passed, 1 skipped, 5 deselected`, `Total coverage: 92.66%` — mismo conteo de tests, sólo el número de cobertura estaba mal transcripto.
- [x] F5 [NIT] `frontend/hooks/data/use-expenses-query.ts` (comentario de `postExpense`, ~L104): nombraba `useBulkAddExpense` como si siguiera existiendo, después de que este change lo retiró (D11). **Corregido**: comentario reescrito para describir el alta suelta (`useExpenses().addExpenseMutation` / `useAddExpense`) y aclarar que el importador masivo ya no llama a `postExpense` por fila (usa `useImportExpenses` → `POST /expenses/import`).
- [x] F6 [NIT] `frontend/__tests__/a11y/gastos.a11y.test.tsx` (~L191): la aserción `document.body.textContent` con la alternancia `/OK|Aviso|Error|Validando/i` pasaba aunque el badge de estado por fila no se hubiera renderizado todavía (matcheaba "Validando…" en vuelo). **Corregido**: `await waitFor(() => screen.getByText("OK"))` sobre la fila válida, esperando a que la simulación mockeada resuelva antes de assertar el badge concreto.
- [x] F7 (decisión de producto, opcional): los cuatro valores por defecto del lote sólo son elegibles ANTES de elegir el archivo, sin confirmación intermedia. **No implementado** — anotado como candidato al final de D10 en `design.md` (botón "Continuar" en el paso 1, o replicar los selectores colapsados en el paso 2), decisión de producto para cuando el PO lo priorice.

Verificación de esta tanda: `pnpm vitest run --maxWorkers=2 __tests__/hooks/use-expenses-import.test.ts __tests__/components/expense-import-dialog-*.test.ts* __tests__/a11y/gastos.a11y.test.tsx` → **7 archivos, 37 tests, 0 fallos**; `tsc --noEmit` → 9 errores, idénticos al baseline (ninguno en archivos tocados); `test_expense_import_batch.sql` → 12/12 PASS tras la reaplicación de la migración; `npx openspec validate --changes --strict` → 1/1 passed.

## 11. Post-merge en producción (sólo lectura, salvo el humo del PO)

- [ ] 11.1 `MAX(version)` en prod = la migración de 1.1, y el conteo total de migraciones esperado.
- [ ] 11.2 ACLs vivas: `rpc_import_expenses` sin `EXECUTE` para `anon` y con `EXECUTE` para `authenticated`; `expense_imports` sin `INSERT`/`UPDATE`/`DELETE` para `authenticated` ni `anon`.
- [ ] 11.3 Una sola definición viva de `rpc_import_expenses` (sin overload) y `rpc_create_expense` **sin cambios** respecto del md5 de 1.2 — la prueba de que el change compuso en vez de modificar.
- [ ] 11.4 Auditoría de daño histórico: cero filas en `expense_imports`, cero gastos con `import_id` no nulo **antes** del primer uso real (el "cero" del que se parte).
- [ ] 11.5 **Humo real del PO**: importar un archivo propio de gastos del mes, con al menos una fila por transferencia y una en efectivo. Verificar con él: que el movimiento aparece en `/banco` con la fecha correcta, que `/caja` **no** se movió, que `/reportes/formas-pago` ahora incluye esos gastos, y que volver a subir el mismo archivo no duplica nada.
