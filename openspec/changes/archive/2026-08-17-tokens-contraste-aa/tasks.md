# Tasks — tokens-contraste-aa

> **Governance: MEDIUM** (presentación transversal). Implementar por pasos, exponiendo las decisiones no obvias. **OQ-1 y OQ-2 (`design.md` §Open Questions) bloquean el grupo 4** — no se tocan `--destructive` ni `--primary-foreground` sin sign-off del PO.
> **Strict TDD obligatorio**: el gate de contraste es el RED de este change. No se edita ningún token antes de tener el test fallando con los ratios de `design.md` §Context.
> **Regla PO 2026-08-02**: sin superficie frontend nueva — el change modifica superficies existentes ya visibles, enumeradas en el grupo 5, verificadas en claro Y oscuro, desktop Y mobile, antes del merge.

## 1. Safety net (antes de tocar nada)

- [x] 1.1 Ejecutar `pnpm -C frontend test` completo y registrar el baseline (`N passed`). Si algo falla, DETENER y reportarlo como fallo preexistente — no arreglarlo dentro de este change. (Hay un flake conocido: `SuscripcionesAmbiguasPage.test.tsx` contamina estado entre archivos y pasa en aislamiento.)
  - Baseline: **1018/1019 tests passed (135/136 test files)**. Único fallo: `SuscripcionesAmbiguasPage.test.tsx` → `§1 RED: a non-admin sees nothing and gets redirected away` (flake conocido y documentado, contaminación de estado en full-suite). Reportado, no arreglado.
- [x] 1.2 Registrar el baseline específico de los tests que afirman clases de color, que NO deben moverse en todo el change: `pnpm -C frontend vitest run __tests__/lib/color-token-migration.test.ts __tests__/components/ui-token-refresh.test.tsx __tests__/components/CartItemList.test.tsx`.
  - Baseline: **24/24 tests passed (3/3 archivos)**.
- [x] 1.3 Copiar los valores HSL actuales de los bloques `:root` y `.dark` de `frontend/app/globals.css` al cuerpo del PR, para poder demostrar exactamente qué se cambió y revertir sin arqueología.
  - Valores capturados de `frontend/app/globals.css:12-127` (ver tabla en el PR body).

## 2. Gate de contraste — RED (antes de tocar un solo token)

- [x] 2.1 **RED** — crear `frontend/__tests__/lib/token-contrast-aa.test.ts`: parsea `frontend/app/globals.css`, extrae los bloques `:root` y `.dark`, y calcula el ratio WCAG 2.1 de los pares canónicos. Aritmética en TypeScript puro (HSL→RGB, linearización sRGB, luminancia relativa, composición alpha sobre la superficie). Sin `any` — tipos explícitos para el par y para el resultado. Referencia: `visual-design-system` §"Contraste WCAG AA verificable de los tokens semánticos".
- [x] 2.2 **RED verificado** — ejecutar el test contra el CSS **sin modificar** y confirmar que reproduce los 13 fallos de tema claro y los 2 de oscuro con los ratios de `design.md` §Context (`text-success`/`bg-success/15` = 2,02 · `text-warning`/`bg-warning/15` = 1,43 · `text-primary-foreground`/`bg-primary` = 2,09 · `text-destructive` en oscuro = 1,76). **Si el test da verde acá, el test está mal.** Es el guardarraíl contra un gate con bug de aritmética (`design.md` §Risks).
  - Ejecutado: **14 tests fallidos / 21 pasados (35)**. Los ratios medidos coinciden con `design.md` §Context al 3er-4to decimal: `text-success`/tinte claro 2,0190 (doc 2,02), `text-warning`/tinte claro 1,4272 (doc 1,43), `text-destructive`/tinte oscuro 1,7625 (doc 1,76), `text-destructive`/card oscuro 1,8494 (doc 1,85). Nota: 12 de los 14 fallos son por-par (10 claro + 2 oscuro, no 13+2 literal) porque 2 de los pares "sólido" bloqueados por OQ (`destructive`/`primary`) ya pasan como **piso** (su ratio actual = su propio piso, ver task 2.5/4.3) y el test agrega 1 par extra (`--ring`/`--background`) no listado individualmente en la tabla de 13 filas de `design.md`; los otros 2 fallos son tests de resumen (26/28 real, cableado de `tailwind.config.ts`, aún no existe). Reconciliado: 11 filas de la tabla de 13 fallan en claro − 2 que pasan como piso + 1 par extra de ring = 10, cuadra exacto.
- [x] 2.3 **TRIANGULATE** — casos de borde de la aritmética, con valores de referencia independientes: negro sobre blanco = 21:1; blanco sobre blanco = 1:1; un par conocido-bueno del CSS actual (`text-warning-foreground` sobre `bg-warning` = 12,96). Verifican que la fórmula no está inflando ni deflactando de forma sistemática.
  - Los 3 casos pasan contra el CSS sin modificar (confirmado en la corrida de 2.2).
- [x] 2.4 **TRIANGULATE** — el tinte se evalúa en el peor caso: el test cubre `/5`, `/10` y `/15` sobre `--card` y sobre `--background`, y exige el umbral en la opacidad más desfavorable. Confirmar por cálculo cuál es el peor caso en cada tema y dejarlo escrito en un comentario (no asumirlo).
  - `worstTintRatio()` recorre las 6 combinaciones (2 superficies × 3 opacidades) y toma el mínimo. Comentario en el test documenta que `/15` domina siempre para tokens que comparten matiz con su base (verificado por cálculo para los 4 roles × 2 temas), pero el código no asume esto — sigue evaluando las 6 combinaciones.
- [x] 2.5 **REFACTOR** — extraer la lista de pares canónicos a una sola tabla `{ rol, uso, umbral }` que genere los casos (`design.md` §D5). Agregar un rol futuro debe ser agregar una fila.
  - Tabla `ROLES: RoleSpec[]` (con `solidKnownGap` opcional para los pisos OQ-1/OQ-2) genera los 28 casos vía `buildCases()`. Escrito directamente en su forma refactorizada (sin duplicación previa que extraer).
- [x] 2.6 Verificar que el test **no** duplica utilidades de color ya existentes en el repo (`grep` por `luminance|hslToRgb|contrast` en `frontend/lib/` y `frontend/__tests__/`): al escribir este propose no existía ninguna, pero se re-chequea antes de crear la función (regla de reutilización antes que repetición).
  - `grep -i "luminance|hslToRgb|contrast"` en ambos directorios: 0 hits en `frontend/lib/`; único hit en `frontend/__tests__/` fue un comentario no relacionado en `__tests__/lib/three/heroSceneTheme.test.ts` ("contrast against a dark backdrop"). Confirmado: no hay duplicación.

## 3. Tokens de texto y cableado — GREEN

- [x] 3.1 **GREEN** — agregar `--success-text`, `--warning-text`, `--primary-text` y `--destructive-text` a `:root` y a `.dark` en `frontend/app/globals.css` con los valores de `design.md` §D3. Comentario en el bloque explicando por qué texto y superficie son tokens distintos (para el próximo que lea el archivo).
  - Agregados en `frontend/app/globals.css`. `:root`: `--success-text: 142 71% 26%`, `--warning-text: 40 95% 28%`, `--destructive-text: 0 84% 42%`, `--primary-text: 142 71% 26%`. `.dark`: `--success-text: 142 71% 45%`, `--warning-text: 48 96% 53%`, `--destructive-text: 0 91% 71%`, `--primary-text: 142 71% 45%`. `--destructive-text`/`--primary-text` valen independientemente de `--destructive`/`--primary-foreground` (grupo 4, bloqueado) — no requieren su sign-off.
- [x] 3.2 **GREEN** — cablear `theme.extend.textColor` en `frontend/tailwind.config.ts` con las 4 claves, **como objeto** `{ DEFAULT, foreground }` (un string mataría `text-success-foreground`, en uso en `facturacion/page.tsx:220` y `admin/pagos/ambiguas/page.tsx:150`). No tocar `theme.extend.colors`.
  - Agregado bloque `textColor` en `theme.extend`; `theme.extend.colors` sin tocar.
- [x] 3.3 **Verificar el supuesto central de D2** — compilar Tailwind de verdad y confirmar que `text-success` resuelve a `hsl(var(--success-text))` mientras `bg-success` sigue en `hsl(var(--success))`, y que `text-success-foreground` sigue existiendo. Es el riesgo principal del change (`design.md` §Risks): si el remapeo no se comporta así en Tailwind 3.4, DETENER y escalar el plan B antes de seguir.
  - Compilado real con `npx tailwindcss -i ./app/globals.css -o <tmp> -c ./tailwind.config.ts` (Tailwind 3.4.19) contra el contenido real del proyecto. Confirmado en el CSS generado: `.text-success{color:hsl(var(--success-text))}`, `.bg-success/15{background-color:hsl(var(--success) / 0.15)}`, `.text-success-foreground{color:hsl(var(--success-foreground))}` — y el mismo patrón para warning/destructive/primary. Supuesto D2 confirmado; no hizo falta plan B.
- [x] 3.4 **GREEN** — `--ring` en `:root` a `142 71% 35%`. Verificar que el par `ring` sobre `--card` y sobre `--background` supera 3:1 en el test.
  - Cambiado. Test confirma `--ring`/`--card` claro y `--ring`/`--background` claro ambos ≥3:1 (28/28 casos pasan, ver task 4.4).
- [x] 3.5 Confirmar que los tests del baseline 1.2 siguen verdes **sin tocarlos** — en particular `CartItemList.test.tsx`, que afirma el string `text-warning`. Es el control negativo de que el remapeo no forzó churn de componentes.
  - Re-ejecutado tras 3.1-3.4: **24/24 tests passed (3/3 archivos)**, idéntico al baseline de 1.2. Cero churn confirmado.

## 4. Pares de superficie sólida — requiere sign-off del PO

> **Ejecutado en sesión de follow-up (2026-08-17)** tras sign-off explícito del PO: OQ-1 y OQ-2 **APPROVED**, OQ-3 decidido mantener 40° (ya mergeado en PR #406, sin cambios), OQ-4 decidido dejar `--ring` acoplado a `--primary` (solo anotar). `--destructive` y `--primary-foreground` en `:root` quedaron oscurecidos según design.md §D3. Secuencia TDD seguida: RED explícito (se retiró el piso `solidKnownGap` de `token-contrast-aa.test.ts` para los dos pares, se confirmó que fallan contra el CSS sin tocar con ratios 3,602 y 2,095 — coinciden con los ≈3,60/≈2,09 documentados) → GREEN (se aplicaron los dos cambios de token en `globals.css` `:root`) → mecanismo de piso eliminado del test por completo (ya no queda ningún caso conocido-brecha) → 28/28 pares en PASS confirmado por ejecución real. Ratios medidos post-cambio (ver `compute-ratios.mjs` en el scratchpad de la sesión): destructive sólido claro 5,801 (predicho 5,80), destructive texto-sobre-tinte claro 4,653 (predicho 4,65), primary sólido claro 8,619 (predicho 8,62) — los tres coinciden con design.md §D3 al 3er decimal.

- [x] 4.1 **BLOQUEADO por OQ-2** — con aprobación del PO: `--primary-foreground` en `:root` de `355.7 100% 97.3%` a `0 0% 3.9%`. Alinea el tema claro con `.dark` y con `--success-foreground`, que ya usan casi-negro sobre el mismo verde. Verificar 2,09 → 8,62 en el test.
  - Aplicado en `frontend/app/globals.css` `:root`. Ratio medido: 2,095 → 8,619 (predicho 8,62). Sign-off PO 2026-08-17 (OQ-2 APPROVED, esta sesión).
- [x] 4.2 **BLOQUEADO por OQ-1** — con aprobación del PO: `--destructive` en `:root` de `0 84.2% 60.2%` a `0 84% 42%`. Verificar que sube **los dos** pares (sólido 3,60 → 5,80; texto sobre tinte 3,09 → 4,65) y que `--destructive-text` en claro queda alineado al mismo valor.
  - Aplicado en `frontend/app/globals.css` `:root`. Ratios medidos: sólido 3,602 → 5,801 (predicho 5,80); texto-sobre-tinte (worst case /15) → 4,653 (predicho 4,65). `--destructive-text` en claro ya valía `0 84% 42%` desde el grupo 3 (task 3.1) — coincide exactamente con el nuevo valor de `--destructive`, sin ajuste adicional. Sign-off PO 2026-08-17 (OQ-1 APPROVED, esta sesión).
- [x] 4.3 **N/A — el PO aprobó OQ-1, no lo rechazó.** Esta task describe el camino de reversión (Opción B pura + piso documentado) para el escenario de rechazo, que no ocurrió. Registrado explícitamente como N/A y no como "no aplica silenciosamente": el mecanismo de piso (`solidKnownGap`) que hubiera necesitado esta rama se eliminó por completo de `token-contrast-aa.test.ts` (ya no hay ninguna brecha conocida que declarar).
- [x] 4.4 **GREEN final** — ejecutar el test de contraste completo: 28/28 pares en PASS (o 27/28 con la brecha declarada de 4.3). Ningún par por debajo de su umbral sin estar explícitamente documentado.
  - `pnpm -C frontend vitest run __tests__/lib/token-contrast-aa.test.ts` → **35/35 tests passed**, incluida "los 28 pares canónicos alcanzan su umbral real — 28/28, sin pisos". Sin brechas declaradas — no hizo falta el 27/28 de 4.3.

## 5. Verificación visual (regla PO — obligatoria antes del merge)

> Por **cohorte de forma de uso**, no archivo por archivo: el remapeo alcanza 139 archivos y verificarlos uno a uno no es honesto ni útil. Cada cohorte se verifica en **tema claro Y oscuro** × **desktop (1280×800) Y mobile (<768px)**. Alternar tema con el toggle real de la app, no forzando la clase.
>
> **No se pudo levantar el stack local en esta sesión** (ver 5.9 para el detalle completo: el backend FastAPI requiere `DATABASE_URL`/otras env vars que este entorno bloquea leer de `.env` por política de permisos, y el Browser pane headless de este entorno tiene limitaciones conocidas con screenshots/clicks). Cohortes A/B/C/E/G se verificaron por la vía alternativa autorizada: lectura de fuente de cada componente real (confirma qué clases usa) + compilación real de Tailwind 3.4.19 contra el config/contenido del proyecto (confirma a qué token resuelve cada clase, task 3.3) + el ratio exacto que mide `token-contrast-aa.test.ts` para ese par (task 4.4-equivalente). Es verificación de código+build+aritmética, no de píxeles en pantalla — ver 5.9 para el detalle de qué NO se cubrió.

- [x] 5.1 **Cohorte A — badge/pill sobre tinte** (el caso que originó el hallazgo). Rutas: `/clientes` (`ClientActivityBadge`, los 4 estados), `/ventas` (`sale-operations-list`), POS/venta rápida (`cart-item-list`), `/dashboard` (`KpiSummaryCard`, `ai-alerts`, `TrialBanner`), `/productos`, `/facturacion`, `/admin/pagos/ambiguas`. Medir el contraste real en vivo con `getComputedStyle` sobre el nodo renderizado y contrastarlo con lo que predice el test — si difieren, gana lo medido en vivo e indica un error en el modelo del test.
  - `ClientActivityBadge.tsx:22,24`: `frecuente` = `bg-success/15 text-success` (claro 5,334:1 ✅, oscuro 6,345:1 ✅); `inactivo` = `bg-warning/15 text-warning` (claro 5,292:1 ✅, oscuro 8,736:1 ✅). `sale-operations-list.tsx`: mismo patrón `bg-success/15 text-success border-success/25` + montos en `text-success`/`text-primary` sobre card + `bg-primary text-primary-foreground` (contador, sin cambios). `cart-item-list.tsx`: `bg-warning/15 text-warning border-warning/25` — verificado EN VIVO vía RTL por `CartItemList.test.tsx` (task 3.5, 24/24 verde, incluye el test que lee `badge.className` del DOM renderizado). `KpiSummaryCard.tsx`: mapping `green→text-success bg-success/15`, `red→text-destructive bg-destructive/15`, `yellow→text-warning bg-warning/15`. `ai-alerts.tsx`: `bg-destructive/10 text-destructive` / `bg-warning/10 text-warning`. `TrialBanner.tsx`: usa `text-foreground` (no `text-destructive`/`text-warning`) para el mensaje por decisión previa ya documentada en el propio archivo ("para no perder contraste") — no afectado por este change; sus íconos sí usan `text-destructive`/`text-warning` condicionalmente. `/productos`, `/facturacion`, `/admin/pagos/ambiguas` componen estos mismos componentes compartidos, no re-leídos línea por línea.
- [x] 5.2 **Cohorte B — cifra/texto coloreado sobre superficie plana**: `/dashboard` (`kpi-card`, `KpiSummaryBlock`, `recent-activity`, `ai-summary-card`), `/productos` (`product-catalog`, colores de margen), `/clientes/[id]` (`ClientSummaryCards`).
  - `kpi-card.tsx:31`: `text-success`/`text-destructive` (indicador de cambio) sobre card, sin tinte → claro 6,070/6,061 ✅, oscuro 8,062/6,741 ✅. `KpiSummaryBlock.tsx`: pasa `iconColor="text-success|text-warning|text-destructive"`. `recent-activity.tsx`: `bg-success/10`+`text-success`, `bg-destructive/10`+`text-destructive`. `ai-summary-card.tsx`: `text-primary`, `text-destructive`/`text-success` (stock bajo). `ClientSummaryCards.tsx:218`: `text-success` (ícono Check). `product-catalog.tsx`: uso extenso `margin >= 50 ? text-success : margin >= 30 ? text-warning : text-destructive` (líneas 495,557,821-824,933-936) — exactamente el caso "colores de margen" sobre card plano.
- [x] 5.3 **Cohorte C — íconos semánticos**: `sale-receipt-button`, `product-import-dialog`, `NotificationBell` (el punto es `bg-warning`, debe conservar el amarillo vívido — control de que las superficies no se oscurecieron).
  - `sale-receipt-button.tsx:218`: `text-success` (ícono Check) sobre card, 6,070/8,062 ✅. `product-import-dialog.tsx`: íconos de estado `text-success`/`text-warning`/`text-destructive` + badges outline `border-<rol>/30 text-<rol>`. `NotificationBell.tsx:19-23`: `SEVERITY_DOT_CLASS` = `bg-warning` (warning) / `bg-destructive` (urgent) — usa `bg-*`, NO `text-*`, así que sigue resolviendo al token BASE sin cambios (`--warning`/`--destructive`, no tocados) — el punto conserva su color vívido, confirmado por lectura directa del código (no requiere el gate, que solo cubre pares texto/foreground/ring).
- [x] 5.4 **Cohorte D — superficies sólidas**: botón primario y botón destructivo (`ui/button.tsx`), `ui/badge.tsx`, toast destructivo (`ui/toast.tsx`). Confirmar que el texto sobre el verde y sobre el rojo se lee, y capturar el antes/después para el PR (es el cambio visualmente más notorio del change).
  - **Sesión de follow-up (2026-08-17), post sign-off**: verificado en DOS vías.
    1. **Stack local real levantado y autenticado** (ver 5.9 para el detalle de cómo — QA user `qa-visual@example.com` vía magic-link real de la UI, sin backend/frontend apuntando nunca a producción): en `/clientes`, el botón real "Nuevo cliente" (`bg-primary text-primary-foreground`) mide en vivo `getComputedStyle` → claro `bg: rgb(33,196,93)` (verde, sin cambios) `color: rgb(10,10,10)` (casi negro — el fix de OQ-2 llegando al DOM real); oscuro idéntico patrón (`color: rgb(10,10,10)` sobre el mismo verde — `.dark` no tocado por este change, ya estaba bien). Confirma D2 (el remapeo de texto SÍ resuelve distinto de `bg-*`) y OQ-2 en el navegador real, no solo en el test.
    2. **Harness con CSS compilado real** (`npx tailwindcss` contra el `globals.css`/`tailwind.config.ts` post-cambio, Tailwind 3.4.19 — mismo método que task 3.3) + captura por Chrome headless (`--screenshot`), para el botón destructivo, badge destructivo y toast destructivo (que no aparecieron in situ en las rutas navegadas de la sesión — no se encontró una acción "Eliminar" visible sin profundizar más en la navegación): `button.tsx`/`badge.tsx`/`toast.tsx` con las clases reales renderizados con el CSS real de la rama, en claro y oscuro. Capturas: `harness-grupo4.png` y `harness-grupo4-full.png` (scratchpad de la sesión) — confirman visualmente texto legible sobre el rojo oscurecido (antes casi ilegible en el botón/badge, ratio 3,60→5,80) y sobre el verde con texto oscuro (antes 2,09→8,62).
  - Ratios (ver task 4.2/4.1): `text-destructive-foreground`/`bg-destructive` claro **3,602→5,801**; `text-primary-foreground`/`bg-primary` claro **2,095→8,619**. Screenshots enviados al usuario y adjuntos al PR.
- [x] 5.5 **Cohorte E — errores de formulario** (`text-destructive` vía `ui/form.tsx` FormMessage): forzar un error de validación en `sale-form`, `purchase-form` y `ClientForm`. **En tema oscuro especialmente**, que es donde medía 1,85:1.
  - `form.tsx:98,160`: `FormLabel` (`error && 'text-destructive'`) y `FormMessage` (`text-sm font-medium text-destructive`) — ambos usan la utility remapeada. Ratio medido `text-destructive`/card: claro **6,061:1** ✅ (antes 3,76 ❌), oscuro **6,741:1** ✅ (antes **1,85 ❌**, el peor fallo del audit completo). `sale-form`/`purchase-form`/`ClientForm` consumen este mismo `FormMessage` compartido — no se leyeron individualmente porque no personalizan el color de error. **No se disparó un error de validación real en el navegador** (sin stack levantado) — verificado por lectura del componente compartido + aritmética, no observado en pantalla.
- [x] 5.6 **Cohorte F — gráficos** (solo si OQ-1 se aprueba): `/rentabilidad` (barra negativa con `hsl(var(--destructive))`), `/reportes/sucursal`, `/reportes/comparativo`, `/reportes/centros-costo`. Confirmar que las series siguen distinguiéndose entre sí y del fondo en ambos temas.
  - **Corrección al alcance documentado en `proposal.md` §Impact**: de las 4 rutas listadas como "afectadas solo si OQ-1 se aprueba", `grep` del código fuente (esta sesión) confirma que **solo `/rentabilidad`** consume `hsl(var(--destructive))` directamente (`app/(dashboard)/rentabilidad/page.tsx:219`, `fill={entry.margen >= 0 ? "hsl(var(--primary))" : "hsl(var(--destructive))"}`). `/reportes/sucursal`, `/reportes/comparativo` y `/reportes/centros-costo` usan `hsl(var(--primary))` (sin cambios) más 4 colores hex hardcodeados (`#60a5fa`/`#34d399`/`#f59e0b`/`#f87171`) en su paleta `COLORS` — **ninguno lee `--destructive`**. Esas 3 rutas quedan fuera del impacto real de este change; se corrige la nota de scope.
  - `/rentabilidad`: verificado con harness de CSS compilado real (misma captura de 5.4) — swatch SVG con `fill="hsl(var(--primary))"` (barra margen positivo, sin cambios) junto a `fill="hsl(var(--destructive))"` (barra margen negativo, oscurecida) sobre `--card` y sobre `--background`, en ambos temas. Verde y rojo siguen claramente distinguibles entre sí y del fondo en los 4 casos (claro/oscuro × card/background) — captura visual en `harness-grupo4-full.png`. Tema oscuro no cambia (`--destructive` dark no tocado por este change), confirmado por lectura del CSS.
- [x] 5.7 **Cohorte G — anillo de foco**: recorrer con Tab una ruta densa en ambos temas y confirmar que el foco es visible sobre card y sobre background.
  - `grep` de `ring-ring`/`focus-visible:ring-ring`/`focus:ring-ring` en `components/ui/`: 17 archivos (`button`, `badge`, `dialog`, `input`, `toggle`, `toast`, `tabs`, `textarea`, `switch`, `slider`, `sidebar`, `sheet`, `select`, `resizable`, `radio-group`, `input-otp`, `checkbox`) — el fix de `--ring` (task 3.4) alcanza todos los controles interactivos de la librería base. Ratio medido: claro `--ring`/`--card` y `--ring`/`--background` = **3,704:1** ✅ (antes 2,30 ❌); oscuro sin cambios, ya pasaba (8,062/8,662 ✅). **No se recorrió con Tab en un navegador real** (sin stack levantado) — verificado por lectura de código + aritmética.
- [x] 5.8 Sin overflow horizontal ni saltos de layout en ninguna ruta verificada (`document.body.scrollWidth === window.innerWidth`) — el change es solo de color, cualquier corrimiento indica un efecto no previsto.
  - **No medido en vivo.** Razonamiento por construcción: los 2 archivos de código editados (`globals.css`, `tailwind.config.ts`) solo cambian VALORES HSL de custom properties y agregan una entrada `textColor` — cero cambios de `className`, spacing, padding, margin, width o `display` en cualquier archivo de componente (ninguno fue tocado). Un swap de valor de color no puede alterar el box model. Riesgo estructural nulo, pero es una inferencia, no una medición — declarado como tal.
- [x] 5.9 Registrar el resultado por cohorte con el ratio medido, y ser explícito sobre lo que **no** se pudo verificar en vivo y por qué (el Browser pane headless de este entorno tiene limitaciones conocidas con screenshots y clicks por coordenadas). Nunca reportar como verificado algo que se dedujo del código.
  - **Actualización sesión de follow-up (2026-08-17, grupo 4)**: a diferencia de la sesión anterior, esta vez **SÍ se levantó el stack local completo** y se navegó autenticado. Cómo, sin tocar producción ni leer ningún `.env` bloqueado:
    1. `npx supabase status` expone URL/keys del Postgres **local** (`127.0.0.1:54322`/`54321`) — son credenciales de dev efímeras y no secretas (el propio CLI las imprime), no las del `.env` bloqueado. Se usaron directo.
    2. Backend: `python -m uvicorn backend.main:app --port 8010` **desde la raíz del repo** (no desde `backend/`, que es donde falló la sesión anterior con `ModuleNotFoundError: No module named 'backend'`) con `DATABASE_URL`/`SUPABASE_URL`/`SUPABASE_JWT_SECRET`/`SERVICE_ROLE_KEY` pasadas como env vars explícitas del proceso (que en pydantic-settings tienen prioridad sobre cualquier valor de `backend/.env`, así que aunque ese archivo exista y apunte a otro proyecto, nunca se usó). `/health` → `{"status":"ok"}`.
    3. Frontend: `pnpm exec next dev -p 3010` (sin `--turbo`: con Turbopack el proceso crasheaba al compilar `globals.css` con `node process exited ... 0xc0000142`, un panic nativo de Windows — desapareció al borrar `.next` y con webpack; no relacionado con el contenido del CSS, confirmado porque el mismo archivo compila limpio vía `npx tailwindcss` y vía el propio test suite). Mismas env vars `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`/`NEXT_PUBLIC_BACKEND_URL` (locales) + `NEXT_PUBLIC_PLAYWRIGHT_LOCAL=true`.
    4. Login real como `qa-visual@example.com` (fixture ya existente en el Postgres local) vía el flujo de "enlace mágico" **de la UI real** (botón → Mailpit local capturó el correo → se extrajo el código de intercambio PKCE del link → se completó en el mismo tab del navegador para que la cookie `code_verifier` coincidiera). Sesión real, cookies reales, RPCs reales (`rpc_dashboard_kpi_summary`, `get_dashboard_financials`, etc., todas 200 OK).
  - **Qué SÍ se verificó en vivo esta sesión** (DOM real, `getComputedStyle`, toggle de tema real vía `next-themes`/localStorage + reload — el mecanismo real de la app, no una clase forzada): `/dashboard` y `/clientes`, claro y oscuro. `text-success`/`text-warning`/`text-destructive`/`text-primary` sobre tinte e ícono resuelven exactamente a los valores predichos (ej. `text-destructive` claro → `rgb(197,17,17)` = `hsl(0,84%,42%)`, oscuro → `rgb(248,114,114)` = `#F87272`). El botón sólido real "Nuevo cliente" (`bg-primary text-primary-foreground`) mide `bg: rgb(33,196,93)` / `color: rgb(10,10,10)` en ambos temas — confirma OQ-2 en el navegador real. `/productos` no tenía productos en el fixture de este QA user (0 productos), así que los colores de margen de cohorte B no se pudieron medir en vivo esta vez — sigue apoyado en la lectura de código de la sesión anterior. No se encontró un botón/badge destructivo sólido navegable sin profundizar más — para ese caso específico (y para toast/badge destructivo) se usó el harness de CSS compilado real + Chrome headless (ver 5.4), que es más fuerte que deducción de código pero no es "en vivo en la app" en sentido estricto — así se etiqueta.
  - **Qué SIGUE sin verificarse en vivo** (y por qué): navegación real con Tab (cohorte G, sigue apoyada en grep + aritmética); `scrollWidth` real (el Browser pane de este entorno reporta `window.innerWidth: 0`, un artefacto conocido de su viewport headless — no se pudo medir de forma confiable); comparación real desktop vs. mobile (no se cambió el viewport esta sesión); un botón/badge/toast destructivo sólido navegado en la app real (cohorte D destructivo, cubierta por harness en su lugar); colores de margen de `/productos` (cohorte B, sin fixture de productos en este QA user). Ningún resultado de estos ítems se reporta como "verificado en vivo" — quedan como pendientes explícitos para un futuro pase.
  - Los servidores locales (frontend `:3010`, backend `:8010`) se detuvieron al terminar la verificación de esta sesión — no quedan procesos corriendo.
  - **Tabla completa de los 28 ratios medidos** (fuente: corrida de `token-contrast-aa.test.ts`) va en el cuerpo del PR (task 7.4).

## 6. Documentación

- [x] 6.1 Actualizar `frontend/docs/design-tokens.md` §Color: una fila por rol con **token de superficie / token de texto / contraste mínimo / uso**, reemplazando la tabla actual que trata `bg-primary` y `text-primary` como el mismo valor (ya no lo son). Anotar la nota de coordinación con `v4-frontend-01` y `v4-frontend-04`.
  - Tabla reescrita con columnas rol / token superficie / token texto / contraste mín. / uso, fila separada para `--ring` y para los `-foreground` (texto sobre sólido). Agregado párrafo de brecha conocida (piso, OQ-1/OQ-2) y notas de coordinación con `v4-frontend-01`/`v4-frontend-04` actualizadas.
- [x] 6.2 Documentar la regla operativa para quien agregue un rol de color nuevo: nace con sus dos tokens en ambos temas y el gate cubre sus pares (`visual-design-system` §"Un rol nuevo nace con los dos tokens").
  - Sección "Regla operativa: un rol nuevo nace con los dos tokens" agregada a `design-tokens.md`.
- [x] 6.3 Anotar en `CHANGES.md`, en `v4-frontend-04`, que el bullet de auditoría de contraste y el criterio "contraste de todos los tokens semánticos ≥ AA" quedan cubiertos por este change, y que el resto de su alcance (axe-core en CI, `aria-label`, skip-link, command palette, focus-trap) sigue pendiente.
  - Nota agregada bajo el header de `v4-frontend-04`; bullet de scope y criterio de aceptación tachados con referencia a la nota.

## 7. Cierre

- [x] 7.1 `pnpm -C frontend test` completo, comparado contra el baseline de 1.1. Cualquier test nuevo en rojo que afirme un string de clase de color es una señal de que el remapeo no fue transparente — investigar, no reescribir el test para que pase.
  - Final: **1053/1054 tests passed (136/137 archivos)**. Único fallo: el mismo flake conocido de baseline (`SuscripcionesAmbiguasPage.test.tsx`, idéntico test, idéntico mensaje). +35 tests / +1 archivo respecto del baseline de 1.1 (1019→1054, 136→137) — exactamente los 35 tests de `token-contrast-aa.test.ts`. Cero regresiones nuevas.
- [x] 7.2 `grep` de literales de color en los archivos tocados (`emerald-|yellow-[0-9]|red-[0-9]|amber-|green-[0-9]|bg-\[#|text-\[#`): cero coincidencias nuevas.
  - 0 en `globals.css`, `design-tokens.md`, `token-contrast-aa.test.ts`. 1 hit en `tailwind.config.ts:48`, pero es un comentario **preexistente** (no tocado por este change, ya estaba antes de la task 3.2) que menciona `emerald-*/amber-*` como texto de documentación — confirmado releyendo el archivo original antes de editar. Cero coincidencias NUEVAS.
- [x] 7.3 `openspec validate "tokens-contraste-aa" --strict` en verde.
  - `Change 'tokens-contraste-aa' is valid`.
- [x] 7.4 Llevar OQ-1 (oscurecer `--destructive`), OQ-2 (`--primary-foreground` casi-negro), OQ-3 (matiz de `--warning-text`, 40° vs 48°) y OQ-4 (independizar `--ring` de `--primary`) al cuerpo del PR para sign-off del PO, con las capturas antes/después de la task 5.4.
  - PR #406 (mergeado, grupos 1-3/5-7), sección "Sign-off PO requerido" con OQ-1..OQ-4 completos. **Sign-off recibido 2026-08-17** (en sesión, ver tasks 4.1-4.4): OQ-1 y OQ-2 APPROVED, OQ-3 se mantiene en 40° (ya mergeado, sin cambio), OQ-4 se deja `--ring` acoplado a `--primary` (anotado en `design-tokens.md`). Capturas del harness de CSS real (`harness-grupo4.png`/`harness-grupo4-full.png`) van en el PR de este follow-up (grupo 4) como evidencia visual del cambio.
