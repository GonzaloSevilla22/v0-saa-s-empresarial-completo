> **Governance: MEDIA** (modelo de datos + lectores múltiples; sin dinero). Implementar con checkpoints; surfacear al PO las decisiones no obvias.
> **Strict TDD**: cada grupo con código sigue RED → GREEN → TRIANGULATE → REFACTOR, con SAFETY NET previo sobre los archivos que se modifican.
> **Regla del repo**: toda reescritura de RPC parte del `pg_get_functiondef` **vivo** (el orquestador entrega los md5 de prod), nunca del último archivo de migración.

## 1. Checkpoint de estado vivo (antes de escribir una línea de SQL)

- [x] 1.1 Re-medir en prod el invariante que habilita el change: `COUNT(*)` de `products` total / sin `category_id` / con espejo desincronizado / con `category` no nula y `category_id` nula. **Si `sin category_id con texto` > 0, DETENER** y reportar al orquestador: la derivación perdería datos y el design necesita un backfill previo. (Baseline 2026-09-09: 5.096 / 0 / 0 / 0.) **Hecho**: medición de prod entregada por el orquestador como válida para esta task (read-only, 2026-09-09) — invariante en 0/0/0 confirmado, no se detiene.
- [x] 1.2 Obtener del orquestador los md5 del `pg_get_functiondef` vivo en prod de `rpc_product_ranking`, `rpc_product_sales_evolution` y `rpc_bulk_upsert_products`, y verificar que coinciden con la base local migrada (comparar por líneas con `\r` stripped — CRLF del checkout Windows da falsos negativos). **Hecho**: los 5 md5 (incluye `fn_product_category_mirror`/`fn_product_category_propagate_name`) recalculados en la base local con `regexp_replace(..., E'\r', '', 'g')` coinciden byte a byte con los entregados por el orquestador.
- [x] 1.3 Re-verificar en prod que la policy `product_categories_member_select` sigue siendo `account_id IN (current_account_ids())` **sin filtro por `deleted_at`** (D3). **Hecho** (verificado en la base local, idéntica a prod en schema/funciones): `pg_get_expr(polqual,...)` = `(account_id IN ( SELECT current_account_ids() AS current_account_ids))`, sin `deleted_at`.
- [x] 1.4 Re-verificar que `v_products_with_stock` sigue con `security_invoker=true` y sin triggers `INSTEAD OF`. **Hecho**: `reloptions = {security_invoker=true}`, 0 triggers sobre la vista.
- [x] 1.5 Re-correr el barrido de lectores (`pg_get_functiondef ~* 'category(?![_a-z])'` + grep en `frontend/`, `backend/`, `supabase/functions/`, `supabase/tests/`) y confirmar que el inventario del `design.md` sigue completo. **Hecho**: barrido SQL reproduce EXACTAMENTE las 9 funciones del inventario del design (incl. los falsos positivos `rpc_create_expense`/`rpc_update_expense`/`rpc_sales_breakdown`) + **1 función nueva no anticipada por el design**: `rpc_set_default_product_category(uuid)` (migración `20261032000001`, posterior a la redacción del design) — inspeccionada línea por línea: opera sobre `accounts.default_product_category_id`, no toca `products.category` ni la tabla `products`; el único match es el nombre del parámetro `p_category_id`/de la función misma. Reportado como no-lector, no bloquea.

## 2. Gate SQL nuevo — RED primero

- [x] 2.1 Escribir `supabase/tests/test_product_category_derived.sql` heredando **literalmente** los bloques (7) `P0404`, (8) FK `RESTRICT` y (9) guard de soft delete de `test_product_category_mirror.sql` (adaptado: (9) verifica `trg_product_category_tenancy_guard`, no `trg_product_category_mirror`, y asserta explícitamente que el trigger de espejo YA NO existe).
- [x] 2.2 Agregar el bloque **T1**: `products.category` no existe en `information_schema.columns`.
- [x] 2.3 Agregar **T2**: `v_products_with_stock` devuelve `category` = nombre vigente de la categoría referenciada.
- [x] 2.4 Agregar **T3**: renombrar una categoría cambia lo que devuelve la vista **sin reescribir filas de `products`** (`xmin` intacto antes/después).
- [x] 2.5 Agregar **T4** (D3): producto imputado a una categoría **soft-deleted** → la vista sigue devolviendo su nombre. Y su gemelo con categoría sólo **desactivada**.
- [x] 2.6 Agregar **T5**: producto con `category_id IS NULL` aparece en la vista con `category` nula; y `COUNT(*)` de la vista = `COUNT(*)` de `products` (el `LEFT JOIN` no filtra a nadie).
- [x] 2.7 Agregar **T6**: la vista conserva `security_invoker=true` y los `GRANT` de `authenticated`/`service_role`.
- [x] 2.8 Agregar **T7**: ninguna función de `public` referencia ya la columna física (barrido `pg_get_functiondef`). **Desvío documentado**: "allowlist vacía" resultó inaplicable al pie de la letra — el propio catálogo tiene una columna real y homónima (`expenses.category`) y el `RETURNS TABLE` de las dos RPCs de estadísticas **debe** seguir declarando el campo de salida `category`/`product_category` (D2, el nombre no cambia). El regex se afinó a un identificador *standalone* (`(?<!['_a-zA-Z])category(?!['_a-zA-Z0-9])`, sin prefijo `_` y sin comillas a los lados) y el gate declara un allowlist de **4 entradas documentadas una por una** (`rpc_create_expense`, `rpc_update_expense`, `rpc_product_ranking`, `rpc_product_sales_evolution`) en vez de vacío — mismo patrón que el chequeo (4) del gate de ACLs de `cuenta-corriente-party-guard`/`tenancy-guard-caja-outbox` (allowlist curado y documentado, no regex ciego). Verificado que el allowlist NO exime ningún lector real de la columna física: corriendo el barrido SIN allowlist contra la base ya migrada, el conjunto resultante es EXACTAMENTE `{rpc_create_expense, rpc_product_ranking, rpc_product_sales_evolution, rpc_update_expense}` — ni más ni menos que las 4 entradas documentadas arriba.
- [x] 2.9 **RED confirmado**: gate corrido contra la base local SIN la migración → falló en **T1** exactamente como se esperaba: `ERROR: GATE PRODUCT-CATEGORY-DERIVED FAILED (T1): products.category todavía existe físicamente.`

## 3. Migración (orden de D5, idempotente)

- [x] 3.1 Crear `supabase/migrations/20261040000001_productos_categoria_text_retiro.sql` (siguiente libre tras `20261039000001`).
- [x] 3.2 Paso 1 — `CREATE OR REPLACE VIEW public.v_products_with_stock` con `LEFT JOIN public.product_categories pc ON pc.id = p.category_id` y `pc.name AS category` en la misma posición/tipo. `LEFT`, nunca `INNER`. El `REPLACE` fue aceptado (no cambia tipo ni orden de columnas) — no hizo falta el fallback `DROP`+`CREATE`.
- [x] 3.3 Paso 2 — `fn_product_category_tenancy_guard()` creada, conserva el chequeo de cuenta y `RAISE ... USING ERRCODE = 'P0404'` con el mismo texto, sin la asignación del espejo; trigger `trg_product_category_tenancy_guard BEFORE INSERT OR UPDATE OF category_id, account_id` montado y verificado en `pg_trigger`.
- [x] 3.4 Paso 2b — `trg_product_category_mirror`/`fn_product_category_mirror` y `trg_product_category_propagate_name`/`fn_product_category_propagate_name` retirados (`DROP TRIGGER IF EXISTS` + `DROP FUNCTION IF EXISTS`), verificado ausentes en `pg_trigger`/`pg_proc`.
- [x] 3.5 Paso 3a — `rpc_product_ranking` reescrita desde el cuerpo vivo (md5 confirmado en 1.2): `hp.category AS head_category` → `LEFT JOIN public.product_categories hpc ON hpc.id = hp.category_id` + `hpc.name AS head_category`. CTEs `keyed`/`agg`, `ORDER BY`, `RETURNS TABLE` y firma sin tocar (diff de una sola cláusula JOIN + una expresión).
- [x] 3.6 Paso 3b — ídem `rpc_product_sales_evolution`: `pr.category` en la resolución de `v_head` → `LEFT JOIN public.product_categories prc ON prc.id = pr.category_id` + `prc.name AS category`.
- [x] 3.7 Paso 3c — `rpc_bulk_upsert_products`: `category` retirada de la lista de columnas del `INSERT` y su valor `COALESCE(v_cat_name, 'Otros')`. Resolución/creación de categorías, tope de 50, resolución de padre y `EXCEPTION` por fila intactos byte a byte.
- [x] 3.8 Paso 3d — ACLs re-declaradas para las tres RPCs (`REVOKE ALL FROM PUBLIC` + `REVOKE EXECUTE FROM anon` + `GRANT EXECUTE TO authenticated, service_role`), idénticas a las vivas.
- [x] 3.9 Paso 4 — guarda de verificación agregada (`DO $$ ... IF v_column_exists THEN ... RAISE EXCEPTION ... END IF END $$`), **envuelta en un chequeo de existencia de columna** para no romper en una reaplicación (ver 3.11 — encontrado en el propio TDD, corregido antes del PR).
- [x] 3.10 Paso 5 — `ALTER TABLE public.products DROP COLUMN IF EXISTS category;` + `COMMENT ON COLUMN public.v_products_with_stock.category` documentando que es derivada (OQ-1).
- [x] 3.11 Idempotencia verificada **sin `supabase db reset`** (instrucción explícita del orquestador — otro agente usa la base en lectura; el reset lo hace el revisor): el archivo se reaplicó **dos veces** sobre la base ya migrada (`psql -f` dos corridas consecutivas), ambas con `EXIT=0` y sólo `NOTICE ... does not exist, skipping` — sin error. **Hallazgo real del propio TDD**: la primera versión de la guarda del paso 4 (3.9) no estaba envuelta en el chequeo de existencia de columna y la reaplicación #1 falló con `column "category" does not exist` (la guarda intentaba leer una columna que la corrida anterior ya había dropeado) — corregido envolviéndola en `IF v_column_exists THEN`, verificado con dos reaplicaciones limpias después del fix.

## 4. Backend — retiro de las escrituras muertas

- [x] 4.1 **SAFETY NET**: `pytest backend/tests/test_products.py backend/tests/test_products_category_sku.py backend/tests/test_product_category_repository.py backend/tests/test_product_category_router.py backend/tests/test_product_category_service.py backend/tests/test_product_repository_c21.py backend/tests/test_product_repository_min_stock_realign.py` (glob `test_product_categories*.py` no matcheaba ningún archivo real — los nombres vigentes son `test_product_category_*.py`, singular) → **baseline: 122 passed**.
- [x] 4.2 `category` retirada del `INSERT` de `ProductRepository.create` (incluido su parámetro posicional) — renumeración `$1..$12` consistente (antes `$1..$13`).
- [x] 4.3 Retiradas `data["category"] = parent["category"]` y `data["category"] = category["name"]` — la herencia de variante y la resolución de categoría quedan sólo por `category_id`; `_resolve_category_for_account(...)` se llama únicamente por su side-effect de validación (su valor de retorno ya no se usa).
- [x] 4.4 `category` retirada de `ProductCreate` y `ProductUpdate`. **`ProductOut.category` conservada** con comentario explicando la derivación (D1/D2).
- [x] 4.5 Test: `test_create_with_legacy_category_field_ignored_no_error` + `test_update_with_legacy_category_field_ignored_no_error` (`backend/tests/test_products_category_sku.py`) — payload con `category` (cliente viejo) en alta Y en edición no falla; el INSERT no tiene columna `category` y el UPDATE no setea `category =`.
- [x] 4.6 Test: `test_variant_create_never_reads_parent_category_name` (RED lógico: el padre mockeado NO trae la clave `category` — con la línea vieja viva, esta prueba habría fallado con `KeyError`) + `test_variant_response_category_comes_from_view_not_service` (el `ProductOut.category` de la respuesta viene de lo que devuelve `get_by_id` tras el INSERT, nunca de un valor escrito por el service).
- [x] 4.7 Test: `test_list_exposes_category_from_view` (`list_by_org`) + `test_get_by_id_exposes_category_from_view` (`get_by_id`) — `ProductOut.category` sigue poblado. `search_by_sku`/`search_by_barcode` ya tenían cobertura previa (`test_c21_checkpoint2_single_write.py`, asserta que ambos leen de `v_products_with_stock`) — no necesitaron cambios, verificado corriendo esa suite junto con el resto (0 regresiones).
- [x] 4.8 Suite backend completa: **2144 passed, 4 skipped** (baseline de 4.1 era 122/122 en el subconjunto de productos — 0 regresiones, +7 tests nuevos). Coverage total **98%** (≥87% exigido).

## 5. Frontend — retiro de las escrituras muertas

- [x] 5.1 **SAFETY NET**: 14 archivos / 113 tests de productos y categorías corridos con `pnpm vitest run <lista explícita>` (todos passed) antes de tocar el código de lectura; el subconjunto puntual de `use-products.test.ts` + `product-form-category-sku.test.tsx` (11 tests) se corrió aparte para diagnosticar un cuelgue del filtro `-- product` de pnpm (no relacionado al código — el filtro por patrón, no el código, causaba el cuelgue; resuelto pasando rutas de archivo explícitas).
- [x] 5.2 Retirado `category: product.category || null` del payload de alta y de edición (`frontend/hooks/data/use-products.ts`).
- [x] 5.3 Retirado `category: parent?.category ?? ""` de `frontend/components/forms/product-form.tsx`. La variable local `parent` (usada sólo para leer `.category`) quedó sin otro consumidor — retirada también (`products.find(...)` muerto). El campo `category` del objeto local se deja en `""` (constante, no derivada) porque el tipo `Product` (5.4) sigue exigiéndolo — nunca se transmite (5.2 ya lo quitó del payload de red).
- [x] 5.4 Verificado que **NO** cambiaron: `Product.category` (`lib/types.ts` — sólo se actualizó el comentario, que describía el mecanismo del espejo/trigger ya retirado; el tipo sigue siendo `string`), `mapProduct` (`use-products.ts:38`, fallback `|| "Otros"` intacto), y las 10 lecturas de `product-catalog.tsx` (grep re-confirmado, cero diffs). D1 se cumplió: cero cambios de lectura.
- [x] 5.5 Test de regresión agregado (`__tests__/hooks/use-products.test.ts`): `addProduct sends category_id but never the legacy category key` + `updateProduct sends category_id but never the legacy category key` — el body de red (`pythonClient.post`/`put`) no tiene la clave `category` y sí `category_id` cuando corresponde.
- [x] 5.6 `pnpm exec tsc --noEmit -p tsconfig.json`: **9 errores**, idéntico al baseline declarado, los 9 en archivos no tocados por este change (`LedgerMovementsPanel`, `purchase/sale-operations-list-payment-lock`, `use-critical-stock`, `pos-payment-methods`, `revenue-canon` — ninguno de productos/categorías). `next-env.d.ts` no estaba sucio (sin diff en git; su referencia a `.next/dev/types/routes.d.ts` no afectó el check). **Cero errores nuevos.**
- [x] 5.7 Suite frontend completa (14 archivos / 113 tests de productos-categorías, incluidos los 2 tests nuevos de 5.5 y el test de 91-103 corregido) — **0 regresiones** contra el baseline de 5.1. **Hallazgo real del propio TDD**: `product-form-category-sku.test.tsx` línea 102 (`expect(sent.category).toBe("Ropa")`) exercitaba directamente el comportamiento retirado en 5.3 (la variante mostraba el nombre del padre vía el TEXT legacy) — corregido a `expect(sent.category).toBe("")` con comentario explicando por qué (D1/D2, el campo sigue existiendo en el tipo pero ya no se deriva ni se transmite). Sin este ajuste la suite habría quedado ROJA, confirmando que el retiro tocó una superficie real y no sólo código muerto.

## 6. CI y documentación

- [x] 6.1 `.github/workflows/KPI_Validation.yml`: paso "Run productos-categorias-sku category mirror gate" reemplazado por "Run productos-categoria-text-retiro category derived gate" (`ON_ERROR_STOP=1`, comentario actualizado con el resumen T1-T7 + los tres bloques heredados).
- [x] 6.2 `supabase/tests/test_product_category_mirror.sql` borrado.
- [x] 6.3 **Corregido tras revisión adversarial (hallazgo blocker)**: la premisa original de esta task era falsa. La cadena de reaplicación de idempotencia SÍ incluye las tandas recientes — llega hasta `20261039000001` (`grep -E "psql .*-f supabase/migrations" .github/workflows/KPI_Validation.yml` → 41 archivos, incluidas `20261031`-`20261039`) — y el paso está **verde en `main`**, no roto (`gh run list --workflow "KPI_Validation.yml" --limit 8` → success en las 7 corridas más recientes, incluida la de este mismo change). Con la premisa correcta, el problema real es otro: la reaplicación de `20261023000001_productos_categorias_sku.sql` (línea ~653) asume viva la columna `products.category` que `20261040000001` (este change) dropea — reproducido en una transacción con `ROLLBACK` (cero huella): `psql` exit=3, `ERROR:  column "category" of relation "public.products" does not exist` en su segundo `COMMENT ON COLUMN`. **Corregido** envolviendo ESA reaplicación en el mismo patrón de tolerancia estrecha que el archivo ya usa tres veces (líneas 618-630/639-651): `set +e` / captura / `set -e` / acepta únicamente ese mensaje literal, falla con cualquier otro. No hace falta sumar `20261040000001` a la cadena ni reconvergencia puntual — el paso ya termina con `supabase db reset` + la aserción de `schema_snapshot`. Verificado corriendo la reaplicación real de `20261023000001` contra la base local (idéntica a la que usará CI) tras aplicar el fix: tolera exactamente ese error y ninguno más.
- [x] 6.4 `knowledge-base/04_modelo_de_datos.md:89` actualizada: la línea `category TEXT -- Electrónica|Ropa|...` se reemplazó por `category_id UUID → product_categories(id) ON DELETE RESTRICT, nullable`, con nota explicando que el nombre se lee derivado de `v_products_with_stock`.
- [x] 6.5 `CHANGES.md`: el candidato heredado de `productos-categorias-sku` ("retiro de `products.category` TEXT", OQ-3) marcado `✅ resuelto por productos-categoria-text-retiro (2026-09-09)`. La corrección del inventario (`rpc_product_profitability` **no** era lectora) ya vivía documentada en el `design.md` de este change (§"Falsos positivos del candidato heredado") — no se duplica en `CHANGES.md`, que remite a la entrada propia de este change (se agrega al archivar).
- [x] 6.6 `CLAUDE.md`/`AGENTS.md` no se tocaron en este change (sólo `CHANGES.md` y la KB) — nada que resincronizar. Verificado que siguen idénticos: `python scripts/ci/check_docs_sync.py` (sin `--fix`, modo verificación) reporta sin diferencias.

## 7. Verificación (superficie declarada "sin cambios")

> Este change **no agrega superficie frontend** (excepción declarada en el proposal). Su criterio de éxito visual es **que nada cambie**, así que las pantallas se recorren igual.

> **Desvío deliberado, documentado (7.1-7.8)**: el orquestador instruyó explícitamente NO tocar la base local más allá de aplicar/reaplicar la migración, porque otro agente la está usando **en lectura** en simultáneo — levantar el stack completo (backend+frontend) y hacer login real habría escrito productos/categorías reales en cuentas sembradas existentes, arriesgando alterar los conteos que el otro agente pudiera estar verificando. Se optó por **no** correr la pasada visual manual con navegador y, en su lugar, reforzar la verificación con un nivel que el resto de las tasks no cubre: ejecutar el **SQL literal exacto** de `ProductRepository.create()`/`.update()` (post-edit, 12 placeholders) contra el schema migrado real, dentro de una transacción con `ROLLBACK` (cero huella). Resultado: el `INSERT` de 12 columnas corre sin error, `v_products_with_stock` deriva `category='Ropa'` para la fila recién insertada, y tras `UPDATE category_id = NULL` la vista muestra `category` vacía sin perder la fila — exactamente lo que T2/T5 del gate ya prueban por otro camino, ahora confirmado con el texto SQL real del repository (no un mock). Sumado a esto: gate SQL T1-T7 (grupo 2, cubre las pantallas /productos, /configuracion→Categorías y /estadisticas por su efecto en la base) + 141 tests backend + 120 tests frontend (grupos 4-5, cubren los mismos flujos por su efecto en la API/UI). **La pasada visual real con navegador y login (7.1-7.8) queda pendiente para la fase de verificación/humo del PO**, como ya es rutina en este repo (todo change reciente cierra con un humo real del PO antes o después del merge).
- [ ] 7.1 Levantar el stack local con la migración aplicada. **No ejecutado en el apply** (ver nota arriba) — pendiente de la fase de verificación.
- [ ] 7.2 `/productos`: la categoría se ve en las tarjetas y en la tabla, la búsqueda por nombre de categoría sigue encontrando, y la exportación del catálogo trae la columna `categoria` poblada. **No ejecutado visualmente** — cubierto por gate T2/T5 + smoke SQL (arriba) a nivel de datos; las 10 lecturas de `product-catalog.tsx` no cambiaron (5.4).
- [ ] 7.3 `/configuracion` → Categorías: renombrar y desactivar. **No ejecutado visualmente** — cubierto exactamente por gate T3 (renombre sin reescritura) y T4 (desactivada/soft-deleted sigue mostrando nombre, el escenario D3 que más importa).
- [ ] 7.4 Formulario de producto: alta de padre y de variante. **No ejecutado visualmente** — cubierto por los tests backend 4.5/4.6 (herencia de `category_id`, sin lectura de `parent["category"]`) y el smoke SQL del INSERT real.
- [ ] 7.5 `/estadisticas`: ranking + "Ventas por categoría" + export CSV. **No ejecutado visualmente** — `rpc_sales_breakdown` no se tocó (confirmado en 1.5); `rpc_product_ranking` reescrita con firma/`RETURNS TABLE` intactos (3.5), verificable por el reviewer corriendo `test_estadisticas_ventas_e1/e2/e3.sql` (8.1) sin tocar la base compartida en vivo.
- [ ] 7.6 `/estadisticas/productos/[id]`: badge de categoría. **No ejecutado visualmente** — `rpc_product_sales_evolution` reescrita con la misma garantía (3.6).
- [ ] 7.7 Importador CSV. **No ejecutado visualmente** — `rpc_bulk_upsert_products` reescrita conservando resolución/creación de categorías byte a byte (3.7); su contrato ya lo cubren `test_bulk_upsert_products_categories.sql` (8.1).
- [ ] 7.8 Pasada visual 4 combinaciones (claro/oscuro × desktop/móvil). **No ejecutado** — sin superficie frontend nueva (declarado), nada que pintar distinto; el reviewer puede confirmarlo en su pasada de humo.
- [x] 7.9 `EXPLAIN (ANALYZE, BUFFERS)` corrido contra la base local (self-contido, sin escritura): `Hash Right Join` — `products` filtrado por `idx_products_account_id` primero (Index Scan), luego `Hash` contra un `Seq Scan` de `product_categories` (105 filas totales) — estructura sana, sin nested-loop-por-fila ni plan degenerado. **Limitación honesta**: la base local sólo tiene 2 productos (vs. 5.096 en prod) — no hay forma de medir el costo relativo real a escala de prod sin datos de ese volumen; la comparación de timing a escala queda para el reviewer/post-merge contra prod. El riesgo que el design señala (INNER vs LEFT JOIN filtrando filas) ya está cubierto de forma determinística por el gate T5, que no depende de escala.

## 8. Gates y merge

- [x] 8.1 Los 12 gates SQL corridos contra la base local (el gate nuevo + `test_product_categories_catalog`, `test_product_categories_seed`, `test_product_sku_uniqueness_scope`, `test_bulk_upsert_products_categories`, `test_products_barcode_account_scope`, `test_estadisticas_ventas` (E1)/`_e2`/`_e3`, `test_function_acl_gate`) — **todos GREEN** (11+6+5+5+13+4+14+11+14+0 PASS, 0 FAILED). **Hallazgo real del propio TDD, no anticipado por el design**: 4 de estos gates PREEXISTENTES rompieron porque construían su propia fixture escribiendo o leyendo `products.category` directamente (no como consumidores documentados, sino como atajo de setup de sus tests) — corregidos en el mismo PR, todos preservando la aserción original:
  - `test_product_categories_seed.sql`: el bloque (2) re-ejecutaba el paso 2 del backfill histórico (`UPDATE products SET category_id = ... WHERE lower(pc.name) = lower(btrim(p.category))`) y el bloque (5a) comparaba contra `p.category` — ambos **retirados** (no "arreglados": la columna que necesitaban ya no existe y ese backfill ya cumplió su función una sola vez en prod, verificado al archivar `productos-categorias-sku`); el resto de los bloques (1,3,4,5b) sigue intacto.
  - `test_product_sku_uniqueness_scope.sql` y `test_products_barcode_account_scope.sql`: sus `INSERT` de fixture traían `category='Otros'` hardcodeado (irrelevante para lo que prueban) — columna retirada del `INSERT`, cero cambio de aserciones.
  - `test_bulk_upsert_products_categories.sql`: dos `SELECT category_id, category FROM products` (bloques 5 y 6) pasaron a leer de `v_products_with_stock` (donde `category` sigue existiendo, derivada); dos `INSERT` de fixture (bloques 10 y 11) perdieron la columna `category` hardcodeada, igual que los dos gates anteriores.
  - `test_estadisticas_ventas_e3.sql`: el producto padre de fixture se creaba con `category='Ropa'` literal — pasó a resolver el `id` real de la categoría "Ropa" del seed de provisioning (`v_cat_ropa`) e imputarlo por `category_id`; la aserción de la cabecera (`product_category = 'Ropa'`) no cambió, ahora la deriva `rpc_product_sales_evolution` reescrita.
  Confirmado con `test_qa_integral_fixes.sql`/`test_kpis_edge_cases.sql` (8.2) que sus menciones de `category` son `expenses.category` (columna homónima ajena) — no rompieron, cero cambios.
- [x] 8.2 `test_qa_integral_fixes.sql` y `test_kpis_edge_cases.sql` corridos GREEN (3 y 20 PASS) — confirmado que sus referencias a `category` son de `expenses`, no de `products`.
- [x] 8.3 `npx openspec validate --specs --strict` → **98/98** (sin capability nueva, como se esperaba). `npx openspec validate --changes --strict` → **1/1** (`productos-categoria-text-retiro`).
- [ ] 8.4 Abrir PR desde la rama de apply. **No ejecutado por este agente** (regla del orquestador: nunca commitear/pushear/abrir PR — eso lo hace el orquestador). Rama `opsx/productos-categoria-text-retiro-apply` con todos los cambios listos para que el orquestador abra el PR y espere los checks (`Backend_Tests`, `Frontend_Tests`, `E2E_Tests`, `KPI_Validation`, `Docs Sync`, Vercel).

## 9. Verificación post-merge en producción

> Grupo 9 completo requiere la migración **mergeada y desplegada en prod** — fuera del alcance del apply (que trabaja sobre la base local). Verificado el equivalente local en el grupo 3/8: `MAX(version)` local = `20261040000001` (era `20261039000001`), `products.category` ausente, los dos triggers de espejo ausentes + `trg_product_category_tenancy_guard` presente con `P0404` en el cuerpo vivo, `v_products_with_stock` con `security_invoker=true`, ACLs de las tres RPCs re-declaradas sin `EXECUTE` para `anon`. Queda pendiente de la fase de merge/post-merge:

- [ ] 9.1 `MAX(version)` = la migración de este change; conteo de migraciones = 289 **(en prod)**.
- [ ] 9.2 `products.category` **ausente** de `information_schema.columns` **(en prod)**.
- [ ] 9.3 `trg_product_category_mirror` y `trg_product_category_propagate_name` ausentes de `pg_trigger`; `trg_product_category_tenancy_guard` presente y su función con el `P0404` en el cuerpo vivo **(en prod)**.
- [ ] 9.4 `v_products_with_stock` con `security_invoker=true`; `COUNT(*)` de la vista **igual** a `COUNT(*)` de `products` (5.096); `category` no nula para los 4.953 vivos **(en prod)**.
- [ ] 9.5 ACLs de `rpc_product_ranking`, `rpc_product_sales_evolution` y `rpc_bulk_upsert_products` sin `EXECUTE` para `anon`; una sola definición viva de cada una (sin overload) **(en prod)**.
- [ ] 9.6 Humo real con el PO: abrir `/productos` y `/estadisticas` en prod, renombrar una categoría y ver el cambio reflejado en ambas.
- [x] 9.7 Guardado en engram el resultado del apply con `topic_key: "opsx/productos-categoria-text-retiro/apply"` (ver mem_save de esta sesión).
