# Tasks — v4-visual-3d-refresh

> Plan por fases incrementales (A→D). Cada fase entrega valor sin depender de la siguiente: la Fase A ya profesionaliza la app sin 3D; el 3D empieza en la Fase B con una escena piloto y se expande en la C.
> Prerequisito de coordinación: los tokens base los registra `v4-frontend-01`; el gate de `any`, `v4-frontend-02`; el gate de axe/contraste, `v4-frontend-04`; el bundle budget/analyzer, `v4-frontend-07`. Este change consume esos gates, no los reimplementa. Reglas de proyecto: NUNCA `any`; componentes en PascalCase.

## 1. Fase A — Fundaciones visuales y refresh 2D (sin 3D)

- [x] 1.1 Definir la matriz de clasificación de superficies (3d / 2d-motion / refresh-only) como documento vivo en `frontend/docs/surface-matrix.md`, con cada ruta del App Router clasificada; confirmar que POS, tablas y listados quedan en 2d-motion/refresh-only
- [x] 1.2 Añadir tokens de motion (`--motion-duration-fast/base/slow`, `--motion-ease-standard/emphasized`) en `app/globals.css` (`:root` y `.dark`) y exponerlos en `tailwind.config.ts theme.extend` (transitionDuration/transitionTimingFunction)
- [x] 1.3 Añadir tokens semánticos de elevación/sombra y escala tipográfica del refresh en `app/globals.css` + `tailwind.config.ts`, verificando que no dupliquen los base de `v4-frontend-01`
- [x] 1.4 Documentar el contrato de tokens del refresh (token → uso → tema claro/oscuro) en `frontend/docs/design-tokens.md` (extendiendo el de `v4-frontend-01` si ya existe)
- [x] 1.5 Instalar `framer-motion` y crear las envolturas reutilizables en `components/motion/` (`FadeIn.tsx`, `MotionList.tsx` con stagger, `Celebrate.tsx`), en PascalCase y sin `any`
- [x] 1.6 Crear el hook SSR-safe `hooks/ui/usePrefersReducedMotion.ts` y cablearlo en las envolturas de `components/motion/` para degradar a fundido mínimo/nulo
- [x] 1.7 Escribir tests (Vitest) de las envolturas de motion: animan con tokens cuando no hay reduced-motion, y degradan cuando `prefers-reduced-motion: reduce`
- [x] 1.8 Aplicar el refresh de tokens a los componentes base shadcn/Radix vía `cva` (Button, Card, Input, Badge, Dialog, Table) sin bifurcar componentes de `components/ui/`
- [x] 1.9 Migrar las clases de color hardcodeadas a tokens semánticos en las superficies `refresh-only`/`2d-motion` de mayor tráfico (dashboard, ventas/listados, productos), verificando que el lint de color de `v4-frontend-01` no reporte nuevas violaciones
- [x] 1.10 Aplicar micro-animaciones 2D (entrada de listas/cards, feedback de estado, hover/press, skeleton animado tokenizado) en las superficies `2d-motion`, incluido el POS, sin tocar handlers/queries/mutaciones
- [x] 1.11 Verificar tema claro y oscuro en las superficies refrescadas de la Fase A (toggle `next-themes` sin colores fuera de tema)

## 2. Fase B — Infraestructura 3D (R3F) + guardrails + escena piloto

- [x] 2.1 Instalar `@react-three/fiber` y `@react-three/drei`; confirmar compatibilidad con React 19 / Next 16 App Router
- [x] 2.2 Crear el gate de capacidad `lib/three/capabilityGate.ts` (reduced-motion, mobile/gama-baja por `hardwareConcurrency`/`deviceMemory`/`pointer:coarse`, detección WebGL, `saveData`/conexión lenta) con umbrales documentados y sin `any`
- [x] 2.3 Escribir tests (Vitest) del gate de capacidad cubriendo cada condición de fallback (reduced-motion, sin WebGL, mobile, save-data) y el caso "califica"
- [x] 2.4 Crear el wrapper `components/three/Lazy3DCanvas.tsx`: consume el gate, monta el `<Canvas>` por IntersectionObserver, pausa/desmonta el loop fuera de viewport (`frameloop="demand"`), envuelve en `<Suspense>` con poster de fallback
- [x] 2.5 Crear el error boundary `components/three/SceneErrorBoundary.tsx` que degrada a poster estático ante fallo de render/contexto WebGL perdido
- [x] 2.6 Configurar el patrón de carga `next/dynamic({ ssr:false, loading: Poster })` como helper/convención para las escenas y verificar con el analyzer de `v4-frontend-07` que el chunk 3D NO entra al first-load de rutas no-3D
- [x] 2.7 Ajustar el CSP en `lib/supabase/middleware.ts` añadiendo `worker-src 'self' blob:`; self-hostear los decoders Draco/KTX2 en `frontend/public/`; verificar en preview de Vercel que los workers no quedan bloqueados por el CSP de prod — **decoders Draco/KTX2 NO aplican**: sign-off PO (Open Question 5) restringe los assets 3D a geometría procedural/low-poly por código, sin glTF comprimido que decodificar; `worker-src` se agrega igual (defensivo/futuro-proof, guardrail explícito de la task) — verificado sin violaciones CSP en consola (2.10)
- [x] 2.8 Definir el presupuesto de peso/bundle por escena (chunk lazy y assets glTF/Draco) y documentarlo en `frontend/docs/3d-budget.md` — medido (no especulativo): ~233 KB gz compartido R3F+three, una sola vez, cacheado entre escenas; sin assets glTF/Draco (procedural-only)
- [x] 2.9 Implementar la escena piloto (hero de landing): `components/three/HeroScene.tsx` (icosaedro+octaedros low-poly/procedural), `HeroPoster.tsx` (SVG propio vía `next/image`), montada tras `Lazy3DCanvas` + `SceneErrorBoundary` dentro de `HeroSceneCanvas.tsx`, expuesta vía `HeroSceneMount.tsx`/`GatedSceneMount.tsx`; integrada en `LandingPageFull.tsx` y `HeroSection.tsx` (adelanta parte de 3.1)
- [x] 2.10 Verificar en la piloto los guardrails E2E (Browser pane, build de producción): aislamiento del chunk 3D confirmado por contenido en rutas sin 3D (`/legal/privacidad`, 0 matches en 13 chunks); gate descalificado (hardwareConcurrency=4, real en el entorno de prueba) → 0 requests al chunk 3D, poster con ambas variantes tema claro/oscuro en el DOM, `aria-hidden` correcto, consola sin errores propios; mobile → oculto vía `hidden lg:block` + gate; bug real encontrado y corregido en el camino (`GatedSceneMount`, ver 3d-budget.md); **no verificado en vivo**: camino "gate califica → Canvas real monta" (sandbox fijo en 4 cores) — cubierto por 29 tests unitarios deterministas + `tsc` limpio contra los tipos reales de R3F; LCP no medido con Lighthouse (pendiente recomendado, ver 3d-budget.md)

## 3. Fase C — Escenas 3D por superficie de alto impacto

- [x] 3.1 Landing/hero: integrar la escena hero sobre la estructura editable existente (`components/landing/*`, `LandingPageFull`/`HeroSection`) sin romper el editor de landing (`LandingManager`) — mayormente hecho en Fase B (2.9); confirmado en esta fase: `LandingManager.tsx` no renderiza `LandingPageFull`/`HeroSection` (es un editor de lista/CRUD sobre `LandingSection[]`, sin preview embebido), por lo que el bloque 3D no puede romperlo — cero solapamiento de código
- [x] 3.2 Login/registro: escena 3D de acompañamiento (fondo) — `components/three/AuthScene.tsx` (anillos toroidales concéntricos, reutiliza `heroSceneTheme` para consistencia de marca), `AuthPoster.tsx` + SVG propio, `AuthSceneCanvas.tsx`/`AuthSceneMount.tsx` (mismo patrón `GatedSceneMount` que el hero). Montada en `app/auth/login` y `app/auth/register` como capa de fondo (`z-0`, `pointer-events-none`) detrás del card (`z-10`); formulario 100% intacto y usable sin 3D
- [x] 3.3 Onboarding: **fuera de alcance real — no existe la ruta.** `surface-matrix.md` (Fase A) ya documentaba esto como "reservado, sin ruta implementada". No se fabricó un flujo de onboarding nuevo solo para colgar una escena 3D (fuera del alcance 100% presentacional de este change, que no debe inventar features de producto). La infraestructura (`GatedSceneMount`, `Lazy3DCanvas`, patrón Hero/Auth) queda lista para reutilizar el día que exista la ruta
- [x] 3.4 Empty states destacados: **degradados a `2d-motion` deliberadamente** (D8 lo permite explícitamente: "objeto 3D ligero **o degradar a 2d-motion** si excede el presupuesto de peso"). No existe un componente de empty-state dedicado en el dashboard (solo ceros cuando no hay datos); el presupuesto ya medido en Fase B (~233KB gz) excede el objetivo especulativo de D5, así que sumar una tercera escena a una superficie secundaria no se justifica. El refresh 2D/micro-animaciones de Fase A ya cubren esta superficie
- [x] 3.5 Celebración "venta cerrada": `components/three/Celebration3D.tsx` (variant="sale") + `CelebrationScene.tsx` (burst de octaedros procedurales) + `CelebrationCanvas.tsx`. Montada en `app/(dashboard)/ventas/pos/page.tsx`, disparada por `key={lastSale?.salesOrderId}`/`show={!!lastSale}` — observa el estado `lastSale` YA existente, no toca el handler/mutación de la venta. Auto-desmonta (`durationMs`, default 1800ms). Reduced-motion (vía el gate) → **sin burst**, no poster (un fallback estático no tiene sentido para un efecto efímero; el card 2D de "Venta confirmada" ya da el feedback). Overlay `pointer-events-none fixed inset-0` — nunca bloquea el flujo de cobro
- [x] 3.6 Celebración "meta alcanzada": mismo `Celebration3D` (variant="goal", paleta ámbar/dorada vía `celebrationTheme.ts`, distinguible de "sale"). Disparador: `lib/three/goalMilestone.ts` (`findCrossedMilestone`, puro) + `hooks/three/useGoalMilestone.ts`, wireado a `todaySales`/`loadingKpis` (YA existentes) en `app/(dashboard)/dashboard/page.tsx`. Política clave: la PRIMERA lectura tras cargar nunca celebra (evita "festejar" un monto que ya estaba ahí en cada reload) — solo un incremento posterior dentro de la sesión de página. Umbrales: $50k/$100k/$250k/$500k/$1M (documentados en el código; sin fetch/polling en vivo en el dashboard actual, así que en la práctica dispara solo ante un cambio de filtro de sucursal que cruce un umbral — infraestructura correcta y lista para cuando el dashboard tenga datos en vivo)
- [x] 3.7 Cada escena lee el tema activo: `HeroScene`/`AuthScene` vía `useTheme()` + `heroSceneTheme.ts` (testeado); `CelebrationScene` vía `useTheme()` + `celebrationTheme.ts` (testeado, variant+tema); todos los `Poster` (Hero/Auth) con variante `srcDark` vía CSS `dark:` (sin lag de hidratación)
- [x] 3.8 Confirmado por code-review + verificación empírica de build: se listaron TODOS los chunks referenciados por el client-reference-manifest de `/ventas/pos`, `/dashboard`, `/stock`, `/clientes`, `/ventas/ordenes`, `/facturacion` (13-18 chunks c/u) y se grepeó cada uno por contenido (`react-three|THREE\.|three/build`) — **0 matches en todos**. `Celebration3D`/`useGoalMilestone` se importan en POS/dashboard (necesario para las celebraciones 3.5/3.6) pero NUNCA el R3F/three real — solo se alcanza vía `loadScene`'s `import()` en tiempo de ejecución, nunca una referencia estática

## 4. Fase D — Pulido, accesibilidad y performance

- [x] 4.1 Barrido final de migración de color: **alcance de `v4-frontend-01` (lint transversal), no reimplementado aquí** — verificado (grep dirigido) que las superficies/archivos que este change tocó (landing, auth/login, auth/register, POS, dashboard, `components/three/*`) no introducen clases Tailwind de color hardcodeadas nuevas (0 matches); documentado en `design-tokens.md`
- [x] 4.2 Auditado accesibilidad de las 6 superficies 3D sin gate axe formal (`v4-frontend-04` no existe todavía como código en este repo — no se reimplementa): `aria-hidden` confirmado en los 4 wrappers (`Lazy3DCanvas`/`Poster`/`GatedSceneMount`/`Celebration3D`), 0 elementos focalizables dentro de ninguna escena (grep de código + verificado en browser real sobre el contenedor del hero), contenido/acciones siempre hermanos del contenedor 3D en el DOM (nunca hijos) — detalle en `3d-budget.md`. Pendiente real: corrida de axe-core/Lighthouse formal (queda para `v4-frontend-04`)
- [x] 4.3 `AdaptiveDpr`/`AdaptiveEvents` (drei) agregados a `Lazy3DCanvas` (antes instalado sin usar) — degradación de resolución antes que FPS, aplicado automáticamente a toda escena. Impacto en bundle nulo medible. FPS real en gama media y LCP con Lighthouse **no medidos** en esta sesión (sin GPU/profiling real en el sandbox) — mitigado por diseño (escenas livianas, `frameloop="demand"`, gate de capacidad); pendiente recomendado documentado en `3d-budget.md`
- [x] 4.4 Degradación verificada en un browser real de producción para 2 de las 6 superficies (landing, login: `hardwareConcurrency=4` real del sandbox → poster, sin canvas, 0 requests al chunk 3D, aria-hidden correcto) más las 4 restantes por code-review (mismo `GatedSceneMount`/`Lazy3DCanvas`/gate compartido — register, POS, dashboard×2 variantes). No se pudo forzar en vivo el camino "sin-WebGL"/"save-data"/"mobile" específicamente (limitación de la herramienta de scripting del sandbox, world aislado) — cubierto por los 12 tests de `capabilityGate.test.ts`, uno por condición
- [x] 4.5 QA visual claro/oscuro verificado en browser real: `/auth/login` — toggle de `.dark` confirma el swap correcto de poster (imagen light `display:none`↔dark `display:block` y viceversa); mismo mecanismo CSS en landing. Superficies 2D de Fase A ya verificadas en su propia fase
- [x] 4.6 `surface-matrix.md`, `design-tokens.md`, `3d-budget.md` actualizados con el estado final (esta fase). Umbrales del gate de capacidad documentados en `capabilityGate.ts`, `3d-budget.md` y engram (`opsx/v4-visual-3d-refresh/apply`)
