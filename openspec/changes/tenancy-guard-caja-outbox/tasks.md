# Tasks — `tenancy-guard-caja-outbox`

> ## 🛑 GATE DE GOVERNANCE — LEER ANTES DE TOCAR NADA
>
> **Governance: CRÍTICO** (seguridad multi-tenant + dinero + contabilidad). El PO
> autorizó **proponer**, no **aplicar**. Ninguna task de este archivo se ejecuta
> sin sign-off explícito del PO en la task **0.1**. No hay excepción "empiezo por
> lo inofensivo": el grupo 1 ya toca prod (aunque sea de lectura) y consume
> tiempo de decisión.
>
> **Strict TDD activo.** Cada task de código sigue RED → GREEN → TRIANGULATE →
> REFACTOR, con safety net previo sobre lo que se toca. Los tests van **antes**
> de la implementación, nunca después.
>
> Las tasks marcadas **[OQ-n]** dependen de la respuesta del PO a la Open
> Question correspondiente de `design.md`. Sin respuesta, se implementa la
> **recomendación**. Las marcadas 🛑 exigen mostrar el resultado al PO antes de
> seguir.
>
> **Los grupos 3 (h1) y 4-5 (h2) no comparten ningún objeto de base de datos**
> (D5): se pueden aplicar, revisar y revertir por separado. La migración los
> separa en dos secciones con encabezado propio.

## 0. Sign-off (bloqueante)

- [x] 0.1 🛑 **[OQ-0]** Obtener aprobación explícita del PO para ejecutar el apply. Presentarle: los dos hallazgos con su reproducción, los conteos de daño histórico preliminares (**0 en los tres**), y el hecho de que hay **3 sesiones de caja abiertas de 3 tenants distintos** y **10 tenants con eventos** en el outbox. Sin respuesta afirmativa registrada, **el apply no arranca**. → *Evidencia: **FIRMADA por el PO (2026-08-24)**: aprobó ejecutar el apply. Registrada en la resolución de la OQ-1 en `design.md` y en el brief del apply.*
- [x] 0.2 🛑 **[OQ-1]** Preguntar si el tramo **h2** sale como hotfix inmediato (patrón #446/#454) en vez de esperar el apply completo. *Recomendación: sí para h2 (revoke + `require_platform_admin`, PR chico con rollback de dos líneas), no para h1 (reescribir una RPC de 15 110 caracteres no se hace con urgencia de hotfix).* Si el PO elige el hotfix, los grupos **4 y 5.1-5.4** salen a un PR propio y este change conserva h1, el retiro del relay y los gates — anotarlo acá y ajustar los grupos 6 y 9. → *Evidencia: **FIRMADA por el PO (2026-08-24), por la recomendación**: h2 salió como hotfix (`fix/outbox-cross-tenant-hotfix`, PR #460, migración `20261012000001_revoke_outbox_cross_tenant.sql`), h1 quedó para este apply. Los grupos 4, 5 y 6.2-6.7 se ejecutaron allá; acá quedó h1 entero. Registrada en `design.md` §OQ-1.*
- [ ] 0.3 Registrar la decisión de 0.1 y 0.2 en `CHANGES.md` y en engram (`topic_key: opsx/tenancy-guard-caja-outbox/apply`) **antes** de escribir una línea de código.

## 1. Reconocimiento y safety net

- [x] 1.1 Verificar el **MAX de `supabase_migrations.schema_migrations` vivo en prod** (`npx supabase migration list`, o `SELECT max(version) …`). Al escribirse este propose era `20261011000001` (260 migraciones) → el archivo nace como `20261012000001_tenancy_guard_caja_outbox.sql`. ⚠️ **El número se re-verifica acá, siempre**: en `cuenta-corriente-party-guard` se movió **tres veces** porque otras ramas tomaron los intermedios (#452 tomó `20261009000001`, el hotfix #454 tomó `20261010000001`). Un archivo con número **menor o igual** al MAX remoto no lo aplica nunca el push automático de Supabase. Si prod está adelante, renumerar y anotarlo en el PR. → *Evidencia: `MAX(version)` vivo en prod re-verificado el 2026-08-24 vía MCP (sólo SELECT): **20261012000001, 261 migraciones** — el número que el propose preveía para h1 se lo llevó el hotfix de h2. La migración de h1 nace entonces como **`20261013000001_tenancy_guard_caja_sesion.sql`**. Se re-verifica otra vez antes del push.*
- [x] 1.2 Correr la suite backend completa (`python -m pytest backend/tests -q -p no:cacheprovider`) y registrar el baseline `N/N`. Referencia post-#458: **1604 passed / 3 skipped**. Cualquier fallo preexistente se **reporta**, no se arregla en este change. → *Evidencia: Baseline medido sobre `origin/main` = `5ce75cf` (con el hotfix h2 ya mergeado): **1580 passed / 3 skipped**, exit 0. Cero fallos preexistentes. OJO: el propose anota 1604/3, número ANTERIOR al hotfix (que retiró los 5 archivos de tests del relay Python y los reemplazó por los del disparador). El baseline vigente contra el que se compara h1 es **1580/3**.*
- [x] 1.3 Levantar el stack local (`npx supabase db reset`) y correr los **30 gates del workflow en el orden exacto de CI**, registrando el baseline. Prestar atención a `test_function_acl_gate.sql`, `test_errcode_5char_gate.sql`, `test_idempotency.sql`, `test_cuenta_corriente_party_guard.sql` y `test_tenancy_rls_role.sql`. → *Evidencia: 31 gates verdes en el preflight; re-corridos los **32** (los 31 + el nuevo) tras el `db reset` de la task 3.6 y la cadena de reapply completa — ver 6.9. Nota de tooling: `test_cuentas_billetera_tipo.sql` usa `\i` con ruta relativa, así que necesita el `docker cp` del repo al contenedor; corrido así, verde.*
- [x] 1.4 Reproducir la **cadena de reapply** del step "Verify G1/G4 migrations are idempotent on reapply" en local, en el orden exacto del YAML, y confirmar que fallan **sólo** los dos eslabones tolerados que el workflow documenta (`20260928000001` y `20261002000001`, cada uno por su marcador literal de gate ANTI-OVERLOAD). → *Evidencia: Cadena de 14 eslabones reproducida en el orden exacto del YAML: fallan **sólo** los dos tolerados, cada uno por su marcador literal (`GATE ANTI-OVERLOAD FAILED` en `20260928000001`; `GATE POS-BANCO-MOVIMIENTOS FAILED (1, 42725)` en `20261002000001`). Repetida tras sumar el eslabón 15 — ver 6.9.*
- [x] 1.5 🛑 **Gate de integridad de función.** Capturar el `pg_get_functiondef` **vivo en prod** de las funciones a reescribir y de las de referencia, en `openspec/changes/tenancy-guard-caja-outbox/baseline/<nombre>.sql`, con cabecera de procedencia, **`md5` y `length` verificados** (molde: `openspec/changes/archive/2026-08-23-cuenta-corriente-party-guard/baseline/`). Los valores medidos el 2026-08-23, para contrastar: → *Evidencia: Los **5 md5 y los 5 `length` de prod coinciden EXACTAMENTE** con los que registró el propose: nada cambió en prod desde el 2026-08-23. Baselines en `openspec/changes/tenancy-guard-caja-outbox/baseline/` (commit `822a2d3`), cada uno con cabecera de procedencia y md5 recomputado sobre el archivo. Los de h2 no se capturaron: ese tramo ya salió en el hotfix.*
  - `_c29_confirm_order_core(text,uuid,text,uuid,text,uuid,text,uuid,uuid)` — md5 `cecd8c5454611f267a5e131d73bf7928`, len 15110
  - `c28_register_cash_movement(uuid,numeric,text,uuid,text)` — md5 `510adc8e150fb5c315e6e9a2635eaff8`, len 2288
  - `rpc_process_outbox_batch(integer)` — md5 `e56e9eddda40754a4fc31a234a8d3309`, len 963
  - `rpc_mark_event_processed(uuid)` — md5 `b1396bac350179e570b091069738db41`, len 266
  - referencia (no se tocan): `rpc_process_outbox_dispatch(integer)` `28ef69cefc0fd0a5d112b656e7795ac6`/5933, `rpc_quick_sale` `ccb8afa0730195cb3df65807eb0a05ed`/4046, `rpc_confirm_sales_order` `38f2902380018ef717ff5b04cc711d20`/777, `rpc_create_sale_operation_v2` `0b6bcc5b6caa1a3c01e0da16518c7d35`/13914
  > Si algún md5 difiere del de arriba, **algo cambió en prod desde el propose**: parar y reportar.
- [x] 1.6 🛑 Diffear cada baseline contra su archivo de migración de referencia: `20261003000001_limpiezas_pagos_admin.sql` ~L760 (`_c29_confirm_order_core`), `20261006000001_banco_caja_historial_ajustes.sql` §5 (`c28_register_cash_movement`), `20260718000001_c25_events_outbox_reconcile.sql` L172/L203 (las dos del outbox). **Si difieren en una sola línea, reportar antes de escribir SQL.** `_c29_confirm_order_core` tiene antecedente concreto de divergencia: `compras-proveedor-cuenta-corriente` la encontró desalineada por una reescritura in-place del G3 de `20261003000001`. → *Evidencia: **IDÉNTICOS los dos.** `_c29_confirm_order_core` coincide línea por línea con `20261003000001` (335 líneas normalizadas de cada lado, cero diferencias): el antecedente de divergencia que registraba el design NO está presente hoy. `c28_register_cash_movement` coincide en cuerpo con `20261006000001`; la única diferencia es de formato (el archivo escribe la lista de parámetros en 6 líneas y `pg_get_functiondef` la aplana en 1).*
- [x] 1.7 Auditar en **prod** el estado real de permisos (`has_function_privilege` para `anon`/`authenticated`/`service_role`) de las 4 funciones a tocar más `rpc_process_outbox_dispatch` y `rpc_register_cash_movement`. Guardar la salida en `baseline/prod_acl_audit_<fecha>.md`. Recordar el gotcha #432 (prod concede EXECUTE **directo**, no vía `PUBLIC`). → *Evidencia: Salida completa en `baseline/prod_acl_audit_h1_2026-08-24.md`. Las 6 funciones con `anon=false`, `authenticated=true`, `service_role=true` en prod. Colateral del gotcha #432 al revés: en **local** `service_role` da `false` para las 6 (nacieron con `REVOKE ALL FROM PUBLIC` + `GRANT` sólo a `authenticated`), así que el gate de la migración **no** afirma nada sobre `service_role` — la verificación de ese rol se hace contra prod.*
- [x] 1.8 Confirmar por `grep` sobre `frontend/`, `backend/` y `supabase/functions/` que **ningún consumidor de aplicación** invoca `rpc_process_outbox_batch` ni `rpc_mark_event_processed` fuera de `OutboxRepository`, y que ninguna pantalla llama a `POST /outbox/process-pending`. Anotar el resultado en el PR. → *Evidencia: Cero consumidores de aplicación. `backend/routers/outbox.py` sólo las nombra en un docstring (el hotfix ya lo reapuntó al dispatch), no hay llamadas en `supabase/functions/`, y en `frontend/` sólo aparecen en `lib/database.types.ts` (generado).*
- [x] 1.9 Enumerar contra **prod** los callers vivos de `c28_register_cash_movement` por `pg_get_functiondef` (**no** por `grep` sobre archivos: hay migraciones superseded que la mencionan y no la llaman). Contrastar contra la tabla de `design.md` D1 — al escribirse el propose eran 5 funciones más el gate embebido de `20260804000003`. → *Evidencia: **5 callers reales, exactamente los de la tabla D1 del design**; ninguno nuevo ni faltante, así que el diseño de la capa 2 no cambia. Los dos que aparecían por `grep` (`_register_bank_movement`, `rpc_atomic_update_sale_operation`) confirmados como menciones en comentarios (invocaciones = 0). Sexto caller fuera de `pg_proc`: el gate embebido de `20260804000003` §(b).*

## 2. Test SQL — RED

- [x] 2.1 **RED**: crear `supabase/tests/test_tenancy_guard_caja_outbox.sql` con el molde de `test_cuenta_corriente_party_guard.sql`: un solo `DO $$ … $$`, **dos tenants sintéticos** A y B provisionados por el trigger de `auth.users` → `account_members`, sesión sintética vía `set_config('request.jwt.claims', …, true)`, guard degrade-don't-fail con `auth.uid()`, ERRCODEs de 5 chars, **sin BOM** (`head -c 3 | od -c`) y **cleanup completo al final** (verificar dos corridas seguidas sin residuos, incluidas las filas de `operation_idempotency` con `operation_kind = 'event_consumer'` y las de `email_logs`, que no cuelgan del anchor). → *Evidencia: `supabase/tests/test_tenancy_guard_caja_outbox.sql`, molde de `test_cuenta_corriente_party_guard.sql`: dos tenants sintéticos vía el trigger de `auth.users`, sesión por `set_config('request.jwt.claims', …, true)`, degrade-don't-fail con `auth.uid()`, sin BOM (`head -c 3 | od -c` → `- -`), y cleanup en DO block separado que resuelve por email. **Verificado: corre dos veces seguidas en verde y deja 0 residuos** (anchors, sucursales/cajas sintéticas, productos y `email_logs`). Las 7 filas huérfanas de `operation_idempotency` con `operation_kind='event_consumer'` que hay en el stack local son de otra sesión (mismo conteo antes y después de correr el gate, y todas con más de 10 minutos).*
- [x] 2.2 **RED [h1]**: `rpc_quick_sale` del tenant A en efectivo con `p_cash_session_id` = sesión abierta del tenant **B** → esperado `P0422`. Debe **fallar hoy**: hoy la venta se confirma y deja **1 fila nueva en `cash_movements` de la sesión de B**. Medir los movimientos de la sesión de B **antes y después** y exigir que queden idénticos — el SQLSTATE solo no alcanza, el spec pide "sin efectos parciales". → *Evidencia: RED ejecutado y fallando con el mensaje previsto (`la venta se confirma y le deja a B un ingreso fantasma en su arqueo`). El assert mide los movimientos de la caja de B **antes y después** (conteo Y suma) y exige que queden idénticos, no sólo el SQLSTATE. GREEN tras la migración.*
- [x] 2.3 **RED [h1]**: espejo por `rpc_confirm_sales_order` (orden creada y confirmada por separado) con la sesión ajena → `P0422`, y la orden **no** queda confirmada. → *Evidencia: Espejo por `rpc_confirm_sales_order` con la orden creada aparte: `P0422`, la orden sigue en `draft` y la caja de la víctima intacta. En el RED se confirmaba (`estado de la orden: confirmed`).*
- [x] 2.4 **RED [h1]**: sesión de **otra sucursal del mismo tenant** → `P0422`. Es el caso que sólo la capa 1 puede cubrir (la capa 2 lo dejaría pasar): sin este assert, D1 no está probado. → *Evidencia: El caso que **sólo la capa 1** puede cubrir: venta de la sucursal A1 imputada a la caja de la sucursal A2 del MISMO tenant → `P0422`. Sin este assert D1 no estaría probado (la capa 2 lo dejaría pasar: mismo `account_id`).*
- [x] 2.5 **RED [h1, capa 2]**: invocar `c28_register_cash_movement` directo (como `postgres`, con los claims de A) contra la sesión de B → esperado `P0401`, y 0 filas nuevas. Hoy inserta. → *Evidencia: `c28_register_cash_movement` invocada directo (como `postgres`, con los claims de A) contra la sesión de B → `P0401` y 0 filas nuevas. En el RED insertaba.*
- [x] 2.6 **RED [h2]**: con `SET LOCAL ROLE authenticated` y los claims del usuario A, `rpc_process_outbox_batch(1000)` → esperado `42501`. Hoy **devuelve eventos del tenant B**; medir cuántos devuelve del `account_id` de B y exigir 0 tras el fix. → *Evidencia: **Cubierto por el hotfix de h2** (PR #460): el invariante vive en `supabase/tests/test_outbox_single_dispatcher.sql` y en el chequeo (5) de `test_function_acl_gate.sql`. No se duplica acá.*
- [x] 2.7 **RED [h2]**: ídem con `rpc_mark_event_processed(<evento pendiente de B>)` → esperado `42501`, y el `processed_at` de ese evento **sin cambios**. Hoy lo setea. → *Evidencia: **Cubierto por el hotfix de h2** (PR #460), mismo domicilio que 2.6.*
- [x] 2.8 **RED [h2 bis]**: candado del invariante "un solo despachador" — insertar un evento de tipo con asiento (`SaleConfirmed`), correr `rpc_process_outbox_dispatch(10)`, y exigir que el evento quede `processed_at IS NOT NULL` **y** con su `journal_entries.source_event_id`. Después, control negativo: verificar que **no existe** ningún camino vivo capaz de marcar `processed_at` sin postear el asiento (assert de que `rpc_process_outbox_batch` / `rpc_mark_event_processed` ya no son alcanzables por `authenticated`). → *Evidencia: **Cubierto por el hotfix de h2** (PR #460): `test_outbox_single_dispatcher.sql` ejerce el despachador real y su control negativo.*
- [x] 2.9 **Barrido global**: assert sobre **toda** la tabla `cash_movements` (no sólo los tenants sintéticos) exigiendo 0 filas cuya sesión pertenezca a un tenant distinto del de la orden/venta que las originó. Mismo patrón que el assert (9) del gate del change anterior: convierte la auditoría del grupo 8 en candado permanente y cubre residuos de otros gates de la misma corrida de CI. → *Evidencia: Barrido global sobre TODA `cash_movements`, por los dos caminos (`reference_id` → `sales_orders` para el POS y → `sales.operation_id` para el formulario). Verde en local; en prod los dos conteos dan 0 (task 7).*
- [x] 2.10 Verificar que el archivo corre y **falla** contra el schema actual, y anotar cuáles asserts fallan. Si el gate es fail-fast, escribir un **diagnóstico independiente** read-only (`BEGIN … ROLLBACK`) versionado en `baseline/red_probe_<fecha>.sql` que pruebe cada comportamiento por separado, y volcar la tabla de resultados acá. Eso es el RED del change entero. → *Evidencia: El gate es fail-fast, así que el RED sólo mostraba 2.2. El diagnóstico independiente está versionado en `baseline/red_probe_h1_2026-08-24.sql` (`BEGIN … ROLLBACK`, read-only). Tabla de resultados contra el esquema PRE-migración: **2.2 `sin error`** (caja de la víctima 0 → 1), **2.3 `sin error`** (0 → 1 más, orden `confirmed`), **2.4 `sin error`** (caja de la otra sucursal 0 → 1), **2.5 `sin error`** (2 → 3), **3.5 control positivo `sin error`** (1 movimiento, importe 2000) y **2.9 barrido global = 2** filas corruptas fabricadas por el propio probe. Ése es el RED del tramo entero.*

## 3. Migración — h1: guard de la sesión de caja

> **Nombre final del archivo: `20261013000001_tenancy_guard_caja_sesion.sql`.**
> El `20261012000001` que preveía el propose se lo llevó el hotfix de h2
> (PR #460), así que h1 renumeró — exactamente lo que la task 1.1 anticipaba.

- [x] 3.1 Crear `supabase/migrations/20261012000001_tenancy_guard_caja_outbox.sql` (número re-verificado en 1.1) con la cabecera del proyecto: las dos familias del hallazgo, D1/D2/D7/D8, MAX de prod, procedencia del baseline, gotcha #432, nota de que `service_role` conserva su EXECUTE, "ninguna firma cambia" y la declaración **BREAKING** de dominio. **Sin BOM UTF-8.** Separar h1 y h2 en dos secciones con encabezado propio (D5). → *Evidencia: `supabase/migrations/20261013000001_tenancy_guard_caja_sesion.sql` (número re-verificado en 1.1). Cabecera con: el alcance (**sólo h1**, y por qué h2 no aparece), el hallazgo con su reproducción y su exposición medida, D1/D2/D7/D8, el `MAX` de prod, la procedencia del baseline con md5 y length, el gotcha #432, la nota de que `service_role` conserva su EXECUTE, «ninguna firma cambia» y la declaración **BREAKING** de dominio. Sin BOM. No hace falta separar h1 y h2 en dos secciones: el archivo es sólo h1.*
- [x] 3.2 **GREEN [capa 1]**: `_c29_confirm_order_core(...)` partiendo del cuerpo capturado en 1.5, con el guard **inmediatamente después de `cash_requires_session`** y **antes de la primera escritura** (D2). El predicado se **copia** de `rpc_create_sale_operation_v2`: `cs.status = 'open' AND cb.branch_id = v_gate_branch` → `P0422 cash_optin_requires_open_session`, con el mismo mensaje literal. Enumerar en la evidencia cada bloque preservado byte a byte (formas de pago, `credit_requires_client`, banco, `sales`, caja, cuenta corriente, evento). → *Evidencia: Capa 1 escrita desde el baseline vivo. **Bloques preservados byte a byte** (verificado mecánicamente, ver más abajo): resolución de kind por catálogo y camino legacy, `cash_requires_session`, `credit_requires_client`, `invalid_payment_method`, resolución y validación de sucursal, canal, idempotencia + replay, loop de stock con `branch_stock`/`sales`/`sale_items`/`stock_movements`, caja, cuenta corriente (`_pay_register_party_charge`), banco (`_pay_register_operation_bank_movement`), bloque fiscal (GATE OQ-G), `INSERT` en `events`, `record_status_transition` y el `UPDATE` final de la orden. Predicado copiado literal del formulario, mismo mensaje y mismo `P0422`. **Detalle declarado**: en ese punto del cuerpo `v_gate_branch` todavía no está asignada (la asignación está unas líneas más abajo y es literalmente `v_order.branch_id`), así que el guard compara contra `v_order.branch_id` — el mismo dato; mover la asignación habría introducido una diferencia contra el baseline que no es el guard.*
- [x] 3.3 **GREEN [capa 2] [OQ-2]**: `c28_register_cash_movement(uuid,numeric,text,uuid,text)` desde el baseline, resolviendo la cuenta por `cash_sessions → cashboxes → branches.account_id` —**copiando el `SELECT` de `rpc_register_cash_movement`** (`20261006000001` §4, L160-166), no escribiéndolo de nuevo— y exigiendo `= ANY(current_account_ids())` → `P0401 unauthorized`. **Membresía, no `is_account_writer`** (D1 (iii)). Sigue `SECURITY INVOKER`, firma intacta. → *Evidencia: Capa 2 escrita desde el baseline vivo, con el `SELECT` de resolución **copiado** de `rpc_register_cash_movement`. Sigue `SECURITY INVOKER`, firma intacta. Membresía y no `is_account_writer` (el gate lo afirma leyendo el cuerpo). **Corrección durante el GREEN**: la primera versión usaba `= ANY(current_account_ids())` y fallaba con `op ANY/ALL (array) requires array on right side` — la función devuelve `SETOF uuid`, no un array; quedó con la forma canónica del proyecto, `IN (SELECT public.current_account_ids())`. Falla cerrado si la cuenta no resuelve.*
- [x] 3.4 ACLs de las dos funciones: reafirmar tras el `CREATE OR REPLACE`. `_c29_confirm_order_core` conserva su `GRANT` a `authenticated` (está en la allowlist del chequeo (4) por decisión de OQ-3 del change anterior — el hueco era alcanzable por sus wrappers, y ahora que se cierra el hueco **revisar si la justificación de la allowlist cambia** y actualizar su comentario en `test_function_acl_gate.sql`). `c28_register_cash_movement` conserva el suyo (OQ-4: no se revoca en este change). `COMMENT ON FUNCTION` de ambas citando el guard nuevo y su ERRCODE. → *Evidencia: ACLs reafirmadas tras cada `CREATE OR REPLACE`, nombrando `PUBLIC, anon` en el REVOKE (gotcha #432) y sin nombrar `service_role`; las dos conservan su `GRANT` a `authenticated`. `COMMENT ON FUNCTION` de ambas citando el guard nuevo y su ERRCODE. **La justificación de `_c29_confirm_order_core` en la allowlist del chequeo (4) cambió y se actualizó**: decía que la entrada era correcta A PESAR de que la función era una primitiva cross-tenant por el `p_cash_session_id` sin validar; ahora que el hueco está cerrado, la entrada vuelve a apoyarse en su motivo original —lee el `account_id` de la propia `sales_order` y aplica `is_account_writer` sobre él— y recién ahora eso es cierto. Gate de ACLs verde tras el cambio.*
- [x] 3.5 **TRIANGULATE**: control positivo — venta POS en efectivo con la sesión **correcta** de la propia sucursal sigue funcionando y escribe exactamente 1 `cash_movements` de tipo `sale` con `reference_id = sales_order_id` y el importe total. Es el assert que impide que el guard rompa el POS. → *Evidencia: Control positivo verde: la venta POS en efectivo con la sesión correcta se confirma (`replayed=false`, orden `confirmed`) y escribe **exactamente 1** `cash_movements` de tipo `sale`, con `reference_id = sales_order_id` y `amount = 2000` (el total).*
- [x] 3.6 🛑 **TRIANGULATE — verificación del ERRCODE contra el gate embebido.** Confirmar empíricamente que con `P0401` la reconstrucción del esquema (`npx supabase db reset`) **no aborta**: el gate (b) de `20260804000003_fix_c28_cash_movement_balance.sql` invoca el helper sobre un anchor cuyo usuario no está en `account_members` de la cuenta del anchor, y su `EXCEPTION WHEN raise_exception` matchea **sólo `P0001`**. Verificar que el `NOTICE` de degradación aparece y que la migración sigue. **Si se eligiera `P0001`, `db reset` abortaría** — dejarlo escrito en la evidencia. → *Evidencia: 🛑 **Verificado empíricamente, y con un hallazgo que corrige el supuesto del design.** (a) `npx supabase db reset` completo: **RC=0, 262 migraciones aplicadas, cero errores** — no aborta. (b) Pero el motivo NO es el que el design suponía: en un reset limpio las migraciones se aplican en orden de timestamp, así que `20260804000003` corre mucho ANTES de que exista `20261013000001` y su gate (b) se ejercita **sin guard**, pasando normalmente. O sea que **la capa 2 no apaga el gate (b) en una DB fresca** y su cobertura no se pierde: la réplica del saldo firmado en 3.7 es cobertura ADICIONAL, no un reemplazo. (c) La elección del ERRCODE importa igual, para cualquier re-aplicación posterior al guard, y se probó con el manejador LITERAL del gate (b) en `baseline/errcode_probe_h1_2026-08-24.sql` (`BEGIN … ROLLBACK`): **CASO 1 (P0401)** → `el guard levantó P0401 y el manejador lo DEGRADÓ a NOTICE`; **CASO 2 (P0001, contrafáctico)** → `el manejador RE-LANZÓ P0001`, que en la migración real aborta el archivo y con él el `db reset`. (d) Hallazgo lateral del experimento: reaplicar `20260804000003` a mano **recrea el overload de 4 argumentos** de `c28_register_cash_movement` (lo dropea `20261006000001` L195), y con los dos vivos la llamada de 3 argumentos del gate (b) queda ambigua (`function … is not unique`) y ese gate se salta por otra razón. No afecta a CI: ese archivo **no** está en la cadena de reapply.*
- [x] 3.7 **TRIANGULATE — recuperar la cobertura que 3.6 degrada.** Replicar dentro del gate nuevo, con un tenant **bien provisionado**, la aserción de saldo firmado que el gate (b) de `20260804000003` deja de ejercitar: `opening = 1000`, luego `+500 / −200 / +300` → `balance_after` = `1500 / 1300 / 1600` (con el bug viejo de `MAX(balance_after)` el tercero daría 1800). **No se edita `20260804000003`**: es una migración ya aplicada en prod y editarla es el anti-patrón que produjo la regresión de julio. → *Evidencia: Réplica de la aserción de saldo firmado sobre un tenant bien provisionado, en una caja limpia: `opening = 1000`, luego `+500 / −200 / +300` → `balance_after` **1500 / 1300 / 1600** (con el bug viejo de `MAX(balance_after)` el tercero daría 1800). **No se editó `20260804000003`.** Detalle de implementación: el `balance_after` se lee por el `id` que devuelve el helper, no por `ORDER BY created_at DESC` — los tres movimientos comparten el `now()` de la transacción y el orden por `id` (uuid) es arbitrario; la primera versión leía la fila equivocada y daba 1300.*
- [x] 3.8 **TRIANGULATE**: verificar que 2.2 y 2.3 pasan **sin haber tocado** `rpc_quick_sale` ni `rpc_confirm_sales_order` (`grep` sobre el archivo de migración → 0 ocurrencias). Es la prueba de que los wrappers heredan el guard del core. → *Evidencia: `grep -c` sobre la migración → **0 ocurrencias** de `rpc_quick_sale` y `rpc_confirm_sales_order` en código (sólo se los nombra en comentarios y en el gate de ACLs del propio archivo, que verifica que conserven su `EXECUTE`). 2.2 y 2.3 pasan igual: heredan el guard. Reforzado con un assert de cuerpo: ninguno de los dos wrappers contiene el literal del guard (si apareciera, alguien duplicó la validación en vez de reusarla).*
- [x] 3.9 **TRIANGULATE**: `rpc_delete_sale_operation` sigue compensando caja normalmente sobre una venta propia (control de regresión de la capa 2 sobre el camino de borrado), y `rpc_register_cash_movement` sigue exigiendo `is_account_writer` (el guard duplicado no relaja nada). → *Evidencia: (a) `rpc_delete_sale_operation` sigue compensando: borrar la venta propia de 3.5 deja exactamente 1 `sale_reversal` y la caja neteada en 0 bajo la capa 2. (b) `rpc_register_cash_movement` conserva su `is_account_writer` en el cuerpo vivo (assert estático sobre el cuerpo **sin comentarios**: una mención dentro de un comentario no es una llamada — mismo falso positivo que la revisión adversarial le corrigió al chequeo (5a) del gate de ACLs).*

## 4. Migración — h2: cierre del outbox [OQ-1] ✅ CUMPLIDO EN EL HOTFIX

> **OQ-1 firmada por el PO: "h2 sale como hotfix ahora, h1 después".** Este
> grupo entero, más el grupo 5 y la parte de h2 del grupo 6, se ejecutó en el
> PR del hotfix `fix/outbox-cross-tenant-hotfix` — exactamente el desenlace
> que este archivo preveía. Migración: **`20261012000001_revoke_outbox_cross_tenant.sql`**
> (`MAX(version)` de prod re-verificado el 2026-08-24: `20261011000001`, 260
> migraciones). Los grupos de h1 (1.5/1.6/1.9, 2.x de caja, 3.x) **no se
> tocaron** y siguen pendientes en este change.
>
> ⚠️ **Consecuencia para h1**: el número `20261012000001` ya está tomado. La
> migración de h1 renumera —re-verificar el `MAX` vivo antes de escribirla,
> como manda la task 1.1— y deja de llamarse
> `20261012000001_tenancy_guard_caja_outbox.sql`.
>
> Lo que queda por hacer acá cuando arranque el apply de h1: sólo
> **verificar** que el estado sigue cerrado (mismo patrón que la
> reconciliación con #454 del change anterior). El gate final del hotfix ya
> deja esa verificación puesta y permanente.

- [x] 4.1 **GREEN**: `REVOKE ALL ON FUNCTION public.rpc_process_outbox_batch(integer) FROM PUBLIC, anon, authenticated;` — sin tocar el cuerpo. Enumerar los tres roles explícitamente (gotcha #432). `service_role` **no** se nombra: conserva su EXECUTE. → *Evidencia: `supabase/migrations/20261012000001_revoke_outbox_cross_tenant.sql` §1. ACL en local antes `authenticated=true` → después `false`; md5 del cuerpo idéntico antes y después (`b82e7ee2c5a9d33f15f64c4997e6c93f`), o sea que no se tocó ninguna definición.*
- [x] 4.2 **GREEN**: ídem para `public.rpc_mark_event_processed(uuid)`. → *Evidencia: mismo archivo §1; md5 del cuerpo intacto (`db29b2e2e7eda4ba762c716f6253e67e`).*
- [x] 4.3 `COMMENT ON FUNCTION` de ambas: abrir con **"SIN GRANT — NO AGREGAR UNO"**, explicar el vector concreto (recorren el outbox de todos los tenants sin filtro, por diseño), nombrar al único caller legítimo (el camino de servicio del disparador manual) y remitir al chequeo (5) del gate. → *Evidencia: mismo archivo §3. **Corregido en la revisión adversarial (2026-08-24, F6)**: los dos `COMMENT ON FUNCTION` sueltos abortaban el archivo si alguna de las dos funciones no existía, contradiciendo la tolerancia al drift que declara la cabecera. Ahora van dentro de un `DO … FOR … LOOP` con `CONTINUE WHEN to_regprocedure(...) IS NULL` y `EXECUTE format('COMMENT ON FUNCTION %s IS %L', …)`; el `%L` conserva el texto íntegro, incluido el `SIN GRANT — NO AGREGAR UNO` con el que abre. Verificado: md5 de los dos comentarios idéntico antes y después del cambio de forma.*
- [x] 4.4 Gate anti-overload + verificación de ACLs dentro de la migración: `count(*) = 1` por nombre, y assert de que las dos del outbox quedan `anon = false` y `authenticated = false` mientras `rpc_process_outbox_dispatch` sigue igual. Añadir la verificación —heredada del change anterior— de que `_pay_register_party_charge` y `_journal_post_from_event` **siguen** cerrados, con `RAISE EXCEPTION` apuntando a #454 si alguien los reabrió. → *Evidencia: mismo archivo §3, sub-gates (a) anti-overload, (b) NEG, (c) POS, (d) dispatcher sin cambios, (e) helpers de #454. **Desviación declarada**: el GATE POS afirma sólo `postgres`, no `service_role`. Medido el 2026-08-24: en prod `service_role` tiene EXECUTE sobre las tres RPCs del outbox y en local NO (nacieron con `REVOKE ALL FROM PUBLIC` + `GRANT` sólo a `authenticated`); como el gate corre contra local, afirmarlo rompía CI por una diferencia de entorno. Lo que importa —que ningún `REVOKE` de este archivo nombre a `service_role`— se sostiene por inspección y está documentado en el archivo. **Ampliado en la revisión adversarial (2026-08-24, F2)**: se sumó el sub-gate **(f)**, que exige que `anon` y `authenticated` no conserven `UPDATE`/`DELETE`/`TRUNCATE` sobre la **tabla** `public.events`, y que `anon` no conserve `SELECT`/`INSERT`. Sólo afirma lo negativo, por el mismo motivo que (c): el privilegio positivo difiere entre entornos (prod tiene los 7 para los dos roles; local sólo `REFERENCES`/`TRIGGER`/`TRUNCATE`).*
- [x] 4.5 Reaplicar el archivo **dos veces seguidas** en local y verificar que el segundo apply es no-op. → *Evidencia: fingerprint = md5 de (cuerpos + `anon`/`authenticated`/`postgres` + md5 del `COMMENT`) de las 5 funciones relevantes **+ los 7 privilegios de tabla de `public.events` para los 4 roles**. Tres corridas: `FP(pre) = FP(apply#1) = FP(apply#2) = 5be8dad6409a10f9098d95a891335edc`. Convergencia además desde un estado SUCIO deliberado (re-`GRANT` de las dos RPCs a `authenticated`, `GRANT SELECT/INSERT/UPDATE/DELETE/TRUNCATE` de la tabla a `anon` y `authenticated`, y los dos `COMMENT` pisados con `'drift'`): un solo apply converge y el segundo es no-op (`f8c5ceeb…` dos veces). Además la migración corre como último eslabón de la cadena de reapply de CI, con su gate en verde.*
- [x] 4.6 **[F2, revisión adversarial 2026-08-24]** `REVOKE` a nivel **tabla** sobre `public.events`: `UPDATE, DELETE, TRUNCATE` de `PUBLIC, anon, authenticated` y `SELECT, INSERT` de `PUBLIC, anon`. → *Motivo: en prod los dos roles tenían los 7 privilegios de tabla concedidos DIRECTO, y lo único que impedía cerrar un evento ajeno con un `PATCH /rest/v1/events` era que no existe policy de UPDATE ni de DELETE (un UPDATE sin policy afecta 0 filas y no da error). O sea que la puerta la cerraba la AUSENCIA de una policy, y ningún gate lo afirmaba. Evidencia: mismo archivo §2 + sub-gate (f) + el chequeo (6 bis) de `test_outbox_single_dispatcher.sql`, que además exige que no exista ninguna policy con `polcmd IN ('w','d','*')` alcanzable por `authenticated`. **`authenticated` CONSERVA `SELECT` e `INSERT` a propósito**: `OutboxRepository.emit_event` hace `INSERT … RETURNING id`, y `RETURNING` exige `SELECT` sobre la columna devuelta — revocarlo rompería toda emisión de eventos el día que se encienda el Paso 2 del pool. Verificado por `grep` que ninguna otra ruta lee `events` con el JWT del usuario (cero `.from('events')` en `frontend/`, cero `FROM public.events` en `backend/` fuera del `INSERT` de `emit_event`, cero en `supabase/functions/`, y `public.events` no está en ninguna publicación de Realtime).*

## 5. Backend — h2 bis: un solo despachador [OQ-3] ✅ CUMPLIDO EN EL HOTFIX

- [x] 5.1 **RED**: `backend/tests/outbox/test_process_pending_endpoint.py` — `POST /outbox/process-pending` con un usuario **no** admin de plataforma → 403. Debe fallar hoy (hoy devuelve 200). → *Evidencia: RED ejecutado y fallando con el mensaje exacto previsto: `AssertionError: ... Recibido: 200 {"processed":0}` / `assert 200 == 403`. 16 de los 19 tests del archivo nacieron en rojo.*
- [x] 5.2 **GREEN**: reescribir `backend/routers/outbox.py` — `get_service_conn` en lugar de `get_db_conn`, `await require_platform_admin(conn, auth)` importado de `backend.core.guards` (**reutilizar**, molde en `backend/routers/fiscal.py` L225-226), y delegar en `OutboxRepository.run_dispatch`. **Corregir el docstring**. → *Evidencia: el router quedó en 3 capas finas (router → repository → RPC), sin capa de servicio; el docstring ahora dice que el cron ejecuta `SELECT rpc_process_outbox_dispatch(100)` directo contra la DB y que este endpoint es el disparador manual. Candado: `test_docstring_del_router_corregido` falla si vuelve la frase vieja.*
- [x] 5.3 **GREEN**: agregar `OutboxRepository.run_dispatch(batch_limit)`. Retirar los cinco métodos del relay. **Conservar `emit_event`**. → *Evidencia: `run_dispatch` con default 100 (el mismo lote del pg_cron) y normalización de `None` a `0`; los cinco retiros cubiertos por un test parametrizado (`test_metodos_del_relay_retirado_no_existen`) y `emit_event` por `test_emit_event_se_conserva`. `purchase_repository` y `stock_repository` siguen verdes.*
- [x] 5.4 **GREEN**: eliminar `backend/services/outbox_relay_service.py`. Verificar por `grep` que no queda ninguna referencia fuera de tests. → *Evidencia: `git rm`; `grep` sobre `backend/` deja cero referencias vivas (la única mención restante es una línea de comentario en `backend/tests/migrations/test_events_reconcile.py` que nombra `claim_idempotency` al describir el contrato de `operation_idempotency`, no una llamada). Candado dinámico: `test_el_relay_python_ya_no_existe` exige `ModuleNotFoundError`. **Corregido en la revisión adversarial (2026-08-24, F5)**: quedaban dos tests de `test_events_reconcile.py` cuyos docstrings y mensajes de assert justificaban la existencia de `rpc_process_outbox_batch` y `rpc_mark_event_processed` con un relay Python que este PR elimina ("Python relay still works", "Python relay kept", "Python relay not deleted"). Reescritos con la razón real: las dos firmas sobreviven porque los archivos de migración no se reescriben; hoy están revocadas y sin caller de aplicación, y el inventario vivo de quién recorre el outbox es el chequeo (5) del gate de ACLs.*
- [x] 5.5 **TRIANGULATE**: admin de plataforma → 200 y el cuerpo informa `processed`. → *Evidencia: `test_admin_gets_200_con_processed` (`{"processed": 7}`) y `test_admin_llama_al_dispatcher_completo`, que afirma que el SQL emitido nombra `rpc_process_outbox_dispatch` y **no** `rpc_process_outbox_batch` ni `rpc_mark_event_processed`.*
- [x] 5.6 **TRIANGULATE — candado del contrato del que depende D3 (OQ-6).** → *Evidencia: `TestServiceConnNoAdoptaRolDeAplicacion::test_no_adopta_authenticated_con_las_dos_palancas_encendidas` fija **las dos** palancas en `True` y exige cero sentencias emitidas y cero transacciones abiertas. Complementa —no duplica— al `test_get_service_conn_has_no_request_transaction_or_claims` existente, que sólo encendía el Paso 1. Segundo candado estático: `test_el_endpoint_cuelga_del_camino_de_servicio`.*
- [x] 5.7 Reemplazar los cinco archivos de tests del relay retirado. **No borrar cobertura sin reemplazarla**: anotar dónde vive ahora cada invariante. → *Evidencia: tabla invariante-por-invariante en el PR del hotfix. Resumen: las aserciones **estáticas** (orden y presencia de los 4 consumers, etiquetas de `consumer_type`, scoping del email, sentinel de idempotencia, `FOR UPDATE SKIP LOCKED`, "nunca UPDATE/DELETE sobre `audit_logs`", aislamiento por evento) ya vivían en `backend/tests/migrations/test_events_reconcile.py` y siguen corriendo; las de **comportamiento** —que los mocks simulaban— se mudaron a `supabase/tests/test_outbox_single_dispatcher.sql`, donde se afirman contra el despachador real en Postgres real. Se agregó cobertura que NO existía en ningún lado: la prueba en runtime de que un consumer fallido deja el evento pendiente **sin efectos parciales** y sin abortar el lote (gate (3)), y de que el retry lo preserva (gate (5)). Los dos archivos que no dependían del relay (`test_journal_consumer.py`, `test_producers.py`, 78 tests) se conservan intactos. **Corregido en la revisión adversarial (2026-08-24, F3)**: la frase "ya vivían en `test_events_reconcile.py` y siguen corriendo" era verdad a medias. Ese archivo fijaba TODAS sus aserciones a `20260718000001`, y el despachador se redefinió cuatro veces después (journal, notifications, asiento-venta, delete-guard), así que el orden de los consumers, el `SKIP LOCKED` y el "nunca UPDATE/DELETE sobre `audit_logs`" se estaban verificando contra un cuerpo que hace rato no corre. Ahora hay una constante `DISPATCHER_MIGRATION_FILE` que apunta a la redefinición MÁS NUEVA (`20261005000001_delete_guard_ledgers.sql`, cuyo cuerpo se verificó byte a byte idéntico al `prosrc` vivo: md5 `b01bdc1bd52829281e96e6b191885cce`, 5730 bytes), y `TestDispatcherMigrationPointer` falla si aparece una redefinición más nueva y nadie mueve el puntero. No se usa `pg_get_functiondef` porque ese job de pytest corre **sin DB** (suite hermética, `Backend_Tests.yml` no levanta servicios): la verificación en runtime contra el despachador real es `test_outbox_single_dispatcher.sql`. Se agregó además `test_consumer_order_is_audit_email_journal_notification`, que afirma el orden 1→2→3→4 y que el `SET processed_at` va después de los cuatro, contra el cuerpo vigente. Y el **scope completo del email** pasó a verificarse en runtime: el gate SQL siembra los tres tipos in-scope (`sale_created`, `stock_adjusted`, `plan_changed`) más uno fuera, con su subject y el `COALESCE` del destinatario.*
- [x] 5.8 Correr la suite backend completa y comparar contra el baseline de 1.2. Explicar el delta. → *Evidencia: baseline **1604 passed / 3 skipped** → **1575 passed / 3 skipped**. Delta = **−29**, que cuadra exactamente: **−48** por los cinco archivos retirados (`test_relay_select` 13, `test_email_consumer` 13, `test_audit_consumer` 9, `test_e2e_outbox` 7, `test_idempotency` 6) **+19** del archivo nuevo. Cero fallos colaterales y cero cambios en los 3 skipped. **Actualizado tras la revisión adversarial (2026-08-24)**: **1575 → 1580 passed / 3 skipped**, delta **+5**, todo en `test_events_reconcile.py` (56 → 61): +3 de `TestDispatcherMigrationPointer`, +1 de `test_consumer_order_is_audit_email_journal_notification`, +1 por partir la aserción de append-only de `audit_logs` en "cuerpo vigente" (UPDATE y DELETE) más "migración de origen" (las dos juntas). Baseline pre-cambio re-medido con el working tree limpio para no discutirlo: 1575/3.*

## 6. CI — gates y cadena de reapply

> **Parcialmente cumplido en el hotfix de h2** (6.2-6.7 completas). Lo que
> queda es la parte de h1: su propio eslabón de reapply y el step de
> `test_tenancy_guard_caja_outbox.sql`. El chequeo (5) del gate de ACLs **ya
> está puesto y verde**, y el gate de comportamiento del outbox
> (`test_outbox_single_dispatcher.sql`) ya corre en CI con step propio.

- [x] 6.1 Agregar `20261012000001_tenancy_guard_caja_outbox.sql` como **último eslabón** de la cadena del step "Verify G1/G4 migrations are idempotent on reapply", con comentario propio. Va último por el mismo motivo que el eslabón anterior: el reapply de migraciones viejas re-otorga GRANTs en silencio (`20261001000001` L137, `20261004000001` L1778), y el gate final de esta migración **audita** el estado que dejan los eslabones previos, incluido el de #454. → *Evidencia: Agregada como **último eslabón**, con comentario propio. Acá el orden no es convención sino precondición dura: **cuatro** eslabones de más arriba redefinen una u otra de las dos funciones (`20261002000001` y `20261003000001` el core, `20261004000001` el core, `20261006000001` el helper), así que puesta antes la cadena le borraría los dos guards en silencio — el mismo mecanismo «phantom regression» de los GRANTs, pero sobre cuerpos de función. Y su gate final audita el estado acumulado (los cierres de #454 y #460), que sólo tiene sentido leer después de esos dos eslabones.*
- [x] 6.2 Descubrir la lista **real** de funciones que leen (`FROM public.events`) o actualizan (`UPDATE public.events`) el outbox, contra la DB de CI post-migraciones **y** contra prod. Comparar y anotar diferencias. → *Evidencia (2026-08-24): **exactamente 4 en prod y las mismas 4 en local**, con firmas idénticas — `rpc_process_outbox_dispatch(p_batch_limit integer)` (no expuesta, modelo correcto), `rpc_process_outbox_batch(p_batch_limit integer)` y `rpc_mark_event_processed(p_event_id uuid)` (expuestas, las cierra el hotfix) y `rpc_atomic_update_sale_operation(...11 args...)` (expuesta y legítima). **Cero diferencias entre prod y local**, que era justamente el riesgo a descartar.*
- [x] 6.3 **RED**: agregar el **chequeo (5)** y verificar que falla listando exactamente los offenders. → *Evidencia: en vez de dejar la lista vacía (que habría listado también las dos legítimas y probado poco), el RED se hizo con dos controles positivos independientes en `BEGIN … ROLLBACK` — ver 6.7. El (5) quedó partido en (5a) "no enumerada" y (5b) "enumerada como no expuesta pero alcanzable", y los dos se probaron en rojo por separado.*
- [x] 6.4 **GREEN [OQ-7]**: poblar la lista con las 4 entradas y su **veredicto**. Criterio: leer o actualizar, **no** insertar. → *Evidencia: `v_cross_tenant_event_fns` (4 entradas, cada una con su veredicto en el comentario de al lado) + `v_cross_tenant_event_exposed_ok` (1 entrada: `rpc_atomic_update_sale_operation`, con la justificación de por qué su acceso al outbox está acotado a la operación que ella misma ya validó por tenant). Gate verde: "4 funciones que recorren el outbox enumeradas, de las cuales 1 con exposición justificada".*
- [x] 6.5 Documentar en el encabezado del gate el porqué del chequeo (5), el punto ciego que cubre, el criterio leer/actualizar, por qué **no** se intentó el chequeo de "account_id sin filtrar", y la regla de mantenimiento. → *Evidencia: bloque de comentario nuevo en la cabecera de `supabase/tests/test_function_acl_gate.sql`, con los cuatro puntos y el "lo que el (5) NO puede hacer, dicho en voz alta". **Ampliado en la revisión adversarial (2026-08-24, F1)**: se documenta paso a paso el detector, con los cuatro vectores de evasión que la primera versión dejaba pasar, el falso positivo que producía, y por qué `pg_depend`/`pg_rewrite` NO es una alternativa (plpgsql es opaco para el planner: la query devuelve 0 filas habiendo 4 funciones que recorren el outbox).*

- [x] 6.10 **[F1, revisión adversarial 2026-08-24]** Rehacer el detector del chequeo (5a): era un regex anclado a `FROM`/`JOIN`/`UPDATE` sobre `prosrc` crudo, evadible y con falsos positivos. → *Ahora: (1) se le quitan al cuerpo los comentarios (`--` de línea y bloques `/* */`), (2) se le restan las ocurrencias de `INSERT INTO [ONLY] [public.]events` —los productores legítimos, que son decenas—, y (3) se busca el identificador como palabra completa (`\mevents\M`), sin exigir de qué cláusula cuelga. **El conjunto resultante son EXACTAMENTE las mismas 4 funciones de siempre** (`rpc_process_outbox_dispatch`, `rpc_process_outbox_batch`, `rpc_mark_event_processed`, `rpc_atomic_update_sale_operation`): ni una de más, ni un productor mal clasificado. Prueba viejo-vs-nuevo con 6 funciones sonda en `BEGIN … ROLLBACK` (`SECURITY DEFINER`, revocadas de `anon`, con `GRANT` a `authenticated`, sin enumerar), cada una corriendo el gate REAL: comma-join `FROM public.audit_logs a, public.events e` → viejo `f` / nuevo `t`; identificador citado `FROM "public"."events"` → `f`/`t`; `UPDATE ONLY public.events` → `f`/`t`; `EXECUTE format('UPDATE %I.events …')` → `f`/`t`; función que sólo nombra el outbox en un comentario → viejo `t` (falso positivo) / nuevo `f`; productor puro (`INSERT INTO public.events`) → `f`/`f`. Contra el gate completo: los 4 vectores dan `rc=3` con `ACL GATE (5a) FAILED`, el comentario y el productor dan `rc=0` con `ACL GATE OK`. Cleanup verificado: `select count(*) from pg_proc where proname like 'rpc_probe%'` = **0**.*
- [x] 6.6 **TRIANGULATE**: control negativo — el chequeo (5) **no** reporta a los productores de eventos, y los chequeos (1)(2)(3)(4) siguen igual. → *Evidencia: en `BEGIN … ROLLBACK`, una función `SECURITY DEFINER` nueva que sólo hace `INSERT INTO public.events` (revocada, para no disparar los chequeos (2) y (4)) deja el gate **verde**. Y los 31 gates del workflow corren en verde sobre una DB reseteada + la cadena de reapply completa, con el (1)(2)(3)(4) dando los mismos conteos que el baseline (5 firmas / 3 helpers / 1 firma interna).*
- [x] 6.7 **Control positivo de cada chequeo nuevo**, en `BEGIN … ROLLBACK` por separado. → *Evidencia, tres controles: (a) `GRANT EXECUTE … TO authenticated` sobre `rpc_process_outbox_batch` → **(5b) falla** listando exactamente esa firma; (b) una función `SECURITY DEFINER` nueva que hace `SELECT ... FROM public.events`, revocada y sin enumerar → **(5a) falla** listándola; (c) tras cada `ROLLBACK`, `has_function_privilege('authenticated', ...)` vuelve a `false` y no queda ninguna función `rpc_probe%`. Además el gate de comportamiento nuevo tiene su propio control positivo por **mutación**: neutralizar `_journal_post_from_event` en una transacción hace fallar su gate (3) —"quedó cerrado sin su asiento"—, que es la prueba de que ese assert no pasa en falso.*
> Nota de 6.8/6.9 (hotfix de h2): el step del gate de **comportamiento** del
> outbox ya está puesto — `Run outbox single-dispatcher gates`, con
> `-v ON_ERROR_STOP=1` y comentario propio; el YAML valida con `yaml.safe_load`
> (37 steps). El chequeo (5) no necesitó step: vive dentro de
> `test_function_acl_gate.sql`, que ya corría. Lo que sigue pendiente de 6.8 es
> el step del gate de **h1** (`test_tenancy_guard_caja_outbox.sql`), que aún no
> existe.
>
> **[F4, revisión adversarial 2026-08-24]** `test_outbox_single_dispatcher.sql`
> pasó a correr entero dentro de un `BEGIN … ROLLBACK` propio. El motivo: el
> despachador elige su lote por `processed_at IS NULL` **sin filtro de cuenta**
> —es un relay—, así que cada corrida cerraba TODO el outbox pendiente de la
> base y no sólo el de la cuenta sintética, y el chequeo (7) de "cero residuos"
> no lo veía porque sólo mira esa cuenta. En CI daba igual (`db reset`), en el
> stack local COMPARTIDO le drenaba el outbox a otra sesión. El encabezado que
> afirmaba "sin tocar ninguna cuenta real" decía algo falso y quedó corregido.
> El rollback es correcto porque la RPC es una `FUNCTION` plpgsql y no puede
> commitear por su cuenta (verificado sobre el cuerpo vivo: cero ocurrencias de
> `COMMIT`/`ROLLBACK`/`dblink`/`pg_background`). Medido: con el rollback, el
> conjunto de eventos pendientes AJENOS queda idéntico antes y después
> (`6 / md5 58d3c6d566dce7f49f5ba55707167706` en las dos puntas). El comentario
> del step en el YAML lo dice, y el step sigue siendo el último.

- [x] 6.8 Agregar el step `Run tenancy-guard-caja-outbox gates` con `-v ON_ERROR_STOP=1` (sin la flag, `psql` imprime los `RAISE EXCEPTION` y sale 0 — los asserts fallidos se verían verdes), con comentario de qué cubre. Validar el YAML con `yaml.safe_load`. → *Evidencia: Step `Run tenancy-guard-caja-outbox gates` con `-v ON_ERROR_STOP=1`, último del workflow, con comentario de qué cubre. YAML validado con `yaml.safe_load`: 1 job, **39 steps**, los tres últimos `Run cuenta-corriente-party-guard gates` → `Run outbox single-dispatcher gates` → `Run tenancy-guard-caja-outbox gates`.*
- [x] 6.9 Con un `npx supabase db reset` limpio, correr la cadena de reapply completa en el orden exacto de CI y después **los 31 gates**, y verificar que están todos verdes. El gate nuevo debe correr **dos veces seguidas en verde** sin residuos (candado del cleanup de 2.1). → *Evidencia: Sobre un `npx supabase db reset` limpio (RC=0, 262 migraciones): cadena de reapply de **15 eslabones** en el orden exacto del YAML —fallan sólo los dos tolerados, cada uno por su marcador literal; el eslabón 15 (esta migración) aplica limpio— y después **los 32 gates en verde**, en orden de CI. El gate nuevo corre **dos veces seguidas en verde** sin residuos. Idempotencia: fingerprint de (cuerpos normalizados + `anon`/`authenticated`/`postgres` + md5 del `COMMENT`) de las 5 funciones relevantes → `FP(pre) = FP(apply#2) = FP(apply#3) = 16962cab9c2fe079f6864eb04f0a43e1`. **Verificación mecánica del gate de integridad** (`baseline/verify_body_diff_h1.py`): se extraen los dos cuerpos de la migración, se les quita el bloque de guard y las variables que declara, y se diffean contra el baseline → **byte idénticos, 15110 y 2288 bytes**, exactamente los `length` que reporta prod.*

## 7. Auditoría del daño histórico en prod (read-only)

> **Sólo `SELECT`.** Los tres conteos preliminares del propose dieron **0**; se re-miden acá y se guarda la salida completa en `baseline/prod_damage_audit_<fecha>.md`.
>
> **Resultado (2026-08-24, MCP, sólo SELECT): 0 en 7.1, 7.2, 7.3 y 7.4.** El
> hueco de h1 está abierto y es alcanzable —los 65 movimientos de caja vivos
> pasaron por el único camino sin guard y hay 3 sesiones abiertas de 3 tenants
> distintos—, pero **todavía no produjo datos corruptos**: no hay reparación
> histórica pendiente y la **OQ-5 queda cerrada por ausencia de datos**, igual
> que en `cuenta-corriente-party-guard`. Del PO sólo resta darse por enterado
> (7.6). La 7.7 no aplica.

- [x] 7.1 Movimientos de caja cuya sesión pertenece a un tenant distinto del de la **orden** que los originó: → *Evidencia: **0** (re-medido en prod el 2026-08-24, sólo SELECT).*
  ```sql
  SELECT count(*) FROM public.cash_movements cm
    JOIN public.cash_sessions cs ON cs.id = cm.session_id
    JOIN public.cashboxes cb     ON cb.id = cs.cashbox_id
    JOIN public.branches b       ON b.id  = cb.branch_id
    JOIN public.sales_orders so  ON so.id = cm.reference_id
   WHERE so.account_id IS DISTINCT FROM b.account_id;
  ```
- [x] 7.2 Espejo por **venta** (el camino del formulario, `reference_id = operation_id`): → *Evidencia: **0** (ídem).*
  ```sql
  SELECT count(*) FROM public.cash_movements cm
    JOIN public.cash_sessions cs ON cs.id = cm.session_id
    JOIN public.cashboxes cb     ON cb.id = cs.cashbox_id
    JOIN public.branches b       ON b.id  = cb.branch_id
    JOIN public.sales s          ON s.operation_id = cm.reference_id
   WHERE s.account_id IS DISTINCT FROM b.account_id;
  ```
- [x] 7.3 Eventos con `processed_at` seteado que **nunca** produjeron su asiento (síntoma del consumidor lesivo): → *Evidencia: **0** (ídem). Ya lo había medido el hotfix de h2; se re-mide acá para cerrar el grupo.*
  ```sql
  SELECT count(*) FROM public.events e
   WHERE e.processed_at IS NOT NULL
     AND e.event_type IN ('SaleConfirmed','PurchaseCreated','SaleOperationCreated',
                          'SaleOperationAdjusted','PaymentReceived','PaymentMade',
                          'CreditNoteIssued','SaleOperationDeleted','PurchaseDeleted')
     AND NOT EXISTS (SELECT 1 FROM public.journal_entries j WHERE j.source_event_id = e.id);
  ```
- [x] 7.4 Espejo del anterior para el consumer de notificaciones (`CashSessionClosed`, `StockBelowMinimum`, `FiscalDocumentRejected`, `QuoteAccepted`, `TransferDispatched` procesados sin fila en `notifications`), descontando el caso legítimo de cierre de caja con diferencia cero. → *Evidencia: **0 eventos procesados sin que su consumer corriera.** No hay join directo (`notifications` no tiene `source_event_id`), así que se midió por el registro del consumer: **156** eventos de tipo notificable procesados = 155 `StockBelowMinimum` + 1 `CashSessionClosed` con `difference = 0` (el caso legítimo que NO produce notificación), y **155** filas de `operation_idempotency` con `consumer_type='Notification'` — o sea que el Consumer 4 corrió para todos los que correspondía. Observación lateral, ajena a este change: hay 149 filas en `notifications` contra esas 155 corridas del consumer; la diferencia es una decisión DENTRO del consumer (dedupe/audiencia/retención), no una supresión por un relay rival, que es lo que este assert busca.*
- [x] 7.5 Contexto de los ceros, para que no sean triviales: `count(*)` de `cash_movements`, `cash_sessions` (y cuántas abiertas), `events` (y cuántos pendientes), `journal_entries`, y `count(DISTINCT account_id)` de eventos. Referencia del propose: 65 / 4 (3 abiertas) / 626 (0 pendientes) / 465 / 10. → *Evidencia: Contexto de los ceros, en prod: `cash_movements` **65**, `cash_sessions` **4** (**3 abiertas**), `events` **626** (**0 pendientes**), `journal_entries` **465**, tenants con eventos **10**. Coincide exactamente con la referencia del propose: los ceros no son triviales (el ledger de caja existe, hay 3 cajas abiertas de 3 tenants distintos y el hueco es alcanzable — simplemente todavía no se explotó).*
- [ ] 7.6 🛑 **Checkpoint**: reportar los conteos al PO. Esperado: **0** en 7.1-7.4.
- [ ] 7.7 🛑 **[OQ-5, sólo si 7.6 > 0]** Script de reparación en `scripts/sql/`, **nunca** dentro de `supabase/migrations/`, gateado por conteos re-medidos inmediatamente antes y firmado por el PO. Atención a RN-99: un movimiento fantasma en una sesión ya **cerrada con arqueo firmado** no se borra — se corrige con contra-movimiento.

## 8. Verificación post-merge (contra prod, sólo `SELECT`)

- [ ] 8.1 Confirmar en prod que `MAX(version)` es `20261012000001` (o el número final tras la re-verificación de 1.1) y el total de migraciones.
- [ ] 8.2 Confirmar por `has_function_privilege` contra **prod** (gotcha #432): `rpc_process_outbox_batch` y `rpc_mark_event_processed` → `anon=false`, **`authenticated=false`**, `service_role=true`; `rpc_process_outbox_dispatch` sin cambios; `_c29_confirm_order_core` y `c28_register_cash_movement` con su `authenticated=true` intacto.
- [ ] 8.3 Confirmar en prod que las dos funciones reescritas tienen **una sola** definición por nombre (gotcha 42725) y que su cuerpo **vivo** contiene el guard: `cash_optin_requires_open_session` en `_c29_confirm_order_core`, y la resolución por `branches` + `current_account_ids()` en `c28_register_cash_movement`.
- [ ] 8.4 Re-correr la query de 6.2 contra prod y confirmar que la lista de funciones que recorren el outbox coincide **exactamente** con la del chequeo (5), sin entradas sobrantes.
- [ ] 8.5 Verificar en prod que el pg_cron job `relay-process-outbox` sigue activo y que `events` no acumula pendientes (`processed_at IS NULL` estable cerca de 0) tras el deploy: es la prueba de que retirar el relay Python no rompió el despacho real.
- [ ] 8.6 🛑 **Demo al PO**: una venta POS en efectivo real, y el intento de disparar `POST /outbox/process-pending` con un usuario no admin (403). Requiere sesión autenticada.
- [ ] 8.7 Registrar en `CHANGES.md` el estado final y actualizar el puntero "Próximo change recomendado" del `CLAUDE.md`. Verificar que **todo** se mergeó vía PR — cero commits directos a `main`, incluidos los fixes triviales post-merge.
- [ ] 8.8 Guardar en engram (`topic_key: opsx/tenancy-guard-caja-outbox/apply`) el resultado: decisiones tomadas, número final de migración, resultado de la auditoría, y cualquier hallazgo lateral nuevo para `CHANGES.md`.

## 9. Revisión adversarial del apply (2026-08-24)

> Cinco hallazgos verificados sobre el apply de h1 (dos pares eran el mismo hallazgo reportado por dos lentes distintas, así que son **tres** defectos reales). Los tres son de **severidad baja** y **ninguno** toca el invariante de tenencia ni abre superficie explotable: dos son afirmaciones del texto más fuertes que el código, y el tercero es una consecuencia de comportamiento que no estaba declarada. En un change de governance **CRÍTICO** eso igual se corrige antes del merge: la cabecera de la migración y el `COMMENT ON FUNCTION` son los artefactos que lee la persona que revisa el diff, y una premisa medida-y-falsa ahí degrada la confianza en el resto — que sí carga afirmaciones de seguridad (md5 del baseline, posición de los guards, ACLs).

- [x] 9.1 **RED — los dos asserts que faltaban.** Verificar contra el cuerpo **VIVO PRE-GUARD** (restaurado desde `baseline/_c29_confirm_order_core.sql` dentro de `BEGIN … ROLLBACK`) que los dos escenarios nuevos se comportan del modo viejo, o sea que los asserts fallan sin el guard. → *Evidencia: probe en el scratchpad, dos tenants sintéticos, `request.jwt.claims`, md5 del cuerpo confirmado en `f` (sin guard) al entrar y en `1246f9e123e3d79700bad25a8494b16c` (con guard) tras el `ROLLBACK`. Salida literal: `(2.6) ACEPTADO — sin error`; `(3.12-original) replayed=false`; `(3.12-a sesion abierta) replayed=true`; `(3.12-b sesion CERRADA) ACEPTADO — sin error`; `(2.6) movimientos en la caja de la victima: 0`. Los dos asserts nuevos son RED contra el baseline.*
- [x] 9.2 **GREEN + TRIANGULATE — asserts (2.6) y (3.12) en `test_tenancy_guard_caja_outbox.sql`.** → *Evidencia: **(2.6)** el invariante de tenencia es **agnóstico del kind** — `rpc_quick_sale` con `p_payment_method => 'transfer'` y la sesión abierta del tenant B rebota con `P0422` y deja el arqueo de la víctima intacto (conteo + suma antes/después). Cierra el modo de falla de que alguien "arregle" el guard metiéndolo dentro del `IF v_kind = 'cash'` y reabra el hueco para todo pago no-cash. **(3.12)** semántica declarada del replay, en dos mitades: (a) reintento con la sesión **todavía abierta** → `replayed = true` y **un solo** movimiento de caja (el guard no rompe la idempotencia en el camino que el POS produce); (b) reintento con esa misma sesión **ya cerrada** → `P0422`, sin tocar la caja. Fixtures nuevos `__gate_tgc_cashbox_a4__` / `v_session_a4` (caja propia, porque su sesión se cierra en medio del assert) y `v_pm_transfer_a`. Gate en verde: **12 PASS**, dos corridas seguidas sobre la misma base y **cero residuos** (`auth.users like 'tenancy-guard-caja-%'`, `cashboxes like '__gate_tgc%'`, `operation_idempotency like 'gate-tgc-%'`, `email_logs`, `products sku='GATE-TGC-1'` → todos 0).*
- [x] 9.3 **Hallazgo (C) — la cabecera afirmaba que el gate (b) de `20260804000003` "queda auto-degradado en toda DB fresca"; medido, no lo queda.** → *Evidencia: se ejercitó el bloque `(b)` **literal** contra los dos cuerpos del helper, en `BEGIN … ROLLBACK`: con el **baseline pre-guard** (el que existe cuando esa migración corre en un `db reset`, por orden de timestamp) el gate **se ejercita y asertó 1500 / 1300 / 1600**; con el **guard puesto** degradó a `NOTICE` por `P0401`. Y `grep -n "migrations/2026" .github/workflows/KPI_Validation.yml` da 15 archivos, de `20260924000001` a `20261013000001`: `20260804000003` **no** está en la cadena de reapply (aparece sólo en un comentario). O sea que la cobertura **no se pierde** y la task 3.7 es cobertura **ADICIONAL**. La elección de `P0401` sobre `P0001` **sigue siendo obligatoria** para cualquier re-aplicación manual posterior al guard (el contrafáctico ya estaba probado en `baseline/errcode_probe_h1_2026-08-24.sql`). Corregido en **7 puntos / 5 archivos**: cabecera de la migración; cabecera del gate, comentario inline de (3.7) y su mensaje de `PASS`; comentario del step en `KPI_Validation.yml`; fila de la tabla D1, párrafo "único costo real" y OQ-2 de `design.md`. El `tasks.md` (3.6) y `baseline/errcode_probe_h1_2026-08-24.sql` **ya lo decían bien** — la corrección estaba escrita y no se había propagado.*
- [x] 9.4 **Hallazgo (A) — la paridad con el formulario es PARCIAL y el texto la afirmaba total. Decisión: no replicar, declarar.** → *Evidencia: sobre el mismo `p_cash_session_id` el formulario aplica **tres** guards (`cash_optin_requires_cash_kind` L275-278, `cash_optin_requires_open_session` L286, `cash_optin_requires_today` L291 del baseline) y la capa 1 replica **uno**: el de tenencia. Reproducido: POS con `kind='transfer'` + sesión propia abierta de la misma sucursal → **aceptado**, y el id se descarta sin escribir nada; el formulario con el mismo payload → `P0422 cash_optin_requires_cash_kind`. **Decisión: NO se replican**, y queda escrito en vez de quedar como omisión. Razones: (1) son higiene de input, no tenencia — el consumo de la sesión vive dentro de `IF v_kind = 'cash'`, así que con kind no-cash no hay escritura posible, y el rechazo por tenencia funciona igual (assert 2.6); (2) `cash_optin_requires_today` se cumple por construcción en el POS, que fija la fecha con `reporting_local_today()` en el propio `INSERT`; (3) replicarlos **agranda** el BREAKING de un change CRÍTICO sobre payloads que ninguna superficie del producto genera (`pos/page.tsx` manda `cash_session_id` sólo con `resolvedKind === "cash"`), a cambio de **cero** seguridad — y el sign-off del PO fue para cerrar h1, no para endurecer la higiene de input. Corregido el texto que sobre-afirmaba: comentario del bloque de guard y `COMMENT ON FUNCTION` (artefacto consultable en prod) ahora dicen **qué** predicado se copia y **cuáles** no, y por qué. Anotado como candidato en D2.*
- [x] 9.5 **Hallazgo (B) — la razón (i) de D2 era falsa contra el baseline que el propio change capturó, y la consecuencia sobre el replay no estaba declarada. Decisión: conservar la posición, corregir la razón, declarar la consecuencia.** → *Evidencia: D2 decía "coherencia con el formulario, donde el bloque `cash_optin_*` está exactamente ahí". Es al revés: en `baseline/rpc_create_sale_operation_v2.sql` la idempotencia está en L113-137 y el bloque `cash_optin_*` en L274-292 —**después** del replay—, y la propia cabecera de ese baseline lo dice (`"al final del cuerpo"`). **Pero el encuadre del hallazgo también era falso, y eso es lo que decide la respuesta**: el replay del POS nunca fue "devolver sin revalidar". El cuerpo **vivo de prod** ya corría antes del `INSERT` de idempotencia (L162) `is_account_writer` (L62), `payment_method_inactive` (L88, P0400), `cash_requires_session` (L122, P0400) y **`branch_closed` (L153, P0422)**. Medido contra el **baseline pre-guard**: `(A) replay con SUCURSAL CERRADA: RECHAZADO P0422 / branch_closed` — ya rebotaba **antes** de este change. El guard hereda la propiedad, no la inaugura. **Se conserva la posición**: mover sólo este guard partiría el bloque en dos y dejaría el contrato de `Idempotency-Key` roto igual para las otras tres precondiciones. Riesgo acotado y verificado: `mutations: { retry: false }` en `query-provider.tsx`, el POS reconstruye el payload en cada submit con la sesión abierta viva (`pos/page.tsx` L449), y `resetIdempotencyKey()` sólo corre en el submit exitoso — o sea que un `P0422` en el reintento **no puede** inducir una venta duplicada. Corregida la razón (i) de D2, declarada la consecuencia en el BREAKING de la migración y en D2, y **congelada con el assert 3.12**. **Restaurar el contrato completo (mover el bloque ENTERO después del replay, más el reanclado del candado 3.2) queda como change propio**, junto con la paridad de guards de 9.4 — los dos tocan el mismo bloque.*
- [x] 9.6 **Verificación de no-regresión de las correcciones.** → *Evidencia: `verify_body_diff_h1.py` **rc=0** tras editar los comentarios **dentro** del bloque de guard (van entre los marcadores box-drawing, que es lo único que el script descuenta): `_c29_confirm_order_core` 15110 bytes y `c28_register_cash_movement` 2288 bytes, byte-idénticos al baseline vivo de prod una vez quitado el guard. Migración re-aplicada en local sin error (idempotente, sin `DROP`, firma intacta, ACLs reafirmadas). Gate en verde tras la re-aplicación.*

## 10. Segunda ronda adversarial del apply (2026-08-24)

> Tres hallazgos verificados, los tres de severidad **baja** y ninguno explotable: el primero es un guard cuya corrección dependía de un chequeo ajeno al change y no congelado por ningún test; el segundo agrandaba la superficie de salto silencioso del único candado permanente del change; el tercero es otra afirmación de texto más fuerte que el código, de la misma clase que la ronda 1 corrigió en siete puntos. Se corrigen los tres antes del merge, por la misma razón: en governance CRÍTICO los artefactos que lee el revisor humano no pueden afirmar lo que no verifican.

- [x] 10.1 **RED + GREEN — el guard de la capa 1 pasa a ser autosuficiente.** El predicado `cb.branch_id IS DISTINCT FROM v_order.branch_id` se satisface con la caja de la víctima cuando el atacante manda TAMBIÉN su sucursal: en `rpc_quick_sale` la sucursal de la orden es `COALESCE(p_branch_id, c26_default_branch(...))` y va al INSERT en `sales_orders` sin chequeo de cuenta (`pg_constraint` sobre `sales_orders` no tiene CHECK ni FK que ligue `branch_id` con `account_id`). Lo único que frenaba la escritura era el `branch_not_found` (P0404) de unas líneas más abajo — ajeno a este change y no congelado por ningún assert: borrar hoy su `AND account_id = v_account_id` no hacía fallar nada y reabría h1 por ese camino. → *Evidencia: **RED medido** con los asserts nuevos contra el guard tal como estaba — `(2.7)` `rpc_quick_sale(p_branch_id => sucursal de B, p_cash_session_id => sesión abierta de B, kind cash)` rebotaba con **`P0404`**, no con `P0422`. **GREEN**: el SELECT del guard JOINea `branches` y exige `b.account_id = v_account_id`; los dos invariantes —caja de la sucursal efectiva Y del tenant que llama— quedan establecidos por el guard solo. **No se movió de posición** (se descartó la sugerencia de bajarlo debajo del `branch_closed`: con el guard corriendo después, el `branch_not_found` se evalúa primero y el caso seguiría devolviendo P0404 — o sea que el movimiento y el assert que lo acompañaba eran incompatibles entre sí). Así el candado de posición (3.2) no se reancla y el descuento por marcadores de `verify_body_diff_h1.py` sigue valiendo. **TRIANGULATE**: `(2.8)`, el espejo por `rpc_confirm_sales_order` con la sucursal ajena ya PERSISTIDA en la orden (IDOR de escritura) → también `P0422`. Los dos asserts miden además cero efectos sobre el arqueo de la víctima. Gate: **14/14 PASS**.*
- [x] 10.2 **El fixture `v_pm_transfer_a` sale de la condición de degradación global del gate.** Lo usa un solo assert (2.6, el más periférico) y estaba en el `IF … RETURN` global, así que una regresión del catálogo apagaba EN SILENCIO los asserts 2.2-2.5 y 2.7-2.8 —los que cubren el invariante que el change existe para cerrar— y los candados 2.9/3.2/3.3/3.8/3.9, con CI en verde. → *Evidencia: reproducido por mutación (`AND kind = 'transfer'` → `'transfer_NO_EXISTE'`): **antes**, `0 PASS / 0 FAILED / RC=0`; **después**, `13 PASS + SKIP (2.6) / RC=0`. Sin la mutación, 14/14. Se resolvió con un `IF … ELSE` dentro del propio 2.6 que emite un `SKIP` visible, no con un `RAISE EXCEPTION`: el gate tiene mandato explícito de degrade-don't-fail (task 2.1) y convertir un hipo de provisioning en CI roja sería peor. **Nota para el revisor**: el gate precedente `supabase/tests/test_cuenta_corriente_party_guard.sql` (change anterior, ya mergeado) tiene una instancia PEOR del mismo patrón — su `v_pm_transfer` condiciona el gate entero y **no lo usa ningún assert** (3 hits: declaración L123, SELECT L217, condición global L219). No se toca acá: es artefacto de otro change y ampliaría el diff de uno CRÍTICO. Queda anotado como candidato.*
- [x] 10.3 **El chequeo (d) de la migración implementa la mitad que su comentario afirmaba.** La cabecera decía "siguen expuestos y SIN el guard escrito", pero el código sólo evaluaba `has_function_privilege(..., 'EXECUTE')`; el `RAISE NOTICE` de cierre repetía la afirmación. El invariante sí estaba cubierto por el assert 3.8 del gate (que corre en CI), pero no por la migración aplicada sola a mano. → *Evidencia: **RED medido** — se corrió el predicado literal de (d) agregándole `_c29_confirm_order_core`, que SÍ tiene el guard: `v_bad = NULL` (pasa), con `position(guard in core, sin comentarios) = 3860`. El predicado es ciego al cuerpo, probado y no argumentado. **GREEN**: se implementó la mitad faltante como chequeo **(b bis)**, junto a los demás chequeos de cuerpo y **antes** del `RETURN` por entorno sin roles de Supabase, así corre también donde (d) ni se evalúa; lee `pg_get_functiondef` con los `--` removidos, mismo patrón que el 3.8. **Verificado por mutación** (`BEGIN … ROLLBACK`): cuerpos vivos → no dispara; el literal dentro de un comentario → no dispara (sin el falso positivo del (5a)); el literal como código real en `rpc_confirm_sales_order` → dispara. (d) pasa a describir sólo el privilegio y remite a (b bis); el NOTICE de cierre deja de afirmar lo que no medía.*
- [x] 10.4 **Verificación de no-regresión.** → *Evidencia: `verify_body_diff_h1.py` **rc=0** (los cambios del guard viven entre los marcadores box-drawing, que es lo único que el script descuenta): `_c29_confirm_order_core` 15110 bytes y `c28_register_cash_movement` 2288 bytes, byte-idénticos al baseline vivo de prod una vez quitado el guard. Migración re-aplicada en local sin error. Gate del change 14/14 en dos corridas seguidas sobre la misma base y **cero residuos**. **Cierre sobre DB LIMPIA** (`npx supabase db reset`, 262 migraciones, RC=0): los **47 pasos** de la cadena del workflow corridos EN ORDEN — 45 en verde y los **2 fallos esperados y tolerados por el propio CI** con su marcador literal (`20260928000001` → `GATE ANTI-OVERLOAD FAILED`, `20261002000001` → `GATE POS-BANCO-MOVIMIENTOS FAILED (1, 42725)`; los dos reconvergen con la migración siguiente, es el comportamiento documentado de la cadena, no una regresión). `test_confirm_core_integrity.sql` —el que congela literales del cuerpo vivo del core— en verde. **pytest: 1580 passed / 3 skipped** (idéntico al baseline de la rama). **Prod re-verificada de solo lectura**: `MAX(version) = 20261012000001` (261 migraciones) → `20261013000001` sigue libre, y los 4 md5 vivos siguen idénticos a los del baseline (`_c29_confirm_order_core` `cecd8c…`/15110, `c28_register_cash_movement` `510adc…`/2288 secdef=f, `rpc_quick_sale` `ccb8af…`/4046, `rpc_confirm_sales_order` `38f290…`/777). Higiene: sin BOM, blobs en `i/lf`, `openspec validate --strict` válido.*
