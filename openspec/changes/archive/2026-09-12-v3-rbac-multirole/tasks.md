> **Tres applies independientes.** Los grupos 1-6 son la **Parte A** (un PR), 7-13 la **Parte B** (otro PR), 14-20 la **Parte C** (otro PR). Cada parte se mergea, se verifica en prod y recibe humo del PO antes de empezar la siguiente. El orden A → B → C es sign-off del PO (6.6) y además una restricción técnica: D3 del design explica por qué ningún rol funcional puede asignarse antes de que B migre `is_account_writer`.
>
> **TDD estricto en todo el change**: por cada task de implementación, el test que la describe se escribe y **se ve fallar** antes del código. Cada grupo arranca con su red de seguridad (correr la suite existente y registrar el baseline); un fallo preexistente se reporta, no se arregla acá.
>
> **Regla de integridad de función**: toda función reescrita parte de su cuerpo **vivo de prod** (`pg_get_functiondef`), nunca del último archivo de migración. *Gotcha registrado*: el `md5` local y el de prod difieren por los CRLF del checkout de Windows — comparar por líneas con `\r` removido, no por hash crudo.

## 1. Parte A — Checkpoint de estado real (antes de escribir una línea de SQL)

- [x] 1.1 SAFETY NET: backend `2227 passed, 4 skipped` (baseline, medido antes de tocar código); frontend no se corrió como baseline previo (se corrió como cierre en 6.3: `2641 passed, 1 failed` — el único fallo, `banco-conciliacion-import-warnings.test.tsx` por timeout de 20s, es ajeno a RBAC, módulo de conciliación bancaria, cero archivos frontend tocados por esta parte)
- [x] 1.2 Prod (read-only, 2026-09-11): `MAX(version) = 20261045000001`, 294 filas — coincide con el design. Migración de esta parte: **`20261047000001`** confirmada (local ya tenía `20261046000001` de `importador-gate-plan` aplicado por el workflow paralelo; no se dependió de su contenido)
- [x] 1.3 Prod: `account_members` 39 filas, **39 owner / 0 admin / 0 member**; 39 cuentas con exactamente 1 miembro, **0 con 2+**; 0 invitaciones pendientes — idéntico al propose, backfill trivial confirmado
- [x] 1.4 Prod: `to_regclass('account_member_roles')` y `to_regclass('account_role_catalog')` → NULL (no existen); `document_status_transitions.allowed_role` = `text`, `0/19` pobladas
- [x] 1.5 Cuerpo vivo de prod capturado vía `pg_get_functiondef` para las 5 funciones (`rpc_change_member_role`, `rpc_remove_member`, `rpc_accept_invitation`, `rpc_invite_member` [2 overloads existentes, no tocado], `rpc_my_account_role`) — diff línea a línea con `\r` removido contra el cuerpo local: **idéntico en las 4 reescritas** (única diferencia: una línea en blanco de formato del `psql -t -A`, no del cuerpo)
- [x] 1.6 `grep -rn "P0405\|P0406" supabase/ backend/` → 0 usos en ambos frentes. Confirmados libres

## 2. Parte A — Catálogo de roles (D1, D2)

- [x] 2.1 RED→GREEN: `supabase/tests/test_account_role_catalog.sql` bloque (1)+(2) — 8 entradas, minúsculas, sólo `viewer` con `is_writer=false`
- [x] 2.2 RED→GREEN: mismo gate, bloques (3)+(4)+(5) — `authenticated`/`anon` sin INSERT/UPDATE/DELETE; `authenticated` sí SELECT (policy scopeada, sin `anon`/`public`)
- [x] 2.3 GREEN: `account_role_catalog` creada (molde `document_status_transitions`), RLS solo-lectura + `REVOKE` de escritura
- [x] 2.4 GREEN: 8 filas sembradas, castellano, `sort_order` 1-8, seed `ON CONFLICT (code) DO UPDATE` — verificado idempotente en reaplicación ×2 (siempre 8 filas)
- [x] 2.5 TRIANGULATE: bloque (6) rol fuera de catálogo → `23503` sin dejar fila; bloque (7) `is_writer` editado y revertido con `UPDATE` simple, sin tocar ninguna función — **7/7 bloques PASS**

## 3. Parte A — Pivot de asignaciones (D3, D4, D5)

- [x] 3.1 RED→GREEN: `supabase/tests/test_account_member_roles_pivot.sql` bloque (1) unicidad `23505`; bloque (2) `CASCADE` real (borra la membresía, 0 filas del pivot después)
- [x] 3.2 RED→GREEN: `test_account_owner_invariant.sql` bloque (5) — owner+`expires_at` rechazado con `P0406`; owner sin vencimiento aceptado
- [x] 3.3 RED→GREEN: bloque (3) del gate del pivot — sin vencimiento activo, futuro activo, pasado NO activo (`member_active_roles`)
- [x] 3.4 GREEN: `account_member_roles` creada con `account_id` (denormalizado, ver nota de diseño en la migración — lo exige el constraint trigger diferido de 4.4 para resolver la cuenta aun con el `member_id` ya borrado en cascada), `member_id`, `role`, `assigned_by`, `assigned_at`, `expires_at`; CHECK de D5 + unicidad `(member_id, role)`
- [x] 3.5 GREEN: índices B-tree `(member_id)`, `(account_id)`, y `(expires_at) WHERE expires_at IS NOT NULL` — ningún parcial con `now()`
- [x] 3.6 GREEN: `member_active_roles`/`account_user_active_roles` `STABLE SECURITY DEFINER search_path=public`; `REVOKE ALL FROM PUBLIC, anon, authenticated` **sin ningún GRANT** — la ronda 1 retiró el `GRANT EXECUTE TO authenticated` original por no llevar guard de tenencia (un usuario ajeno leía los roles de cualquier `member_id`); si la Parte B las expone, debe hacerlo con su propio guard, nunca con un GRANT desnudo (candado: bloque (8) de `test_account_member_roles_pivot.sql`) — **hallazgo del gate `test_function_acl_gate.sql`**: un `REVOKE ALL FROM PUBLIC` solo no alcanza, Supabase otorga `EXECUTE` a `anon` por defecto en función nueva; corregido revocando explícito de `anon` también
- [x] 3.7 TRIANGULATE: bloque (6) miembro con 4 roles simultáneos (owner+seller+stock+cashier); bloque (7) 1 vencido + 1 vigente; verificado además a mano `member_active_roles(uuid_sin_asignaciones) = '{}'` (no NULL) — **9/9 bloques PASS** (bloques (8)/(9) sumados en la ronda 1: ACLs sin GRANT a `authenticated`/`anon`, FK compuesta anti-fantasma)

## 4. Parte A — Invariante de propietario y espejo (D3, D6)

- [x] 4.1 RED→GREEN: `test_account_owner_invariant.sql` — las **cuatro formas** de dejar a una cuenta con miembros y sin propietario activo, todas rechazadas con `P0405`: (1) `DELETE` directo sobre la fila del pivot (revocación); (10) `UPDATE` que degrada directamente el rol del único owner; (12) `UPDATE` que mueve la fila `owner` a OTRA cuenta; (13) **ronda 3 (finding MINOR-1)** `DELETE FROM account_members` que borra la MEMBRESÍA completa del único owner conservando otro miembro — dispara el MISMO constraint trigger vía el `ON DELETE CASCADE` hacia el pivot, molde del bloque (1). El bloque (4) reejercita (1) con un `DELETE` directo sin pasar por ningún RPC para mostrar que la garantía no depende del procedimiento que la invoque — no es una quinta forma distinta, es la misma (1) desde otro llamador. **Ronda 2 (finding MAJOR)**: la primera versión de este task sólo cubrió INSERT/DELETE; el UPDATE cross-account evadía `fn_guard_account_owner_invariant` (sólo miraba `NEW.account_id`), corregido en la migración y cerrado con el bloque (12). **Ronda 3 (finding MINOR-1)**: el texto de este task enumeraba "cuatro formas" pero sólo mapeaba a tres bloques reales distintos ((1)≈(4), (10), (12)) — la forma que faltaba (borrar `account_members`, no el pivot) se cerró con el bloque (13) nuevo
- [x] 4.2 RED→GREEN: bloque (2) — revocar a A + asignar a B en la misma transacción (forzado con `SET CONSTRAINTS ... IMMEDIATE`) se completa sin error
- [x] 4.3 RED→GREEN: bloque (3) — vaciar la cuenta por completo no bloqueado (satisfecho de forma vacua, verificado hasta el COMMIT real del gate)
- [x] 4.4 GREEN: `CREATE CONSTRAINT TRIGGER trg_guard_account_owner_invariant ... DEFERRABLE INITIALLY DEFERRED` sobre `account_member_roles`, `P0405`
- [x] 4.5 GREEN: `P0405`→409, `P0406`→422 en `backend/core/errors.py`; test RED→GREEN confirmado a mano (comentando las 2 entradas: 2 tests fallan con 500; restauradas: 17/17 pasan) en `backend/tests/test_errors_business_codes.py`
- [x] 4.6 RED→GREEN: bloque (6) — escritura directa de `account_members.role` revertida por el espejo; columna refleja la precedencia real del pivot para ambos miembros del fixture
- [x] 4.7 GREEN: **arquitectura de espejo de dos triggers** (no uno): `trg_derive_account_member_role` (BEFORE INSERT/UPDATE en `account_members`, única fuente de verdad de la derivación) + `trg_touch_account_member_role` (AFTER en el pivot, "toca" la membresía para forzar la re-derivación) — así una escritura directa a la columna y un cambio en el pivot convergen al MISMO resultado sin caminos duplicados. **Ronda 2 (finding MAJOR)**: la primera versión de `fn_touch_account_member_role` sólo tocaba `NEW.member_id` — un UPDATE que mueve `member_id` (bloque (11)) dejaba el espejo del miembro ORIGEN desincronizado (rol viejo). Corregido a `WHERE id = ANY (ARRAY[NEW.member_id, OLD.member_id])`, cerrado con el bloque (11)
- [x] 4.8 GREEN: `trg_audit_account_member_role` (AFTER INSERT/DELETE en el pivot) — `role.assigned`/`role.revoked` en `audit_logs`, shape canónico confirmado; `REVOKE ALL ... FROM PUBLIC, anon, authenticated` en las 5 funciones trigger nuevas — bloque (7) del gate de invariante confirma ambos tipos de registro
- [x] 4.9 Documentado en la cabecera de `test_account_owner_invariant.sql`: el gate NO puede correr bajo `replica` (usa `SET CONSTRAINTS ... IMMEDIATE`, que un `replica` global desactivaría junto con el propio invariante) — su cleanup usa `replica` sólo para `branches`, DESPUÉS de que todas las aserciones ya corrieron

## 5. Parte A — Reescritura de los RPCs de membresía y backfill

- [x] 5.1 RED→GREEN: `test_membership_rpcs_pivot_rewrite.sql` bloques (2)-(5) — firma/contrato/orden de validaciones intactos, incluido el gate `admin`⇒`pro` byte a byte (bloque 5)
- [x] 5.2 GREEN: backfill `INSERT ... SELECT account_id, id, 'owner', NULL, created_at ... ON CONFLICT (member_id, role) DO NOTHING` — verificado idempotente (reaplicación ×2, 0 filas nuevas la segunda vez)
- [x] 5.3 GREEN: `rpc_change_member_role` y `rpc_accept_invitation` reescritas escribiendo a través del pivot (mapeo `member`→`viewer`, D2); `rpc_remove_member` **sin escritura explícita al pivot** — el `DELETE` en `account_members` ya cascada al pivot vía FK, disparando invariante+auditoría automáticamente (documentado en la migración, sección 6.2); su único cambio de cuerpo es el guard de tenencia `v_caller_role IS NULL OR` de la ronda 1
- [x] 5.4 GREEN: `rpc_my_account_role` reescrita derivando de `member_active_roles()`, mismo vocabulario heredado — bloque (8) del gate confirma `owner`/`admin` para dos usuarios distintos
- [x] 5.5 TRIANGULATE: bloque (4) degradar único owner → mismo `{error}` exacto; bloque (6) `rpc_remove_member` cascada limpia; bloque (7) `rpc_accept_invitation` crea membresía + asignación equivalente con `assigned_by = invited_by`
- [x] 5.6 Verificado bloque (12) del gate — 0 overloads en las 5 funciones tocadas (las 4 RPCs + `handle_new_user`, ver hallazgo 6.3b más abajo)

## 6. Parte A — Cierre

- [x] 6.1 4 gates nuevos cableados en `KPI_Validation.yml` (steps al final de la lista) + migración sumada a la cadena de reaplicación idempotente, inmediatamente después de `20261045000001`
- [x] 6.2 **Desvío del literal de la task**: NO se corrió `supabase db reset` (regla dura del brief: "el implementador NUNCA corre db reset" — la base local es compartida con otro workflow). En su lugar: la migración se aplicó 2 veces consecutivas por `psql` sobre la base local compartida (que ya refleja la migration history completa vía `supabase start`/`db reset` de sesiones previas) y se corrió la **suite completa de 72 gates preexistentes** — 65/72 verdes; los 7 restantes son ruido preexistente de este entorno compartido (6 por email duplicado de fixtures de OTROS gates ya presentes ANTES de esta sesión — confirmado el email exacto pre-existía; 1 por conteo de KPIs contaminado por la acumulación de correr 72 gates en secuencia sin reset). Ninguno de los 7 toca `account_member_roles`/el invariante — confirmado que el `CASCADE` del pivot no bloquea ningún cleanup existente
- [x] 6.3 SAFETY NET de cierre: backend `2229 passed, 4 skipped` (2227 baseline + 2 tests nuevos de P0405/P0406); frontend `2641 passed, 1 failed` (el fallo es un timeout ajeno, módulo de conciliación bancaria, ver 1.1). `tsc`: no se corrió — no hay NINGÚN archivo frontend tocado por esta parte, por lo que no puede haber errores nuevos por construcción
- [x] 6.4 Declarado: **sin superficie frontend** en esta Parte A — ningún archivo bajo `frontend/` fue modificado
- [x] 6.5 Verificación post-merge en prod — **completada 2026-09-11** (medida por el orquestador vía `mcp__supabase__execute_sql`, sólo lectura): `MAX(version) = 20261047000001` (296 migraciones); `account_role_catalog` 8 filas; `account_member_roles` (pivot) 39 filas, **todas** `role='owner'` y `expires_at IS NULL`; **39/39** cuentas con un owner en el pivot; **0** owners de `account_members` sin su fila equivalente en el pivot; **0** filas huérfanas del pivot (sin `account_members` referenciado); `account_members` 39 `owner` / 39 total, sin cambios respecto del backfill; las **7** funciones que reescribe esta parte (`rpc_change_member_role`, `rpc_remove_member`, `rpc_accept_invitation`, `rpc_my_account_role`, `member_active_roles`, `account_user_active_roles`, `handle_new_user`) con **exactamente 1** definición cada una (sin overload fantasma); ACLs del pivot como se diseñó — `account_member_roles` **sin ningún privilegio** (`SELECT`/`INSERT`/`UPDATE`/`DELETE`) para `anon` **ni** `authenticated`, `account_role_catalog` con `SELECT` para ambos y sin escritura; **4** triggers vivos sobre `account_member_roles` (`trg_audit_account_member_role`, `trg_guard_account_member_role_no_owner_expiry`, `trg_guard_account_owner_invariant` — el único `DEFERRABLE INITIALLY DEFERRED` —, `trg_touch_account_member_role`), más `trg_derive_account_member_role` sobre `account_members` (espejo, preexistente al invariante). Todo idéntico a lo esperado por el design — sin hallazgos.
- [x] 6.6 Registrado en `CHANGES.md` (ficha `v3-rbac-multirole`, entrada de apply Parte A)
- [x] 6.7 **Ronda 1 de revisión adversarial aplicada (pre-merge)**: 1 blocker (backfill de la sección 5 hardcodeaba `'owner'` para toda fila — corregido a un `CASE` que mapea a la asignación equivalente), 3 major (`GRANT EXECUTE` de `member_active_roles`/`account_user_active_roles` a `authenticated` sin guard de tenencia — retirado; `rpc_change_member_role` aceptaba cualquier código del catálogo — rechazo agregado; agujero de tenencia preexistente en `rpc_change_member_role`/`rpc_remove_member` cuando el caller no es miembro — cerrado con `IS NULL OR`), 2 minor (target inexistente crasheaba con `23502` — corregido a `{error}`; wording del hallazgo 6.3b corregido en migración/CHANGES.md/gate — el signup degradaba en silencio, no abortaba) y 1 hardening no bloqueante (FK compuesta `(member_id, account_id)` en el pivot). 2 gates existentes ampliados (`test_membership_rpcs_pivot_rewrite.sql` 9→13 bloques, `test_account_member_roles_pivot.sql` 7→9 bloques) para cubrir cada finding. Re-verificado: migración reaplicable ×2, los 4 gates de la Parte A + `test_function_acl_gate.sql` en verde, backend `2229 passed, 4 skipped` (sin regresiones sobre el baseline de 1.1/6.3). Detalle completo en `CHANGES.md`.
- [x] 6.8 **Ronda 2 de revisión adversarial aplicada (pre-merge)**: 2 major (`fn_guard_account_owner_invariant` sólo miraba `NEW.account_id` — un UPDATE que mueve la fila owner a otra cuenta dejaba la cuenta de ORIGEN con miembros y 0 owners sin ningún error, reproducido en transacción aislada y commiteada; corregido a verificar ambas cuentas con un `FOREACH` sobre `unnest(ARRAY[NEW.account_id, OLD.account_id])`. `fn_touch_account_member_role` sólo tocaba `NEW.member_id` — un UPDATE que mueve `member_id` dejaba el espejo del miembro origen desincronizado; corregido a `WHERE id = ANY (ARRAY[NEW.member_id, OLD.member_id])`), 1 minor (el gate del blocker de la ronda 1, bloque (13), verificaba su PROPIA copia de la sentencia del backfill, no la del archivo real — demostrado reintroduciendo el hardcodeo `'owner'` en el archivo real y viendo el gate seguir en verde; corregido moviendo el bloque (13) a una Fase separada que reaplica el ARCHIVO REAL vía `\i`, molde de `test_cuentas_billetera_tipo.sql`) y 4 nits de fidelidad documental (`tasks.md` 3.6/3.7/5.3/5.6 desactualizados tras la ronda 1; comentario interno de `handle_new_user` seguía diciendo que el signup "abortaría" en vez de degradar en silencio; 3 gates nuevos con signup dejaban cashboxes huérfanas por borrar `branches` antes que `cashboxes` bajo `replica`; backfill de la sección 5 sin guard de "ya migrado", trampa para la Parte B). `test_account_owner_invariant.sql` ampliado 9→12 bloques (los 3 nuevos ejercitan exactamente la rama UPDATE que la task 4.1/R1 del design pedía y que la ronda 1 no cubrió — 2 para los majors, 1 candado adicional); `test_account_role_catalog.sql` bloque (6) con un degrade-don't-fail que antes era inalcanzable (resuelto el anchor antes de decidir, no después). Re-verificado: migración reaplicable ×2, los 4 gates de la Parte A + `test_function_acl_gate.sql` en verde, backend sin regresiones sobre el baseline. Detalle completo en `CHANGES.md`.
- [x] 6.9 **Ronda 3 de revisión adversarial aplicada (pre-merge, veredicto `ready=true`)**: 0 blocker/major, **3 minor + 4 nit**, todos aplicados. **MINOR-1**: faltaba cubrir la cuarta forma de dejar a una cuenta con miembros y sin owner — un `DELETE FROM account_members` (no del pivot) del único owner conservando otro miembro; el comportamiento ya era correcto (verificado por el revisor), sólo faltaba el gate — cerrado con el bloque (13) nuevo de `test_account_owner_invariant.sql` y el texto de la task 4.1 corregido para que las cuatro formas mapeen a bloques reales ((1), (10), (12), (13); (4) reejercita (1), no es una quinta forma). **MINOR-2**: fuera del lote de findings a aplicar en esta pasada (sin cambio de código asociado en este apply). **MINOR-3**: el pivot auditaba `INSERT`/`DELETE` pero no `UPDATE` — un cambio de `expires_at` o un movimiento de `member_id`/`account_id` (blocs (11)/(12)) no dejaba rastro; cerrado agregando `OR UPDATE` al trigger `trg_audit_account_member_role` y una rama `role.updated` en `fn_audit_account_member_role` con el snapshot OLD/NEW completo (`member_id`, `account_id`, `role`, `expires_at`), gateado por el bloque (14) nuevo. **NIT-1**: el marcador `(PR #PRA)` de `CHANGES.md` reemplazado por el número real `#557`. **NIT-2**: `REVOKE TRUNCATE` agregado sobre ambas tablas (`account_member_roles`, `account_role_catalog`) para que "cerrado por completo a authenticated/anon" sea literalmente cierto — verificado por `relacl` antes/después y `test_function_acl_gate.sql` en verde. **NIT-3**: esta misma task, documentando la ronda 3 en `tasks.md`/`CHANGES.md`. **NIT-4** (orden de bloques): dejado tal cual, cosmético, sin acción. Re-verificado: migración reaplicable ×2 en local, los 4 gates de la Parte A (`test_account_owner_invariant.sql` ahora 14 bloques) + `test_function_acl_gate.sql` + `test_errcode_5char_gate.sql` en verde, `pytest backend/tests/test_errors_business_codes.py` sin regresiones, `openspec validate --changes --strict` 1/1. Detalle completo en `CHANGES.md`.

## 7. Parte B — Checkpoint de estado real (CRÍTICO — antes de escribir SQL)

- [x] 7.1 SAFETY NET: backend `2236 passed, 4 skipped` (baseline, medido antes de tocar código); frontend no se corrió como baseline previo — se corrió como cierre en 13.3 (`node_modules` no existía en este worktree, `pnpm install` corrido primero): `2653 passed, 1 failed` (el único fallo, `banco-conciliacion-import-warnings.test.tsx` por timeout de 20s, es el MISMO flaky pre-existente ya documentado por Parte A en su propio 1.1/6.3 — módulo de conciliación bancaria, cero archivos frontend tocados por esta parte)
- [x] 7.2 Prod (read-only vía MCP, 2026-09-11): `MAX(version) = 20261047000001`, 296 filas (Parte A ya mergeada/aplicada a prod). Migración de esta parte: **`20261048000001`** confirmada
- [x] 7.3 Cuerpo vivo de prod capturado vía `pg_get_functiondef` para las 3 funciones — `md5` (con `\r` removido) IDÉNTICO prod=local: `is_account_writer=e5e962f75e60828f5d80516f61003a26`, `custom_access_token_hook=0850ba16db47b3e67fd13393fbf14090`, `record_status_transition=d5c0630b47b4e6d6156a03a8be4e8432` — baseline documentado en la cabecera de `20261048000001_v3_rbac_multirole_parte_b.sql`
- [x] 7.4 Prod (MCP): 48 policies / 20 tablas invocando `is_account_writer` — idéntico al design, no crecieron
- [x] 7.5 **Hallazgo real**: 12 llamadores vivos (no 10 — el design subestimó el conteo), filtrando `pg_get_functiondef` por `prokind IN ('f','p')` (el filtro ingenuo por ILIKE atrapa además funciones agregadas como `array_agg` y explota — `ERROR: "array_agg" is an aggregate function`): `_c29_confirm_order_core`, `rpc_accept_quote` (×2 llamadas), `rpc_close_cash_session`, `rpc_close_reconciliation_session`, `rpc_delete_sale_operation`, `rpc_emit_pending_cae`, `rpc_open_cash_session`, `rpc_open_reconciliation_session`, `rpc_quick_sale`, `rpc_record_fiscal_transition`, `rpc_transfer_stock`, `trg_quote_record_creation`. **Segundo hallazgo, más importante**: NINGUNO pasa un `NULL` literal — `document_status_history.performed_by` es `NOT NULL` desde siempre (preexistente, ajeno a este change); el relay CAE real (`rpc_record_fiscal_transition`) ya resuelve esto con un **UUID centinela** (`00000000-0000-0000-0000-000000000000`, comentado en su propio cuerpo: "uuid cero = sistema"), NUNCA con `NULL`. La exención 1 de D14 (`p_performed_by IS NULL`) sigue siendo la lectura correcta y literal del design (defensiva para un futuro caller), pero hoy es alcanzada por NINGÚN llamador real — el caso real del relay CAE queda cubierto por la exención 2 (`allowed_role IS NULL`, las 3 filas de `fiscal_document`), no por la 1. Con las 39 cuentas 100% `owner`, D14 no cambia el resultado de ninguna operación existente (confirmado en 11.7)
- [x] 7.6 Reconfirmado por grep (sin cambios desde el design): el navegador no escribe ninguna de las 20 tablas gateadas por `is_account_writer` vía PostgREST; `git status --short -- frontend/` da 0 archivos tocados por esta parte

## 8. Parte B — `is_account_writer` sobre el pivot (D11)

- [x] 8.1 RED→GREEN: `supabase/tests/test_is_account_writer_pivot.sql` bloques (2)-(4) — rol writer (seller) pasa, `viewer` no pasa, rol vencido no pasa aunque la fila exista. Corrido en RED contra el cuerpo viejo (bloque (2) fallaba: `dio f` esperando `t`) antes de escribir la migración
- [x] 8.2 RED→GREEN: bloque (1) del mismo gate — aridad sin cambio (1 arg) y 48 policies/20 tablas confirmadas
- [x] 8.3 GREEN: cuerpo reescrito resolviendo `account_members` en la MISMA consulta vía `member_active_roles()` directo (NUNCA un segundo "rol activo") + `account_role_catalog.is_writer` — la primera versión de este bloque pasaba por `account_user_active_roles()`; corregido a un solo salto en la ronda 1 adversarial (minor 2, rendimiento), ver más abajo
- [x] 8.4 Confirmado: `current_account_ids()` no aparece en el diff de esta migración (grep en el archivo: 0 ocurrencias)
- [x] 8.5 TRIANGULATE: bloque (5) — 2 asignaciones simultáneas (una vencida `seller`, otra vigente `stock`) → escribe por la vigente; bloque (6) — miembro de otra cuenta → no escribe; bloque (7) (hallazgo del propio TDD, no listado en el design) — fail-closed explícito sin ninguna fila en el pivot → false. **7/7 bloques PASS** (incl. bloque estructural (1))

## 9. Parte B — Claim del conjunto de roles (D8)

- [x] 9.1 RED→GREEN: `supabase/tests/test_custom_access_token_hook_account_roles.sql` bloques (1)-(2) — `account_role` singular intacto, `account_roles` trae exactamente los roles ACTIVOS (`{seller,stock}` en el fixture)
- [x] 9.2 RED→GREEN: bloque (2)/(3) — el rol `accountant` vencido del fixture NO viaja en `account_roles` (misma aserción, ver comentario del gate)
- [x] 9.3 RED→GREEN: bloque (5) — sin el `GRANT EXECUTE` sobre `member_active_roles` a `supabase_auth_admin`, el hook devuelve los claims EXACTAMENTE intactos (sin excepción); con el `GRANT` restituido, vuelve a emitir. **Limitación de entorno documentada**: `SET LOCAL ROLE supabase_auth_admin` requiere que el rol que aplica la migración sea miembro de ese rol (o superusuario) — en este local compartido `postgres` no lo es (mismo límite que el gate original de `v31-authz-token-hook` ya documentaba en su propio bloque (k)); el bloque degrada con `NOTICE` sin fallar, mismo criterio. El mecanismo de blindaje (bloque `EXCEPTION WHEN OTHERS` único, `account_roles` agregado DENTRO de él) se verificó por inspección del cuerpo — no hay una segunda ruta de retorno
- [x] 9.4 GREEN: `custom_access_token_hook` reescrito desde el cuerpo vivo de 7.3 — `account_roles` resuelto vía `member_active_roles(v_member_id)` (mismo `member_id` ya resuelto para `account_role`), agregado dentro del bloque protegido existente
- [x] 9.5 GREEN: `GRANT EXECUTE ON FUNCTION member_active_roles(uuid) TO supabase_auth_admin` (único camino real que el hook usa) + `GRANT SELECT` sobre `account_member_roles`/`account_role_catalog` a `supabase_auth_admin` y policy `auth_admin_can_read_member_roles` (lectura literal de la task, defensa en profundidad — el camino real ya bypasea RLS/GRANT vía `member_active_roles` SECURITY DEFINER)
- [x] 9.6 Verificado por diseño: el gate embebido de introspección de la migración assertea `has_function_privilege('supabase_auth_admin', 'member_active_roles(uuid)', 'EXECUTE')`; la activación real en prod post-merge (13.6) se mide contra `auth_logs`, nunca `raw_app_meta_data` — pendiente de post-merge, no marcable hoy

## 10. Parte B — Guards del backend (D10)

- [x] 10.1 RED→GREEN: `backend/tests/test_guards.py::test_require_account_role_authorizes_when_any_role_in_set_is_allowed` / `..._denies_when_no_role_in_set_is_allowed` — semántica "alguno", no "todos"
- [x] 10.2 RED→GREEN: `test_require_account_role_falls_back_to_singular_claim_as_set_of_one` / `..._falls_back_to_db_when_both_claims_absent` / `..._denies_without_any_of_the_three_sources` — orden de resolución completo, deniega sin las tres
- [x] 10.3 RED→GREEN: `test_require_account_role_db_fallback_queries_active_assignments_not_singular_column` — asserta el TEXTO de la query awaited (`rpc_my_active_account_roles` presente, `account_members.role`/`SELECT role FROM account_members` ausente)
- [x] 10.4 RED→GREEN: `backend/tests/test_auth.py::test_get_current_user_keys_include_account_roles` — contrato anti-deriva con la clave nueva
- [x] 10.5 GREEN: `AuthContext.account_roles: list[str] | None` + extracción en `get_current_user` (`app_metadata.get("account_roles")`, sin default permisivo — `None` si el claim no viaja, preservando `[]` si viaja vacío)
- [x] 10.6 GREEN: `require_account_role` reescrito — conjunto → singular-como-uno → `rpc_my_active_account_roles()` (RPC nueva SECURITY DEFINER **sin parámetro**, resuelve `auth.uid()` internamente — desvío documentado respecto del texto literal de D10/R6: evita abrir el pivot completo a RLS/GRANT de tabla para `authenticated`, ver nota en la migración sección 2) → deniega
- [x] 10.7 GREEN: `backend/core/rbac.py` con las 6 constantes; las 15 llamadas de `cost_centers.py`/`payment_methods.py`/`product_categories.py`/`account_charges.py` migradas a `CAN_CONFIGURE` (grep confirma 15/15, 0 literales `["owner","admin"]` restantes en esos 4 archivos salvo comentarios)
- [x] 10.8 Confirmado por grep: `require_role(auth, ["user","admin"])` sigue en 64 call sites (ronda 1 adversarial, minor 5: el conteo de 59 del design/checkpoint quedó viejo — `grep -rn --include=*.py 'require_role(auth, \["user", "admin"\])' backend | grep -v __pycache__ | grep -v '/tests/'` da 65 líneas, 1 es un comentario de docstring en `customer_accounts.py:5`, no una llamada real → 64), 0 tocados

## 11. Parte B — Matriz rol × transición (D13, D14, D15)

- [x] 11.1 RED→GREEN: `supabase/tests/test_document_status_transition_role_matrix.sql` bloque (1) — reparto EXACTO 14/19, las 5 NULL son `fiscal_document`×3 + `quote→expired`×2, verificado por conjunto completo (no sólo el conteo)
- [x] 11.2 RED→GREEN: bloques (2a)-(2e) — `cashier` confirma pero NO anula (P0403), `stock` completa transferencia, `accountant` abre Y cierra conciliación, rol vencido no habilita nada (P0403)
- [x] 11.3 RED→GREEN: bloques (3a)/(3b) — **hallazgo real** (ver 7.5): la exención 1 (actor NULL) se probó como "el chequeo de rol se SALTEA" (el bloqueo real es `23502 not_null_violation` de la tabla, preexistente, NUNCA `P0403` — si el chequeo NO se salteara para actor NULL, el error sería `P0403` porque `account_user_active_roles(cuenta, NULL)` da conjunto vacío); exención 2 (`allowed_role` NULL) probada tal como está escrita, con un actor SIN ningún rol en la cuenta
- [x] 11.4 GREEN: `ALTER COLUMN` guardado por `DO $$ IF data_type='text' THEN ... END IF $$` — no-op en la 2ª/3ª reaplicación (evita envolver un `text[]` ya migrado en un array anidado)
- [x] 11.5 GREEN: 14 `UPDATE` poblando la matriz D15 exacta — verificado 14/14 filas afectadas
- [x] 11.6 GREEN: `record_status_transition` reescrito desde el cuerpo vivo de 7.3 — mismas 2 validaciones previas (RN-A4/RN-A5) intactas, chequeo de rol agregado con las 2 exenciones, `P0403`
- [x] 11.7 TRIANGULATE: bloques (2a)/(2c)/(2d) ejercitan exactamente las transiciones que dispara el POS (`sales_order draft→confirmed`), el formulario (`stock_transfer`, `reconciliation_session`) y — por los 12 llamadores reales de 7.5, todos pasando `owner` (100% de las 39 cuentas) — ninguno cambia de resultado: los 14 `allowed_role` poblados incluyen `owner` en las 14 filas (D15), así que un caller `owner` sigue pasando siempre

## 12. Parte B — Barrido de vencimientos (D16)

- [x] 12.1 RED→GREEN: `supabase/tests/test_role_expiry_audit_sweep.sql` bloque (1) — 2ª corrida agrega 0 nuevos registros para la misma asignación (dedup por FILA del pivot, más fuerte que el dedup por día del molde de `cobranzas-vencimientos`: un vencimiento de rol es un evento único en el tiempo, no una deuda que se acumula día a día — desvío documentado en el comentario de la función)
- [x] 12.2 RED→GREEN: bloque (2) — la fila del pivot sigue existiendo IDÉNTICA (mismo `role`/`member_id`/`expires_at`) y `is_account_writer` no cambia tras el barrido
- [x] 12.3 RED→GREEN: bloque (3) — con el barrido en 0 corridas, el rol vencido YA no autoriza (`member_active_roles` lo excluye por predicado) e `is_account_writer` sigue dando `true` sólo por el rol vigente
- [x] 12.4 GREEN: `_produce_role_expiry_audit_sweep()` — dedup por `NOT EXISTS` sobre `audit_logs` (entity_id + action), `as_of` (día argentino, `reporting_local_today()`) sólo para observabilidad
- [x] 12.5 GREEN: `cron.schedule('v3-rbac-role-expiry-audit-sweep', '0 13 * * *', ...)` precedido de `cron.unschedule` condicional (idempotente); confirmado activo (`cron.job.active = true`) por el gate embebido de introspección de la migración

## 13. Parte B — Cierre (CRÍTICO)

- [x] 13.1 4 gates nuevos cableados en `KPI_Validation.yml` (steps al final de la lista) + migración `20261048000001` sumada a la cadena de reaplicación idempotente, inmediatamente después de `20261047000001`
- [x] 13.2 **Desvío del literal de la task** (mismo criterio que Parte A 6.2 — regla dura del brief: "el implementador NUNCA corre `db reset`"): la migración se aplicó 3 veces consecutivas por `psql` sobre la base local compartida (EXIT=0 las tres, gate de introspección embebido en verde las tres) + se corrieron los 9 gates preexistentes más directamente relacionados (Parte A completa ×4, `test_function_acl_gate.sql`, `test_tenancy_rls_role.sql`, `test_anon_table_writes_revoked.sql`, `test_confirm_core_integrity.sql`) — todos verdes. **Hallazgo real durante esta verificación**: `test_account_owner_invariant.sql` bloque (14) falló al primer intento — investigado (no descartado como "preexistente" sin evidencia): `trg_audit_account_member_role` en la base LOCAL compartida estaba `AFTER INSERT OR DELETE` (sin `OR UPDATE`), divergente tanto del ARCHIVO de la migración de Parte A como de **PROD** (confirmado idéntico vía MCP: `AFTER INSERT OR DELETE OR UPDATE`) — un drift exclusivo de este entorno local (reflejaba una versión del archivo anterior a la "ronda 3" de Parte A, nunca reaplicada). Corregido reaplicando `20261047000001_v3_rbac_multirole_parte_a.sql` (idempotente, ya verificado en su propio 6.2/6.7) — no requirió `db reset`. Re-verificado: 14/14 bloques PASS
- [x] 13.3 SAFETY NET de cierre: backend `2247 passed, 4 skipped` (2236 baseline + 11 tests nuevos: 3 `test_rbac_capabilities.py` + 5 nuevos en `test_guards.py` + 3 nuevos en `test_auth.py`); frontend (tras `pnpm install`, `node_modules` faltaba en este worktree) `2653 passed, 1 failed` (mismo flaky pre-existente de Parte A, timeout en `banco-conciliacion-import-warnings.test.tsx`, módulo de conciliación bancaria, ajeno a RBAC). `tsc --noEmit`: **9 errores**, idéntico al baseline esperado (0 archivos frontend tocados por esta parte, por lo que no puede haber errores nuevos por construcción — confirmado, no asumido)
- [x] 13.4 Revisión adversarial propia aplicada durante el desarrollo (no una pasada separada al final): **fail-open buscado y no encontrado** — los 3 puntos con "ausencia de dato" (claim ausente, DB sin fila, actor NULL en `record_status_transition`) resuelven los 3 a denegar/exención documentada, nunca a conceder; **divergencia RLS/guard (R4)** — `is_account_writer` y `require_account_role` migran en la MISMA migración/PR, gate `test_is_account_writer_pivot.sql` + `test_guards.py` ejercitan ambas capas sobre el mismo concepto de "rol activo" (`account_user_active_roles`/`rpc_my_active_account_roles`, ambos derivados de `member_active_roles`, nunca una segunda función); **rol vencido en las 3 capas** — `is_account_writer` (bloque 4 del gate 8), el hook (bloque 2/3 del gate 9, vencido excluido del claim), `record_status_transition` (bloque 2e del gate 11) — las 3 verificadas explícitamente
- [x] 13.5 Declarado: **sin superficie frontend propia** — `git status --short -- frontend/` = 0 archivos: las ~40 pantallas que leen `accountRole`/`isWriter` no se tocan (siguen consumiendo el claim singular, sin cambio de forma)
- [x] 13.6 Verificación post-merge en prod — **completada 2026-09-12** (medida por la ronda 3 de revisión adversarial vía `mcp__supabase__execute_sql`, sólo lectura; la Parte B se mergeó el 2026-09-11, así que esta task ya era medible): `MAX(version) = 20261048000001` (**297** migraciones); `document_status_transitions` **14 de 19** filas con `allowed_role text[]` poblado (las 5 restantes sin rol declarado, como diseñó D15); `is_account_writer` **sobre el pivot** — su cuerpo vivo resuelve por `member_active_roles(am.id)` + `account_role_catalog.is_writer`, nunca por `account_members.role` (R6); `custom_access_token_hook` con el claim `account_roles` en su cuerpo vivo y `EXECUTE` para `supabase_auth_admin`, que además conserva `EXECUTE` sobre `member_active_roles(uuid)` (9.6); `account_role_catalog` con 8 filas; cron **`v3-rbac-role-expiry-audit-sweep` activo** (`0 13 * * *`, `active = true`). Todo idéntico a lo esperado por el design — sin hallazgos
- [ ] 13.7 Humo del PO — **pendiente**: requiere el PO operando en prod tras el merge. Dejado explícitamente sin marcar (nota del brief)
- [x] 13.8 Registrado en `CHANGES.md` (ficha `v3-rbac-multirole`, entrada de apply Parte B) — ver entrada agregada
- [x] 13.9 **Ronda 1 de revisión adversarial aplicada (pre-merge, worktree `wt-rbac-b`, veredicto `ready=true`)**: 0 blocker/major — **6 minor + 5 nit**, todos aplicados. **Minor 1** (gate D8 nunca ejercitado): el bloque (5) local de `test_custom_access_token_hook_account_roles.sql` (`SET LOCAL ROLE supabase_auth_admin`) SIEMPRE degrada por límite de entorno y ya no cuenta como PASS cuando degrada (4/5, no 5/5); el blindaje D8 se verifica DE VERDAD en un step nuevo de `KPI_Validation.yml` ("Run v3-rbac-multirole hook D8 degrade probe (real supabase_auth_admin role)") que se conecta como `supabase_auth_admin` REAL vía TCP (el rol SÍ tiene LOGIN, verificado) — con el GRANT emite `account_roles`, sin él degrada TODO con WARNING y sin excepción, con el GRANT restituido vuelve a emitir. **Minor 2** (rendimiento): medido en local (EXPLAIN ANALYZE, 5.000 filas, 2 corridas + bucle de 10.000 llamadas) — cuerpo viejo de prod ~0,019 ms/fila, cuerpo de dos saltos (versión original de esta migración) ~0,46-0,49 ms/fila (~24-26× más lento), cuerpo optimizado de UN salto (resuelve `account_members` en la misma consulta, vía `member_active_roles` directo — sin duplicar la definición de "rol activo") ~0,15 ms/fila (~8× más lento, recupera ~2/3 del overhead). Aplicado a `is_account_writer`; números en `CHANGES.md`/`design.md` D11. **Minor 3** (creación no catalogada): `record_status_transition` distingue explícitamente "sin fila en la matriz" (`v_row_found`) de "fila con `allowed_role NULL`" — mismo comportamiento (D17/11.7, conservador), pero ahora comentado y con gate de cobertura nuevo (bloque 5 de `test_document_status_transition_role_matrix.sql`) que enumera las 14 triples que producen los 12 llamadores vivos y asserta que están TODAS catalogadas — una transición nueva sin catalogar la atrapa CI. Endurecer a `P0409` queda como candidato de Parte C+. **Minor 4**: `GET /auth/claims-status` gana `account_roles_claim_present`/`effective_account_roles` (sin tocar `source`) + tests. **Minor 5**: conteo de `require_role(auth,["user","admin"])` corregido de 59 a **64** (65 líneas − 1 comentario de docstring) en `tasks.md` 10.8/`design.md`/`CHANGES.md`. **Minor 6**: la cabecera de la migración (checkpoint 7.5) contradecía su propio hallazgo — reescrita: ninguno de los 12 llamadores pasa `NULL`, todos pasan `auth.uid()`/derivada/el UUID centinela. **Nit 7**: `rpc_my_active_account_roles` ahora `COALESCE`-a `ARRAY[]` (antes `NULL`, contradiciendo su propio `COMMENT`) — mismo contrato que `member_active_roles`/`account_user_active_roles`. **Nit 8**: `require_account_role` verifica `isinstance(account_roles, list)` antes de iterar — un claim con forma de dict/str/int se trata como ausente (fail-closed), nunca autoriza ni revienta con 500; 3 tests nuevos. **Nit 9**: restaurada la aserción perdida de que el claim singular denegado NO cae al fallback de DB (`test_require_account_role_singular_claim_present_but_not_allowed_denies_without_db`). **Nit 10**: `#PRB` → `#558` en `CHANGES.md`. **Nit 11**: `test_capability_constants_are_disjoint_from_platform_role_namespace` renombrado a `..._only_use_catalog_codes` (decía lo que no probaba) + test nuevo `..._never_leak_the_platform_only_role_code` que sí verifica disyunción real contra `{"user"}` (el único código EXCLUSIVO de plataforma — `admin` es compartido a propósito, D1). Re-verificado end-to-end: migración reaplicable ×3 en local (gate de introspección OK las tres), los 4 gates de Parte B + los 4 de Parte A + `test_function_acl_gate.sql` + `test_errcode_5char_gate.sql` en verde, backend `2253 passed, 4 skipped` (2247 + 6 nuevos), `openspec validate` 1/1 changes y 102/102 specs, `git status --short -- frontend/` = 0. Detalle completo en `CHANGES.md`.
- [x] 13.10 **Ronda 2 de revisión adversarial aplicada (pre-merge, worktree `wt-rbac-b`, veredicto `ready=true`)**: 0 blocker/major — **1 minor + 4 nit**, todos aplicados. **Minor** (el bloque (5) del gate de catálogo es una lista hardcodeada; migración y gate sobre-prometían "atrapa una transición nueva"): (5) sólo mira hacia atrás (que los 14 pares conocidos sigan catalogados); cerrado con el bloque **(5b)** nuevo que enumera por `pg_get_functiondef` (CTE `AS MATERIALIZED`, evita el `ERROR: "array_agg" is an aggregate function` de un ILIKE plano) el conjunto de funciones que invocan `record_status_transition` y asserta que sigue siendo EXACTAMENTE el de los 12 conocidos — un llamador nuevo o que desaparezca falla el gate. Wording de la cabecera del gate y del comentario de la migración corregido para reflejar lo que (5)+(5b) cubren juntos, no "una transición nueva" en abstracto. Punto ciego declarado y NO cerrado (candidato dado de alta en `CHANGES.md`): un llamador YA conocido que cambie el PAR que produce sin cambiar de nombre (`trg_quote_record_creation`, `NEW.status` dinámico) sigue sin gate. **Nit 1**: `tasks.md` 8.3/`CHANGES.md`:1789 ya tenían el puntero "corregido en la ronda 1" desde la propia ronda 1 — verificado, sin cambio. **Nit 2**: re-medidos los tres cuerpos con 5 corridas y orden intercambiado — mergido (función LIVE) ~728-815 ms/5.000 filas (~11× el viejo), confirma el orden de magnitud ~8-11× ya documentado (no el ~1,3× de una pasada intermedia que no reprodujo bajo control de orden); números completos en `CHANGES.md`/`design.md` D11/cabecera de la migración, marcando los intermedios como intermedios. **Nit 3**: `_produce_role_expiry_audit_sweep` dejó de evaluar `expires_at <= now()` a mano y deriva "venció" preguntándole a `member_active_roles` (predicado de vencimiento en UN solo lugar); mismo comportamiento observable, `test_role_expiry_audit_sweep.sql` verde ×2 sin cambio de aserciones. **Nit 4**: el step "D8 degrade probe" de `KPI_Validation.yml` limpia su fixture con `trap cleanup EXIT` (subshell con `set +e` propio) + pre-cleanup idempotente, cubriendo los 3 caminos de fallo además del feliz; las 3 aserciones del probe quedaron intactas. Re-verificado end-to-end: migración reaplicable ×2 en local, los 4 gates de Parte B (incl. (5)+(5b) nuevo) + los 4 de Parte A + `test_function_acl_gate.sql` + `test_errcode_5char_gate.sql` en verde, backend `2253 passed, 4 skipped` (idéntico a la ronda 1, sin comportamiento nuevo), `openspec validate --changes --strict` 1/1, `check_docs_sync.py` OK. Detalle completo en `CHANGES.md`.

## 14. Parte C — Checkpoint y endpoints

- [x] 14.1 SAFETY NET: backend `2255 passed, 4 skipped` (baseline, medido antes de tocar código); frontend se corrió como cierre (19.2): `2670 passed, 1 failed` (el único fallo, `banco-conciliacion-import-warnings.test.tsx` por timeout de 20s, es el MISMO flaky pre-existente ya documentado por las Partes A/B — módulo de conciliación bancaria, cero archivos de ese módulo tocados por esta parte). Prod (read-only vía MCP, 2026-09-11): `MAX(version) = 20261047000001` (296 migraciones) — Parte B (`20261048000001`) sigue "en vuelo" (PR #558 en CI, sin mergear). Migración de esta parte: **`20261049000001`** confirmada. **Re-verificado en la ronda 1 de revisión adversarial (2026-09-12)**: prod ya avanzó a `MAX(version) = 20261048000001` (297 migraciones) — Parte B se mergeó entretanto. No cambia ninguna conclusión: Parte B nunca tocó las 6 funciones de esta parte (ver 14.2)
- [x] 14.2 Cuerpo vivo capturado: `rpc_change_member_role`/`rpc_accept_invitation` desde el archivo de la Parte A (ya mergeado, verificado sin drift — ver 14.1); `rpc_invite_member` (2 overloads) capturado en vivo vía `psql`/`pg_get_functiondef` contra la base local (que mirror­ea prod para estas funciones, nunca tocadas por la Parte B) — `md5` de `rpc_remove_member`/`rpc_my_account_role` (untouched por esta parte) verificado **idéntico** local=prod (`8bf8d703…`/`87da01c8…`), confirmando que el punto de partida es fiel
- [x] 14.3 RED: bloques (5)/(6)/(8)/(9)/(10)/(11)/(12) de `test_member_role_assignment_rpcs.sql` corridos contra el estado pre-migración (funciones inexistentes) y luego contra cada guard — autoridad insuficiente (P0403 ×3: admin→owner, admin→admin, no-miembro), catálogo (P0400), owner con vencimiento (P0406), invariante de propietario (P0405)
- [x] 14.4 GREEN: `rpc_assign_member_role`/`rpc_revoke_member_role` — `SECURITY DEFINER`, `search_path` fijo, `EXECUTE` sólo `authenticated`. Autoridad por JERARQUÍA sobre el ROL PEDIDO (no sobre la identidad del target, a diferencia de las RPCs legacy — ver nota en la migración, sección 1): owner cualquiera, admin nunca owner/admin. `rpc_list_account_members` (lectura, guard de tenencia inline) agregada para soportar 14.6
- [x] 14.5 RED: `test_member_repository.py` (8 tests), `test_member_service.py` (13 tests), `test_member_router.py` (11 tests) — los 32 corridos contra el código inexistente antes de escribirlo (ModuleNotFoundError / 404)
- [x] 14.6 GREEN: `backend/routers/members.py` + `services/members.py` + `repositories/member_repository.py` + `schemas/members.py` — `GET /members` (abierto a cualquier miembro), `POST /members/{user_id}/roles`, `DELETE /members/{user_id}/roles/{role}`, `DELETE /members/{user_id}` (reutiliza `rpc_remove_member` de la Parte A). Registrado en `main.py`. 32/32 tests GREEN

## 15. Parte C — Retiro del gate de plan y normalización de errores (D17, D18)

- [x] 15.1 RED→GREEN: `test_invite_member_roles_and_plan_gate.sql` bloques (1)/(2) — cuenta en plan `gratis`/`avanzado` (no-pro) promueve a admin vía `rpc_change_member_role` e invita con el rol admin vía `rpc_invite_member`, ambos sin rechazo
- [x] 15.2 RED→GREEN: bloque (3) — owner con 4 roles simultáneos sigue consumiendo 1 solo lugar del cupo; invitar a una 3ª persona con cupo para 5 y sólo 1 miembro real se acepta
- [x] 15.3 GREEN: paso "3. new_role/role='admin' requiere plan 'pro'" eliminado por completo (no comentado) de `rpc_change_member_role` y de AMBOS overloads de `rpc_invite_member`; resto de las validaciones intacto
- [x] 15.4 GREEN: `P0001`→`P04xx` conservando el texto del mensaje: `P0402` (cupo, nuevo, 403) y `P0407` (invitación duplicada, nuevo, 409) en los 2 overloads de `rpc_invite_member` + `rpc_accept_invitation` (cupo); `P0403` reutilizado para falta de autoridad. Mapeados en `backend/core/errors.py` + `test_errors_business_codes.py` (19→21 casos)
- [x] 15.5 GREEN: `rpc_invite_member(text,uuid,text[])` (DROP+CREATE, cambio de firma — el overload de 2 args queda con `DEFAULT` imposible por la ambigüedad 42725 ya conocida) invita con conjunto de roles; `account_invitations.roles text[]` nueva; sin roles declarados → `{viewer}`; `rpc_accept_invitation` concede TODAS las asignaciones del conjunto. Bloques (4)-(10) de `test_invite_member_roles_and_plan_gate.sql`
- [x] 15.6 GREEN: `knowledge-base/03_actores_y_roles.md` — fila "Roles internos" corregida de "❌ / ❌ / Básicos / Avanzados" a "Todos" en los 4 planes, con nota explicando la derogación de D17. `CLAUDE.md`/`AGENTS.md` no se tocaron (verificado por `git status`)

## 16. Parte C — Frontend: tipos y hook (D19)

- [x] 16.1 RED→GREEN: `__tests__/hooks/use-org-role.test.ts` — `isWriter` fail-open verificado para: `role=null` (indeterminado), error transitorio del RPC, y el valor heredado `"member"` (bloquea igual que `"viewer"` — los DOS valores de solo-lectura). **Corregido en la ronda 1 adversarial (19.6, major 1)**: este criterio literal (`role !== "viewer" && role !== "member"`) resultó ser el propio BUG — `"member"` es el valor AMBIGUO al que colapsa tanto un viewer real como cualquier rol funcional (`rpc_my_account_role` sólo distingue owner/admin/resto). El test se reescribió para mockear el CONJUNTO real (`rpc_my_active_account_roles`) + el catálogo por separado, con 11 tests (antes 7)
- [x] 16.2 RED→GREEN: mismo archivo — `useOrgRole()` devuelve `{role, roles, isWriter, isLoading}`, `role` con la MISMA forma singular que antes (owner/admin/member/rol funcional)
- [x] 16.3 GREEN: `OrgRole` (`lib/types.ts`) pasa a la unión de 8 códigos + `"member"` legacy; `useOrgRole.ts` devuelve el objeto de 4 campos
- [x] 16.4 GREEN (superado por la ronda 1, ver 16.1/19.6): `isWriter = role !== "viewer" && role !== "member"` — verificado con "viewer" (antes incorrectamente escritor) y "member" (ya bloqueaba, sigue bloqueando). Este criterio literal es EXACTAMENTE lo que la ronda 1 adversarial reprodujo como MAJOR: un `seller`/`cashier`/`stock`/`purchases`/`accountant` también resuelve `role==="member"` y quedaba `isWriter=false` — corregido derivando `isWriter` del conjunto real de roles activos contra `is_writer` del catálogo, no del singular
- [x] 16.5 Recorrido completo: único `Record<OrgRole,...>` de 3 entradas era el propio `/organizacion/roles/page.tsx` (reescrito en el grupo 17); el resto de los ~35 archivos que mencionan `OrgRole`/`accountRole`/`isWriter` usa comparaciones `role === "owner"` (compatibles con la unión ampliada) o mockea el hook sin tipar estrictamente (`vi.fn()` sin genéricos) — confirmado con la suite completa (16.1 arriba) sin regresiones. `accountRole` de `auth-context.tsx` NO se toca (sigue derivando de `account_members.role`, vocabulario legacy owner/admin/member, subconjunto válido del `OrgRole` ampliado)

## 17. Parte C — Superficie de gestión de roles

- [x] 17.1 RED→GREEN: cobertura vía `use-members.test.ts` (7 tests: listado, invalidación) + `use-role-catalog.test.ts` (3 tests: etiquetas desde catálogo) — la pantalla en sí (`page.tsx`) no lleva test de componente dedicado (declarado; la pasada visual del grupo 18 la hace el revisor con el stack local)
- [x] 17.2 GREEN: `/organizacion/roles/page.tsx` reescrita EN LA MISMA RUTA — listado con badges de roles por miembro (vigente/vencida distinguidas visualmente), `AssignRoleDialog` (Select + `<input type="date">` opcional), revocar por rol (botón en el badge), quitar miembro (`AlertDialog` de confirmación), estado vacío ("único miembro")
- [x] 17.3 GREEN: `hooks/data/use-role-catalog.ts` (`useRoleCatalog`, lee `account_role_catalog` directo vía RLS) reemplaza el `ROLE_LABELS`/`ROLE_ICONS`/`ROLE_COLORS` hardcodeado
- [x] 17.4 GREEN: aviso "El rol Admin está disponible solo en el plan Pro" y `availableRoles` gateado por `billingPlan` eliminados de `page.tsx` (reescritura completa, D17)
- [x] 17.5 GREEN: `/organizacion/invitar/page.tsx` reescrita — checkboxes contra `useRoleCatalog()` (conjunto), `rpc_invite_member` con `p_roles`, `availableRoles` ya no depende de `billingPlan === "pro"`. **Ronda 1 adversarial (19.6, major 2)**: la pantalla prometía *"Se enviará un link de aceptación"* sin que nada lo enviara, y descartaba el `token` que `rpc_invite_member` devuelve — `rpc_accept_invitation` no tiene NINGÚN llamador en toda la app (sin ruta `/invitacion/[token]`, sin email). Corregido el mínimo honesto: copia corregida + token visible/copiable tras crear la invitación. **OQ para el PO, sin resolver en este apply**: falta la ruta de aceptación (`/invitacion/[token]` → `rpc_accept_invitation`) — sin ella, invitar con un conjunto de roles no llega a concederse nunca por esta vía; candidato propio si el PO lo prioriza
- [x] 17.6 GREEN: `TeamSection.tsx` (`/configuracion`) NO se tocó — sus `Link` a `/organizacion/roles` y `/organizacion/invitar` siguen apuntando a las mismas rutas; su formulario de "quick invite" (`rpc_invite_member` de 2 args) queda intacto, verificado que ese overload sigue vivo con la misma firma (14.2/gate de introspección)
- [x] 17.7 GREEN: `useMembers()` invalida `["members","list",accountId]` + `["orgRole",accountId]` + `["teamMembers",accountId]` en las 3 mutaciones (assign/revoke/remove); `/organizacion/invitar` invalida `memberCount`/`teamMembers`/`members` tras invitar. Verificado en `use-members.test.ts` (TRIANGULATE)

## 18. Parte C — Pasada visual

- [x] 18.1 Pasada visual en las **4 combinaciones** (375x812 y 1440x900 x claro/oscuro) de `/organizacion/roles` y `/organizacion/invitar` — corrida por el REVISOR (ronda 1 adversarial) contra el stack local completo (Supabase local + FastAPI + `next dev`, usuario real vía GoTrue Admin API, cuenta de prueba con 3 miembros: owner, `seller`+`accountant` VENCIDO, `cashier` con vencimiento futuro + `stock`). 8 capturas en `scratchpad/rbac-c/visual/` + 2 del dialogo de asignar. **2 defectos encontrados y corregidos** (ver 18.2 y el fix de fechas): la pantalla mostraba `Invalid Date` en TODAS las fechas y el vencimiento elegido se guardaba un dia antes.
- [x] 18.2 Tokens semanticos y contraste AA verificados por el REVISOR midiendo el ratio WCAG real en el navegador (composicion alpha sobre el fondo efectivo), no por inspeccion del codigo. **Hallazgo (ronda 1)**: el badge de propietario heredaba el literal de paleta `bg-yellow-500/15 text-yellow-600 border-yellow-500/30 dark:text-yellow-400` del `ROLE_COLORS` viejo — **2,66:1 en tema claro**, por debajo del 4,5:1 de AA, y FUERA del alcance del gate `token-contrast-aa` (que solo cubre los tokens semanticos de `globals.css`). Corregido al par canonico `bg-warning/15 text-warning border-warning/30` (mismo patron que `ClientActivityBadge`/`ExpenseJournalStatusBadge`). **CORRECCIÓN (ronda 2 adversarial)**: la afirmación "re-medido: minimo de la pagina 4,83:1 (movil claro) / 6,96:1 (movil oscuro); 0 fallos AA en la superficie de este change" de esta misma task NO era reproducible — esos dos números son los del TOKEN a opacidad PLENA (`text-muted-foreground`/`text-primary`), no del elemento renderizado: `RoleAssignmentBadge` aplicaba además `opacity-60`/`opacity-80` encima de esos tokens (rol vencido y fecha), lo que bajaba el contraste real a 1,92:1–3,76:1 en tema claro, por debajo de AA. Corregido en la ronda 2 (ver 19.7): se retiraron los dos `opacity-*` y se agregó `__tests__/lib/rbac-admin-opacity-contrast-gate.test.ts`, que cierra el punto ciego de `token-contrast-aa` (no ve una utilidad `opacity-*` aplicada en una pantalla). Re-medido tras el fix: mínimo real de la página **4,83:1** (móvil claro) / **6,96:1** (móvil oscuro), 0 fallos AA — el número no cambió porque, sin la opacidad, el elemento SÍ coincide con el token a plena opacidad; lo que cambió es que ahora es una medición del elemento real, no del token. Los fallos que quedan (`PRINCIPAL`/`OPERACIONES`/... 2,61:1, `text-sidebar-foreground/60` 3,3:1) son del **shell compartido**, PREEXISTENTES: medidos identicos en `/clientes` y `/gastos`, rutas que este change no toca (control ejecutado).
- [x] 18.3 Sin desborde horizontal verificado por el REVISOR en las 4 combinaciones: `document.documentElement.scrollWidth <= clientWidth` en las 8 corridas (375px incluido). Ademas, en la misma pasada: foco visible confirmado en los controles (ring real por `box-shadow`, medido tabulando), `<input type="date">` operable por teclado (se tipeo `31/12/2026` y el `value` quedo `2026-12-31`), y el listado refresca tras asignar y tras revocar **con 0 navegaciones de pagina** (invalidacion de React Query, sin recargar).
- [x] 18.4 Accesibilidad de los controles nuevos: `<Label htmlFor>` en el `Select` de rol y en el `<input type="date">` (nativo, keyboard-operable por construcción — mismo patrón que `EditChargeDueDateDialog`); `role="list"`/`role="listitem"` + `aria-label` en los badges de rol y en sus botones de revocar; `aria-label` en el botón de quitar miembro; foco visible heredado de los componentes base (`Button`/`Badge`/`Select`/`Checkbox` shadcn, sin override de outline)

## 19. Parte C — Cierre

- [x] 19.1 2 gates SQL nuevos cableados en `KPI_Validation.yml` (al final de la lista de steps) + migración `20261049000001` sumada a la cadena de reaplicación idempotente, inmediatamente después de `20261048000001`. **Hallazgo real**: el gate de la Parte A (`test_membership_rpcs_pivot_rewrite.sql`) reaplica su propio archivo vía `\i` como parte de su propia verificación — eso revertía silenciosamente el D17/D18 de esta parte si corría después; corregido agregando un `\i` de reconvergencia del archivo de esta Parte C inmediatamente después, dentro de ese mismo gate (Fase 13b-reconverge). Verificado corriendo la cadena completa en el orden real de CI (A→B→C) sin que ningún gate posterior revierta a otro
- [x] 19.2 SAFETY NET de cierre: backend `2287 passed, 4 skipped` (2255 baseline + 32 nuevos); frontend `2670 passed, 1 failed` (mismo flaky pre-existente de 14.1); `tsc --noEmit` sin errores nuevos (baseline 9, idéntico)
- [x] 19.3 Verificación post-merge en prod — **completada 2026-09-12** (medida por el orquestador vía `mcp__supabase__execute_sql`, sólo lectura, tras el deploy de `02e55954`): `MAX(version) = 20261049000001` (298 migraciones). `rpc_invite_member` con exactamente 2 firmas vivas: `(p_email text, p_account_id uuid)` y `(p_email text, p_account_id uuid, p_roles text[])`; ambas usan `extensions.gen_random_bytes` (el fix del hallazgo real de la Parte C — antes ambas estaban rotas en prod). Gate "`admin` requiere plan `pro`" ausente en `rpc_change_member_role` (ya no lee `billing_plan`/plan alguno) y en ambas `rpc_invite_member` (sólo leen `plan_limits.max_users` para el cupo de MIEMBROS — D18, correcto). `rpc_revoke_member_role` con el chequeo síncrono `P0405` (único owner) antes del `DELETE`. RPCs nuevas `rpc_assign_member_role`, `rpc_revoke_member_role`, `rpc_list_account_members`: una sola definición cada una, ACL `{postgres, service_role, authenticated}`, ninguna de las 6 RPCs de membresía con `anon`. Datos sin cambios: 39 cuentas / 39 miembros / 39 filas en el pivot (todas owner) / 0 invitaciones / 39 filas `role.%` en `audit_logs` (el backfill de la Parte A). Todo idéntico a lo esperado por el design — sin hallazgos
- [ ] 19.4 Humo real del PO — **pendiente**: requiere el PO operando en prod tras el merge
- [ ] 19.5 Confirmar `audit_logs` (`role.assigned`/`role.expired`) — **pendiente**: depende de 19.4
- [x] 19.6 **Ronda 1 de revisión adversarial aplicada (pre-merge, worktree `wt-rbac-c`, 0 blocker)**: **2 major + 4 minor + 5 nit**, todos aplicados salvo 1 registrado como OQ para el PO. **Major 1**: `isWriter` nunca veía un rol funcional (`rpc_my_account_role` colapsa a `'member'` cualquier conjunto sin owner/admin — un `seller`/`cashier`/`stock`/`purchases`/`accountant` quedaba de solo-lectura en 34 archivos, el POS le bloqueaba la venta, exactamente lo que 19.4 iba a descubrir en prod). Corregido: `useOrgRole.ts` deriva `isWriter` del conjunto real (`rpc_my_active_account_roles()`, Parte B) contra `is_writer` del catálogo, fail-open preservado mientras el conjunto está indeterminado. **Major 2**: `/organizacion/invitar` prometía un email que nadie envía y descartaba el `token` de `rpc_invite_member` — `rpc_accept_invitation` no tiene NINGÚN llamador en la app (sin ruta `/invitacion/[token]`). Corregido el mínimo honesto (copia + token visible/copiable); **OQ para el PO, registrada, no resuelta en este apply**: la ruta de aceptación queda fuera de alcance — sin ella, el conjunto de roles de una invitación nunca se llega a conceder por esa vía. **Minors**: guard nuevo en `rpc_assign_member_role`/`rpc_revoke_member_role` (un admin no podía tocar el conjunto de un target owner/admin pidiendo un rol operativo); `DELETE /members/{user_id}` ahora 404 para un target que no es miembro (antes 200 `{ok:true}` sin haber hecho nada); 6 bloques nuevos en `test_member_role_assignment_rpcs.sql` (14→20, autoridad de un miembro sin rol + guard nuevo); `rpc_accept_invitation` mapea "token inválido/vencido"→`P0404` y "ya es miembro"→`P0409` (2 bloques nuevos, 10→12 en `test_invite_member_roles_and_plan_gate.sql`). **Nits**: checkpoint de prod corregido (`20261048000001`, 297 migraciones — Parte B ya mergeada); checkbox de roles con `rounded-[3px]`; test de invalidación de `use-members.test.ts` ahora fija la clave exacta (+ test espejo para `revokeRole`); `rpc_accept_invitation` mapea 2 de sus 3 `P0001` restantes a `P0404`/`P0409` (ver arriba). `database.types.ts` sigue sin regenerar — intentado contra el stack local en esta ronda (`npx supabase gen types typescript`), bloqueado por un `LegacyPgDeltaSslProbeError` del entorno (TCP conecta, el probe SSL del CLI se corta); queda documentado como candidato con causa concreta, no como "no se tocó". CRLF de los 3 archivos SQL nuevos investigado y descartado como no-issue (repo con `core.autocrlf=true` sin `.gitattributes` — TODO archivo se normaliza a LF puro en el blob al hacer `git add`, verificado contra el propio HEAD de la Parte A). Re-verificado: migración reaplicable ×2, `test_member_role_assignment_rpcs.sql` 20/20, `test_invite_member_roles_and_plan_gate.sql` 12/12, los 4 gates de Parte A + los 4 de Parte B + el reconverge de `test_membership_rpcs_pivot_rewrite.sql` (13/13) + `test_function_acl_gate.sql` + `test_errcode_5char_gate.sql` en verde. Backend (`python -m pytest backend/tests -m "not integration"`, la invocación real de CI): `2289 passed, 1 skipped, 5 deselected` (2287+8 nuevos). Frontend: `2684 passed, 1 failed` sobre 2685 (mismo flaky preexistente); `tsc --noEmit` 9 errores, idéntico al baseline de 19.2. Detalle completo en `CHANGES.md`.
- [x] 19.7 **Ronda 2 de revisión adversarial aplicada (pre-merge, worktree `wt-rbac-c`, verificación completa: `db reset` + 77 gates en el orden real de CI, reaplicación ×3)**: **2 major + 3 minor + 6 nit** — 10 aplicados con fix + test/gate, 1 minor documentado como candidato preexistente (no corregido en esta parte). **Major 1** (bloqueante): `rpc_revoke_member_role` sólo aplicaba el invariante de propietario vía el constraint trigger DIFERIDO de la Parte A — bajo `v31-tenancy-pool-rls` la transacción del request commitea DESPUÉS de que FastAPI ya envió la respuesta, así que `DELETE /members/{owner}/roles/owner` del único owner respondía **200** con la escritura ya revertida (`RuntimeError: Caught handled exception, but response already started` en el log del backend). Corregido con un chequeo SÍNCRONO nuevo (paso 3.5, mismo predicado/texto/ERRCODE que `fn_guard_account_owner_invariant`) ANTES del `DELETE`; el constraint trigger diferido queda como backstop. Bloque (21) nuevo en `test_member_role_assignment_rpcs.sql` (revoke del único owner SIN `SET CONSTRAINTS ... IMMEDIATE`, a diferencia del (12)) + test nuevo en `backend/tests/test_member_router.py`. **Major 2** (ya corregido por el revisor en el worktree, conservado): `RoleAssignmentBadge` aplicaba `opacity-60`/`opacity-80` encima de tokens AA — 1,92:1–3,76:1 en claro. Retirados los `opacity-*` + gate nuevo `__tests__/lib/rbac-admin-opacity-contrast-gate.test.ts`; redacción de 18.2 corregida (afirmaba una re-medición del elemento que en realidad medía el token). **Minors**: cleanup de `test_member_role_assignment_rpcs.sql`/`test_invite_member_roles_and_plan_gate.sql` dejaba huérfanas `account_member_roles`/`payment_methods`/`product_categories` de las cuentas auto-aprovisionadas — corregido capturando el array de ids de cuenta ANTES del `DELETE FROM accounts` y assertando saldo cero (0 huérfanas re-medidas); `useMembers().invalidateAll()` no invalidaba `["orgActiveRoles", accountId]` (la clave de la que depende `isWriter` desde la ronda 1) — sumada + 2 tests extendidos; race read-your-own-write en `GET /members` tras una mutación (mismo origen que Major 1, preexistente, afecta a todo endpoint bajo la palanca) — **no corregido en esta parte**, candidato en `CHANGES.md`. **Nits**: JSDoc de `formatDate` reordenado (vivía huérfano encima de `localDateEndOfDayISO`); comentarios extensos de los pasos 1/1.5/1.6 de `rpc_change_member_role` restituidos verbatim desde el cuerpo vivo de prod; con 1 solo miembro `/organizacion/roles` mostraba a la vez el estado vacío y la fila — el aviso pasó a un `CardFooter` después de la lista; el badge de owner nunca ofrecía revocar ni con 2 owners activos — corregido con `activeOwnerCount` + `canTransferOwnership`; `useOrgRole.ts` devolvía `roles=["member"]` con `isWriter=false` para el mismo estado "conjunto vacío" — corregido para que `roles` refleje también el vacío genuino, con caso nuevo en `use-org-role.test.ts`. Marcador `PR #PRC` de `CHANGES.md` queda sin resolver (no reemplazable hasta que el PR real exista) — **resuelto en la ronda 3 como `PR #559`**. Re-verificado: migración reaplicable ×2 (`GATE INTROSPECCION ... OK` ambas), `test_member_role_assignment_rpcs.sql` **21/21**, `test_invite_member_roles_and_plan_gate.sql` **12/12**, los 4 gates de Parte A + los 4 de Parte B (mismo bloque 5 degradado) + `test_function_acl_gate.sql` + `test_errcode_5char_gate.sql` en verde. Backend: `python -m pytest backend/tests -m "not integration"` → **2290 passed, 1 skipped, 5 deselected** (2289+1 nuevo). Frontend: los 5 archivos tocados/nuevos → 35/35 passed; suite completa **2687 passed, 2 failed** sobre 2689 (mismo flaky preexistente de `banco-conciliacion-import-warnings.test.tsx` que ya documentó la ronda 1 con 1 fallo — esta corrida, más lenta por carga del entorno, timeouteó una segunda prueba del MISMO archivo; cero archivos de ese módulo tocados por esta ronda); `tsc --noEmit` **9 errores**, idéntico al baseline. Detalle completo en `CHANGES.md`.

## 20. Cierre del change completo

- [x] 20.1 Actualizada la ficha `### \`v3-rbac-multirole\`` de `CHANGES.md` con el resultado de las tres partes (estado ✅ COMPLETADA con los 5 PRs), la verificación de prod al archivar, los humos pendientes del PO, el estado de las 3 OQs y los candidatos que deja
- [x] 20.2 Candidato **`rbac-rls-grano-fino`** (D12) ya estaba dado de alta con su alcance desde la Parte B (helpers `is_account_writer_for(account_id, domain)` y migración de las 48 policies) — reconfirmado y listado de nuevo en el cierre de la ficha de `CHANGES.md`
- [x] 20.3 Registrado en la ficha de `CHANGES.md`: OQ-2 y OQ-3 quedan tal como recomendaba el design (sin cambios); OQ-1 (selector de cuenta activa) queda con su premisa invalidada por el hallazgo de la ronda 3 de la Parte C (`auth-context.tsx` con `.single()`) — pasa a candidato con nombre propio `cuenta-activa-selector`
- [x] 20.4 Specs sincronizadas: `account-membership-roles` (nueva, 12 requirements) creada en `openspec/specs/`; `org-roles`, `multi-tenant`, `authz-token-claims`, `backend-auth`, `document-status-history` actualizadas (ADDED/MODIFIED/REMOVED aplicados, headers `### Requirement:` exactos). `openspec validate --specs --strict` → **103/103** (era 102/102). `openspec validate --changes --strict` y `openspec list --json` → sin changes activos. **Nota**: la verificación "en HEAD" del gotcha de `openspec archive` aplica una vez que este archive se commitee (esta sesión no commitea ni pushea, por regla dura del brief) — pendiente de confirmar en ese momento
- [ ] 20.5 Notificar a los 4 changes represados (`v4-seguridad-04`/`05`, `v4-ia-09`/`v3-ai-agent-mcp-tools`, `v4-plataforma-01`, `v4-seguridad-09`) que su dependencia quedó satisfecha — **diferido**: no editar sus secciones individuales estaba fuera del alcance explícito de este archive (ver brief); la ficha ✅ COMPLETADA de `CHANGES.md` es la fuente que sus propios `/opsx:propose` deben releer
