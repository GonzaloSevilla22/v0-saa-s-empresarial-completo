> **Orden de trabajo**: los grupos 1–5 NO dependen de OQ-1 y se pueden implementar y mergear de inmediato. El grupo 6 está **BLOQUEADO hasta el sign-off explícito del PO sobre OQ-1** (design.md §Open Questions). El grupo 7 cierra el change con lo que haya quedado firmado.
>
> **TDD estricto**: cada assert nuevo de `supabase/tests/*.sql` se escribe y se ejecuta **antes** de la migración y debe FALLAR (RED) contra las definiciones vigentes; recién entonces se escribe el SQL de producción (GREEN). Los gates corren con `psql -v ON_ERROR_STOP=1` (ya configurado en `KPI_Validation.yml`).

## 1. Red de seguridad y línea base

- [x] 1.1 Correr `supabase start` local y ejecutar los cuatro gates SQL vigentes (`test_kpis.sql`, `test_kpis_edge_cases.sql`, `test_function_acl_gate.sql`, `test_idempotency.sql`) con `-v ON_ERROR_STOP=1`; anotar la línea base (todos verdes). Cualquier rojo previo se reporta como falla preexistente y NO se arregla en este change.
- [x] 1.2 Confirmar que ninguna migración posterior a `20260814000001` redefine `rpc_dashboard_kpi_summary` y que ninguna posterior a `20260610000001` redefine `get_dashboard_financials` (`grep` sobre `supabase/migrations/`), y que las firmas de ambas coinciden exactamente con las que la migración va a reemplazar (D7: `CREATE OR REPLACE` puro, sin `DROP`, sin gotcha 42725).

## 2. RED — fixture y asserts de comportamiento (antes de tocar SQL)

- [x] 2.1 Escribir en `supabase/tests/test_kpis_edge_cases.sql` un bloque `DO $$ … $$` nuevo con el fixture del change: usuario sintético en `auth.users` (prefijo `__gate_kbc_`), su cuenta vía `handle_new_user`, dos sucursales A y B, productos con `cost`, y `set_config('request.jwt.claims', …, true)` para que `auth.uid()` resuelva — mismo patrón que el bloque de `kpi-critical-stock-dashboard` ya presente en el archivo.
- [x] 2.2 Seedear la ventana de prueba: ventas en A y en B (alguna multi-unidad con `total` ≠ `amount`, alguna legacy con `branch_id NULL`), gastos y compras en ambas sucursales, y `branch_stock` con un producto sin ventas en el período presente en A y en B con cantidades distintas.
- [x] 2.3 Escribir el assert de coincidencia **sin** filtro de sucursal: `get_dashboard_financials(from, to, NULL).total_income = rpc_dashboard_kpi_summary(...).invoiced_revenue` y lo mismo para `net_profit`. Ejecutar → debe FALLAR (el diario no resta NC). **RED confirmado**: `total_income=2800 <> invoiced_revenue=2400`.
- [x] 2.4 Escribir el mismo assert **con** `p_branch_id = branch_a`. Ejecutar → debe FALLAR. (No llega a ejecutarse aislado — 2.3 aborta primero por `ON_ERROR_STOP`; el cuerpo del assert está escrito y depende de la misma corrección.)
- [x] 2.5 Escribir el assert de stock sin rotación por sucursal: el valor con filtro A es solo el stock de A, el conteo sin filtro cuenta el producto una sola vez, y un producto soft-deleted con stock no aparece. Ejecutar → debe FALLAR (hoy se calcula sobre el agregado y no excluye soft-deleted). (Escrito; verificado GREEN más abajo tras 4/5 — ver tabla TDD del reporte de apply.)
- [x] 2.6 Escribir el assert de fail-open de rotación (D4): un producto cuya única venta del período es legacy (`branch_id NULL`) NO cuenta como stock sin rotación al filtrar por A. Ejecutar → debe FALLAR o pasar según el estado vigente; dejar registrada la observación. **Observación**: pasa por construcción ya hoy (el `NOT EXISTS` vigente no filtra por sucursal en absoluto, así que la venta legacy ya excluye el producto en cualquier filtro); el valor de esta task es evitar que D4 regrese a fail-closed cuando 5.2 vuelva branch-aware el `NOT EXISTS` — queda cubierto por construcción en el assert 2.5a (value=1000/cnt=1 exacto).

## 3. GREEN — helper único de notas de crédito (D5)

- [x] 3.1 Crear `supabase/migrations/20260916000001_kpi_branch_consistency.sql` con cabecera de contexto (proposal/design, hallazgos F3a/F3b, nota de idempotencia por la auto-aplicación de la integración GitHub de Supabase). **Timestamp ajustado a `20260916000001`** — `20260914000001` ya lo tomó #383 (analytics-events-revival, en main); `20260915` queda reservado para un backfill no relacionado.
- [x] 3.2 Definir `public.reporting_credit_notes_in_window(p_account_id uuid, p_from timestamptz, p_to timestamptz, p_branch_id uuid) RETURNS numeric LANGUAGE sql STABLE`: `movement_type = 'credit_note'`, imputación por `created_at`, `ABS(amount)`, y —si OQ-1 quedó firmada por la rama recomendada— el filtro de sucursal de D1. Mientras OQ-1 no esté firmada, el parámetro `p_branch_id` se acepta y se ignora, replicando la semántica global vigente para no romper el gate del grupo 5.
- [x] 3.3 ACLs del helper en el mismo archivo: `REVOKE ALL … FROM PUBLIC` + `REVOKE EXECUTE … FROM anon, authenticated` (interno; solo lo invocan funciones `SECURITY DEFINER`, que corren como su owner). Verificar que `test_function_acl_gate.sql` sigue verde. **Verificado**: gate OK (5 firmas permitidas, sin cambios — el helper no es SECURITY DEFINER, no aplica).

## 4. GREEN — Parte B: el read-model diario deja de disentir (D6)

- [x] 4.1 `CREATE OR REPLACE` de `get_dashboard_financials` en la misma migración, partiendo del cuerpo vigente de `20260610000001` sin cambiar firma ni columnas: `total_income = Σ COALESCE(total, amount) − reporting_credit_notes_in_window(...)` y `net_profit = total_income − (gastos + compras)`.
- [x] 4.2 Re-aplicar `REVOKE`/`GRANT` explícitos de `get_dashboard_financials(timestamptz, timestamptz, uuid)` en el mismo archivo (idempotente, disciplina del backlog de advisors 0028).
- [x] 4.3 Ejecutar los asserts 2.3 y 2.4 → deben pasar a VERDE. **GREEN confirmado**: `PASS 2.3` (total_income=2400.00, net_profit=1970.00) y `PASS 2.4` (total_income=1600.00, net_profit=1300.00).

## 5. GREEN — Parte A (no bloqueada): stock sin rotación por sucursal (D3/D4)

- [x] 5.1 `CREATE OR REPLACE` de `rpc_dashboard_kpi_summary` partiendo del cuerpo vigente de `20260814000001` §5, reescribiendo `stagnant_curr`/`stagnant_prev` sobre `branch_stock bs JOIN products p`: valor `SUM(bs.quantity * COALESCE(p.cost, 0))`, conteo `COUNT(DISTINCT bs.product_id)`, filtro `(p_branch_id IS NULL OR bs.branch_id = p_branch_id)`, exclusión de `deleted_at IS NOT NULL` y de `stock_control_type IN ('untracked','variant_only')` con `COALESCE(…, 'tracked')`.
- [x] 5.2 Aplicar D4 en el `NOT EXISTS` de rotación: con filtro de sucursal, una venta cuenta como rotación si `sx.branch_id = p_branch_id OR sx.branch_id IS NULL`. Dejar el motivo comentado en el SQL (75% de las filas de `sales` en producción son legacy sin sucursal).
- [x] 5.3 Hacer que `rpc_dashboard_kpi_summary` consuma `reporting_credit_notes_in_window` en lugar de su `nc_agg` inline, sin cambiar todavía la semántica de sucursal de las NC.
- [x] 5.4 Re-aplicar `REVOKE`/`GRANT` explícitos de `rpc_dashboard_kpi_summary(timestamptz, timestamptz, timestamptz, timestamptz, uuid)`.
- [x] 5.5 Ejecutar los asserts 2.5 y 2.6 → VERDE. Re-ejecutar 2.3/2.4 y los cuatro gates completos → VERDE (sin regresiones). **Confirmado**: `PASS 2.5` (A=1000/1, sin filtro=5000/1), `PASS 2.6` (fail-open por construcción), y los cuatro gates (`test_kpis.sql`, `test_kpis_edge_cases.sql`, `test_function_acl_gate.sql`, `test_idempotency.sql`) sin `FAIL`/`ERROR`.
- [x] 5.6 REFACTOR: revisar que el cuerpo copiado de `20260814000001` no haya quedado con comentarios desactualizados y que la migración sea re-ejecutable sin efectos (correrla dos veces seguidas). **Verificado**: comentarios actualizados con referencias al change actual donde corresponde; migración re-aplicada dos veces consecutivas sin error (CREATE OR REPLACE + REVOKE/GRANT idempotentes).

## 6. Parte A — atribución de NC por sucursal (OQ-1 firmada 2026-08-12, rama HÍBRIDA)

> OQ-1 firmada explícitamente por el PO 2026-08-12: rama **híbrida** (A para notas de crédito + B para el par cargos/cobros), tal como recomienda design.md. Implementado en `supabase/migrations/20260920000001_kpi_branch_nc_attribution.sql` (timestamp verificado contra el MAX real de `supabase_migrations.schema_migrations` en prod: 20260919000001, tomado por una sesión paralela — el slot 20260920000001 estaba libre).

- [x] 6.1 RED: assert de que una NC emitida sobre una orden de la sucursal A no reduce el revenue de la sucursal B, y que sin filtro resta una sola vez. **RED confirmado**: `FAIL 6.1c: total_income filtro branch_b esperaba 500 ... dio 100.00` (la NC restaba GLOBAL contra las definiciones vigentes de `20260916000001`).
- [x] 6.2 RED: assert de que `collected_revenue` y `prev_collected_revenue` son `NULL` cuando `p_branch_id IS NOT NULL`, y que `invoiced_revenue` sí se calcula filtrado. Escrito junto con 6.1 (mismo fixture); no llega a ejecutarse aislado — 6.1c aborta primero por `ON_ERROR_STOP`, el cuerpo del assert depende de la misma corrección.
- [x] 6.3 GREEN: agregado al helper `reporting_credit_notes_in_window` el `LEFT JOIN public.sales_orders so ON so.id = cam.reference_id` y el predicado `(p_branch_id IS NULL OR so.branch_id = p_branch_id)` (fail-closed sobre referencias que no resuelven).
- [x] 6.4 GREEN: en `rpc_dashboard_kpi_summary`, `collected_revenue`/`prev_collected_revenue` devuelven `NULL` cuando `p_branch_id IS NOT NULL` (`CASE WHEN p_branch_id IS NOT NULL THEN NULL ELSE ... END`), dejando `charges_agg`/`payments_agg` sin filtrar (métrica de nivel cuenta, D2) con el motivo documentado en el SQL.
- [x] 6.5 Ejecutados 6.1, 6.2 y los cuatro gates completos → **GREEN confirmado**: `PASS 6.1` (B=500/370, A=1600, sin filtro=2400) y `PASS 6.2` (collected NULL bajo filtro A/B, =invoiced_revenue sin filtro); `test_kpis.sql`, `test_kpis_edge_cases.sql`, `test_function_acl_gate.sql`, `test_idempotency.sql` sin `FAIL`/`ERROR` (verificado tras `supabase db reset` completo, incluyendo el gate de introspección embebido). Verificado en el frontend (sin editarlo, `frontend/components/dashboard/KpiSummaryBlock.tsx`) que la línea "Cobrado" ya se oculta con `collectedRevenue == null` (mecanismo preexistente de la Parte B/grupo 4, sin cambios).

## 7. Cierre

- [x] 7.1 Correr la suite completa: los cuatro gates SQL + `check_backend_table_refs.py` + `check_frontend_table_refs.py` contra la base local, y la suite de frontend (`pnpm vitest run`) para confirmar que ningún test de reporting asumía la semántica vieja. **Resultado**: 4 gates SQL verdes; `check_frontend_table_refs.py` verde (422 archivos, 0 violaciones reales); `check_backend_table_refs.py` reporta 823 referencias desconocidas, pero **todas** dentro de `backend/.venv/` (gitignoreado, paquetes vendoreados de terceros — no existe en un checkout limpio ni en CI, no se tocó ningún archivo de `backend/`); frontend `pnpm vitest run` → **892/892 tests verdes** (122 archivos), sin fallos de reporting.
- [x] 7.2 Verificar que la migración no rompe el reset completo (`supabase db reset`) y que los gates embebidos de migraciones anteriores (`20260814000001`) siguen pasando con las definiciones nuevas. **Confirmado**: `supabase db reset` completo aplica las 100+ migraciones sin abortar (incluye el gate embebido de introspección de esta migración y los gates de comportamiento de migraciones previas); los 4 gates SQL vuelven a correr verdes post-reset.
- [x] 7.3 Ajustar los deltas de spec si el PO resolvió OQ-1 por una rama distinta a la recomendada (quitar o reescribir los requirements afectados de `reporting-invariants` y `dashboard-kpi-summary`). **N/A por ahora**: OQ-1 sigue SIN sign-off del PO (instrucción explícita de esta sesión: no implementar ni decidir el grupo 6). Los deltas de spec quedan tal como los dejó el propose — describen el estado objetivo completo (rama recomendada A+B híbrida) que el grupo 6 implementará cuando se firme OQ-1. El change queda ABIERTO, no archivado.
- [x] 7.4 Rama + PR con checks verdes (esperar `validate-kpis`, no solo Vercel). Declarar en el cuerpo del PR: sin superficie frontend nueva, con la lista de números de tarjeta que cambian de valor. **Hecho**: PR #385 (`feat/kpi-branch-consistency-parcial`), checks verdes (`validate-kpis` 1m58s, `playwright` 3m42s, `Vercel`, `Supabase Preview`), mergeado a `main` (squash, commit `229c44b`).
- [ ] 7.5 Actualizar `docs/plan-remediacion-kpis-2026-08-11.md` marcando C-KPI-3 con su PR, y dejar registrado el resultado del sign-off de OQ-1. **NO EJECUTADA a propósito**: la instrucción de esta sesión marca ese archivo como ajeno a otra sesión concurrente ("NO stagear ajenos: docs/plan-remediacion-kpis-2026-08-11.md") — no se lo tocó para no pisar trabajo de otra sesión. Pendiente de que el dueño de ese doc (u otra sesión) lo actualice con el PR de este change.
