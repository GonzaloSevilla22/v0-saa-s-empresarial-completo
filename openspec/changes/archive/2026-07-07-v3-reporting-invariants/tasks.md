## 0. Sign-off del PO (bloquea el apply)

- [x] 0.1 Presentar al PO el shift de números visibles (design.md D1/D7 + tabla de hechos): revenue de `/rentabilidad`, `/reportes/comparativo` y `/reportes/sucursal` sube ~+17,5 % agregado; operaciones del comparativo baja para ventas multi-línea. Registrar el visto (OQ1) y la decisión de comunicación (nota de release vs aviso in-app) en design.md antes de tocar código. **Visto obtenido 2026-07-06**: OQ1 aprobado (fix ya, sin comunicación especial — es corrección de bug); OQ2 aprobado (línea "Cobrado" alcanza, D8); OQ3 queda abierto no bloqueante. Registrado en engram `opsx/v3-reporting-invariants/po-signoff`.

## 1. Migración `20260814000001_v3_reporting_invariants.sql` (TDD: gates RED→GREEN dentro de la migración, patrón C2/C3)

- [x] 1.1 Capturar con `pg_get_functiondef` los cuerpos VIGENTES en prod de `rpc_dashboard_kpi_summary`, `rpc_product_profitability`, `rpc_period_comparison` y `rpc_branch_report`, y citarlos como base en el header de la migración (regla: partir SIEMPRE del cuerpo vivo, no del archivo de migración histórico).
- [x] 1.2 Crear helper `reporting_local_today()` → `(now() AT TIME ZONE 'America/Argentina/Mendoza')::date`, `STABLE`, con COMMENT que documente la constante de plataforma (design D5) y el punto de extensión futuro a timezone por organización.
- [x] 1.3 `CREATE OR REPLACE rpc_product_profitability`: `total_revenue` = Σ `COALESCE(s.total, s.amount)`; `v_since_date` anclado a `reporting_local_today()`; costo snapshot intacto (no tocar la cascada D6 de v3-snapshot-pattern). Firma y RETURNS TABLE idénticos.
- [x] 1.4 `CREATE OR REPLACE rpc_period_comparison`: revenue/purchases = Σ `COALESCE(total, amount)`; resta de NC del período (`customer_account_movements.movement_type='credit_note'` por `created_at`); operaciones = `COUNT(DISTINCT COALESCE(operation_id, id))` en ventas y compras (+ gastos por fila); bordes `>= p_start::timestamptz AND < (p_end + 1)::timestamptz`. Firma idéntica; mantener delta NULL con período A = 0 y ERRCODE P0403.
- [x] 1.5 `CREATE OR REPLACE rpc_branch_report`: `total_sales` = Σ `COALESCE(total, amount)`; `operation_count` = `COUNT(DISTINCT COALESCE(operation_id, id))`; mismo fix de bordes; conservar la verificación de membership sobre `p_account_id` sin cambios.
- [x] 1.6 `DROP FUNCTION IF EXISTS` + `CREATE rpc_dashboard_kpi_summary` con las 4 columnas nuevas (`invoiced_revenue`, `prev_invoiced_revenue`, `collected_revenue`, `prev_collected_revenue`) según fórmulas D3, resta de NC en `net_profit`/`invoiced_revenue` (RN-D1), mismos parámetros de entrada, REVOKE/GRANT idénticos a los vigentes. El resto del cuerpo (COGS snapshot, stagnant stock vía `v_products_with_stock`) intacto.
- [x] 1.7 Gates de introspección (corren SIEMPRE, incluso en prod): cada cuerpo nuevo contiene los predicados clave (`COALESCE(s.total, s.amount)`, `credit_note`, `reporting_local_today`, `COALESCE(operation_id`, columnas nuevas en el RETURNS de kpi_summary) — RAISE EXCEPTION si falta alguno.
- [x] 1.8 Gates de comportamiento (solo DB vacía/CI, NOTICE-degrade en prod) con anchor sintético vía `handle_new_user`: (a) venta `quantity=2` → revenue = total de línea en los 4 RPCs; (b) NC de prueba resta en dashboard y comparativo; (c) cargo cta cte + `payment_received` → `collected` vs `invoiced` según D3; (d) venta con `date` a las 15:00 UTC del último día del rango incluida en comparativo y sucursal; (e) venta 3 líneas = 1 operación. Cleanup hijo→padre por email del anchor cubriendo TODO lo sembrado: movimientos cta cte, payments_received, NC, sales/sale_items/stock_movements, branch_stock, cashboxes, branches (seed de `20260812000001`), memberships, accounts — gate final `accounts = 0`.
- [x] 1.9 Header de la migración: problema, fix, both-worlds-safe (auto-apply del GitHub integration), plan de rollback con cita exacta de los cuerpos previos (dashboard/profitability: `20260806000001` §5.1/5.2b; comparison: `20260606120000`; branch_report: cuerpo capturado en 1.1).

## 2. Frontend (wiring mínimo, TDD con la suite existente)

- [x] 2.1 RED: extender `frontend/__tests__/hooks/use-dashboard-kpi-summary.test.ts` con los 4 campos nuevos (mapeo snake→camel, null-safety) — debe fallar.
- [x] 2.2 GREEN: mapear `invoiced_revenue`/`collected_revenue` (+ prev) en `use-dashboard-kpi-summary.ts` (interfaz `DashboardKpiSummary` + `RpcRow`).
- [x] 2.3 Tarjeta Ganancia Neta: línea secundaria "Cobrado: $X" solo cuando `collectedRevenue !== invoicedRevenue` (ambos no-null). Test de render condicional: no aparece cuando son iguales o null (cuentas de contado — el caso del 100 % de prod hoy).
- [x] 2.4 Verificar que `use-period-comparison.ts`, `use-profitability.ts` y `/reportes/sucursal` no requieren cambios (contratos estables) — correr sus tests existentes como safety net. Ambos hooks son wrappers finos del RPC con nombres de parámetros sin cambios; no existe test file dedicado (ninguno se rompe). Página `/reportes/sucursal` consume `rpc_branch_report` con la misma firma.

## 3. Verificación

- [x] 3.1 Suite frontend completa verde (baseline actual + tests nuevos); `pnpm build` sin errores de tipos. Resultado: 49 test files / 441 tests passed; `pnpm build` completa sin errores de TypeScript.
- [x] 3.2 Reset local de Supabase (`npx supabase db reset`) verde: la migración corre después de `20260813000001`, gates GREEN, `accounts=0` al final; re-aplicación idempotente (segunda corrida del `db reset` o `db push` sin error). Verificado 2x: ambas corridas exit 0, 0 ErrorResponse, `accounts=0` confirmado por query directa. Durante la verificación se corrigieron 3 bugs reales descubiertos por el propio reset (no cubiertos por el gate hasta ejecutarlo): (1) `products.user_id NOT NULL` faltaba en el INSERT del gate; (2) `customer_account_movements.movement_type` NO tiene el valor `'charge'` que asumía design.md — el CHECK vigente en prod es `('sale','payment_received','credit_note','adjustment')`, y `'sale'` es el tipo real que incrementa la deuda (corregido en `charges_agg`/`nc_agg` de `rpc_dashboard_kpi_summary` y en `nc_a`/`nc_b` de `rpc_period_comparison`, más el gate); (3) el ledger de cta cte guarda el monto de la NC como NEGATIVO (decrementa el balance) — las fórmulas de resta de NC ahora usan `ABS(cam.amount)`.
- [x] 3.3 Post-merge en prod (read-only): `pg_get_functiondef` de los 4 RPCs contiene los predicados nuevos; smoke query de `rpc_period_comparison` sobre una cuenta real compara contra el cálculo manual `SUM(COALESCE(total, amount))`; dashboard carga sin errores y `collected == invoiced` en cuentas sin cta cte. Verificado vía MCP read-only (gxdhpxvdjjkmxhdkkwyb) tras el deploy de PR #284 (SHA `041a234`): los 4 RPCs vivos contienen `COALESCE(s.total, s.amount)`; `rpc_dashboard_kpi_summary`/`rpc_period_comparison` contienen `credit_note`; `rpc_product_profitability`/`reporting_local_today` contienen el helper de fecha local; `rpc_dashboard_kpi_summary.RETURNS TABLE` expone las 16 columnas (12 previas + `invoiced_revenue`/`prev_invoiced_revenue`/`collected_revenue`/`prev_collected_revenue`). Delta de revenue confirmado exactamente como lo predijo la auditoría: `SUM(amount)=$7.905.976,19` vs `SUM(COALESCE(total,amount))=$9.291.711,19` → **+17,53%**, 45 filas con `total≠amount`. `customer_account_movements` tiene 0 filas en prod → `collected_revenue == invoiced_revenue` se cumple trivialmente en todas las cuentas (sin cta cte activa todavía). Cero residuo del gate sintético (`auth.users` sin el email `v3-reporting-invariants-gate@test.local`).
- [x] 3.4 Actualizar CHANGES.md (`v3-reporting-invariants` → completada, con resumen de veredictos de la auditoría) y registrar en `knowledge-base/05_reglas_de_negocio.md` los RN-D1/D3/D5 como reglas vigentes de reporting (sección Modelo V3), citando la spec `reporting-invariants`. Ambos hechos en el mismo PR #284 (mergeado como `041a234`); esta nota post-merge agrega el número de PR/SHA.
