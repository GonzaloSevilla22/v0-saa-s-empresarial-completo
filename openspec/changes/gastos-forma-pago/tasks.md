# Tasks — `gastos-forma-pago`

> ## 🛑 GATE DE GOVERNANCE — LEER ANTES DE TOCAR NADA
>
> **Governance: MEDIUM, con un tramo de severidad ALTA** (los grupos 4, 5 y 6 escriben
> dinero en caja y en banco desde RPCs `SECURITY DEFINER` nuevas). Implementación con
> **checkpoints 🛑 explícitos** en esos tramos: se implementa en pasos y se sube la
> decisión al PO donde está marcado. No hay autonomía plena.
>
> **Strict TDD activo.** Cada task de código sigue RED → GREEN → TRIANGULATE →
> REFACTOR, con safety net previo sobre lo que se toca. Los tests van **antes** de
> la implementación, nunca después. Prohibidas las aserciones triviales.
>
> Las tasks marcadas **[OQ-n]** dependen de la respuesta del PO a la Open Question
> correspondiente de `design.md`. **Sin respuesta, se implementa la recomendación** y
> se registra que salió así (precedente del proyecto).
>
> **Reutilización antes que repetición.** Este change no crea ni un helper SQL. Cada
> predicado se **copia** de venta o de compra. Si una task parece necesitar lógica
> financiera nueva, es señal de que se está resolviendo el problema equivocado: parar
> y reportar.
>
> **Nunca commitear a main.** Todo por rama + PR, incluidos los fixes triviales
> posteriores al merge.

## 0. Sign-off y decisiones de producto

- [x] 0.1 🛑 Presentar al PO, **antes de escribir código**, los tres hechos que cambian su experiencia: (a) los **175 gastos históricos** ($8.723.710,63) quedan sin forma de pago y fuera de la conciliación **para siempre** (D7, no hay backfill honesto); (b) un gasto que ya impactó caja o banco pasa a ser **inmutable** — se corrige borrando y recargando (D11); (c) **borrar un gasto en efectivo va a exigir la caja abierta** (`P0426`, D8). Registrar la respuesta acá. — ✅ **Firma del PO 2026-08-29** ("dale, arranca con el apply"), tras presentarle los tres hechos: (a) 175 gastos históricos sin imputar y fuera de la conciliación para siempre; (b) gasto con caja/banco posteado = inmutable; (c) borrar un gasto en efectivo exige caja abierta (`P0426`).
- [x] 0.2 🛑 **[OQ-1]** Confirmar si el opt-in de caja arranca **pre-marcado** (recomendación) o desmarcado como en el formulario de venta. Explicarle la asimetría deliberada: la venta arranca desmarcada por 223 operaciones históricas, el gasto no tiene esa deuda (0/175). — ✅ **[OQ-1] Firmada 2026-08-29**: opt-in de caja **pre-marcado** (recomendación). Asimetría con el form de venta explicada y aceptada.
- [x] 0.3 🛑 **[OQ-2]** Confirmar el criterio de cuenta bancaria: selector obligatorio en el formulario + `P0412` en servidor (recomendación). Aprovechar para pedirle que configure el destino bancario por defecto de sus formas de pago bancarias en Configuración → Formas de pago: la pantalla **ya existe** y hoy hay **0 de 37** catálogos configurados. — ✅ **[OQ-2] Firmada 2026-08-29**: selector de cuenta bancaria **obligatorio** en el formulario + `P0412` en servidor (recomendación). Sigue en pie el pedido paralelo al PO de configurar el destino bancario por defecto: medido hoy, **0 de 37** catálogos configurados.
- [x] 0.4 **[OQ-3..OQ-11]** Registrar la respuesta del PO a las OQ restantes (crédito bloqueado, inmutabilidad, reporte de formas de pago, históricos, importador, `P0426`, sugerencia por categoría, **migración del listado a la lectura paginada del backend (OQ-10, D18)** y **columna de forma de pago en el export por Edge Function (OQ-11)**). Sin respuesta → recomendación, y se anota. — ✅ Registrado 2026-08-29: el PO autorizó el apply sin responder OQ-3..OQ-11 una por una, así que **todas salen por su opción recomendada**, como manda la regla del proyecto. Queda anotado acá y en `baseline/prod_acl_audit_2026-08-29.md`.
- [ ] 0.5 Registrar las decisiones de 0.1-0.4 en `CHANGES.md` y en engram (`topic_key: opsx/gastos-forma-pago/apply`) **antes** de escribir una línea de código.

## 1. Reconocimiento y safety net

- [x] 1.1 Re-verificar el **MAX de `supabase_migrations.schema_migrations` vivo en prod** (sólo `SELECT`). Al escribirse este propose era `20261014000001` (263 migraciones) → el archivo nace como `20261015000001_gastos_forma_pago.sql`. ⚠️ **Se re-verifica acá, siempre**: en este proyecto la renumeración mordió tres veces. Un archivo con número **menor o igual** al MAX remoto no lo aplica nunca el push automático de Supabase. — ✅ 2026-08-29. Prod: `MAX(version) = 20261014000001`, **263** migraciones, idéntico al último archivo de `origin/main`. **Sin renumeración**: la migración nace como `20261015000001_gastos_forma_pago.sql`. Se re-verifica otra vez en la task 2.2, antes de escribir el archivo.
- [x] 1.2 Correr la suite backend completa (`python -m pytest backend/tests -q -p no:cacheprovider`) y registrar el baseline `N/N`. Cualquier fallo preexistente se **reporta**, no se arregla acá. — ✅ 2026-08-29. **1591 passed, 3 skipped** (exit 0). **0 fallos preexistentes.**
- [x] 1.2b **SAFETY NET DIRIGIDO (obligatorio, D15).** El módulo Gastos **sí tiene tests hoy** — el propose original decía lo contrario. Correr y registrar el conteo de cada uno **por separado**, antes de tocar los archivos que cubren: — ✅ 2026-08-29. `test_expenses.py` **7/7**; los 4 archivos de frontend juntos **11/11** (`use-expenses` 5 · `expense-form-date-default` 1 · `expense-import-dialog-parse-and-validate` 3 · `RecentActivity` 2). Detalle y los 7 tests backend por nombre en `baseline/safety_net_2026-08-29.md`; ahí queda justificado de antemano que `test_get_expenses_ok` y `test_get_expenses_empty` van a cambiar por D18 (lista plana → `{items,total,page,pages}`).
  - `backend/tests/test_expenses.py` (7 `def test_`) → antes de tocar `backend/repositories/expense_repository.py` / `services/expenses.py` / `routers/expenses.py`
  - `frontend/__tests__/hooks/use-expenses.test.ts` (5 casos, con mocks de `pythonClient.post/put/delete` que assertan los **payloads actuales**) → antes de tocar `frontend/hooks/data/use-expenses-query.ts`
  - `frontend/__tests__/components/expense-form-date-default.test.tsx` → antes de tocar `expense-form-v2.tsx`
  - `frontend/__tests__/components/expense-import-dialog-parse-and-validate.test.ts` y `frontend/__tests__/components/RecentActivity.test.tsx` (mockea `useExpenses`) → antes de tocar el importador y el contrato del hook
  > Los cuatro tienen que quedar **verdes al final**. Cada aserción que cambie va **justificada por escrito** en el PR (este change amplía payloads a propósito). Sin este paso, una rotura dirigida se leería como "baseline distinto" en 1.2/1.3, que miden el total agregado.
- [x] 1.3 Correr la suite frontend (`pnpm vitest run`) y registrar el baseline. Gotcha conocido: hay 1 fallo preexistente ajeno; confirmarlo y no tocarlo. No pipear a `tail` (enmascara el exit code). — ✅ 2026-08-29. **184 archivos / 1457 tests, todos verdes** (exit 0). ⚠️ **Divergencia con lo que preveía esta task**: **NO hay ningún fallo preexistente** (el dato del 1331/1332 era de la época de `compras-proveedor-cuenta-corriente`). Consecuencia: cualquier fallo de frontend a partir de acá es de este change.
- [x] 1.4 Levantar el stack local (`npx supabase db reset`) y correr los gates del workflow en el orden exacto de CI, registrando el baseline. Prestar atención a `test_function_acl_gate.sql`, `test_errcode_5char_gate.sql`, `test_delete_guard_ledgers.sql`, `test_payment_method_report.sql` y `token-contrast-aa.test.ts`. — ✅ 2026-08-29. `npx supabase db reset` limpio (exit 0, 263 migraciones). **33/33 gates SQL verdes** en el orden exacto de CI, más los 2 gates de referencia (`check_backend_table_refs` 131 archivos / `check_frontend_table_refs` 460 archivos). `token-contrast-aa.test.ts` va dentro del 1457/1457 de 1.3.
- [x] 1.5 Reproducir la **cadena de reapply** del step "Verify G1/G4 migrations are idempotent on reapply" en local, en el orden exacto del YAML, y confirmar que fallan **sólo** los eslabones tolerados que el workflow documenta. ⚠️ **Repetirlo DESPUÉS de aplicar la migración nueva** (re-check al cerrar el grupo 6) y verificar el punto exacto donde aborta el reapply de `20260928000001`: tiene que ser su **gate ANTI-OVERLOAD de la sección 9** (línea ~1813), que corre **antes** del `CREATE` de `rpc_payment_method_report` de la sección 10. Eso es lo que evita que el `CREATE` con la firma vieja de 7 columnas choque con la nueva de 8 (**42P13**). Si el abort ocurriera en otro lado, o el reapply llegara a la sección 10, **parar**: hay que tolerar acotadamente el `42P13` y reconverger reaplicando la migración nueva, con el precedente documentado en `KPI_Validation.yml:66-70`. — ✅ 2026-08-29 (**primera pasada**; el re-check post-migración sigue pendiente para el cierre del grupo 6, como dice la propia task). Cadena extraída **literal** del YAML (333 líneas, 16 `psql -f`) y corrida en local: **exit 0**, fallan **sólo** los dos eslabones tolerados y por el motivo documentado (`20260928000001` con marcador `GATE ANTI-OVERLOAD FAILED`, `20261002000001` con `GATE POS-BANCO-MOVIMIENTOS FAILED`). 🛑 **Verificación clave de D14 medida, no supuesta**: el reapply aborta en la **línea 1838** (gate de la sección 9) y el `CREATE` de `rpc_payment_method_report` está en la **1850** → las secciones 10-12 se saltean y **no se introduce ningún 42P13**. Post-cadena, las 11 funciones quedan con 1 sola definición y md5 idéntico a prod.
- [x] 1.6 🛑 **Gate de integridad de función.** Capturar el `pg_get_functiondef` **vivo en prod** de cada función que se reescribe o de la que se copia un predicado, en `openspec/changes/gastos-forma-pago/baseline/<nombre>.sql`, con cabecera de procedencia, **`md5` y `length`** (molde: `archive/2026-08-23-cuenta-corriente-party-guard/baseline/`): — ✅ 2026-08-29. **11 baselines** capturados en `baseline/`, cada uno con procedencia, `md5` y `length` del `pg_get_functiondef` **vivo de prod**. Se agregó un `.gitattributes` propio (`*.sql -text`) para que el md5 documentado siga siendo verificable tras un checkout (el baseline archivado de `cuenta-corriente-party-guard` quedó `w/crlf` y no lo es).
  - **A reescribir**: `rpc_payment_method_report`
  - **De referencia, no se tocan**: `rpc_create_sale_operation_v2` (opt-in de caja + derivación de `kind`), `rpc_create_purchase_operation` (pata bancaria de egreso), `rpc_delete_sale_operation` (compensación de las dos patas), `rpc_atomic_update_sale_operation` (guards `P0423`), `c28_register_cash_movement`, `_pay_register_operation_bank_movement`, `_pay_resolve_bank_account`, `_register_bank_movement`, `c26_default_branch`, `reporting_local_today`
  > Si algún cuerpo vivo difiere de su archivo de migración, **parar y reportar antes de escribir SQL**. Antecedente concreto: el G3 de `20261003000001` había sido reescrito in-place.
- [x] 1.7 🛑 Diffear cada baseline del 1.6 contra su archivo de migración de referencia. Si difieren en una sola línea, reportar. — ✅ 2026-08-29. **Cero divergencias, 11 de 11.** Verificado en la forma fuerte: los 11 md5 del stack local recién reseteado (que reproduce las mismas 263 migraciones del repo) **coinciden EXACTO** con los de prod → la cadena del repo produce byte a byte el cuerpo vivo. Además, para cada función el cuerpo vivo aparece **textual** en el último archivo que la declara — relevante porque **36 migraciones del repo reescriben funciones in-place vía `pg_get_functiondef`**. El antecedente del G3 de `20261003000001` **no se reprodujo**.
- [x] 1.8 Auditar en prod los permisos (`has_function_privilege` para `anon`/`authenticated`/`service_role`) de `rpc_payment_method_report` y de los helpers reutilizados. Guardar en `baseline/prod_acl_audit_<fecha>.md`. Recordar el gotcha #432: prod concede `EXECUTE` **directo**, no vía `PUBLIC`. — ✅ 2026-08-29. En `baseline/prod_acl_audit_2026-08-29.md`. Hallazgos que condicionan el diseño: los 3 helpers de dinero tienen `authenticated = false` (sólo alcanzables desde un `SECURITY DEFINER` → las 3 RPCs nuevas **tienen** que serlo); `c28_register_cash_movement` es `SECURITY INVOKER`. Además se midió el `pg_default_acl` vivo y **queda probado** que el `DROP`+`CREATE` de D14 con sus 3 sentencias de ACL reconstruye exactamente el `proacl` actual (`service_role` vuelve solo por default privilege).
- [x] 1.9 Confirmar por `grep` los consumidores actuales de gastos en `frontend/` y `backend/`, y anotar cualquier camino de escritura a `expenses` distinto de `ExpenseRepository` (esperado: ninguno; el importador CSV pasa por el mismo endpoint). — ✅ 2026-08-29. Backend: **un solo** camino de escritura (`ExpenseRepository` L26/L46/L54). Frontend: **hallazgo no previsto** — `frontend/lib/supabase/services.ts:66` hace `supabase.from('expenses').insert(...)` por PostgREST directo, pero es **código muerto** (`createExpense` sin llamadores y el módulo sin importadores), así que hoy **no** hay bypass real; queda como bypass latente y candidato a borrar (además está tipado con `any`). Las lecturas de IA (`buildBusinessSnapshot`, `aiCopilotService`) no se ven afectadas.
- [x] 1.10 Auditoría **read-only** en prod de daño preexistente: gastos con `cash_movements` o `bank_movements` colgando (esperado **0**, porque el camino nunca existió) y `cash_movements` con `movement_type='expense'` (esperado **0**). Si algo da > 0 → checkpoint 🛑. — ✅ 2026-08-29. **0 en los cinco conteos** (`movement_type='expense'`, `'expense_reversal'`, `cash_movements.reference_id`→gasto, `source_doc_type='expense'`, `bank_movements.source_doc_ref`→gasto). **Sin daño histórico, sin blocker.** De paso se re-verificaron las premisas del design y **ninguna se movió**: 175 gastos, 0 con `branch_id`, 0 con `cost_center_id`, 0 catálogos con `bank_account_id`, 4 cuentas con banco activo, 67 `cash_movements`.

## 2. Migración — esquema (RED primero)

- [ ] 2.1 **RED** — escribir `supabase/tests/test_gastos_forma_pago.sql` con los primeros casos de esquema: existe `expenses.payment_method_id` con su FK y su `ON DELETE SET NULL`; el `CHECK` de `cash_movements.movement_type` incluye `expense_reversal`. Correrlo y verlo **fallar**.
- [ ] 2.2 **GREEN** — crear `supabase/migrations/20261015000001_gastos_forma_pago.sql` (número de la task 1.1) con la sección de esquema: `ADD COLUMN payment_method_id` idempotente + índice `(account_id, payment_method_id)` + ampliación idempotente del `CHECK` de `cash_movements`. Sin BOM, ERRCODEs de 5 chars, **`P0001` prohibido**. Correr el gate → verde.
- [ ] 2.3 **TRIANGULATE** — casos adicionales: las filas históricas de `expenses` y de `cash_movements` sobreviven intactas a la migración; la migración es **idempotente** al reaplicarse; un `movement_type` fuera del conjunto ampliado sigue siendo rechazado.
- [ ] 2.4 Verificar en local que `npx supabase db reset` completo pasa sin abortar (control directo de que no se coló un `P0001`).

## 3. RPC de alta — `rpc_create_expense`

> 🛑 **Tramo de severidad ALTA: acá empieza la escritura de dinero.** Cada sub-paso
> es RED → GREEN → TRIANGULATE con su propio caso, no un único bloque.

- [ ] 3.1 **RED** — casos de la ruta básica en el gate SQL: alta de gasto sin forma de pago persiste la fila y **no toca ningún libro**; alta con forma de pago activa de la cuenta persiste `payment_method_id`.
- [ ] 3.2 **GREEN** — esqueleto de `rpc_create_expense` (`SECURITY DEFINER`, `search_path` fijo): resolución del tenant **desde la sesión** (`current_account_ids()`, nunca por parámetro), `is_account_writer`, `INSERT` de la fila. `REVOKE` de `PUBLIC`/`anon` + `GRANT` a `authenticated`.
- [ ] 3.3 **RED → GREEN** — derivación del `kind` desde el catálogo (`WHERE id = … AND account_id = … AND is_active AND deleted_at IS NULL` → `P0404`), copiada literal del baseline de `rpc_create_sale_operation_v2`. Casos: forma de pago de otra cuenta, inactiva y borrada.
- [ ] 3.4 **RED → GREEN** — **[OQ-3]** rechazo de `kind = 'credit'` con `P0400`, con el mensaje que redirige a la compra a proveedor.
- [ ] 3.5 **RED → GREEN** — persistencia de `branch_id` con `COALESCE(p_branch_id, c26_default_branch(cuenta))`. Casos: sin sucursal informada toma la default; con sucursal informada la respeta; sucursal ajena o cerrada es rechazada.
- [ ] 3.6 🛑 **RED → GREEN** — **pata de caja**. Copiar **verbatim** el bloque de opt-in de `rpc_create_sale_operation_v2` (baseline de 1.6), con las dos únicas adaptaciones que D1 autoriza: (a) el parámetro se declara **`p_date date`** y se compara **directo** (`p_date <> reporting_local_today()`) — ⚠️ **prohibido** `p_date timestamptz` con `p_date::date`: ese cast se resuelve en el TimeZone de la sesión (UTC acá) y rechazaría con `P0422` un gasto legítimo cargado entre las 21:00 y las 23:59 ART; (b) `c28_register_cash_movement(sesión, -importe, 'expense', id_del_gasto)`. **No reescribir el predicado desde cero.** Mostrar el diff contra el baseline antes de seguir.
- [ ] 3.7 **TRIANGULATE (caja)** — los seis casos: `cash_session_id` nulo = no-op; kind no efectivo con sesión informada → `P0422`; sesión cerrada → `P0422`; sesión de otra sucursal → `P0422`; fecha de ayer → `P0422`; y el caso de **zona horaria**: el alta de un gasto con `p_date = reporting_local_today()` se corre **dos veces**, bajo `SET LOCAL TimeZone = 'UTC'` y bajo `SET LOCAL TimeZone = 'America/Argentina/Mendoza'`, exigiendo **idéntica aceptación** en ambas. ⚠️ El caso "gasto de hoy a las 15:00" que decía el propose original **no sirve como control**: a las 18:00 UTC cae en el mismo día calendario y pasa igual con el cast defectuoso.
- [ ] 3.8 **RED → GREEN** — caso de tenencia: sesión de caja de **otra organización** es rechazada. Verificar que el rechazo viene del guard que `tenancy-guard-caja-outbox` ya puso en `c28_register_cash_movement` (`P0401`) **sin agregar una línea**. Si hiciera falta agregar algo, es un hallazgo: reportar.
- [ ] 3.9 🛑 **RED → GREEN** — **pata bancaria**. Llamada **incondicional** a `_pay_register_operation_bank_movement(..., 'out', 'expense', id, p_date, sucursal, NULL)` — `p_date` ya es `date` por 3.6, sin ningún cast dependiente de la zona de la sesión —, calcada de la de `rpc_create_purchase_operation`. Mostrar el diff contra el baseline.
- [ ] 3.10 **TRIANGULATE (banco)** — transferencia con cuenta resuelta escribe el egreso con la **fecha del gasto** como fecha valor; tarjeta escribe el tipo de liquidación por el bruto; efectivo y `other` no escriben nada; cuenta bancaria informada sobre un kind no bancario → `P0400`; cuenta bancaria de otra organización → rechazo; gasto con fecha dentro de un período conciliado y cerrado → `P0424` que **revierte la operación entera**.
- [ ] 3.10b **RED → GREEN** — el movimiento del gasto **es conciliable de verdad** (escenarios `bank-movement` "El movimiento del gasto nace conciliable" y "La sugerencia automática engancha el movimiento del gasto"): nace con `reconciliation_status = 'unreconciled'`, aparece en los pendientes de su cuenta, y con una línea de extracto por el **mismo importe** dentro de la ventana de días alrededor de la fecha valor, la sugerencia automática los **empareja**. No alcanza con verificar la fecha valor (3.10): sin este caso, el objetivo literal del PO queda sin verificación.
- [ ] 3.10c **RED** — **control negativo de D10**: el alta de un gasto con movimiento bancario y/o de caja **no** inserta ninguna fila en `public.events` y **no** crea ningún `journal_entry`/`journal_line`. Es la garantía que D10 declara y el escenario "El movimiento del gasto no genera asiento contable por sí mismo" de `bank-movement` exige; sin control negativo se cumple por accidente hasta que alguien agregue un `INSERT`.
- [ ] 3.11 **RED → GREEN** — **[OQ-2]** guard de cuenta bancaria exigible (D5): kind bancario + cuenta no resuelta + la organización **tiene** cuentas activas → `P0412`. Caso complementario obligatorio: la organización **sin** cuentas bancarias guarda el gasto sin movimiento y **sin error**.
- [ ] 3.12 **RED** — control negativo de no-regresión: registrar una **venta** por transferencia sin cuenta destino resuelta en una organización con cuentas activas y verificar que **sigue procediendo sin movimiento**, igual que antes. Prueba directa de que el endurecimiento quedó en el caller y no en el helper compartido.
- [ ] 3.13 **RED → GREEN** — atomicidad: forzar el fallo de la pata de caja y de la pata bancaria por separado y verificar que **no queda ninguna fila** en `expenses`.
- [ ] 3.14 **REFACTOR** — revisar el cuerpo con los tests en verde: nombres de variables espejo de la venta, comentarios que citen de dónde se copió cada bloque, cero lógica duplicada.

## 4. RPC de edición — `rpc_update_expense`

- [ ] 4.1 **RED** — guards de inmutabilidad: gasto con `cash_movements` referenciándolo → `P0423`; gasto con `bank_movements` de `source_doc_type='expense'` → `P0423`. Los dos con mensajes distinguibles.
- [ ] 4.2 **GREEN** — implementar los dos `EXISTS` **antes de cualquier escritura**, copiando los predicados del baseline de `rpc_atomic_update_sale_operation`.
- [ ] 4.3 **RED → GREEN** — contrato **tri-estado** con el par `(p_<campo>, p_set_<campo>)` para `payment_method_id`, `branch_id` y `cost_center_id`, calcado de `rpc_atomic_update_purchase_operation`. Casos por campo: ausente conserva · nulo explícito desimputa · uuid reimputa.
- [ ] 4.4 **TRIANGULATE** — reimputar a un valor ajeno, inactivo o borrado es rechazado y **el gasto conserva sus valores anteriores**; un gasto sin movimientos se edita normalmente; un gasto histórico (sin forma de pago ni sucursal) se edita normalmente.
- [ ] 4.5 **RED → GREEN** — localización por tenant: editar un gasto de otra cuenta → `P0404`, sin distinguir "no existe" de "es de otro".
- [ ] 4.6 **REFACTOR** + verificar que la edición **no postea** movimientos nuevos (la inmutabilidad hace que el caso no exista, pero el test lo deja explícito).

## 5. RPC de borrado — `rpc_delete_expense`

- [ ] 5.1 **RED** — borrado de un gasto sin libros: procede, sin exigir caja abierta ni tocar nada.
- [ ] 5.2 **GREEN** — esqueleto con resolución de tenant, `is_account_writer`, localización por cuenta (`P0404`) y `DELETE`.
- [ ] 5.3 🛑 **RED → GREEN** — **compensación de caja**: agrupar `cash_movements` por `reference_id = id_del_gasto AND movement_type='expense'`; resolver la sesión **abierta actual** del mismo `cashbox` (`ORDER BY opened_at DESC LIMIT 1`); sin sesión abierta → `P0426`; con sesión → `c28_register_cash_movement(sesión_abierta, -v_cash_amount, 'expense_reversal', id)` (con `v_cash_amount` negativo da el ingreso positivo). Copiado del baseline de `rpc_delete_sale_operation`, **con una inversión obligatoria y explícita**:
  > ⚠️ **El guard de signo se invierte.** El original (`20261005000001_delete_guard_ledgers.sql:1221`) es `IF v_cashbox_id IS NOT NULL AND v_cash_amount > 0 THEN`, porque los movimientos de **venta** son positivos. Los del **gasto** son **negativos** (`expense` negativo, `expense_reversal` positivo). Copiado verbatim, ese guard es **falso para todo gasto**: se saltea el bloque entero, no se registra el `expense_reversal`, **nunca se lanza `P0426`** y el `DELETE` procede igual — se reintroduce el borrado inseguro que motivó `delete-guard-ledgers` (204 operaciones backfilleadas). El guard correcto es **`AND v_cash_amount < 0`**. Mostrar el diff contra el baseline señalando esta línea.
- [ ] 5.4 **TRIANGULATE (caja)** — la sesión **original nunca se toca** (append-only, verificar el conteo y el arqueo de la sesión cerrada); la sesión original ya cerró y hay otra abierta → el contra-movimiento va a la de hoy; sin ninguna sesión abierta → `P0426` y **el gasto sigue existiendo**.
- [ ] 5.4b **CONTROL NEGATIVO obligatorio del guard invertido (5.3)** — gasto en efectivo cuyo movimiento quedó en una sesión **cerrada** y **sin ninguna sesión abierta** en ese `cashbox`: el borrado **DEBE** ser rechazado con `P0426` y el gasto **debe seguir existiendo**. Y el caso complementario: con sesión abierta, el borrado **DEBE** dejar exactamente **un** `expense_reversal` positivo. ⚠️ Assertar el **efecto** (la fila del contra-movimiento, el `P0426`), nunca "no hubo error": con el guard mal copiado los dos casos quedarían verdes por omisión.
- [ ] 5.5 🛑 **RED → GREEN** — **compensación bancaria**: loop sobre `bank_movements` con `source_doc_type='expense' AND source_doc_ref = id`, espejo invertido vía `_register_bank_movement(..., -amount, ..., CURRENT_DATE, sucursal, 'Reversión por borrado de gasto')`. Copiado del loop de `rpc_delete_sale_operation`.
- [ ] 5.6 **TRIANGULATE (banco)** — el movimiento original queda intacto; el saldo de la cuenta vuelve al valor previo; el espejo nace **no conciliado**; un gasto sin movimiento bancario se borra sin espejo.
- [ ] 5.7 **RED → GREEN** — atomicidad: forzar el fallo de cada compensación por separado y verificar que el gasto **sigue existiendo** y ningún libro queda con contra-movimientos parciales.
- [ ] 5.8 **REFACTOR** + verificar que el gate SQL deja **cero residuos** (todos los tenants sintéticos y sus filas se limpian).

## 6. Read-model del reporte de formas de pago

- [ ] 6.1 🛑 **[OQ-5]** Confirmar que el reporte incluye gastos (recomendación: sí). Si el PO dice que no, este grupo entero se salta y se anota.
- [ ] 6.2 **RED** — casos en `test_payment_method_report.sql` (o en el gate propio): un gasto imputado a "Efectivo" y otro a "Transferencia" aparecen en sus filas; los gastos sin imputar caen en "Sin especificar"; los importes de gasto **no se mezclan** con los de compra.
- [ ] 6.3 **GREEN** — reescribir `rpc_payment_method_report` **partiendo del `pg_get_functiondef` vivo** capturado en 1.6, agregando `total_spent` con el mismo criterio con que `rpc_cost_center_report` agrega los gastos (`amount`, una unidad de conteo por fila). Mantener el guard de membresía `P0401`, los bordes de rango y los invariantes RN-D. **Mecánica obligatoria (D14) — no alcanza `CREATE OR REPLACE`:**
  1. `DROP FUNCTION IF EXISTS public.rpc_payment_method_report(uuid, date, date);` **antes** del `CREATE`. La función vigente se creó con `CREATE OR REPLACE` **sin `DROP` previo** (`20260928000001:1851`, `RETURNS TABLE` de 7 columnas); sumarle `total_spent` cambia el tipo de retorno y Postgres rechaza el `CREATE OR REPLACE` con **42P13** (`cannot change return type of existing function`).
  2. Re-emitir **las tres ACLs completas** inmediatamente después, en el mismo archivo: `REVOKE ALL ... FROM PUBLIC` + `REVOKE EXECUTE ... FROM anon` + `GRANT EXECUTE ... TO authenticated` (molde literal de `20260928000001:1945-1947`). **El `DROP` resetea las ACLs**; el chequeo (2) del gate de ACLs es la red, no el mecanismo.
  3. Gate **ANTI-OVERLOAD** propio: conteo de definiciones de `rpc_payment_method_report` en `public` = **1** (molde de `20260928000001:1813-1837`). Si el `DROP` no matcheara la firma exacta, el `CREATE` dejaría dos overloads y la próxima llamada posicional revienta con 42725.
  4. Verificar en local, tras el `db reset`, `has_function_privilege` de `anon`/`authenticated` sobre la firma nueva.
- [ ] 6.4 **TRIANGULATE** — el caller de otra cuenta sigue recibiendo `P0401`; los casos de ventas y compras que ya existían siguen dando **exactamente los mismos números** (no-regresión); los bordes del rango incluyen el día final completo.
- [ ] 6.5 **RED → GREEN** — **reporte por sucursal** (escenario "El reporte por sucursal empieza a ver los gastos" de `expense-operation`): un gasto nuevo con `branch_id` aparece atribuido a esa sucursal en `rpc_branch_report`, que **ya agrega `public.expenses` por `branch_id`** (`20260814000001_v3_reporting_invariants.sql:397-405`) y hoy nunca ve ninguno porque son 0/175. No se toca la RPC: es el efecto colateral de D6 y hay que verificarlo, no suponerlo.

## 7. Backend Python — schemas, repositorio y servicio

- [ ] 7.1 **RED** — tests de `backend/tests/` para `ExpenseCreate`/`ExpenseUpdate`: los campos nuevos son opcionales; `ExpenseUpdate` distingue **ausente** de **nulo explícito** vía `model_fields_set`. Aserción del contrato, no del tipo.
- [ ] 7.2 **GREEN** — `backend/schemas/expenses.py`: sumar `payment_method_id`, `branch_id`, `cash_session_id`, `bank_account_id` a la entrada (passthrough opt-in, con el comentario de contrato: "ausencia = no-op, las condiciones se validan en la RPC, nunca acá"); sumar `payment_method_id`, `branch_id` e `is_payment_locked` a la salida. Conservar el `field_validator` de coerción de fecha.
- [ ] 7.3 **RED → GREEN** — `backend/schemas/cash.py`: `MovementType` suma `expense_reversal` y entra en `_INCOME_TYPES` (**signo positivo**: revertir un egreso repone plata). ⚠️ Ojo con el "espejo de `sale_reversal`": `sale_reversal` está en `_EXPENSE_TYPES` (`cash.py:38-42`), o sea **signo opuesto**. El espejo con `sale_reversal` es de **familia de UI** (task 9.7), no de signo (D9). ⚠️ Gotcha #442: todo `field_validator` cross-field va sobre un campo declarado **después** de los que lee. Verificar que el rechazo de `expense_reversal` negativo trae `loc == ('amount',)`.
- [ ] 7.4 **RED → GREEN** — `ExpenseRepository`: `create`/`update`/`delete` pasan a **una** llamada a su RPC. Los mocks de test replican el **transporte real** (no el ideal): parámetros posicionales, tipos que asyncpg realmente manda. Lección de #436/#451: un contrato endurecido con mocks que no replican el transporte se rompe en prod y no en CI.
- [ ] 7.5 **RED → GREEN** — derivados de lectura en `get_by_id`/`list_by_org`, con los **mismos predicados** que los guards del servidor y espejo literal de `SaleOut` (`backend/schemas/sales.py:120`): `is_payment_locked` (hay `cash_movements` o `bank_movements` del gasto → bloquea editar), `has_cash_movement` y `has_bank_movement` (por separado, para que el diálogo de borrado enumere qué libro compensa) y `is_delete_blocked` (hay movimiento de caja **y** no existe sesión abierta en ese `cashbox` — el mismo `EXISTS` que precede al `P0426` de `rpc_delete_expense`). Test que asserta que cada derivado coincide con el guard sobre el mismo dato.
- [ ] 7.5b **RED → GREEN** — **[D18]** `GET /expenses` adopta el contrato paginado estándar `{items, total, page, pages}` (`v3-api-standards`) con `page`, `page_size`, `date_from`, `date_to`, `search`, `cost_center_id` y `payment_method_id` server-side, y resuelve `payment_method_name` por join al catálogo. Es la **misma** plomería que ya tiene `GET /sales` (`useSales()` la consume): copiarla, no inventarla. ⚠️ **BREAKING de API interna**: el endpoint deja de devolver una lista plana — el único consumidor es el frontend propio, y `recent-activity.tsx` pasa a leer `items`.
- [ ] 7.6 **RED → GREEN** — `backend/services/expenses.py` y `routers/expenses.py`: sólo validación + delegación, cero lógica de negocio (DEC-24). Verificar que los ERRCODEs llegan traducidos: `P0404`→404, `P0423`→409, `P0400`→400, `P0422`→422, `P0424`→409, `P0426`→409, todo RFC 7807.
- [ ] 7.6b **RED → GREEN** — **[D19] `P0412` necesita un override por endpoint.** Hoy está mapeado a **404** (`backend/core/errors.py:98`, *"cuenta bancaria no encontrada / inactiva"*) y `_FIELD_BY_ERRCODE` (errors.py:154-156) sólo tiene `P0413` — así que "falta elegir la cuenta bancaria de origen" saldría como **404 sin campo ofensor** para lo que es una validación de payload. Definir `EXPENSE_ERRCODE_STATUS = {**_BUSINESS_ERRCODE_STATUS, "P0412": 422}` y resolverlo en el service/router de gastos, con `field = "bank_account_id"` en el 7807 — molde literal de `BANK_ACCOUNT_CREATE_ERRCODE_STATUS` (errors.py:163-167). **`P0412` conserva su 404 global** para el resto de los callers. Test: el 7807 del gasto sin destino trae `status = 422` y `field = "bank_account_id"`; el de banco sigue trayendo 404.
- [ ] 7.7 **TRIANGULATE** — tests de integración por camino: gasto sin pago, en efectivo con y sin opt-in, por transferencia con y sin cuenta, a crédito (rechazado), edición bloqueada, borrado con y sin caja abierta.
- [ ] 7.8 Suite backend completa verde contra el baseline de 1.2.

## 8. Frontend — canonización previa (Regla de Tres ya cumplida)

- [ ] 8.1 **RED** — test de `PaymentMethodBadge`: renderiza el nombre, cae en "Sin especificar" cuando no hay imputación, y **no usa colores literales** (aserción contra el gate de contraste).
- [ ] 8.2 **GREEN** — crear `frontend/lib/payment-method-meta.ts` subiendo `KIND_LABELS` desde el interior privado de `PaymentMethodManager` y tomando `UNASSIGNED_PAYMENT_METHOD_LABEL` de `lib/payment-method-report.ts`. Crear `components/payment-methods/PaymentMethodBadge.tsx` con tonos **semánticos**.
- [ ] 8.3 **REFACTOR** — reemplazar las **4 duplicaciones literales** existentes (`sale-operations-list.tsx` ×2, `purchase-operations-list.tsx` ×2) por el componente. Los tests de esos listados deben seguir verdes sin cambios de aserción.
- [ ] 8.4 🛑 **RED** — antes de tocar `sale-form.tsx`: escribir tests que fijen el comportamiento **vigente** del opt-in de caja del formulario de venta (las tres condiciones y el estado del checkbox). Es código en producción que mueve caja.
- [ ] 8.5 **GREEN + REFACTOR** — extraer `useCashOptin({ kind, branchId, date })` a `frontend/hooks/`, con la lógica hoy inline en `sale-form.tsx` (`useBranches` + `useCashboxes` + `useCurrentSession` + comparación con el día local). `sale-form.tsx` pasa a consumirlo. Los tests de 8.4 siguen verdes **sin cambiar una aserción**.

## 9. Frontend — tipos, hook de datos y bugs preexistentes

- [ ] 9.1 **RED** — test del `mutationFn` de alta que asserta que `branch_id` **viaja en el payload** (hoy falla: el form lo manda y el hook lo descarta).
- [ ] 9.2 **RED** — test del `mutationFn` de edición que asserta que `cost_center_id` **viaja** (hoy falla: se borra en cada edición, en silencio).
- [ ] 9.3 **GREEN** — `use-expenses-query.ts`: payload completo en alta y edición, contrato **tri-estado** en el `PUT` (omitir conserva, `null` desimputa), y `mapExpense()` mapeando `branch_id`, `payment_method_id`, `payment_method_name`, `is_payment_locked`, `has_cash_movement`, `has_bank_movement` e `is_delete_blocked`. ⚠️ Con el safety net de 1.2b delante: `__tests__/hooks/use-expenses.test.ts` fija los payloads actuales de `post`/`put`/`delete`.
- [ ] 9.3b **RED → GREEN** — **[D18]** el hook pasa a ser el origen de datos del **listado**: `page`/`pageSize`/`dateFrom`/`dateTo`/`search`/`costCenterId`/`paymentMethodId` en estado local, query key con los params, respuesta `{items,total,page,pages}` y `buildPaginationMeta` — calcado de `useSales()` (`frontend/hooks/data/use-sales.ts:96-160`), no inventado. ⚠️ Hasta este cambio, `mapExpense()` **no participaba del listado**: su único consumidor de lectura era `components/dashboard/recent-activity.tsx:11` (`/gastos` armaba la lista con `usePaginatedQuery({ table: "expenses" })` y su `mapRow` local). Verificar que `recent-activity.tsx` sigue verde contra el contrato nuevo.
- [ ] 9.4 **RED → GREEN** — test que asserta el **set completo de invalidaciones** de las tres mutaciones: `expenses`, `cashSessions`, `cashMovements`, `bankAccounts`, `bankReconciliation` y el reporte de formas de pago. Precedente: en ventas hubo que sumar `customerAccounts` después de que la UI quedara stale.
- [ ] 9.5 **GREEN** — refresco de `/gastos` tras cada mutación (invalidación de `queryKeys.expenses.all()` con los params vigentes; con D18 el listado ya está en TanStack Query, así que **no** hace falta un `pq.refetch()` imperativo). ⚠️ **NO se bumpea el `refreshToken` de `LedgerMovementsPanel`**: la task original pedía algo sin mecanismo posible — ese token es `useState` **local** de `/banco` (`banco/page.tsx:183`) y `/caja` (`caja/page.tsx:243`), no hay contexto, store ni query key que una mutación de `/gastos` pueda tocar, y esos paneles ni siquiera están montados mientras el usuario está en `/gastos` (al navegar a /caja o /banco montan y hacen fetch, `LedgerMovementsPanel.tsx:207`). El repo ya fijó esta división en `use-cash-movements.ts:121-125`. Si algún día se quisiera refresco cross-página, hay que mover el token a un store: **alcance nuevo, no de este change**.
- [ ] 9.6 **GREEN** — `frontend/lib/types.ts`: `Expense` suma `paymentMethodId`, `paymentMethodName`, `branchId`, `isPaymentLocked`. Usar `PaymentMethodKind`, `BANK_PAYMENT_KINDS` e `isBankPaymentKind()` que **ya son canónicos** en ese archivo; está prohibido reimplementar el chequeo por componente.
- [ ] 9.7 **GREEN** — `frontend/lib/ledger/cash-movement-meta.ts`: entrada `expense_reversal` (etiqueta "Reversa de gasto", ícono `RotateCcw`, tono `warning`) en la familia **`reversal`**, y agregarla a `CASH_MOVEMENT_FAMILIES.reversal.types` junto a `sale_reversal`. ⚠️ **No** va a la familia `income`: `sale_reversal` **no está** en `income` (`cash-movement-meta.ts:29`, `types: ["sale", "advance"]`), tiene su propia familia `reversal` (línea 31). "Espejo de `sale_reversal`" es de **familia**; el espejo de **signo** es el opuesto y vive en `backend/schemas/cash.py` (task 7.3). Puesto en `income`, el filtro "Reversas" mostraría las reversas de venta y no las de gasto (D9).
- [ ] 9.8 Regenerar `frontend/lib/database.types.ts`.

## 10. Frontend — formulario de gasto

- [ ] 10.1 **RED** — tests del formulario con el molde de `sale-form-payment-effects.test.tsx`: elegir efectivo con caja abierta muestra el control de caja; elegir transferencia muestra el selector de cuenta bancaria; elegir cuenta corriente **no aparece como opción**.
- [ ] 10.2 **GREEN** — montar `PaymentMethodSelect` en `expense-form-v2.tsx` con `context="expense"`, y **extender el componente** (unión de `context` + rama del texto de apoyo + filtrado de `kind='credit'` en ese contexto). Extensión aditiva: **no** se crea un selector nuevo.
- [ ] 10.3 **GREEN** — montar `BankAccountDestinationSelect` con la condición literal de venta y compra (`isBankPaymentKind` + hay cuentas activas + `!isEdit`), **obligatorio** en gastos (D5), con el mensaje de la organización sin bancos.
- [ ] 10.4 **GREEN** — **[OQ-1]** bloque de opt-in de caja consumiendo `useCashOptin` (task 8.5). Pre-marcado cuando aplica; cuando no aplica **se muestra deshabilitado con el motivo concreto** — nunca oculto en silencio (D4 de `pagos-cableados-restantes`).
- [ ] 10.5 **TRIANGULATE** — casos de motivo visible: sin caja abierta · fecha anterior a hoy · forma de pago no efectivo. Los tres con su texto propio.
- [ ] 10.6 **RED → GREEN** — el formulario envía `cashSessionId` **sólo** cuando el opt-in está elegible y marcado; en cualquier otro caso manda `null`.
- [ ] 10.7 **RED → GREEN** — test a11y del formulario con el molde de `__tests__/a11y/proveedores.a11y.test.tsx` (labels, roles, foco, `aria-describedby` del texto de apoyo).

## 11. Frontend — listado y reporte

- [ ] 11.0 🛑 **GREEN** — **[D18]** migrar el origen de datos de `frontend/app/(dashboard)/gastos/page.tsx`: sale `usePaginatedQuery({ table: "expenses" })` + `mapRow` local (líneas 41-53 y 74-88) y entra el hook paginado de 9.3b, espejo de lo que `/ventas` ya hace con `useSales()`. Sin esto no hay camino de datos para el lock (`is_payment_locked` es un derivado del backend, **no** una columna de `expenses`) ni para el nombre de la forma de pago. Conservar el comportamiento vigente de búsqueda, filtro de fechas y filtro por centro de costo — con su RED previo sobre el estado actual del listado.
- [ ] 11.1 **RED → GREEN** — badge de forma de pago en el listado usando `PaymentMethodBadge` (task 8.2), con el `payment_method_name` que resuelve el backend (espejo de `SaleOut`). Variante desktop y variante mobile.
- [ ] 11.1b **RED → GREEN** — escenario "Desactivar una forma de pago usada por gastos" (`payment-method`): una forma dada de baja **deja de ofrecerse** para gastos nuevos, pero los gastos históricos que la usan **conservan su imputación y su nombre visible** en el listado y en el filtro. El selector del filtro se puebla con `usePaymentMethods(true)` — `includeInactive` es exactamente lo que sostiene este escenario.
- [ ] 11.2 **RED → GREEN** — filtro por forma de pago como **query param server-side** (`payment_method_id`) del endpoint paginado, con el mismo patrón que `useSales()` (`setPaymentMethodId` resetea la página a 0). Ya no va por `extraFilters` de `usePaginatedQuery`: con D18 el listado no usa ese hook.
- [ ] 11.3 **RED → GREEN** — lock visible derivado de los campos del backend (7.5): "Editar" deshabilitado **con la razón** cuando `is_payment_locked`; "Eliminar" deshabilitado **con la razón** cuando `is_delete_blocked` (movimiento de caja + sin sesión abierta en ese `cashbox` → sería `P0426`). Aserción de que los dos estados vienen de los mismos predicados que evalúa el servidor, no de una regla reimplementada en cliente.
- [ ] 11.4 **GREEN** — diálogo de borrado que enumera qué se va a compensar en cada libro antes de confirmar, con el molde del de operaciones.
- [ ] 11.5 **GREEN** — sumar "Forma de pago" al `exportToCSV` **local** de `gastos/page.tsx`.
- [ ] 11.5b **GREEN** — sumar "Forma de pago" al camino del `ExportButton`, que **no es frontend**: vive en la Edge Function Deno `supabase/functions/generate-export/index.ts` — headers del branch `expenses_csv` (`['fecha','categoria','descripcion','monto','moneda','sucursal']`, línea 256) y `fetchExpensesRows` (líneas 91-103), que necesita el join a `payment_methods` para el nombre. Ese mismo `fetchExpensesRows` alimenta la hoja "Gastos" del XLSX consolidado (línea 236-243): la columna aparece en los dos. ⚠️ La Edge Function **tiene deploy propio** — no viaja con la migración del merge; anotar el paso de deploy en el PR. Si sólo se toca `exportToCSV`, los dos exports quedan divergentes.
- [ ] 11.6 **GREEN** — **[OQ-5]** columna de gastos en `lib/payment-method-report.ts` y en `/reportes/formas-pago`, con su test de suma.
- [ ] 11.7 **GREEN** — **[OQ-7]** texto de ayuda del paso 1 de `expense-import-dialog.tsx`: *"Los gastos importados quedan sin forma de pago y sin impacto en caja ni en banco. Podés imputarles la forma de pago después desde el listado, pero eso es sólo una etiqueta: para que el gasto impacte caja o banco hay que cargarlo desde el formulario."* ⚠️ **Prohibida** la redacción original (*"imputalos después desde el listado si querés que impacten caja o banco"*): es **falsa** — la edición no postea movimientos (task 4.6 lo verifica explícitamente y `rpc_update_expense` ni siquiera recibe `p_cash_session_id`/`p_bank_account_id`), así que prometería un efecto que el sistema no produce (D13).
- [ ] 11.8 **RED** — test que asserta que el importador **no** manda forma de pago en el payload de ninguna fila.

## 12. Gate SQL propio y CI

- [ ] 12.1 Consolidar `supabase/tests/test_gastos_forma_pago.sql` con todos los casos de los grupos 2-6, dos tenants sintéticos, y **cleanup sin residuos**.
- [ ] 12.2 Sumar el step del gate nuevo a `.github/workflows/KPI_Validation.yml` y agregar la migración como **último eslabón** de la cadena de reapply idempotente. 🛑 **Además, dejar por escrito en el YAML el razonamiento del reapply de `20260928000001`**: ese reapply ya falla hoy y está tolerado, y aborta en su **gate ANTI-OVERLOAD de la sección 9** (línea ~1813), **antes** del `CREATE` de `rpc_payment_method_report` de la sección 10 — por eso la firma vieja de 7 columnas nunca choca con la nueva de 8 (**42P13**). Verificarlo con la task 1.5 corrida **después** de aplicar la migración nueva, no suponerlo. Si el reapply llegara a la sección 10, la tolerancia hay que ampliarla al SQLSTATE **42P13** de forma **acotada** (grep del marcador, como ya se hace) y reconverger reaplicando `20261015000001` — precedente documentado en `KPI_Validation.yml:66-70` para `20260930000001`.
- [ ] 12.3 Verificar que `test_function_acl_gate.sql` sigue verde **sin tocar su allowlist**: las tres RPCs nuevas son `rpc_*` (fuera del filtro del chequeo (4)) y ninguna lee ni actualiza `public.events` (fuera del chequeo (5)). Si alguno rompe, es señal de que se creó un helper que D4 prohíbe.
- [ ] 12.4 Verificar `test_errcode_5char_gate.sql` verde: cero ERRCODEs nuevos, cero `P0001`.

## 13. Verificación integral

- [ ] 13.1 Suite backend completa contra el baseline de 1.2; suite frontend contra el de 1.3. Cero regresiones.
- [ ] 13.2 `pnpm tsc --noEmit` sin errores nuevos. Gotcha conocido: `next-env.d.ts` queda sucio tras un `next dev`.
- [ ] 13.3 Todos los gates del workflow verdes en local, en el orden de CI, tras un `db reset` limpio.
- [ ] 13.4 🛑 **Verificación visual manual**: formulario y listado de gastos en **desktop y mobile** × **tema claro y oscuro** (4 combinaciones), más los estados del bloque de caja (elegible, sin caja, fecha vieja) y el lock del listado. Capturas en el PR.
- [ ] 13.5 Revisión a11y de la superficie nueva con `web-design-guidelines` (contraste, foco, labels, targets táctiles en mobile).
- [ ] 13.6 Verificar que `token-contrast-aa.test.ts` sigue verde y que **ningún elemento nuevo** usa colores literales. `categoryColors` queda como está (deuda ajena declarada).
- [ ] 13.7 **Recorrido E2E manual**: cargar un gasto en efectivo con caja abierta y verlo en el historial de /caja; cargar uno por transferencia y verlo en pendientes de conciliar en /banco; intentar editar los dos y recibir el bloqueo con motivo; borrar el de efectivo con la caja abierta y ver el contra-movimiento; cerrar la caja e intentar borrar otro y recibir `P0426`.

## 14. Documentación y entrega

- [ ] 14.1 Actualizar `knowledge-base/04_modelo_de_datos.md` (columnas nuevas de `expenses`, vocabulario ampliado de `cash_movements`) y `knowledge-base/05_reglas_de_negocio.md` (RN-93 cumplida para gastos).
- [ ] 14.2 Actualizar `CHANGES.md`: la entrada `gastos-forma-pago` deja de ser candidato diferido y pasa a change con su resultado, incluidas las OQ resueltas y cómo se resolvieron.
- [ ] 14.3 Actualizar el puntero de "próximo change" del `CLAUDE.md` del proyecto si corresponde, y correr el `--fix` del gate Docs Sync para regenerar `AGENTS.md` **en el mismo PR** (nunca editarlo a mano).
- [ ] 14.4 Guardar en engram (`topic_key: opsx/gastos-forma-pago/apply`) las decisiones, los hallazgos y los gotchas del apply.
- [ ] 14.5 PR con commits convencionales en español y el trailer `Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>`. **Nunca commitear a main.**
- [ ] 14.5b **Deploy propio de la Edge Function** `supabase/functions/generate-export` (task 11.5b): **no** viaja con la migración del merge. Dejarlo escrito en el PR y verificar después del deploy que `expenses_csv` y la hoja "Gastos" del XLSX traen la columna nueva.
- [ ] 14.6 Post-merge: verificar en prod (sólo `SELECT`) `MAX(version)` = la migración nueva, las ACLs de las tres RPCs, el `CHECK` vivo de `cash_movements`, y que **ningún gasto histórico** quedó con movimientos huérfanos.
- [ ] 14.7 🛑 Demo en vivo al PO sobre prod: los cinco pasos del recorrido de 13.7. Sin la demo, el change no se archiva.
