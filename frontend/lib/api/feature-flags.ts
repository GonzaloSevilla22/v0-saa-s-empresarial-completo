/**
 * Feature flags for the application.
 *
 * Note: NEXT_PUBLIC_USE_PYTHON_API and NEXT_PUBLIC_USE_PYTHON_API_ETAPA* flags
 * have been removed in C-18 (frontend-decouple-datacontext). The frontend now
 * always uses the Python API directly via React Query hooks.
 */

// Reserved for future feature flags
export const featureFlags = {} as const
