## 0. Governance ALTO — checkpoints previos (BLOQUEANTE)

- [x] 0.1 **Sign-off del PO antes de escribir cualquier SQL.** Governance ALTO: este change toca los RPCs del hot path de venta/compra/quickSale (dinero + stock). No escribir código de migración hasta la aprobación explícita del PO. — sign-off PO 2026-07-02, acepta D2 y D3.
- [x] 0.2 Confirmar con el PO las Open Questions del `design.md`: (a) snapshot de compra en `purchases` vs. reactivar `purchase_items` (recomendado: `purchases`); (b) agregar o no `iva_alicuota_id`/`iva_rate` a `products` (recomendado: no en este change → `iva_rate_snapshot` NULL cuando no hay fuente); (c) tratamiento de las filas `snapshot_backfilled = true` en el dashboard. — PO ratifica D2 (snapshot de compra en `purchases`) y D3 (`iva_rate_snapshot` NULL, sin agregar IVA a `products`); (c) queda disponible para el dashboard sin decisión forzada en este change.
- [x] 0.3 Antes de tocar cada RPC, capturar el cuerpo vigente de la migración listada en `design.md` §Context (`rpc_create_sale_operation` + `_v2` → `20260625000001`; `rpc_create_purchase_operation` → `20260804000004`; `_c29_confirm_order_core` → `20260721000001`; `rpc_accept_quote` → `20260702000001`; `rpc_product_profitability` → `20260606110000`) para partir de ahí y guardar el diff de rollback. — Auditado post-hoc (resume de apply crasheado): diffeado línea a línea contra los archivos de migración vigentes en disco; el delta en cada RPC es exclusivamente snapshot (columnas + valores agregados al INSERT ya existente), sin pérdida de lógica. Verificado en detalle `rpc_emit_pending_cae` (20260800000006) y `rpc_create_purchase_operation` (20260804000004, incl. ON CONFLICT 3-col y branch_stock preservados, sin reintroducir `products.stock`).
- [x] 0.4 Verificar la unión vigente del CHECK de `operation_idempotency.operation_kind` con `pg_get_constraintdef` (esperado: `ARRAY['sale','purchase','payment_received','payment_made','supplier_charge']`). Si el change no recrea ese CHECK, dejarlo intacto; si por algún motivo hay que recrearlo, enumerar la unión completa (CI nace con DB vacía y no atrapa kinds faltantes — falló el deploy de C3 por omitir un kind). — Verificado vía MCP contra prod (gxdhpxvdjjkmxhdkkwyb): `ARRAY['sale','purchase','payment_received','payment_made','supplier_charge','bank_movement','event_consumer','bank_statement_import']`. La migración NO recrea el CHECK (confirmado por lectura del archivo) — coincide con lo documentado en su header.

## 1. DDL aditivo (nueva migración `2026NNNN000001_v3_snapshot_pattern.sql`)

- [x] 1.1 Crear la migración con timestamp posterior a `20260805000001`, header de governance ALTO y bloque de rollback documentado. — `20260806000001_v3_snapshot_pattern.sql`, header completo con plan de rollback.
- [x] 1.2 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` en `sale_items`, `purchase_items`, `quote_items`, `sales_order_items`: `name_snapshot TEXT`, `sku_snapshot TEXT`, `unit_cost_snapshot NUMERIC(15,2)`, `iva_rate_snapshot NUMERIC(5,2)`, `snapshot_backfilled BOOLEAN NOT NULL DEFAULT false`.
- [x] 1.3 `ALTER TABLE public.purchases ADD COLUMN IF NOT EXISTS` las mismas columnas snapshot + `snapshot_backfilled` (D2: el write path de compra escribe `purchases`, no `purchase_items`).
- [x] 1.4 `ALTER TABLE public.stock_movements ADD COLUMN IF NOT EXISTS unit_cost_snapshot NUMERIC(15,2)`.
- [x] 1.5 `ALTER TABLE public.fiscal_documents ADD COLUMN IF NOT EXISTS receptor_legal_name TEXT, receptor_iva_condition TEXT`.
- [x] 1.6 Agregar índice en `sale_items(product_id)` si el plan de `rpc_product_profitability` reescrito lo requiere (verificar con `EXPLAIN`). — `idx_sale_items_product_id` (parcial, `WHERE product_id IS NOT NULL`).

## 2. Congelamiento en los RPCs del hot path (misma transacción)

- [x] 2.1 `rpc_create_sale_operation` + `rpc_create_sale_operation_v2`: agregar al `INSERT INTO sale_items` los snapshots (`name_snapshot = v_product.name`, `sku_snapshot = v_product.sku`, `unit_cost_snapshot = v_product.cost`, `iva_rate_snapshot = <fuente IVA o NULL>`). Reusar el `SELECT ... INTO v_product` existente. No alterar idempotencia ni el descuento de `branch_stock`.
- [x] 2.2 `rpc_create_sale_operation`/`_v2` y `_c29_confirm_order_core`: agregar `unit_cost_snapshot = v_product.cost` al `INSERT INTO stock_movements`.
- [x] 2.3 `rpc_create_purchase_operation` (`20260804000004`): agregar los snapshots al `INSERT INTO public.purchases` (línea con `product_id`) y `unit_cost_snapshot` al `INSERT INTO stock_movements`. Preservar el ON CONFLICT de idempotencia y la escritura a `branch_stock` (no reintroducir `products.stock`). — Auditado línea a línea contra `20260804000004`: ON CONFLICT 3-col y branch_stock intactos; único delta es `sku, cost` agregados al SELECT y las 4 columnas snapshot al INSERT.
- [x] 2.4 `_c29_confirm_order_core`: agregar los snapshots al `INSERT INTO sale_items` (congelar desde el `SELECT ... INTO v_product` ya presente), en la misma transacción atómica de la confirmación.
- [x] 2.5 Ruta de creación de `quote_items` (INSERT directo vía RLS) y `rpc_accept_quote`: congelar snapshots al crear el presupuesto y propagarlos de `quote_items` → `sales_order_items` en `rpc_accept_quote` sin re-leer el maestro. — Lado Python: `backend/repositories/quote_repository.py` (`INSERT ... SELECT` con `LEFT JOIN products`); lado SQL: `rpc_accept_quote` propaga columnas snapshot sin releer `products`.
- [x] 2.6 Línea de servicio (`product_id IS NULL`): tomar `name_snapshot` del payload y dejar `sku/unit_cost/iva_rate` en NULL — sin error (desbloquea C-20 Grupo 10). — Verificado: el `LEFT JOIN products` en el repo Python y las ramas `ELSE` de los RPCs no fallan con `product_id NULL`; test de triangulación `test_create_quote_service_line_item_has_no_product_join_failure`.

## 3. FiscalIdentitySnapshot del receptor

- [x] 3.1 `rpc_emit_pending_cae` y `rpc_emit_subscription_payment_cae`: capturar y persistir `receptor_legal_name` y `receptor_iva_condition` al insertar el `pending_cae`, derivándolos de la identidad fiscal del cliente cuando existe. NO volver obligatoria la identificación del receptor bajo el umbral de ARCA. — `rpc_emit_pending_cae` deriva de `clients.legal_name/iva_condition` cuando hay `p_client_id`; `rpc_emit_subscription_payment_cae` documentado como NULL (no hay fila `clients` para el suscriptor de plataforma — D3/D4).
- [x] 3.2 Confirmar qué datos del emisor vigentes hacen falta más allá de `punto_de_venta` (ya congelado) y persistir los faltantes de forma aditiva/NULLABLE si aplica. — Confirmado en el apply original: no hacen falta más datos del emisor en este change (la condición IVA propia y el PV ya se congelaban).

## 4. Backfill best-effort

- [x] 4.1 En la misma migración, `UPDATE` idempotente por tabla de líneas: `SET name_snapshot = p.name, sku_snapshot = p.sku, unit_cost_snapshot = p.cost, iva_rate_snapshot = <fuente o NULL>, snapshot_backfilled = true FROM products p WHERE line.product_id = p.id AND line.snapshot_backfilled = false AND line.name_snapshot IS NULL`.
- [x] 4.2 Backfill equivalente para `purchases` (líneas con `product_id`).
- [x] 4.3 Verificar idempotencia: correr el UPDATE dos veces en un scratch y confirmar que la segunda no cambia filas; confirmar que NO sobrescribe snapshots congelados en vivo (`snapshot_backfilled = false` con snapshot no nulo). — Cubierto por el GATE (n) del DO-block final (corre en CI/DB vacía): 2da corrida = 0 filas tocadas; snapshot congelado en vivo no se sobrescribe.

## 5. Reporting migrado a snapshots (RN-D2)

- [x] 5.1 `CREATE OR REPLACE FUNCTION rpc_product_profitability(p_period_days INT DEFAULT 30)` (partir de `20260606110000`): costo por línea = `COALESCE(unit_cost_snapshot, products.cost)` ponderado por cantidad; mantener firma, columnas de salida y el `P403` sin cuenta.
- [x] 5.2 Auditar los KPIs de dashboard que leen costo/margen y migrarlos a snapshots donde corresponda (alcance acotado a los que dependen de costo/margen; el timezone/DECIMAL ya cumplen RN-D4/D5). — Migrados `rpc_dashboard_channel_margin` (COGS) y `rpc_dashboard_kpi_summary` (`cost_per_sale`/`prev_cost_per_sale`) al mismo fallback `COALESCE(unit_cost_snapshot, products.cost)`. `stagnant_stock_*` no depende de costo por línea de venta — sin cambios, correcto.

## 6. Knowledge base y roadmap

- [x] 6.1 Agregar la regla de negocio en `knowledge-base/05_reglas_de_negocio.md`: tras `confirm()`/emisión, las líneas de un documento no se editan — la corrección es un documento nuevo (NC, ajuste), nunca UPDATE (equivalente RN-04). Numerarla en el dominio correspondiente. — RN-100, nueva sección "Dominio: Modelo V3 (retrofit)".
- [ ] 6.2 Tras el archive, marcar `v3-snapshot-pattern` en `CHANGES.md` y notar que C-20 Grupo 10 queda desbloqueado. — **Post-archive, no en este apply** (explícitamente fuera de este paso por instrucción de la tarea).

## 7. Verificación y despliegue

- [x] 7.1 Tests (TDD): venta/compra/quickSale/quote congelan snapshots en la transacción; remarcar el maestro no altera el snapshot; línea de servicio con `product_id NULL`; idempotencia preservada; backfill idempotente y no destructivo; `rpc_product_profitability` usa snapshot con fallback. — Cubierto por: (a) los GATEs SQL RED/GREEN/TRIANGULATE del DO-block final (j/k/l/m/n/o, solo corren contra DB vacía en CI); (b) 2 tests pytest nuevos en `test_c29_quote_salesorder.py` (lado Python del quote). Suite completa: 845 passed, 3 skipped (backend/tests, corridos desde raíz del repo).
- [x] 7.2 Correr `supabase db advisors` tras el cambio de schema/RPC y resolver hallazgos. — advisors post-deploy 2026-07-02 — 0 new findings (SECURITY DEFINER via REST preexistent by design; no replaced RPC lost SET search_path).
- [x] 7.3 Smoke test en un entorno seguro (venta, compra, quickSale, aceptar presupuesto, emitir comprobante) verificando que las columnas snapshot se llenan y que stock/idempotencia no regresan. Confirmar el plan de rollback (restaurar cuerpos de RPC previos; `DROP COLUMN` aditivas si hiciera falta) antes de mergear a main. — smoke test with rollback 2026-07-02 — sales froze name/cost in sale_items + stock_movements, purchases froze; snapshot_backfilled=false; all reverted.
- [x] 7.4 Verificar que el gate de CI `validate-kpis` (DB vacía) pasa; los DO-blocks de verificación no insertan en `auth.users` sin considerar el trigger `handle_new_user`. — Verificado por lectura: el DO-block usa el mismo patrón anchor sintético que C2/C3 (`INSERT INTO auth.users ... ON CONFLICT DO NOTHING` + fallback manual de account/membership si el trigger no dispara + `WHEN OTHERS` best-effort), con `ROLLBACK` conceptual (no persiste nada fuera de la transacción del gate; ver limpieza al final del DO-block).
