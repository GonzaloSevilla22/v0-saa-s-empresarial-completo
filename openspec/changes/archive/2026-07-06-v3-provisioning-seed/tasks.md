## 1. Pre-flight (safety net — el trigger es el camino de registro)

- [x] 1.1 Extraer la definición VIGENTE de `handle_new_user` desde `supabase/migrations/20260801000003_register_province_admin_email.sql` (última deployada) y verificar línea a línea que es la base a extender: profile (name/last_name/terms/opt-in/province) + account + membership owner + 2 email_logs. Confirmar que ninguna migración posterior a `20260801000003` redefine el trigger (grep `CREATE OR REPLACE FUNCTION public.handle_new_user`). **Confirmado**: grep de las 10 migraciones que tocan `handle_new_user` — la última es `20260801000003`. Diff byte-a-byte contra la definición VIGENTE en prod (`pg_get_functiondef`) = idéntica.
- [x] 1.2 Confirmar contra prod (SQL read-only vía MCP `execute_sql`, NUNCA `apply_migration`): (a) cuántas cuentas no tienen ninguna branch; (b) cuántas cuentas tienen branch pero cero cashboxes; (c) que el UNIQUE `(account_id, name)` de `branches` existe (`pg_get_constraintdef`). Anotar los conteos en el PR como baseline del backfill. **Baseline (2026-07-06)**: total_accounts=29; accounts_without_branch=**3**; accounts_with_branch_no_cashbox=**24**; constraint `branches_account_name_unique` = `UNIQUE (account_id, name)` confirmado.
- [x] 1.3 Correr la suite pytest completa como baseline (912 esperados verdes) — este change no toca backend, el baseline prueba que nada se rompe por accidente. **Resultado**: 912 passed, 3 skipped, 0 failed.

## 2. Migración `20260812000001_v3_provisioning_seed.sql` (RED primero: el gate es el test)

- [x] 2.1 Escribir la Parte C PRIMERO (behavior gate, DO-block auto-limpiante) como test RED: anchor sintético `auth.users` con email `provisioning-seed@test.local` (`ON CONFLICT DO NOTHING`), resolver la account vía `account_members` (NUNCA insertar accounts a mano), assert: exactamente 1 branch "Casa Central" activa + ≥1 cashbox `currency='ARS'` sobre esa branch. Degradar con `RAISE NOTICE` + salida temprana si el contexto no permite el gate (prod). Limpieza hijo→padre por email del anchor (cashboxes → branches → account_members → accounts → profiles → auth.users), `accounts=0` al final. Contra el trigger actual este gate DEBE fallar (RED) — verificar el fallo razonando/en CI, no hay Docker local. **RED confirmado por razonamiento**: contra la definición vigente en prod (sin seed), `v_branch_count` para "Casa Central" sería 0 → `Assert 1` dispara `GATE ... FAILED`. Mismo patrón RED→GREEN que `20260809000001` (sin Docker local, gate en un solo archivo de migración: B se define antes de C en el archivo final, pero C fue diseñado/razonado primero contra el trigger sin seed).
- [x] 2.2 Parte B (GREEN): `CREATE OR REPLACE FUNCTION public.handle_new_user()` basada en la versión de 1.1, agregando SOLO el bloque de seed dentro de un sub-bloque `BEGIN … EXCEPTION WHEN OTHERS THEN RAISE WARNING … END`: INSERT branch "Casa Central" (`is_active=TRUE, status='active', opened_at=now()`, `ON CONFLICT (account_id, name) DO NOTHING`) + INSERT cashbox "Caja Principal" (`currency='ARS'`) con guard `NOT EXISTS` (cashboxes no tiene UNIQUE). El core provisioning (profile/account/membership/emails) queda FUERA del sub-bloque.
- [x] 2.3 Parte A (backfill idempotente de cuentas existentes): A.1 branch "Casa Central" para cuentas con CERO branches (`NOT EXISTS` sobre `branches.account_id`); A.2 cashbox "Caja Principal" para cuentas cuya(s) branch(es) no tengan ninguna cashbox (`NOT EXISTS` sobre `cashboxes.branch_id`), colgada de la branch default (`c26_default_branch`). Solo INSERTs aditivos, cero UPDATE/DELETE.
- [x] 2.4 TRIANGULATE — extender el gate con los casos de idempotencia: (i) re-ejecutar el bloque de seed para la account del anchor no duplica branch ni cashbox; (ii) simular cuenta "branch sin cashbox" (borrar la cashbox del anchor, re-correr el backfill A.2, assert 1 cashbox y 1 branch). Ambos degradan con NOTICE si no aplican.
- [x] 2.5 REFACTOR + revisión both-worlds-safe: la migración entera es re-ejecutable (doble aplicación por auto-apply de Supabase + `db push` de Actions), sin DDL de schema, `TIMESTAMPTZ` en todo timestamp nuevo, comentario de cabecera con GOVERNANCE/APPLY/ROLLBACK según convención del proyecto (rollback = restaurar la definición de `20260801000003`, backfill no se revierte). **Archivo**: `supabase/migrations/20260812000001_v3_provisioning_seed.sql`.

## 3. Verificación en CI y prod

- [x] 3.1 Abrir PR en feature branch (NUNCA commit a main) y verificar que el job `validate-kpis` corre las migraciones desde cero con el gate nuevo en verde y `accounts=0` al final del reset. Recordar: Actions rojo ≠ migración sin aplicar (auto-apply de Supabase al mergear); el check "Supabase Preview" rojo se ignora (no hay branching en el plan). **PR #279**. Log de CI confirma: `Applying migration 20260812000001_v3_provisioning_seed.sql...` → `NOTICE: GATE PROVISIONING-SEED PASSED: ...` → `Started supabase local development setup.` sin errores. `validate-kpis` = pass (1m46s). "Supabase Preview" rojo, ignorado por convención del proyecto.
- [x] 3.2 Post-merge, verificar en prod (SQL read-only): (a) 0 cuentas sin branch; (b) 0 cuentas sin cashbox; (c) los conteos del backfill coinciden con el baseline de 1.2; (d) ninguna WARNING de seed en los logs de Postgres. **Verificado 2026-07-06 post-merge (PR #279, SHA 689647f)**: `pg_get_functiondef('handle_new_user')` en prod = nueva definición con el sub-bloque de seed. `accounts_without_branch = 0` (era 3). `accounts_with_branch_no_cashbox = 0` (era 24) — coincide exactamente con el backfill esperado (3+24=27 cuentas tocadas de las 29 totales; total_accounts sigue en 29, sin cambios). Anchor del gate (`provisioning-seed@test.local`) con 0 residuos (`auth.users`/`branches`). Deploy Supabase confirmó "Remote database is up to date" en `db push` (Supabase auto-apply ya la había corrido). Advisors leídos: 1 finding de seguridad para `handle_new_user` (SECURITY DEFINER ejecutable por `anon` vía RPC) — PRE-EXISTENTE, inherente a cualquier trigger `AFTER INSERT ON auth.users` con este patrón (no se tocó ningún GRANT/REVOKE en este change); no es un finding nuevo.
- [ ] 3.3 T3 manual (criterio V3 "<5 minutos para vender"): registrar un usuario de prueba en prod y confirmar que puede abrir sesión de caja y hacer una quickSale sin crear branch/caja a mano. Documentar el resultado en el PR. **PENDIENTE — acción manual del PO**: requiere el flujo real de signup en prod (captcha + verificación de email), fuera de lo que un agente debe automatizar contra producción real (evitar registros sintéticos que generen emails reales a `danielsevilla@alia-data.com` o interactúen con el captcha). Verificación DB-level equivalente ya hecha en 3.2 (branch+cashbox se crean correctamente vía el trigger real, ejercitado por el gate de CI y confirmado en prod contra la definición vigente); falta solo la confirmación end-to-end humana de abrir caja + quickSale en la UI. No bloquea el cierre del change (ver design.md: "no bloqueante").

## 4. Docs y cierre

- [x] 4.1 Marcar el checkbox de `v3-provisioning-seed` en `CHANGES.md` §"Roadmap Modelo V3", con nota de lo scoped OUT (price lists / formas de pago / plan de cuentas / unidades — justificación en design.md). **Hecho**: sección `v3-provisioning-seed`, tabla de gap-analysis §7.5, secuencia recomendada y "Próximo trabajo" actualizados con ✅ 2026-07-06 (PR #279).
- [x] 4.2 Registrar las preguntas abiertas restantes del PO (nombre de la caja default si quiere otro; catálogo de formas de pago y `price_lists` como changes futuros del roadmap V3). **Registrado en design.md §"Open Questions"** (ya presente desde propose, sin cambios: nombre "Caja Principal" propuesto/no bloqueante; backfill incluido por diseño; catálogo de formas de pago y `price_lists` quedan como changes futuros del roadmap V3 si el PO los prioriza — ninguno bloquea el cierre de este change).
