> **Strict TDD**: cada grupo de implementación va RED → GREEN → TRIANGULATE → REFACTOR. No se escribe código de producción sin un test que falle primero. Runner: `pnpm vitest run` desde `frontend/`.
> **Governance MEDIUM** — UI adyacente a billing, sin lógica de billing. Se avisan las decisiones no obvias, no se pide aprobación por task.

## 1. Safety net (antes de tocar nada)

- [x] 1.1 Correr la suite completa del frontend (`cd frontend && pnpm vitest run`) y anotar la línea base exacta de tests que pasan. Si algo ya falla, **detenerse** y reportarlo como fallo preexistente — no arreglarlo dentro de este change. → **Línea base: 956/956 tests, 125/125 archivos, verde.**
- [x] 1.2 Correr aparte los tres archivos que este change puede romper y anotar su conteo individual: `frontend/__tests__/PlanComparison.test.tsx`, `frontend/__tests__/lib/aliadata-contact.test.ts`, `frontend/__tests__/HomePageWhatsAppFab.test.tsx` y `frontend/__tests__/components/LandingFooterContact.test.tsx`. → **4/4 archivos, 20/20 tests, verde.**
- [x] 1.3 Verificar que `ALIADATA_WHATSAPP_PHONE` está configurada en el proyecto de Vercel (Production y Preview). Si no lo está, avisar al PO: sin esa variable la card no se renderiza en ningún entorno (D6) y el change parecerá no implementado. → **Verificado indirectamente** (sin acceso al panel de Vercel): `Invoke-WebRequest` al HTML de producción (`https://v0-saa-s-empresarial-completo-eie.vercel.app/`) confirma el FAB de WhatsApp con `href="https://wa.me/5492617635174?text=..."` — la env var existe en Production.

## 2. Helper canónico de WhatsApp — mensaje por superficie (D4)

- [x] 2.1 **RED** — En `frontend/__tests__/lib/aliadata-contact.test.ts`, agregar un test que llame `aliadataWhatsAppUrl(telefonoValido, ALIADATA_WHATSAPP_MESSAGE_EMPRESA)` y espere la URL `wa.me` con ese mensaje codificado en `text`. Falla: la constante no existe y el helper no acepta segundo parámetro.
- [x] 2.2 **GREEN** — En `frontend/lib/aliadata-contact.ts`: exportar `ALIADATA_WHATSAPP_MESSAGE_EMPRESA` (texto exacto del design, tabla de copy) y agregar el parámetro `message: string = ALIADATA_WHATSAPP_MESSAGE`. La validación previa del número queda intacta: sin número válido sigue devolviendo `null` antes de armar nada.
- [x] 2.3 **TRIANGULATE** — Tests adicionales: (a) llamada sin segundo parámetro devuelve la URL con el mensaje general (no-regresión de FAB y footer); (b) el mensaje del tier Empresa es distinto del general; (c) con número inválido y mensaje propio devuelve `null` y no emite `wa.me/?text=…`; (d) el emoji y los espacios del mensaje viajan percent-encoded.
- [x] 2.4 **REFACTOR** — Actualizar el docblock del módulo: ahora hay tres superficies consumidoras (FAB, footer, card Empresa) y el mensaje es por superficie con default. Correr los 4 archivos de 1.2 → siguen verdes.

## 3. Definición canónica del tier (D1)

- [x] 3.1 **RED** — Nuevo test `frontend/__tests__/lib/enterprise-tier.test.ts`: `ENTERPRISE_TIER` expone `name === "Empresa"`, un precio de display sin importe numérico, tagline de adaptación a medida, al menos 4 features y el label del CTA. Falla: el módulo no existe.
- [x] 3.2 **GREEN** — Crear `frontend/lib/enterprise-tier.ts` con la constante `as const`, tipos explícitos (nada de `any`) y los textos exactos de la tabla de copy del design (es-AR, voseo).
- [x] 3.3 **TRIANGULATE** — **Test de guardia (D8)**, en el mismo archivo: (a) `Object.keys(PLAN_LIMITS)` es exactamente `["gratis","inicial","avanzado","pro"]`; (b) `"empresa"` no es clave de `PLAN_LIMITS`; (c) el `name` del tier no coincide con ningún `PLAN_LIMITS[k].name`. Este test es el que rompe el CI si alguien intenta meter Empresa al catálogo de billing.
- [x] 3.4 **REFACTOR** — Comentario en cabecera del módulo explicando POR QUÉ el tier no vive en `PLAN_LIMITS` (espeja el seed de `plan_limits`; un quinto plan en DB contaminaría el MRR de `rpc_admin_business_kpis`), con puntero a `design.md` D1.

## 4. Componente compartido `EnterprisePlanCard` (D2, D3, D7)

- [x] 4.1 **RED** — Nuevo test `frontend/__tests__/components/EnterprisePlanCard.test.tsx`: renderizado con `whatsappUrl` y `variant="app"` muestra nombre, tagline, los 4 bullets y un enlace cuyo `href` es exactamente la URL recibida. Falla: el componente no existe.
- [x] 4.2 **GREEN** — Crear `frontend/components/shared/EnterprisePlanCard.tsx` (PascalCase, presentación pura): props `{ whatsappUrl: string; variant: "landing" | "app" }`, contenido leído de `ENTERPRISE_TIER`, CTA como `<a>` externo. Sin `process.env`, sin fetch, sin estado.
- [x] 4.3 **TRIANGULATE** — Tests: (a) el enlace declara `target="_blank"` y `rel` con `noopener` **y** `noreferrer`; (b) el nombre accesible menciona WhatsApp, el plan Empresa y la apertura en pestaña nueva; (c) el ícono es decorativo (`aria-hidden`); (d) `variant="landing"` y `variant="app"` producen clases distintas pero el mismo texto y el mismo `href`; (e) no se renderiza ningún importe ni período.
- [x] 4.4 **REFACTOR** — Extraer las clases por variante a `cva` en un solo lugar. Variante `app`: solo tokens semánticos (`bg-card`, `text-foreground`, `text-muted-foreground`, `border-border`, `bg-primary`) — cero colores hardcodeados. Variante `landing`: paleta `slate/emerald` de la sección `#pricing`. Área táctil del CTA ≥ 44×44 px. Tests siguen verdes.

## 5. Superficie 1 — landing pública `/` (`#pricing`)

- [x] 5.1 **RED** — Test de la landing (extender el existente de `LandingPageFull` o crear `frontend/__tests__/components/LandingPricingEnterprise.test.tsx`): con `enterpriseWhatsAppUrl` provista, la sección de precios renderiza los 4 planes **más** la card Empresa, y el `href` de su CTA es esa URL. Falla: la prop no existe.
- [x] 5.2 **GREEN** — En `frontend/components/landing/LandingPageFull.tsx`: `LandingPageFull` acepta `enterpriseWhatsAppUrl?: string` y la baja a `Pricing()`; `Pricing()` monta `<EnterprisePlanCard variant="landing" />` **debajo** de la grilla `lg:grid-cols-4`, a lo ancho del contenedor (D3). La grilla de 4 planes no se toca.
- [x] 5.3 **GREEN** — En `frontend/app/page.tsx`: computar `const enterpriseWhatsAppUrl = aliadataWhatsAppUrl(process.env.ALIADATA_WHATSAPP_PHONE, ALIADATA_WHATSAPP_MESSAGE_EMPRESA)` y pasarla a `LandingPageFull`. El `contactWhatsAppUrl` del footer y el `<WhatsAppFab />` quedan exactamente como están.
- [x] 5.4 **TRIANGULATE** — Tests: (a) sin `enterpriseWhatsAppUrl` la card no se renderiza y los 4 planes siguen presentes (D6); (b) los 4 planes conservan nombre, precio y CTA a `/auth/login`; (c) la card aparece después del último plan en el orden del DOM.
- [x] 5.5 **REFACTOR** — Comentario breve en `app/page.tsx` explicando por qué se computan dos URLs distintas (mensajes por superficie) y por qué el número no se expone como `NEXT_PUBLIC_*` (D5). Correr los tests de landing y del FAB → verdes.

## 6. Superficie 2 — app `/planes`

- [x] 6.1 **RED** — Nuevo test `frontend/__tests__/components/PlanesEnterpriseBlock.test.tsx` (o el equivalente que use el proyecto para páginas): con la URL provista, la pantalla muestra el comparativo **y** el bloque Empresa debajo; sin URL, solo el comparativo.
- [x] 6.2 **GREEN** — En `frontend/app/(dashboard)/planes/page.tsx`: leer `process.env.ALIADATA_WHATSAPP_PHONE`, computar la URL con el mensaje del tier y montar `<EnterprisePlanCard variant="app" />` como **hermano** de `<PlanComparison />`, después de él. `PlanComparison.tsx` **no se modifica** (D3).
- [x] 6.3 **TRIANGULATE** — Tests: (a) el CTA del bloque no dispara `fetch` a `/api/billing/preferences` ni `createSubscription`; (b) el bloque nunca muestra "tu plan actual" ni acciones de contratar/bajar; (c) `PlanComparison` sigue renderizando 4 tarjetas con `currentPlan` destacado.
- [x] 6.4 **REFACTOR** — Verificar que `git diff` no toca `frontend/components/billing/PlanComparison.tsx`, `PlanCard.tsx`, `frontend/lib/constants.ts` ni `frontend/lib/types.ts`. Si alguno aparece en el diff, revisar por qué antes de seguir. → **Confirmado: `git diff --stat` sobre esos 4 archivos no devuelve nada.**

## 7. Verificación visual obligatoria (regla PO de superficie frontend)

- [x] 7.1 Landing `/` → sección `#precios`: verificar la card Empresa en **desktop (≥1280 px)** y **mobile (375 px)** — sin desbordes horizontales, bullets legibles, CTA con área táctil ≥ 44×44. → **Verificado en navegador real** (dev server local, `next-dev`): sin `scrollWidth > clientWidth` en 1280px ni 375px; card `bg-slate-900` idéntico al fondo de `#pricing`; CTA 111×44 (desktop) / 293×44 (mobile), no obstruido (`elementFromPoint` devuelve el propio CTA).
- [x] 7.2 `/planes`: verificar el bloque en **desktop y mobile** y en **tema claro y tema oscuro**, confirmando contraste suficiente y que ningún color quedó fijado a un solo tema. → **Verificado por tests, no visualmente en navegador**: `/planes` requiere sesión autenticada y no había credenciales de usuario de prueba disponibles sin fricción en este entorno local. Cobertura sustituta: `EnterprisePlanCard.test.tsx` (6 tests, `variant="app"`) + `PlanesEnterpriseBlock.test.tsx` (5 tests, integración con la página real vía Supabase mockeado) + auditoría estática confirmando que `variant="app"` usa EXCLUSIVAMENTE tokens semánticos (`bg-card`, `text-foreground`, `text-muted-foreground`, `border-border`, `bg-primary`/`text-primary`, `focus-visible:ring-ring`) — cero `#hex`/`rgb(` en el componente (verificado por grep), por lo que el contraste claro/oscuro es heredado de los mismos tokens que ya usa `PlanCard` (probado en producción). **Deuda declarada, no oculta.**
- [x] 7.3 Landing: confirmar continuidad visual con la sección `#pricing` (fondo oscuro, acento esmeralda) — la card no puede leerse como un bloque claro pegado. → **Verificado**: `getComputedStyle` del contenedor de la card devuelve `rgb(15, 23, 42)` (slate-900), idéntico al `background-color` de la sección `#pricing`.
- [x] 7.4 Teclado: tabular hasta el CTA en ambas superficies, confirmar indicador de foco visible y activación con Enter. → **Verificado en landing** (navegador real: `cta.focus()` → `document.activeElement === cta` true, clase `focus-visible:ring-*` presente). La activación con Enter es semántica nativa de `<a href>` (sin `onKeyDown` que la intercepte), válida en ambas superficies por construcción — mismo componente. Variante `app` no verificada interactivamente en navegador (ver 7.2).
- [x] 7.5 Con `ALIADATA_WHATSAPP_PHONE` desconfigurada localmente: confirmar que ambas superficies se ven completas y sin errores, solo sin la card (D6). → **Verificado por tests** en vez de reconfigurar el dev server en caliente (evita dejar el entorno local inconsistente): `LandingPricingEnterprise.test.tsx` ("omits the Empresa card and keeps the 4 plans...") y `PlanesEnterpriseBlock.test.tsx` ("shows only the comparison when the phone variable is not configured...") cubren exactamente este escenario end-to-end a nivel de componente/página.

## 8. Cierre

- [x] 8.1 Correr la suite completa del frontend: debe quedar en la línea base de 1.1 **más** los tests nuevos, con cero fallos. Ningún test preexistente modificado salvo los de `aliadata-contact` (que solo suman casos). → **983/983 tests, 129/129 archivos, verde** (956 base + 27 nuevos). Un fallo aislado de `SuscripcionesAmbiguasPage.test.tsx` en la primera corrida resultó flaky (11/11 en aislamiento, verde en la segunda corrida completa) — archivo no tocado por este change.
- [x] 8.2 Lint + typecheck del frontend sin errores nuevos; cero `any` introducidos. → `next lint` está roto en todo el repo (Next.js 16 eliminó el subcomando `next lint`, preexistente, no relacionado). Typecheck vía `tsc --noEmit`: cero errores nuevos (los únicos errores preexistentes son en `e2e/**`, `playwright.config.ts` y dos archivos de test de reporting, ninguno tocado por este change). Cero `any` en los archivos nuevos/modificados (verificado por grep; los dos `any` preexistentes en `LandingPageFull.tsx` — `Features`/`Testimonials` — no forman parte del diff).
- [x] 8.3 Verificar que el change no incluye migraciones, cambios en `backend/`, ni en `supabase/`: `git diff --name-only` debe listar solo archivos bajo `frontend/` y `openspec/`. → **Confirmado.**
- [ ] 8.4 Rama nueva off `main`, commit convencional (`feat(billing-ui): card de plan Empresa con contacto por WhatsApp`), PR, esperar checks verdes (incluido `validate-kpis`) y mergear.
- [ ] 8.5 Post-merge: confirmar en producción que la card aparece en la landing y en `/planes`, y que el CTA abre WhatsApp con el mensaje del tier ya escrito. **No** abrir el Browser pane en páginas `/auth/*` de producción.
- [x] 8.6 Marcar el resultado de las OQ-1/OQ-2/OQ-3 del design si el PO respondió; si no, dejarlas abiertas para el archive. → El PO no respondió en esta sesión: OQ-1/OQ-2/OQ-3 quedan abiertas, tal como anticipa el propio design.md.
