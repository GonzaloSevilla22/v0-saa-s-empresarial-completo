# Tasks — ventas-unidades-conversion

## 1. Base de datos — migración `20261061000001_ventas_unidades_conversion.sql`

- [ ] 1.1 Verificar los cuerpos VIVOS de `_c29_confirm_order_core`, `rpc_create_sale_operation_v2`, `rpc_create_purchase_operation`, `rpc_atomic_update_sale_operation` y `rpc_atomic_update_purchase_operation` contra prod (sólo `SELECT pg_get_functiondef`) y contra el stack local (`supabase db reset`); registrar el md5 CR-stripped de cada uno y la firma vigente de `rpc_set_product_min_stock` en la cabecera de la migración. Verificación: los cinco md5 coinciden entre prod y local y quedan escritos en el archivo.
- [ ] 1.2 Helper `_uom_normalize_quantity(uuid, uuid, numeric) RETURNS numeric` (`STABLE`, `SECURITY INVOKER`, `SET search_path = public`) con los cuatro casos de D1/D3 y los tokens `P0404` / `P0400 unit_type_mismatch` / `P0400 unit_requires_base_unit`, más rechazo de una normalización que dé `0`; `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated`. Verificación: `SELECT _uom_normalize_quantity(kg_product, gramo, 450)` devuelve `0.4500` y `has_function_privilege('authenticated', ..., 'EXECUTE')` es `false`.
- [ ] 1.3 `rpc_create_sale_operation_v2` y `rpc_create_purchase_operation`: retirar el bloque `v_unit_factor` y obtener `v_qty_norm` del helper, firma intacta. Verificación: `pg_get_functiondef` de ambas contiene `_uom_normalize_quantity(` y no contiene `* v_unit_factor`; el gate `test_confirm_core_integrity.sql` sigue en verde.
- [ ] 1.4 `_c29_confirm_order_core`: `v_qty_norm` desde el helper en lugar de la cantidad cruda, sin tocar guards, caja, cuenta corriente, fiscal ni outbox. Verificación: `quickSale` de 450 g sobre un producto en kg con stock 1 deja `branch_stock = 0.55` y `quantity_delta = -0.45`.
- [ ] 1.5 `rpc_atomic_update_sale_operation` y `rpc_atomic_update_purchase_operation`: pata de aplicación con la cantidad normalizada; pata de reversa con `−quantity_delta` del último movimiento de la fila vieja y fallback al helper si no hay movimiento (D6). Verificación: editar una venta de 450 g a 300 g registra `+0.45` y `-0.3` y deja el stock en `0.7`.
- [ ] 1.6 `branch_stock.min_stock` a `numeric(15,4)` (`USING`), guardado equivalente para `products.min_stock` en local; `DROP FUNCTION rpc_set_product_min_stock(uuid, int)` + `CREATE ... (uuid, numeric)` con el mismo cuerpo y el mismo `GRANT`. Verificación: `information_schema.columns` reporta `numeric` con escala 4 y `pg_proc` tiene una sola firma de la RPC.
- [ ] 1.7 Gate de introspección embebido al final de la migración: las cinco funciones llaman al helper, ninguna conserva la multiplicación inline, ACLs del helper cerradas, una sola firma de `rpc_set_product_min_stock`, tipo de `min_stock`. Verificación: `supabase db reset` limpio pasa; un `sed` que reintroduzca `* v_unit_factor` en una copia local hace fallar el reset.

## 2. Gate SQL y CI

- [ ] 2.1 `supabase/tests/test_ventas_unidades_conversion.sql` con fixtures propias (cuenta, sucursal, kg/g/L, productos base-kg / base-g / sin-base) y cleanup bajo `session_replication_role = replica`; bloques: cinco caminos × base kg + línea g; base g + línea kg (=500); sin base + mL rechazado con `unit_requires_base_unit`; tipo cruzado rechazado en cada camino con cero rastro (sin movimiento, caja, outbox); edición en gramos con las dos patas; borrado revierte lo normalizado; invariante `SUM(quantity_delta) = branch_stock.quantity` con unidades mixtas. Verificación: el gate pasa contra el stack local y cada bloque falla si se comenta la línea del helper correspondiente (control negativo documentado en el archivo).
- [ ] 2.2 Bloque de `min_stock` en el mismo gate: `rpc_set_product_min_stock(producto, 0.5)` propaga `0.5` a todas las sucursales, una venta que baje la cantidad a `0.45` inserta `low_branch_stock_alert`, `get_dashboard_critical_stock` cuenta el producto, y un mínimo entero de un producto por unidad no cambia. Verificación: las cuatro aserciones pasan.
- [ ] 2.3 Cablear el gate en `.github/workflows/KPI_Validation.yml` (mismo formato `psql -v ON_ERROR_STOP=1 ... -f`) y re-ejecutar localmente los cinco gates que introspectan las funciones reescritas (`test_confirm_core_integrity`, `test_operacion_party_guard`, `test_cuenta_corriente_party_guard`, `test_tenancy_guard_caja_outbox`, `test_document_status_transition_role_matrix`) más `test_function_acl_gate.sql` y `test_edicion_preserva_contexto.sql`. Verificación: todos en verde en el orden real del workflow; cualquier ajuste a un gate existente queda comentado con el motivo en el propio archivo.

## 3. Backend

- [ ] 3.1 `backend/schemas/products.py`: `min_stock` pasa a `Decimal` con `ge=0` en `ProductIn`, `Decimal | None` en `ProductUpdate` y en la fila de import; `backend/repositories/product_repository.py`: `_SET_MIN_STOCK_SQL` con `$2::numeric` y `_propagate_min_stock(..., min_stock: Decimal)`. Verificación: `POST /products` con `min_stock = 0.5` responde `201` y devuelve `0.5`; con `-1` responde `422`.
- [ ] 3.2 Actualizar `test_products.py`, `test_product_repository_min_stock_realign.py`, `test_product_repository_c21.py` y `test_products_category_sku.py` (casos decimales, negativo rechazado, entero intacto) y agregar la traducción de los tokens `unit_type_mismatch` / `unit_requires_base_unit` al mapa RFC 7807 existente si el router de ventas/compras traduce `P0400` por token. Verificación: `pytest backend/tests -q` en verde con cobertura ≥ 87%.

## 4. Frontend — capa canónica

- [ ] 4.1 `lib/unit-utils.ts`: `compatibleUnits(units, baseUnit)` (D5) y `toBaseQuantity(displayQty, unit, baseUnit)` con división por el factor de la base y redondeo a 4 decimales; `isProductoMedible` y demás predicados intactos. Verificación: tests Vitest nuevos en `lib/__tests__/unit-utils.test.ts` cubren base kg + g → 0.45, base g + kg → 500, sin base → sólo factor 1, tipo cruzado excluido, redondeo `450 × 0.001`.
- [ ] 4.2 `lib/operation-errors.ts` (y el `friendlyError` del POS, ya unificado por `candidatos-ux`): mensajes para `unit_type_mismatch` y `unit_requires_base_unit` con el nombre de la unidad y del producto. Verificación: test unitario del mapeo de errores con los dos tokens.

## 5. Frontend — selectores de unidad

- [ ] 5.1 POS (`app/(dashboard)/ventas/pos/page.tsx`): opciones desde `compatibleUnits`, base del producto preseleccionada al elegir producto, reinicio de la unidad y de la cantidad al cambiar de producto, validación local con `toBaseQuantity(..., baseUnit)`; retirar `unitFactor` del `CartItem` a favor de `quantityBase`. Verificación: test del POS con producto en kg que ofrece sólo kg/g/tn y que al cambiar a un producto en litros reselecciona Litro; `tsc` sin errores.
- [ ] 5.2 `components/forms/sale-form.tsx` y `components/forms/purchase-form.tsx`: mismos cambios sobre la misma función, incluida la ruta de edición (la unidad prefilleada desde la lectura debe seguir siendo compatible o caer a la base). Verificación: tests de ambos formularios con los tres casos de D5 y con la edición de una línea en gramos.
- [ ] 5.3 Escaneo de código de barras en el formulario de venta: la línea agregada por escaneo toma la unidad base del producto y la cantidad mínima de esa unidad (sin cambio funcional, sólo confirmar que no usa `unitFactor`). Verificación: test existente de `handleBarcodeScan` sigue en verde.

## 6. Frontend — la unidad en pantalla

- [ ] 6.1 `app/(dashboard)/stock/page.tsx`: columna de stock y fila expandida con `formatStock(stock, símbolo)` resolviendo la unidad base por `useUnitsOfMeasure` + `resolveUnit`; retirar el `"uds"` fijo; el mínimo con el mismo símbolo. Verificación: test de la página con un producto en kg muestra `0.550 kg` y `0.550 / 0.500 kg`, y uno sin unidad `12 uds`.
- [ ] 6.2 `components/stock/stock-movements-panel.tsx`: delta y antes/después con `formatQuantity` y el símbolo de la unidad base del producto, resuelto por los mapas cacheados de productos y unidades (sin consulta nueva). Verificación: test del panel con un movimiento `-0.45` sobre un producto en kg muestra `-0.450 kg` y `1 kg → 0.550 kg`.
- [ ] 6.3 `lib/receipt.ts` + `components/ventas/sale-receipt-button.tsx`: `ReceiptData.items[].unit`, relleno desde `unitId`, impreso en HTML, texto y texto corto con `formatQuantity`. Verificación: tests de `receipt.ts` con una línea de `450 g` en los tres formatos y una de `3` sin unidad.
- [ ] 6.4 Verificación visual de POS, `/stock` e historial en desktop y mobile, tema claro y oscuro (regla PO 2026-08-02), con capturas en el PR. Verificación: capturas adjuntas y sin regresión de contraste (tokens semánticos, sin colores literales nuevos).

## 7. Integración, documentación y cierre

- [ ] 7.1 Corrida completa local: `supabase db reset`, los 41 gates de `KPI_Validation.yml` en orden, `pytest` con cobertura, `vitest`, `tsc`. Verificación: todo en verde; cualquier fallo preexistente conocido se nombra en el PR con evidencia de que es ajeno.
- [ ] 7.2 `CHANGES.md`: entrada del change con las decisiones D1–D9, el movimiento dañado (`cf4550c0-…`) y los tres candidatos que quedan (unidad base de los 37 productos, ranking con unidades mixtas, `Math.ceil` del importador); `knowledge-base/05_reglas_de_negocio.md` RN-24 actualizada con la regla de normalización (unidad base del producto, mismo tipo, sin base sólo factor 1) y RN-23 con el umbral fraccionario; `CLAUDE.md` (ítem del roadmap) + `python scripts/ci/check_docs_sync.py --fix`. Verificación: el gate `Docs Sync` pasa y la KB describe la conversión vigente.
- [ ] 7.3 Verificación post-merge en prod (sólo `SELECT`): `MAX(version)`, tipo de `branch_stock.min_stock`, firma única de `rpc_set_product_min_stock`, ACLs del helper, los cinco cuerpos vivos con la llamada. Verificación: los cinco chequeos registrados en `CHANGES.md` con fecha.
- [ ] 7.4 Humo real del PO en prod: producto en kg con stock 1, venta de 450 g desde el POS y desde el formulario, edición a 300 g, borrado, `/stock` en `0.550 kg`, mínimo `0.5` con alerta; decisión del PO sobre el ajuste manual del movimiento dañado y sobre OQ-1. Verificación: resultado y decisiones anotados en `CHANGES.md`.
