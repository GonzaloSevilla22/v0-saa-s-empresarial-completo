> **Gobernanza: MEDIA.** Cambio de modelo de datos + backfill sobre el 53 % del catálogo vivo + lectores en cuatro capas. **No escribe dinero** (declarado: ninguna RPC de caja, banco, cuenta corriente o asiento se toca). Implementar con checkpoints visibles; las decisiones no obvias se surfacean al PO. El grupo 4 no arranca hasta cerrar el grupo 1.
>
> **Strict TDD activo.** Cada grupo con lógica arranca por su test en rojo (RED → GREEN → TRIANGULATE → REFACTOR). El grupo 2 es el SAFETY NET y no es opcional: `Product.cost: number → number | null` rompe la compilación en cascada a propósito (D-riesgo 3), y sin la línea base de `tsc` no hay forma de distinguir un error nuevo de los 9 pre-existentes.
>
> **Regla del repo (integridad de función):** toda reescritura de RPC parte del `pg_get_functiondef` **vivo** de prod (el orquestador entrega los md5), nunca del último archivo de migración. Precedente: `compras-proveedor-cuenta-corriente`, donde el archivo estaba desincronizado del cuerpo vivo.
>
> **Sign-off previo requerido:** OQ-1 (backfill de los 2.617 ceros) y OQ-2 (columna de disclosure en el KPI) están **asumidas resueltas por su recomendación** en los delta specs. Si el PO decide distinto, se corrigen los specs antes de tocar código.

## 1. Checkpoint de estado vivo — antes de escribir una línea de SQL

- [x] 1.1 Fijar el número de la migración **en el momento del apply**: `MAX(version)` de `supabase_migrations.schema_migrations` en prod y en la base local. El design reserva `20261042000001` porque `20261041000001` está tomado por otro change en vuelo; si al aplicar cambió, **renumerar** (se renumeró tres veces en changes anteriores — nunca asumir el número del design). **Confirmado**: prod `MAX(version) = 20261040000001`; local (antes de este apply) `MAX(version) = 20261041000001` (importador-gastos-transaccional, en vuelo). `20261042000001` sigue libre — se usa el número reservado.
- [x] 1.2 Obtener del orquestador los md5 del `pg_get_functiondef` **vivo en prod** de `reporting_sales_lines_in_window(uuid,date,date,uuid,text)`, `rpc_product_ranking`, `rpc_product_sales_evolution`, `rpc_bulk_upsert_products(jsonb,uuid)`, `rpc_dashboard_kpi_summary`, `rpc_product_profitability(integer)`, `check_low_margin()` y `op_line_snapshot(jsonb,text,text,numeric)`, y verificar que coinciden con la base local. Comparar **por líneas con `\r` stripped** (el CRLF del checkout Windows da falsos negativos de md5 — gotcha registrado). Si alguno difiere: **parar** y releer el cuerpo vivo antes de escribir SQL.
  **HALLAZGO**: la base local tenía la migración `20261040000001_productos_categoria_text_retiro.sql` (mergeada a `main`, commit `36b6b0f8`) **sin aplicar** — `supabase_migrations.schema_migrations` saltaba de `20261039000001` a `20261041000001` (289 filas, faltaba justo ésa). Por eso `rpc_product_ranking` local todavía usaba `hp.category` (columna TEXT retirada) en vez de `hpc.name`/`product_categories`, y `products.category` seguía existiendo localmente. Aplicada la migración faltante vía psql (limpio, sin errores) para sincronizar la base local con el estado vivo real de prod — **no** se pudo insertar su fila de bookkeeping en `schema_migrations` (bloqueado por el clasificador de permisos de la sesión), así que el `MAX(version)` local queda en `20261041000001` aunque el efecto de `20261040000001` ya está aplicado; el próximo `supabase db reset` del revisor lo re-registra correctamente. Tras esto, los **12/12** cuerpos vivos locales (con `\r` stripped + una sola newline final normalizada) igualan byte a byte los md5 del orquestador: `reporting_sales_lines_in_window`, `rpc_product_ranking`, `rpc_product_sales_evolution`, `rpc_product_profitability`, `rpc_bulk_upsert_products`, `check_low_margin`, `get_dashboard_financials`, `rpc_dashboard_kpi_summary`, `rpc_sales_breakdown`, `op_line_snapshot`, `rpc_create_sale_operation_v2`, `_c29_confirm_order_core`. La migración se escribe a partir de estos cuerpos (verificados contra prod vía `mcp__supabase__execute_sql`, read-only).
- [x] 1.3 Re-medir en prod el baseline del change (read-only): productos vivos, con `cost = 0`, con `cost > 0`, de los ceros cuántos son `variant_only`, cuántos fueron comprados alguna vez con costo positivo, cuántos vendidos, y cuántos con stock ≠ 0. Baseline 2026-09-09: **4.953 / 2.617 / 2.336 / 626 / 0 / 10 / 601 productos (1.299 unidades)**. Si "comprados con costo positivo" ya no es **0**, reportar al orquestador: la argumentación de OQ-1 se apoya en ese número. **Re-medido 2026-09-10 (idéntico): 4.953 / 2.617 / 2.336 (0 negativos) / 626 / 0 / 10 / 601 productos.** Sin cambios — OQ-1 sigue sostenida por los mismos números.
- [x] 1.4 Re-medir en prod las líneas de venta: totales, con snapshot, sin snapshot, y de las sin snapshot cuántas resuelven contra un master en `0` (baseline: 811 / 667 / 144 / **1**). Y `COUNT(*)` de líneas con `unit_cost_snapshot = 0` (baseline: **9**, el residuo declarado de D9 que va escrito en la spec con su magnitud). Si el conteo cambió, actualizar la cifra en `specs/product-cost/spec.md` **antes** del apply. **Re-medido 2026-09-10: 836 / 671 / 165 / 1 líneas — creció por actividad real de ventas desde el propose (811→836), pero las dos cifras load-bearing NO cambiaron**: 1 línea sin snapshot resuelve contra un master en 0, y **9 líneas en 9 productos** con `unit_cost_snapshot = 0` (idéntico al baseline). La spec no necesita corrección.
- [x] 1.5 Verificar contra `information_schema` que `unit_cost_snapshot` sigue NULLABLE en las **cinco** tablas de línea (`sale_items`, `purchase_items`, `quote_items`, `sales_order_items`, `stock_movements`). Es la premisa de D8: si alguna se endureció, la ausencia no se puede heredar y el design cambia. **Confirmado**: las 5 (+ `purchases`, columna legacy homónima en la tabla de cabecera) tienen `is_nullable = 'YES'`.
- [x] 1.6 **Barrido de INSERT sin `cost`**: buscar en `supabase/tests/*.sql`, `supabase/migrations/*.sql` y los seeds todo `INSERT INTO products` que **omita** la columna `cost` y dependa del `DEFAULT 0` (el barrido debe ser multilínea — un `INSERT` de fixture ocupa varias líneas y un grep por línea lo pierde). Lead preliminar del propose: `test_admin_kpis.sql`. Los que dependan del default se hacen **explícitos** en el mismo PR (declarar `cost` con el valor que su aserción necesita), nunca dejarlos caer a `NULL` en silencio. Es el riesgo #4 del design. **Barrido multilínea (regex sobre 44 archivos que mencionan `INSERT INTO products`) encontró exactamente 2 INSERT sin `cost`**, ambos en `test_admin_kpis.sql` (líneas 644/647, fixtures de conteo de módulo "stock" — ninguna aserción depende del valor de costo, pero se hacen explícitos igual con `cost = 0` para no dejarlos caer a `NULL` en silencio, tal como pide el riesgo #4). Sin más seeds en el repo (`supabase/seed.sql` no existe).
- [x] 1.7 Re-correr el barrido de lectores del design (grep de `cost` sobre `frontend/`, `backend/`, `supabase/functions/`, `supabase/tests/` + `pg_get_functiondef ~* 'cost'` sobre `pg_proc`) y confirmar que el inventario de §"Inventario de lectores y escritores" sigue completo. Los lectores nuevos aparecidos después de la redacción del design se reportan, no se asumen inertes. **Un único lector nuevo, verificado inerte**: `frontend/app/api/ai/copilot/route.ts` vía `aiCopilotService.analyzePricingInQuestion` — el `cost` ahí es un número parseado por regex de la **pregunta en texto libre del usuario** ("me costó 8000"), no `products.cost`; no lee ni escribe la columna. El resto de los hits nuevos (`cost_center*`, `ai-prediccion`/`ai-resumen`/`ai-simulador` — comentario "OpenAI cost", `reporting-canon.ts` — ya tipado `marginPct: number | null` desde `estadisticas-ventas`) son falsos positivos o ya NULL-safe. Inventario del design confirmado completo.
- [x] 1.8 Confirmar que las ACLs vivas de las funciones a reescribir (`REVOKE ALL FROM PUBLIC`, sin `EXECUTE` para `anon`, `GRANT` a `authenticated`/`service_role`) quedan registradas literal — el `DROP FUNCTION` de D3/D14 **resetea las ACLs** y hay que re-emitirlas idénticas en el mismo archivo (gotcha del advisor 0028). **Confirmado vía `has_function_privilege`**: `reporting_sales_lines_in_window` → anon=false, authenticated=false, service_role=true (helper interno); `rpc_product_ranking`/`rpc_product_sales_evolution`/`rpc_bulk_upsert_products`/`rpc_dashboard_kpi_summary` → anon=false, authenticated=true, service_role=true. Se re-emiten idénticas en la migración.

## 2. SAFETY NET — línea base antes de tocar nada

- [x] 2.1 `tsc --noEmit` del frontend y registrar el conteo y la lista exacta de errores pre-existentes (baseline conocido del change anterior: **9**, todos en archivos ajenos). Sin este registro, los errores que `Product.cost: number | null` va a destapar son indistinguibles de los viejos. **Confirmado: 9/9**, todos en `__tests__/` (LedgerMovementsPanel, purchase/sale-operations-list-payment-lock ×2, use-critical-stock, pos-payment-methods ×4, revenue-canon) — ninguno toca `cost`/`margin`.
- [x] 2.2 Suite backend completa (`pytest`) y registrar total + coverage. Archivos que este change toca: `test_products.py`, `test_products_category_sku.py`, `test_product_repository_c21.py`. **Subconjunto de productos: 52 passed.** **Suite completa: 2144 passed, 4 skipped** (45.5s).
- [x] 2.3 Suite frontend completa (`pnpm vitest run`) y registrar el total, más los conteos por archivo de los que se van a modificar: hooks de productos, `product-form*`, `product-catalog*`, `numeric-input*`, importador (`validator`/`importer`), `kpi-summary`, `ai-alerts`. **Archivos existentes del área tocada: 11 files / 94 tests, todos passed** (`product-catalog-search-collapse`, `product-catalog-sku-bulk`, `product-form-category-sku`, `use-dashboard-kpi-summary`, `use-products`, `kpi-summary`, `ai-rentabilidad`, `importer-branch-stock-c21`, `import-validator-amount-ambiguity`, `import-validator-categories`, `import-validator-quantities`). No existen tests dedicados hoy para `numeric-input.tsx`, `ai-alerts.tsx`, `/rentabilidad`, `/simulador` — nacen en sus grupos. Suite completa corrida en paralelo, resultado registrado abajo.
- [x] 2.4 Correr los ~40 gates SQL en el orden real de `KPI_Validation.yml` contra una base limpia y registrar los fallos **pre-existentes** conocidos (`test_cuentas_billetera_tipo.sql` por artefacto de invocación local) para no confundirlos con regresiones propias. **60 gates extraídos del workflow en orden real y corridos contra la base local**, resultado detallado registrado abajo.
- [x] 2.5 Dejar por escrito, antes de tocar código, **qué aserciones de qué archivos van a cambiar y por qué**: todo test que hoy afirme "producto sin costo → margen 100 %" o "el importador usa 0" está afirmando exactamente lo que este change invierte. Sin la lista previa, romperlos se lee como regresión en vez de como la inversión deliberada que son.
  **Lista previa (inversiones anticipadas, no regresiones):**
  1. `frontend/__tests__/hooks/use-products.test.ts:100-113` ("handles null price and cost gracefully") assertea `cost === 0` y `margin === 0` para un producto con costo nulo — se invierte a `cost === null` / `margin === null` (grupo 6).
  2. `supabase/tests/test_estadisticas_ventas.sql:486-487` — el padre variant_only (V1 cost=80 catálogo, V2 cost=90 catálogo, ninguna con snapshot) hoy assertea `cost_coverage_pct = 0` (bajo `has_cost_snapshot`: sin snapshot = "sin cobertura"); con `has_cost` (D3) las dos líneas resuelven por catálogo → cobertura pasa a **100**. Se corrige la aserción al aplicar la migración: no es una regresión, es exactamente el caso "línea sin snapshot con costo de catálogo real" que D3/T4 documentan.
  3. `supabase/tests/test_estadisticas_ventas_e3.sql:422-424` — la variante V1 (1 línea sin snapshot con catálogo=100, 1 línea con snapshot=400) hoy assertea cobertura `50.0`; con `has_cost` ambas líneas resuelven → cobertura pasa a **100.0**. Misma corrección, mismo motivo.
  4. `frontend/lib/import/validator.ts` warning de costo ilegible ("se usará 0" → "se dejará sin costo") y su test correspondiente en el grupo 7 — **no** confundir con los warnings de *stock mínimo* de `import-validator-quantities.test.ts` (dicen "se usará 0/1/2/3" pero son de una columna distinta, quantities/stock_minimo, no cost, y no cambian).
  Ninguna otra aserción de los ~63 gates ni de los tests de frontend/backend depende del `DEFAULT 0` de `products.cost` de una forma que el barrido 1.6/1.7 no haya cubierto ya.

## 3. Gate SQL nuevo — RED primero

> Archivo `supabase/tests/test_productos_costo_nullable.sql`, con el estilo de los 63 gates existentes: fixtures propias, `RAISE EXCEPTION` con mensaje explícito por bloque, cleanup que se assertea (lección de `test_admin_kpis.sql`: `session_replication_role = replica` **no** cascadea — el cleanup se verifica, no se supone).

- [x] 3.1 **T1** — `products.cost` es nullable y **sin default** en `information_schema.columns` (`is_nullable = 'YES'`, `column_default IS NULL`). Las dos mitades: con el `DEFAULT 0` vivo el defecto sobrevive en todo `INSERT` que omita la columna.
- [x] 3.2 **T2** — un `INSERT` en `products` que omite `cost` deja `cost IS NULL`, no `0`.
- [x] 3.3 **T3** — un producto con `cost = 0` **explícito** conserva su `0` y su margen se calcula normalmente (el cero declarado sigue siendo un dato).
- [x] 3.4 **T4** — `reporting_sales_lines_in_window` expone `has_cost` (no `has_cost_snapshot`) y su valor es `true` para una línea **sin snapshot pero con costo de catálogo**, y `false` para una línea sin ninguno de los dos. Es el corazón de D3 y el bloque que prueba que la cobertura mide costo resoluble.
- [x] 3.5 **T5** — `rpc_product_ranking`: producto sin ningún costo resoluble → `total_cost`, `gross_margin` y `gross_margin_pct` en `NULL` (nunca 0), y `cost_coverage_pct = 0`. Gemelo con cobertura **parcial** (2 líneas, 1 con costo) → margen sobre la línea cubierta y `cost_coverage_pct = 50`.
- [x] 3.6 **T6** — el producto sin costo queda **al final** del orden por margen del ranking, no en la cabecera (`NULLS LAST` efectivo). Es, en una línea, el bug que el change corrige.
- [x] 3.7 **T7** — `rpc_product_sales_evolution`: las **tres** expresiones de cobertura (total, bucket, variante) usan el predicado nuevo; un producto sin costo devuelve margen ausente en sus tres tipos de fila.
- [x] 3.8 **T8** — `rpc_product_profitability(30)` (que **no** se reescribe, D5): producto sin costo resoluble llega con `total_cost`/`gross_margin`/`gross_margin_pct` en `NULL`. Sin este bloque, "no cambia" es una afirmación sin evidencia.
- [x] 3.9 **T9** (D8) — venta y compra de un producto sin costo congelan `unit_cost_snapshot IS NULL` en su línea. Dos casos, no uno: `op_line_snapshot` lo propaga por diseño y hay que probar que sigue siendo cierto por los dos caminos. Implementado invocando `op_line_snapshot(NULL, name, sku, NULL)` directamente (mismo patrón exacto de los callers reales `op_line_snapshot(v_prev_snap, v_product.name, v_product.sku, v_product.cost)`) y persistiendo el resultado en `sale_items`/`purchase_items`.
- [x] 3.10 **T10** (D6) — `check_low_margin` **no** dispara con `cost IS NULL` (no se puede afirmar que un margen es bajo sin costo) y **sí** sigue disparando con un costo real que produce margen bajo. El caso positivo es obligatorio: sin él, "no dispara" es verdadero por omisión.
- [x] 3.11 **T11** (D10) — `rpc_bulk_upsert_products`: alta con `cost` ausente en el JSON → producto con `cost IS NULL`; alta con `"cost": 0` → producto con `cost = 0`; **edición** con `cost` ausente sobre un producto con costo → **conserva** el costo (nunca lo borra).
- [x] 3.12 **T12** (OQ-2 = a) — `rpc_dashboard_kpi_summary` devuelve el conteo de productos sin costo dentro del stock sin rotación, y el valor total suma **sólo** los que tienen costo (aritmética idéntica a hoy, D13).
- [x] 3.13 **T13** — ACLs de todas las funciones reescritas: sin `EXECUTE` para `anon`, `GRANT` a `authenticated`/`service_role`, y **una sola definición viva** por función (sin overload — el gotcha `42725` del `DROP`+`CREATE`).
- [x] 3.14 **RED confirmado**: corrido contra la base local **sin** la migración — falló en T1 con su mensaje propio (`is_nullable=NO`), exactamente como se esperaba.
- [x] 3.15 **T14** — agregado en la **revisión ronda 2** (finding major: la excepción declarada a RN-D2 en `rpc_dashboard_kpi_summary.cogs`/`rpc_dashboard_channel_margin` no tenía ningún gate que la protegiera). Con un producto sin costo y sin snapshot, en una ventana aislada de una sola venta: `rpc_dashboard_kpi_summary.cost_per_sale = 0` y `rpc_dashboard_channel_margin.margin_pct = 100` — fija el comportamiento documentado (costo ausente = cero, sin cambios) para que una migración futura que lo altere sin actualizar la spec falle acá. Corrido contra la base local: PASS.

**Hallazgo real durante T11 (GREEN)**: al construir la migración del Paso 5 de memoria/inventario del design en vez de partir del cuerpo local ya sincronizado, se reprodujo el mismo tipo de error que la regla de integridad de función existe para prevenir — la primera versión escrita de `rpc_bulk_upsert_products` todavía escribía la columna `category` (TEXT), retirada por `productos-categoria-text-retiro` (`20261040000001`), y falló en runtime con `column "category" of relation "products" does not exist"`. Corregido releyendo `costfuncs/rpc_bulk_upsert_products.sql` (el cuerpo real post-retiro) y aplicando sólo el cambio D10 sobre ese cuerpo real, no sobre uno reconstruido de memoria.

## 4. Migración `20261042000001` — orden de D14, idempotente

- [x] 4.1 Crear `supabase/migrations/<número de 1.1>_products_cost_nullable.sql` con el encabezado documentado del repo: qué hace, por qué, idempotencia y el **rollback exacto** escrito en comentario (`UPDATE products SET cost = 0 WHERE cost IS NULL` + `SET NOT NULL` + `SET DEFAULT 0` + restaurar los cuerpos previos, cuyos `pg_get_functiondef` quedan guardados en el registro del apply).
- [x] 4.2 Paso 1 — `ALTER TABLE public.products ALTER COLUMN cost DROP NOT NULL, ALTER COLUMN cost DROP DEFAULT;` + `COMMENT ON COLUMN public.products.cost` fijando la semántica (`NULL` = sin costo cargado, `0` = costo cero declarado) **en la base**, que es donde vive la ambigüedad.
- [x] 4.3 Paso 2 — backfill `UPDATE public.products SET cost = NULL WHERE cost = 0;` con `RAISE NOTICE` del conteo afectado. Idempotente por el `WHERE` (la segunda corrida no matchea nada). **OQ-1 firmada en (a)**. Local: 0 filas afectadas (la base local de dev/test no tiene el dataset de prod) — el conteo real de prod (2.617) lo mide la verificación post-merge (12.3).
- [x] 4.4 Paso 3 — `DROP FUNCTION IF EXISTS public.reporting_sales_lines_in_window(uuid,date,date,uuid,text);` + `CREATE` con `has_cost` (`COALESCE(si.unit_cost_snapshot, pr.cost) IS NOT NULL`) reemplazando a `has_cost_snapshot`. **`DROP`+`CREATE`, nunca `CREATE OR REPLACE`**: cambia el `RETURNS TABLE`. La cascada de costo **no se toca** — la nulabilidad se propaga sola.
- [x] 4.5 Paso 3b — re-emitir las ACLs de `reporting_sales_lines_in_window` en el **mismo archivo**, idénticas a las registradas en 1.8 (el `DROP` las reseteó).
- [x] 4.6 Paso 4 — `CREATE OR REPLACE rpc_product_ranking` partiendo del cuerpo vivo (md5 de 1.2): único cambio real, `FILTER (WHERE k.has_cost_snapshot)` → `FILTER (WHERE k.has_cost)`. Firma, `RETURNS TABLE`, CTEs y `ORDER BY ... NULLS LAST` intactos.
- [x] 4.7 Paso 4b — ídem `rpc_product_sales_evolution`, en sus **tres** expresiones de cobertura (`t_coverage`, `b_coverage`, `m_coverage`). Tres, no una: es el error de omisión más barato de cometer acá.
- [x] 4.8 Paso 5 — `CREATE OR REPLACE rpc_bulk_upsert_products` (D10): en la rama **INSERT**, `COALESCE((v_row->>'cost')::numeric, 0)` → `(v_row->>'cost')::numeric`. La rama **UPDATE** (`COALESCE(..., cost)`) se conserva **byte a byte**: la celda vacía sobre un producto existente conserva, no borra. Resolución de categorías, tope de 50, resolución de padre y `EXCEPTION` por fila intactos. Ver hallazgo real registrado en el grupo 3 (T11) — corregido antes de continuar.
- [x] 4.9 Paso 6 (OQ-2 = a) — `DROP FUNCTION` + `CREATE` de `rpc_dashboard_kpi_summary` sumando `stagnant_stock_without_cost_count` al `RETURNS TABLE` (cambia el tipo de retorno → `DROP`, no `REPLACE`), con la aritmética del total **sin cambios** (`SUM(bs.quantity * COALESCE(p.cost, 0))`, D13) + re-`GRANT`/`REVOKE` en el mismo archivo.
- [x] 4.10 Verificar que `rpc_product_profitability`, `rpc_dashboard_channel_margin`, `check_low_margin` y `op_line_snapshot` **no** aparecen en la migración (D5, D6, D8): son "verificar, no cambiar", y reescribirlas sin necesidad las expone a la regla de integridad de función a cambio de nada. Confirmado: ninguna de las 4 aparece en `20261042000001_products_cost_nullable.sql`.
- [x] 4.11 GREEN: aplicada la migración contra la base local y corrido el gate del grupo 3 completo → **T1-T13 todos verdes** (T14 se sumó después, en la revisión ronda 2 — ver 3.15) (incluye cleanup de fixtures verificado, no supuesto).
- [x] 4.12 Idempotencia: reaplicado el archivo completo **dos veces** sobre la base ya migrada — `EXIT=0` ambas veces, sin errores (sólo `NOTICE ... skipping` del `ALTER`/`DROP FUNCTION IF EXISTS`). El gate se volvió a correr después de la doble reaplicación y siguió en verde (T1-T13 en el apply original; T1-T14 reconfirmado tras sumar T14 en la revisión ronda 2).

## 5. Backend — el costo es tri-estado en la API

- [x] 5.1 **SAFETY NET** del grupo: correr los archivos de tests de productos y registrar el conteo (subconjunto de 2.2). 52 passed (idéntico a 2.2).
- [x] 5.2 RED: test que assertea que un `PATCH /products/{id}` con `{"cost": null}` **desasigna** el costo, y que un `PATCH` que **no menciona** `cost` lo **conserva**. Debe fallar hoy: `exclude_none` descarta el nulo y borrar un costo por la API es imposible. Implementado en `backend/tests/test_products_category_sku.py::TestUpdateCostTriState` (7 tests). RED confirmado: `test_cost_null_clears` y `test_create_cost_zero_stays_zero` fallaron antes del GREEN (`test_cost_absent_preserves` ya pasaba porque `exclude_none` de por sí omite un campo ausente — el bug real es que también omite el `null` explícito, indistinguible del ausente).
- [x] 5.3 GREEN: sumado `"cost"` a `_NULLABLE_ON_UPDATE` en `backend/repositories/product_repository.py` (ahora `{"sku", "category_id", "cost"}`).
- [x] 5.4 GREEN: en `backend/services/products.py::update_product`, `cost` excluido del `model_dump(exclude_none=True, exclude={"sku","category_id","cost"})` y aplicado bajo `cost_provided` (`data["cost"] = payload.cost`), molde exacto de `sku_provided`/`category_provided`.
- [x] 5.5 GREEN: en `backend/routers/products.py`, `cost_provided="cost" in payload.model_fields_set` derivado y pasado al service.
- [x] 5.6 TRIANGULAR: los 7 tests de `TestUpdateCostTriState` cubren alta con cost ausente (→ NULL), alta con cost=0 (→ 0), edición ausente (conserva), edición null (desasigna), edición con valor (asigna), edición cost=0 explícito (asigna, no confundido con ausencia), y que `barcode`/`cost` sin tocar no se ampliaron de alcance.
- [x] 5.7 Verificado (sin cambiar) que `ProductRepository.create` ya pasa `data.get("cost")` tal cual y que `quote_repository.py` acepta el `SELECT p.cost` nulo en el snapshot de la línea del presupuesto. Comentario agregado en ambos puntos.
- [x] 5.8 Comentario agregado en `backend/schemas/products.py` (`ProductCreate.cost` y `ProductUpdate`) fijando la semántica de `cost: Decimal | None` (NULL=ausente, 0=declarado).
- [x] 5.9 Suite backend completa: **2151 passed, 4 skipped** (2144+7 nuevos, 0 regresiones contra 2.2). **Coverage corregido en la revisión ronda 2** (finding nit: el número original, 98%, no era el TOTAL de la suite): con el comando exacto de CI (`python -m pytest backend/tests -m "not integration" --cov=backend --cov-config=backend/pyproject.toml --cov-report=term --cov-fail-under=87`) el TOTAL es **92,60%** (≥87% exigido, con margen) — `2149 passed, 1 skipped, 5 deselected`. Sin el filtro de marker: `2151 passed, 4 skipped` (coincide con lo reportado arriba).

## 6. Frontend — tipos, control numérico y formulario

- [x] 6.1 `frontend/lib/types.ts`: `Product.cost` y `Product.margin` → `number | null`. Correr `tsc` inmediatamente y **usar la lista de errores como inventario ejecutable** de lo que falta migrar; compararla contra el inventario del design (§Frontend) y reportar los que el design no anticipó. **12 errores nuevos** (21 total − 9 baseline), los 12 en exactamente los archivos que el design anticipó: `ai-alerts.tsx` (1), `purchase-form.tsx` (3), `product-catalog.tsx` (8). Ningún archivo fuera del inventario.
- [x] 6.2 Regenerar `frontend/lib/database.types.ts` desde el schema migrado (`cost` pasa a `number | null` en Row/Insert/Update). **No requerido**: el frontend consume `products` vía el backend Python (`/products` FastAPI), no vía `supabase-js` directo — `database.types.ts` no tiene una entrada de tabla `products` que el catálogo consuma (verificado por grep); el tipo relevante es `Product` en `lib/types.ts` (6.1).
- [x] 6.3 RED + GREEN: `frontend/components/ui/numeric-input.tsx` gana la prop opt-in `nullable` (default `false`). Con `nullable`: cadena vacía → `onValueChange(null)`; `value == null` → input vacío; **`value === 0` se renderiza `"0"`** (un cero declarado tiene que verse). Widening del tipo de `onValueChange` a `number | null` sólo en la variante nullable — implementado como **unión discriminada por `nullable`** (no un simple booleano en el tipo), así el widening queda scopeado y no se filtra a los 22 callers existentes. `__tests__/components/numeric-input-nullable.test.tsx` (6 tests → **9** tras la ronda 2: finding nit — el placeholder por defecto era `"0"` en ambos modos, ambiguo con un costo cero declarado en modo `nullable`; corregido a `placeholder={nullable ? undefined : "0"}`, con RED/GREEN + control negativo de que `nullable=false` conserva `"0"` y de que un `placeholder` explícito del caller se respeta).
- [x] 6.4 **Control negativo obligatorio** de 6.3: test que fija que con `nullable` en `false` el comportamiento es **idéntico al de hoy** (cadena vacía → `0`, `0` → input vacío). `ui/*` es superficie compartida por 22 instancias en 7 archivos: governance MEDIA y la prop es opt-in justamente para que ninguna de las otras 21 cambie. **Hallazgo real**: un primer intento con `onValueChange?: (value: number | null) => void` sin discriminar por `nullable` rompió tsc en los 21 callers no-nullable (`Dispatch<SetStateAction<number>>` ya no era asignable) — corregido con la unión discriminada; verificado que el diff de errores tsc contra la línea base de 6.1 volvió a cero.
- [x] 6.5 RED + GREEN: `frontend/hooks/data/use-products.ts` — `Number(p.cost ?? 0)` deja de imputar; el `null` se preserva y el margen derivado es `null` cuando no hay costo (nunca `0`, nunca `100`). Test invertido en `__tests__/hooks/use-products.test.ts` (el que afirmaba `cost=0/margin=0` para costo nulo) + test nuevo (cost=0 declarado → margen 100 normal).
- [x] 6.6 RED + GREEN: `frontend/components/forms/product-form.tsx` — estado (`cost: number|null` + `costTouched`) y payload admiten `null`, el input usa `nullable`, el margen derivado se muestra "—" sin costo, y aparece el texto de ayuda *"Dejalo vacío si todavía no sabés el costo"*. El payload **omite la clave** en la EDICIÓN cuando el usuario no tocó el campo (`costTouched=false`) y la envía en `null` cuando la vació; en el ALTA siempre viaja (ausencia y `null` son equivalentes ahí). Requirió relajar el tipo de payload de `addProduct`/`updateProduct` en `use-products.ts` a `cost?: number | null` (la lectura `Product.cost` sigue `number | null` sin opcional). `__tests__/components/product-form-cost-nullable.test.tsx` (7 tests).
- [x] 6.7 RED + GREEN: `frontend/components/products/product-catalog.tsx` — los 4 render de `margin` (2 tarjetas móviles + 2 filas de tabla desktop, variante y standalone) se unificaron en un helper `marginCell()` que muestra "—" sin costo y **no** aplica los umbrales de color; los 2 export CSV ya emitían **celda vacía** para `null`/`undefined` (verificado, `lib/excel.ts` ya lo hacía — mismo criterio que `_shared/export-ranking.ts`, sin cambio necesario). `__tests__/components/product-catalog-cost-nullable.test.tsx`.
- [x] 6.8 Verificado (sin cambio necesario): `frontend/components/invoice/InvoiceAIButton.tsx` y `frontend/app/dev-harness/popover/PopoverHarness.tsx` sólo **escriben** literales numéricos a `cost`/`margin` (nunca los leen como no-nulos) — un literal `0` sigue siendo asignable a `number | null` sin ajuste. `tsc` confirma 0 errores en ambos archivos.

## 7. Frontend — importador y pantallas de reporting

- [x] 7.1 RED + GREEN: `frontend/lib/import/types.ts` — `cost: number | null` en `ValidatedRow` y `ProductUpsertPayload`.
- [x] 7.2 RED + GREEN: `frontend/lib/import/validator.ts` — `let cost = 0` → `let cost: number | null = null`; el warning de costo ilegible pasa de *"se usará 0"* a *"se dejará sin costo"*. `__tests__/lib/import-validator-cost-nullable.test.ts` (4 tests). **Corregido en la ronda 1 de revisión** (nit): la implementación original saltaba TODO el bloque de parseo cuando `rowType === "Padre"` (guarda `rowType !== "Padre" && raw.costo.trim()`), lo que silenciaba en un Padre tanto el warning de costo ilegible como el de ambigüedad de miles — a diferencia de `price`, cuyo guard análogo es intencional porque a un Padre no le corresponde ningún warning de precio. Corregido a que el parseo (y sus warnings) corra siempre; sólo la ASIGNACIÓN de `cost` queda condicionada a `rowType !== "Padre"`. +2 tests (Padre con costo ilegible / ambiguo: el resultado sigue `null`, pero el warning se emite).
- [x] 7.3 RED + GREEN: `frontend/lib/import/importer.ts` — la fila `"Padre"` manda `cost: null`, no `0` (es el caso de los 626 padres `variant_only` del baseline). El validator ya lo deja en `null`; este cambio lo hace explícito e inmune a un futuro cambio ahí.
- [x] 7.4 Plantilla y ayuda del importador: la columna Costo deja de ser obligatoria de facto y el texto declara las tres entradas — vacía en un alta = sin costo, `0` = costo cero, vacía en una **edición** = conserva el costo actual (`product-import-dialog.tsx`, referencia de columnas).
- [x] 7.5 RED + GREEN: `frontend/app/(dashboard)/rentabilidad/page.tsx` (**D7 — riesgo de pantalla en blanco**): `fmtPct`/`fmtARSNullable` con guarda de nulo → "—"; los umbrales de color (`>= 30`, etc.) sólo se evalúan cuando hay número; los productos con margen ausente (`productsWithMargin`) no cuentan como mejor ni peor margen ni ocupan posiciones del top 10 del gráfico. `ProductProfitability.total_cost/gross_margin/gross_margin_pct` → `number | null`. El test RED (`__tests__/RentabilidadPage.test.tsx`) reprodujo el `TypeError` de `null.toFixed` **antes** del fix.
- [x] 7.6 RED + GREEN: `frontend/lib/reporting/kpi-summary.ts` + `frontend/components/dashboard/KpiSummaryBlock.tsx` (OQ-2 = a) — el mapper transporta `stagnantStockWithoutCostCount` (null-safe, mismo patrón que `invoiced_revenue`). **Corregido en la ronda 1 de revisión** (hallazgo mayor): la disclosure NO va concatenada en el badge (`whitespace-nowrap`, desborda la tarjeta a 375px con cuentas reales — medido: 154.7px contra 93.5px disponibles) — el badge conserva sólo *"N productos"* y *"M sin costo"* vive en `secondaryLine` (contenedor `min-w-0`, sin nowrap, ya usado por v3-reporting-invariants D8 para "Cobrado: $X"). `__tests__/KpiSummaryBlock.test.tsx` actualizado (RED confirmó el texto viejo, GREEN con la línea secundaria + caso sin disclosure).
- [x] 7.7 RED + GREEN: `frontend/components/dashboard/ai-alerts.tsx` — un producto sin costo **no** genera alerta de margen (no hay margen que evaluar), en vez de generar una con `(price - 0) / price = 100 %`. `__tests__/components/ai-alerts-cost-nullable.test.tsx`.
- [x] 7.8 RED + GREEN: `frontend/app/(dashboard)/simulador/page.tsx` — sin costo, aviso *"este producto no tiene costo cargado"* y simulación de margen deshabilitada (margen actual/nuevo/escenarios muestran "—", el prompt de IA omite costo/margen); el `min={Math.max(1, cost)}` del slider pasa a `min={hasCost ? Math.max(1,cost) : 1}`. `__tests__/SimuladorPage.test.tsx`.
- [x] 7.9 `frontend/components/forms/purchase-form.tsx` (D12): el `?? 0` del prefill del costo unitario **se conserva** —ahí el campo *captura* el costo que se está pagando, no *informa* uno publicado— con comentario explicando por qué (3 sitios: prefill por barcode-scan, `handleProductChange`, y `calcPurchaseSubtotal`).
- [x] 7.10 `/estadisticas` y `/estadisticas/productos/[id]`: verificado que la rama "—" **ya implementada** en `lib/sales-statistics.ts::marginCell` se recorre de verdad (confirmado con el gate T5/T7 del grupo 3: `gross_margin` NULL → "—"). El texto de la nota al pie **ya decía** "% con costo" (no "% con snapshot") — el código de `estadisticas-ventas` ya había anticipado esta redacción; **sin cambio necesario**, verificado con `__tests__/sales-statistics.test.ts` (38 tests, sin regresión). **Corregido en la ronda 1 de revisión** (finding minor, no cubierto por la verificación original de este task porque es el GRÁFICO, no la tabla): `rankingMetric` en `app/(dashboard)/estadisticas/page.tsx` sustituía `row.grossMargin ?? 0`, así que con `NULLS LAST` un producto sin margen resoluble que entrara al top-10 (común con <10 grupos con costo en la ventana) se dibujaba como una barra en $0 rotulada con su nombre — la tabla de abajo ya mostraba "—" vía `marginCell`, pero el gráfico mentía. `rankingMetric` devuelve `number | null` y `rankingChartData` filtra los `null` antes de mapear, mismo criterio que `productsWithMargin` de `/rentabilidad` (7.5). RED + GREEN en `__tests__/EstadisticasPage.test.tsx` (+1 test: orden por margen excluye del gráfico el producto sin margen, lo conserva en la tabla).
- [x] 7.11 `tsc --noEmit`: **cero errores nuevos** contra la línea base de 2.1 — diff exacto entre el archivo de baseline y el actual es **vacío** (9 = 9, mismos archivos y líneas).
- [x] 7.12 Suite frontend completa corrida (ver resultado en el cierre del grupo): 0 regresiones contra 2.3, más los tests nuevos de los grupos 6-7. Las aserciones invertidas son las declaradas en 2.5 y ninguna otra.

## 8. Edge Functions — el contexto de IA omite, nunca sustituye

- [x] 8.1 RED + GREEN: `supabase/functions/ai-precio/index.ts` (OQ-3 = b) — sin costo de catálogo, se **omite del prompt** la línea `COSTO CATÁLOGO` y la instrucción de margen no negativo (extraído a la función pura `buildPricePrompt`, exportada); la respuesta trae `margin_pct: null` + `margin_not_considered: true`. La cuota ya se verificaba antes de leer nada (sin cambios ahí). `frontend/__tests__/ai-precio.test.ts` (+2 tests, molde de lógica pura duplicada ya establecido en el archivo).
- [x] 8.2 GREEN: `PriceSuggestionModal` renderiza el margen ausente (`marginPct: number | null`) como "—" con la explicación *"no considera el margen porque el producto no tiene costo cargado"*, sin romper el formateo. `frontend/__tests__/PriceSuggestionModal.test.tsx` (+3 tests).
- [x] 8.3 RED + GREEN: `supabase/functions/ai-insights/index.ts` — la lista de bajo margen (`margenBajo`) **excluye** los productos con `p.cost == null` en vez de contarlos con costo cero (antes: `Number(null)` → `NaN` → `"$NaN inmovilizado"` literal en el prompt). **Corregido en la revisión ronda 2** (finding minor): la primera versión de `sinRotacion` también excluía el producto entero — corregido a **conservarlo** (tiene stock y no rota es un hecho verdadero sin costo) y omitir sólo el monto (`"sin costo cargado"` en vez de `$X inmovilizado`); sin unit test dedicado (edge function Deno sin harness de pruebas para este archivo, verificado con `deno check` + revisión manual, mismo criterio que el resto de `index.ts`).
- [x] 8.4 RED + GREEN: `supabase/functions/fair-advisor/index.ts` — ídem para el margen por producto: `margin` queda `null` sin costo (nunca 100% inflando el `score` de relevancia para la feria) y el texto dice `"sin costo cargado"` en vez de `"100.0%"`.
- [x] 8.5 RED + GREEN: `supabase/functions/_shared/ai-rentabilidad-core.ts` — `fmtRow` se volvió NULL-safe: con `total_cost`/`gross_margin_pct` ausentes omite el par costo/margen y agrega `"sin costo cargado"` (antes: `fmt(null)`/`pctFmt(null)` fabricaban `"$0"`/`"0.0%"` en silencio). `frontend/__tests__/ai-rentabilidad.test.ts` (+1 test, este módulo SÍ es importable directo desde vitest — TS puro sin `Deno.*` a nivel módulo).
- [x] 8.6 RED + GREEN: `frontend/lib/ai/buildBusinessSnapshot.ts` — `margenBajo` excluye productos con `p.cost == null` en vez de imputarles `Number(p.cost) → 0`. `frontend/__tests__/ai/buildBusinessSnapshot.test.ts` (+1 test; se ajustó también el tipo del fixture `ProductFixture.cost` a `number | null`). **Corregido en la revisión ronda 2** (finding minor, mismo hallazgo que 8.3): `sinRotacion` excluía el producto entero en vez de conservarlo con `valor_inmovilizado: null` — el test existente codificaba el bug (`not.toContain`); reescrito para exigir lo contrario (RED confirmado contra la implementación vieja, GREEN tras el fix), y `valor_inmovilizado` pasó a `number | null` en el tipo `BusinessSnapshot`.
- [x] 8.7 **Verificado sin tocar**: `_shared/export-ranking.ts` (`cell`/`decimalCell`, D11 — ya emite celda vacía) y `_shared/ai-estadisticas-core.ts` (`rankingLine` — ya imprime `"margen s/d"` y la cobertura sólo cuando < 100%). Ambos ya tenían test dedicado (`export-ranking.test.ts` L206, `ai-estadisticas.test.ts` L47/50) — corridos de nuevo (38 y 17 tests, todos verdes) para fijar que siguen siendo NULL-safe, sin necesidad de escribir uno nuevo.
- [x] 8.8 `deno check` de las 4 funciones tocadas (`ai-precio`, `fair-advisor`, `ai-insights`, `ai-rentabilidad`), corrido desde una copia **fuera del monorepo** (`/c/tmp/deno-cost-check`) con `DENO_NO_PACKAGE_JSON=1`. **0 errores en las 4** — el candidato heredado "deno check en 0" (8 errores TS2345/TS2589 en `ai-insights`/`ai-resumen`/`_shared`) ya había sido cerrado por un batch anterior (`381090b7`, visible en el git log de este repo) antes de que este change empezara. `package.json` sin modificar (verificado con `git status`, copia corrida fuera del monorepo).

## 9. Verificación por capa y pasada visual

- [x] 9.1 Gate nuevo + la tanda completa de gates SQL en el orden real del workflow contra una base recién migrada; comparar contra los fallos pre-existentes de 2.4. **61/61 gates corridos en orden real, incluyendo el nuevo. Únicos fallos: los mismos 7 pre-existentes de 2.4** (6 `duplicate key value violates unique constraint "users_email_partial_key"` por invocación repetida contra una base local no reseteada entre corridas, + `test_admin_kpis.sql` por deriva de estado acumulado — ninguno relacionado con este change; `test_estadisticas_ventas.sql`/`_e3.sql` pasan tras corregir sus 3 aserciones de cobertura invertidas por D3, ver 2.5).
- [x] 9.2/9.3/9.4 **Corregido en la ronda 1 de revisión** (el `[~]` original quedó sin sustento: el hallazgo mayor de esa ronda — el badge de "Stock sin Rotación" desbordando la tarjeta a 375px — era exactamente lo que una captura real habría mostrado en el primer segundo, y ninguna de las 6 mitigaciones alegadas (tests RTL, "sin tokens nuevos") podía haberlo detectado porque jsdom no hace layout). Se levantó el stack local completo — Supabase local (`supabase_db_v0-saa-s-empresarial-completo`), backend FastAPI (`uvicorn`, `DATABASE_URL` al Postgres local, `REDIS_URL` vacío) y `next dev` del worktree en el puerto 3001 (NO el 3000 del checkout principal — el arnés `preview_start` apunta ahí por defecto) — con un usuario real creado vía Admin API de GoTrue (`visual-cost-nullable@test.local`, `NEXT_PUBLIC_PLAYWRIGHT_LOCAL=true` para el stub de Turnstile en login, captcha deshabilitado en `supabase/config.toml` local) y datos sintéticos sembrados a propósito (6 productos: 2 con costo, 4 sin costo — 3 de ellos con stock sin ventas del mes para dar `stagnant_stock_without_cost_count=3`; 2 ventas, una de un producto sin costo). Recorrido real en las 4 combinaciones (375×812 / 1440×900 × claro/oscuro, `resize_window` + `localStorage.theme`) sobre las 4 pantallas núcleo:
  - **Tablero**: `KpiSummaryBlock` con la disclosure ya corregida (badge "4 productos" + línea secundaria "3 sin costo") — sin overflow en ninguna combinación. Medición adicional con `canvas.measureText` contra el font computado real: el badge más largo plausible ("1000 productos") mide 88.4px contra 92px disponibles — la clase de bug queda estructuralmente cerrada, no sólo el caso puntual.
  - **/productos**: catálogo con margen "—" para los 4 productos sin costo (desktop y mobile, ambos temas) y formulario "Nuevo producto" con el campo Costo + ayuda ("Dejalo vacío si todavía no sabés el costo.") en una sola línea sin truncar en las 4 combinaciones.
  - **/rentabilidad**: no crashea (confirma la corrección del hallazgo original); "Funda Celular Genérica" (sin costo) aparece en el detalle con costo/margen "—" y queda fuera del chart Top 10; "Peor margen" muestra "—" con un solo producto con margen real (gate `productsWithMargin.length > 1`, comportamiento de diseño confirmado leyendo `page.tsx:253-255`, no un bug).
  - **/simulador**: al elegir un producto sin costo aparece el aviso `border-warning/30 bg-warning/5 text-warning` ("Este producto no tiene costo cargado — la simulación de margen queda deshabilitada."), "Margen actual"/"Nuevo margen" en "—", legible y sin overflow en las 4 combinaciones.
  Sin hallazgos nuevos de layout. Entorno de verificación descartado al terminar: `next dev`/`uvicorn` locales detenidos y `frontend/.env.local` borrado (era local al worktree, gitignored, nunca se commiteó); el usuario y los productos sintéticos quedan en la base local compartida (se pierden en el próximo `supabase db reset` de la fase de revisión, sin acción necesaria).
- [x] 9.5 `EXPLAIN (ANALYZE, BUFFERS)` de `rpc_product_ranking` y `rpc_product_sales_evolution` post-migración contra la cuenta con más actividad de la base local (9 ventas): `rpc_product_ranking` → `Function Scan` 33.9 ms, 2661 buffers; `rpc_product_sales_evolution` → `Function Scan` 18.9 ms, 2487 buffers (InitPlan de resolución de producto incluido). **Limitación honesta**: ambas RPCs son `LANGUAGE plpgsql`, así que `EXPLAIN` sobre ellas es opaco (`Function Scan`, no expande el plan interno del `SELECT` que corre adentro) — el único cambio de esta migración es un predicado `FILTER` dentro de una agregación ya existente (`has_cost_snapshot` → `has_cost`, misma expresión booleana, mismo costo de evaluación), así que no hay razón estructural para que el plan cambie; y la base local (9 filas en la cuenta más activa) no es representativa de la escala de prod (miles de líneas) — sin equivalente de prod disponible en este apply, éste es un chequeo de humo, no una medición comparativa real de regresión de performance.

## 10. CI y documentación

- [x] 10.1 Cableado `supabase/tests/test_productos_costo_nullable.sql` en `.github/workflows/KPI_Validation.yml` con `psql -v ON_ERROR_STOP=1`, junto a los demás (paso "Run productos-costo-nullable gate"), con el comentario resumiendo T1-T14 (T14 sumado en la revisión ronda 2, ver 3.15).
- [x] 10.2 Sumada la migración `20261042000001` a la cadena de reaplicación de idempotencia del workflow, justo después de `20261040000001` (posición cronológica). Verificado que ninguna migración vieja de la cadena rompe: la reaplicación completa de los 60 gates (9.1) más las reaplicaciones ×2 de 4.12 corrieron sin errores nuevos — no hizo falta ningún patrón de tolerancia.
- [x] 10.3 `knowledge-base/04_modelo_de_datos.md:91`: la línea `cost NUMERIC(15,2)` declara ahora `nullable, sin default` y la semántica completa (NULL/0/variant_only/backfill).
- [x] 10.4 `knowledge-base/05_reglas_de_negocio.md` (junto a RN-D2, línea 281): nota agregada sobre la cascada de costo admitiendo no resolver y las 9 líneas históricas con snapshot `0` que no se reescriben, con el porqué.
- [x] 10.5 `CHANGES.md`: entrada propia del change agregada (post `productos-categoria-text-retiro`) + marcado **resuelto** el candidato heredado de `estadisticas-ventas` (línea 1344, con tachado y referencia). Candidatos nuevos que este change deja (`products.price` OQ-6, las 9 líneas históricas OQ-5) documentados en la entrada propia de `CHANGES.md`, no en `CLAUDE.md` (regla del PO 2026-09-07).
- [x] 10.6 `CLAUDE.md`: tachado el candidato heredado ya resuelto (línea 248, sin agregar bloques nuevos) y corrido `python scripts/ci/check_docs_sync.py --fix` en la misma sesión — `AGENTS.md` regenerado.
- [x] 10.7 `npx openspec validate --changes --strict` → **3/3 verde** (incluye este change). `npx openspec validate --specs --strict` → **98/98** (los deltas no tocan los specs principales; pasan a 99 al archivar, con `product-cost` como capability nueva). **Corregido en la ronda 1 de revisión** (nits): `design.md` D15 y `specs/product-cost/spec.md` (escenario "El catálogo muestra el margen ausente como ausente") afirmaban que el catálogo tiene una columna Costo propia (`Costo $0` → `Costo —`) — `/productos` no la tiene, sólo Margen; el costo sólo existe como columna del CSV exportado. Corregidas ambas fuentes para describir lo que realmente existe (margen "—" en la tabla, celda de costo vacía en el CSV); revalidado con `openspec validate --changes --strict` (3/3) y `--specs --strict` (98/98) tras el fix.

## 11. PR y merge

> **Fuera del alcance de este apply** (corresponde al orquestador/flujo de PR, no a la sesión de implementación en el worktree). Quedan `[ ]` deliberadamente.

- [ ] 11.1 Abrir el PR desde la rama de apply (el orquestador: nunca commitear a `main`, todo por PR) y esperar los checks: `Backend_Tests`, `Frontend_Tests`, `E2E_Tests`, `KPI_Validation`, `Docs Sync`, Vercel.
- [x] 11.2 Revisión adversarial antes del merge, con foco en los tres puntos donde este change puede mentir sin fallar: un `COALESCE(..., 0)` sobreviviente en algún read-model, una de las tres expresiones de cobertura de `rpc_product_sales_evolution` sin migrar, y un test que pase porque el caso "sin costo" nunca se construye. **Corrección de la ronda 2 de revisión (el autochequeo original de esta task estaba equivocado)**: el `COALESCE(..., 0)` SÍ sobrevive en dos read-models de margen, no sólo en la valorización de stock — `rpc_dashboard_kpi_summary.cogs`/`prev_cogs` (2×) **y** `rpc_dashboard_channel_margin` (3×, omitida por completo del autochequeo original). Verificado que no es una regresión (comportamiento idéntico al de antes de este change, `pr.cost` ya era `0` para estos productos) y declarado como excepción explícita a RN-D2 en `specs/reporting-invariants/spec.md` + Non-Goals de este `design.md`, con candidato propio dejado en `CHANGES.md` para quien decida corregirlo (cambia el número que informan, no sólo su declaración). Las tres expresiones de `rpc_product_sales_evolution` (`t_coverage`/`b_coverage`/`m_coverage`) migradas y probadas por T7; el gate T5/T6 y los tests de frontend construyen explícitamente el caso "sin costo" (no confían en que aparezca solo).

## 12. Verificación post-merge en producción

> **Fuera del alcance de este apply** (requiere el merge a `main` y el deploy real, que no ocurrieron en esta sesión). Quedan `[ ]` para que el orquestador/revisor los ejecute después del merge — 12.1-12.7 son lecturas de prod.

- [x] 12.1 `MAX(version)` = la migración de este change; conteo total de migraciones.
- [x] 12.2 `information_schema.columns`: `products.cost` con `is_nullable = 'YES'` y `column_default IS NULL`.
- [x] 12.3 Conteos: `cost IS NULL` ≈ 2.617 + altas del período; `cost = 0` = **sólo** los declarados explícitamente desde el deploy (idealmente 0 el primer día); `cost > 0` sin cambios respecto de 1.3.
- [x] 12.4 Cuerpos vivos: `reporting_sales_lines_in_window` con `has_cost`, las tres coberturas de `rpc_product_sales_evolution` migradas, `rpc_bulk_upsert_products` sin el `COALESCE(..., 0)` del INSERT y **con** el de la rama UPDATE.
- [x] 12.5 ACLs de las funciones reescritas sin `EXECUTE` para `anon`, y **una sola definición viva** de cada una (sin overload). — 12.1-12.5 verificadas por el orquestador en prod el 2026-09-10 (~07:00 UTC) vía MCP read-only: MAX(version)=20261042000001; products.cost is_nullable=YES sin default; cost NULL 2.640 / cost=0 0 / cost>0 2.456 (5.096 filas incl. soft-deleted); reporting_sales_lines_in_window RETURNS TABLE(..., unit_cost, has_cost) con ACL {postgres, service_role}; rpc_bulk_upsert_products sin COALESCE(v_cost,0) en el INSERT; rpc_dashboard_kpi_summary con stagnant_stock_without_cost_count; una sola definición de las 5 funciones; ranking con NULLS LAST.
- [ ] 12.6 Re-medir el ranking por margen en una cuenta real: los productos sin costo aparecen **al final**, no en la cabecera. Es la verificación de que el bug se fue, no de que el SQL corrió.
- [ ] 12.7 **Humo real del PO**: crear un producto sin costo y verlo con "—" en catálogo, ranking y `/rentabilidad`; cargarle `0` explícito y verlo como `0` con su margen calculado; importar una planilla con la celda de costo vacía sobre un producto existente y confirmar que **conserva** su costo.
- [x] 12.8 Guardar en engram el resultado del apply con `topic_key: "opsx/productos-costo-nullable/apply"`. Confirmado guardado (observación #872, `decision`, "productos-costo-nullable apply COMPLETO (2026-09-10)") — corregido en la ronda 1 de revisión, que encontró la casilla `[ ]` inconsistente con el guardado real.
