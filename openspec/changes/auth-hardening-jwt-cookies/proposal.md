## Why

La auditoría de JWT y cookies del **2026-09-12 → 2026-09-14** (52 agentes, 82 afirmaciones, 75 confirmadas, 22 riesgos únicos, F1–F6 medidos directamente contra producción) dejó **13 hallazgos abiertos**. El más estructural: **la sesión de Supabase vive en cookies `HttpOnly=false`** — el valor es el JSON base64url de la sesión **completa, access token + refresh token** (`frontend/node_modules/@supabase/ssr/dist/main/utils/constants.js:4-11`, sin clave `secure` y con `httpOnly:false`), legible por cualquier `document.cookie`. Un XSS en cualquier pantalla del dashboard no roba "una sesión de 1 hora": roba el **refresh token**, y con él una sesión renovable durante 400 días fuera del navegador de la víctima.

No es un olvido: es estructural para la arquitectura actual. `createBrowserClient` implementa su storage sobre `document.cookie` (`cookies.js:94`, `:101-105`) y de ahí sale el Bearer de **todas** las llamadas a FastAPI (`frontend/lib/api/python-client.ts:17`). Cambiarlo exige mover la sesión al servidor, no pasar un flag.

**Por qué ahora**: el PO lo ordenó textualmente el **2026-09-16** — *"arreglá todos los hallazgos de la auditoría, quiero que las cookies sean httpOnly y que sólo se puedan usar dentro de la página para que no se la puedan robar, y que JWT y cookies queden de forma profesional con los últimos estándares de la industria"*. Y porque los otros doce hallazgos no son independientes de éste: **12 de los 29 árboles de ruta del dashboard no tienen gate de sesión** (F1: `GET /caja`, `/cobranzas`, `/banco`, `/estadisticas`, `/planes`, `/facturacion` devuelven **200 a un anónimo en prod**, mientras `/ventas` da 307), el fallback de esas pantallas redirige a `/login` — **ruta que no existe** (F2, aterriza en un 404), el CORS del backend **refleja cualquier origen** con `credentials: true` (F5, medido con `Origin: https://evil.example`), y **la única rama de verificación de JWT que corre en producción (JWKS/ES256) no tiene ni un test** (§10: grep de `jwks|ES256|RS256|PyJWKClient` sobre `backend/tests/` → 0 hits; los 17 bloques de patch corren por HS256 con `"dev-secret"`).

Cerrarlos por separado sería peor: el token handler httpOnly sin CSP con nonce deja el XSS operando desde adentro; las rutas protegidas sin el `safeNext()` compartido dejan el open redirect del callback; y el hardening del backend sin el fail-fast de configuración deja viva una rama HS256 alcanzable por una variable de entorno mal puesta.

## What Changes

El change se ejecuta en **tres partes, cada una con su propio apply, PR, revisión adversarial y verificación en prod**. El orden A → B → C no es cosmético: A y B son independientes del transporte de la sesión, C lo reemplaza — y C sólo es seguro de mergear cuando las rutas ya están cubiertas (B) y el backend ya rechaza lo que debe rechazar (A).

### Parte A — Backend, base de datos, Edge Function y documentación

- **Verificación del JWT endurecida** (`backend/core/auth.py`): se valida **emisor** (`{supabase_url}/auth/v1`) y **audiencia** (`authenticated`, `verify_aud` encendido), `require = ["exp", "sub"]` (hoy un token válidamente firmado **sin `exp`** se acepta) y `leeway` de 30 s (hoy 0 con `verify_iat` activo: un reloj atrasado rechaza tokens recién emitidos). Un `sub` ausente pasa de **500** (KeyError por el catch-all de `backend/main.py:132-140`) a **401**. Un fallo del endpoint JWKS deja de ser indistinguible de un token forjado: se loguea como warning propio, sin cambiar la respuesta.
- **BREAKING (configuración)**: la rama **HS256** deja de ser alcanzable por omisión. Pasa a exigir una palanca explícita `AUTH_ALLOW_HS256_FALLBACK` (default `False`); sin ella, un `SUPABASE_URL` ausente o no-`https` es un **error de arranque**, no un fallback silencioso al secreto por default `"dev-secret"` (`backend/core/config.py:6`). Fail-fast en el startup, nunca en el primer request.
- **Tests de la rama que corre en producción**: camino JWKS/ES256 ejercitado con una clave EC generada en el test y `PyJWKClient.get_signing_key_from_jwt` parcheado — cierra el hueco de cobertura de §10.
- **CORS acotado** (`backend/main.py:64-70`): allow-list por `allow_origin_regex` para `aliadata.com.ar` (con y sin `www`) y los previews de Vercel, más `localhost:3000` fuera de producción; **`allow_credentials=False`** (nada usa cookies contra el backend: cero lecturas de cookie en `backend/`); `"*"` prohibido cuando `app_env == "production"`. La reflexión manual de `backend/core/errors.py:78-85` sigue el mismo criterio. Valores por defecto seguros para que prod siga funcionando aunque `BACKEND_ALLOWED_ORIGIN` no se configure.
- **Re-chequeo del rol de tenant en la base para acciones de configuración**: cuando el conjunto permitido por el guard es un conjunto de **configuración** (`owner`/`admin`), `require_account_role` consulta `rpc_my_active_account_roles()` **aunque el claim esté presente**. El claim es un caché; para acciones sensibles la base es la autoridad. Cierra la ventana de staleness de un rol revocado o vencido (hoy el claim presente gana sobre la base, `backend/core/guards.py:63-71`).
- **BREAKING (superficie)**: se **retira** el endpoint `/ws/{room_id}`, `backend/core/ws_manager.py`, `backend/routers/ws.py` y `backend/tests/test_ws.py`. Autentica el handshake pero **no autoriza la sala** (`room_id` nunca se compara contra la cuenta del portador, `ws.py:54`, `:60`), recibe el JWT por **query string** (queda en logs) y valida una sola vez para toda la vida de la conexión. No tiene productor ni consumidor: cero `new WebSocket` en el frontend, DEC-16 lo declara fuera de producción y `openspec/specs/in-app-notifications/spec.md:86` prohíbe usarlo. Se cierra **antes** de que alguien cablee el primer productor.
- **Edge Function `invoice-ocr`**: `storage_path` se lee de la fila `invoice_documents` ya validada, no del body. Hoy se autoriza `document_id` y se descarga **otro objeto** con SERVICE_ROLE (`invoice-ocr/index.ts:104`, `:112-115`, `:132-135`) — confused deputy de una línea.
- **Migración `20261051000001`** (una sola): `rpc_accept_invitation` reescrita **desde su cuerpo vivo de prod** (`pg_get_functiondef`, md5 `02517dd9bcbbabd723a5bbb0f1c1a406`, 4853 chars, firma única `rpc_accept_invitation(text)`) sumando (a) **binding de email** — si la invitación tiene `email`, el que acepta debe ser esa identidad — y (b) `SELECT … FOR UPDATE` sobre la invitación (hoy el `status='accepted'` es la última sentencia, sin lock). Más `ALTER PUBLICATION supabase_realtime ADD TABLE public.fiscal_documents`, guardado idempotentemente: **verificado en prod el 2026-09-16, la publicación contiene sólo `notifications`**, así que la suscripción realtime de `FiscalDocumentBadge` (`frontend/components/fiscal/FiscalDocumentBadge.tsx:76-96`) **nunca recibió un solo evento** desde que se escribió.
- **Documentación reconciliada**: `openspec/specs/backend-auth/spec.md:11` (SHALL HS256 → JWKS ES256/RS256 con HS256 sólo bajo palanca), `knowledge-base/08_arquitectura_propuesta.md:238`/`:310`/`:132`, `CLAUDE.md`+`AGENTS.md` (nota de divergencia `python-jose` que ya no existe: ambos manifiestos declaran sólo `PyJWT[crypto]`; mención de `ws.py`/`ws_manager`), y los cinco `verify_jwt = true` de `supabase/config.toml` que el deploy contradice con `--no-verify-jwt` en cada push (`.github/workflows/deploy.yml:63`).
- **Sin superficie frontend** en esta parte (declarado).

### Parte B — Higiene de rutas y de cierre de sesión (sin cambiar dónde vive la sesión)

- **Cobertura de rutas invertida**: `PROTECTED_PREFIXES` (17 entradas, `frontend/lib/supabase/middleware.ts:57-61`) pasa a **allow-list de rutas públicas** + todo `app/(dashboard)` protegido por construcción, con un test de vitest que **lee el árbol del filesystem** y falla si aparece un directorio nuevo sin cobertura. Cierra F1 por causa raíz, no enumerando los 12 que faltan hoy.
- **F2**: los cuatro `redirect("/login")` (`planes/page.tsx:30`, `planes/success/page.tsx:27`, `facturacion/page.tsx:78`, `ventas/ordenes/[id]/page.tsx:60`) pasan a `/auth/login?next=…`, con un test que prohíbe el literal `"/login"`.
- **`safeNext()` compartido**: una sola validación de `next` consumida por el middleware (que hoy sí valida, `:186`) **y** por `/auth/callback` (que hoy concatena sin validar, `route.ts:9`, `:43` — open redirect latente).
- **Cookies rotadas en todo redirect**: las seis salidas por redirect del middleware construyen su propia respuesta y no copian las cookies de sesión recién escritas. Hoy es autocurativo por el `refresh_token_reuse_interval`; pasa a ser correcto por construcción.
- **`clearAuthUxCookies()` compartido** por `logout()`, `performIdleLogout()` y `closeAllSessions()`, borrando `auth:last-activity` **y** `tenant:active`. Cierra el bounce del primer re-login post-idle (hoy `performIdleLogout` borra sólo `tenant:active`, `idle-logout.ts:50`, y el middleware descarta las cookies `sb-*` recién emitidas, `:144-148`).
- **BREAKING (comportamiento)**: el logout ordinario y el de inactividad pasan a `signOut({ scope: 'local' })`. Hoy el `signOut()` pelado es **global** por default de la librería (`GoTrueClient.js:3150`) y cerrar sesión en el celular **desloguea la tablet del mostrador**. `closeAllSessions()` conserva `'global'`, que es su propósito.
- **La rama de idle del middleware revoca del lado del servidor** antes de borrar cookies: hoy borra cookies y la sesión **sigue viva en GoTrue** (cero `signOut` en `middleware.ts`).
- **Recuperación del 401**: un 401 del backend Python consulta el estado de sesión y, si no hay, navega a `/auth/login?reason=expired&next=…` en vez de pedirle al usuario que "recargue la página" (`python-client.ts:26-30`), consejo que en las 12 rutas de F1 no recuperaba nada.
- **Atributo `Secure`** en las cookies de sesión vía un único `lib/supabase/cookie-options.ts` compartido por los cuatro sitios que construyen cliente. En este paso las cookies siguen siendo legibles por JS; la parte C las cierra.
- **Comentarios falsos corregidos**: `frontend/lib/cookies.ts:5` ("tokens stay in Supabase httpOnly cookies" — hoy es falso) y `frontend/lib/api/python-client.ts:14` ("reads from local storage" — lee cookies), más `idle-logout.ts:25`/`:42` ("local scope" — hoy es global).

### Parte C — Token handler httpOnly + CSP con nonce

- **BREAKING (arquitectura)**: la sesión (access + refresh token) pasa a vivir **sólo** en cookies `HttpOnly`, `Secure` (prod), `SameSite=Lax`, `Path=/`, escritas **únicamente por el servidor**. El navegador **nunca** ve el refresh token.
- **Todas las operaciones de auth se mueven a Server Actions / Route Handlers**: `signInWithPassword` (+`captchaToken`), `signUp`, `signInWithOtp`, `resetPasswordForEmail`, `resend`, `updateUser`, `exchangeCodeForSession`, `signOut`. Las pantallas existentes (`/auth/login`, `/auth/register`, `/auth/forgot-password`, `/auth/reset-password`, `/auth/verify-email`, `/configuracion`) **conservan su UI** y se recablean al servidor.
- **Token handler**: el cliente de navegador pasa de `createBrowserClient` a `createClient(url, key, { accessToken: () => getAccessToken() })` de `@supabase/supabase-js` 2.104.1 (soportado: `dist/index.mjs:135-138`, `:383-407`, `:527`; REST, Storage, Functions y Realtime toman el token de ese callback). `getAccessToken()` mantiene un **access token efímero en memoria**, obtenido de un Route Handler que lee la cookie httpOnly, refresca si hace falta (reescribiendo las cookies rotadas) y devuelve `{ access_token, expires_at, user }` — **nunca** el refresh token.
- **`onAuthStateChange` se reemplaza por un bus de eventos de sesión de la app**, reutilizando el transporte cross-tab que ya existe (`frontend/lib/auth/idle-transport.ts`: BroadcastChannel con fallback a localStorage) — con `accessToken` configurado, cualquier acceso a `supabase.auth.*` **lanza** (`index.mjs:389`) y `_listenForAuthEvents` no se instala (`:407`).
- **CSP con nonce por request**: `script-src 'unsafe-inline' 'unsafe-eval'` (`middleware.ts:47`, con el comentario "tighten later with nonces" desde que se escribió) pasa a `'nonce-…' 'strict-dynamic'` + el host de Turnstile en producción; `'unsafe-eval'` sobrevive sólo fuera de producción. `style-src` conserva `'unsafe-inline'` (Tailwind/Radix y el `<style>` de `components/ui/chart.tsx:81`). El nonce se genera en el middleware, viaja por `x-nonce` y lo consume `next-themes` (acepta `nonce`).
- **Sin re-login forzado**: los nombres y el contenido de las cookies no cambian; el servidor simplemente las reescribe como httpOnly en el primer refresh. Las sesiones vivas siguen funcionando.
- **Riesgo residual declarado**: un XSS sigue pudiendo actuar como el usuario **mientras la página está abierta**, pero ya no puede exfiltrar el refresh token ni persistir más allá del TTL del access token. Eso es el estándar de la industria (patrón *token handler* / BFF), no una cura del XSS.

## Capabilities

### New Capabilities

- `session-cookies`: dónde vive la sesión del navegador y bajo qué garantías — cookies httpOnly escritas sólo por el servidor, operaciones de auth en el servidor, token handler con access token efímero en memoria, bus de eventos de sesión de la app, CSP con nonce, cobertura de rutas protegidas derivada del árbol del dashboard, validación compartida de `next`, y uniformidad del cierre de sesión.

### Modified Capabilities

- `backend-auth`: la verificación del JWT pasa a exigir emisor y audiencia, `exp`/`sub` obligatorios y tolerancia de reloj; la rama HS256 deja de ser el fallback por omisión y pasa a requerir una palanca explícita, con fail-fast de configuración en el arranque; se retira el requisito que declara el query param del WebSocket como transporte válido del token; y el guard de rol de tenant re-consulta la base para los conjuntos de configuración.
- `realtime-websocket`: se retiran todos los requisitos de comportamiento del canal WebSocket propio y la capability pasa a declarar, como invariante negativo, que ese canal **no existe** y que el tiempo real es Supabase Realtime.
- `python-backend`: la estructura del proyecto y la lista de routers registrados dejan de incluir el módulo WebSocket, y el requisito de CORS pasa de un origen único con credenciales a una allow-list por expresión regular sin credenciales, con `"*"` prohibido en producción.
- `account-membership-roles`: la aceptación de una invitación pasa a exigir que la identidad que acepta coincida con el email invitado cuando la invitación lo declara, y a tomar lock sobre la fila de la invitación.
- `afip-fiscal-document`: la tabla de comprobantes fiscales pasa a formar parte de la publicación de tiempo real, para que la transición `pending_cae → authorized`/`rejected` llegue a la UI sin recargar.
- `auth-captcha`: el requisito de CSP para Turnstile pasa a expresarse en términos compatibles con un `script-src` con nonce y `'strict-dynamic'`, en vez de apoyarse en `'unsafe-inline'`.
- `idle-session-server-enforcement`: el corte por inactividad del servidor pasa a revocar la sesión del lado del servidor antes de borrar las cookies, y su alcance deja de depender de una lista enumerada a mano.
- `idle-session-timeout`: el cierre por inactividad pasa a usar el mecanismo de cierre compartido —alcance local y borrado de **todas** las cookies de UX de sesión, incluida la de última actividad—, en vez de replicar parcialmente el logout.

## Impact

**Backend Python**: `backend/core/auth.py` (`_decode_supabase_jwt`, `get_current_user`), `backend/core/config.py` (palanca `auth_allow_hs256_fallback`, validator de arranque, CORS), `backend/core/guards.py` (`require_account_role`, docstring envejecido de `:81`), `backend/main.py` (CORS, registro de routers), `backend/core/errors.py` (reflexión CORS). **Se eliminan** `backend/routers/ws.py`, `backend/core/ws_manager.py` y `backend/tests/test_ws.py`. Tests nuevos: rama JWKS/ES256, `iss`/`aud`, `sub` ausente → 401, fail-fast de configuración, re-chequeo en base.

**Base de datos**: una migración (`20261051000001`) — reescritura de `rpc_accept_invitation` desde el cuerpo vivo, con `REVOKE`/`GRANT` re-emitidos tras el `DROP`/`CREATE`, gate de integridad de función según el patrón ya usado por el repo (`supabase/tests/test_operacion_party_guard.sql:521-561`), y `ALTER PUBLICATION` idempotente sobre `fiscal_documents`.

**Edge Functions**: `supabase/functions/invoice-ocr/index.ts`.

**Frontend**: `frontend/middleware.ts` y `frontend/lib/supabase/middleware.ts` (CSP con nonce, cobertura de rutas, rotación de cookies en redirects, revocación en la rama idle); `frontend/lib/supabase/client.ts`, `server.ts`, `cookie-options.ts` (nuevo), `frontend/app/auth/callback/route.ts`; `frontend/contexts/auth-context.tsx` (9 llamadas a `supabase.auth.*`); Server Actions nuevas bajo `frontend/app/auth/` y un Route Handler de token bajo `frontend/app/api/auth/`; los **43 archivos** que hoy usan `supabase.auth.*` desde el navegador y los **69** que importan `@/lib/supabase/client`; `frontend/lib/api/python-client.ts` y `frontend/lib/api/subscriptions-client.ts` (transporte duplicado); `frontend/lib/auth/idle-logout.ts`, `frontend/lib/cookies.ts`, `frontend/components/theme-provider.tsx` (nonce); los cuatro Server Components de F2.

**Superficie frontend** (regla del PO, 2026-08-02): **no hay pantalla nueva ni ruta nueva de usuario**. Las seis pantallas de auth y la de configuración conservan su UI y su navegación, recableadas a acciones de servidor; el único agregado sin UI es el Route Handler de token. Se exige igualmente verificación visual en **desktop + móvil** y **tema claro + oscuro** de las siete pantallas antes del merge de la Parte C, porque el recableado toca sus estados de carga y de error.

**Documentación**: `CLAUDE.md` + `AGENTS.md` (con `python scripts/ci/check_docs_sync.py --fix` en el mismo PR), `knowledge-base/02_descripcion_general.md`, `knowledge-base/08_arquitectura_propuesta.md`, `knowledge-base/09_decisiones_y_supuestos.md` (DEC-16), `supabase/config.toml`, `CHANGES.md`.

**Operación (PO, fuera del código)**: `Access token expiry` a 900 s en el Dashboard de Supabase una vez vivo el token handler; confirmación del toggle del hook; `BACKEND_ALLOWED_ORIGIN`, `SUPABASE_URL` y `SUPABASE_JWT_SECRET` en Render; Turnstile habilitado en el Dashboard si aún no lo está.

**Governance**: **CRÍTICO** (auth). El PO firmó el **enfoque** el 2026-09-16; cada parte requiere su sign-off de merge y su verificación en prod antes de empezar la siguiente.
