# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Agent build artifacts
.atl/

# Common ignores
node_modules/
.next/
.env*.local
.DS_Store

# Build artifacts
tsconfig.tsbuildinfo

# Debug / local scripts (never commit)
sync_env.js
get_keys.js
keys_raw.json
status.json
status.txt
status_final.json
status_utf8.json
supabase_status.json
edge_logs.txt
start_error.txt

# IDE
.idx/
.gstack/

# Python backend
backend/__pycache__/
backend/**/__pycache__/
backend/.venv/
backend/venv/
backend/.env
backend/*.egg-info/
backend/.pytest_cache/

# Frontend (moved to frontend/)
frontend/.next/
frontend/node_modules/
frontend/.env*.local

# AFIP/ARCA homologación — certificados reales (NUNCA commitear la clave privada)
backend/.afip-homo/
*.key
*.crt
*.pem
*.p12
*.pfx

# Comprobantes/recibos REALES exportados al root (contienen PII de usuarios — NUNCA commitear).
# Las versiones ANONIMIZADAS de muestra viven en docs/ejemplos/.
/comprobante-ejemplo.pdf
/recibo-ejemplo.pdf

# Entregables de consultoría (auditoría técnica + roadmap de mejoras) — NO versionar en el repo.
# Se generan bajo demanda; viven solo en la máquina local.
/AUDITORIA_ALIADATA.md
/AUDITORIA_ALIADATA.docx
/ROADMAP_MEJORAS_ALIADATA.md
/ROADMAP_MEJORAS_ALIADATA.docx
/PLAN_IMPLEMENTACION_ALIADATA.md
/PLAN_IMPLEMENTACION_ALIADATA.docx
/audit/
/v4/

# Playwright (QA, Fase A)
frontend/test-results/
frontend/playwright-report/
frontend/e2e/.auth/
