> **Gobernanza: MEDIA.** El change no escribe dinero, pero cierra dos huecos de autorización (rol de escritura y límite de plan) y **revoca** una función que hoy es el camino de importación en producción. Los grupos 3, 4 y 5 se implementan en pasos con checkpoint visible; nada de SQL se escribe antes de cerrar el grupo 1.
>
> **Sign-off previo obligatorio del PO sobre OQ-1** (aplicar el límite de plan al importar): rechaza importaciones de cuentas reales medidas. Sin su respuesta, el grupo 4.7 no se escribe y el resto del change sigue en pie.
>
> **Strict TDD activo.** Cada grupo con lógica arranca por su test en rojo. El grupo 2 es el SAFETY NET y no es opcional.

## 1. Checkpoints previos — medir el estado vivo antes de escribir una línea de SQL

- [x] 1.1 Confirmado `MAX(version) = 20261042000001` en la base local (291 migraciones registradas, incluida `20261041000001` — la nota del brief de que faltaba resultó desactualizada, medición real gana). `20261043000001` (asiento-contable-gastos, en curso en otro worktree) NO está aplicada. Migración nueva fijada en `20261044000001`, tal como preveía el design.
- [x] 1.2 Hasheado el cuerpo vivo local: `cc3d92a38432492b11fbca4bd47288fd` (12163 bytes, sin `\r`) — **difiere** del `d76c6b6b232ef8b6fd956def9597005d` del design (esperado: `productos-costo-nullable` ya mergeó) y también del `8963a42e…`/12087b anotado como "prod" en el brief del apply (76 bytes de diferencia, atribuible a divergencia de entorno entre la base local y la medición de prod citada, no a contenido: confirmado por inspección línea por línea que el cuerpo ya no tiene `COALESCE(v_cost,0)` en el INSERT, igual que D10 de costo-nullable exige). La migración parte de **este** cuerpo local, verificado byte a byte antes de escribir el `CREATE OR REPLACE`.
- [x] 1.3 `rpc_import_bank_statement` vivo hasheado (`75f0200bc61d05fad1d981caaec76747`) y `rpc_import_expenses` confirmado existente y registrado (`20261041000001`, aplicado y con `'expense_import'` ya en el `CHECK` vivo) — los dos moldes leídos y copiados (estructura de guards/subtransacción/idempotencia/dry_run), no reinventados.
- [x] 1.4 `CHECK` vivo leído con `pg_get_constraintdef`: 12 kinds (incluye `expense_import`). Barrido de `P0[0-9]{3}`: `P0427`/`P0429` confirmados en uso por `importador-gastos-transaccional` con la semántica esperada; `P0430` confirmado libre.
- [x] 1.5 Inventario por grep: únicos callers de `rpc_bulk_upsert_products` fuera de migraciones son `frontend/lib/import/importer.ts` (retirado en el grupo 8) y los gates SQL `test_bulk_upsert_products_categories.sql`, `test_productos_costo_nullable.sql`, `test_products_barcode_account_scope.sql`, `test_product_category_derived.sql` — los cuatro corren con el DSN `postgres:postgres` de `KPI_Validation.yml` (superusuario), no como `authenticated`: el `REVOKE` no los afecta, confirmado corriendo `test_bulk_upsert_products_categories.sql`/`test_productos_costo_nullable.sql` después de aplicar la migración (sin cambios de comportamiento). Ningún caller en `supabase/functions/` ni `backend/`.
- [x] 1.6 Confirmado por grep: `importProductsFromFile`/`resolveHierarchy`/`validateImportRows`/`parseImportFile` sólo se usan desde `product-import-dialog.tsx` y sus tests; `parseImportText` sólo en tests.
- [x] 1.7 Confirmado contra el cuerpo vivo: sin guard `is_account_writer`, con *lazy-create* de `'Casa Central'` intacto (líneas 51-61 del cuerpo dumpeado).
- [x] 1.8 Medido en la base local (no en prod — worktree sin acceso a prod, medición de referencia queda en el design con fecha 2026-09-10): estado consistente con lo que el design registra.

## 2. SAFETY NET — baseline antes de tocar nada

- [x] 2.1 `backend/tests/test_products.py`: 14/14 verde (baseline, antes de tocar backend).
- [x] 2.2 Frontend: los 4 archivos corrían verdes antes de tocar el código (`importer-branch-stock-c21.test.ts` 3/3, `product-import-dialog-categories.test.tsx` 6/6, los 3 `import-validator-*` + `product-catalog-sku-bulk.test.tsx` — 48 tests en conjunto, medidos post-cambio ya que no tocan el importador salvo los dos primeros, que SÍ se reescribieron a propósito, ver 2.4).
- [x] 2.3 Backend completo: 2192 passed, 4 skipped (0 failed) — medido DESPUÉS de todo el cambio backend, sin regresiones nuevas. Frontend completo: ver nota de verificación (grupo 10) — corrida en curso al momento de este commit de tasks.md; sin fallos nuevos atribuibles a este change en los archivos tocados. Conocidos pre-existentes: `AdminSegurosPage.test.tsx` (flaky bajo carga, no relacionado).
- [x] 2.4 Aserciones que cambian, documentadas por adelantado:
      - `__tests__/importer-branch-stock-c21.test.ts`: el mock deja de ser `supabase.rpc` y pasa a ser `pythonClient` (task 8.9) — el archivo quedó documentado con la justificación completa de por qué se partió en dos "fronteras" (prepareProductImport / useImportProducts) y qué caso se retira (el "6.3 reimport" del RPC directo, reemplazado por semántica de replay ya cubierta por el gate SQL de servidor).
      - `__tests__/components/product-import-dialog-categories.test.tsx`: el mock de `@/lib/import/importer` (`importProductsFromFile`) se retira (la función ya no existe) y se reemplaza por un mock de `@/hooks/data/use-products` (`useImportProducts`); las aserciones sobre categorías nuevas ahora esperan a que la simulación de servidor (mockeada) resuelva antes de leer el bloque anunciado, porque D7 hace que el anuncio final salga del veredicto del servidor.
- [x] 2.5 Gate SQL nuevo corrido dos veces consecutivas sobre la base local (12/12 bloques en verde ambas veces, cleanup se auto-verifica); la tanda completa de KPI_Validation no se corrió end-to-end en este apply (worktree sin runner de CI local para las ~90 gates completas) — se corrieron puntualmente los gates que podían verse afectados (ACL gate, ver 5.5) y el nuevo. Fallos pre-existentes conocidos no re-verificados uno por uno en esta sesión.

## 3. Migración — tabla de importaciones, `CHECK` y permisos

- [x] 3.1 RED confirmado: antes de escribir el archivo, `product_imports` no existía y el `CHECK` vivo no contenía `'product_import'` (medido en el checkpoint 1.4).
- [x] 3.2 Archivo creado: `supabase/migrations/20261044000001_importador_productos_fastapi.sql`, con encabezado documentado (integridad de función, ERRCODEs, idempotencia).
- [x] 3.3 `CREATE TABLE IF NOT EXISTS public.product_imports` + `UNIQUE(account_id, file_hash)` + RLS + policy `product_imports_select`, alcance calcado de `expense_imports_select`/`bank_statement_imports_select`.
- [x] 3.4 `GRANT SELECT` a `authenticated` únicamente; `REVOKE ALL` de `anon`; `REVOKE INSERT/UPDATE/DELETE/TRUNCATE` de `authenticated` (mismo gotcha de `ALTER DEFAULT PRIVILEGES` que `expense_imports` ya documentó).
- [x] 3.5 `CHECK` de `operation_idempotency.operation_kind` extendido con `'product_import'` sobre la lista viva de 12 (ahora 13) kinds, con `DROP CONSTRAINT IF EXISTS` + `ADD CONSTRAINT` + `COMMENT`.
- [x] 3.6 GREEN: migración aplicada con `psql` sobre la base local — bloque (1) del gate en verde.
- [x] 3.7 Idempotencia verificada: reaplicado el archivo completo dos veces, sin error y sin duplicar (`NOTICE: ... already exists, skipping` en los objetos ya creados).

## 4. `rpc_import_products` — la unidad de trabajo

- [x] 4.1 RED confirmado (la función no existía antes de este archivo).
- [x] 4.2 GREEN: guards `auth.uid()`/`current_account_ids()`/`is_account_writer` (`P0401`), validación de forma (`P0427`: array, `1..2500` — bajado de `1..5000`, ver 6.9 — `file_name`/`file_hash`), bloque de lote con `EXCEPTION WHEN SQLSTATE 'P0429'`. Las dos propiedades de PL/pgSQL documentadas en el cuerpo (subtransacción deshace el estado; variables locales sobreviven).
- [x] 4.3 Corazón: una sola invocación `v_res := public.rpc_bulk_upsert_products(p_rows, v_uid);`, comentada explicando por qué (tope de categorías de D1). Cero reglas del upsert copiadas — verificado en el refactor (4.14).
- [x] 4.4 TRIANGULADO — bloque (3) del gate: padre + variante (por `sku_parent` dentro del mismo lote) + simple con categoría nueva → 3 productos, jerarquía resuelta, `branch_stock` correcto, categoría creada Y anunciada en `new_categories`, `committed=true`. PASS.
- [x] 4.5 Bloque (4) del gate: 60 categorías nuevas (primera mitad de 30, bajo el tope de 50) → `P0400` propagado (rechazo de forma/cuota, no de fila — decisión documentada en el cuerpo de la migración) y **cero** categorías de la primera mitad creadas. PASS.
- [x] 4.6 Bloque (5) del gate: `price` no numérico (error estructural, `22P02`) → lote rechazado entero, error con su fila (2) y su `SQLSTATE`. PASS.
- [ ] 4.7 **NO IMPLEMENTADO — sin sign-off del PO sobre OQ-1.** El gate de plan (`P0430`) queda documentado como pendiente en design.md §OQ-1 y no tiene código en `rpc_import_products`. `P0430` se registró en `backend/core/errors.py` (403) como reservado, para que activarlo después no requiera un ERRCODE nuevo.
- [x] 4.8 Bloque (11) del gate: idempotencia por clave (replay), dedupe por `file_hash` con otra clave (replay), y lote rechazado que NO quema clave ni hash (reintento con archivo corregido se aplica). PASS.
- [x] 4.9 Bloque (7) del gate: `p_dry_run=true` sobre un lote válido → `committed=false`, `dry_run=true`, `inserted` informa lo que habría entrado, `new_categories` anuncia la categoría que se crearía, y CERO escrituras en las 5 tablas + idempotencia. PASS.
- [x] 4.10 Bloque (6) del gate: mismo archivo por `rpc_import_products` y por `rpc_bulk_upsert_products` directo → mismo `account_id`/`price`/`is_variant` y mismo conteo de `branch_stock`. PASS — confirma que la llamada anidada `SECURITY DEFINER` no cambia la resolución de tenencia.
- [x] 4.11 Bloque (8) del gate: `sku_parent` que referencia un producto de OTRA cuenta → error de fila, lote rechazado, nada escrito en ninguna de las dos cuentas. PASS.
- [x] 4.12 Bloque (10) del gate: 2501 filas → `P0427`; lote vacío → `P0427`; cero escrituras en ambos casos. PASS.
- [x] 4.13 ACLs en el mismo archivo (`REVOKE ALL FROM PUBLIC, anon` + `GRANT EXECUTE TO authenticated`) — bloque (12) del gate lo verifica.
- [x] 4.14 REFACTOR: releído el cuerpo completo — cero reglas del upsert duplicadas (ninguna resolución de SKU/padre/categoría/sucursal vive en `rpc_import_products`; todo delega en la única llamada a `rpc_bulk_upsert_products`).

## 5. La única modificación del upsert, y el `REVOKE`

- [x] 5.1 RED confirmado: antes de este archivo, el objeto de error de `rpc_bulk_upsert_products` no traía `row` (verificado leyendo el cuerpo vivo en 1.2).
- [x] 5.2 `CREATE OR REPLACE FUNCTION public.rpc_bulk_upsert_products(jsonb, uuid)` partiendo byte a byte del cuerpo vivo de 1.2, misma firma, único agregado: `'row', (v_row->>'row_no')::int`. Diff revisado línea por línea antes de aplicar (el resto del cuerpo es una copia exacta).
- [x] 5.3 GREEN + compatibilidad: bloque (2)/(3) del gate assertean `errors[0].row` correcto cuando `row_no` viene; ninguna fila del gate omite `row_no`, pero el campo es opcional en el `jsonb_build_object` (`(v_row->>'row_no')::int` es `NULL` si la clave no viene) — comportamiento previo conservado por construcción (`COALESCE`/cast sobre clave ausente = NULL, no error).
- [x] 5.4 `REVOKE EXECUTE ... FROM authenticated` aplicado al final del archivo, después de que `rpc_import_products` ya tiene su `GRANT`. Verificado: `service_role` conserva `EXECUTE` (bloque 12 del gate).
- [x] 5.5 Bloque (12) del gate: `rpc_bulk_upsert_products` sin `EXECUTE` para `authenticated`/`anon`, `rpc_import_products` con `EXECUTE` para `authenticated` y sin él para `anon`. Corrido también `test_function_acl_gate.sql` completo: los chequeos (1)-(4) pasan limpio con las funciones nuevas (rpc_import_products cae fuera del chequeo (4), que excluye `rpc_*` a propósito).
      **Corrección post-review**: la nota original de este ítem atribuía un fallo del chequeo **(5a)** a un hallazgo "pre-existente y ajeno" de `gastos-forma-pago`/`cobranzas-reverso`. Es una atribución equivocada — la revisión de código confirmó, reseteando la base con SOLO las migraciones de este worktree, que el gate PASA limpio (`ACL GATE OK`) en ese estado; el fallo sólo aparece cuando la base compartida recibe (vía `psql`, no vía `openspec`) la migración `20261043000001` de `asiento-contable-gastos` (worktree hermano, EN CURSO), cuyas versiones reescritas de `rpc_update_expense`/`rpc_delete_expense` leen `public.events` (`SELECT 1 FROM public.events WHERE event_type = 'ExpenseCreated'`) sin enumerarse en `v_cross_tenant_event_fns` — texto que no existe en ningún archivo de ESTE worktree (`grep -rn 'ExpenseCreated' supabase/migrations/*.sql supabase/tests/*.sql` da vacío acá). No es un defecto de `gastos-forma-pago` ni de `cobranzas-reverso`: es un defecto de `asiento-contable-gastos`, que debe enumerar sus dos RPCs reescritas en `v_cross_tenant_event_fns` en su propio PR — corregido en `CHANGES.md`. **Reconfirmado en vivo durante esta misma corrección**: la base compartida recibió, sin acción de este worktree, un segundo `supabase db reset` desde `asiento-contable-gastos` mientras se verificaban los gates finales — el chequeo (5a) volvió a fallar señalando exactamente `rpc_update_expense`/`rpc_delete_expense` (mismas dos firmas), reproduciendo el mecanismo con evidencia independiente antes de reaplicar la cadena propia (41/42/44) y devolver la base al estado de este worktree.
- [x] 5.6 Los 4 gates de 1.5 corren como `postgres` — no requieren ajuste; confirmado re-corriendo los 4 (`test_bulk_upsert_products_categories.sql`, `test_products_barcode_account_scope.sql`, `test_product_category_derived.sql`, `test_productos_costo_nullable.sql`) sin cambios de comportamiento tras el `REVOKE`. **Hallazgo real, fuera del inventario de 1.5**: `test_productos_costo_nullable.sql` (T13) sí *asserteaba* la ACL vieja de `rpc_bulk_upsert_products` (`expect_authenticated = true`) — no la invoca directo, pero fija su estado de ACL como parte de "las 5 funciones reescritas/verificadas" de ese change. Corregido en el mismo archivo (`false`, con comentario que explica el porqué) — es la contraparte lógica de checkpoint 1.5 (gates que *invocan* la función) que ese checkpoint no cubría (gates que *assertean su ACL*). Reconfirmado en verde tras el fix (14/14 bloques T1-T14).

## 6. Gate SQL y cableado en CI

- [x] 6.1 Consolidado en `supabase/tests/test_product_import_batch.sql` (12 bloques + setup + cleanup), estilo calcado de `test_expense_import_batch.sql`.
- [x] 6.2 Cleanup verificado: corrido el gate dos veces seguidas sin residuos (segunda corrida también 12/12 PASS, sin colisión de fixtures).
- [x] 6.3 Cableado en `.github/workflows/KPI_Validation.yml` (paso "Run importador-productos-fastapi gate", después del gate de `productos-costo-nullable` — `supabase start` ya aplica la migración nueva al levantar el stack, igual que el resto de los gates recientes, así que no hace falta un paso de apply separado). **Corrección post-review**: la migración también se sumó a la cadena de reaplicación manual "Verify G1/G4 migrations are idempotent on reapply" (después de `20261042000001`, siguiendo la convención vigente — se había omitido en el apply original), con su comentario de idempotencia; reaplicada localmente dos veces consecutivas sin error (3.7/6.2).
- [x] 6.4 Gate nuevo corrido en verde **4 veces** en total a lo largo del apply (12/12 bloques cada vez) — dos de ellas después de que el revisor de `asiento-contable-gastos` reseteara la base compartida desde su worktree (evento anticipado y documentado en el brief: reaplicadas a mano las migraciones `20261041`/`20261042`/`20261044` cada vez, sin tocar `supabase db reset` desde este worktree). La tanda completa de KPI_Validation.yml no se corrió end-to-end en este worktree (sin runner de CI local); se corrió puntualmente el ACL gate (ver 5.5) que es el único con superficie de intersección real.
- [x] 6.9 **EJECUTADA en la corrección post-review (2026-09-10) — nunca en el apply original.** Medición de `rpc_import_products` en la base local por tamaño de lote (dry + real, anchor propio, cleanup completo entre corridas):
      | filas | dry (ms) | real (ms) | total (ms) | ms/fila combinado |
      |---|---|---|---|---|
      | 200 | 45.7 | 35.4 | 81.1 | 0.406 |
      | 1.000 | 143.5 | 158.4 | 301.9 | 0.302 |
      | 2.000 | 283.4 | 282.1 | 565.6 | 0.283 |
      | 2.500 | 357.9/606.9† | 359.6/533.1† | 717.5/1140.0† | 0.287-0.456 |

      †dos corridas independientes, valores similares. Escalado observado: **~LINEAL** (0,28-0,46 ms/fila, sin degradación visible). Por separado, `rpc_bulk_upsert_products` invocada DIRECTO a **5.000 filas** (bypaseando el tope de `rpc_import_products`, que ya rechaza ese tamaño) tardó **752ms** (0,15 ms/fila) — consistente con la tendencia lineal, no con una degradación superlineal.

      **Esto CONTRADICE el hallazgo de la revisión de código** (33,1s dry + 34,2s real a 5.000 filas, con escalado que reportó como superlineal: 200→804ms, 1000→4189ms, 2000→9999ms, 5000→33101ms) — casi dos órdenes de magnitud más lento que lo medido acá para los mismos tamaños. No se pudo determinar la causa de la discrepancia entre ambas mediciones (bases distintas — la del revisor estaba pre-reconciliación con `asiento-contable-gastos`/`productos-costo-nullable`, la de acá post-reconciliación —, contención del Postgres compartido en el momento de esa corrida, o alguna otra condición del entorno no capturada). **Se mantiene igual el tope en 2.500** (D6/OQ-2 corregidas): no depende de esta medición en disputa para justificarse — el design original ya lo hacía por cobertura de uso real (1.393 mayor lote real, 2.372 catálogo más grande) — y bajarlo no tiene costo de usabilidad. Candidato dejado en `CHANGES.md`: remedir contra un entorno más parecido a prod (Render free tier + latencia real a Supabase) antes de reconsiderar subirlo.

## 7. Backend — schemas, repository, service, router (3 capas)

- [x] 7.1 RED confirmado (`backend/tests/test_product_import.py` no existía) → GREEN: 22 tests, todos verdes. Contrato del repository: una sola llamada `fetchrow`, orden posicional exacto de los 5 parámetros, `rows` serializado a JSON string.
- [x] 7.2 Schemas en `backend/schemas/products.py`: `ProductImportAttributeIn`, `ProductImportRowIn` (sólo `name` obligatorio; `price`/`cost`/`stock`/`min_stock`/etc. todos `| None = None`, sin default numérico), `ProductImportIn` (`rows` acotado a `PRODUCT_IMPORT_MAX_ROWS = 2500` — bajado de 5000 post-review, ver 6.9 —, `idempotency_key` fallback deprecado) y `ProductImportOut` (`committed`, `import_id`, `inserted`, `updated`, `errors[]`, `new_categories[]`, `replayed`, `dry_run`). Sin campo `plan` (D5/4.7 no implementado).
- [x] 7.3 Test dedicado del contrato null-preserving (`TestNullPreservingTransport`, 2 tests): `cost` ausente → `null` en el JSON enviado al repository (nunca `"0"`); `cost="0"` declarado → se preserva como `"0"`. PASS.
- [x] 7.4 `ProductRepository.import_batch(...)` — un `fetchrow`, espejo de `ExpenseRepository.import_batch`/`BankReconciliationRepository.import_statement`.
- [x] 7.5 Service `import_products`: sólo `require_role(auth, ["user","admin"])` + delegación. Cero reglas de dominio. `user_id` nunca leído del payload (verificado con `test_user_id_never_read_from_payload`).
- [x] 7.6 Router `POST /products/import` con `require_idempotency_key`, declarado antes de `/{product_id}` (mismo criterio que `bulk-category`).
- [x] 7.7 `P0427` ya estaba registrado (422, por `importador-gastos-transaccional`); `P0430` registrado nuevo (403, marcado explícitamente como RESERVADO sin sign-off de OQ-1 — comentario en el propio mapeo).
- [x] 7.8 Triangulado: lote aplicado (200), lote rechazado (200 con `errors[]`, no 4xx), `dry_run` forwardeado, replay por clave, replay por hash, sin rol de plataforma (403), sin `Idempotency-Key` (422), `P0427` mapeado a 422.
- [x] 7.9 `test_connection_stays_usable_after_a_rejected_batch`: dos requests seguidos sobre la MISMA conexión mockeada (rechazo → éxito) — PASS.
- [x] 7.10 `test_import_batch_at_the_row_cap_reaches_the_repository`: el lote en el tope (`PRODUCT_IMPORT_MAX_ROWS`, 2500 filas tras 6.9) llega al repository en una sola llamada, sin trocear.

## 8. Frontend — pipeline de importación

- [x] 8.1 Test del transporte (`__tests__/hooks/use-products-import.test.ts`, 6 tests): payload exacto snake_case, `row_no` por fila, `Idempotency-Key` en `extraHeaders` (nunca en el body), una sola llamada, null-preserving, mapeo camelCase de la respuesta, invalidación separada de la mutación. PASS.
- [x] 8.2 Tipos del lote en `frontend/lib/types.ts`: `ProductImportAttribute`, `ProductImportRow`, `ProductImportRowIssue`, `ProductImportNewCategory`, `ProductImportInput`, `ProductImportOut` → `ProductImportResult`. Sin `any`.
- [x] 8.3 `importer.ts` reescrito: se retiraron `createClient()`, `chunkArray` y el bucle de trozos. Nace `prepareProductImport()` (parsea + valida + resuelve, SIN llamar a ningún transporte) + `toApiRow()` (agrega `rowNo` desde `lineNumber`, preserva `null` en `price`/`cost`/`stock`). **Desvío del texto literal de la task**: la llamada HTTP (`pythonClient.post`) vive en `useImportProducts()` (`hooks/data/use-products.ts`), no en `importer.ts` — mismo patrón que `useImportExpenses`/`useImportStatement` (regla "reutilización antes que repetición": el hook es la capa canónica establecida para llamadas HTTP con invalidación de queries en este repo). El invariante que la task protege (una sola llamada, sin trocear, `row_no` presente) se cumple igual, verificado en 8.1.
- [x] 8.4 `IMPORT_BATCH_SIZE` retirado de `types.ts`, reemplazado por `PRODUCT_IMPORT_MAX_ROWS = 2500` (bajado de 5000 post-review, ver 6.9 — constante del lado cliente, rechazo temprano — la autoridad es el servidor). Confirmado por grep: cero consumidores del símbolo viejo.
- [x] 8.5 `resolver.ts` reescrito: retirados `createClient()` y las dos consultas a `products`. La función ya no es `async` ni recibe `userId` — una referencia explícita (`skuParent`/`nameParent`) viaja tal cual al servidor sin intentar resolverla en cliente; la agrupación secuencial y la política de huérfano sin referencia se conservan intactas.
- [x] 8.6 Hash con `hashFileSHA256` (`lib/bank-statement-parser.ts`) — sin segundo hasher, confirmado por import directo en el diálogo.
- [x] 8.7 Clave de idempotencia generada una vez por archivo (`handleFile`, `crypto.randomUUID()` al terminar de parsear) — la simulación y la confirmación del mismo archivo la COMPARTEN (mismo criterio que gastos: es el archivo, no el intento, lo que identifica el lote).
- [x] 8.8 `useImportProducts()` en `use-products.ts` + `useInvalidateProductImport()` — invalidación de productos + categorías + stock por sucursal, una sola vez, decidida por el diálogo tras `committed=true`.
- [x] 8.9 `__tests__/importer-branch-stock-c21.test.ts` reescrito con justificación completa en el encabezado del archivo — el mock pasa a ser `pythonClient` (vía `useImportProducts`) y `prepareProductImport` (sin transporte). 3/3 PASS.

## 9. Frontend — el diálogo de importación

- [x] 9.1 `__tests__/components/product-import-dialog-categories.test.tsx` extendido: mock de `useImportProducts` en lugar del retirado `importProductsFromFile`; las 6 aserciones existentes se conservan (template, columnas, categorías nuevas con conteo, sin bloque cuando no hay nuevas, tope excedido bloquea). 6/6 PASS.
- [x] 9.2 Paso 2 dispara la simulación (`dryRun: true`) automáticamente al entrar (una vez por archivo, `dryRunTriggeredRef`), con badge de carga (`role="status"`) y veredicto por fila combinando cliente + servidor (número de fila, ícono de estado, mensaje).
- [x] 9.3 El anuncio de categorías a crear lee del veredicto del servidor una vez que llega (`serverResult.newCategories`), con la conjetura del cliente como aviso temprano mientras la simulación está en curso. **Gate de plan (CTA de upgrade) NO implementado** — no hay veredicto de plan que mostrar (4.7 sin sign-off).
- [x] 9.4 Confirmación deshabilitada mientras `errorCount > 0` (cliente O servidor) — `confirmDisabled`. Texto "importar las que se pueda" retirado; el mensaje bajo la tabla dice explícitamente "el lote es todo o nada".
- [x] 9.5 Paso 3 lee `serverResult` (creados, actualizados, categorías creadas, aviso de repetición). `lineNumber: 0` retirado — los errores ahora usan `row` (puede ser `null` sólo si un caller viejo omitiera `row_no`, nunca en este flujo).
- [x] 9.6 Paso 1: nuevo bloque "Cómo se importa" con el tope (`PRODUCT_IMPORT_MAX_ROWS.toLocaleString("es-AR")`) y la política todo-o-nada. Template y esquema de columnas sin cambios.
- [x] 9.7 Estados de fila con tokens semánticos (`text-destructive`/`text-warning`/`text-success`/`text-muted-foreground`) — sin literales nuevos.
- [x] 9.8 Test de a11y (`__tests__/a11y/product-import-dialog.a11y.test.tsx`, 3 tests): estado de carga con `role="status"`/`aria-live`, fila de revisión navegable por texto (nombre/línea/motivo), botón deshabilitado con `aria-describedby` apuntando al motivo. PASS.

## 10. Verificación

- [x] 10.1 Backend: 2192 passed, 4 skipped, 0 failed (cov. 98%, ≥ umbral de CI del 87%). Sin regresiones contra el baseline de 2.1/2.3.
- [x] 10.2 Frontend: suite completa verde tras el fix de dos suites afectadas por un import transitivo nuevo (`__tests__/plan-gate-price-suggestion.test.tsx` y `__tests__/components/product-catalog-search-collapse.test.tsx` — `ProductImportDialog` pasó a importar `useImportProducts`/`hooks/data/use-products.ts`, que toca `python-client` al cargarse igual que `use-product-categories` ya lo hacía; mismo mock agregado, mismo patrón ya documentado en esos archivos). **Confirmado con la corrida final completa: 292/292 test files, 2588/2588 tests, 0 fallos.**
- [x] 10.3 `tsc --noEmit`: 9 errores fuera de `e2e/`, **idéntico al baseline** (los 9 pre-existentes listados en el contexto del apply) — cero errores nuevos en archivos tocados por este change.
- [x] 10.4 **NO ejecutado en el apply original** por la restricción de esa fase ("NO hagas vos `supabase db reset` en la fase Apply"); la migración se aplicó limpia sobre la base tal como estaba (`MAX(version)=20261042000001` → `20261044000001`) y se reaplicó 2 veces sin error ni duplicación (3.7/6.2). **Ejecutado en la corrección post-review** (con permiso explícito de esa fase): `npx supabase db reset` desde este worktree reconstruyó la base con la migration history real completa hasta `20261044000001` (sin `20261043000001`, exclusiva de `asiento-contable-gastos`) — confirma que la migración aplica limpia como parte de un reset ordinario, no sólo reaplicada a mano sobre una base ya parcheada.
- [x] 10.5 Gate nuevo corrido 2 veces consecutivas en verde (12/12 bloques, sin residuos). La tanda completa de ~90 gates de `KPI_Validation.yml` no se corrió end-to-end (sin runner de CI local en este worktree); se corrió puntualmente el único gate con superficie de intersección real (`test_function_acl_gate.sql`, ver 5.5) y los 2 gates de 1.5 con mayor riesgo de verse afectados por el `REVOKE`.
- [x] 10.6 **Ejecutado (2026-09-10), pasada visual real.** Stack local completo levantado desde `main` checkout apuntando a este worktree (`backend/.env` + `frontend/.env.local` propios del worktree, gitignored; `.env` sólo llevó las claves ANON/SERVICE_ROLE **locales** impresas por `npx supabase status`) — backend FastAPI (`uvicorn backend.main:app`, puerto 8000) + `next dev` (puerto 3001, corrido con el venv/`node_modules` del checkout `main` pero sirviendo el código de ESTE worktree) + Supabase local compartido (migración `20261044000001` ya aplicada, verificado antes de empezar). Sesión real vía GoTrue (`POST /auth/v1/token?grant_type=password` — nunca transcripción manual del JWT, que corrompía la firma ES256 en el primer intento) contra un usuario sintético (`visual-impfa@test.local`, cuenta con 0 productos). Recorridas las 4 combinaciones (1440×900 / 375×812 × claro/oscuro) sobre los pasos 1 y 2 del diálogo con un CSV real de 22 filas (2 padres + 8 variantes + 12 productos simples + 1 categoría nueva + 1 fila con `SKU Padre` inexistente): sin desborde horizontal en ninguna combinación (`document.documentElement.scrollWidth === clientWidth` y lo mismo para el diálogo, medido con Playwright, no sólo visual), footer del paso 2 wrappea correctamente a 375px (`↻ Cambiar archivo` en una fila, `Cancelar`/`Importar N filas` en la siguiente), tabla de revisión scrollea DENTRO del diálogo y no el diálogo entero (`[data-radix-scroll-area-viewport]` con `scrollHeight > clientHeight` mientras el `[role="dialog"]` mismo tiene `scrollHeight === clientHeight`, verificado a 1440 y 375). **Hallazgo real, corregido**: el párrafo de ayuda "Si omitís la columna Tipo…" (`product-import-dialog.tsx`, bloque "Columna Tipo") usaba `text-muted-foreground/60` a 11px — contraste real medido (compositing de alpha de texto y fondo con la fórmula WCAG, no el `rgba()` crudo) de **~2.30:1 en claro / ~3.32:1 en oscuro**, ambos bajo el mínimo AA de 4.5:1 para texto normal. Corregido a `text-muted-foreground` (mismo tono que el párrafo hermano de "Cómo se importa" que no tenía `/60`) → **~4.74:1 claro / ~6.98:1 oscuro**, con comentario inline explicando la medición. Sin otros hallazgos de layout. 8 capturas (4 combinaciones × pasos 1 y 2) + 3 capturas adicionales del flujo E2E en `scratchpad/impfa/visual/` (`{combo}_step1.png`, `{combo}_step2.png`, `desktop-light_step2_ok.png`, `desktop-light_step3_result.png`, `desktop-light_step3_replay.png`). Suites re-verificadas tras el fix: los 4 archivos de test del diálogo (14 tests) en verde, `tsc --noEmit` 9 errores idéntico al baseline (ninguno en archivos tocados).
- [x] 10.7 **Ejecutado (2026-09-10) junto con 10.6.** Medido con `psql` antes/después contra la cuenta de prueba (0 productos de base): el archivo con la fila de `SKU Padre` inexistente se corrió en dry-run 8 veces (una por combinación, dos rondas) desde la UI real — servidor devuelve el error de fila (`SKU Padre "PADRE-NOEXISTE-999" no encontrado para la variante "Remera Estampada Talle XXL"`, texto visible en la fila L22) y el botón de confirmación queda deshabilitado; conteo de `products` para la cuenta se mantuvo en **0** en las 8 corridas (nunca se pudo confirmar un lote con errores desde la UI, y el propio dry-run no escribe). Con el archivo corregido (mismo SKU Padre apuntando a `REM-001`, ya presente en el lote): confirmar desde la UI real produjo **22 productos importados correctamente** (paso 3 visible con el conteo real: "22 nuevos" + "1 categoría nueva") — `SELECT count(*)` pasó de 0 a 22, sin duplicados por nombre. Volver a subir el MISMO archivo (mismos bytes/hash, `Idempotency-Key` nueva por ser una nueva selección) mostró el aviso "Este archivo ya se había importado antes — confirmar no va a crear un lote nuevo" y el conteo final se mantuvo en 22 (dedupe por `file_hash` confirmado, no sólo por gate SQL). Verificado además, con una segunda cuenta sintética limpia, que **cambiar el archivo reinicia la simulación**: subir el archivo válido (22 filas, 0 errores, confirmar habilitado) y luego usar "Cambiar archivo" para subir el archivo con la fila mala vuelve a mostrar "1 error" y deshabilita confirmar — no queda el veredicto viejo pegado (regresión de la ronda 2 de revisión, confirmada cerrada en vivo).
- [x] 10.8 Cubierto por el gate SQL bloque (11): misma clave → replay sin duplicar; mismo `file_hash` con otra clave → replay; lote rechazado no quema clave ni hash, reintento con archivo corregido se aplica. Verificación equivalente por servidor, no por UI en vivo (mismo motivo que 10.6/10.7).
- [x] 10.9 Cubierto por el gate SQL bloque (12): `has_function_privilege('authenticated', 'rpc_bulk_upsert_products(...)', 'EXECUTE')` = `false` — un token de cualquier usuario autenticado, cuya conexión asume el rol `authenticated` (Paso 2 de `v31-tenancy-pool-rls`, `SET LOCAL ROLE authenticated`), recibiría `insufficient_privilege` de Postgres al intentar la llamada directa. Verificación a nivel de rol de base, equivalente a la llamada real vía PostgREST.
- [x] 10.10 Cubierto por el gate SQL bloque (9): miembro con `role='member'` (sin `is_account_writer`) → `P0401`, cero escrituras.
- [x] 10.11 **Verificación completa de la corrección post-review (2026-09-10), sobre `npx supabase db reset` desde este worktree** (10.4): `test_function_acl_gate.sql` → `ACL GATE OK` limpio (confirma 5.5: el chequeo (5a) NO es un hallazgo de este change); `test_product_import_batch.sql` → 12/12 PASS (incluidas las dos correcciones nuevas 11.1b/11.2b de `dry_run` en replay y el tope de 2.500 en el bloque 10); `test_bulk_upsert_products_categories.sql`, `test_product_category_derived.sql` y `test_productos_costo_nullable.sql` → PASS sin cambios. Backend: 2192 passed, 4 skipped, 0 failed (idéntico a 10.1). Frontend: **296/296 test files, 2596/2596 tests, 0 fallos** (292→294 archivos y 2588→2594 tests por los 2 archivos nuevos de ESTA corrección — `import-resolver-hierarchy.test.ts` y `product-import-dialog-server-verdict.test.tsx` —; 294→296 archivos y 2594→2596 tests por 2 archivos más que sumó la ronda de revisión siguiente y que esta línea había quedado sin nombrar: `product-import-dialog-file-swap.test.tsx` y `product-catalog-import-result-step.test.tsx` — corrección de revisión ronda 3, F4). `tsc --noEmit`: 9 errores, idéntico al baseline de 10.3, ninguno en archivos tocados. `openspec validate --changes --strict` → 3/3; `--specs --strict` → 99/99 (sin cambios respecto de 11.4).

## 11. Documentación y cierre del propose

- [x] 11.1 Entrada propia agregada en `CHANGES.md` (después de `productos-costo-nullable`), con lo verificado, el hallazgo lateral del ACL gate (5a) y los candidatos abiertos (OQ-1 sin sign-off, groups 10.6/10.7 pendientes de stack local, `min-w-[560px]`/footer que `importador-gastos-transaccional` ya había anotado como posible en este mismo archivo — confirmado corregido en la implementación real).
- [x] 11.2 Puntero de "próximo change" actualizado en `CLAUDE.md` (§"Candidatos para el próximo `/opsx:propose`") y `python scripts/ci/check_docs_sync.py --fix` corrido — `AGENTS.md` regenerado y verificado sincronizado.
- [x] 11.3 Respuestas del PO a las 9 OQs registradas en `design.md` (nota inline en §OQ-1) y en el resumen de `CHANGES.md`: OQ-1 **sin sign-off, no implementada** (única); OQ-2 a OQ-9 aplicadas por su recomendación (mismo criterio "sí a todo" del programa cero candidatos que originó el change).
- [x] 11.4 `npx openspec validate --changes --strict` → 3/3 (incluye `importador-productos-fastapi`, `productos-costo-nullable`, `asiento-contable-gastos`, los tres changes activos en este árbol de worktrees). `npx openspec validate --specs --strict` → 99/99 (el conteo sube a 100 recién al archivar, cuando nace la capability `product-import`).

## 12. Post-merge en producción (sólo lectura, salvo el humo del PO)

> **Todo este grupo queda `[ ]` — no ejecutable desde este apply.** Requiere que el PR de propose+apply esté mergeado y desplegado en producción (migración `20261044000001` corrida por el pipeline de CI/CD, DEC/regla del proyecto: "CI/CD: merge = build+deploy+migración automáticos"). Este apply corrió en un worktree local contra la base compartida de desarrollo, sin acceso a prod. Queda para la sesión de archive, siguiendo el mismo patrón que el resto de los changes recientes de este roadmap (`productos-costo-nullable`, `importador-gastos-transaccional`, etc.).

- [ ] 12.1 `MAX(version)` en prod = la migración de 1.1, y el conteo total de migraciones esperado.
- [ ] 12.2 ACLs vivas: `rpc_import_products` con `EXECUTE` para `authenticated` y sin él para `anon`; **`rpc_bulk_upsert_products` sin `EXECUTE` para `authenticated` ni `anon`**, con `service_role` intacto; `product_imports` sin `INSERT`/`UPDATE`/`DELETE` para ningún rol de aplicación.
- [ ] 12.3 Una sola definición viva de `rpc_import_products` (sin overload) y `rpc_bulk_upsert_products` **con la misma firma** que antes del change — la prueba de que la modificación fue quirúrgica y no un `DROP` + `CREATE` encubierto.
- [ ] 12.4 Auditoría del punto de partida: cero filas en `product_imports` antes del primer uso real.
- [ ] 12.5 Re-medir el conteo de cuentas que superan el `max_products` de su plan efectivo (1.8) y dejarlo registrado: es la población que el gate de OQ-1 va a empezar a rechazar.
- [ ] 12.6 **Humo real del PO**: importar un archivo propio de su catálogo, con al menos un padre con variantes y una categoría nueva. Verificar con él: que la revisión muestra el veredicto del servidor antes de confirmar, que un archivo con una fila mala no escribe nada, que el archivo corregido entra completo, y que volver a subir el mismo archivo no duplica nada.
